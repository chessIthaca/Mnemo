+++
title = "Fix backlog_status schema: id type integer → string (matches UUID IDs + String struct)"
created = "2026-08-25"
+++

Symptom: backlog_status tool calls fail with "invalid arguments: invalid type: integer `2`, expected a string" — the schema declares id as type "integer" but the struct field is String and actual IDs are UUID strings, so every call that follows the schema sends an integer that serde rejects. · regression test: status_tool_schema_declares_id_as_string

Full record for plan 02886137-77d1-4d1f-9562-6cd3b154c745 (see .coding/plans/02886137-77d1-4d1f-9562-6cd3b154c745.md for the plan file).

regression test: status_tool_schema_declares_id_as_string · path .coding/plans/02886137-77d1-4d1f-9562-6cd3b154c745.md · branch fix/read-files-path-shorthand @ 6b012d1 (unmerged — exists only on this branch)
