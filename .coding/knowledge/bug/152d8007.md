+++
title = "web_fetch: re-validate the SSRF blocklist on every redirect hop"
created = "2027-01-05"
+++

Symptom: web_fetch follows up to 5 HTTP redirects automatically (reqwest redirect::Policy::limited(5), src/tool/agent/web_fetch.rs:186) without re-running the SSRF internal-address blocklist on redirect targets — the gate runs exactly once, on the initial URL (lines 161-177). A public URL answering 302 → http://169.254.169.254/... (cloud metadata or any internal host) is fetched, bypassing SSRF protection; the inline comment documents this as a known residual. · regression test: redirect_to_internal_address_is_refused

Full record for plan 152d8007 (see .coding/plans/152d8007.md for the plan file).

regression test: redirect_to_internal_address_is_refused · path .coding/plans/152d8007.md · branch wt/agenticcoding @ 58849c7 (unmerged — exists only on this branch)
