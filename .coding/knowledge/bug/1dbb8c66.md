+++
title = "Finish gate: force a real code-graph re-index before \"regression test symbol not found\" errors"
created = "2027-01-07"
+++

Symptom: A bug_fixing plan's finish gate rejected the verify step with "regression test symbol 'checkFillContract' not found in the code graph (even after a refresh) — the verify step must record the actual test function name" even though the test function exists in the source (live case 2027-01-08) — the dispatched agent then "fixes" a non-problem (renames the test, records a wrong name) or stalls. Root: the gate's one incremental refresh (src/tool/workflow/plan.rs:1425, spawn_blocking(|| g.index(None))) is mtime/hash-based — src/codegraph/mod.rs:287-294 (the mtime fast-path skips the read entirely when stored mtime == disk mtime && has_content && stored_hash.is_some()) and :312 (needs_reindex only when the hash differs or there is no content row) — so when the DB's meta rows are fresh but the SYMBOL rows are stale/missing (partial write, schema drift, a prior best-effort skip at :341-343), the pass never re-parses that file, the retry misses, and the gate errors on a symbol that exists on disk. · regression test: finish_force_reindexes_when_meta_fresh_but_symbols_missing

Full record for plan 1dbb8c66 (see .coding/plans/1dbb8c66.md for the plan file).

regression test: finish_force_reindexes_when_meta_fresh_but_symbols_missing · path .coding/plans/1dbb8c66.md · branch wt/agenticcoding @ 12ad722 (unmerged — exists only on this branch)
