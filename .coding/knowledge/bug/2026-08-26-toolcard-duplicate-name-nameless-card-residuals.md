+++
title = "ToolCard duplicate-name + nameless-card residuals (plan 9a131eb7, wt/agenticcoder)"
created = "2026-08-26"
+++

BUG: read_files/file-tool ToolCard headers STILL show duplicate file names and sometimes no file name despite prior fixes 30820ec (full-path dedupe) and 5c37673 (single-file `path` shorthand link). Two residual mechanisms: (A) DUPLICATE NAME — buildPathChips (frontend/src/lib/toolCardPaths.ts) sets chip text = basename(path) AFTER path-level dedup, so distinct paths sharing a basename (src/provider/openai.rs vs src-tauri/src/openai.rs) render two chips with identical display text. Fix in progress (plan 9a131eb7, branch wt/agenticcoder): qualify colliding chips' TEXTS with parent-dir segments until unique; links/titles untouched; dedupePaths semantics unchanged (test at line ~286 still valid). (B) MISSING NAME — ToolInvocation.args streams via tool_call_arg_delta fragments (frontend/src/lib/types.ts ~line 260); an aborted/interrupted call permanently stores truncated JSON; argPaths returned [] whenever JSON.parse failed even though a recoverable "path": "…" literal sat inside. Fix: regex salvage of first "path"/"file" literal on parse failure (label-only tools excluded before salvage). Regression tests: frontend/src/lib/toolCardPaths.test.ts — describe("buildPathChips — unique display names", 5 cases) + salvage cases inside describe("argPaths").
