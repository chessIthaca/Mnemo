+++
title = "tool cards stuck running after mid-stream steer cut / bad-JSON retry — announced call gets no ToolResult (backlog 63cbc20f)"
created = "2026-08-30"
+++

BUG (backlog 63cbc20f, 2026-09-18): SYMPTOM — read_files tool card stuck "running" forever with the amber border after a mid-stream steer lands (the amber is ToolCard's running color border-yellow-600/40, Message.tsx:703 — the card never transitions out of running). ROOT CAUSE — (a) turn.rs mid-stream stop block (~1253) emits Finished and returns BEFORE acc.finalize() + the tool loop, so a call already announced via ToolCallStart (forwarded at 1085) never gets a ToolResult; the tool loop's stop paths (1499/1820) synthesize "interrupted: not run" but this earlier path doesn't. (b) bad-JSON branch (~1321) pushes Role::Tool errors into history but emits no ToolResult event. (c) Frontend reduceFinished sweeps nothing (reduceError sweeps vision cards only). FIX — backend: synthesize ToolResult::error("interrupted: not run (turn stopped)") per accumulated call before Finished (UI-only; NO Role::Tool push — partial assistant msg records tool_calls:vec![]); same for bad-JSON arms. Frontend: sweepRunningCards on finished/final-error/child_finished (tool calls result===null, memory+vision running) with "(interrupted)" note; late real tool_result overwrites by id. REGRESSION TESTS (RED confirmed pre-fix) — mid_stream_steer_emits_not_run_result_for_announced_tool_call + bad_json_tool_call_emits_tool_result_event (src/agent/tests.rs) + frontend sweep tests. Sibling: 300eddf git_read card.
