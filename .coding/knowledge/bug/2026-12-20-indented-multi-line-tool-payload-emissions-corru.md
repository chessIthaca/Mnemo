+++
title = "Indented multi-line tool-payload emissions corrupt at transport - use flush-left PS string-array inserts"
created = "2026-12-20"
status = "superseded"
+++

Symptom: During plan 5c94b497 step 1 (thought_signature feature), EVERY file_edit payload containing indented multi-line Rust source was emitted corrupted (~5 consecutive attempts): fragments replaced whole blocks ('## boundary' overwrote LlmEvent::Usage fields), doc comments '///' mangled to '##', truncation plus escape artifacts ('\ufeffSkip'). Damage recurred identically despite varied strategies (exact old_string match mode, line-range mode, unicode \u002f escape hardening). Meanwhile ~30 other calls that session — prose memory_write bodies, flat-left create_plan step strings using \n without leading indentation, all structured git/search/read args — emitted byte-perfect.\nRoot cause hypothesis: emission-layer processing that consumes/restructures leading-whitespace-indented lines inside tool-call string arguments (markdown-ish list/heading handling); not length-bound since longer prose survived intact.\nMitigation protocol adopted mid-session:\n1) Never hand-edit source via file_edit with indented multiline new_string/old_string while unstable.\n2) Perform insertions/replacements through shell PowerShell scripts where every Rust line is passed FLUSH-LEFT inside short single-quote string array elements and indentation is prepended programmatically by the script ('    ' joins) so emitted payload contains zero indented lines.\n3) Scripts assert each anchor exists exactly once before mutating; abort without write on mismatch.\n4) After any mutation-bearing call immediately verify via targeted read + git diff --stat bounded review before proceeding.\n5) Cap retries at three identical failures then surface to user rather than looping deeper.\nRecovery lever proven safe x3 this session: git restore --source=HEAD -- <file> returned pristine state each time because damage was always bounded to last-mutated hunk.\nFollow-up candidate backlog item exists already from user (714196da): editor should force fresh read after invalid-edit input instead of timeout-after-three-failures loop.\u0020
