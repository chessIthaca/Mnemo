+++
title = "DeepSeek 400 recurrence (id 1042) = stale pre-fix binary, not a fix hole"
created = "2026-12-24"
+++

Symptom: singleton DeepSeek 400 "The `reasoning_content` in the thinking mode must be passed back to the API" at 2026-09-04 03:59:11 EDT (provider-errors.jsonl id 1042, 18 s after commit ec0d794) — looked like a recurrence of the b23463c-fixed bug, fired by the plan-completion auto-continue. ADJUDICATION: NOT a hole in the fix — the request was built by a STALE PRE-FIX BINARY (a long-running app process keeps its launch-time code; the release exe was rebuilt 04:14, 15 min after the incident). Evidence: (1) request forensics (.coding/analysis/deepseek-1042-shape.txt) — 398 msgs, deepseek-v4-flash, effort=max; tail = [assistant(finish call, raw unkeyed), tool(finish result), system("# RECALLED MEMORIES"), system(footer)]; the age-0 assistant went out WITHOUT the reasoning_content key; (2) code-path proof: assistant_age counts ASSISTANT turns only (openai.rs:1360-1376) so the tail-owner is age 0 regardless of trailing system messages; the policy consult is a pure (kind, model) function (policy.rs:225-233) -> DeepSeek policy (reasoning_required + reasoning_content field) active for this body; the age-0 injection (openai.rs:1443-1451) and the unconditional synthetic path (:1529-1532) both key the turn — no post-b23463c path can emit an unkeyed age-0 assistant message; (3) regression test deepseek_tail_owner_keyed_when_system_follows_tool_results (exact incident shape incl. historical tolerance + no keys on system messages) PASSES at HEAD. Remedy: rebuild + relaunch the running app; if this 400 ever recurs on a post-fix build, re-open with the new request body. Sibling observation (NOT this bug, uninvestigated): claude-opus-5 400 burst ids 1022-1030 "messages.3.content.0.thinking: each thinking block must contain thinking" — separate Claude serialization issue.
