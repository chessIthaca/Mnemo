+++
title = "DeepSeek echo/loop hardening (R10 byte-period guard) — tail placement moved from fold-into-head to trailing user messages, FIXED (bbfd822)"
supersedes = "a0eab9ca"
created = "2027-01-07"
+++

MERGED into main at 3a2accd (3a2accde38b70ee404ba3cdd1e75f9f0492fb6a4) on 2027-01-11 (merge_to_main skill), branch wt/agenticcoding deleted (pre-merge tip 951ded0). FIXED (plan a0eab9ca, review PASS round 3): (a) DeepSeek-vendor requests fold the volatile tail into the leading system message and omit the CONTEXT_FOOTER (ProviderPolicy::fold_volatile_tail — the trailing system blocks that triggered the context echo are no longer sent); (b) the R10 repetition guard (detect_repetition, src/provider/stream.rs) detects loops of ANY byte period ≤ 200 over the last 600 tail bytes (was exactly-200 only — the 74-byte exit-note period could never fire). Regression tests: detect_repetition_fires_on_exit_note_loop_with_74_byte_period + deepseek_vendor_folds_volatile_tail_no_trailing_system_messages (both verified failing pre-fix). Full suite 2256 + 16 passed, 0 failed, zero warnings; merged tree verified with npm run build + src-tauri cargo build.

Amended 2027-01-11: PLACEMENT REVISED (plan 506b85e2): folding the volatile tail into the leading system message stopped the echo but broke the DeepSeek prefix cache — the PROGRESS line lived in messages[0], so every complete_step rewrote the cache prefix (measured 5.1-9.0% hit, 115-120K tokens re-billed per check-off; traces 2026-09-14 04:48:52-04:51:06 UTC). The tail + CONTEXT_FOOTER now ride at the END as USER-role messages for DeepSeek-vendor and Local/Ollama, selected by `ProviderPolicy::tail_as_user_messages` (was `fold_volatile_tail`) and decided once in `AgentLoop::tail_is_user_role`; the R10 byte-period guard and every other part of this hardening stand unchanged. The text-only suggestion steer is also user-role now (`AgentLoop::push_suggestion_message`, all three sites), closing the residual this record noted. Tests: `deepseek_head_stays_byte_stable_across_plan_progress_bumps`, `deepseek_vendor_tail_rides_as_user_messages_no_trailing_system_block`, `local_provider_tail_rides_as_user_messages_no_trailing_system_block`. Live DeepSeek acceptance (>=95% cached on check-off rows, no echo) is still pending a rebuilt app + real session; if the echo returns, revert the commit.
