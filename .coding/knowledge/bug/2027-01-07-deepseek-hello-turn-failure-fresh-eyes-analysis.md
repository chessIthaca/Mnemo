+++
title = "deepseek hello-turn failure — fresh-eyes analysis (2027-01-07)"
created = "2027-01-07"
+++

BUG: deepseek hello-turn failure — fresh-eyes analysis (2027-01-07, plan 77bf6692). Symptom: after switching to deepseek and saying hello, the turn was interrupted ('Turn interrupted — send a message to resume.'), the deepseek-v4-flash request shows cancelled, and the response was corrupted — content began by echoing the prompt-tail workflow-state footer including the literal '</context footer>' sentinel, then the reasoning looped about create_plan without ever emitting a tool call. Root cause (data-derived): degenerate model generation on the deepseek turn-starts (3 turn-starts per emit-diag.log:276-311); no app defense catches an intra-response reasoning loop that keeps streaming (stall detector provider/mod.rs:908-929 needs a byte gap; repetition guard turn.rs:704-741 is turn-level); user interrupt was the only stop. The interrupted turn's packaging smears roles (user resume text + model reasoning adjacent to the garbled fragment) in every later request. No app-side merge path exists (turn.rs:669-681/1279-1287/2945-2953/3102-3113 keep reasoning_content separate; agent.rs:235-256 pushes steers as user messages; context.rs:5-11 sliding window). Diagnostics gap: user cancels leave no surviving file record (provider-errors carries error/stall rows only; trace file mirrors a bounded ring, trace.rs:1359-1366 — failure-window records ids 1-3 rotated out within minutes). Gateway was flaky before the attempt: two glm-5.3 90-s zero-token stalls (provider-errors.jsonl:518-519). Open: which model generated the final loop (deepseek attempts vs the 2:49 completed glm request — token-count inference leans glm); whether the user re-sent hello per attempt. Fix direction: F1 intra-response loop defense + F2 prompt-tail echo flag + F3 cancel/trace persistence. Regression tests: to be designed with the fix plan.

Amended 2027-01-07: 2027-01-07 amendment: F1-F4 queued as backlog items — F1 intra-response reasoning-loop defense (backlog 1f56297a; stall detector provider/mod.rs:908-929 needs a byte gap, repetition guard turn.rs:704-741 is turn-level; add an intra-response loop detector in turn.rs/stream.rs); F2 prompt-tail echo flag (backlog 8cfe9e8e; compare response opening against the injected footer/sentinel); F3 persist user cancels + stop the trace ring erasing history (backlog 20bd12c5; trace.rs:1359-1366); F4 fix role smear in interrupted-turn context packaging (backlog cd08dd45; grace-window fold loop_impl.rs:566-574; isolate the serialization with a mid-stream-interrupt regression test). Clean repro protocol: traces.jsonl cleared 2027-01-07 (plan 06648a64); the user re-runs the deepseek hello repro WITHOUT interrupting (natural end = completion cap / stall timeout / model stop) to pin the open questions (which model generates the loop; the end state). The follow-up analysis MUST read .coding/logs/traces.jsonl FIRST — the ring rotates records out within ~8-11 requests.

Amended 2027-01-07: 2027-01-11 amendment: a clean-trace repro (plan b2172228, traces.jsonl emptied first) independently re-derived and CONFIRMED the model-degeneration root cause with hard evidence: the request was clean (HTTP 200, no loop text anywhere in the payload), and deepseek-v4-flash itself echoed the trailing volatile-tail RECALLED MEMORIES block, fabricated a "REMAINING THOUGHTS"/"exit note" section, then looped 366× on a 69-char unit in the content channel. This record's open questions are now answered: the loop was generated by deepseek-v4-flash itself (trace record model + SSE chunk model field), single "hello" send, no re-send. New finding the 2027-01-07 analysis could not know: the R10 intra-response guard added after that incident (src/provider/stream.rs:447) is structurally blind to loops whose byte period does not divide 200 — this repro's 71-byte period evaded it for 64.5 s. See the BUG record "deepseek exit-note loop evades R10 repetition guard (period ∤ 200)" for the fix direction and regression-test design.

Amended 2027-01-07: Correction (review 2026-09-11 L4): the 2027-01-11 repro's loop period is 74 bytes (69 chars; the em-dash arrives as the mojibake sequence U+00E2 U+20AC U+201D), not 71 as stated in the 2027-01-11 amendment above. The conclusion is unchanged: 74 does not divide 200, so the R10 guard's old exact-window suffix check was structurally blind to it.
