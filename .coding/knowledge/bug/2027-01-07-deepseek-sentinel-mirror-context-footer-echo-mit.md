+++
title = "DeepSeek sentinel-mirror (CONTEXT_FOOTER echo) — MITIGATED by fold_volatile_tail, MERGED into main (3a2accd)"
supersedes = "7bb9a608"
created = "2027-01-07"
+++

MERGED into main at 3a2accd (3a2accde38b70ee404ba3cdd1e75f9f0492fb6a4) on 2027-01-11 (merge_to_main skill), branch wt/agenticcoding deleted (pre-merge tip 951ded0). The sentinel-mirror echo trigger is ELIMINATED for DeepSeek-vendor requests: ProviderPolicy::fold_volatile_tail (plan a0eab9ca) folds the volatile tail into the leading system message and omits the CONTEXT_FOOTER entirely — DeepSeek-vendor requests end with the user/tool message, so there are no trailing system blocks to mirror. Historical symptom: DeepSeek (flash tier, thinking mode, via the proxy) opened its first response by mirroring the harness's context footer — a mangled "cache-stable sentinel" echo. The ThinkTagFilter ladder-step fix was PROVEN NOT TO WORK and reverted (that finding stands — do not retry it). Residual (accepted): a text-only suggestion steer still leaves a short system message as the last message on fold vendors (turn.rs Suggestion push site, documented there). If any echo recurs, the R10 any-period repetition guard caps the damage within ~600 tail bytes. Root-cause lineage: the 2027-01-11 exit-note loop repro (47553472) confirmed the echo class from a clean trace.

Amended 2027-01-11: SUPERSEDED MITIGATION (plan 506b85e2): the fold is gone. Folding the volatile tail into the leading system message stopped the mirroring but put the workflow PROGRESS line into messages[0], so every complete_step rewrote the cache prefix: DeepSeek hit 5.1-9.0% with 115-120K tokens re-billed per check-off (traces 2026-09-14 04:48:52-04:51:06 UTC, .coding/analysis/cache-hit-6-aggregates.txt section B = HEAD k=0). New placement: the volatile tail + CONTEXT_FOOTER ride as trailing USER messages — the echo trigger was the SYSTEM role in the tail position, not trailing content as such — so the request still ends the way a normal client turn does, while messages[0] stays the pure stable head. Selected by ProviderPolicy::tail_as_user_messages (renamed from fold_volatile_tail; also covers Local/Ollama, which allows a system message only in first position) and decided in one place, AgentLoop::tail_is_user_role (src/agent/turn.rs). The text-only suggestion-steer residual from the 2026-09-11 review is closed the same way via AgentLoop::push_suggestion_message (3 sites: turn.rs handle_pending_swap, turn.rs re-injection, runtime/agent.rs compact_context). Guarded by agent::tests::deepseek_head_stays_byte_stable_across_plan_progress_bumps + deepseek_vendor_tail_rides_as_user_messages_no_trailing_system_block + local_provider_tail_rides_as_user_messages_no_trailing_system_block. PENDING LIVE CONFIRMATION: a real DeepSeek session must show >=95% cached on check-off rows and no echo/exit-note loop; if the echo returns, revert this commit (the flag alone now only picks user vs system role).
