+++
title = "descendant tracker lagged the finish notification — complete_step refused a full turn after children ended (2c406d72)"
created = "2027-01-07"
+++

Symptom: complete_step stayed refused ("cannot change workflow state while spawned subagents are still running") for a full turn or more after every spawned child had verifiably ended (finish notification received, report on disk) — live-observed twice in the 2026-12-30 full-review session (plan 72329f2c). Root cause: in the event forwarder (src-tauri/src/ipc/events.rs) the notification match ran BEFORE the running-state match for the same Finished/final-Error event — notify_parent_on_completion sent the completion Suggestion (waking the parent immediately) and only afterwards did the forwarder emit ChildFinished (webview) and clear the running flag; under a saturated frontend that window stretched across a whole parent turn, so the woken parent hit the descendant gate (has_running_descendants) on a still-marked child. Fix: notify_parent_on_completion clears set_running(agent_id, false) FIRST (events.rs:800) — the tracker and the notification agree by construction; the forwarder's later running-state match clears again (idempotent; events are processed one at a time, so no interleaving). Regression tests: child_finish_notification_leaves_no_running_descendant, child_final_error_notification_leaves_no_running_descendant, tracker_agrees_at_notification_receipt (src-tauri/src/ipc/events.rs mod tests; A/B-verified — all three fail with the fix line removed). Plan c8c49338; review .coding/reviews/2027-01-07-descendant-tracker-finish-notification-order-review.md (0 high, 1 low — this record is the LOW-1 fix).
