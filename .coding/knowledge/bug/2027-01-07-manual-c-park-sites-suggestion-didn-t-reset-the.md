+++
title = "manual-'c' park sites — Suggestion didn't reset the streak; interrupted turns looked like hangs (5c33e945)"
created = "2027-01-07"
+++

BUG (backlog 5c33e945, live 2027-01-07 twice + once mid-audit): (1) a turn parked in the closing sequence after the round-2 reviewer finished (no descendants running) — root cause: the auto_continue_streak reset existed ONLY in the Prompt arm (src/runtime/agent.rs), so the between-turn Suggestion (the reviewer-finish resume, the closing sequence's ONLY resume mechanism) drove one turn on an exhausted budget and the None-arm gate failed on `12 < 12` → park until manual "c". (2) A mid-Executing stop during a test-matrix shell batch ("[tool error] interrupted during execution") — the user's own Interrupt (the only production sender is the UI stop command, src-tauri/src/ipc/agent.rs) parks by design, but the UI could not distinguish it from a hang. (3) A premature turn end mid-Planning exploration parks (workflow_expects_progress covers Executing|Reviewing only) — documented gap, queued as backlog a6a7727a (covering Planning would self-answer text question-waits).

FIX (plan 494f9d12): the Suggestion arm resets the streak (external progress, not guidance); new AgentEvent::Parked {reason: ParkReason, workflow_state, descendants_running, auto_continue_streak} emitted at every main-agent park site (Interrupt arm → Interrupted; None-arm gate failures → BudgetExhausted | WaitingForDescendants | NoWorkExpected; subagent routine turn-end parks excluded — a user Stop on a subagent still emits) — fed to the watchdog ring by the events.rs forwarder and surfaced as the InputBar parked banner for interrupted/budget_exhausted so an interrupted turn never looks like a hang. Regression test: suggestion_resets_auto_continue_budget_after_exhaustion (src/runtime/agent.rs — red pre-fix: the Suggestion turn parks on `12 < 12`; green post-fix: cap+2 calls). Emission tests: interrupted_turn_emits_parked_event, budget_exhaustion_emits_parked_event, descendant_park_emits_waiting_for_descendants.
