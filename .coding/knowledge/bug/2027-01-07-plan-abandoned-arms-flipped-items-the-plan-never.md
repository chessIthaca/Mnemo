+++
title = "plan_abandoned arms flipped items the plan never owned (bba2c82d)"
created = "2027-01-07"
+++

BUG (backlog bba2c82d, plan 6c6f985d, found as review LOW-1 2027-01-08 in the e33a07fd closing review): the plan_abandoned arms transitioned the pointer-referenced backlog item to Failed with NO status/linkage guard — the same steer-pivot damage class as 5bb1e4cd. Symptom: a stale in-memory dispatch pointer referencing a re-queued (Pending) item while an UNRELATED plan ran; that plan was abandoned → the re-queued item flipped to Failed and its re-queue note was wiped (Pending → Failed is a legal row, src/backlog.rs:308-313). Root cause: SIX unguarded arms in src-tauri/src/ipc/run_all.rs (on_spawned_turn_resolved success/failure, on_main_turn_resolved run-all success/failure + the single-dispatch rows) all called store.transition(item, Failed) on plan_abandoned with no status/linkage check; and the linkage evidence was unavailable live (after the abandonment pop, main_agent_top_plan_id no longer names the abandoned plan). Fix: (1) the evidence chain — the forwarder (events.rs) tracks prev_top_plan_id per agent (updated on every WorkflowStateChanged) and passes the PRE-event top to note_plan_abandoned at the abandonment transition → the TurnResolveLatch stores it (abandoned_plan_ids) → the resolution functions receive abandoned_plan_id; (2) the pure guard plan_linkage_allows_failed (mirror of plan_linkage_allows_done): status != InFlight → false (a re-queued Pending item stays Pending, note preserved); (Some, Some) → equality; (None, Some) → false; blind (no captured id) → true; (3) all six arms read the item's LIVE status + plan_id, compute may_flip, and either flip or block (no transition, no annotate, eprintln diagnostic, remove_worktree = true — the R3-L2 lane-redispatch blocker class). Regression tests: failed_transitions_are_guarded_on_status_and_plan_linkage + abandonment_evidence_captures_the_abandoned_plan_id (source-contract pins, written red first) + plan_linkage_allows_failed_matrix + abandoned_plan_id_round_trips_and_clears_per_turn. Review round 1: .coding/reviews/2026-09-08-bba2c82d-failed-transition-guard-review.md (0 high, 4 low — all fixed).
