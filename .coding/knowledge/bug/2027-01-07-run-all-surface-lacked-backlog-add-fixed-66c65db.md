+++
title = "run-all surface lacked backlog_add — FIXED (66c65db9, plan 274af962)"
supersedes = "2027-01-07-run-all-surface-lacked-backlog-add-the-skill-all"
created = "2027-01-07"
+++

FIXED (2027-01-07, plan 274af962, commit "fix: expose backlog_add inside skill allow-lists (66c65db9)" on wt/agenticcoding; reviews round-1 (0 high, 2 low → both fixed) + round-2 verification PASS). Symptom: during the 2026-12-30 unattended run-all batch (backlog 569b5922, plan 593f4a4e), the user asked mid-run to add a backlog item; the run-all session's tool surface had NO backlog_add (backlog_list and backlog_status were present) — the agent could not comply and had to stash the drafted item in memory for a later interactive session to queue. Root cause: the run-all session rides the MAIN agent's normal state-filtered surface (run_all_dispatch_next at src-tauri/src/ipc/run_all.rs:1289 sends a plain AgentCommand::Prompt — no run-all tool construction exists); all base states already exposed backlog_add (the 2026-12-30 reversal, commit 61cc0ab, itself triggered by this incident); the remaining hole — the exact reproduction of the reported symptom — was the Skill allow-list branch (src/tool/mod.rs:505): backlog_status + backlog_list rode the always-available set but backlog_add did not, reachable from run-all whenever an item invokes a skill mid-run (e.g. merge_to_main). Fix: backlog_add added to the Skill allow-list always-available set (the item's option 1 — the filter is state-derived with no run-all special-casing; the loop-prevention concern is already accepted for every base state); the widening applies uniformly to interactive and run-all skill surfaces; the interactive BASE surface is unchanged; the reviewer ROLE filter stays read-only. Regression tests: backlog_add_visible_in_all_states (flipped Skill assertion — backlog_add visible under Skill(vec![]), confirmed failing pre-fix) + run_all_dispatch_rides_the_main_agent_surface (source-contract: the dispatch is a plain Prompt with no ToolFilter/tool_allowlist/set_reviewer_allowlist/.schemas( manipulation). Both suites green (root 2031/0, src-tauri 233/0).
