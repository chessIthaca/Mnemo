+++
title = "DeepSeek reasoning_content 400 — 3rd recurrence: cross-vendor re-entry left the open tool-call chain unkeyed (FIXED, plan c6cb69f7)"
created = "2027-01-11"
+++

BUG: DeepSeek reasoning_content 400, THIRD recurrence (2026-09-12, plan c6cb69f7) — FIXED. Symptom: non-retryable HTTP 400 "The `reasoning_content` in the thinking mode must be passed back to the API" from api.deepseek.com (deepseek-v4-flash, reasoning_effort=max) killed the agent session twice at 17:31:17 / 17:32:57 EDT — provider-errors.jsonl ids 104/112, both bodies mirrored in .coding/logs/traces.jsonl. Root cause: the December contract (SPEC e010881d / probes A/D/G2/T/T2) only guaranteed the age-0 tail-owner, and in BOTH failures the tail-owner WAS keyed (skill_end turn, 1295/742 chars) and correctly paired — so the 400 came from elsewhere. Discriminator: both were the FIRST deepseek request after a MID-TURN CROSS-VENDOR RE-ENTRY — the merge_to_main skill runs on glm-5.3-flash (Ollama Cloud, ~/.mnemo/config.toml [models.skill.merge_to_main]); Rule 5 strip_cross_vendor_reasoning (src/provider/mod.rs) had stripped reasoning_content from every cross-vendor assistant turn; skill_end then re-resolved the provider back to the deepseek pin (loop_impl resolve_turn_provider) with a stripped open tool-call chain. Census of the failing bodies: 72/78 and 79/85 assistant turns keyless — every tool-call turn except the tail-owner; the only other keyed turns were text-only DeepSeek summary turns (rc native in raw). Fix (src/provider/openai/request.rs): one shared needs_reasoning_readd predicate now re-adds the key on the age-0 turn AND on EVERY assistant turn carrying tool_calls (value "" at age >= 1, preserving the retention strip's token savings; also fires when the strip is about to remove the key, so the hole cannot reopen under pressure); fingerprint_prefix hashes a re-add MODE (0/1/2) instead of a bool so a serialized-prefix splice can never keep an age-0 serialization the fresh path would not build. Regression test: deepseek_keys_every_tool_call_turn_after_cross_vendor_reentry (src/provider/openai/tests.rs — verified failing before the fix); deepseek_tail_owner_keyed_when_system_follows_tool_results and prefix_cache_rebuilds_when_the_readd_ages_out updated to the corrected contract. Residual: the exact API-side mechanism is inferential (two readings — "every tool-call turn needs the key" and "an undeclared tail-owner tool call makes the validator fall back to the previous keyless turn"; skill_end is absent from the post-state tools array while skill_start is present) — the fix is a strict superset curing both; live confirmation = a thinking-mode turn whose tail-owner issued tool calls after relaunching. Related: plans c9b5cbe4 + 1e64d71d (steps 2-4 now superseded), memories 429a3d7b, 8401fb46.
