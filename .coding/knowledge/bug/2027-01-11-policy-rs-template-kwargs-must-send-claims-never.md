+++
title = "policy.rs template_kwargs — must-send claims, never sent (BUG 10)"
created = "2027-01-11"
+++

BUG (2027-01-09 provider-comms review finding 4, LOW / executive summary BUG 10; fixed 2027-01-11, plan d4b951be, backlog be85a2a2): src/provider/policy.rs registered template_kwargs — GLM 4.7+ {"clear_thinking": false}, Qwen 3.5/3.6 + Kimi K2.5/K2.6 {"preserve_thinking": true} — whose doc comments asserted vendor must-send requirements, but NO request builder ever read the field: the params never left the process. Neither name appears in current public vendor docs (Zhipu's documented params: do_sample, temperature, top_p, max_tokens, stream, thinking, reasoning_effort). Root cause: the registry predates the 2027-01-09 rollback to e0ac5dd (think_tags/vendor-policy layer removed); the wiring was never present at HEAD. Fix: DELETED the field, all seven registry entries, the must-send doc claims, and the module-table extras; the three kwargs tests became vendor-detection tests (qwen→Alibaba, kimi→Moonshot, glm→Zhipu — vendor coverage preserved). The wire path was rejected: the vendor-policy config direction was rolled back by user decision (2027-01-09, main reset to e0ac5dd) and is TBD; wiring unverifiable param names risks 400s and cannot be live-verified unattended. include_params (the sibling field) is NOT dead — the Responses builder hardcodes its include value. Regression test: policy_source_carries_no_never_sent_template_kwargs (src/provider/policy.rs) — scans the module's non-test source (split_once "mod tests") and fails if template_kwargs/clear_thinking/preserve_thinking reappear.
