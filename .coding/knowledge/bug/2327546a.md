+++
title = "Fix v0.1.1 release build failures (windows tauri build + macOS cargo test)"
created = "2027-01-11"
+++

Symptom: The v0.1.1 tag build run (35514715816) failed on both platform legs and the release was never published: windows job step "Tauri build (unsigned installers)" (`npx tauri build`) exited 1 in ~2 seconds; macos job step "Rust tests (workspace, macOS compile validation)" (`cargo test --workspace`) exited 101 after ~3 minutes; the release job (needs both) was skipped. Both steps had NEVER executed before — the workflow never loaded until the L52 + secrets-if fixes (56132d3, b5d8ef8) — these are latent pre-existing breakages the fixes exposed, not regressions (nothing in the landed changes affects the Rust compile or the bundler). · regression test: watchdog_windows_only_items_stay_cfg_gated

Full record for plan 2327546a (see .coding/plans/2327546a.md for the plan file).

regression test: watchdog_windows_only_items_stay_cfg_gated · path .coding/plans/2327546a.md
