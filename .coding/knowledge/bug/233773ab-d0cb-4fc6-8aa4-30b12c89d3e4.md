+++
title = "git_read stuck-running card (memory entry steals tool_result) + info chips — FIXED (300eddf)"
created = "2026-08-25"
+++

BUG: git_read card stuck "running" (memory entry steals tool_result) + missing info chips — FIXED & VERIFIED. Symptom (backlog 12d7ecb8, 2026-08-25): git_read tool card stayed "running" forever when its result arrived while a memory_search entry was running; git_read showed no info chip; memory_search showed no query. Root cause: reduceToolResult's memory-finalization loop matched ANY running memory entry on ANY tool_result (no tool_call_id correlation) and skipped the tool-card loop. Fix (plan 233773ab, commit 300eddf, 2026-08-25): memory entries carry id/index/args (stamped at tool_call_start; arg deltas accumulate by index); finalization gated on entry.id === event.tool_call_id (agentEventReducer.ts ~:555); UX chips — git_read op chip via argLabel (Message.tsx ~:518), memory_search query chip + "N matched" via memorySearchLabel (Message.tsx ~:124, toolCardPaths.ts). Regression test: useAgentStore.test.ts "regression: a foreign tool's result must not be stolen by a running memory entry (git_read stuck \"running\")". Two reviews passed (.coding/reviews/2026-08-25-gitread-stuck-card-review*.md). 2026-08-25+ re-verified in current source (fix intact after later refactors incl. 17faa9e; tree clean); backlog 12d7ecb8 was never closed → stale auto-dispatch re-sent it with the ORIGINAL pre-fix screenshot; closed as done. Knowledge: .coding/knowledge/bug/2026-08-25-tool-result-stolen-by-memory-entry-git-read-card.md + spec/2026-08-25-id-correlated-tool-result-routing-git-read-memor.md.
