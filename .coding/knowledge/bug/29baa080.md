+++
title = "git branch list: allow the read-only --show-current flag (backlog 41cd5ad0)"
created = "2027-01-11"
+++

Symptom: `git branch --show-current` — a purely read-only flag that prints the current branch's name — is refused by the git tool's branch-list allowlist: "flag '--show-current' is not allowed for `git branch list` — only read-only listing flags are accepted: -v/-vv/--verbose, -a/--all, -r/--remotes, --list, --format=, --sort=, --color=/--no-color, --merged/--no-merged/--contains/--no-contains, -i/--ignore-case". Reproduce: git tool with subcommand=branch, action=list, args=["--show-current"] → refused (live 2026-09-19, plan 995436c2's closing sequence — the agent fell back to `git branch -v` and parsed the `*` marker just to learn the current branch). · regression test: branch_list_allowlist_accepts_show_current

Full record for plan 29baa080 (see .coding/plans/29baa080.md for the plan file).

regression test: branch_list_allowlist_accepts_show_current · path .coding/plans/29baa080.md · branch wt/mnemo @ 672da17 (unmerged — exists only on this branch)
