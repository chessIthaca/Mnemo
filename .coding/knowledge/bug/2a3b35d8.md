+++
title = "Fix slash-command mid-run asymmetry — commands execute in every agent state"
created = "2026-12-30"
+++

Symptom: With the slash menu Esc-dismissed (or any menu-closed state) and the agent running, Enter/Send on an argument-less command (/compact, /new, /clear) is sent as a steer suggestion instead of executing — while the menu-open path runs it directly (completeSlashCommand invokes handleSlashCommand, bypassing steer interception by design). Site: frontend/src/components/layout/InputBar.tsx handleSend steer branch (~:275-284). · regression test: slashCheckPrecedesSteerBranch

Full record for plan 2a3b35d8 (see .coding/plans/2a3b35d8.md for the plan file).

regression test: slashCheckPrecedesSteerBranch · path .coding/plans/2a3b35d8.md · branch wt/agenticcoding @ f556a3d (unmerged — exists only on this branch)
