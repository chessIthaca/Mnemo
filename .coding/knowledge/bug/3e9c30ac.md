+++
title = "Repetition guard: per-interaction error accounting — same-batch calls don't abort, repair attempts don't count as repetition"
created = "2027-01-11"
+++

Symptom: Turn aborted with "repetition guard: 3 identical consecutive responses — breaking the tool loop" (src/agent/turn.rs:777) while the model was re-emitting identical tool calls after receiving error feedback — repair attempts were counted as repetition. Root cause: tool_error_count (src/agent/turn.rs:1464-1474) is per-CALL and reset by ANY success within a batch, so a mixed [fail, success] batch leaves the counter at 0 and the repetition ring (clear keyed on tool_error_count > 0 at :758) counts error-following responses as repetition; separately, a single batch of 3 identical failing calls increments the counter 0→3 in ONE interaction, so the post-batch MAX_RETRIES check (:876) aborts the turn without the model ever getting a repair chance ("if all three were called at the same time it shouldn't break"). · regression test: single_batch_of_identical_failing_calls_does_not_abort

Full record for plan 3e9c30ac (see .coding/plans/3e9c30ac.md for the plan file).

regression test: single_batch_of_identical_failing_calls_does_not_abort · path .coding/plans/3e9c30ac.md · branch wt/macos-fix @ d488267 (unmerged — exists only on this branch)

Amended 2027-01-11: Landed outcome (2027-01-24, plan b9b12840, commit 29ee0b0 on wt/macos-fix): a follow-up incident (12:41, read_files re-reads of AsmModeller.js/ShapeTools.js) showed the guard's only remaining reachable trip path was identical responses following SUCCESSFUL batches — error-following responses already reset the ring — which aborted legitimate re-reads. Per user decision the turn-level repetition guard was REMOVED outright (sig computation, ring, trip block, recent_response_sigs field); cross-iteration loop defense is owned by MAX_RETRIES (same error across three error interactions — unchanged by this removal), within-response loops by the in-stream R10 guard (detect_repetition, src/provider/stream.rs). Regression test: identical_responses_after_successful_batches_do_not_abort (src/agent/tests.rs) — failed pre-fix, passes post-fix; full suite 2515 passed, warning-free. docs/FEATURES.md:54 synced.
