+++
title = "Fix switch-restart window: same place, same size, on top"
created = "2027-01-11"
status = "superseded"
+++

Symptom: Opening a different project restarts Mnemo, but the new window does not open on top (foreground) like the window it closed, and does not reliably reopen at the same place/sizing — a maximized window restarts un-maximized at its last normal bounds, and a move/resize within the 400 ms save debounce is lost to the hard process exit. · regression test: switch_restart_focuses_the_restarted_window

Full record for plan 3f87686a (see .coding/plans/3f87686a.md for the plan file).

regression test: switch_restart_focuses_the_restarted_window · path .coding/plans/3f87686a.md · branch wt/mnemo @ de1efff (unmerged — exists only on this branch)
