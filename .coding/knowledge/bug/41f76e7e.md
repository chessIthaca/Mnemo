+++
title = "Reasoning bar counters render per event — drop the count-up interpolation"
created = "2027-01-07"
+++

Symptom: The inflight bar's token counters (↑ prompt / ↓ completion / 🧠 reasoning / ctx used) animate toward their targets via a ~400ms rAF count-up interpolation, which is distracting during streaming — the user expects the counters to update per event as streaming data arrives (like the trace graph), not count up (user report 2027-01-07, backlog item d9b560a0). · regression test: countersRenderPerEventNoInterpolation

Full record for plan 41f76e7e (see .coding/plans/41f76e7e.md for the plan file).

regression test: countersRenderPerEventNoInterpolation · path .coding/plans/41f76e7e.md · branch wt/agenticcoding @ fac67cf (unmerged — exists only on this branch)
