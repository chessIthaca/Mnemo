+++
title = "Raise the tools-panel ceiling to 75% and lower the chat floor to 360px (manual resize past 50%)"
created = "2027-01-11"
+++

Symptom: The tools (right) panel cannot be resized past 50% of the window width by hand — dragging the divider stops dead at half the window, which the user reports is "too aggressive, I want to resize bigger if I do so by hand." Symptom is reproducible on any viewport: drag the handle left and the panel halts at exactly 50% of innerWidth. Root cause is the flat PANEL_MAX_FRAC = 0.5 ceiling in clampPanelFraction (frontend/src/hooks/appearance.ts:249/263), which is tighter than the chat-column floor (w - 480)/w for every window wider than ~960px. · regression test: clampPanelFraction

Full record for plan 4347c8a3 (see .coding/plans/4347c8a3.md for the plan file).

regression test: clampPanelFraction · path .coding/plans/4347c8a3.md · branch wt/mnemo @ 9b65477 (unmerged — exists only on this branch)

Amended 2027-01-11: Branch-pointer correction (reviewer note, 2026-09-18 circuit-breaker review): this record's "wt/mnemo @ 9b65477 (unmerged — exists only on this branch)" marker is stale — the retune landed as commit bb9d310 ("Retune right-panel band: 75% ceiling, 360px chat floor (plan 4347c8a3)"), which is in main's history (wt/mnemo was later recreated from main for plan a9875b73); drop the unmerged reading. The regression-test reference "clampPanelFraction" names the function, not the test: the guarding tests are the "clampPanelFraction — the panel band after the 2027-01-16 retune" describe block in frontend/src/components/layout/RightPanel.width.test.tsx plus the clampPanelFraction block in frontend/src/hooks/appearance.test.ts.
