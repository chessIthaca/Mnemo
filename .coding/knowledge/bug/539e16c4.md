+++
title = "Fix R14 cache-break: hysteresis tool-result compaction (no-op below a 20-result high-water mark)"
created = "2027-01-04"
+++

Symptom: Cache hit ~46% instead of ~95% on consecutive requests: compact_old_tool_results (src/agent/context.rs:482, called from src/agent/turn.rs:2105 after EVERY tool batch) truncates the next-oldest tool result in place each batch, so each request differs from the previous one at that message and the provider's longest-common-prefix cache breaks there. Measured traces.jsonl id 15 (2027-01-04, glm-5.3-gcp): prompt 45,495 / cached 20,928 = 46.0% — exactly the prefix up to the newly-truncated tool result (predicted ~20-21K, match within 3%). · regression test: compact_below_high_water_mark_mutates_nothing

Full record for plan 539e16c4 (see .coding/plans/539e16c4.md for the plan file).

regression test: compact_below_high_water_mark_mutates_nothing · path .coding/plans/539e16c4.md · branch wt/agenticcoding @ 78fa9cb (unmerged — exists only on this branch)
