+++
title = "graph_search stale-index miss — inline reindex-and-reserve (F10 pattern for the symbol index)"
created = "2027-01-07"
+++

Symptom: graph_search for a symbol added in-session (live case 2027-01-07: `normalize_new_item_text`, added to src/backlog.rs hours earlier in commit 568d405, exists at :121) returns "No symbols matched" — the codegraph symbol index is a startup-rebuilt gitignored cache; when the best-effort file watcher misses or lags an edit, the total miss is served silently: no staleness note, no inline repair, so the agent concludes the symbol doesn't exist or falls back to grep walks — exactly the round-trip waste the graph exists to eliminate. The content-index sibling got the reindex-instead-of-warn fix (backlog 42302c2d, commit 60f508d); the symbol index has no equivalent. · regression test: stale_symbol_miss_reindexes_and_serves_fresh_results

Full record for plan 55163f1e (see .coding/plans/55163f1e.md for the plan file).

regression test: stale_symbol_miss_reindexes_and_serves_fresh_results · path .coding/plans/55163f1e.md · branch wt/agenticcoding @ 1fd143f (unmerged — exists only on this branch)
