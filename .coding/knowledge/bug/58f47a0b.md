+++
title = "Add .worktrees to IGNORED_DIRS (backlog 39b3bbfb)"
created = "2027-01-11"
+++

Symptom: `.worktrees/runall-<item>/**` is not in IGNORED_DIRS (src/tool/agent/search.rs:56), so the main content index has ALWAYS ingested duplicate worktree trees as content rows; since plan 2a2ae0aa narrowed the watcher's blanket `.coding` exclusion, each worktree's own `.coding/codegraph.db` writes (continuous index passes, seeded once by snapshot_db's VACUUM INTO in src-tauri/src/ipc/spawn.rs:146-177 — dest is the WORKTREE's .coding, not the main one) now also fire debounced main-tree index passes: unbounded-ish churn while a run-all is in flight, on top of the duplicate index rows. NOT a loop (a main-tree pass never writes under .worktrees). Reported as LOW-1 in .coding/reviews/2026-09-25-watcher-coding-artifacts-2a2ae0aa-review.md (found 2027-01-25). · regression test: is_indexable_path_accepts_any_file_and_rejects_ignored

Full record for plan 58f47a0b (see .coding/plans/58f47a0b.md for the plan file).

regression test: is_indexable_path_accepts_any_file_and_rejects_ignored · path .coding/plans/58f47a0b.md · branch wt/mnemo @ 167e6f1 (unmerged — exists only on this branch)
