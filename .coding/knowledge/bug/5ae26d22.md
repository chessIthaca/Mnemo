+++
title = "Fix: browser_navigate auto-opens the Browser tab + readable browser tool cards"
created = "2026-08-28"
status = "superseded"
+++

Symptom: (1) browser_navigate fails with "the child WebView2 has no page target to attach to — is the Browser tab open?" when the tab has never loaded a URL — the agent cannot open the tab itself; the user had to manually type a URL first. (2) Browser tool calls render in chat with no human-readable summary — browser_navigate shows no URL, browser_click no selector, browser_screenshot's PNG inline (already works via ToolImage) is the only visual; cards read "browser_navigate ✗" with raw JSON hidden behind expand. · regression test: webview_navigate_auto_ensures_missing_child

Full record for plan 5ae26d22 (see .coding/plans/5ae26d22.md for the plan file).

regression test: webview_navigate_auto_ensures_missing_child · path .coding/plans/5ae26d22.md · branch wt/agenticcoder @ d92c278 (unmerged — exists only on this branch)
