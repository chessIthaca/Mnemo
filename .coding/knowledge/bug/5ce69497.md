+++
title = "Second mnemo instance shows blank white window — per-instance WebView2 user data folder"
created = "2027-01-11"
+++

Symptom: Starting a second production (release) mnemo instance on the same Windows machine brings up an empty white window with no UI; the process stays alive and the first instance keeps working normally. Multi-instance is the DESIGNED model (run-all worktrees; the CDP-port fix backlog 55ba23b1 assumes two live instances), so the fix must make instances coexist, not enforce single-instance. · regression test: agent_chat_builder_uses_per_instance_data_dir

Full record for plan 5ce69497 (see .coding/plans/5ce69497.md for the plan file).

regression test: agent_chat_builder_uses_per_instance_data_dir · path .coding/plans/5ce69497.md · branch wt/mnemo @ aaec6b6 (unmerged — exists only on this branch)
