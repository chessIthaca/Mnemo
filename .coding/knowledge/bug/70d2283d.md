+++
title = "Bound the over-threshold re-compaction loop + cross-turn re-emission guard"
created = "2027-01-07"
+++

Symptom: A turn whose context stays over the summarize threshold re-compacts every iteration unboundedly — each cycle runs an extra summarizer LLM call plus a model request on a nearly-identical context, driving byte-identical re-emission of the same tool call. Observed live: ~114 LLM requests over 95 minutes with "no new work" (the session summary recorded "resumed/re-injected with this summary thirty-one times"), duplicated identical read_files messages in the transcript, and a 5,526,459-token request rejected with 400 (context limit 1,048,576). · regression test: over_threshold_turn_bounds_compaction_attempts

Full record for plan 70d2283d (see .coding/plans/70d2283d.md for the plan file).

regression test: over_threshold_turn_bounds_compaction_attempts · path .coding/plans/70d2283d.md · branch wt/agenticcoding @ 68f98ed (unmerged — exists only on this branch)
