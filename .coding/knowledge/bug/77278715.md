+++
title = "Per-model think_tags config + config-driven vendor policy (no hardcoded provider names)"
created = "2027-01-07"
status = "superseded"
+++

Symptom: Reasoning tokens from proxy-served DeepSeek models (deepseek-v4-flash-gcp/aws via the the LiteLLM proxy, <gateway>) render in the MAIN CHAT WINDOW as assistant text instead of the reasoning panel. GLM via the same proxy is unaffected (its reasoning arrives in proper reasoning_content). Root cause (established, see SPEC memory 747cc41d successor): commit 949fed2 gated the ThinkTagFilter to Local-kind endpoints only (`think_tags_enabled = matches!(kind, Local)`, src/provider/openai.rs:722) because the raw-echo work made content→reasoning misrouting catastrophic — but the LiteLLM→vertex path streams DeepSeek reasoning inside `content` wrapped in mdat tags, the exact shape that filter extracts. Pre-gate it was caught (filter engaged for every OpenAI-compatible endpoint per the 2026-08-22 review Low 3); post-gate it leaks as TextDelta → streamingText → transcript. · regression test: think_tag_extraction_engages_for_openai_kind_when_configured

Full record for plan 77278715 (see .coding/plans/77278715.md for the plan file).

regression test: think_tag_extraction_engages_for_openai_kind_when_configured · path .coding/plans/77278715.md · branch wt/agenticcoding @ 5361db7 (unmerged — exists only on this branch)

Post-rollback note (2027-01-09): the pointers above describe the pre-rollback state — 5361db7/b05e72d are git-reflog-only (~90 days) and the plan file was removed by the reset to e0ac5dd; the recovery path is documented in DECISION 2027-01-07-vendor-policy-direction-rolled-back.
