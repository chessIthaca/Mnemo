+++
title = "DeepSeek sentinel-mirror (CONTEXT_FOOTER echo) — OPEN, fix reverted; ThinkTagFilter ladder step PROVEN NOT TO WORK"
created = "2027-01-07"
status = "superseded"
+++

OPEN on main (2769fca) as of 2027-01-10. Symptom: DeepSeek (flash tier, thinking mode, via the proxy) opens its first response by mirroring the harness's context footer — a mangled "cache-stable sentinel — ignore</context>" (invented closing tag) — and its reasoning tokens also render in the MAIN CHAT WINDOW (open BUG 5, backlog 0d4f54e3). Fix history: the bracket-form fix (plan 7bb9a608: CONTEXT_FOOTER unclosed-tag form → square brackets in src/agent/prompt.rs) merged as 5cdd822 but FAILED live verification — the user's session still mirrored the footer with the fix live — and the merge was rolled back the same day at user request (main reset to 2769fca; commits reflog-only). Root cause stands: DeepSeek's chat template hoists all system messages into one blob glued directly before the user turn; the sentinel renders as the blob's tail — completion-bait for a thinking model. Fallback ladder: (1) relocate the volatile tail + sentinel into the final USER message for DeepSeek (Anthropic-style; re-verify the last-message-identity cache law first); (2) BUG 5 stream-level ThinkTagFilter heuristic (backlog 0d4f54e3) so reasoning-borne echoes stay in the reasoning panel; (3) freeze new DeepSeek-specific behavior + audit serving-stack assumptions (direct api.deepseek.com vs the GCP vLLM-SGLang proxy). USER CORRECTION 2027-01-10 (authoritative): the reasoning-leak solutions tried so far are PROVEN NOT TO WORK — twice: (a) the vendor-policy per-model think_tags fix (plan 77278715) rolled back 2027-01-09 with the leak persisting, and (b) the reasoning STILL leaked into the main window with the sentinel fix live at 5cdd822. The reasoning bug is REAL; the tag-based solutions are NOT. Ladder step 2 (the stream-level ThinkTagFilter, backlog 0d4f54e3 — the same approach a third time) must NOT be prescribed as the leak's solution; the leak's fix needs fresh investigation (the proxy's stream parsing / serving stack — step 3's audit), not another render-level attempt.
