+++
title = "Classify user cancels as Cancelled, not provider errors (D1)"
created = "2027-01-07"
+++

Symptom: 45+ rows in .coding/logs/provider-errors.jsonl carry "stream aborted — consumer dropped" — these are USER interrupts (Interrupt/Cancel/Compact/Clear), not provider failures. The trace records are marked failed even though the HTTP response was healthy, and no request_stats row is written, so cancelled requests are invisible to latency/token accounting even though the provider billed the tokens generated so far. A genuine stream error can also be masked when the consumer exits right after it. · regression test: consumer_drop_stamps_cancelled_not_failed

Full record for plan 814f27c9 (see .coding/plans/814f27c9.md for the plan file).

regression test: consumer_drop_stamps_cancelled_not_failed · path .coding/plans/814f27c9.md · branch wt/agenticcoding @ 27e1d2e (unmerged — exists only on this branch)
