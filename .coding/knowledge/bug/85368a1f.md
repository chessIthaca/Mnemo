+++
title = "Code-graph indexing: parallel waves + parser reuse"
created = "2027-01-11"
+++

Symptom: Code-graph indexing has become really slow (2027-01-11 user report): project opens and re-indexes take noticeably longer than before, and CPU sits at ~3% during indexing — the pass is I/O- and round-trip-bound, not CPU-bound. The index loop (src/codegraph/mod.rs index_inner :355-487) is fully SEQUENTIAL: per file, 3 separate SQLite SELECTs (file_hash/file_mtime/has_content under the store lock), a blocking std::fs::read, a fresh tree_sitter::Parser::new + set_language per file (src/codegraph/extract.rs :188-204, plus a fresh JS parser per HTML script block at :1078), and a per-file upsert transaction that re-prepares ~6 statements each call (src/codegraph/store.rs upsert_file :196-283). Regression source: the all-language coverage work (plan c87093c1) added 10 grammars + 7 visitors + HTML script-block sub-parsing to this path. · regression test: large_fixture_indexes_on_worker_threads

Full record for plan 85368a1f (see .coding/plans/85368a1f.md for the plan file).

regression test: large_fixture_indexes_on_worker_threads · path .coding/plans/85368a1f.md · branch wt/agenticcoding @ 575ba94 (unmerged — exists only on this branch)
