+++
title = "Swallowed errors on durable writes — log + collect (quality review LOW 6)"
created = "2027-01-07"
+++

Symptom: Durable-write failures are silently swallowed with no trace. (a) The plan-completion semantic-memory write (src/agent/turn.rs:953, store.write inside tokio::spawn) discards its Result — a failed memory write vanishes with no log. (b) The config multi-file save rollback (src/config/mod.rs:215+219) discards restore_from_backup Results — a failed rollback leaves a possibly half-renamed config set with no trace, and the returned error tells the user nothing about the half-committed state. Quality review LOW 6 (.coding/reviews/2027-01-06-full-review-quality-coverage.md). · regression test: rollback_collects_restore_failures

Full record for plan 8b5f37fb (see .coding/plans/8b5f37fb.md for the plan file).

regression test: rollback_collects_restore_failures · path .coding/plans/8b5f37fb.md · branch wt/agenticcoding @ 4e1bae9 (unmerged — exists only on this branch)
