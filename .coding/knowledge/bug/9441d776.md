+++
title = "Bug-fix plans: checkable detailed sub-steps survive restart"
created = "2027-01-11"
+++

Symptom: A bug_fixing plan's "## Detailed steps" section persists as raw text with no per-sub-step completion state: after a session restart mid-fix, the resumed plan shows an unchecked "Minimal fix" skeleton step plus a wall of detail text — nothing records WHICH sub-steps already shipped, so a lesser model cannot tell without re-deriving from git diff, and work gets redone or dropped (live case 2027-01-11, plan c87093c1: gate disk-check broadening, 10 grammars, 7 visitors, HTML sub-parsing, docs — ~2000 lines across 5 files — collapsed into one unchecked step; the plan would not have survived continuation after restart). · regression test: create_plan_bug_fixing_renders_detailed_steps_as_checkable_substeps

Full record for plan 9441d776 (see .coding/plans/9441d776.md for the plan file).

regression test: create_plan_bug_fixing_renders_detailed_steps_as_checkable_substeps · path .coding/plans/9441d776.md · branch wt/agenticcoding @ 6309326 (unmerged — exists only on this branch)
