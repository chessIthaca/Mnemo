+++
title = "Stop emitting the visible error event for the harness tool-call correction"
created = "2027-01-11"
+++

Symptom: The repeat-failure circuit breaker's corrective injection is double-surfaced: besides pushing the harness correction message into the LLM context (wanted), it emits AgentEvent::Error { retrying: true } with text "tool-call correction: repeated identical `X` failure — schema reminder injected into the next request" (src/agent/turn.rs:1658-1669), which the frontend renders as a red "error: tool-call correction: …" box in the chat transcript (frontend/src/components/chat/Message.tsx:334-341) and an activity-log entry (frontend/src/hooks/agentEventReducer.ts:1413-1420). User report: the context injection is good and must stay; the visible error line does not need to be in the output. · regression test: repeat_failure_injects_schema_correction_on_second_identical_error

Full record for plan 995436c2 (see .coding/plans/995436c2.md for the plan file).

regression test: repeat_failure_injects_schema_correction_on_second_identical_error · path .coding/plans/995436c2.md · branch wt/mnemo @ b94ce37 (unmerged — exists only on this branch)
