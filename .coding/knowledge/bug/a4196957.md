+++
title = "Self-describing bookkeeping tool results (d779060a)"
created = "2027-01-11"
+++

Symptom: The confirmation messages of the three plan/memory bookkeeping tools are opaque at a glance — the transcript alone cannot show what was done without opening the plan file or looking the memory id up: (1) memory_amend (src/tool/memory/mod.rs:868) names the amended record only by id + knowledge-file path, never the record's title/tier, so "Amended knowledge record 12527cd7-…" says nothing about WHICH memory changed; (2) update_plan (src/tool/workflow/plan.rs:1050) reports the plan title/id and step counts but never WHAT was updated — context appended vs replaced, steps replaced/appended, regression_test/landed_design set — so a mid-flight amendment's effect is unverifiable from the transcript; (3) complete_step (src/tool/workflow/plan.rs:1291) reports the step INDEX ("Complete Step 3 (3/4, Plan Title)") but not the step's header text — which work item completed requires opening the plan file. Reproduce: run any memory_amend / update_plan / complete_step and read the tool result — none identifies the amended memory's title, the updated fields, or the completed step's header. · regression test: update_plan_result_enumerates_applied_changes

Full record for plan a4196957 (see .coding/plans/a4196957.md for the plan file).

regression test: update_plan_result_enumerates_applied_changes · path .coding/plans/a4196957.md · branch wt/agenticcoding @ 50f9885 (unmerged — exists only on this branch)
