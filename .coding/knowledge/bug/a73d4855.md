+++
title = "Map reasoning_effort \"off\" → \"none\" for DeepSeek-family endpoints"
created = "2026-12-29"
+++

Symptom: DeepSeek rejects reasoning_effort "off" with a non-retryable HTTP 400 "unknown variant `off`" (live-verified 2026-12-23, probe F): the enum is none|minimal|low|medium|high|xhigh|max, and the OpenAI request builder (openai.rs build_request_json :1763) emits the client config's effort value verbatim with no policy guard, so any path that carries "off" past the resolvers puts the 400 trigger on the wire — while DeepSeek never receives the explicit thinking-off value ("none") the user's effort=off selection means. · regression test: build_request_json_maps_off_effort_to_none_for_deepseek

Full record for plan a73d4855 (see .coding/plans/a73d4855.md for the plan file).

regression test: build_request_json_maps_off_effort_to_none_for_deepseek · path .coding/plans/a73d4855.md · branch wt/agenticcoding @ a73d38f (unmerged — exists only on this branch)
