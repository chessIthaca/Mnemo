+++
title = "Run-All checkpoint: branch guard — fork wt/* before checkpointing, never commit to main"
created = "2026-12-31"
+++

Symptom: Run-All's pre-item checkpoint can commit directly to main (security review MEDIUM; verified). run_all_dispatch_next (src-tauri/src/ipc/run_all.rs:962-984) calls checkpoint() before dispatching the next backlog item, and checkpoint_impl (src/project/git_ops.rs:104-123) runs git add -A + git commit on whatever branch is checked out — no branch check. The per-directory wt/* auto-fork only happens later inside create_plan (ensure_work_branch, git_ops.rs:588-642). So when the session sits on main — e.g. right after merge_to_main lands a branch — the next Run-All tick commits the dirty tree (typically .coding/backlog.jsonl status flips) straight to main, violating the never-commit-to-main constitution. · regression test: checkpoint_on_main_lands_the_commit_on_the_work_branch

Full record for plan a91e71d3 (see .coding/plans/a91e71d3.md for the plan file).

regression test: checkpoint_on_main_lands_the_commit_on_the_work_branch · path .coding/plans/a91e71d3.md · branch wt/agenticcoding @ 97c6ae9 (unmerged — exists only on this branch)
