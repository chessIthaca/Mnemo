+++
title = "Slid resolution with a re-queued (Pending) item still stalls the run"
created = "2027-01-07"
+++

Symptom: Run-All stalls forever when the in-flight item is externally re-queued to Pending mid-run (the 5bb1e4cd-style .coding/backlog.jsonl edit, which cannot touch the in-memory current_item pointer) AND the completing turn's resolution slides (the e33a07fd slide: the finish turn's Finished is deferred because a descendant still runs, then discarded when the descendant's completion notification starts a new turn): the notification turn lands at (Complete, false) with the item Pending → closed_earlier=false (it requires InFlight, src-tauri/src/ipc/run_all.rs:4476-4480) → the waiting arm annotates + returns (:4601) → the run never dispatches the next item (Complete is a resting state; nothing ever re-checks) — a manual restart is required. Found as review LOW-3, 2027-01-08, in the e33a07fd closing review (.coding/reviews/2026-09-08-run-all-stall-fix-review.md). · regression test: slid_resolution_with_requeued_pending_item_dispatches_next

Full record for plan af1058ee (see .coding/plans/af1058ee.md for the plan file).

regression test: slid_resolution_with_requeued_pending_item_dispatches_next · path .coding/plans/af1058ee.md · branch wt/agenticcoding @ d34b784 (unmerged — exists only on this branch)
