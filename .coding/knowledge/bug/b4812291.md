+++
title = "Sandbox: close the hardlink / 8.3-alias / ladder-fallback write-protection bypasses"
created = "2027-01-11"
+++

Symptom: Three pre-existing bypasses of Sandbox's path-prefix write protection (found in review rounds 2-4 of plan ee65fd4b, 2027-01-11; BUG memory d7ebe9f9, backlog 1a5bffcf). (1) A hardlink inside .coding/ to a protected file (`.coding/x.toml` hardlinked to `.coding/safety.toml`) is not protected by name — canonicalize cannot see a hardlink (no link to follow; the path IS the file) — so any plan kind can truncate the shared inode; `.coding/x.rs` hardlinked to `src/lib.rs` does the same outside-in. (2) A Win32 8.3 spelling (`.coding/KNOWLE~1/new/x.md`) reads as ordinary — aliases are not symlinks, so the per-component case/trailing-dot trim cannot see them — and validate_for_write's create_dir_all resolves the alias and plants a new subtree inside the real `.coding/knowledge/` (overwriting an EXISTING protected file stays impossible: an alias of an existing file canonicalizes to the long name). (3) validate_for_write's step-5 revalidation failure returns the LEXICAL path (`Err(_) => Ok(validated)`), so a write through a link that escapes the root lands outside it — reachable for implementation/bug_fixing plans (the research route was closed by plan ee65fd4b). · regression test: protected_hardlink_inside_coding_refused

Full record for plan b4812291 (see .coding/plans/b4812291.md for the plan file).

regression test: protected_hardlink_inside_coding_refused · path .coding/plans/b4812291.md · branch wt/mnemo @ 78d8329 (unmerged — exists only on this branch)

Amended 2027-01-11: LANDED + VERIFIED (2027-01-15): fixed by plan b4812291 — (1) the hardlink guard inside `is_protected_write_target` (link count > 1 under `.coding/**`, fail-closed, directories exempt), (2) the canonical-ancestor check before `create_dir_all` plus the canonical re-check in step 5, which close the Win32 8.3 spelling, and (3) the ladder's fail-closed step 5 with its one re-checked lexical fallback (the just-created-parent case only). Merged into main via the wt/mnemo merge at 630ec31 (work commit 78d8329) and verified there: sandbox 39/39 and file_write 23/23 with the per-bypass regression tests green (`protected_hardlink_inside_coding_refused`, `protected_win32_8dot3_alias_refused` — exercised, `validate_for_write_refuses_in_root_link_into_protected_tree`, `validate_for_write_fails_closed_on_out_of_root_link`, `file_write_through_out_of_root_link_is_denied`), full suite 2380 passed / 0 failed, warning-free. Backlog item 1a5bffcf closed done.
