+++
title = "Park auto-continue while spawned subagents are running"
created = "2026-12-20"
+++

Symptom: A parent agent waiting on its tool-spawned background agents (e.g. a reviewer) gets spammed with "[harness note] continue from where you left off." synthetic turns: every normal turn end while the workflow is Executing triggers auto-continue (src/runtime/agent.rs:224-227), even though the parent is genuinely blocked — the state machine refuses all workflow transitions while subagents run. In the 2026-12-20 session this produced 5 spurious notes/LLM turns before the agent parked via ask_user; the child-completion Suggestion is the designed resume but never gets a chance because auto-continue fires first. · regression test: auto_resume_parks_while_descendants_running

Full record for plan bbd712c9 (see .coding/plans/bbd712c9.md for the plan file).

regression test: auto_resume_parks_while_descendants_running · path .coding/plans/bbd712c9.md · branch wt/agenticcoder @ 588b3a7 (unmerged — exists only on this branch)
