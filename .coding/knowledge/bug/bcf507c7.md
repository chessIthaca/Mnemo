+++
title = "Reap offline browsers — watchdog + marker-aware profile sweep"
created = "2027-01-11"
+++

Symptom: Offline browsers accumulate throughout a session (user report 2027-01-12, backlog 2ccf92b0): a headless Chromium that goes offline (process death or dropped CDP connection) is only cleaned up when the NEXT browser operation runs (`ensure_browser`, src/browser/mod.rs:248) — if none comes, the process (if the connection dropped while the process lives) and its `mnemo-browser-*` temp profile dir linger until app exit. Every respawn creates a fresh profile dir, and `sweep_stale_profiles` (src/browser/mod.rs:320) runs only at launch time and only removes dirs >1h old — so in-session orphans accumulate all session, and while the browser stays alive the sweep never runs at all. · regression test: sweep_reaps_orphans_but_never_live_profiles

Full record for plan bcf507c7 (see .coding/plans/bcf507c7.md for the plan file).

regression test: sweep_reaps_orphans_but_never_live_profiles · path .coding/plans/bcf507c7.md · branch wt/agenticcoding @ 341b25d (unmerged — exists only on this branch)
