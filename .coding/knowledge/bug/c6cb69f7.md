+++
title = "Fix DeepSeek reasoning_content 400, third recurrence — tail-owner escape (live 2026-09-12)"
created = "2027-01-11"
+++

Symptom: DeepSeek thinking-mode requests die non-retryably with HTTP 400 'The `reasoning_content` in the thinking mode must be passed back to the API' — THIRD recurrence. Live evidence 2026-09-12 ~17:35-17:45 EDT: the running app killed THIS agent session twice with it (deepseek-v4-flash @ api.deepseek.com), surfaced in the UI as 'SERIALIZATION BUG (not retried): ... The request builder dropped or mutated a reasoning field the provider requires.' Prior history: first fix b23463c (plan c9b5cbe4) — age-0 raw-echo injection + synthetic-path unconditional key; second occurrence id 1042 (2026-09-04, plan 1e64d71d, steps 2-4 still open) — hypothesis 'tail-owner turn still escapes the age-0 guarantee'. Contract to enforce: on DeepSeek thinking requests (reasoning_effort present) the tail-owner assistant turn — last message OR issuer of trailing tool results — must carry the reasoning_content KEY, any value including empty string. · regression test: deepseek_keys_every_tool_call_turn_after_cross_vendor_reentry

Full record for plan c6cb69f7 (see .coding/plans/c6cb69f7.md for the plan file).

regression test: deepseek_keys_every_tool_call_turn_after_cross_vendor_reentry · path .coding/plans/c6cb69f7.md · branch wt/mnemo @ 1346141 (unmerged — exists only on this branch)
