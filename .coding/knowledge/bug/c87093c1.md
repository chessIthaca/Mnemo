+++
title = "All-language coverage: finish-gate disk check + code-graph indexing"
created = "2027-01-07"
+++

Symptom: finish falsely errors for a regression test written in any language the code graph doesn't parse: "regression test symbol 'X' not found in the code graph and no source file on disk contains the name (verified after a forced re-index)" — e.g. a .js or .py test that exists on disk and is runnable. The agent is blocked from finishing and steered toward a phantom rename. Root cause: CodeGraph::reindex_files_containing (src/codegraph/mod.rs:604) filters its disk scan to Lang::from_extension (.rs/.ts/.tsx only), so non-indexed code files are invisible to the gate's containment check, and the graph cannot parse them so the symbol never resolves. · regression test: finish_passes_when_regression_test_is_in_non_indexed_language

Full record for plan c87093c1 (see .coding/plans/c87093c1.md for the plan file).

regression test: finish_passes_when_regression_test_is_in_non_indexed_language · path .coding/plans/c87093c1.md · branch wt/agenticcoding @ 2d95bd2 (unmerged — exists only on this branch)
