+++
title = "Tool output strips LLM special tokens — boundary-token round-trip escaping (verify + fix)"
created = "2027-01-07"
+++

Symptom: Files containing LLM special tokens (e.g., the GLM end-of-text token) cannot be read-then-written faithfully: the agent's view of tool results shows the token position as an empty string — live-verified 2027-01-08 (a regex search matched the raw token in 11 lines across 4 files while every displayed line showed it stripped) — so read-then-write silently corrupts the file (incident 2026-12-24, plan 7383a4d8: patch.rs reads showed the stop literal as an empty string, a reconstructed old_string failed to match, a line-range edit wrote a wrong value). · regression test: file_write_restores_boundary_markers

Full record for plan cc92a5d0 (see .coding/plans/cc92a5d0.md for the plan file).

regression test: file_write_restores_boundary_markers · path .coding/plans/cc92a5d0.md · branch wt/agenticcoding @ 47c36ca (unmerged — exists only on this branch)
