+++
title = "429-fallback viability via live token count (a117e827)"
created = "2027-01-07"
+++

Symptom: A 429 on a model served with a 200k-window endpoint never fails over to an endpoint serving the same model with a 128k cap even when the live conversation is 20k tokens — try_429_fallback (src/agent/loop_impl.rs:1701-1710) compares context WINDOWS (alt_cm.max_tokens() < the current CM's max_tokens()), not the live conversation size. The turn fails once with "no viable alternate" and later turns keep hitting the rate-limited endpoint. Fails safe (no stall, no data loss) but silently disables failover for mixed-window configurations of the same model id (e.g. a proxy capping context below the direct endpoint). · regression test: rate_limit_429_falls_back_to_smaller_window_alternate_when_live_conversation_fits

Full record for plan de1f874b (see .coding/plans/de1f874b.md for the plan file).

regression test: rate_limit_429_falls_back_to_smaller_window_alternate_when_live_conversation_fits · path .coding/plans/de1f874b.md · branch wt/agenticcoding @ 982d0b1 (unmerged — exists only on this branch)
