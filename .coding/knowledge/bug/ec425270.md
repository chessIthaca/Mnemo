+++
title = "Self-healing wedged offscreen browser (CDP timeouts + force-reap + watchdog probe)"
created = "2027-01-11"
+++

Symptom: offscreen_browser_* tool calls hang forever when the shared headless Chromium is wedged (alive process, open CDP connection, unresponsive renderer — e.g. a page stuck in an infinite JS loop). The watchdog never reaps it (the handler task is still running, and reap_dead_browser only fires on a finished handler), so the wedge persists until an app restart. · regression test: wedge_renderer_times_out_and_self_heals

Full record for plan ec425270 (see .coding/plans/ec425270.md for the plan file).

regression test: wedge_renderer_times_out_and_self_heals · path .coding/plans/ec425270.md · branch wt/mnemo @ bc3fd5e (unmerged — exists only on this branch)
