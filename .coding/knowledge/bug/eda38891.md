+++
title = "Block agent file writes to .git (sandbox escape)"
created = "2027-01-07"
+++

Symptom: Agent file tools can write the .git/ control plane (sandbox escape — 2027-01-09 security review HIGH-1). file_write/file_edit/file_append/convert_line_endings (and the IPC write_sandboxed mirror in src-tauri/src/ipc/files.rs) accept targets like .git/hooks/pre-commit or .git/config (core.fsmonitor = <command>) — both inside the project root, so the path sandbox passes them and AutoApproveProject auto-approves. The next routine commit — or an auto-approved read-only `git status` (fsmonitor chain, zero approvals) — executes the payload with full user privileges: API-key exfiltration from ~/.mnemo/keys.toml, permanent safety-rule tampering. Root cause: is_protected_write_target (src/tool/agent/sandbox.rs:189-234) protects .coding state (DBs, safety.toml, backlog.jsonl, plans/, reviews/, knowledge/) but has no .git entry; the git tool never passes --no-verify, so planted hooks run. · regression test: protected_git_control_plane

Full record for plan eda38891 (see .coding/plans/eda38891.md for the plan file).

regression test: protected_git_control_plane · path .coding/plans/eda38891.md · branch wt/agenticcoding @ 50969db (unmerged — exists only on this branch)
