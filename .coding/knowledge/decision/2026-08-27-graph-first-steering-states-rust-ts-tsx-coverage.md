+++
title = "graph-first steering states Rust+TS/TSX coverage explicitly (4ff4952)"
created = "2026-08-27"
+++

DECISION: graph-first semantic steering now states Rust + TS/TSX coverage explicitly (commit 4ff4952 on wt/agenticcoder, plan e075b4e9; review .coding/reviews/2026-12-graph-first-frontend-ts.md PASS, no findings). TOOL_STRATEGY graph line (src/agent/prompt.rs ~:159) and the graph_search/graph_context schema descriptions (src/tool/agent/codegraph.rs) say the graph indexes .rs/.ts/.tsx sources — truthful per walk.rs Lang::from_extension (rs/ts/tsx ONLY; no js/md) — so frontend symbol lookups start with graph_search instead of grep + whole-file reads, and graph_impact's shared-symbol rule names frontend lib exports. Pinned by tool_strategy_lists_graph_tools_before_search (asserts "frontend TS/TSX") + schema_descriptions_state_language_coverage. DELIBERATE: graph_impact/graph_path descriptions stay lean (downstream tools; framing lives in TOOL_STRATEGY + entry-point descriptions). This resolves the 2026-08-27 evidence miss (TraceStats legend located via grep + 2 whole-file reads) recorded in the HOW memory "frontend TS IS codegraph-indexed" — that memory's caveat about graph line-number staleness for uncommitted edits still holds.

Amended 2027-01-07: 2027-01-11 amendment (plan c87093c1, all-language coverage): the coverage set this decision pinned is superseded — Lang::from_extension (src/codegraph/walk.rs) now parses 12 languages (Rust, TypeScript/TSX incl. .mts/.cts, JavaScript .js/.jsx/.mjs/.cjs, Python, Go, Java, C, C++, C#, Ruby, PHP) plus HTML (.html/.htm) whose <script> blocks are sub-parsed as JavaScript with line offsets (visit_html in src/codegraph/extract.rs). The TOOL_STRATEGY graph line and the graph_search/graph_context schema descriptions state the full set; the pinned test phrase changed from "frontend TS/TSX" to "JavaScript, Python" (tool_strategy_lists_graph_tools_before_search). The finish gate's disk check additionally covers every code extension (is_code_extension) with a disk-containment pass-with-note for non-indexed languages.
