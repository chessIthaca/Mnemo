+++
title = "steering layer — tool-result nudges + create_plan recall rider over grep habits"
created = "2026-08-29"
+++

DECISION (2026-09-10, plan 77768e94, branch wt/agenticcoder): steering layer routes agents to memory+graph over grep/search at the moment of tool choice — prose mandates alone underperformed (documented 2026-08-22 hardening + the 2026-09-10 session where the compiled MANDATORY rules were still skipped). Four mechanisms: (1) shared miss_hint in src/tool/agent/codegraph.rs — every graph_* total miss points at `search` (literals unindexed), near-misses at candidate ids (graph_search already had it; context/impact/path unified); (2) GREP_NUDGE in src/tool/agent/shell.rs — grep-family commands (first token of command or any piped segment: grep/egrep/fgrep/rg/ag/ack/git grep/findstr/Select-String/sls) get a one-line TIP prepended to output, raw data fields nudge-free; (3) RECALLED CONTEXT rider on create_plan (CreatePlanTool.with_memory, wired in factory.rs register_workflow_tools only production site) — passive recall_peek (NO access bump) over title+goal(+bug symptom, bug plans get extra record_type Bug pass), deduped, ≤5 hits, one-line ≤140-char gists, silent skip on no store/hits/error; (4) cost-model line in TOOL_STRATEGY (prompt.rs) — indexed lookup beats tree walk beats process spawn. No new commands needed: the tool surface was complete, the gap was steering.
