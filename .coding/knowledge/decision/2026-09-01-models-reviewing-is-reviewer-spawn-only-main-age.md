+++
title = "[models.reviewing] is reviewer-spawn-only — main agent keeps executing through Reviewing"
created = "2026-09-01"
+++

DECISION (user, 2026-12-06, backlog 86c90ff6, plan 0d124d58): [models.reviewing] is reviewer-spawn-only. The main agent NEVER runs on the reviewing model — src/model_resolver.rs maps WorkflowState::Reviewing to the EXECUTING slot (same override as Executing; reviewing-set-but-executing-unset resolves None → default), so the main agent keeps its executing model through the Reviewing closing sequence. The reviewing slot's sole consumer is the new ModelResolver::resolve_reviewer_model() (trait default None; ConfigModelResolver resolves each link independently): reviewing → executing (back-compat) → subagent → None/default, called once at spawn by spawn_agent's role:"reviewer" pin (explicit model arg still wins). Regression tests: model_resolver::tests::reviewing_state_keeps_executing_model_not_reviewing_slot (fails under old code), reviewer_model_chain_reviewing_executing_subagent, reviewer_model_falls_back_to_subagent_then_none; spawn_agent tests assert the pin never consults resolve() (asked stays empty, reviewer_asks==1). Docs synced: README.md agents bullet, PLAN.md "Reviewer-spawn-only reviewing slot", ModelsSection.tsx hint, config field + wire DTO docs. Workflow state machine + finish gates UNCHANGED ("Executing then goes to complete with the same gates" is future direction, not this change).

ACCEPTED LIMITATION (review LOW 1, 2026-12-06-reviewer-spawn-only-reviewing-slot.md): the on-disk spec export .coding/knowledge/spec/2026-08-27-spawned-reviewers-run-on-the-models-reviewing-mo.md still describes the OLD two-step pin (resolve(Reviewing,false).or_else(resolve(Reviewing,true))) — .coding/knowledge/ files are file-tool-protected for the agent, so this record (and the supersession of the old SPEC digest in-memory) is the amendment carrier; README.md, PLAN.md, and this decision file carry the new resolve_reviewer_model mechanism for any instance re-deriving from files. Per-link validation note: a dangling reviewing ref now falls through to executing (old code dropped the combined spec to subagent/None) — deliberate, closer to user intent.

Note: this export's filename/created date (2026-09-01) predates the actual decision date (2026-12-06) — known export-dating bug, queued in the backlog.
