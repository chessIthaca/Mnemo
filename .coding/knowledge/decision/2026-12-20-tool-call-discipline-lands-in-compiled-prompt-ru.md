+++
title = "tool-call discipline lands in compiled prompt + runtime enforcement"
created = "2026-12-20"
+++

DECISION (2026-12-21, plan 73ddb869 on wt/agenticcoder, user: "these rules belong in the system agent.md — or maybe into the tools? you make the right choice"). Tool-call discipline rules land in BOTH layers, split by what each can enforce: (1) GENERATION-TIME discipline → new TOOL_CALL_DISCIPLINE const in the compiled stable head (src/agent/prompt.rs, pushed in build_stable_head between APP_RULES and TOOL_STRATEGY) — the system-level agent.md equivalent every project inherits (the project agent.md delegates universal rules there); carries content-first/call-second (never draft inside the call — create_plan/update_plan content must already exist in the same message), schema-first pre-flight field checklist (update_plan chunking via append:true is covered here), non-blank required fields, no empty-args for tools with required fields (truthful carve-out: current_plan/backlog_list/memory_search-browse are genuinely argument-free), escape traps (Windows paths with forward slashes), malformed-JSON → rewrite the COMPLETE call, two-identical-failures → stop and re-read schema. File-write chunking deliberately NOT restated — it lives in the preamble CORE PRINCIPLES; a duplicate bullet was removed in review (L1). (2) RUNTIME enforcement of the enforceable slice: CreatePlanTool rejects trim-empty title/goal (plan.rs execute, was silently accepted — serde only rejects missing); update_plan's all-None payload was ALREADY rejected by the workflow layer as no-op (message now teaches content-first); the turn.rs malformed-JSON feedback (max-strikes arm) now teaches rewrite-complete/no-empty-args instead of just "retry with valid JSON". Corrections applied vs user's draft: "no tool accepts an empty argument object" is FALSE (no-arg tools exist) — phrased truthfully; never-resend rule already existed (APP_RULES:106 + MAX_RETRIES=3/MAX_BAD_JSON_RETRIES=8) — only cross-referenced. Prompt markers test-locked: new test stable_head_carries_tool_call_discipline pins the block; regression tests create_plan_rejects_blank_title/goal, update_plan_blank_title_errors_as_noop. Files: src/agent/prompt.rs, src/tool/workflow/plan.rs, src/workflow/mod.rs, src/agent/turn.rs, src/agent/tests.rs, PLAN.md.
