+++
title = "reasoning_effort off→\"none\" for DeepSeek-family — policy-driven mapping at resolvers + builder guard"
created = "2026-12-28"
+++

DECISION (2026-12-29, bug plan for backlog 3f3044b9): DeepSeek's reasoning_effort enum is none|minimal|low|medium|high|xhigh|max — literal "off" is an instant non-retryable 400 (live-verified probe F, 2026-12-23). Code trace at HEAD: the backlog item's mechanism claim ("harness sends 'off' verbatim") is STALE — all resolvers already map Some("off")→None/omit (config_io.rs resolve_reasoning_effort :86/:101; endpoints.rs effective_reasoning_effort_for :342/:354), so no production path puts "off" on the wire. Real gaps: (1) openai.rs build_request_json :1763 emits the config value VERBATIM with no policy guard — any future path that skips the resolvers puts the 400 trigger on the wire; (2) DeepSeek never receives its explicit thinking-off value: omitting the field leaves the provider default in charge on thinking models, silently ignoring the user's "off". Fix design (policy-driven, ProviderKind/model aware): policy.rs gains reasoning_effort_off_wire_value(kind, model) → Some("none") iff ProviderPolicy::for_kind_and_model(kind,model).vendor == Vendor::DeepSeek (the existing model-id DeepSeek detector; Anthropic kind gated out); both resolvers' "off" arms consult it via client_factory provider_kind (made pub); openai.rs emission guards effort=="off" → policy value or omit. Safety: the reasoning_content age-0 injection (openai.rs :1574) is policy-driven by model id, NOT effort-gated, so emitting "none" cannot reintroduce the tail-validation 400 (bug 1042). Frontend unchanged (dropdown label "off" is UI-only; configured endpoints.toml value untouched).
