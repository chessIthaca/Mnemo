+++
title = "show_tool_activity consolidates show_memory_activity — render-time filter, store keeps everything"
created = "2026-12-30"
status = "superseded"
+++

One toggle [ui] show_tool_activity (bool, default FALSE) now governs ALL agent-activity chat cards (tool calls, memory reads/writes, vision image-parsing, skill announcements) — the memory-specific show_memory_activity toggle is REMOVED end-to-end (backend UiConfig field, patch.rs + settings_dto.rs apply paths, GetSettingsUi wire, per-agent AgentState flag + stamping, both reducer gates, localStorage mirror mh.showMemoryActivity; old config.toml keys are ignored by serde and drop on next save). Visibility is a RENDER-time concern: Conversation.tsx skips entries via isActivityEntry (exported from agentState.ts; kinds tool/memory/vision/skill) when the toggle is off; the transcript STORE always contains every entry (the model's context echo is built server-side and is unaffected); the Output tab + console still log every tool call. Always-visible kinds: user/assistant/error/steer/qa. Persistence: config.toml single source, no localStorage mirror (the show_tool_images/sounds pattern); App.tsx hydration always applies. Hidden entries render nothing (simple version — no "N tool calls" summary line; the InflightBar shows live activity). Tests: Rust (show_tool_activity_defaults_false, round_trips, save_round_trips_all_sections, wire assertion, synced contract fixture) + frontend (new Conversation.test.ts: source pins + isActivityEntry pure tests; the old "suppressed when off" store test rewritten to assert the entry IS in the store with the toggle off). Plan 0e71e4f9, backlog db489070.
