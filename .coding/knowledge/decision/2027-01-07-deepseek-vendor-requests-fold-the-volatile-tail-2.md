+++
title = "DeepSeek-vendor requests fold the volatile tail into the head (fold_volatile_tail policy) + R10 any-period guard — MERGED into main (3a2accd)"
supersedes = "2027-01-07-deepseek-vendor-requests-fold-the-volatile-tail"
created = "2027-01-07"
+++

MERGED into main at 3a2accd (3a2accde38b70ee404ba3cdd1e75f9f0492fb6a4) on 2027-01-11 (merge_to_main skill), branch wt/agenticcoding deleted (pre-merge tip 951ded0). DECISION (plan a0eab9ca, review PASS round 3): DeepSeek-vendor requests fold the volatile tail into the leading system message and omit the CONTEXT_FOOTER — the standard user/tool request shape — because DeepSeek models echo trailing system blocks instead of answering the user (sentinel-mirror 5cf5469c + the 2027-01-11 exit-note loop: 366× on a 74-byte unit for 64.5 s). GLM/Anthropic/OpenAI keep the cache-optimized trailing placement, byte-identical to before; the DeepSeek prefix-cache sacrifice is accepted (vendor otherwise unusable). Mechanism: ProviderPolicy::fold_volatile_tail (src/provider/policy.rs, true only in deepseek(), model-name-keyed — applies on ANY endpoint serving a DeepSeek-family model, including the official api.deepseek.com) wired in turn.rs install_system_messages (fold_tail = is_local || policy.fold_volatile_tail). Companion: R10 detect_repetition (src/provider/stream.rs) detects loops of ANY byte period ≤ 200 over the last 600 tail bytes (was exactly-200 only). Residual (accepted): text-only suggestion steers still leave a short system message as the last message on fold vendors. Reasoning-channel accumulation remains open in backlog 1f56297a.
