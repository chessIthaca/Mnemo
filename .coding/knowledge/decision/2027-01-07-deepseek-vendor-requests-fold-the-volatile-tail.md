+++
title = "DeepSeek-vendor requests fold the volatile tail into the head (fold_volatile_tail policy) + R10 any-period guard"
created = "2027-01-07"
status = "superseded"
+++

DECISION (plan a0eab9ca, commits e54fb7b + 15fd691 on wt/agenticcoding, review PASS round 3): DeepSeek-vendor requests fold the volatile tail into the leading system message and omit the CONTEXT_FOOTER — the standard user/tool request shape — because DeepSeek models echo trailing system blocks instead of answering the user (sentinel-mirror 5cf5469c + the 2027-01-11 exit-note loop: the model echoed the injected # RECALLED MEMORIES block, fabricated sections, and looped 366× on a 74-byte unit for 64.5 s). GLM/Anthropic/OpenAI keep the cache-optimized trailing placement (byte-stable CONTEXT_FOOTER, empirical last-message-identical cache law) — byte-identical to before. Tradeoff: DeepSeek prefix-cache reuse is sacrificed (accepted for a vendor that is otherwise unusable). Mechanism: ProviderPolicy::fold_volatile_tail (src/provider/policy.rs, true only in deepseek(), registry-fixed — no config key) wired in turn.rs install_system_messages (fold_tail = is_local || policy.fold_volatile_tail, reusing the Local fold path). Companion defense: R10 detect_repetition (src/provider/stream.rs) now detects loops of ANY byte period ≤ 200 over the last 600 tail bytes (was exactly-200 only — the 74-byte exit-note period could never fire); reasoning-channel accumulation remains open in backlog 1f56297a. Residual (accepted): a text-only suggestion steer still leaves a short system message as the last message on fold vendors (turn.rs Suggestion push site, documented there).
