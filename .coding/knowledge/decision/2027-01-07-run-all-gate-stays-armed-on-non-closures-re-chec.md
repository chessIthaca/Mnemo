+++
title = "run-all gate stays armed on non-closures — re-checks at the next resolution (96e2862a)"
created = "2027-01-07"
+++

DECISION (2027-01-07, plan 96e2862a, commits 0283504 + a6cd974 + 990d89f on wt/agenticcoding): the run-all gate KEEPS THE RUN ARMED on non-closure and turn-failure resolutions (guarded annotate with exact-repeat suppression + emit, no end_run; the next turn resolution re-checks) instead of halting permanently — mirroring the stopped-run path (b83e891f). Rationale: the agent routinely recovers on its own (auto-continue resumes mid-plan turn ends; reviewer-finish notifications resume the closing sequence) and finishes the plan; the old halt destroyed the run pointer the completing turn's resolution needed (the amplifier — live twice: a6a7727a turn-abort, 207dc316 non-closure). The single-dispatch branch restores its consumed pointer on non-closures (check-and-set) with the same annotate guard. Intervention semantics preserved: the post-resolution stopped check still ends a stopped run without dispatching. Amended specs: 2027-01-07-backlog-item-status-plan-lifecycle-failed-means.md + 2027-01-07-backlog-resolution-contract-finished-terminal-cl.md. Regression tests: non_closure_resolution_keeps_the_run_armed, turn_failure_resolution_keeps_the_run_armed, single_dispatch_non_closure_restores_the_pointer (src-tauri/src/ipc/run_all.rs tests).
