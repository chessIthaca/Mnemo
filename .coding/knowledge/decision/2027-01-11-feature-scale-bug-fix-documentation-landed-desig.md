+++
title = "feature-scale bug-fix documentation — landed_design flag + finish gate"
created = "2027-01-11"
+++

DECISION (plan 7d333b1d, backlog e5a84ce9, commit 57e2188, round-2 PASS): a bug_fixing fix that turns out FEATURE-SCALE mid-flight (new pub types / cross-module surface — plan-time steering 041dd8d0 already sends bug-triggered features to kind=implementation; this covers the contained-at-plan-time cases that grew) MUST, before the verify step: (1) amend the plan context via update_plan append with a paragraph starting "Landed design —" (architecture, deviations from the sketch, measured numbers, invariants to preserve), and (2) record landed_design=true via update_plan. At finish: write a SPEC memory (architecture + invariants) and amend the BUG record with the landed outcome, in addition to the auto-captured BUG digest. ENFORCEMENT: the bug_fixing finish gate (src/tool/workflow/plan.rs, between the regression_test presence check and the code-graph gate) blocks landed_design=true whose plan context lacks "Landed design" (case-sensitive substring on the CONTEXT field; the amendment is a prose paragraph, NOT a "## " header — the plan-file parser treats those as section starts); with the amendment present it emits a SPEC/BUG reminder note. The flag persists as the `## Landed design` plan-file section (parse accepts yes/true; serde(skip) keeps it off the wire). RATIONALE: the item's diff-threshold gate (>500 changed lines / any new pub item) was REJECTED as the heavy option (git wiring in the finish tool + a plan-schema baseline commit + numstat parsing + flaky git-state tests) — the self-reported flag catches the honest-forgetful case (sets the flag, forgets the amendment — the realistic 85368a1f failure mode) at a fraction of the cost; the never-sets-the-flag case relies on the constitution rule (agent.md '## Documentation expectations') + the skeleton verify-step instruction. Residuals (documented in the plan context + round-1 report): a sketch-context substring false positive is possible but unlikely; the SPEC/BUG amendments are not gate-verifiable (memory writes are invisible to the plan gate). Tests: landed_design_flag_roundtrips, update_plan_sets_and_unsets_landed_design, finish_blocks_landed_design_without_context_amendment, finish_accepts_landed_design_with_amendment (+2 extended).
