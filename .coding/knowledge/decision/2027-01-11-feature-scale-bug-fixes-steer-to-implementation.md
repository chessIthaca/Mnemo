+++
title = "feature-scale bug fixes steer to implementation plans"
created = "2027-01-11"
+++

DECISION (plan 041dd8d0, backlog 51ee41c1, commit eb73f9c, round-2 PASS): bug_fixing is for CONTAINED defect fixes; a bug-triggered FEATURE (the fix adds capabilities, new dependencies, or spans multiple modules) files as kind=implementation with the bug documented as motivation in goal/context. The rule is stated coherently on FIVE surfaces (keep them in sync when touching any): the compiled prompt's PLAN KIND section (src/agent/prompt.rs — the stable-head literals kind:"bug_fixing" + never "implementation" preserved, pinned by stable_head_carries_bug_fixing_habit + stable_head_carries_feature_scale_carve_out), the kind enum's schema description (src/tool/workflow/plan.rs), the run-all preamble's classification rule 5 (src-tauri/src/ipc/run_all.rs RUN_ALL_STEER — pinned by run_all_prompt_steers_defect_items_to_bug_fixing), PLAN.md's plan-kind bullet, and README's "Bugs get their own plan kind" bullet. ENFORCEMENT NET: create_plan emits an ADVISORY steering note (never a blocking ask_user — unattended run-alls must not stall; the plan still files as bug_fixing when it fires — the caller judges) when a bug_fixing payload looks feature-scale: feature_scale = detailed_steps > 4 AND referenced_module_dirs(steps+context+goal) >= 3 — the helper counts DISTINCT src/<module>/ prefixes (same-module files once; bare src/lib.rs none; src-tauri/src/... and frontend/src/... via their inner src/). The AND is deliberate: contained cross-module fixes (9441d776: 4 steps, 3 modules) and long single-module fixes (85368a1f: 4 steps, 1 module) must stay silent; the c87093c1 shape (many steps + many modules) trips both. Dependency-addition is NOT textually detected (fragile) — the prompt guidance carries that judgment. Tests: the fire path (5 steps + 3 modules → NOTE, still BugFixing), the silent path (4 steps + 1 module), and the AND-legs pin (create_plan_bug_fixing_and_legs_pin_independently: each leg suppresses the note on its own). Sibling item e5a84ce9 (feature-scale fix documentation) is complementary: this prevents the misclassification at create_plan time; e5a84ce9 handles documentation when feature-scale work lands under bug_fixing anyway. Detail: .coding/plans/041dd8d0.md + .coding/reviews/2026-09-12-feature-scale-steering-review-round2.md.
