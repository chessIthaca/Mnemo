+++
title = "Laya classifier integration — optional/opt-in, queued as a 5-item backlog chain"
created = "2027-01-11"
+++

DECISION (user-directed; queueing only, no implementation started this session). Mnemo adds Laya (https://huggingface.co/convaiinnovations/laya) as an OPTIONAL 'System 1' classifier: Apache 2.0, non-autoregressive, ~33 ms single forward pass, typed questions (choice / score / noul→yes-no) → calibrated probabilities; never generates text. Serving: laya-serve (pip install laya[serve]) exposes POST /v1/systemone with the TypeSafe-Jev request/response shape; binds 0.0.0.0:8000, no auth unless LAYA_API_KEY (Bearer). (laya[mcp] also exists if an MCP-shaped surface is ever wanted.) Design constraint: opt-in only — disabled/absent = zero behavior change; mirror the embedder pattern: Classifier trait beside src/memory/embedder.rs, build_classifier in src/provider/client_factory.rs:313, embedder_status-style IPC at src-tauri/src/ipc/state.rs:323, Laya section in Settings (sidecar download or endpoint URL + toggle).

Queued chain (backlog append order = dispatch order; all pending): 1) bb54bdcc-b692-47f2-9a72-7f0692f9f7c2 foundation (Classifier trait + Laya HTTP backend + config + IPC status). 2) 091e694d-fb75-45a5-acff-3e643841b4d6 pre-prompt model routing (src/model_resolver.rs). 3) e2c47d5f-95a3-41f9-b17b-21ae07126978 tool-choice steering replacing regex auto-delegation (src/tool/agent/search.rs, search_read.rs, codegraph.rs; plan 6b7e3be0). 4) 1a4049c1-7254-474d-b4c2-471b39eb46d2 error/flaky-test triage (MAX_RETRIES src/agent/mod.rs:52, src/agent/turn.rs:828; MAX_BAD_JSON_RETRIES). 5) a147b63c-bf52-4699-a10a-78346c6bbdcb memory auto-typing (src/tool/memory/mod.rs) — lowest stakes, noted to ship first among 2–5. Every item is confidence-gated with fallback to current behavior; 2–5 depend on 1.

Model-card limits carried in the items: base checkpoints near-chance zero-shot on custom tasks (typed-decisions 0.362 English / 0.352 multilingual vs 0.318 random, 0.461 majority; fine-tuned checkpoint 0.766) — 'a fast base to specialise, not a zero-shot decision engine'; shipped probabilities over-confident until temperature refit per (question type, option count) on own data (ECE 0.466 → 0.081); ordinal score weakest primitive; noul can follow its false:/true: labels over the state; choice degrades past ~20 options (head_max_len); repo-root checkpoint is English-only (Router auto-routes non-English to laya-multilingual).
