+++
title = "one frozen tool surface for all plan kinds + narrow landing sync tolerance (plan f69317d4)"
created = "2027-01-11"
+++

Two behavior decisions landed in plan f69317d4 (branch `wt/mnemo`, UNMERGED as of 2027-01-11; HEAD e8f5d2a, review PASS in `.coding/reviews/2026-09-14-round6-followups-review-round3.md`). Both reverse earlier documented intent on purpose — do not "restore" the old behavior.

1. **One advertised tool surface for every plan kind (R6-2, reverses the per-kind research surface of 2026-01-perf-review L4).** `Workflow::schema_filter` (src/workflow/mod.rs) returns the same `PlanFrozen` array for implementation and research plans — byte-stable for the plan's lifetime. Rationale: the tools array rides at the head of the request body, so a narrower research surface shrinking it at the plan-kind boundary reset the provider prefix cache — measured 6,016-token prompt collapse plus 15.7-17.9 s TTFT on the request after the transition, re-billing the whole conversation. Research restrictiveness is NOT lost: `ExecutingResearch` still governs `allowed_tools()`, so a research plan's write tools (`file_edit`/`file_write`) are rejected at DISPATCH (advertisement is frozen, enforcement is not). Guards: `schema_filter_is_stable_across_plan_kinds`, `research_plan_still_cannot_write_despite_the_advertised_surface`, `plan_frozen_tool_set_is_stable_within_and_across_plans`. Docs: PLAN.md ("plan-frozen advertisement"), src/tool/mod.rs (`PlanFrozen`/`ExecutingResearch` docs), docs/FEATURES.md.

2. **The app-managed landing syncs main before merging, with NARROW tolerance (follow-up B, mirrors the merge_to_main skill's step 2 from plan d826b9ad).** `src/project/worktrees.rs::land_item_branch_impl` gates on `git rev-parse --abbrev-ref --symbolic-full-name @{u}`: no upstream → skip the sync (local-only repos must never fail a landing); upstream tracked → `git fetch origin` + `git pull --no-rebase` errors SURFACE as `LandError` (an unreachable remote would otherwise land on the stale main the sync exists to prevent). Tests: `land_syncs_main_with_its_upstream_before_merging`, `land_fails_when_a_configured_remote_cannot_be_reached`, `land_still_succeeds_without_any_remote` — each proven failing with the pre-fix behavior.
