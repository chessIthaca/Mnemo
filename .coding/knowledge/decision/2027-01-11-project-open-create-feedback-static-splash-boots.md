+++
title = "project open/create feedback — static splash → BootSplash → phase-aware picker"
created = "2027-01-11"
+++

DECISION (plan 969510fc, backlog 486955d5, commits 18de87a + 0bb658a, round-3 PASS): the project open/create flow's feedback is LAYERED, each layer owning one silent window. (1) index.html static pre-React splash (inline-styled, inside #root — createRoot().render replaces it): covers first-paint → React-mount on EVERY startup and switch_project restart, so the relaunched window never flashes white. Dark-only by design (#0f172a = the dark --bg-primary default; the light-mode override is a user setting pre-JS code cannot know). Text wordmark ("mnemo"), not MnemoLogo — the static splash cannot use React components, so both twins use the text wordmark, and the static values are aligned to the React twin's Tailwind utilities (text-2xl / tracking-widest / h-5 w-5 / animate-spin / text-sm) — the parity contract is pinned in the HTML comment; keep the twins in sync when touching either. (2) BootSplash (frontend/src/components/common/BootSplash.tsx) + an App early return BEFORE the picker/error gates: covers the pre-index boot phases while getNeedsProject/getStartupSnapshot wait on the backend (build_brain blocks the setup hook until IpcState is managed — without the early return App's empty main-UI shell flashes). (3) projectPickerPhases.ts: BusyPhase = preparing | restarting | removing + pure createButtonLabel/openButtonLabel (the vitest node-env pattern has no DOM testing — phase logic is exported pure functions); ProjectPicker walks preparing → restarting on create, restarting on open, removing on remove; every error path resets; the index phase itself stays with IndexingOverlay. TESTING LESSON (round-2 LOW 1): source-contract indexOf guards must bound BOTH fall-through directions — backward via start-offset anchoring (each search starts after the previous anchor), forward via a needle UNIQUE to the target site (handleOpen's `await switchProject(path)` — the `path` arg is unique; handleCreate passes chosenPath); an unbounded forward search is satisfied by later occurrences and the guarded regression passes undetected. Detail: .coding/plans/969510fc.md + .coding/reviews/2026-09-12-project-open-progress-feedback-review-round3.md.

Amended 2027-01-11: 2027-01-11 amendment (plan bfe5198e, commit 5450ddb, round-2 PASS): the layering gained its fourth layer — the boot-window reconcile. The startup memory-index reconciliation (the one open/create phase that can exceed ~1s with no live feedback on a drifted memory DB) now renders inside the BootSplash: App passes its tracked memory://reconcile state into the boot early return, and BootSplash upgrades from the generic spinner to "Building memory index" + the shared SplashProgress bar with live N/M counts (indeterminate "…" before the first progress event; "Memory index ready" / "Memory index check failed" transient hints — the post-boot reconcile dialog still carries the summary/error). Mechanism (corrected per review round 1): the reconcile is spawned inside build_brain before it returns (src-tauri/src/main.rs:1267), and the startup checks cannot resolve before build_brain returns — the boot window always covers the reconcile's start; one that outlives it hands off to the dialog mid-run. Recorded residual: the pre-event window (authored-row migration + staleness pre-check before the first `started` event) stays uncovered — covering it needs an earlier backend event that would break the in-sync-silence contract (`started` fires only when stale, main.rs:1360-1366).
