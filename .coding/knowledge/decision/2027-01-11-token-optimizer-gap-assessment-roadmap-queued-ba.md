+++
title = "token-optimizer gap assessment — roadmap queued (backlog e4a50d22)"
created = "2027-01-11"
+++

Gap assessment vs alexgreensh/token-optimizer (audited 2026-09 from README/BENCHMARK.md) queued as backlog item e4a50d22-51c5-42fb-97dd-6b98763d04cb. Their thesis = ours (cache-hit round 4, .coding/analysis/cache-hit-4-report.md): command-output compression is 15-25% of waste; the rest is re-reads, compaction loss, behavior. Our gaps, value-ordered: (1) re-reads serve full content every call (src/tool/agent/read_files.rs — theirs: diff-only ~20%, signature skeletons 720KB→250 tokens), (2) semantic command-output compression vs our lossy truncate_tool_result_at:829 / truncate_to_token_budget:1591 in src/agent/context.rs, (3) no progressive disclosure (archive + FTS5 expand) — truncated tails lost forever, (4) no compaction survival (checkpoints + decision extraction + heuristic digest vs our one summarize shot + mechanical_compaction fallback), (5) no savings ledger (metered vs estimated per-event before/after tokens; we have record_request_stats only), (6) no quality score (fill % only, InflightBar.tsx), (7) no lean-output nudge, (8) dashboard upgrade needed (StatsView.tsx has per-model/per-day, lacks per-turn, cache hit/TTL, savings, quality overlay). Where we lead: engineered prompt-cache stable-head split (they only promise cache-safe), native tool layer (no hook processes), telemetry + pricing tables exist. Roadmap: P0 ledger → P1 archive/expand → P2 delta+skeleton reads → P3 compression families → P4 compaction checkpoints → P5 quality+nudges → P6 dashboard. Invariants: never mutate the sent prefix; archive before substitute, fail-open; flag-gated, A/B before default-on.

Amended 2027-01-11: The dashboard + per-project savings-ledger slice of this roadmap was split out as its own backlog item on 2027-01-11 at the user's request: 652ae094-ace0-4662-a177-d953567d05b1 ("Token Dashboard: per-project savings ledger + toolbar view before Memory") — a dedicated Dashboard view placed before the Memory tool in the left toolbar, plus a savings_events SQLite ledger (kinds truncation + compaction now; delta_read/skeleton/compression/archive reserved). It supersedes this roadmap's P5/P6 (e4a50d22 items 5+8); e4a50d22 remains the roadmap for the optimization levers themselves, whose phases should record savings via the ledger schema as they land. Full implementation recipe with verified anchors: .coding/plans/a990cf74.md.
