+++
title = "ttft_ms anchors at POST-send; connect_ms keeps record→headers"
created = "2027-01-11"
+++

DECISION (2027-01-11, plan 25e8771c, backlog c60869d8, commit 778427e on wt/agenticcoding, perf review 2026-09-09 L1): ttft_ms = POST-send → first streamed chunk at EVERY ttft site in both providers — the anchor `post_sent` is stamped immediately before the first `client.post` (openai: PreparedRequest→StreamTask; anthropic: local before .post; the reasoning_effort retry does NOT re-stamp, so a retry's wait lands in ttft). connect_ms KEEPS record_created → response headers: the item's instruction 2 ("Keep connect_ms as-is") won over the contradictory instruction-5 phrase — the prefill wait becomes VISIBLE in ttft rather than removed from connect, so the two buckets overlap by the prefill window BY DESIGN (the stacked phase chart double-counts it; accepted). Root cause: SSE providers deliver headers WITH the first chunk, so the old headers→first-chunk window measured ~0 (ttft 0 on 99.2% of request_stats rows). Regression tests: ttft_measures_post_send_to_first_chunk (src/provider/openai/tests.rs) + anthropic_ttft_measures_post_send_to_first_chunk (src/provider/anthropic.rs). Detail: .coding/knowledge/bug/2027-01-11-ttft-ms-dead-anchor-stamped-at-headers-arrival-n.md + the 2027-01-11 amendment in .coding/knowledge/spec/2026-12-30-trace-phase-attribution-connect-backoff-stall-sp-2.md.
