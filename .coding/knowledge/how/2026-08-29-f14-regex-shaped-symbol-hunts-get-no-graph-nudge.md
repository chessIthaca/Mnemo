+++
title = "F14 — regex-shaped symbol hunts get no graph nudge (future-round candidate)"
created = "2026-08-29"
+++

HOW (F14, steering-coverage gap — recorded 2026-09-17, findings round plan 44387fcd; NO code this round): regex-shaped symbol hunts get NO graph nudge. Evidence (this session, corroborated by the user: "another bunch of searches where symbol lookup should have helped"): `fn .*watcher|struct.*Watcher` and `struct Memory(Supersede|Update)Tool|fn name` style alternation-regex searches returned symbol definitions but fired no search→graph nudge — symbol_nudge (src/tool/agent/search.rs) covers bare identifiers and definition-prefixed names ("fn X") only; alternation patterns that reduce to identifier alternatives are invisible to it. The misses cost 4-5 round-trips that one graph_search each would have answered (recalled_context_block call sites, get_steering_stats, MemorySupersedeTool/MemoryUpdateTool, GraphWatcher). FUTURE-ROUND candidate: extend symbol_nudge to detect alternation-only regexes where every branch is an identifier or definition-prefix (e.g. `(fn|struct) .*name` or `name1|name2`), nudging with the matching indexed symbol ids; keep the budget rule (one indexed SELECT per search). Related: HOW 2026-08-29-search-glob-shapes (same evidence-driven-tally protocol); the graph-first correction is already constitutional (graph tools MANDATORY first for symbol questions) — this gap is purely the NUDGE surface, not the habit.
