+++
title = "search glob shapes — braces now expand; bare ** tails always worked"
created = "2026-08-29"
+++

HOW (durable tool gotcha, learned 2026-09-15 backlog-batch session — 5 wasted calls; CORRECTED 2026-09-17 by probe evidence during findings round F7, plan 44387fcd): the search/search_read `glob` parameter wants extension-anchored shapes. WORKING: `**/*.rs`, dir-scoped single-extension `frontend/src/**/*.css`, `.coding/plans/**/*.md`, bare filename `src/agent/prompt.rs`, and (since the F7 fix) brace alternation `**/*.{ts,tsx}` — expand_braces rewrites it into concrete alternatives (cap 32; the glob crate has no native alternation and previously matched literal braces, i.e. nothing). ALSO WORKING, and the 2026-09-15 claim against it was WRONG: bare `**` tails (`frontend/src/**`) — the 0-file observation did not reproduce; glob 0.3.4 matches them (probe: `frontend/src/**` vs `frontend/src/a.ts` = true); that session's 0 hits had some other cause. A glob that matches zero files now says so: "the glob matched no files — if that's unexpected, extension-anchored shapes like **/*.rs are safest" (F7 hint). Zero-hit content searches also fall back to filename matching (F2). Regression tests: walk_searchable_honors_bare_recursive_tails + walk_searchable_expands_brace_alternation + brace_glob_works_on_the_index_path + walk_searchable_dir_scoped_recursive_braces (the dir-scoped RECURSIVE brace shape, added 2026-09-17 during the backlog-41f39672 close-out after a live 0-file scare turned out to be a stale app binary, not a code defect) (src/tool/agent/search.rs).

Amended 2027-01-11: the zero-file hint's wording changed — the advisory shapes ("extension-anchored shapes like **/*.rs are safest") were converted to imperatives (plan 987fef4c, backlog 56168c38, "Imperative phrasing for steering notes and tool-strategy guidance"). The hint now reads "the glob matched no files — if that's unexpected, use extension-anchored shapes like **/*.rs" in BOTH src/tool/agent/search.rs (walk path) and src/tool/agent/search_read.rs; docs/FEATURES.md's quote was updated. Everything else in this record — the working glob shapes (brace alternation, bare `**` tails, dir-scoped recursive braces) and the correction that bare `**` always worked — is unchanged.
