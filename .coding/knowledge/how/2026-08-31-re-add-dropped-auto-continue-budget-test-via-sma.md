+++
title = "re-add dropped auto-continue budget test via small-increment file_edit"
created = "2026-08-31"
+++

The e2e test `prompt_resets_auto_continue_budget_after_exhaustion` + its `CountingMockProvider` mock harness (src/runtime/agent.rs, end of `mod tests`) was SUCCESSFULLY re-added on 2026-12-16 (and is now committed, part of the passing suite). It had been DROPPED twice before (per .coding/knowledge/decision/2026-08-31-auto-cont-m2-ships...md) because inline-composing the ~100-line test body in one file_edit new_string corrupted the working tree.

METHODOLOGY THAT WORKED (oracle-first, small increments — each <60-line new_string):
1. Add struct+impl ALONE first → cargo check --tests (expect dead_code error = confirms signatures correct).
2. Add the test STUB (setup + first prompt + a `// __phases_go_here__` marker + robust select!-drain cleanup) → cargo check --tests (dead_code clears, exit 0).
3. Replace the marker with the three phases (phase1 wait-for-cap, phase2 assert-parked, phase3 second-prompt wait-for-cap+1) → cargo check --tests.
4. Full `cargo test` gate.

KEY CORRECTNESS FACTS (updated 2026-12-31 plan 75deaec0 + ca7c152; reset sites updated 2027-01-07 backlog 5c33e945; unattended-Planning coverage added 2027-01-07 backlog a6a7727a): MAX_AUTO_CONTINUE=12. External input resets auto_continue_streak=0 — the Prompt arm AND (since 2027-01-07, backlog 5c33e945) the between-turn Suggestion arm: a Suggestion is external progress (a user steer or a finished descendant's completion notification, the closing sequence's ONLY resume), not mere guidance — the sibling test `suggestion_resets_auto_continue_budget_after_exhaustion` pins it (same plateau in Reviewing, then a Suggestion → cap+2 = 15 calls). Auto-continue None-arm (run_turn_with_retry): `if streak < MAX && workflow_expects_progress(unattended) && !has_running_descendants() && !is_subagent() { streak+=1; continue }` else parks — the gate covers Executing AND Reviewing (renamed from is_workflow_executing by plan 75deaec0; the descendant park was added by bbd712c9), so a Reviewing parent with NO running descendants self-resumes, while Complete/Skill park; Planning parks when INTERACTIVE (a deliberate turn end there is usually a question-wait — auto-continuing would self-answer it) but auto-continues when UNATTENDED (run-all dispatched: the Prompt embedded UNATTENDED_PROMPT_PREFIX, the core constant run_all_prompt builds the steer with; the AgentTask re-derives `unattended` per Prompt — assignment, so a plain user prompt clears it; backlog a6a7727a, 2027-01-07) and a parent with running descendants parks (the child-completion Suggestion resumes it). SUBAGENTS ALWAYS PARK on a normal turn end (single-task; the child-completion Suggestion resumes the parent) — the !is_subagent guard was added by ca7c152 (High 1 of the 2026-12-31 auto-continue-reviewing review: without it a finished reviewer self-continued the full 12-turn plateau, because Workflow::load_latest derives the subagent's state from the shared main plan). Plateau = MAX+1 = 13 provider calls then park; second prompt → call 14 (cap+1). Unattended-Planning tests (2027-01-07, backlog a6a7727a): `unattended_planning_auto_continues` (fresh Planning workflow + prefixed prompt → chain fires), `interactive_planning_parks_with_no_work_expected` (plain prompt → 1 call + NoWorkExpected park, streak 0), `plain_prompt_clears_unattended_mode` (chain → plateau BudgetExhausted park at cap, THEN the plain prompt — sent while IDLE; a mid-chain prompt is folded into the running turn as a steer, bypassing the Prompt arm — runs cap+1 and parks NoWorkExpected). Workflow state for the tests: Executing via create_plan, or Reviewing via create_plan_with_kind(Implementation) + complete_step(0) (the workflow-level index is 0-based; the IPC tool converts 1→0).

DEADLOCK PREVENTION: drain fanin_rx (cap 64) via try_recv inside EVERY wait-loop iteration; cleanup uses `tokio::select!` racing `&mut handle` against a 5ms sleep that drains — robust against any event count (the agent keeps auto-continuing after phase-3 break, to call 26, before parking+exiting on the closed cmd channel).

DUAL-HANDLE IDIOM (mandatory): `let calls = Arc::new(AtomicUsize::new(0))` then inline `Arc::new(CountingMockProvider{ calls: Arc::clone(&calls), .. })` AT the provider field position. Binding a concrete Arc then Arc::clone-ing into the field hits E0308 (unsize-coercion doesn't fire there for AgentLoopConfig).
