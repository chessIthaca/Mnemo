+++
title = "reportless reviewer failure on GLM — retry with emission-hazard guidance (plan eab99bc4)"
created = "2026-12-31"
+++

On 2026-01-03, a role:"reviewer" spawn for plan eab99bc4 (stream guard debug telemetry) finished WITHOUT writing any report — no file in .coding/reviews/ matched the plan id or the changed symbols (apply_stream_guard, GuardCutDetails), and no memory trace existed. Likely cause: the same GLM boundary-tag emission fragility that stalled the main session's own turns — the reviewer reads guard code saturated with raw tag literals, and emitting them cuts the turn (the very bug under review). Protocol followed: no blind respawn; ask_user offered retry-on-another-model / retry-same-model / abandon; the user chose same-model. The retry task added an EMISSION HAZARD note — "do NOT quote the raw GLM boundary-tag literals verbatim anywhere in your output; refer to them as 'the four GLM boundary tags'; keep the report concise" — and the retry succeeded, writing a full report (round-1 FINDINGS 0 high 2 low, then round-2 PASS after fixes). Reusable lesson: when a reviewer dies reportless on GLM, retry with the emission-hazard guidance baked into the task text. Note: the plan's own diff was tag-free (new tests used <custom_stop> placeholders) — only pre-existing surrounding code carries the literals, so the hazard is in what the reviewer READS and might echo, not in the change itself.
