+++
title = "indexing progress overlay on project open (bar + counter)"
supersedes = "2026-08-27-indexing-progress-overlay-on-project-open-bar-co"
created = "2026-08-27"
status = "superseded"
+++

SPEC: Indexing progress overlay on project open — MERGED into main at d0438848 (2026-08-27, plan fad07509, backlog 4f87731f done). Tauri stream `codegraph://index-progress` (IndexProgressEvent in src-tauri/src/ipc/codegraph_cmds.rs: started/progress{done,total}/done/failed, "type"-tagged kebab-case, pinned by index_progress_event_wire_shape). Emitters: startup pass (main.rs build_brain_inner — 1s gate via startup_should_forward + PROGRESS_EVERY=50 throttle, terminal event only if ≥1 tick forwarded, so already-indexed projects stay silent) and create_project seed pass (immediate Started, no gate). seed_stores takes Option<&dyn Fn(usize,usize)>. Frontend: IndexingOverlay.tsx (self-subscribing, pure applyIndexProgress reducer, useBrowserOverlay when visible), mounted in App.tsx normal tree + ProjectPicker STARTUP branch only (exactly one live instance — a second in switch-mode stacked the failed card, review LOW 1). Frontend test files MUST be added to frontend/vitest.config.ts include (it replaces default discovery — silent test loss otherwise, review HIGH 1). 2026-08-28 (plan ef6828ba, branch wt/agenticcoder, NOT yet merged): BOTH startup dialogs (IndexingOverlay + the App.tsx memory://reconcile dialog) rebuilt on frontend/src/components/common/SplashCard.tsx — branded splash band (MnemoLogo badge + "mnemo" wordmark + "SMART CODING HARNESS" + "MEMORY. SPEC. IMPROVEMENT." over an inline-SVG network motif in the icon-source.svg palette: navy #050A16→#0D1B33 gradient, edges #2E4A8F, nodes #4C7DFF/#587CFF/#6D8BFF; def ids namespaced via useId) + shared SplashProgress block (label + mono counter + cyan bar; pct=null renders the indeterminate 30% stub). The verbose body paragraphs ("on-disk knowledge changed…"/"First-time indexing…") were dropped per user request. App.tsx reconcile dialog now calls useBrowserOverlay(reconcile !== null) with the top-level hooks (closes the pre-existing punch-through gap flagged in the overlay review). Pin suite: frontend/src/components/common/SplashCard.test.tsx (brand text, palette, per-instance id namespacing, children slot, SplashProgress pct/null).
