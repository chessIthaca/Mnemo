+++
title = "Trace stats legend chips = per-chart series toggles (commit 016ab80)"
created = "2026-08-27"
+++

SPEC: Trace-stats legend chips are per-chart series toggles (commit 016ab80 on wt/agenticcoder, plan 195b2eb1; not yet merged to main). frontend/src/components/views/TraceStats.tsx + frontend/src/lib/traceStats.ts: every legend chip (7 phase segments; 4 token entries incl. cached) is a click-toggle — aria-pressed button, off = faded swatch + line-through. Hidden keys persist per chart in localStorage `tracestats.hiddenSegments` ({"phase":[],"token":[]} sparse HIDDEN-key lists; corrupt-JSON tolerant; future segments default visible). Column heights AND maxima recompute over visible series only (stackTotal over chartVisibility map) so hiding a dominant segment rescales the rest (Relative mode). `cached` is a NON-ADDITIVE overlay: tokenHeightVisibility() always excludes it from height sums (chip toggle height-neutral; hiding prompt leaves completion+reasoning; review High 1). All stack-series hidden → "All series hidden" hint (every-based). tokenStackTotal + allSegmentsVisible deleted (divergent duplicates, review Low 4); tokenTipRows derives colors from TOKEN_SEGMENTS. Docs: README.md Trace bullet + .coding/llm-trace.md stats-strip paragraph. Reviews: .coding/reviews/2026-12-trace-legend-toggles.md (FINDINGS 1 high, 4 low) and 2026-12-trace-legend-toggles-verify.md (PASS).
