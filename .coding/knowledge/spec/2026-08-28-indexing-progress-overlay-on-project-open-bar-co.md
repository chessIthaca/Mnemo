+++
title = "indexing progress overlay on project open (bar + counter)"
supersedes = "2026-08-27-indexing-progress-overlay-on-project-open-bar-co-3"
created = "2026-08-28"
+++

SPEC: Indexing progress overlay on project open — now covers app start, project CREATE, and project SWITCH (plan eedeaeab, wt/agenticcoder; supersedes the cold-start-only gate design of the -3 record / file …-co-3.md). Stream `codegraph://index-progress` (IndexProgressEvent, src-tauri/src/ipc/codegraph_cmds.rs: started/progress{done,total}/done/failed, "type"-tagged kebab-case, pinned by index_progress_event_wire_shape). Emitters: (1) STARTUP pass (main.rs build_brain_inner): switched_open captured where take_pending_project() is consumed; callback records EVERY tick into the module snapshot BEFORE the gate; startup_should_forward(elapsed, done, total, switched) — 1s gate SKIPPED when switched=true, PROGRESS_EVERY=50 throttle always; switched launch emits Started immediately + forwarded flag starts true. TERMINALS: Done ALWAYS emitted (a lone Done folds to null — cold starts stay flash-free, late overlays get a definitive end); Failed forwarded-gated only. Snapshot visibility (H2, 2026-08-28 review): mark_startup_index_active(switched) publishes at mark time ONLY when switched=true; cold passes publish on the first FORWARDED tick via ensure_startup_index_visible() — a gated sub-second cold pass NEVER exposes the snapshot (no-flash contract; pinned by startup_snapshot_records_and_clears, which fails without the switch-aware design). (2) create_project seed pass unchanged (immediate Started, no gate, never touches the snapshot). Catch-up: `get_index_progress() -> Option<IndexProgressSnapshot{done,total}>` (registered in main.rs invoke_handler; wire pinned by index_progress_snapshot_wire_shape; done.min(total) clamp against torn Relaxed reads). Frontend IndexingOverlay.tsx: subscribes FIRST, then getIndexProgress() seeds via setState(prev => prev ?? snap) with a TOKEN GUARD (bails when any event arrived during the fetch — events are fresher than the snapshot; H3) + disposed check; MIN_VISIBLE_MS=800 + pure hideDelayMs(shownAt, now) hold on `done` (token-guarded timer, newer events cancel pending hides); reducer applyIndexProgress unchanged; exactly one live mount (App.tsx tree + ProjectPicker STARTUP branch). Pin suite: frontend/src/components/projects/indexingOverlay.test.ts (extends the EXISTING registered file; new test FILES must still be added to frontend/vitest.config.ts include). README.md:52 updated ("emits nothing visible" on cold sub-second passes). Review: .coding/reviews/2026-08-28-switch-indexing-overlay-review.md (H1 always-Done, H2 snapshot visibility, H3 token guard, L4 clamp, L6 test title — all fixed).
