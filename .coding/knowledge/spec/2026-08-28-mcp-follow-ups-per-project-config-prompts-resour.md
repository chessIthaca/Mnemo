+++
title = "MCP follow-ups — per-project config, prompts/resources/sampling, OAuth"
created = "2026-08-28"
+++

SPEC: MCP follow-ups batch (plan 844180a8, commit 4601008 on wt/agenticcoder, NOT yet merged) — three follow-ups on top of MCP v1. (1) PER-PROJECT CONFIG: optional `.coding/mcp.toml` union-merged over global `~/.mnemo/mcp.toml` — merge_servers (project wins by name, non-colliding globals kept); Settings → MCP tags each server with source ("global"/"project", McpServerWire flatten) and a save splits by source into both files; restore_shadowed_globals keeps a project override from deleting the global twin it shadows (review HIGH 1); purely-global saves no longer create an empty project file (LOW 5). (2) PROMPTS/RESOURCES/SAMPLING: ServerCapabilities parsed from initialize gates per-server meta tools mcp__<server>__get_prompt / __read_resource (registered at reveal, snapshot listings capped 20); stdio reader task shares the child stdin and declines server→client requests with JSON-RPC -32601 — sampling/createMessage answered rather than hanging the server; HTTP opens no server→client stream (documented). (3) OAUTH 2.1 for remote servers: def gains auth="oauth" + client_id + scopes (validated: remote-only, client_id required, no dynamic client registration); src/mcp/oauth.rs — PKCE S256 (sha2 dep added), RFC 8414 discovery, loopback redirect with state validation, tokens in keys.toml under sanitized mcp-<name> keys as JSON blobs (McpTokenStore), ORIGIN-BOUND (re-pointing url invalidates tokens — review LOW 3), Bearer per request, automatic 401 refresh retry-once, percent-decoded code (LOW 4); ipc mcp_oauth_start returns the authorize URL for Settings Connect. save_all preserves on-disk mcp-* keys so tokens survive unrelated settings saves (review HIGH 2). Reviews: round-1 FINDINGS 2 high/3 low (all fixed) + round-2 PASS; suites green (cargo 1615, frontend 671).
