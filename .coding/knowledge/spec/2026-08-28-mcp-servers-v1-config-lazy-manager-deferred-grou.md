+++
title = "MCP servers v1 — config, lazy manager, deferred groups, approval gating"
created = "2026-08-28"
+++

SPEC: MCP servers v1 (plan b277ebfc, commit 8bb01f3 on wt/agenticcoder, NOT yet merged to main) — how MCP works in Mnemo. STORAGE: ~/.mnemo/mcp.toml, [[server]] entries (name + enabled + exactly one transport: stdio command/args/env or remote url/headers_env); env entries are variable NAMES only — values resolve from the environment at connect time, so the file is safe to log; names reject whitespace (unrevealable group) and '__' (mcp__ aliasing). CLIENT: in-crate JSON-RPC (initialize/tools.list/tools.call) over stdio (tokio::process, kill_on_drop, hung-server kill, reader-task id routing) + streamable HTTP (reqwest, Mcp-Session-Id, SSE frame parsing) — rmcp was evaluated and REJECTED (zero new deps for three calls). RUNTIME: src/mcp McpManager — inert at construction, lazy per-server connections, cached tool lists, transparent respawn after failure, call() re-ensures instead of panicking on the concurrent-removal race (review LOW 1), replace_servers for live Settings saves. SURFACE: each enabled server = one deferred group mcp.<server> (deferred_groups_with extends the static browser/image table — built-ins untouched); load_tools reveals by connecting + materializing McpTool adapters (mcp__<server>__<tool>, inputSchema passthrough, capped output) into the registry's ToolSlot (shared dynamic store; ToolRegistry holds Arc<dyn Tool> + consults the slot in get/iter/schemas/dispatch). GATING: every MCP tool is Agent-category + NeedsApproval → visible Executing/Reviewing, hidden Planning/Complete; research excludes by mcp__ name prefix; reviewer allow-list never includes them; dispatch denies the REVEAL itself in blocked states (review LOW 2) so no foreign process spawns unusably. SETTINGS: Settings → MCP (list/edit/test/save; test = 45s-bounded connect + tool list; saves apply to agents built afterwards — restart always picks up). Reviews: 2026-02-13-mcp-servers-v1-review.md (0 high/3 low, fixed) + -verify-review.md (PASS). Deferred to backlog: per-project mcp.toml, prompts/resources/sampling, OAuth, per-server trust, connection-health UX.

Amended 2027-01-11: The mcp.* reveal state gate is LIFTED (user request 2027-02-05 — session-recorded; the Amended-stamp clock lags session dates, a known skew — backlog 77efc1b7, plan 402cc091): mcp_reveal_allowed and its dispatch enforcement are deleted — load_tools(group) now succeeds in every workflow state (Planning/Complete/research included), and load_tools also rides the ToolFilter::Skill always-available set so a skill whose allow-list names deferred tools (browser/image/mcp) can reveal them. The revealed McpTools stay filter-gated exactly as before (mcp__ names stay excluded in Planning/Complete/research; the reviewer never gets load_tools) — only the reveal/listing connection is now allowed everywhere, which the user explicitly accepted over the LOW-2 spawned-connection concern. Pinned by tool::tests::load_tools_allowed_in_every_state_and_inside_skills.
