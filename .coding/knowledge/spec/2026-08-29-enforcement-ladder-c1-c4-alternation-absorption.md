+++
title = "enforcement ladder C1–C4 — alternation absorption, literal retry hint, nudge escalation, redirection warning"
created = "2026-08-29"
status = "superseded"
+++

SPEC (enforcement ladder C1–C4, plan e43684e7, branch wt/agenticcoder, 2026-09-17; C3 amended 2026-12-04 plan c5cc9a84): advisory escalation for the 2026-09-17 tally's own waste classes — never gates. (C1/F14) symbol_nudge alternation absorption: alternation_identifier_branches (top-level | split, 2–3 branches, EVERY branch must reduce via reduce_alternation_branch — bare / definition-prefixed / wildcard-decorated / keyword±space+`.*` shapes; the `.*`-after-keyword requirement keeps `classic` from reducing) + alternation_nudge resolving each branch LOOSELY via the new Store::symbol_id_fuzzy (exact → case-insensitive → shortest case-insensitive substring using instr — no LIKE wildcard pitfalls; CodeGraph::symbol_id_fuzzy delegate), dedup by id, every entry carrying the SEARCH_NUDGE_MARK substring; symbol_nudge's None arm delegates to it. (C2) has_regex_metachars (is_index_eligible_regex deduped onto it) + literal_metachar_hint — appended to EMPTY literal-mode results only, on both engines, in search AND search_read. (C3) SteeringStats escalation: per-agent agent_fired/agent_switched [u64;9] maps, ESCALATION_THRESHOLD=2 (amended 2026-12-04 from 3 — two ignored fires is already a habit, not noise; the escalation note was also rewritten from permissive "consider following it ... or deliberately ignoring it" to directive "follow it (its named target tools) on the next call; each repeat wastes a round-trip the target tools would answer directly"), one-shot per (agent, kind), cancelled by any switch, pending_escalations drained by take_escalations; the dispatch funnel's attach_escalations appends to SUCCESS results only, AFTER observe_result (the note phrasing carries no marker substring so it never self-counts). (C4) shell.rs REDIRECT_NOTE + has_blinding_redirection (>$null / >/dev/null / >nul) prepended at the GREP_NUDGE point (stacked leading lines); MarkerKind::ShellRedirect — 9th kind, fired-only, shell+first-line detect, label "shell-redirect". Regression tests: alternation_symbol_hunts_absorb_the_graph_lookup; literal_metachar_zero_hits_get_the_retry_hint (+ the search_read variant); redirection_detector_matches_null_sinks_only; redirected_output_carries_the_warning; repeat_ignored_actionable_nudges_escalate_once; escalation_notes_attach_to_success_only; steering MATRIX now 7 rows. RATIONALE (per the tally analysis): gates stay reserved for expensive/irreversible wrongs; memory nudges remain fired-only; grep is never gated behind graph. File: .coding/knowledge/spec/2026-08-29-enforcement-ladder-c1-c4-alternation-absorption.md
