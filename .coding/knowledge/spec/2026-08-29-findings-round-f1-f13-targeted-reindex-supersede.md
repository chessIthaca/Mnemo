+++
title = "findings round F1–F13 — targeted reindex, supersede pointers, glob shapes, index freshness, filename fallback, rider boost, memory nudges"
created = "2026-08-29"
+++

SPEC (findings round F1–F13, plan 44387fcd, branch wt/agenticcoder, 2026-09-17). SHIPPED — (F1) reindex_knowledge_files (src/memory/indexer.rs) resolves `supersedes` against the on-disk corpus: predecessor-context expansion (predecessor_rel probe — successor's type dir first, then any type dir; chains to a fixed point; MAX_PREDECESSOR_CONTEXT=64) + conservative post-loop set_derived_metadata reconciliation (computed real-successor wins; a computed sentinel/None never regresses a row's more specific existing value). (F12) MemorySupersedeTool rewrites stale hand-written `File:` pointer lines to the canonical new_rel (reconcile_file_pointers + KnowledgeStore::update); the finding's "successor slug uses the predecessor's created date" claim was DISPROVEN by evidence (git + front matter: the 2026-08-29-prefixed successor was hand-authored; KnowledgeStore::supersede uses fmt_date(now()) — guarded by the two-clock test supersede_dates_the_successor_from_the_clock_not_the_predecessor). (F7) search.rs expand_braces/compile_globs (cap 32) — brace globs work on walk AND index engines; zero-file glob hint when files_searched==0 && glob given; bare-`**` tails VERIFIED always worked (glob 0.3.4 probe) — the knowledge-file claim was corrected in place. (F10) try_index freshness check (FtsOutcome::Stale) via stored_mtimes (cg_files.mtime, unix seconds — cg_files already carried mtime, NO schema change needed) → walk fallback + "content index stale for N file(s)" note. (F2) filename_matches/filename_hint shared fallback on zero content hits + CurrentPlanTool reports the active plan's on-disk file (.coding/plans/<id>.md, existence-checked). (F8) recalled_context_block gained title: Option<&str> — RIDER_CANDIDATES=8 fetch, title_terms stable promotion, BROAD_HITS=3 cap; create_plan passes Some(title), spawn_agent None. (F9) known_memory_note/backlog_id_shape (uuid-shaped only — commit-sha residual documented) + merged_note 4th param + MarkerKind::KnownMemoryHit (fired-only). (F11) consolidation_due_note (threshold 15, once-per-session static gate) appended by AgentLoop::with_consolidation_note at both dispatch funnel returns before observe_result + MarkerKind::ConsolidationDue (fired-only, full-output detect). F13 VERIFICATION: full suite green (1682 passed, 0 failed) exercising all 8 marker kinds; the live Trace-tab counter read was NOT reachable this session (the app serves bundled assets — no dev server for the child webview — and the running process predates the new code), so live-counter confirmation defers to the next session after restart; session-observed firings recorded directly in this session's outputs: search-nudge, literal-tip, recall-rider, shell-tip. F14 (regex-shaped symbol hunts get no graph nudge) recorded as HOW for a future round — no code. Regression tests: reindex_knowledge_files_resolves_supersedes_against_the_corpus; knowledge_wired_write_lands_in_a_file_and_derived_row (F1 block); supersede_dates_the_successor_from_the_clock_not_the_predecessor; reconcile_file_pointers_rewrites_only_stale_leading_pointers; supersede_rewrites_stale_file_pointers_to_the_canonical_path; walk_searchable_honors_bare_recursive_tails; walk_searchable_expands_brace_alternation; brace_glob_works_on_the_index_path; stale_index_serves_fresh_walk_results; no_content_matches_falls_back_to_filename_matches; filename_fallback_hints_when_only_paths_match; current_plan_reports_the_plan_file_path; title_term_overlap_promotes_an_on_point_hit; known_backlog_id_earns_the_memory_note; consolidation_note_fires_once_at_the_threshold.
