+++
title = "steering round 3 — tool-scoped detection, 14-day rider age gate, expandable memory_search hits"
created = "2026-08-29"
+++

SPEC (2026-09-15, plan 0fa048a9, commits c40d6b7 + ade4ce4 on wt/agenticcoder, round-2 verify PASS; extends the round-2 steering spec 1c73bfdc — nudge wording/id embedding unchanged): THREE behavior changes. (1) Steering-stats detection is TOOL-SCOPED — MarkerKind::detect(tool_name, output) gates each of the 5 markers to its owning tool (SearchNudge: search/search_read + first-line "is an indexed symbol"; ShellTip: shell + first-line; GraphMiss: 4 graph_* tools, contains in pretty-JSON; ReadNudge: read_files starts_with("SYMBOL NUDGE:"); RecallRider: create_plan only — spawn_agent's rider rides the TASK, not a result, so never counted); observe_result(agent_id, tool_name, output) — both dispatch_with_interrupt arms pass parsed_call.name; a content-bearing payload quoting a marker can no longer fire (review 2026-09-14 finding 3's real fix); first-match-wins MARKERS comment retired (co-occurrence impossible by construction). Counters remain advisory-signal, not measurement. (2) RECALLED CONTEXT riders age-gate: src/tool/steering.rs STEERING_MAX_AGE_DAYS=14, broad-query hits dropped when created_at < now-14d (>= boundary kept), bug-pass (record_type Bug) hits exempt by construction (gate runs before bug-append); injected-clock recalled_context_block_at for tests; spawn_agent's SPEC test seed changed 1_000_000_000 → SystemTime::now (bug-typed plan.rs seed unchanged, rides via bug pass). (3) memory_search activity cards expand their per-hit list (tier · title · score) — parseMemoryEntry returns hits[] (global line regex incl. [superseded] + legacy no-id lines), reduceToolResult attaches non-empty lists, MemoryEntryCard gates expansion on hits.length (no longer auto-recall-only) — backlog 41f39672 (close after rebuild). Related: DECISION record 2026-08-29-recall-rider-vs-explicit-memory-search-measureme (do NOT de-emphasize explicit memory_search until ~2 wks of tool-scoped counter data; HOW memories carry the session's search-usage tally for the optimization analysis).
