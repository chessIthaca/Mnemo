+++
title = "announced tool calls always get a terminal ToolResult — MERGED into main (7f4a1c1)"
supersedes = "2026-08-30-announced-tool-calls-always-get-a-terminal-toolr"
created = "2026-08-30"
+++

SPEC (backlog 63cbc20f, plans 24d2b6b9/b840f00c) — MERGED into main at 7f4a1c1 (2026-09-18, merge_to_main skill), branch wt/agenticcoder deleted (tip 862f453). Every tool call announced to the UI (AgentEvent::ToolCallStart forwarded mid-stream) now gets a terminal ToolResult even when the turn is cut or retried: (1) turn.rs mid-stream stop block (steer/interrupt/compact/clear) emits UI-only ToolResult::error("interrupted: not run (turn stopped)") per accumulated call BEFORE Finished — no Role::Tool history pushes (partial assistant message records tool_calls: vec![]; orphan tool messages provider-rejected). (2) Bad-JSON arms: retry emits "arguments malformed or truncated — not run; retrying" per call (turn keeps running); cap arm emits bare "not run" before final Error — UI-only, count toward the FRONTEND doom streak by design, invisible to backend MAX_RETRIES. (3) Frontend sweepRunningCards(transcript, note) finalizes tool calls with result===null, running memory entries, running vision entries ("(interrupted)") — wired into reduceFinished, reduceError (final arm only), reduceChildFinished; a late real tool_result still overwrites the placeholder (reduceToolResult matches by id). Regression pins: mid_stream_steer_emits_not_run_result_for_announced_tool_call, bad_json_tool_call_emits_tool_result_event, bad_json_aborts_at_higher_cap (7-retry/1-cap payload counts), + 4 frontend sweep tests. Reviewed PASS in 3 rounds (round3: .coding/reviews/2026-09-18-tool-cards-stuck-running-63cbc20f-review-round3.md).
