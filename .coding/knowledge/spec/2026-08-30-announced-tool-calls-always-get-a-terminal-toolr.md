+++
title = "announced tool calls always get a terminal ToolResult (mid-stream cuts, bad-JSON retries, frontend terminal-event sweep)"
created = "2026-08-30"
status = "superseded"
+++

SPEC (backlog 63cbc20f, plans 24d2b6b9/b840f00c, shipped on wt/agenticcoder as 6b707b9 + 5c798cc, 2026-09-18, NOT yet merged): every tool call announced to the UI (AgentEvent::ToolCallStart is forwarded mid-stream) now gets a terminal ToolResult even when the turn is cut or retried. (1) turn.rs mid-stream stop block (steer/interrupt/compact/clear): finalizes acc and emits UI-only ToolResult::error("interrupted: not run (turn stopped)") per announced call BEFORE Finished — NO Role::Tool history pushes (the partial assistant message records tool_calls: vec![]; orphan tool messages are provider-rejected). (2) Bad-JSON arms: retry arm emits "arguments malformed or truncated — not run; retrying" per call (turn keeps running), cap arm emits the bare "not run" before the final Error — UI-only, count toward the FRONTEND doom streak by design, invisible to backend MAX_RETRIES. (3) Frontend sweepRunningCards(transcript, note) in agentEventReducer.ts — finalizes tool calls with result===null, running memory entries, running vision entries with "(interrupted)" — wired into reduceFinished, reduceError (final arm ONLY; retrying errors keep cards alive), reduceChildFinished; a late real tool_result still overwrites the placeholder (reduceToolResult matches by id). Regression pins: mid_stream_steer_emits_not_run_result_for_announced_tool_call, bad_json_tool_call_emits_tool_result_event, bad_json_aborts_at_higher_cap (payload-discriminating 7-retry/1-cap counts) + 4 frontend sweep tests. Reviewed PASS in 3 rounds (.coding/reviews/2026-09-18-tool-cards-stuck-running-63cbc20f-review-round3.md).
