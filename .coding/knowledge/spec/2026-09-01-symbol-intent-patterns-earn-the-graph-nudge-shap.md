+++
title = "symbol-intent patterns earn the graph nudge — shapes, guards, and the graph-first contract"
created = "2026-09-01"
+++

SPEC (plan 08eb13d8, backlog 8b8f40d2, commits 45d7905 + 3e44aed on wt/agenticcoder): the symbol-lookup nudge now covers symbol-INTENT patterns, not just exact shapes.

MECHANISM (src/tool/agent/search.rs): symbol_nudge resolves candidates in order — definition-prefix (`fn X`) → bare identifier → NEW strip_symbol_intent shapes → alternation fallback (C1, unchanged). Intent shapes: trailing `(` (`foo(` — call-site hunt, fires even on the broken-regex literal-fallback path via merged_note), `::`-paths (full form first, then last segment; guard allows alnum/_/:/./), and phrasing ("callers of X" / "who calls X" / "who uses X" / "where is X defined" with optional " defined"/" used"/" called" qualifiers). EVERY extracted name still passes the is_bare_identifier gate + the EXACT case-sensitive CodeGraph::symbol_id lookup — non-symbol targets ("who calls the shots") stay silent. The C5 intercept gate (dispatch.rs symbol_search_redirect) calls symbol_nudge directly, so intent shapes inherit escalation + interception automatically (no separate shape list).

CONTRACTS: both note variants keep the "is an indexed symbol" marker substring; the bare-identifier sentence stays byte-pinned (full-sentence contains-assertion added 2026-12-06 per review F2). search/search_read DESCRIPTIONs, the search.rs module doc, README:53, and PLAN.md:102-110 all state: symbol questions → graph_search/graph_context FIRST; search is for text. The escaped `foo\(` form deliberately does NOT nudge (backslash fails the identifier gate — pinned by the existing negative test). Procedural side recorded separately: HOW "symbol lookups lead with graph_search → graph_context — search is for text". Round-2 PASS: .coding/reviews/2026-12-06-symbol-intent-nudge-review-pass2.md.

AMENDMENT (2026-12-29, backlog b804012f, plan 7b77cf0c): the nudge is now the FALLBACK — an exact single-name hit AUTO-DELEGATES (`symbol_delegation_block`, codegraph.rs): the graph answer rides inline (definition + top callers/callees + the graph_context pointer), the walk is skipped, and the block's first line still carries the "is an indexed symbol" marker (delegated answers count as SearchNudge fires AND switches — observe_result auto-switches on the "AUTO-DELEGATED" first line — so the C5 gate never punishes delegation). The advisory note fires on the escape repeat (re-issuing the same query runs the plain search + this nudge — the byte-pin assertions live in that phase of the same tests) and for alternation branches that resolve only fuzzily. Memory-targeted queries (knowledge/reviews globs, typed prefixes) delegate to `memory_search` the same way (`memory_delegation_block`).

Amended 2027-01-11 (plan 987fef4c, backlog 56168c38 — "Imperative phrasing for steering notes and tool-strategy guidance"): the nudge SENTENCE TAILS are now imperatives. The bare-identifier branch reads "'{pattern}' is an indexed symbol — graph_context(id=\"{id}\") gives its definition + callers in one call; use graph_search/graph_context for symbol lookups — use search only for text" (the fuzzy/definition-prefixed branch: "the symbol '{name}' (from pattern '{pattern}') is an indexed symbol — … same tail"); the alternation note ends "; use graph_search/graph_context for symbol hunts — use search only for text". UNCHANGED and still contract: every marker substring (SEARCH_NUDGE_MARK "is an indexed symbol"), the embedded id + graph_context(id=…) pointer, the alternation prefix "from the alternation pattern", the per-entry shapes, the re-issue escape-hatch line, and the metric/label wiring (src/agent/steering_stats.rs MarkerKind). The old tails ("prefer the graph tools for symbol lookups (search is for text)" / "for symbol hunts (search is for text)") are gone; the sentence's full-output byte pin moved with them (search.rs bare_identifier_symbol_searches_earn_the_graph_nudge). The frontend display filter (frontend/src/lib/delegationNotes.ts, plan 3ae367bd) keys on the unchanged phrases, so per-kind toggles are unaffected.
