+++
title = "file_edit hardening — freshness contract (gate threshold 1) + emission-artifact validation + delivered-args tap + gate telemetry + size advisory"
supersedes = "2026-12-23-file-edit-stale-read-steering-fresh-read-nudge-t"
created = "2026-12-23"
+++

SPEC: file_edit hardening — freshness contract + emission-artifact validation (supersedes the threshold-2 gate, backlog 714196da tightened by e8b39d72, plan 48b36dc3 on wt/agenticcoder 2026-12-24).

(1) FRESHNESS CONTRACT (H3): every drift-class file_edit failure (old_string not found / fuzzy miss / regex miss / past-EOF / empty file) carries the fresh-read nudge (EDIT_STALE_READ_MARK); the EditStaleRead gate now intercepts the next blind edit after a SINGLE failure with zero reads (EDIT_GATE_THRESHOLD = 1 — the contract "any drift failure forces a read before the next edit", NOT timeout-after-three). The C3 escalation note and the symbol-search gate keep ESCALATION_THRESHOLD = 2 (patience dials).
(2) EMISSION-ARTIFACT VALIDATION (H2): validate_emission_artifacts runs in ALL THREE prepare tails (lines/regex/literal) and rejects pre-write: '## ' line starts in code files (rs/ts/tsx/js/json/toml), sentinel whole-values ('unused'/'placeholder'), adjacent byte-identical doc lines (/// //!), a delimiter-balance regression for .rs (delta-scoped: deficit(new) > deficit(content); scoped lexer skips strings/raw strings/chars/comments). Error = InvalidInput with the escape-hatch message (re-emit smaller fragments / file_write) — deliberately NOT the drift nudge (a re-read cannot fix a corrupted emission).
(3) DELIVERED-ARGS TAP (H1): LlmRequestRecord.raw_tool_calls (id/name/arguments verbatim post-JSON-parse pre-normalization; 4 KiB/call char-boundary-safe, 32 calls; budget-exempt — survives response_raw eviction), stamped via provider.record_raw_tool_calls (client's own last_record_id, never newest-in-ring) from turn.rs right after acc.finalize() BEFORE the bad-JSON filter. The Trace panel renders it ("Delivered tool calls") — the generation-vs-harness discriminator: compare against executed/normalized args to separate model-emission decay from harness mutation.
(4) GATE TELEMETRY (H4): SteeringStats.note_edit_gated incremented by file_edit_redirect on real interceptions; MarkerCounts.gated (per kind; zero for kinds without a gate) in the snapshot → types.ts SteeringMarkerCounts.gated → the Trace panel's marker table ("N gated"). The interception still never feeds fired.
(5) SIZE ADVISORY (H5): max(old,new).len() >= 700 (EMISSION_FRAGILITY_PAYLOAD_BYTES — the observed fragility point) appends a NOTE to the success result steering toward smaller fragments / file_write. Advisory only.
Tests: emission_artifacts_* (file_edit.rs), should_gate_file_edit_* (steering_stats.rs, Some(1) at 1 fire), file_edit_redirect_intercepts_* (dispatch.rs, gated counts), note_edit_gated_counts_in_snapshot, set_raw_tool_calls_* / record_raw_tool_calls_* (trace.rs/openai.rs), large_payload_note_fires_at_threshold. Suites: cargo 1883+16 / vitest 727 (51 files), 0 failed.
