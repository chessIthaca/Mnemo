+++
title = "file_edit stale-read steering — fresh-read nudge + third-attempt interception gate"
created = "2026-12-23"
status = "superseded"
+++

SPEC: file_edit drift-class failures steer to a fresh read, and the third blind attempt is intercepted (user directive, backlog 714196da, plan ab6c9829, commit 5c8bac0 on wt/agenticcoder 2026-12-23). Contract: the five drift-class file_edit errors (old_string not found in literal/fuzzy/regex mode, line-range start past EOF, empty file — file_edit.rs with_fresh_read_nudge) carry the fresh-read instruction whose "Re-read the file" substring is the pub(crate) EDIT_STALE_READ_MARK const in steering_stats.rs (single source of truth — change the text and the const in the same commit). MarkerKind::EditStaleRead: index 9, label "edit-stale-read", detect tool-scoped to file_edit + first-line; switch targets = read_files + search_read (file_read is NOT a registered tool name). After 2 fires with 0 switches, dispatch.rs file_edit_redirect intercepts the next file_edit (EDIT INTERCEPTED: ... read_files path="...") — the intercepted call never runs, so it never feeds its own counter; the gate lifts on the first read (per-agent). Deliberately NOT nudged: the new_content==content no-op arm (caller mistake, not drift) and schema errors (identical strings, mode exclusivity). Load-bearing for future kinds: every marker-array size must be MARKERS.len() (a new kind on a stale [..;9] literal panics at runtime on index 9) — all literals were replaced; keep it that way. The SteeringStats UI renders by_marker dynamically (no hardcoded kind list).
