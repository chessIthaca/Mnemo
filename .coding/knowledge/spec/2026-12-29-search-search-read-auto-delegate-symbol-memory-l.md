+++
title = "search/search_read auto-delegate symbol + memory lookups — the nudge is the residual path"
supersedes = "2026-08-29-symbol-nudge-carries-the-symbol-id-graph-context"
created = "2026-12-29"
+++

SPEC (backlog b804012f, plan 7b77cf0c, commit 6cf10d5, round-2 PASS .coding/reviews/2026-12-28-auto-delegate-search-review-round2.md; supersedes the 2026-09-15 nudge spec — the nudge is no longer the primary path): symbol-shaped search/search_read queries (bare identifier, definition-prefixed fn X / pub struct X, symbol-intent X( / a::b / who-calls-X) AUTO-DELEGATE to the code graph — a compact block (definition file::name::line + kind, top callers/callees, the graph_context(id=...) pointer) rides above the output; the file walk is skipped when the query is glob-less or memory-delegated, else the block is prepended to normal results. Memory-targeted queries (a glob under .coding/knowledge|reviews, a pattern referencing those paths, or a typed SPEC:/DECISION:/BUG:/PLAN:/HOW:/REVIEW: prefix) delegate to memory_search the same way (top hits: title + one-line gist + pointer); detection is conservative — plain patterns and src globs never delegate. Blocks open with "AUTO-DELEGATED to the code graph —" / "AUTO-DELEGATED to memory —" and end with the escape line "re-issue this exact search to get the plain file search instead". Escape hatch (2026-01-03, plan 2a78cc3c): every delegated query's DelegatedKey (pattern+glob+literal) joins a bounded set on the tool (cap 32, dedup, oldest evicted); a repeat of ANY recorded key skips delegation and runs the plain search — sticky per exact query, never displaced by interleaved delegating queries (no delegate/escape ping-pong). A delegated answer counts as a steering switch (the C5 intercept gate never punishes it — src/agent/steering_stats.rs). The id-carrying nudge (byte-pinned 2026-09-15 sentence) survives only on escape repeats, fuzzy alternation branches, and non-indexed symbols. Best-effort: an unindexed symbol or an empty recall falls through to plain search, never fails it. Code: src/tool/agent/search.rs, src/tool/agent/search_read.rs, src/tool/agent/codegraph.rs.
