+++
title = "trace phase attribution — connect/backoff/stall split out of send (inflight bar amended 2026-12-31: stall keeps the streaming phase)"
supersedes = "2026-12-30-trace-phase-attribution-connect-backoff-stall-sp"
created = "2026-12-30"
+++

SPEC: trace-graph phase attribution (plans 6e734ccb + 26c01541 + f2b62d85 + the 2026-12-31 status-bar alignment, wt/agenticcoding). Per-request record fields (src/provider/trace.rs LlmRequestRecord): prep_ms = turn-loop start → record created (includes compact_ms); backoff_ms = the ACTUAL jittered retry sleeps (retry_backoff_ms, src/error.rs — equal jitter, parked on the provider via LlmClient::record_backoff_ms, stamped on the NEXT attempt's record; BOTH complete_with_retry (dispatch.rs) and run_turn_attempt (runtime/agent.rs) park it); connect_ms = record created → response headers; ttft_ms = headers → first chunk; generation_ms = first → last chunk; stall_ms = byte-silence gaps > StallTracker::THRESHOLD (2s, src/provider/mod.rs) inside generation, subset of generation_ms; tools_ms unchanged. Error paths stamp the SAME windows as success. Inflight bar (AMENDED 2026-12-31, user request — the status bar must match the trace graphs, ignoring stall): the STALL_PHASE_AFTER=10s mid-stream Waiting flip was REMOVED from turn.rs — a mid-stream stall keeps the current streaming phase (Reasoning/Streaming); Waiting is pre-first-byte only (connect/POST/TTFT/backoff). Frontend chart (traceStats.ts): 8 stacked segments prep/compact/backoff/connect/wait/reason/generate/tools + a NON-ADDITIVE stall overlay — stall counts as generate (generate = gen − reason, stall INCLUDED) and renders as a dithered red hatch, coverage = stallSharePct = min(100, stall/(gen−reason)×100) (the clamp matters: a reasoning-window stall lands in BOTH reasoning_ms and stall_ms); the stall chip is height-neutral; PhaseTimes chips p c b n w r g (s) t. Regression tests: complete_with_retry_attributes_backoff_sleeps_to_next_attempt + mid_stream_stall_keeps_streaming_phase (src/agent/tests.rs, paused clock — the 11s silence emits NO phase event; renamed from mid_stream_stall_flips_phase_to_waiting), run_turn_attempt_attributes_backoff_to_next_attempt, http_error_stamps_connect_bucket_and_parked_backoff + transport_failure_stamps_connect_bucket (openai.rs), stall_tracker_* (provider/mod.rs), retry_backoff_ms_* (error.rs).

Amended 2027-01-11: AMENDED 2027-01-11 (plan 25e8771c, backlog c60869d8, perf review 2026-09-09 L1): the ttft_ms definition in this record is superseded — ttft_ms = POST-send → first chunk, at EVERY ttft site in both providers (the anchor `post_sent` is stamped immediately before the first `client.post`: openai PreparedRequest→StreamTask, anthropic a local before .post; the no-chunk error arms included). Root cause: SSE providers deliver headers WITH the first chunk, so the old headers→first-chunk window measured ~0 (ttft_ms was 0 on 99.2% of request_stats rows). connect_ms KEEPS record-created → headers, unchanged — the prefill wait becomes VISIBLE in ttft rather than removed from connect, so the two buckets overlap by the prefill window by design. Regression tests: ttft_measures_post_send_to_first_chunk (src/provider/openai/tests.rs) + anthropic_ttft_measures_post_send_to_first_chunk (src/provider/anthropic.rs). Detail: .coding/knowledge/bug/2027-01-11-ttft-ms-dead-anchor-stamped-at-headers-arrival-n.md.
