+++
title = "backlog_add optional position parameter — top or end, default end"
created = "2027-01-07"
+++

SPEC (2027-01-08, commit 534aef8 on wt/agenticcoding, plan 2bde5c7a, backlog 06a31736): backlog_add optional position parameter — both write paths (agent tool src/tool/workflow/backlog.rs + IPC command src-tauri/src/ipc/backlog_cmds.rs) take `position` ("top"|"end", default/absent/explicit-null = "end"): Top = store-level insert-at-0 (BacklogStore::add_at in src/backlog.rs; `add` is now a one-line delegate to add_at(End, ..), so all ~60 pre-existing callers are unchanged and absent-position appends byte-identically to today) so the item dispatches first — urgent/priority items only, the queue is otherwise FIFO. Union-merge safe (the task's key NOTE, verified + pinned by test): a top insert PREPENDS one line — a minimal diff that merges cleanly with a concurrent append (non-overlapping hunks, union driver not even invoked); worst-case union concatenation dedups by first occurrence in parse_jsonl — no item lost or duplicated (test: union_merge_of_top_insert_and_concurrent_append_parses_cleanly). The headline+body shape contract + ≥160-char detail bar run BEFORE position is consumed on both paths — position cannot bypass validation. Tool output gains ", top of the queue" when Top + a "position" field in the data payload; schema documents the parameter (enum/default/urgent-only guidance). Frontend deliberately unchanged (composer has send-to-top; tauri.ts backlogAdd omits position → End). 9 new tests across store/tool/IPC; review PASS 0 findings (.coding/reviews/2026-09-08-backlog-add-position-review.md).
