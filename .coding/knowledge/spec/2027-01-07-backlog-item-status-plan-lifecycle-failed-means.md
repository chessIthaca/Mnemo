+++
title = "backlog item status ⇄ plan lifecycle — failed means exactly 'the plan was abandoned'"
supersedes = "2026-12-06-backlog-status-plan-lifecycle"
created = "2027-01-07"
+++

Backlog 45dcf577 (plan b921fccf): an item's status is derived from the plan dispatched for it — never from how the session ended.

- `in_flight` ⇔ a plan dispatched for the item is ACTIVE. Stamped when the workflow enters `Executing` (the `create_plan` moment — `stamp_backlog_in_flight`, src-tauri/src/ipc/events.rs), which also records the ROOT plan id on the item (`BacklogItem.plan_id`, the item↔plan linkage; refreshed on every Executing entry so a replacement plan re-links). Stays `in_flight` through interruptions, steering pauses, and sub-plan pushes (a sub-plan always pops back to its parent).
- `done` ⇔ the plan reached `Complete` — the turn-resolution plan-loop gate (`plan_loop_allows_done`: Complete + a real workflow transition observed this turn) → commit_success + Done.
- `failed` ⇔ ONLY root-plan abandonment: `abandon_plan` observed as an `Executing`/`Reviewing` → `Planning` transition (`is_root_plan_abandonment`), latched per-turn on `TurnResolveLatch.plan_abandoned`, applied at turn resolution AFTER the gate (an abandon followed by a successor plan that finishes still resolves Done).
- Every other turn end — loop open, resting Complete (no plan ran), unverifiable state, terminal Error (crash/provider exhaustion) — is NOT a failure: no status transition (`plan_open_note` annotates what happened), NO rollback (the work stays in the tree for a resumed session), and no further items are dispatched until the item resolves (the run WAITS on the Run-All path — the next turn resolution re-checks, the amplifier fix 2027-01-07: the agent routinely recovers on its own — auto-continue, reviewer-finish resume — so the run stays armed for the completing turn's resolution; a STOPPED run still ends when its item resolves, backlog b83e891f; auto-feed is suppressed on the single-dispatch path via `resolved_terminally`, with the consumed pointer RESTORED on non-closures so the next resolution re-checks the same item).
- Halt-for-approval stamps NOTHING: the run state is deliberately KEPT (the agent's turn is still live, blocked on the approval) so the post-approval turn resolution resolves the item under the same rules; the stop flag prevents the next dispatch. A main-agent exit while a run is active drains it (`drain_run_all_on_main_exit` — the item is REQUEUED to `Pending` with the checkpoint sha preserved: a dead agent's turn resolution never comes and an ownerless `InFlight` item is unrecoverable — dispatch only considers `Pending`, the guarded requeue refuses `InFlight`; the queue is the recovery, the work stays in the tree — run-all orphans, 2027-01-07). Hard crashes leave the same ownerless `InFlight` class: run-all START adopts such orphans (`adopt_orphaned_in_flight` — requeues every `InFlight` item with no live dispatch pointer, excluding the live single-dispatch pointer and the main agent's active root plan), and the main-agent exit clears `single_in_flight` so a stale pointer cannot misattribute the next agent's first turn resolution.
- A git checkpoint failure at dispatch leaves the item `Pending` (annotate only — infrastructure, not abandonment).
- Escape hatches: the deliberate `InFlight → Pending` deferral (via the `backlog_status` tool or the UI) and the terminal → `Pending` requeue. A steer/interrupt is NOT an escape hatch: ANY dispatched item — run-all or single-dispatch — is KEPT InFlight (backlog b83e891f for run-all items; plan cace17a6 for single-dispatch items, whose consumed `single_in_flight` pointer is RESTORED check-and-set so the completing turn's resolution resolves the item through the normal plan-tied rules); only a run-all item whose run was drained before the intervention resolution requeues (no pointer left — the queue is genuinely its only recovery).
- The `backlog_status` agent tool documents the same contract; `CantResolve` is the agent's explicit dead end — the harness resolution paths never set it.

Amended 2027-01-07 (run-all orphans, plan 5f6515f5): the main-agent-exit drain now REQUEUES the item to `Pending` (checkpoint sha preserved) instead of keeping it InFlight, run-all start adopts crash-orphaned InFlight items, and the main-agent exit clears `single_in_flight`. Supersedes the 2026-12-06 text, which described the old keep-InFlight drain.

Amended 2027-01-07 (the amplifier fix, plan 96e2862a): a non-closure or turn-failure resolution no longer ends the run — the run stays armed and the next turn resolution re-checks (the agent routinely recovers: auto-continue, reviewer-finish resume; live incidents a6a7727a + 207dc316), and the single-dispatch branch restores its consumed pointer on non-closures. The escape hatch remains for genuine misses.

Amended 2027-01-07 (the done-orphan guard, backlog 6c6966b9, plan 5b988455): the main-agent-exit drain and the run-start adoption sweep now consult landed-work evidence BEFORE requeueing — the item's plan file shows all steps checked AND ≥1 commit exists on the branch after the pre-item checkpoint sha (the note head). A landed orphan auto-resolves `Done` with an explanatory note (checkpoint sha preserved) instead of requeueing for duplicate re-dispatch; unverifiable evidence (no plan id, missing/incomplete plan, no sha, git error) still requeues to `Pending` as before.

Amended 2027-01-09 (single-dispatch intervention, plan cace17a6, user request): a steer/interrupt on a SINGLE-DISPATCHED item now also KEEPS the item InFlight — the handler restores the `single_in_flight` pointer it consumed (check-and-set, never clobbering a concurrent ▶ dispatch) instead of requeueing, so the item stays in sync with its active plan (live incident: item 0296d448 sat Pending with finished work after a mid-plan steer). The requeue arm narrows to the drained-run case (run-all item whose run a main-agent exit ended before the intervention resolution — review round-1 LOW B). Supersedes the "single-dispatch intervention requeue" escape hatch listed above and the same sentence in the b83e891f decision record.
