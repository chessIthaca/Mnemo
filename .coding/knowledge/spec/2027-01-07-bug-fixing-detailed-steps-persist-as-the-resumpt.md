+++
title = "bug_fixing detailed steps persist as the resumption section (77ff8f45)"
created = "2027-01-07"
+++

SPEC (2027-01-07, plan 4405d82d / backlog 77ff8f45, commits 41384d6→5c63085 on wt/agenticcoding): bug_fixing create_plan no longer discards the caller's steps — they persist as the `## Detailed steps` section in the plan file, alongside the locked 4-step skeleton (the skeleton lock is unchanged: update_plan still refuses steps replacement; the detail is immutable like the skeleton). The plan file is the COMPLETE crash-resumption document: the create_plan result states plainly "your {m} detailed steps are persisted as the '## Detailed steps' section"; the tool description + steps schema + kind doc + the compiled PLAN KIND text carry the crash-resumption framing (bug + context + steps must carry everything a fresh session needs: symptom, root cause, locations, fix + test design, verification). The resume footer (the volatile tail) surfaces BUG (160-char budget) + CONTEXT (240-char budget) one-line truncated via short_context_line — the full detail, including Detailed steps, stays in the plan file. Section-injection hardening: escape_section_markers (src/tool/workflow/plan.rs) prefixes `\` to lines whose trimmed form starts with `## ` or `# ` before persisting — a step quoting the plan format cannot corrupt the re-parsed plan (pinned by detailed_steps_cannot_inject_sections). API: Workflow::create_plan_with_kind_and_detail (the 7-param full variant); create_plan_with_kind keeps its exact signature (23 call sites untouched), delegating with None. PlanFile.detailed_steps: Option<String>, `#[serde(skip)]` (off the frontend wire), round-trips verbatim; absent → None (old plans parse fine). Budget ceilings raised with dated notes (Executing 24_800→25_000 measured 24_957; ExecutingResearch 21_100→21_600 measured 21_543). Regression test: create_plan_bug_fixing_persists_detailed_steps_section (red→green). Reviews: .coding/reviews/2026-09-08-4405d82d-detailed-steps*.md (3 rounds, PASS at round 3).

Amended 2027-01-11: AMENDED 2027-01-11 (plan 9441d776, backlog "bug-fix plans lose sub-step progress across restarts"): the "immutable like the skeleton" claim above is superseded — the detailed steps' TEXT stays immutable, but each sub-item is now a CHECKABLE bullet (`- [ ]`/`- [x]`) ticked via complete_step's new detailed_step_index param (1-indexed; no workflow-state transition — sub-steps never complete the plan). create_plan renders `- [ ] {text}` bullets; PlanFile::complete_detailed_step flips the nth mark in the raw section body (legacy plain-dash bodies normalize to checkboxes on first tick, so in-flight older plans become tickable); the marks round-trip serialize→parse verbatim. A mid-fix restart now sees exactly which sub-steps shipped (regression tests: create_plan_bug_fixing_renders_detailed_steps_as_checkable_substeps + bug_fixing_detailed_step_ticks_survive_restart).
