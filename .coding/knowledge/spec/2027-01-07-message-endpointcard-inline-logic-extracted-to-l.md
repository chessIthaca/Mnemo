+++
title = "Message/EndpointCard inline logic extracted to lib + settings/types (LOW 2, two of seven targets)"
created = "2027-01-07"
+++

Quality review LOW 2's two named extraction targets are done (plan 74e8d388, commit 7cd8205 on wt/agenticcoding): (1) Message.tsx's memo-equality (MessageProps + arePropsEqual) → frontend/src/lib/messageEquality.ts, and its tool-card branching (argLabel + BACKLOG_TITLE_MAX + displayName) → frontend/src/lib/toolCardPaths.ts (the label-helper home — all deps were already in-file); Message.tsx went 1,057 → 750 lines, keeps only rendering + the memo() wiring. (2) EndpointCard.tsx's caps/input logic → frontend/src/components/settings/types.ts: DiscoveredCaps + discoveredCapsById(list) (the /models mapping), effectiveCaps(ep, modelId) (per-model override ?? endpoint-level ?? null — the trailing ?? null normalizes optional-field undefined; all usage sites use != null so behavior-equivalent), parseEffortsList (comma-split/trim/filter-empty), parsePositiveIntInput (THREE-OUTCOME contract: "" → null = persist the clear; finite > 0 → floor; else → undefined = no change, caller skips onChange — this preserved the old invalid-input no-change path at all four deduped number-input sites). Two source-contract tests were replaced by behavior tests: attachImages' imagesEvicted check → messageEquality.test.ts (11 tests, 1:1 with arePropsEqual's branches); toolCardPaths' webFetchLabel dispatch check → the "web_fetch delegates to webFetchLabel" behavior test. messageArgLabel.test.ts imports argLabel from toolCardPaths now. REMAINING from LOW 2 (future items): StatsView.tsx, MemoryDebugView.tsx, FileViewer.tsx, GitView.tsx, App.tsx still have no test files; the knip/ts-prune CI dead-export suggestion was NOT taken (separate tooling decision). Reviews r1/r2 at .coding/reviews/2027-01-07-message-endpointcard-extraction*.md (final PASS).
