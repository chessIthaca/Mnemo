+++
title = "[models.summarize] slot — compaction-summary routing — MERGED into main (66bd329)"
supersedes = "2027-01-07-models-summarize-slot-compaction-summary-routing"
created = "2027-01-07"
+++

SPEC: [models.summarize] routes compaction summaries (plan 0b07d0da, commit 5220a1e — MERGED into main at 66bd329 (66bd329041dd62874ac237d51b8c64a2f6c4d50c) on 2027-01-09, branch wt/agenticcoding deleted). Consumer: ModelResolver::resolve_summarize_model (src/model_resolver.rs) — a dedicated trait method like the reviewing slot's, NEVER consulted by the per-turn state chain. All three summary call sites resolve their provider through AgentLoop::summarize_provider (src/agent/loop_impl.rs, above resolve_turn_provider): maybe_compact + handle_pending_swap (src/agent/turn.rs — engine fill-rate + pre-swap paths) and compact_context (src/runtime/agent.rs — the run-all between-items compact funnels here via AgentCommand::Compact). Resolution: slot set → sticky_endpoint reroute → build_turn_provider (provider half only); unset / dangling endpoint / build failure → the turn's provider, byte-identical to pre-slot behavior. The resolved model is never stamped into the turn's display state; the compact_ms metric stays on the turn's provider trace. Config surface: ModelsConfig.summarize, ModelsConfigDto absent/null/set semantics + validation + apply, ModelsConfigWire, Settings → Models "Summarization" row (7th fixed slot). Caveat: the summarize model's own context window is never consulted — the whole to-summarize region is sent to it (fails safe; pick a cheap model with a large window).
