+++
title = "Plan-frozen tools-array advertisement (schema_filter) — L4 prefill"
created = "2027-01-07"
+++

SPEC (2027-01-10, plan 8911500b, backlog 13292f09 "Reduce the 1.4MB request body / prefill cost (L4)"): the tools array is the PLAN-FROZEN ADVERTISEMENT. While a plan is active (Executing/Reviewing), the per-request schema array is built from Workflow::schema_filter() (src/workflow/mod.rs, next to allowed_tools) — ToolFilter::PlanFrozen (Executing surface ∪ finish) for implementation/bug_fixing plans, ExecutingResearch for research plans — keyed on the ACTIVE plan exactly like the research arm; byte-stable for the plan's whole lifetime because the array rides at the head of the request body and any byte change resets the provider prefix cache (perf review L4: ~40-75s re-bill at late-plan context sizes). ADVERTISEMENT ONLY: dispatch (src/agent/dispatch.rs:93-102) keeps re-checking the per-state allowed_tools() — complete_step/create_plan advertised during Reviewing and finish advertised during Executing are rejected at dispatch; Planning still hides mutation tools (no plan → per-state filter); reviewer/skill allow-lists win over the freeze (schema_filter returns allowed_tools() when tool_allowlist is set). Freeze ends at finish/abandon (plan popped → per-state filter). Tests: plan_frozen_* (src/tool/mod.rs), schema_filter_* (src/workflow/mod.rs), plan_frozen_tools_head_and_footer_byte_stable_across_executing_reviewing + plan_frozen_tool_set_varies_across_plans_not_within_one (src/agent/tests.rs, CapturingProvider::new_with_tool_sets captures the serialized tools array). COORDINATION: backlog 211e215e (340K cliff) owns the send-gate + trigger margin capping worst-case prefill; compaction aggressiveness (instruction 1 of the item) deliberately out of scope (review D2 documents the information-loss tradeoff). Acceptance honesty: implementation-plan bodies keep the same schemas (+finish) so avgP moves little; the win is hit-rate protection (no TOOLS-CHANGE resets mid-plan). Side fix in the same plan: tools_array_stays_within_context_budget ceilings raised for pre-existing workspace-unification drift (Planning 16,700 / Executing 28,800 / ExecutingResearch 23,600 / Reviewing 25,200 / Complete 16,700 — measured 2027-01-10, dated comments in src/agent/factory.rs; failure confirmed on the base tree via stash before raising). PLAN.md "Enforced plan-first workflow" section documents the advertisement/enforcement split.

Amended 2027-01-07: 2027-01-10 amendment (commit 737c866, wt/agenticcoding): round-1 review .coding/reviews/2026-09-10-plan-frozen-tools-array-l4-review.md found 0 high / 5 low, all fixed in the same commit — (F1) the five budget-raise comments in src/agent/factory.rs now credit the plan resumability-gate commit 077d375 (create_plan/update_plan schema docs), not eb202c6, and explain why plain `cargo test` stayed green while `cargo test --workspace` failed (workspace-scale vs standalone-scale budgets); (F2) the budget guard gained its own (ToolFilter::PlanFrozen, 29_900) entry (measured 29_565 chars, 25 tools — the largest array the app sends); (F3) README.md:32 now says "in Reviewing only the closing tools are callable (during a plan the advertised array is frozen for prefix-cache stability; out-of-state calls are still rejected at dispatch)"; (F4) the commit stages exactly the declared files, excluding the unrelated .coding/backlog.jsonl deleted_at drift and two stray status-bar files; (F5) CapturingProvider::complete's tools param renamed _tools → tools. cargo test --workspace green (2,262+ tests).
