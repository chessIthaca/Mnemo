+++
title = "plan resumability gate — create_plan/update_plan reject thin plans (b58439f4)"
created = "2027-01-07"
+++

SPEC: The plan resumability gate (2027-01-09 design, shipped 2026-09-10, plan b58439f4, commits 077d375 + 7cd4976 on wt/agenticcoding) — create_plan and update_plan reject plan content too thin to resume from after a recompile+restart, with ONE actionable error naming every violation (the backlog_add detail-bar UX). Enforced at the TOOL LAYER ONLY (src/tool/workflow/plan.rs: MIN_CONTEXT_CHARS=40, context_issue/step_issue/validate_plan_resumability/resumability_gate_error): (1) context ≥40 chars — symptom/root-cause, file anchors, verification commands; (2) every step names concrete file path(s) or an explicit no-code marker — the SAME path/marker vocabulary as the backlog detail bar via crate::backlog::body_carries_detail_marker (pub(crate) in src/backlog.rs); (3) bug_fixing plans must name the regression-test design — word-boundary TEST_DESIGN_RE (?i)\b(?:test|regression) over context + detailed steps (so "latest"/"contested" don't satisfy it), with the forced BUG_FIXING_SKELETON exempt from the per-step path check. update_plan validates what it writes: new steps per-step (bug plans exempt — the engine's locked-skeleton refusal owns that path), context as the EFFECTIVE post-update text (replace → new; append → existing + new combined, mirroring the engine's empty-existing replace semantics at src/workflow/mod.rs:644-650). The ENGINE (src/workflow/mod.rs) stays permissive BY DESIGN — its ~81 callers are tests using thin placeholders and production creation flows only through the tools (same layering as the blank-title/blank-goal guard); the optional LLM self-check was deliberately skipped (needs provider wiring inside the tool). Docs: tool schemas, plan.rs module doc, README feature bullet, PLAN.md enforced-workflow section. CAUTION: the tools-array context-budget test (src/agent/factory.rs:1804) is sensitive to schema-description length — Planning ≤16000 chars, Executing ≤28000; the gate's schema docs were trimmed to fit and Executing headroom is ~40 chars, so further schema growth needs trims or a deliberate ceiling raise. Reviews: .coding/reviews/2026-09-10-plan-resumability-gate-review.md (6 low, all fixed) + -round2-review.md (PASS). Plan file: .coding/plans/b58439f4.md.

Amended 2027-01-07: 2026-09-10: Merged into main at 7f0dcab (7f0dcab738b0abc8ff3f76048c92b5c9c9f13926) via the merge_to_main skill; branch wt/agenticcoding deleted (pre-merge tip fa0ad32). The gate is live on main — the "on wt/agenticcoding" commit references above are historical (pre-merge branch tips).
