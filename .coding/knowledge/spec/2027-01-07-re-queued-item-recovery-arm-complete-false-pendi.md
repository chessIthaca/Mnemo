+++
title = "re-queued-item recovery arm — (Complete, false) + Pending lands recover (8a6bcece)"
created = "2027-01-07"
+++

SPEC (2027-01-08, commits e5c6492 + d34b784 on wt/agenticcoding, plan af1058ee, backlog 8a6bcece): the re-queued-item recovery arm — the (Complete, false) + Pending landing in run-all resolutions.

Behavior contract: when a run-all turn resolution lands at (Complete, false) with the current item Pending (the slid-resolution + re-queued-item combination: the item externally re-queued mid-run via a backlog.jsonl edit — the store sees Pending, the in-memory dispatch pointer still references it — and the completing turn's resolution slid per the e33a07fd slide), the resolution RECOVERS instead of waiting forever (Complete is a resting state; the waiting arm's annotate-and-return was a permanent stall). Recovery, in order: (1) landed evidence — orphan_work_landed (main) / orphan_work_landed_at with spawned_plans_dir + the worktree (spawned lane) on the item's plan_id + note → transition Done with the drain's done-orphan note-preserving suffix "work already landed (plan complete + commits after the checkpoint) — auto-resolved done, not re-dispatched" (re-dispatching would redo landed work, the 6c6966b9 class); the spawned lane also lands the branch via land_spawned_branch (a conflict keeps the branch + the note carries it) and sets remove_worktree = true; (2) otherwise — no transition, no annotate (the re-queue note is recovery evidence, the bba2c82d discipline), fall through to the tail's unconditional pointer clear + dispatch next (the item is already Pending — eligible for re-dispatch); the spawned not-landed branch also sets remove_worktree = true (a stale wt/runall-* branch would block every later lane dispatch).

Lock discipline: snapshot (note, plan_id) under the store lock → consult the landed predicate WITHOUT the lock (blocking git subprocess) → re-verify still-Pending under the lock before the Done flip. The 2026-08-20 never-planned hole stays plugged: a Pending item with no plan_id never resolves Done (the predicate requires a plan_id; anything unverifiable is false → the not-landed fall-through).

Both arms sit immediately BEFORE the waiting arm in on_main_turn_resolved and on_spawned_turn_resolved (src-tauri/src/ipc/run_all.rs); the module doc carries the "RE-QUEUED variant of the slide" bullet and both function docs carry the recovery sentence. Regression tests (source-contract, red-first): slid_resolution_with_requeued_pending_item_dispatches_next + slid_resolution_spawned_lane_requeued_pending_recovers. Review: round 1 FINDINGS (0H+1L — stale function docs, fixed), round 2 PASS (.coding/reviews/2026-09-08-af1058ee-round2-slid-resolution-requeued-pending-review.md).
