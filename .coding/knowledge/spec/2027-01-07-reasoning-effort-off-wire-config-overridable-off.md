+++
title = "reasoning_effort_off_wire — config-overridable off encoding"
created = "2027-01-07"
+++

The effort-"off" wire encoding is config-overridable (backlog 1e40407c, plan f2b14799, commit e837be8 on wt/agenticcoding). New `endpoints.toml` field `reasoning_effort_off_wire: Option<String>` on Endpoint + ModelSpec (model-level wins, resolved like stop_token_ids_for via `Endpoint::reasoning_effort_off_wire_for`, src/config/endpoints.rs:~527). Resolution at all three seams (effective_reasoning_effort_for, resolve_reasoning_effort in src-tauri/src/ipc/config_io.rs, request-builder guard in src/provider/openai/request.rs:~338): config value when set, else the built-in kind+name policy `reasoning_effort_off_wire_value` (src/provider/policy.rs:~272 — DeepSeek-family model names → "none", others omit; a literal "off" is an instant non-retryable 400 on DeepSeek). Anthropic-kind endpoints always resolve None (Messages API has no reasoning_effort field). The field is hand-edited TOML like stop_*/extra_body — NOT in the settings DTO — preserved across Settings saves via apply_endpoints carry-over (src/config/patch.rs:~242/265, both levels). Design: option (b) config-override-with-default — purely additive, zero behavior change when unset; the config is the escape hatch for renamed/aliased/fine-tuned DeepSeek models. Docs: README.md config paragraph, PLAN.md item 7. Tests: 8 new (endpoints.rs ×4, openai/tests.rs ×2, config_io.rs ×1, patch.rs ×1). Reviews: round-1 FINDINGS 1 low (stale doc bullet, fixed), round-2 PASS (.coding/reviews/2027-01-07-reasoning-effort-off-wire-config-round2-verification.md).
