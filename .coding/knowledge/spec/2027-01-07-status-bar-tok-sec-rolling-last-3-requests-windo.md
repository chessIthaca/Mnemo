+++
title = "Status-bar tok/sec = rolling last-3-requests window (not session-wide)"
created = "2027-01-07"
+++

SPEC: The InflightBar status bar's output tok/sec is a ROLLING WINDOW over the last 3 timed requests, not a session-wide mean (user request 2027-01-09; plan 70328e41, commit 41f3cae on wt/agenticcoding, round-2 review PASS). Mechanism: SessionTiming.recentTiming ring buffer (RECENT_TIMING_WINDOW=3, agentState.ts) — appended in reduceUsage (agentEventReducer.ts) only when generation_ms > 0 (a sample with no time denominator would inflate the rate), oldest dropped via .slice(-RECENT_TIMING_WINDOW), reset on clearConversation. Display rate = recentOutputTokPerSec (agentState.ts): sum(tokens)/sum(ms) over the window via aggregateTokPerSec — total-over-total, NOT average-of-rates (N× inflation lesson from 463ddd4); '—' while the window is empty; tooltip interpolates RECENT_TIMING_WINDOW. SCOPE: sessionTiming totals still accumulate over ALL requests and feed StatsView (SessionCard/ProjectCard/ModelTable), per-request lastRequestTiming rates and traceStats.ts untouched. Tests: useAgentStore.test.ts "recentTiming: rolling last-3 window feeds the status-bar tok/sec" + agentState.test.ts describe("recentOutputTokPerSec").

Amended 2027-01-11: 2027-01-12 re-verification (run-all done-orphan, research plan fd111ac1): the backlog item (a21993a4) was orphaned in flight and requeued by the run-all machinery; this re-dispatch verified the work is fully landed in the current tree — commit 41f3cae in branch history (10 files, +234/−31, citing the round-1 review), plan .coding/plans/70328e41.md complete (6/6 steps), all mechanism pieces present (SessionTiming.recentTiming ring buffer frontend/src/hooks/agentState.ts:108, generation_ms > 0 gated reducer append frontend/src/hooks/agentEventReducer.ts:794-808, recentOutputTokPerSec total-over-total via aggregateTokPerSec frontend/src/hooks/agentState.ts:149-161, InflightBar display + interpolated "last N requests" tooltip frontend/src/components/chat/InflightBar.tsx:8/:148/:330), both test suites present (frontend/src/hooks/useAgentStore.test.ts:988, frontend/src/hooks/agentState.test.ts:254), the scope guard held (recentOutputTokPerSec's only UI caller is InflightBarPanel; StatsView/lastRequestTiming/traceStats/sessionTiming totals untouched), and the acceptance suites green at re-verification time (cd frontend; npm test 79 files/1097 tests; npx tsc --noEmit clean). Note: the round-2 review report file was not located under the conventional -round2-review.md name; the round-1 report (.coding/reviews/2026-09-10-status-bar-tok-sec-rolling-window-review.md, FINDINGS 0H/3L all copy-precision, fixed before ship), the commit, the completed plan, and this SPEC are the surviving records. No code changes were needed; the item closed as a done-orphan.
