+++
title = "trace record mutation version + LlmTraceView detail-fetch skip (L5)"
created = "2027-01-07"
+++

SPEC (2027-01-10, plan 0a54dcd5, backlog cd7bd623, L5 from .coding/reviews/2026-09-09-perf-comm-review.md): the LLM trace record carries a mutation `version` (u64, starts 0) — bumped on EVERY with_record mutation (src/provider/trace.rs with_record, after the is_complete maintenance) and on every raw-budget payload eviction (enforce_raw_budget — a wire-visible change: response_raw → None + response_evicted); carried verbatim by both LlmRequestDetail and LlmRequestSummary (no DTO mapping at src-tauri/src/ipc/trace.rs). LlmTraceView's detail (selectedId) and compare (selectedId-1) polls skip the getLlmRequest fetch while the selected record's version is unchanged since the last fetch: pure exported predicate shouldSkipRefetch(lastVersion, listVersion) — skip only when both non-null and equal; null lastVersion (first fetch / selection change) and null listVersion (record absent from list) always fetch so the poll can observe null and stop. The list poll feeds requestsRef beside every setRequests call (list tick + clear handler — lockstep is pinned by test); each load keeps an effect-scoped `let lastVersion` reset per effect run. Pinned by: frontend/src/components/views/LlmTraceView.versionSkip.test.ts (shouldSkipRefetch table + ?raw source contracts, registered in frontend/vitest.config.ts) and two Rust regression tests in trace.rs (version_bumps_on_every_mutation_and_stays_stable_when_idle, version_bumps_on_payload_eviction). Shipped at ccb568d on wt/agenticcoding; round-1 review (1 LOW: clear() ref lockstep, fixed) .coding/reviews/2026-09-10-llmtrace-version-skip-review.md, round-2 PASS .coding/reviews/2026-09-10-llmtrace-version-skip-round2-review.md.

Amended 2027-01-11: 2027-01-12 re-verification (run-all done-orphan): the backlog item was re-dispatched by a later run-all batch; a research plan (6b834b3d) verified the work is fully landed in the current tree — commit ccb568d in branch history, version field + both bump points present in src/provider/trace.rs (with_record :889, enforce_raw_budget :1123), both Rust regression tests green (cargo test version_bumps: 2 passed), shouldSkipRefetch + requestsRef wiring present in frontend/src/components/views/LlmTraceView.tsx (:141/:816), the 9-test contract suite registered in frontend/vitest.config.ts (:40), and both suites green at re-verification time (cargo 2282+16; vitest 78 files / 1087 tests). No code changes were needed; the item closed as a done-orphan.

Amended 2027-01-11: 2027-01-12 second re-dispatch (run-all done-orphan, plan b1a89ab5): the item was re-dispatched again immediately after plan 6b834b3d closed it. Re-verified unchanged: HEAD moved only via the run-all machinery's own bookkeeping checkpoints (531fecf, 94d68af, 26141e7 — backlog.jsonl dispatch-pointer commits; no code changes since e786556), shouldSkipRefetch still at frontend/src/components/views/LlmTraceView.tsx:141-146, both Rust regression tests still at src/provider/trace.rs:1606/:1628, and the prior amendment + completed plan 6b834b3d.md (4/4) confirmed on disk — last turn's green suite runs (cargo 2282+16; vitest 78 files/1087 tests) stand as acceptance evidence for the identical tree. Closed again as a done-orphan with zero code changes. Flip-miss evidence for the run-all done-orphan guard (backlog 6c6966b9): the machinery's dispatch-time linkage works (it wrote plan_id b1a89ab5 + status in_flight onto the item at dispatch), but the automatic done-flip never fired for research-plan completions — research plans reach Complete without a finish call, and the flip appears keyed to finish (the finish-closed item a09199b1/plan aa3c8dae flipped correctly). Two research-plan completions have now failed to flip their items (395d6dad, 6b834b3d); this session closes the item manually via backlog_status after plan Complete.
