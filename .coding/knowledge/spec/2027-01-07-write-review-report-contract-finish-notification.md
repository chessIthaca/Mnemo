+++
title = "write_review_report contract + finish-notification report status (5b46674d)"
created = "2027-01-07"
+++

SPEC (2027-01-07, plan 7da7476b / commit d49051c, backlog 5b46674d): write_review_report contract — the date prefix is generated from the system clock (a reviewer-supplied date-shaped YYYY-MM-DD- prefix is stripped and replaced with today's UTC date via format_epoch_date); the returned path (output + with_data, captured into last_review_report) is the PROJECT-RELATIVE ".coding/reviews/<file>" (never the canonicalized \\?\ alias); verdict/append rejections quote the received/existing first non-blank line (120-char cap) so a reviewer fixes them in one retry; append resolution order: as-given (if the file exists) → today-normalized → the unique <any-date>-<stripped-name> variant (ambiguous = fail-closed) → append-create with verdict check. Finish-notification contract: report-less completions say "no report was written"; the failed-reviewer protocol (distinct text + parent reviewer_failure_pending latch) fires for ANY report-less reviewer (role==reviewer && no report, regardless of exit status). Retry accounting: REVIEW_REPORT_MAX_FAILURES=3 — a dedicated consecutive write_review_report failure counter on TurnState, reset ONLY by a successful write_review_report (not by other tools' successes), aborts the turn with a distinct final Error before the generic MAX_RETRIES check. finish resolves the report path as-given (absolute/CWD-relative) → basename under the reviews dir. Detail: BUG record fe84a777 + .coding/knowledge/bug/2027-01-07-reviewer-report-failures-are-silent-fixed-5b4667.md; reviews 2027-01-07-reviewer-report-failures-hardening-review.md (round-1, 4 low → fixed) + round-2 verification PASS.
