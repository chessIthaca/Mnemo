+++
title = "empty-call sweep — no-zero-argument rule + recovery hint on the six fumbled tools"
created = "2027-01-11"
+++

The read_files precedent (backlog 26cdbaf8) extended to the six most-fumbled tools (backlog d9ad618e, plan e02134b2, commit a44d1c8 on wt/macos-fix): shell, graph_search, graph_context, graph_path, git_read, memory_write. Each description carries the no-zero-argument rule + an inline example of the exact call shape; each empty-call error carries the recovery hint ("rewrite the full call, do not resend the empty shape") via invalid_args_error (src/tool/agent/read_files.rs:75 — sanitized base + received keys + hint). graph_context's hint rides the SEMANTIC either-or check (codegraph.rs ~465: its serde arm can't fire on an empty call — SymbolArgs is two Options); graph_impact's twin check deliberately untouched (not in the fumble evidence — scope discipline). All six budget ceilings raised in src/agent/factory.rs (Planning/Complete 19_500, Executing 34_600, PlanFrozen 35_900, ExecutingResearch 28_600, Reviewing 29_800) — workspace-unified printouts pinned in the comments (19_241/34_169/35_455/28_294/29_433/19_241); the standalone `cargo test --lib` run alone stays green — only the workspace-unified run (browser-on feature unification, +484 load_tools per filter; what CI checks) crosses ceilings. Test pairs per tool (schema advertises the rule; empty-call error carries the hint). Reviews: round 1 FINDINGS (0 high, 1 low — budget-comment accuracy) fixed; round 2 PASS (.coding/reviews/2026-09-21-empty-call-sweep-d9ad618e-verify-round2.md).
