+++
title = "ruleset-aware run-all landing — two paths, PR on protected mains"
created = "2027-01-11"
+++

The app-managed run-all landing (backlog b52b041a, plan 8432b8cb, commit a3fd0fe on wt/macos-fix) is ruleset-aware, mirroring the merge_to_main skill's DECISION 2027-01-11. src/project/worktrees.rs: main_is_protected (gh repo view --json nameWithOwner → parse_github_remote_url fallback → gh api repos/{o}/{r}/rulesets → rulesets_json_blocks_direct_push: ACTIVE + pull_request; ANY failure → false, fail-open) gates land_item_branch_impl FIRST — protected → land_via_pull_request (push -u origin <branch>, title from the tip subject with a branch-name fallback on empty, gh pr create --base main --head <branch>, gh pr view fallback for an already-open PR, failures → LandError::Git naming the PR requirement + the exact command); unprotected → the direct local --no-ff merge (unchanged; it never pushed — the old agent.md "pushes main directly" claim was inaccurate, the defect was the unpushable stranded merge + branch delete). Landed { Merged, PullRequest(url) }; the three run_all.rs landing sites wire PullRequest → remove_worktree_only (branch KEPT — the PR's head; no sweeper yet, follow-up backlog 64662ef2) + the item note carrying the URL. gh_raw (src/project/git_ops.rs) is the gh twin of git_raw. Invariants: never bypass the ruleset (plain push, no force/admin/ruleset edits); no path deletes a PR-head branch; detection fail-open (a false negative strands one local merge — documented residual; a false positive would PR-flow every unprotected repo); the PR path does not build (the spawned agent's closing sequence already ran the tests — the producer-side guarantee). Reviews: round 1 FINDINGS (0 high, 6 low) all fixed; round 2 PASS (.coding/reviews/2026-09-21-b52b041a-ruleset-aware-run-all-landing-verify-round2.md).
