+++
title = "stale-reindex ceilings aligned at 32 — content index (06276aa) and symbol index (2026-09-26)"
supersedes = "2027-01-11-stale-reindex-caps-diverged-content-index-32-ada"
created = "2027-01-11"
+++

SPEC: both stale-reindex inline-repair ceilings now stand at 32 — the earlier divergence is CLOSED. Content index (`search`/`search_read`): src/tool/agent/search.rs:371 `STALE_REINDEX_CAP = 32` (raised from 8 by commit 06276aa, backlog 9201704f). Symbol index (`graph_search`): src/tool/agent/codegraph.rs `STALE_REINDEX_CAP = 32` (raised 8 → 32 on 2026-09-26 — plan ced9308a follow-on, user directive: accurate results matter more than the note; the old 8 left a 61-file checkout/landing-scale drift unrepaired and this plan's own regression-test symbol unindexed, forcing a manual-reindex detour). In BOTH tools the ceiling is an ADAPTIVE upper bound, NOT the latency guard: the real guard is `STALE_REINDEX_BUDGET` (500 ms, src/codegraph/mod.rs:209), passed at both repair sites (the `reindex_stale_files` call sites in codegraph.rs and search.rs), whose pass stops between files once the budget is spent — so a slow set degrades to the plain miss / tree walk instead of a slow tool call. The symbol index repairs only on a TOTAL symbol miss and then re-resolves ONCE from the fresh view; beyond the ceiling, or while an index pass is already running, it serves the staleness note (`symbol index may be stale — {n} file(s) on disk are newer than the index`) and the miss stands; a successful repair is disclosed as `reindexed {n} stale file(s) — serving fresh graph results`. Tests pinning this: codegraph.rs::stale_within_the_reindex_ceiling_repairs_inline (a 9-file drift repairs and serves the fresh symbol) and codegraph.rs::stale_above_the_reindex_cap_notes_instead_of_reindexing (ceiling+1 notes instead). Manual repair remains `codegraph_refresh` (src-tauri/src/ipc/codegraph_cmds.rs:143); `finish` also force-reindexes when a regression-test symbol is missing.
