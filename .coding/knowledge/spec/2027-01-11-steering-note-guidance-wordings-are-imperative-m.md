+++
title = "steering-note + guidance wordings are imperative — markers/labels unchanged (plan 987fef4c)"
created = "2027-01-11"
+++

SPEC (plan 987fef4c, backlog 56168c38, branch wt/mnemo; reviews .coding/reviews/2026-09-15-imperative-steering-phrasing-review.md — round 1: 0 high/3 low, all fixed — and …-review-round2.md: PASS): every advisory guidance sentence in tool output + the prompt preamble is now an IMPERATIVE. User request 2027-01-14: "guidance sentences written as advisory preferences must become commands."

Rewritten sites (markers untouched): src/tool/agent/search.rs — alternation tail "; use graph_search/graph_context for symbol hunts — use search only for text" and both symbol_nudge branch tails ("… for symbol lookups — use search only for text"); the zero-file glob hint in search.rs + search_read.rs ("— use extension-anchored shapes like **/*.rs"); src/tool/agent/shell.rs REDIRECT_NOTE ("— run the command unredirected"); src/tool/steering.rs consolidation note ("— run memory_consolidate(session_id) to distill them"); src/tool/agent/file_write.rs schema ("use file_edit"); src/tool/agent/file_edit.rs large-payload note ("use smaller fragments, or file_write for a full-file replacement"); src/tool/workflow/plan.rs update_plan ("use this to adjust …"), abandon_plan ("Use update_plan whenever the plan is still broadly correct … DESTRUCTIVE LAST RESORT"), the feature-scale NOTE ("re-file it as kind=implementation … This is a NOTE, not a gate."); src/agent/prompt.rs CORE PRINCIPLES ("Edit existing files with file_edit (targeted), not a full file_write rewrite") + two doc comments; doc comments in src/tool/agent/codegraph.rs + src/codegraph/mod.rs. docs/FEATURES.md quotes synced.

INVARIANTS to preserve: every MarkerKind::marker() substring and label() is byte-unchanged ("is an indexed symbol", "TIP: for file-content search", "No symbols matched", "RECALLED CONTEXT", "SYMBOL NUDGE:", "TIP: pattern has no regex metacharacters", "known memory hit:", "working-memory events accumulated this session", "TIP: output redirection detected", "Re-read the file") — the frontend display registry (frontend/src/lib/delegationNotes.ts, plan 3ae367bd) keys on those stable phrases and only needed fixture/comment sync. The re-issue escape hatch line is unchanged. The full-sentence byte pin moved with the wording (bare_identifier_symbol_searches_earn_the_graph_nudge, src/tool/agent/search.rs) and the steering_stats.rs fixtures were updated to the new strings.

Deliberately NOT converted (judged defensible by both review rounds): src/tool/agent/codegraph.rs:241 "(exact match preferred)" (internal comment; graph schemas are hand-built json!) and src/tool/workflow/ask_user.rs:43/:106 "(at least 2; 2–6 recommended/ideal)" (cardinality guidance over an ENFORCED 2-floor — an imperative would over-state the contract).
