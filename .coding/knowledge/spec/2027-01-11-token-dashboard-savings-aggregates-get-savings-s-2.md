+++
title = "Token Dashboard — savings aggregates + get_savings_stats IPC + Dashboard tab — PR #6 OPENED against main"
supersedes = "2027-01-11-token-dashboard-savings-aggregates-get-savings-s"
created = "2027-01-11"
status = "superseded"
+++

PR #6 OPENED against main (head wt/mnemo, branch tip ec4605d), awaiting human review — main is ruleset-protected (ruleset 23755694: pull_request rule, required_approving_review_count 1), so the author cannot self-approve; the human approves and merges. Carries the token dashboard (plan 6494b738, backlog 652ae094: commits 1a37d2f ledger + dd76b9f dashboard, round-3 review PASS), superseding this record's earlier "committed dd76b9f on wt/mnemo — NOT yet merged to main" status line. Architecture + invariants (metered figures are tokens only, dollars ESTIMATED in the cache section, never summed; [general.optimizer] levers default-off; dashboard tab immediately before Memory): see the knowledge file.
