# Plan: Fix backlog_status schema: id type integer → string (matches UUID IDs + String struct)

## Goal
Fix the backlog_status tool's schema/struct mismatch: the schema declares `id` as `"type": "integer"` but the struct field is `String` and actual IDs are UUID strings. This causes every call to fail with "invalid type: integer, expected a string" when the LLM follows the schema and sends an integer.

## Kind
bug_fixing

## Context


## Steps
- [x] 1. **Reproduce with failing regression test** — Write a regression test that reproduces the defect (constitution: every defect gets a regression test that fails without the fix and passes with it). Run it and confirm it FAILS.
- [x] 2. **Document root cause** — Investigate and document the root cause. memory_write a BUG: record (symptom → root cause → fix + regression test name, ≤600 chars).
- [x] 3. **Minimal fix** — Apply the minimal fix that makes the regression test pass. Do not refactor unrelated code.
- [x] 4. **Verify** — Run the regression test + the full test suite (cargo test unpiped, warning-free). Record the regression test name via update_plan (regression_test field) — finish is blocked without it.

## Bug
backlog_status tool calls fail with "invalid arguments: invalid type: integer `2`, expected a string" — the schema declares id as type "integer" but the struct field is String and actual IDs are UUID strings, so every call that follows the schema sends an integer that serde rejects.

## Regression test
status_tool_schema_declares_id_as_string
