# Plan: Verify all local+remote branches are fully merged into main

## Goal
Determine authoritatively whether every local and remote branch is fully merged into main right now

## Kind
research

## Context
Session context already on disk: pull-sync landed on local+origin main @0c08e0f minutes ago; local wt/429-fallback-hang (@69128eb) and junk-named 'list' (@18e7286) were deleted after containment verification; remote stray origin/wt/agenticcoding (@127a13d) was push-deleted after 'main..origin/wt/agenticcoding' proved empty. EXPECTED current topology: local = {main}, remote = {origin/main}. However other agent instances may have created or pushed refs since that fetch window, so an authoritative YES requires fresh inspection of BOTH local heads/*/*/* = */*/*/* * * * *

## Steps
- [x] 1. **Refresh refs & inventory all branches** — Run PowerShell at project root C:\AgenticCoder\AgenticCoder (unpiped conventions per project constitution): (1) git fetch --prune origin then print fetch_exit=$LASTEXITCODE on its own line; (2) print current branch via two lines: $cur = git rev-parse --abbrev-ref HEAD then "current_branch=" + $cur; (3) print "--- LOCAL ---" then raw output of git branch -v ; (4) print "--- REMOTE ---" then raw output of git branch -r ; capture everything verbatim as evidence.
- [x] 2. **Compute merge status per non-main ref** — Build $refs = @(git for-each-ref "refs/heads" "refs/remotes/origin" "--format=%(refname:short)") ; foreach entry skip exactly 'main', 'origin/main', 'origin/HEAD' ; for each remaining R run $c = @(git rev-list --count ("main.." + $b)) using $b as loop var and print "{0} : {1} commits not in main" -f $b ($c[0]) ; cross-check the classification with raw outputs of git branch -a --merged main AND git branch -a --no-merged main printed under headers "--- MERGED ---" / "--- NOT MERGED ---".
- [x] 3. **Report verdict & reconcile** — From step-2 table give a one-word overall verdict plus per-ref evidence lines ; also print push parity as two counts : @(git rev-list --count "origin/main..main") ahead-of-origin and @(git rev-list --count "main..origin/main") behind-origin ; if ANY non-main ref shows count>0 print its unique commits via git log --oneline "main..<ref>" for user follow-up but DELETE NOTHING (user asked only whether merged) ; finally compare against expected topology {local=main-only , remote=origin/main-only} stored above and state explicitly whether reality matches , including any newly appeared refs discovered by prune.
