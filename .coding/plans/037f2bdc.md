# Plan: Full review round: memory/perf, quality/coverage, Mac readiness

## Goal
Run three parallel read-only reviewers over the full codebase at HEAD (wt/agenticcoding @ 9a0d5b9) — (1) wasted memory + performance in the display-update pipeline and model-interaction paths, (2) maintainability + code quality + test coverage, (3) macOS readiness/compatibility — then triage the three reports and queue the actionable findings to the backlog with a summary for the user.

## Kind
research

## Context
User-requested full review round (2027-01-06), mirroring the 2026-12-29 four-axis precedent (plan 72329f2c: three parallel read-only reviewers, 16 findings queued). Axes per the user: (1) wasted memory + performance — particularly the display-update pipeline and model interaction; (2) maintainability + quality + test coverage; (3) macOS readiness/compatibility. Target: full codebase at HEAD on wt/agenticcoding (9a0d5b9 — the backlog-headline feature; everything committed). Project shape: Tauri 2 app — Rust lib (src/), app shell (src-tauri/), React/TS frontend (frontend/src). Constitution yardstick for axis 3: no Windows-only APIs/paths/shell syntax outside the sanctioned WebView2 Browser tab / game_* tooling (Windows-only by design, gated); the app must build and behave on both macOS and Windows. Reviewers are read-only (role:"reviewer", no model param) and author reports via write_review_report into .coding/reviews/ — the main agent never writes reviews. Findings get queued to the backlog afterward (established outcome of a review round).

## Steps
- [x] 1. **Spawn the three axis reviewers in parallel** — Spawn three parallel role:"reviewer" agents (read-only, no model param) with self-contained briefs. Common context: Mnemo, Tauri 2 app (Rust lib src/ + app src-tauri/ + React/TS frontend/src), review the FULL codebase at HEAD 9a0d5b9 on wt/agenticcoding (not just uncommitted changes). Axis 1 (memory+perf): the display-update pipeline (agent events → IPC → useAgentStore/agentEventReducer/deltaFlush → React re-renders; memoization gaps, selector granularity, unbounded arrays/caches, timer/listener leaks, base64 images in state, markdown re-parsing, polling) and the model-interaction path (provider SSE streaming buffers, request building, client rebuild vs reuse, context/token accumulation, the memory system, Arc/Mutex/channel usage, per-request allocations); prioritize user-visible impact (streaming latency, UI jank, RAM growth over a long session). Axis 2 (quality+coverage): duplication, dead code, long functions, error handling (unwrap/expect outside tests, swallowed errors), doc-comment coverage on public items (constitution requirement), module coupling; test coverage gaps (Rust modules without tests in critical paths; frontend files vs the vitest.config.ts include list — vitest only runs registered files), weak assertions. Axis 3 (Mac readiness): cfg(windows)/target_os gates outside the sanctioned WebView2/game_* exception; hardcoded Windows paths (backslashes, C:\, drive letters), path concat vs PathBuf, case-sensitivity/CRLF assumptions; PowerShell/cmd syntax in tool implementations; tauri.conf.json bundle targets (darwin/dmg), Cargo target-specific deps; frontend platform checks; README Windows-only claims. Each writes .coding/reviews/2027-01-06-full-review-<axis>.md via write_review_report, opening "## Verdict: PASS" or "## Verdict: FINDINGS (n high, n low)", findings severity-tagged with file:line evidence + concrete fix direction.
- [x] 2. **Read + triage the three reports** — When all three reviewers finish: read the three reports in full; dedupe cross-axis overlaps; classify findings by severity (high/low) and actionability; cross-check against the existing backlog (backlog_list) and memory (memory_search) so already-tracked items are not re-queued.
- [x] 3. **Queue findings + summarize** — Queue the actionable findings to the backlog (one item per finding, following the established format: problem + fix direction + investigation pointers), write REVIEW: digest memories for the round, and give the user a compact summary (per-axis verdicts, finding counts, top items).
