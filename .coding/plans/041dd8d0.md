# Plan: Steer feature-scale bug fixes to implementation plans (51ee41c1)

## Goal
Steer bug-triggered FEATURES to implementation plans: the compiled prompt's plan-kind guidance gains the feature-scale carve-out (bug_fixing is for CONTAINED defect fixes; a fix that adds capabilities, new dependencies, or spans multiple modules is kind=implementation with the bug as motivation), and create_plan emits an advisory steering note when a bug_fixing payload looks feature-scale — while small true bugs go through bug_fixing unchanged and all existing bug_fixing tests stay green.

## Kind
implementation

## Context
Root problem (backlog 51ee41c1, user-steered): the compiled prompt's PLAN KIND section (src/agent/prompt.rs :82-90) says "Bugs: ALWAYS kind:\"bug_fixing\", never \"implementation\"" with no carve-out for bug-triggered FEATURES, so feature-scale work (cf. plan c87093c1: 10 tree-sitter grammars + 7 visitors + HTML sub-parsing + new crate deps filed as bug_fixing) runs under the locked 4-step skeleton with a mega "Minimal fix" step and a BUG: auto-capture that mislabels a feature. DESIGN (both fix directions from the item): (1) PROMPT GUIDANCE — amend the PLAN KIND section: after "BUG: memory auto-captured at finish." add the carve-out "— EXCEPT bug-triggered FEATURES (the fix adds capabilities, new dependencies, or spans multiple modules): file those as kind:\"implementation\" with the bug documented as motivation in goal/context; bug_fixing is for CONTAINED defect fixes." The two literals the stable-head test asserts (kind:\"bug_fixing\" at :1391, never \"implementation\" at :1395) are preserved verbatim; add a NEW test stable_head_carries_feature_scale_carve_out asserting the carve-out text. (2) CREATE_PLAN STEERING NOTE (the enforcement net, advisory — ask_user would block unattended run-alls): in src/tool/workflow/plan.rs, a new helper referenced_module_dirs(&[&str]) -> usize counting DISTINCT `src/<module>/` prefixes (find "src/", take the segment up to the next '/', only when a '/' follows — so src/codegraph/mod.rs + src/codegraph/store.rs count once, bare src/lib.rs counts none; src-tauri/src/ipc/... and frontend/src/components/... match via their inner src/); in the bug_fixing branch (:565-632), after detail_texts is built, compute feature_scale = detail_texts.len() > 4 AND referenced_module_dirs(detail_texts + context + goal) >= 3 — add it as a 5th tuple element (else arm: false); append an advisory NOTE to the create_plan result after the detailed-steps note (:754-762): the payload looks feature-scale, consider re-filing as kind=implementation with the bug as motivation — bug_fixing is for contained defect fixes. HEURISTIC DECISION (documented): AND not OR — a contained cross-module fix (9441d776: 4 steps, 3 modules) and a long single-module fix (85368a1f: 4 steps, 1 module) must NOT warn; the c87093c1 shape (many steps + many modules) trips both. Dependency-addition is not textually detected (fragile) — the prompt guidance carries that judgment; the note is the net. Also amend the kind enum description (:502) with the same carve-out. VERIFICATION: cargo test (the existing bug_fixing create_plan tests in src/tool/workflow/plan.rs must stay green — the note is additive to the output, no behavior change for contained payloads; the prompt stable-head tests) + the reviewer. Relationship to sibling item e5a84ce9 (feature-scale fix documentation): complementary — this item prevents the misclassification at create_plan time; e5a84ce9 handles documentation when feature-scale work lands under bug_fixing anyway (the steering is advisory).

## Steps
- [x] 1. **Prompt guidance — the feature-scale carve-out** — src/agent/prompt.rs: amend the PLAN KIND section (:82-90) — after "BUG: memory auto-captured at finish." insert "— EXCEPT bug-triggered FEATURES (the fix adds capabilities, new dependencies, or spans multiple modules): file those as kind:\"implementation\" with the bug documented as motivation in goal/context; bug_fixing is for CONTAINED defect fixes." Preserve the two literals the stable-head test asserts (kind:\"bug_fixing\", never \"implementation\"). Add a new test stable_head_carries_feature_scale_carve_out (next to stable_head_carries_bug_fixing_habit :1384) asserting the head contains the carve-out (e.g. "bug-triggered FEATURES" and "CONTAINED").
- [x] 2. **create_plan steering note + helper** — src/tool/workflow/plan.rs: (a) add the helper `fn referenced_module_dirs(texts: &[&str]) -> usize` near the other bug_fixing helpers (~:229) — counts DISTINCT `src/<module>/` prefixes (find "src/", take the segment up to the next '/' only when one follows; same-module files count once; bare src/file.rs counts none; src-tauri/src/... and frontend/src/... match via their inner src/), with a doc comment citing backlog 51ee41c1. (b) In the bug_fixing branch (:565-632): after detail_texts is built, compute `feature_scale = detail_texts.len() > 4 && referenced_module_dirs(&detail_texts + args.context + args.goal) >= 3`; extend the tuple to a 5th element (else arm returns false). (c) After the detailed-steps note in the result (:754-762), append the advisory NOTE when feature_scale: the payload looks FEATURE-SCALE (more detailed steps than the locked skeleton, spanning multiple modules), consider re-filing as kind=implementation with the bug documented as motivation in goal/context — bug_fixing is for contained defect fixes; advisory, the caller judges. (d) Amend the kind enum description (:502) with the same carve-out.
- [x] 3. **Tests — steering fires, contained stays silent** — src/tool/workflow/plan.rs tests: (a) `referenced_module_dirs_counts_distinct_modules` — a unit test: src/codegraph/mod.rs + src/codegraph/store.rs + src/workflow/mod.rs → 2; src/lib.rs alone → 0; src-tauri/src/ipc/projects.rs + frontend/src/components/x.tsx → 2. (b) `create_plan_bug_fixing_feature_scale_notes_steering` — a bug_fixing payload with 5 detailed steps whose texts+context reference ≥3 module dirs → the result output contains the steering NOTE. (c) `create_plan_bug_fixing_contained_fix_stays_silent` — a contained payload (4 steps, ≤2 modules) → no NOTE in the output (the acceptance: small true bugs go through bug_fixing unchanged). Mirror the existing create_plan test harness (the json! payloads + ToolResult assertions used by create_plan_bug_fixing_persists_detailed_steps_section :2492).
- [x] 4. **Verify — suites + reviewer** — Run `cargo test plan` (the src/tool/workflow/plan.rs suite — all existing bug_fixing tests green + the 3 new ones) and `cargo test prompt` (the src/agent/prompt.rs suite — the stable-head tests incl. the new carve-out assertion). Then run the full `cargo test` lib suite (read the `test result:` lines, not the pipe exit code). Then spawn the read-only reviewer over the uncommitted changes (changed files: src/agent/prompt.rs, src/tool/workflow/plan.rs).
