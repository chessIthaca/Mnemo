# Plan: file_edit failure classes: `lines` tuple, escape-normalization fallback, no-op + miss diagnostics (backlog 838b6f4e)

## Goal
Kill the three observed file_edit failure classes — the half-filled line-range pair (one required `lines` tuple in the schema), the apostrophe/escape trap (auto-fallback with a visible note), and no-op/malformed edits (clear early rejection + a first-difference miss pointer) — each pinned by a regression test that fails without its fix.

## Kind
implementation

## Context
Backlog 838b6f4e (observed 2027-01-24, wt/mnemo topology work): three file_edit failure classes. (1) Line-range mode passed start_line without end_line → "must both be set" — the schema advertises them as two independent optional integers (src/tool/agent/file_edit.rs:74-78; mode dispatch :462; the error :493-503). (2) Highest frequency: old_string containing ' fails byte-exact matching when the file has \' (JS-escaped) — the matching core is literal_splice (:702-761, LF-projected EOL-agnostic matching, fuzzy_find :660 for whitespace normalization) shared by the single and batch paths. (3) No-op edits surface the generic "old_string and new_string are identical" from deep inside the splice (:716-720). Cross-reference audit done: ONLY file_edit.rs references its own line-range fields (args, dispatch, append/batch mutual-exclusivity checks :905/:1027, schema :1284-1285, ~19 test sites at 1467/1482/1495/1518/1535/1878/1884/2128-2330/2418/2499/2657/2673/2742); read_files/file_read/search_read start_line and frontend toolCardPaths are separate unaffected surfaces. Check src-tauri/src/ipc/contract_fixtures.rs:339 during step 1 — update only if it constructs file_edit args. The advertised schema flows through ToolRegistry::schemas (src/tool/mod.rs) automatically. Verify: cargo test green + warning-free (#![deny(warnings)] at both crate roots).

## Steps
- [x] 1. **Replace start_line/end_line with a `lines` tuple** — In src/tool/agent/file_edit.rs: FileEditArgs loses start_line/end_line, gains `lines: Option<Vec<usize>>` (serde default). Validate where line-range mode dispatches (prepare_edit ~:462 → prepare_edit_lines ~:491): exactly 2 elements ("lines must be exactly [start, end] — got N"), start >= 1, end >= start; drop the both-set arm (impossible now). Update the append mutual-exclusivity check (~:905), validate_batch_args (~:1027), the "old_string is empty" error text (~:470 — name `lines`), and schema() (~:1284-1285): one `lines` property, type array of integers, minItems 2 / maxItems 2, description "1-indexed inclusive [start, end] line range replaced by new_string — no old_string needed". Update every test in src/tool/agent/file_edit.rs referencing start_line/end_line to the `lines` shape; replace the "must both be set" tests (~:2175-2220) with a 1-element-array rejection test. Check src-tauri/src/ipc/contract_fixtures.rs:339 — update only if it constructs file_edit args.
- [x] 2. **No-op edit rejected early with a clear message** — In src/tool/agent/file_edit.rs execute() after the boundary-token restore (~:1329): single-mode (no edits/append/lines) with non-empty old_string == new_string → ToolResult::error "no-op edit: old_string equals new_string — nothing to change". Keep the in-splice normalized-equal guards (literal_splice ~:716-728, line-range no-change ~:566) for the whitespace/EOL-normalized cases. Update prepare_edit_identical_strings_error (~:1840) to the new message and position.
- [x] 3. **Escape-normalization fallback in the literal core** — In src/tool/agent/file_edit.rs literal_splice (~:702): when the exact LF-normalized needle matches nothing, try variants before returning None: (i) unescaped needle (\'→', \\→\), (ii) escaped needle ('→\', \→\\), (iii) whitespace-normalized (fuzzy_find) when not already fuzzy. Change literal_splice's return type to carry the match origin (e.g. Result<Option<(String, MatchOrigin)>> with MatchOrigin::{Exact, Unescaped, Escaped, WhitespaceNormalized}); on a variant match, splice with the variant needle under the same limit semantics (the batch ambiguity guard counts with the variant too). prepare_edit_literal and prepare_edit_batch (both in src/tool/agent/file_edit.rs) collect the origin(s) and append a success note "NOTE: matched via escape-normalization (…)" / "(whitespace normalization)" — never on exact matches; batch items get per-item notes joined into the output.
- [x] 4. **First-difference miss diagnostic** — In src/tool/agent/file_edit.rs: on a genuine literal miss (after variants), compute the best near-match in the LF projection: for each position sharing a >= 8-char common prefix with the needle, track the longest; report "matched P/Q chars; first difference at char K: file has X, you sent Y" (display-escaped, capped). Append to the fresh-read nudge in prepare_edit_literal's miss branch and the batch item miss (batch_item_error) — the EDIT_STALE_READ_MARK steering contract stays intact (append, never replace).
- [x] 5. **Quote-safety pre-flight in the description** — In src/tool/agent/file_edit.rs schema() description (~:1262) + module doc (~:5): add "old_string must match byte-exact: JS-escaped apostrophes (\'), backslashes (\\), and line endings are literal. If the anchor contains a quote, a backslash, or is >3 lines, prefer line-range mode (lines)."
- [x] 6. **Tests for every failure class** — New unit tests in src/tool/agent/file_edit.rs (tests module): 1-element `lines` array rejected with the clear message; escape fallback file-has-\'/needle-has-' → applies + note; reverse direction + note; whitespace-normalized fallback + note; exact match → NO note; no-op rejection message; miss error carries the first-difference pointer (construct a near-miss anchor); batch item escape fallback + per-item note. Each must fail without its fix.
- [x] 7. **Verify + bookkeeping** — cargo test green + warning-free (#![deny(warnings)]); search for stale start_line/end_line references to file_edit in docs/README/PLAN.md (expected none — the schema description is the doc surface); close backlog item 838b6f4e (backlog_status done + note naming this plan).

## Regression test
escape_fallback_file_escaped_needle_plain_matches_with_note
