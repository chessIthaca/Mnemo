# Plan: Clear trace log for clean repro capture

## Goal
Empty .coding/logs/traces.jsonl so the upcoming clean deepseek repro captures only fresh records, and hand the user the repro protocol.

## Kind
research

## Context
Follow-up to the fresh-eyes analysis (plan 77bf6692, BUG memory 2027-01-07): the user will re-run the deepseek hello repro WITHOUT interrupting, and wants the trace log clean so only the new capture's records are in the file. The old file mirrors the in-memory ring (~8-11 records of this analysis session) and would bury the new records; emptying it gives the repro a clean slate. The in-memory ring itself clears only on app restart — noted in the handoff instructions. Log-file maintenance only; no source-code changes.

## Steps
- [x] 1. **Empty the trace log** — NO-CODE RESEARCH (log-file maintenance, no source changes). Write empty content to .coding/logs/traces.jsonl via file_write (path '.coding/logs/traces.jsonl', content '') so the upcoming repro starts from an empty file. Confirm the write succeeded, then report the repro protocol to the user: optionally restart the app first (clears the in-memory trace ring — but log tracing is OFF by default after restart, so re-enable it), a fresh session gives the cleanest prompt conditions, then switch to deepseek, say hello, and do NOT interrupt — let the turn reach its natural end (completion-token cap, stall timeout, or model stop). The follow-up analysis must read .coding/logs/traces.jsonl FIRST, before making other requests, so the ring cannot rotate the repro records out.
