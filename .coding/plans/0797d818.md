# Plan: R12-R17: Cache-hit + context optimization batch

## Goal
Implement the 5 cache-hit + context optimization recommendations (R12-R15, R17) from the round-4 analysis: fix the cache-hit heuristic that masks real misses, lower the summarization threshold, truncate consumed tool results, reduce read_files default line cap, and add a sane max_output_tokens ceiling.

## Kind
implementation

## Context


## Steps
- [ ] 1. **R12: Fix cache-hit heuristic — report 0 when provider says 0, remove min(prev,curr) fallback (turn.rs + session struct + tests.rs + openai.rs + anthropic.rs + mod.rs + channels.rs + frontend types.ts + traceStats.ts + ipc-contract.test.ts + ipc-fixtures + contract_fixtures.rs + DeltaAccumulator + trace.rs LlmRequestSummary + LlmTraceView.tsx + InflightBar.tsx + agentState.ts + agentEventReducer.ts + useAgentEvents.ts + useAgentStore.ts + AgentEvent serialization in channels.rs + frontend AgentEvent type in types.ts + traceStats.ts + InflightBar.tsx + agentState.ts + agentEventReducer.ts + useAgentEvents.ts + useAgentStore.ts + ipc-contract.test.ts + ipc-fixtures + contract_fixtures.rs + DeltaAccumulator in turn.rs or wherever it is + LlmTraceView.tsx + all frontend references to cached_tokens + all test assertions on cached_tokens=0 behavior + the heuristic test if one exists + last_prompt_tokens field in session struct + its construction sites + any test referencing last_prompt_tokens + the AgentEvent::Usage serialization in channels.rs + the frontend AgentEvent type + all frontend consumers of cached_tokens + the trace record LlmRequestSummary cached_tokens field + the trace IPC serialization + the frontend trace types + the trace stats computation + the inflight bar cache display + the agent state cache hit tracking + the event reducer cache handling + the contract test fixtures + the ipc fixtures + all of it — change cached_tokens from u32 to Option<u32> in LlmEvent::Usage so None=not reported (heuristic applies) vs Some(0)=provider says zero (report 0). This is the correct fix but touches ~13 construction sites + 1 match arm + 4 test sites + the session struct + frontend types + trace records + IPC + contract fixtures. ALTERNATIVE SIMPLER FIX: just remove the heuristic entirely — when cached_tokens==0 report 0, remove last_prompt_tokens field + its write. This is ~15 LOC in turn.rs + session struct + any test referencing last_prompt_tokens. The simpler fix is correct for all providers that report cached_tokens (DeepSeek, OpenAI, glm via litellm all do). For providers that don't report it, 0 is more honest than a fabricated min(prev,curr). GO WITH THE SIMPLER FIX unless the Option approach is clearly better. Search for last_prompt_tokens to find the session struct field + all references. Remove the heuristic branch (turn.rs:1163-1173 else branch), remove the last_prompt_tokens write (turn.rs:1175-1182), remove the field from the session struct, remove any test that asserts heuristic behavior. Update the comment at turn.rs:1140-1146. Run cargo test.**
