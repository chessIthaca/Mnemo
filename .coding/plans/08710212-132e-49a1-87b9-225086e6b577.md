# Plan: Research: can a Tauri add_child webview share the parent's CDP debug port?

## Goal
Determine definitively whether a Tauri 2 `Window::add_child` webview can share the parent WebView2's CDP debug port (9222), so the agent's game_* tools can attach to the child. If yes → produce the concrete fix design. If no → document that the current child-webview inspection architecture is fundamentally unreachable and the iced_webview pivot is the justified path. No source changes; investigation only.

## Kind
research

## Context


## Steps
- [x] 1. **Confirm the child's process model + port-binding failure** — Re-run the Win32 + process diagnostic cleanly (fix the IntPtr conversion bug from the prior attempt): enumerate top-level + child HWNDs of the two myharness PIDs (26492, 47260) to confirm a child webview HWND exists (class Chrome_WidgetWin_0 / a child of the main window). Cross-reference with the process list: which PID owns the child HWND, and does that PID carry --remote-debugging-port=9222 but fail to bind it (netstat -ano | findstr 9222 shows only ONE listener). This confirms 'child = separate browser process, port already bound, no CDP' vs 'child never created'.
- [x] 2. **Read the Tauri 2 child-webview creation path** — Read src-tauri/src/ipc/browser_webview.rs (browser_webview_ensure, the add_child call) + the pinned tauri-2.x source in the cargo registry (~/.cargo/registry/src/.../tauri-2.x/) for WebviewBuilder + Window::add_child. Determine: does add_child create the child with the SAME WebView2 environment (CoreWebView2Environment) as the parent, or a new one? The environment is what owns the debug port. Find whether WebviewBuilder exposes an API to reuse the parent's environment (e.g. a with_environment / shared env option) or whether wry/tao forces a new environment per child.
- [x] 3. **Read wry + WebView2 environment semantics** — Read the pinned wry source (~/.cargo/registry/src/.../wry-*/) for how WebView2 environments are created on Windows: is --remote-debugging-port read per-environment (via WEBVIEW2_ADDITIONAL_BROWSER_ARGUMENTS) and does a second environment in the same process try to bind the same port? Search wry for 'additional_browser_arguments' / 'remote-debugging' / 'environment'. Determine whether two webviews in one process CAN share one debug port, or whether WebView2 fundamentally binds one port per environment (making a 2nd webview un-debuggable when the port is taken).
- [x] 4. **Check the prior spike's actual evidence** — Read .coding/reviews/2026-12-child-webview-browser-review.md + the plan d5d99364 step 1 result: the plan claimed (b) 'Invoke-RestMethod http://localhost:9222/json lists TWO page targets' was verified GO. Reconcile that claim with today's live probe showing ONE target. Was the spike run in a different config / a debug-only path that no longer holds? Determine whether this is a regression (something changed) or whether the spike verification was never actually run / was wishful. This tells us if there's a known-good state to bisect toward.
- [x] 5. **Produce the go/no-go + design memo** — Write findings to a memory_write (semantic): (1) the definitive answer to 'can a Tauri add_child webview share the parent CDP port', with the exact API evidence; (2) if YES — the concrete fix design (e.g. reuse parent environment, or a different child-creation API, or a per-child port); (3) if NO — a clear statement that the child-webview inspection architecture is fundamentally unreachable over CDP and the iced_webview pivot is the justified next step, with the specific reason. This memo is the gate for the next implementation plan (fix-in-place vs iced rewrite).
