# Plan: Measure full cargo-nextest run

## Goal
Measure the full cargo-nextest run time and compare it to the cargo test --lib baseline (13.55s). The test binary is already compiled; nextest should just execute. If it times out at the 300s shell limit, investigate why (nextest may compile its own harness) and report the finding.

## Kind
research

## Context


## Steps
- [x] 1. **Measure full cargo nextest run --lib (test binary already compiled). Run `cargo nextest run --lib` and capture the Summary line (total tests, passed, failed, ignored) + wall time. Compare to the cargo test --lib baseline (13.55s, 1653 passed, 0 failed, 12 ignored). If it times out at 300s, run `cargo nextest run --lib --no-fail-fast` in the background or try a subset approach to estimate the full time. Record the measured time + the speedup/slowdown vs cargo test.**
