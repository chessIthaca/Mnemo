# Plan: Graph-first discipline: symbol-intent search patterns get the id-carrying nudge

## Goal
Symbol-lookup discipline: search/search_read on symbol-INTENT patterns (trailing '(', '::'-paths, callers/who-calls/where-defined phrasing) gets the id-carrying graph nudge on FIRST use, the tool descriptions tell the agent graph-first for symbol questions, and a HOW memory records the discipline.

## Kind
implementation

## Context
Backlog 8b8f40d2 (reference HOW a146c39d: graph_search is lexical). Existing machinery: symbol_nudge (src/tool/agent/search.rs:565) fires ONLY for bare identifiers, definition-prefixed names (strip_definition_prefix), or alternations (alternation_nudge) — exact case-sensitive graph.symbol_id() lookup, embeds the id. C3 escalation (ESCALATION_THRESHOLD=2) + C5 intercept (dispatch.rs symbol_search_redirect) punish repeat ignoring, but nothing fires for symbol-INTENT patterns that aren't exact shapes: the 2026-12-04 instances (send_suggestion, send_prompt, on_main_turn_resolved, run_all_dispatch_next, backlog_add, steer) searched for symbol usage whose natural patterns (`foo(`, `who calls X`, `X::y`) never nudge. WORDING CONTRACT: both note variants must carry the "is an indexed symbol" marker substring; the bare-identifier sentence is byte-pinned (2026-09-15 steering spec) — new shapes must go through the existing variant sentence ("the symbol '{name}' (from pattern '{pattern}') is an indexed symbol — …"). The exact symbol_id() lookup stays the precision guard (no fuzzy matching; non-symbols like "who calls the shots" fail lookup → None). Also: live session proof the C5 gate works (it intercepted this session's own 'fn symbol_nudge' search); the strengthened nudge must not fire when the graph is absent/unindexed (existing None paths).

## Steps
- [x] 1. **HOW memory + tool descriptions** — Write the procedural HOW memory now (backlog 8b8f40d2): symbol questions (
- [x] 2. **Extend symbol_nudge intent matching** — src/tool/agent/search.rs: add a pattern→name extraction helper (e.g. strip_symbol_intent) handling: (a) trailing '(' — 'foo(' → foo (call-site hunting); (b) '::'-paths — try the full path 'a::b' then the last segment via graph.symbol_id; (c) phrasing — 'callers of X' / 'who calls X' / 'who uses X' / 'where is X defined' → X. All extracted names still go through the EXACT case-sensitive symbol_id() lookup (precision guard, false positives return None) and reuse the existing variant sentence with the marker substring. Doc comment on symbol_nudge updated to enumerate the shapes. Do NOT touch the byte-pinned bare-identifier sentence or the alternation fallback order (prefix/bare/alternation first, then the new intent shapes).
- [x] 3. **Tests for the new shapes** — In src/tool/agent/search.rs tests (reuse the indexed-graph test pattern from dispatch.rs make_indexed_graph or the module's own fixture): (1) 'foo(' → Some, contains graph_context(id=...) and 'is an indexed symbol'; (2) 'who calls foo' → Some; (3) 'where is foo defined' → Some; (4) '::'-path resolving via last segment → Some, unresolvable → None; (5) intent phrasing with a non-symbol target ('who calls the shots') → None; (6) plain text pattern unchanged → None; (7) regression: bare 'foo' still emits the byte-pinned sentence exactly (assert equality with the current pinned string). Run cargo test --lib search:: — all green including existing nudge tests.
- [x] 4. **Docs sweep + full-workspace verify** — Check README.md / module docs for steering/nudge prose that enumerates the nudge shapes (search.rs module doc, dispatch.rs C5 doc comment, any steering spec mentions) and update to include the intent shapes. Then verify: root cargo test UNPIPED ($LASTEXITCODE), cd src-tauri cargo test unpiped. Mark backlog 8b8f40d2 in_flight→done as work proceeds.
- [x] 5. **Closing sequence** — spawn_agent role:'reviewer' over ALL uncommitted changes (self-contained task: changed files, the intent-shape design, wording-contract constraints, plan goal; ask for correctness incl. false-positive risk of the phrasing extraction, wording-contract compliance (marker substring + pinned sentence), test regression value, security, docs sync, multi-platform neutrality). Fix every finding, re-run tests, commit on the working branch including the review report, then finish (PASS report or round-2 verification per established pattern).
