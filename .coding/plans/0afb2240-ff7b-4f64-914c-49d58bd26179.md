# Plan: Drop Safety tab/sidebar button + fix misleading red tool card

## Goal
(1) Remove the redundant right-panel "Safety" tab and its left-sidebar toggle button — safety rules are now edited in the Settings dialog's SafetySection. (2) Fix the chat ToolCard so a fail→redo→succeed sequence no longer stays red: the card reflects the last call's outcome instead of "any call failed".

## Context
Verified during exploration:
- RightPanelTab union + ALL_RIGHT_PANEL_TABS live in frontend/src/hooks/agentState.ts (lines 163-183). The "safety" member drives BOTH the right-panel registry (rightPanelViews.tsx:54) and the left-sidebar TOOL_META (Sidebar.tsx:25). Removing "safety" from the union+array removes it from both UI sites at once.
- rightPanelViews.test.ts asserts parity between the union and the registry — removing "safety" from BOTH keeps parity (7 entries each). It will still pass.
- disabledTabs is NOT persisted to localStorage (confirmed: only fonts/colors/theme/sizes use readLs/writeLs), so no orphaned "safety" entry on reload. Store default disabledTabs = ALL_RIGHT_PANEL_TABS.filter(t => t !== "plan") still valid.
- Only rightPanelViews.tsx imports the SafetyRules.tsx view component. SafetySection.tsx (Settings) has its OWN inline textarea editor and does NOT import SafetyRules.tsx — so deleting SafetyRules.tsx is safe.
- SettingsDialog.tsx "section === 'safety'" is the SETTINGS section id (types.ts SettingsSectionId), NOT the right-panel tab — stays untouched. StatusBar safety-mode dropdown + SafetyToggleDialog stay (runtime mode toggle).
- Message.tsx ToolCard (lines 335-402): failed = calls.some(c => c.result !== null && !c.result.success). No rendering test asserts the color (searched). Changing to "last call's outcome once all done" fixes fail→redo→succeed. Per-call ✓/✗ already shown in CallDetail (line 440).
- tsconfig: noUnusedLocals/noUnusedParameters = false, so leftover imports won't fail tsc, but I'll clean them anyway.
- No Rust changes. Frontend checks: npm run build (tsc+vite), npm test (vitest).

## Steps
- [x] 1. **Remove "safety" from the RightPanelTab union + ALL_RIGHT_PANEL_TABS** — in frontend/src/hooks/agentState.ts: delete the `| "safety"` line (line 169) from the `RightPanelTab` union, and delete the `"safety",` entry (line 180) from the `ALL_RIGHT_PANEL_TABS` array. Leave md/plan/diff/output/files/stats/backlog. This single edit removes the tab from both the right panel and the left sidebar (both derive from this union/array).
- [x] 2. **Drop the safety entry from the right-panel view registry** — in frontend/src/hooks/rightPanelViews.tsx: delete the line `{ id: "safety", label: "Safety", icon: ShieldCheck, component: SafetyRules },` (line 54). Remove the now-unused `import { SafetyRules } from "../components/views/SafetyRules";` (line 26). Remove `ShieldCheck` from the lucide-react import on line 18 (it's no longer used by any registry entry).
- [x] 3. **Drop the safety entry from the left-sidebar TOOL_META** — in frontend/src/components/layout/Sidebar.tsx: delete the `safety: { label: "Safety", icon: ShieldCheck },` line (line 25) from the TOOL_META record. Remove `ShieldCheck` from the lucide-react import on line 1-13 (no longer used).
- [x] 4. **Delete the now-dead SafetyRules.tsx view** — run `Remove-Item frontend/src/components/views/SafetyRules.tsx` (PowerShell). Nothing imports it after step 2. If the command can't run, leave it (harmless dead code) and note it; the build still passes since nothing references it.
- [x] 5. **Fix the misleading red ToolCard on fail→redo→succeed** — in frontend/src/components/chat/Message.tsx, in the ToolCard function (around line 343-344): replace `const failed = calls.some((c) => c.result !== null && !c.result.success);` with logic that reflects the LAST call's outcome once all calls are done: `const lastCall = calls[calls.length - 1]; const failed = !running && lastCall.result !== null && !lastCall.result.success;` (place it AFTER the `running` line so `running` is defined). Add a short comment: the card reflects the last call's outcome (not "any failed") so a fail→redo→succeed sequence ends green; per-call ✓/✗ is still shown in the expanded detail. The existing `running` line stays unchanged. The header `{failed ? "✗" : "✓"}` and the borderColor/bgColor/textColor ternaries already key off `failed`, so they automatically follow.
- [x] 6. **Build + test + review + commit** — From C:\AgenticCoder\AgenticCoder\frontend run `npm run build` (tsc + vite) and `npm test` (vitest); confirm both pass (esp. rightPanelViews.test.ts parity still holds with 7 entries). Then run `cargo test` from the project root per the constitution (no Rust changed, but confirm green). Spawn a read-only reviewer subagent to review ALL uncommitted changes (git diff HEAD): list changed files (frontend/src/hooks/agentState.ts, rightPanelViews.tsx, Sidebar.tsx, chat/Message.tsx, deleted SafetyRules.tsx) and the plan's two goals; ask for correctness/bugs/security/constitution findings (esp. that no dangling "safety" references remain, the parity test still holds, the ToolCard last-result logic is correct when some calls are still running, and dead-code deletion is clean). Have it write findings to .coding/reviews/<date>-safety-tab-removal-review.md. Read the report, fix every finding, re-run npm build+test, then commit to the current feature branch with a clear message and include the review report.
