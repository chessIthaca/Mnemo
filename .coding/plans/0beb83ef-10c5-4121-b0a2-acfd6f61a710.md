# Plan: Fix Vite build warnings

## Goal
Resolve the Vite v8 build warnings: (1) the `optimizeDeps.rollupOptions` deprecation by switching to `@vitejs/plugin-react-oxc` and removing any deprecated options, and (2) the `INEFFECTIVE_DYNAMIC_IMPORT` warning by replacing the redundant dynamic `import("./lib/tauri")` calls with static imports (the module is already statically imported in both files).

## Context
Project root is C:\myHarness (not C:\AgenticCoder\AgenticCoder). It's an npm workspace: root package.json has workspaces:["frontend"], root package-lock.json, build via `npm run build --workspace frontend`. No frontend lockfile.

Latest @vitejs/plugin-react-oxc is 0.4.3 (NOT ^1.0.0). Its peer dep is vite ^6.3.0 || ^7.0.0 (doesn't list Vite 8), but the currently-installed @vitejs/plugin-react@4.7.0 is ALSO already invalid for Vite 8 (caps at ^7.0.0) and the build works anyway — Vite itself recommends the oxc switch. So switching is the right move; the peer-dep mismatch is pre-existing and harmless.

Installed: vite@8.2.1, @vitejs/plugin-react@4.7.0.

## Steps
- [x] 1. **Switch React plugin to @vitejs/plugin-react-oxc** — In `frontend/package.json`, replace `"@vitejs/plugin-react": "^4.3.3"` in devDependencies with `"@vitejs/plugin-react-oxc": "^0.4.3"`. Then in `frontend/vite.config.ts` change the import `import react from "@vitejs/plugin-react"` to `import react from "@vitejs/plugin-react-oxc"` (the `react()` call in `plugins: [react()]` stays the same). This removes the deprecated `optimizeDeps.rollupOptions` path that the old plugin sets.
- [x] 2. **Install the new plugin** — Run `npm install` from the project root C:\myHarness (npm workspace) to update package-lock.json and install @vitejs/plugin-react-oxc. Confirm it resolves without error.
- [x] 3. **Replace dynamic import in App.tsx with static import** — In `frontend/src/App.tsx`, add `getSettings` to the existing static import block from `"./lib/tauri"` (lines 6-13, which already imports getGitBranch, getWorkflowState, etc.). Then replace `const { getSettings } = await import("./lib/tauri");` at line 204 with a plain `const settings = await getSettings();` call (drop the dynamic import entirely).
- [x] 4. **Replace dynamic import in StatusBar.tsx with static import** — In `frontend/src/components/layout/StatusBar.tsx`, add `getGitBranch` to the existing static import block from `"../../lib/tauri"` (lines 4-11). Then replace `const { getGitBranch } = await import("../../lib/tauri");` at line 78 with a plain `setGitBranch(await getGitBranch());` call (the dynamic import is fully redundant since getGitBranch is already statically imported).
- [x] 5. **Build and verify warnings are gone** — Run `npm run build` from C:\myHarness and confirm: no `optimizeDeps.rollupOptions` deprecation, no `INEFFECTIVE_DYNAMIC_IMPORT` warning, and the build still produces the frontend-dist output. Also run `npm test --workspace frontend` to ensure nothing broke.
- [x] 6. **Review and commit** — Spawn a read-only reviewer subagent to review all uncommitted changes (frontend/package.json, frontend/vite.config.ts, frontend/src/App.tsx, frontend/src/components/layout/StatusBar.tsx, package-lock.json) for correctness/bugs/security/constitution. Have it write findings to `.coding/reviews/<date>-vite-warnings-fix-review.md`. Fix any findings, re-run build+test, then commit to the current feature branch with a clear message.
