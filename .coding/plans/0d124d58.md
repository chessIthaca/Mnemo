# Plan: [models.reviewing] is reviewer-spawn-only — main agent stays on [models.executing]

## Goal
[models.reviewing] applies ONLY to the spawned reviewer subagent (spawn-time pin); the main agent keeps the [models.executing] model in the Reviewing state (Reviewing resolves the executing slot, never the reviewing one).

## Kind
implementation

## Context
Decision 2026-12-06 (user, backlog 86c90ff6): [models.reviewing] is reviewer-spawn-only. Key reading: the requirement is "main agent stays on [models.executing] even in Reviewing". A literal `Reviewing => None` in the resolver would fall to the DEFAULT provider (resolve_model_ref(None) -> None -> default), not the executing model — so the fix is `WorkflowState::Reviewing => models.executing.as_ref()` in src/model_resolver.rs:239 (removes the models.reviewing lookup from the main-agent chain; reviewing state now resolves exactly like executing). The reviewer pin (src/tool/agent/spawn_agent.rs:253-263) currently rides that state chain via resolve(Reviewing,...) — it must switch to an explicit [models.reviewing] resolution, preserving the exact precedence chain explicit model > reviewing > executing > subagent > default (tests reviewer_falls_back_to_executing_model / reviewer_falls_back_to_subagent_slot / reviewer_without_any_configured_model_gets_default / explicit_model_arg_beats_reviewer_pin / non_reviewer_role_gets_no_pin). Current code facts: resolve() order = skill > subagent(if is_subagent) > state > resolve_model_ref (dangling endpoint dropped per link); ModelResolver trait has ConfigModelResolver + test doubles; main agent passes live WorkflowState (incl. Reviewing) from loop_impl.rs:969; workflow state machine + finish gates UNCHANGED (the "Executing then goes to complete" sentence is future direction, out of scope); the "fewer tools" secondary benefit is commentary, no tool change. Docs to sync: README.md:56, PLAN.md ~412-426, model_resolver module doc, loop_impl.rs:206+1380 comments, knowledge spec 2026-08-27, possibly Settings UI copy.

## Steps
- [x] 1. **Resolver: Reviewing arm -> executing slot** — In src/model_resolver.rs ConfigModelResolver::resolve (~line 239): change `WorkflowState::Reviewing => models.reviewing.as_ref().or(models.executing.as_ref())` to `WorkflowState::Reviewing models.executing.as_ref()` with a comment: [models.reviewing] is reviewer-spawn-only (decision 2026-12-06) — the main agent keeps the executing model in Reviewing; the reviewing slot is consumed only by resolve_reviewer_model (added next step). Update the resolve() doc-comment priority list (~224-242) and the module doc (~lines 5-30) accordingly. Keep arm order (skill > subagent > state > resolve_model_ref) untouched.
- [x] 2. **Resolver: add resolve_reviewer_model + tests** — Add to the ModelResolver trait: `fn resolve_reviewer_model(&self) -> Option<crate::config::ModelRef> { None }` (default None; doc comment stating the chain reviewing -> executing (back-compat) -> subagent -> None/default, and that this is the SOLE consumer of the reviewing slot). Implement on ConfigModelResolver with per-link validation (mirrors current fall-through on dangling endpoints): `cfg.resolve_model_ref(models.reviewing.as_ref()).or_else(|| cfg.resolve_model_ref(models.executing.as_ref())).or_else(|| cfg.resolve_model_ref(models.subagent.as_ref()))`. Tests in the existing tests mod: (a) rewrite reviewing_state_resolves_with_fallback_to_executing -> main-agent semantics: reviewing+executing set -> resolve(Reviewing,None,false) returns EXECUTING; executing only -> executing; reviewing ONLY -> None (reviewing never applies to the main agent — regression test for this defect); both unset -> None. (b) new resolve_reviewer_model tests: reviewing set -> reviewing; unset -> executing; both unset -> subagent; none -> None; dangling reviewing endpoint falls through to executing.
- [x] 3. **spawn_agent: pin via resolve_reviewer_model + tests** — src/tool/agent/spawn_agent.rs: replace the two-step pin (253-263, resolve(Reviewing,false).or_else(resolve(Reviewing,true))) with a single `resolver.resolve_reviewer_model()` call inside the same role=="reviewer" && no-model guard; keep feeding it through the existing explicit-model flow. Update the tool's doc comment (~70-81): reviewer chain is explicit model > [models.reviewing] > [models.executing] > [models.subagent] > default, resolved at spawn via resolve_reviewer_model — the main agent itself never uses [models.reviewing] (stays on executing in Reviewing). Update the StateResolver test double: implement resolve_reviewer_model from its reviewing/executing/subagent fields (record the call like `asked` does); make its resolve() return None for Reviewing (or panic-free sentinel) so the pin tests prove the state chain is no longer consulted — e.g. reviewer_pins_reviewing_model must fail if the pin regressed to resolve(Reviewing). Update all pin tests: reviewer_without_model_arg_pins_reviewing_model, reviewer_falls_back_to_executing_model, reviewer_falls_back_to_subagent_slot, reviewer_without_any_configured_model_gets_default, explicit_model_arg_beats_reviewer_pin (asked stays empty), non_reviewer_role_gets_no_pin.
- [x] 4. **loop_impl + stale comment sweep** — src/agent/loop_impl.rs:206 and :1380 — drop "reviewing" from the lists of main-agent state overrides ([models.planning]/[models.executing]/[models.complete]) and, where prose implies the main agent runs the reviewing model, state the reviewer-spawn-only rule. Sweep `models.reviewing` mentions in src/ + src-tauri/ for other stale prose (config/general.rs struct doc for the reviewing field, settings.rs wire/DTO docs — slot stays configurable, but doc text saying the main agent switches to it in Reviewing must be corrected). Config parse/save/patch code itself is UNCHANGED.
- [x] 5. **Docs sync** — README.md:56 reviewer bullet — keep the reviewer-model chain sentence (still true) and fix the trailing "per-workflow-state model routing (different models for planning vs. executing vs. reviewing)" clause -> planning/executing/complete for the main agent, [models.reviewing] reviewer-spawn-only. PLAN.md ~412-426: amend the models paragraph — main agent keeps [models.executing] through the Reviewing state; the reviewing slot feeds only the spawned-reviewer pin via ModelResolver::resolve_reviewer_model. Add a dated addendum to .coding/knowledge/spec/2026-08-27-spawned-reviewers-run-on-the-models-reviewing-mo.md (mechanism now resolve_reviewer_model; main-agent Reviewing arm dropped). Check frontend Settings copy + example *.toml for prose claiming the main agent switches to reviewing in Reviewing; fix if found.
- [x] 6. **Verify: full-workspace tests** — Root `cargo test` UNPIPED (PowerShell: run it, then `"exit=$LASTEXITCODE"`; deny(warnings) means green = zero warnings), then `cd src-tauri; cargo test` unpiped the same way (cross-crate trait change — full workspace per HOW memory). Frontend `npx vitest run` + `npx tsc --noEmit` only if any frontend file changed. Fix any failures before review. Mark backlog item 86c90ff6 in_flight->done as work proceeds; write DECISION memory (reviewer-spawn-only reviewing slot + resolve_reviewer_model mechanism) and supersede the stale 2fd29f36 pin-mechanism digest.
- [x] 7. **Closing sequence** — spawn_agent role:"reviewer" over ALL uncommitted changes (self-contained task: list changed files + plan goal; ask for correctness of the precedence chain, main-agent-keeps-executing semantics, test coverage, security, docs sync, multi-platform neutrality). Fix every finding, re-run tests, commit on the working branch including the review report, then finish.
