# Plan: Steering follow-ups: tool-scoped detection, recency-gated riders, expandable memory hits

## Goal
Steering follow-ups from the round-2 session's own tool-choice findings: make steering-marker detection tool-scoped so content-bearing tool outputs can never fire a marker falsely; age-gate the recalled-context riders so stale digests stop riding every plan; and surface memory_search's matched hits expandably in the UI (backlog 41f39672). Also record the decision on how to use the new steering counters before de-emphasizing explicit memory_search.

## Kind
implementation

## Context
Follow-ups to the shipped round-2 (plan b30475ba, commits b7150a3+d01e4ee on wt/agenticcoder, round-2 verify PASS, tree clean; user will REBUILD + RESTART the app after this plan is created — the plan's own changes are NOT in that rebuild). Items chosen from the main agent's own tool-choice analysis during b30475ba plus review round 1's finding 3 (tidied, not fully solved, by doc notes) and backlog 41f39672. GROUNDING (verified this session): (1) SteeringStats (src/agent/steering_stats.rs) currently runs ONE substring scan over EVERY tool result against the 5-marker table (MARKERS const :114-120, first-match-wins; observe_result at :190, signature (agent_id, output)); the dispatch call site is AgentLoop::dispatch_with_interrupt (src/agent/dispatch.rs:391-436 — arm A :405-408 observes `r.output`, arm B channel-closed :429-431 observes `result.output`; both have parsed_call.name in scope). Evidenced misfire this session: a file_edit result whose OUTPUT echoed the user phrase "deletion session" ... concretely, content-bearing tools (read_files bodies, web_fetch text, shell stdout, file_edit diffs) can contain any marker substring ("is an indexed symbol", "RECALLED CONTEXT" literally appear in this repo's knowledge specs). Round-2 review finding 3 recommended only a doc note; the cleaner fix is tool-scoped detection. Actual nudge emitters (reviewer-verified): search.rs:243 symbol_nudge + search_read (note PREPENDED via merged_note/with_note → always on the FIRST LINE of output), shell.rs:41 GREP_NUDGE ("TIP: for file-content search", prepended at shell.rs:261 → first line), codegraph.rs miss_hint ("No symbols matched", serialized inside pretty-JSON "hint" field; graph outputs carry no arbitrary file content), read_files.rs:265 ("SYMBOL NUDGE:" is output.starts_with by construction — single-prefix join), plan.rs recall_rider append ("RECALLED CONTEXT", create_plan only — spawn_agent's task copy never becomes a tool result). (2) Recency: recalled_context_block (src/tool/steering.rs:30-95) caps at 5 hits, dedupes by id, uses recall_peek limit 3 (+Bug limit 2) — NO age filter, so month-old digests ride every create_plan (observed: 2026-08-10 and 2026-08-14 PLAN digests surfaced during this session while fresher, more relevant context existed). Memory::new(tier, title, content, ts) takes an explicit unix-seconds created ts (plan.rs test seeds 1_000_000_000 for deterministic age) — confirm the exact created/last_accessed field names on the Memory struct and ScoredMemory in src/memory/types.rs before coding. MemorySearchConfig (src/memory/mod.rs:1355 area) already powers decay + caps — a steering-age const can live in steering.rs (simplest; config plumbing only if trivial). (3) Frontend memory_search cards: parseMemoryEntry (frontend/src/hooks/agentEventReducer.ts:371) ALREADY extracts per-hit {tier, title, id, score, strength} from the "N memories matched:" output; Message.tsx:124 builds memorySearchLabel from args, toolCardPaths.ts:287 has memorySearchLabel (+ the count is parsed per spec 2026-08-25). The transcript card renders only the count — the hits are invisible (backlog 41f39672, exact ask: "12 matched ... alas no expanding to see the 12"). vitest include list REPLACES discovery — new test FILES must be registered in frontend/vitest.config.ts; prefer extending existing registered files (agentEventReducer.memory.test.ts, toolCardPaths.test.ts). Node-env tests only (no DOM renderer) — use a pure hit-parser + source-contract test if needed. (4) Explicit memory_search vs auto-recall redundancy (6/9 mid-session lookups returned noise the fresh auto-recall already carried): analysis-only decision — NOW MEASURABLE via the round-2 counters; record a DECISION memory telling the steering-data analysis (post-rebuild) how to compare recall-rider fired/switched against memory_search usage before de-emphasizing the prompt habit. NO code change for this item. Constraints: #![deny(warnings)] both crates, doc comments on public items, cargo test before each step (unpiped, $LASTEXITCODE), npm run build for frontend steps, NEVER reorder TOOL_STRATEGY (prompt.rs pinned ~:997-1006, :1237-1244 — untouched here), platform-neutral, no #[allow], never commit to main. Branch: reuse wt/agenticcoder (create_plan default). marker: covers backlog 41f39672 (frontend step); do NOT touch backlog state via file tools.

## Steps
- [x] 1. **Tool-scoped marker detection in SteeringStats** — Rework the metric input from full-output substring scanning to tool-scoped detection (kills the content-bearing misfire class: review 2026-09-14 finding 3's real fix; observed case: a file_edit result echoing 'deletion session''). In src/agent/steering_stats.rs: extend MarkerKind with a fn detect(tool_name: &str, output: &str) -> Option<MarkerKind>-style per-tool detector table (or a free fn `fn detect(tool_name, output)` beside MARKERS — read the file first and pick the shape that keeps MARKERS cohesive): SearchNudge fires ONLY for tools search/search_read AND only when the OUTPUT'S FIRST LINE contains "is an indexed symbol" (the note is prepended, so line-1 scoping eliminates file-content echo); ReadNudge ONLY for read_files via output.starts_with("SYMBOL NUDGE:"); ShellTip ONLY for shell (prepended — first-line check "TIP: for file-content search"); GraphMiss ONLY for graph_search/graph_context/graph_impact/graph_path (output.contains "No symbols matched" — their outputs are structured JSON, no arbitrary content); RecallRider ONLY for create_plan (output.contains "RECALLED CONTEXT"). observe_result gains a tool_name: &str param; update the dispatch call sites (both arms of dispatch_with_interrupt pass parsed_call.name — it is already in scope) and keep snapshot()/wire shape (5 kinds) UNCHANGED so the frontend types need no edits. Update the module doc: tool-scoped detection replaces the advisory-substring caveat's cross-tool worst case (keep a note that even tool-scoped detection is approximate). Rewrite the test suite: keep next_target_call_switches / non-target / per-agent tests (update call sites with tool names); replace each_marker_fires_and_is_classified with a matrix asserting per kind: its own tool+output fires it, ANOTHER tool carrying the same text does NOT (e.g. read_files output containing 'is an indexed symbol' fires nothing; a hypothetical search result containing 'SYMBOL NUDGE:' fires nothing), first-line scoping (marker on line 5 of a search output → no fire). doc-comment MARKERS: multi-marker co-occurrence is now impossible by construction (one tool can emit at most its own kind) — update the first-match-wins comment. Run cargo test (steering_stats + dispatch tests).
- [x] 2. **Recency gate for the recalled-context riders** — Stop month-old digests from riding the riders (observed during b30475ba: 2026-08-10/2026-08-14 PLAN digests surfaced via create_plan/spawn_agent riders while fresher context existed). In src/memory/types.rs confirm the Memory timestamps (created vs last_accessed unix secs) and ScoredMemory shape. In src/tool/steering.rs recalled_context_block: add a const STEERING_MAX_AGE_DAYS: u64 = 14 with a doc comment (riders surface CURRENT project context; old plans/reviews are discoverable via memory_search and would otherwise ride every plan for months); after the dedupe/truncate, apply the age filter to the BROAD query's hits only — drop hits whose created (use the created-at field you confirmed) is older than now - 14 days; the record_type Bug pass (bug_query) is EXEMPT (a recorded root cause is durable knowledge regardless of age — doc-comment why). Use std fallback 'now' (unix seconds) so it is deterministic and testable: refactor the block to take an internal now_secs helper (single impl, tests may not need to inject since const-based cutoffs are stable — if the hint would drift, add a #[cfg(test)] injection point; choose the simplest shape that lets tests seed 1_000_000-recent vs 30-days-old hits like plan.rs does). Tests in steering.rs (new test module mirroring plan.rs's helper style: HashEmbedder + MemoryStore::open_in_memory, Memory::new with explicit ts): a fresh semantic hit (ts = now-1day) rides; a stale one (ts = now-30d) is silently dropped (result/output unchanged); a stale BUG hit still rides via the bug pass; boundary (ts = exactly the cutoff) is kept (>= semantics). The two plan.rs rider tests must stay GREEN unchanged (their seeds are ts=1_000_000_000 — evaluate: that is 2001 → >30d old → they would now FAIL; update the seeded ts in those two tests to a recent value via SystemTime and keep their other assertions unchanged — deliberate, documented change). Run cargo test.
- [x] 3. **Expandable memory_search hits in the activity card** — Backlog 41f39672: 'memory search: 12 matched ... alas no expanding to see the 12'. Frontend-only. The parser already extracts per-hit fields (frontend/src/hooks/agentEventReducer.ts parseMemoryEntry, ~:371: tier/title/id/score/strength from the '[tier] title (id: …, score: x, strength: y)' lines; header 'N memories matched:'). (1) Extract/confirm a pure helper memorySearchHits(output: string): {tier,title,id,score?,strength?}[] in the memory-entry parsing area (if parseMemoryEntry already returns hits, extract/reuse it — do not duplicate); make it exported for tests. (2) UI: in the memory activity card (frontend/src/components/chat/Message.tsx — the memory_search branch around :120-124 — follow whatever component renders memory entries; if the card lives in its own component, extend it), render the parsed hits as a collapsed-by-default expandable row under the existing 'query chip + N matched' header: each hit one line with a tier badge + truncated title + score (click toggles the list — same expand pattern as the ToolCard body toggle; keep the raw output available via the card's existing expand so no information is lost). Cap rendering at the parsed hit list only (the backend already caps results). (3) Tests: extend the EXISTING registered frontend/src/hooks/agentEventReducer.memory.test.ts with a hits-list test for a real 2-hit output + zero-hit output (already covered) — and if the card lives in Message.tsx, add a source-contract test in an ALREADY-REGISTERED file (check frontend/vitest.config.ts:57+ first; e.g. extend toolCardPaths.test.ts or create + register a new file ONLY if necessary). Run `cd frontend; npm test` (vitest) then `npm run build` (tsc strict catches unused) — both unpiped via $LASTEXITCODE.
- [x] 4. **Docs + decision record + full matrix + closing sequence** — (1) README.md: the steering bullet (~:53) — update the Trace-tab counters clause: detection is tool-scoped (content-bearing outputs can no longer fire kinds spuriously) and the recalled-context riders now age-gate at 14 days (bug root causes exempt); the Agents&interface or Trace bullet gains a half-line: memory_search activity cards show their matched hits expandably (tier · title · score). (2) PLAN.md: the Agent-tools bullet's steering sentence — replace the advisory-substring caveat wording with tool-scoped detection; the Workflow-tools/create_plan rider mention gains '(recent 14 days; bug records exempt)'. (3) memory_write ONE DECISION record (semantic): 'Recall-rider vs explicit memory_search — measurement plan': the round-2 counters (get_steering_stats / recall-rider marker) let a future session compare rider fired/switched against mid-session explicit memory_search redundancy (observed 6/9 unrelated during b30475ba); decision: do NOT de-emphasize memory_search in TOOL_STRATEGY yet — decide after ~2 weeks of real steering data; the age gate (step 2) is the cheap half of that fix. (4) Run the FULL matrix UNPIPED reading $LASTEXITCODE + 'test result:' lines: cargo test at root, cd src-tauri; cargo build, cd frontend; npm run build. Then the closing sequence: spawn reviewer role:"reviewer" over ALL uncommitted changes (self-contained task: this plan's goal + the changed file list; focus = detector-table/nudge-site substring parity — any drift silently zeroes a metric, age-gate boundary semantics + the two updated plan.rs seed timestamps, frontend no-loss-of-information for the card, docs sync, multi-platform neutrality, no #[allow]); fix EVERY finding, re-run the matrix, commit to wt/agenticcoder including the review report; if the verdict is FINDINGS, a round-2 verify reviewer (PASS required) before finish. backlog 41f39672 is covered by step 3 — note that in the commit message so the user can close the backlog item after rebuild.
