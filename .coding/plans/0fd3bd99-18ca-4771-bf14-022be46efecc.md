# Plan: Verify auto-recall fix resolution + Grammarly decay diagnosis

## Goal
Verify with code + live-store evidence whether the auto-recall fixes (recall_peek no-bump, expandable hits UI) are resolved, diagnose why the Grammarly memory still surfaces at high score, and assess the few-writes hypothesis — then report findings and a recommendation to the user.

## Kind
research

## Context
User asks to verify plan 420ed603 ("Break memory recall self-reinforcement + expandable auto-recall hits") is really resolved in the running app: (1) they still see "auto-recall — 5 hit(s) — top: Grammarly…" appearing after file reads; (2) the April Grammarly memory still tops recall scores — they expected decay/deterioration by now, and suspect too few new memory writes; (3) they want the "N hit(s)" line clickable/expandable like ToolCard args. Search already confirmed recall_peek exists at src/agent/turn.rs:385/396, src/memory/mod.rs:847, and Message.tsx:95 has `const expandable = entry.name === "auto-recall" && hits.length > 0;` in the working tree. Unknowns: whether the expandable UI is complete + committed (vs uncommitted on feat/search-content-index), why a MemoryRecalled event fires after file reads (per-turn cache invalidation on large tool appends, turn.rs:1303), why Grammarly still scores 0.74 (query keyword match vs decay vs small store vs fix recency), and how many memory writes actually happen.

## Steps
- [x] 1. **Verify recall_peek fix + when events fire** — Read src/agent/turn.rs:130-430 (per-turn recall cache, both fresh branches, event emit) and :1290-1320 (large-append invalidation) to explain why an auto-recall entry appears after file reads. Confirm src/agent/turn.rs:385+396 use store.recall_peek and src/tool/memory/mod.rs still uses recall (bumping).
- [x] 2. **Verify expandable hits UI end-to-end + commit state** — Read frontend/src/components/chat/Message.tsx memory entry/MemoryEntryCard (expansion UI, chevron, hits rendering), frontend/src/hooks/agentEventReducer.ts reduceMemoryRecalled (entry.hits wiring), src/runtime/channels.rs MemoryRecalled/RecallHit. Run git_diff to see which of these files are uncommitted vs committed on the current branch — determines whether the running build should have the feature.
- [x] 3. **Diagnose Grammarly score + decay mechanics** — Read src/memory/strength.rs (decay formula, half-life, floor) and src/memory/mod.rs scoring (sim/strength/keyword/tier weights) + recall_peek/batch_access. Determine: does passive recall still refresh anything? Does decay depend on last_accessed_at (i.e., decay only started when the fix landed)? With limit-5 recall, can a small store surface everything regardless of decay?
- [x] 4. **Inspect live store + write volume** — Call memory_recall with query 'Grammarly' to see its live tier/score/strength/access_count. Count/tier of memories via additional recall probes (e.g. broad queries) to estimate store size and whether top-5 always fills. This is a deliberate lookup (bumping path is correct here).
- [x] 5. **Synthesize findings + recommendation** — Write up: what's fixed vs not, why Grammarly still tops (likely: query contains the literal keyword 'Grammarly' + tiny semantic store + decay only began when recall_peek landed), assessment of the few-writes hypothesis (session-end consolidation + prompt guidance), and any real defects found (e.g. duplicate auto-recall transcript entries after big file reads). Ask user which fixes to pursue, if any.
