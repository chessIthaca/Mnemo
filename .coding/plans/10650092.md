# Plan: Frameless memory cards + normal-size tool &amp; memory card text (backlog 900fe8d8)

## Goal
Extend the frameless tool-card treatment to the memory family and the vision card, and render tool + memory card body text at the same size as normal assistant text (backlog 900fe8d8): MemoryEntryCard and VisionEntryCard drop their border/bg/rounded frames and text-xs downscales and adopt the ToolCard's frameless template (py-[0.125em] outer, em-based spacing per SPEC 4c2f16ba, state via text color, all content and affordances preserved — expand/collapse, hit lists with tier/title/score, copy); the ToolCard header drops its text-[0.875em] downscale so card body text inherits 1em of the chat content (--app-font-size), with labels/status keeping a distinct smaller em-based treatment; zero rem-based utilities remain in the card components; source-contract tests pin the contract; builds green.

## Kind
implementation

## Context
Full audit (traced this session): the frameless template is ToolCard (Message.tsx:659-866, plan 3de07a3b/commit 51bbe72): outer `py-[0.125em]`, no border/bg/rounded, state via text color, em-based spacing (gap-[0.25em], mt-[0.25em], chevron h-[1em] w-[1em]). Still framed: MemoryEntryCard (Message.tsx:117-212) — outer `rounded-lg border border-violet-600/30 bg-violet-950/20 px-3 py-1.5 text-xs`, chevron h-3 w-3, tier badge `px-1.5 py-0.5 text-[0.65rem]`, expanded `mt-1.5 space-y-0.5 border-t border-violet-600/20 pt-1.5`, gaps gap-2; and VisionEntryCard (Message.tsx:225-301) — outer `rounded-lg border border-border bg-bg-tertiary/50 px-3 py-1.5 text-xs`, same rem pattern, expanded has border-t. ToolCard residuals: header line `text-[0.875em]` (a text-sm-equivalent downscale), searchNotes + error summary at `text-[0.75em]` (content), images row `gap-2` (rem). Normal text size = 1em inherited: the Conversation container sets fontSize var(--app-font-size) (Conversation.tsx:94) and globals.css:234 forces `.prose { font-size: inherit; }` so the finalized assistant markdown renders at --app-font-size (default 14px, globals.css:8) — the cards must match by inheriting (no text-xs/text-sm/text-[0.875em] on card containers). Label treatment may stay smaller: ToolCard status/running at text-[0.75em], CallDetail labels at text-[0.75em], CallDetail pre blocks at text-[0.75em] (code treatment — mirrors the assistant prose's own CodeBlock text-xs), tier badges at text-[0.65em] (converted rem→em). Out of scope (documented): the user bubble, the error entry, the question/approval prompts (message-level or interactive UI, not activity cards); the streaming assistant text's text-sm (assistant text itself, pre-existing); InlineMarkdown/CodeBlock shared styling (part of "normal assistant text"). Verification approach (documented decision): the repo's established source-contract pattern (toolCardPaths.test.ts:697-733 pins Message.tsx source — "no DOM renderer"; the CSS tests pin globals.css the same way) — new assertions pin frameless outers, 1em inheritance (no downscale classes on card containers), and zero rem-based utilities in the three card fns; em/inherited classes scale with --app-font-size by construction, which IS the two-font-size guarantee (rem-based would not scale — that's what the no-rem audit pins). Content shapes (memory_search browse results, memory_write confirmations) are already covered by agentEventReducer.memory.test.ts + the existing source-contract block. Commands: npm run test (vitest run), npm run build (tsc && vite build), cargo test (constitution; no Rust changes expected).

## Steps
- [x] 1. **Convert MemoryEntryCard to the frameless template** — frontend/src/components/chat/Message.tsx:117-212: outer `rounded-lg border border-violet-600/30 bg-violet-950/20 px-3 py-1.5 text-xs` → `py-[0.125em]` (frameless, matching ToolCard; the violet identity survives via the 🧠 icon, the violet name, and the violet tier badges); chevron `h-3 w-3` → `h-[1em] w-[1em]`; line `gap-2` → `gap-[0.25em]`; tier badge `px-1.5 py-0.5 text-[0.65rem]` → `px-[0.375em] py-[0.125em] text-[0.65em]` (label treatment kept, rem→em); the trailing status span (matched count + ✓/✗) `gap-2` → `gap-[0.25em]` + `text-[0.75em]` (the ToolCard's label treatment); the expanded hit list `mt-1.5 space-y-0.5 border-t border-violet-600/20 pt-1.5` → `mt-[0.25em] space-y-[0.15em]` (drop the border-t divider — frameless) with hit-row `gap-2` → `gap-[0.25em]`; the body line (name, queryLabel, tier, title, snippet) and the hit rows inherit 1em (no text-size class). ALL content and affordances stay: the expand/collapse chevron + role=button span, the query chip, the matched count, the per-hit tier/title/score rows.
- [x] 2. **Convert VisionEntryCard to the frameless template** — Message.tsx:225-301: outer `rounded-lg border border-border bg-bg-tertiary/50 px-3 py-1.5 text-xs` → `py-[0.125em]`; chevron `h-3 w-3` → `h-[1em] w-[1em]`; `gap-2` → `gap-[0.25em]`; the status ✓/✗ gets `text-[0.75em]`; the expanded section `mt-1.5 space-y-1.5 border-t border-border pt-1.5` → `mt-[0.25em] space-y-[0.4em]` (drop the border-t); the 'vision query'/'vision response' labels keep `text-[0.75em]` (label treatment); the query/response content divs inherit 1em. Neutral slate identity preserved via text colors.
- [x] 3. **Normalize the ToolCard body to 1em + finish the rem audit** — Message.tsx:659-866: the header span drops `text-[0.875em]` (inherits 1em — the name, chips, and labels render at chat content size); the searchNotes lines and the one-line error summary drop `text-[0.75em]` → inherit 1em (content the user reads); the running/status indicators KEEP `text-[0.75em]` (label treatment); the inline-images row `gap-2` → `gap-[0.5em]`. Then audit all three card fns + CallDetail for any remaining rem-based utility (px-*, py-*, gap-N, h-3/w-3, mt/pt/space-y-N, text-[N rem]) — CallDetail's labels and pre blocks keep their em-based text-[0.75em] (label/code treatment mirroring the assistant prose's CodeBlock), only rem-based values convert.
- [x] 4. **Source-contract tests + builds** — extend frontend/src/lib/toolCardPaths.test.ts (the established readSrc pattern, next to the existing 'expandable memory_search card' block) with a 'frameless + normal-size cards (backlog 900fe8d8)' describe: (a) MemoryEntryCard + VisionEntryCard outers use py-[0.125em] and contain no rounded-lg/border/bg- tint frame; (b) no text-xs or text-[0.875em] anywhere in the three card fns (body inherits 1em — the two---app-font-size guarantee: em/inherited classes scale, rem-based would not); (c) no rem-based spacing utilities (gap-2, px-3, py-1.5, h-3, w-3, mt-1.5, pt-1.5, space-y-0.5/1.5, px-1.5, py-0.5, text-[0.65rem]) in the card fns; (d) affordances intact (ChevronDown/ChevronRight, role="button", h.tier/h.title/score). Run: npm run test (vitest), npm run build (tsc + vite), and cargo test (constitution; no Rust changes expected — confirms nothing regressed).
- [x] 5. **Doc sync + memory records** — check README.md / PLAN.md for tool-card/memory-card styling claims and sync them to the frameless + normal-size contract; memory_update DECISION 344bc076 (frameless by design) to record that the treatment now covers the memory and vision activity cards too; memory_write a SPEC record for the font rule (card body text inherits 1em of the chat content / --app-font-size; labels/status may keep a smaller em-based treatment; all card utilities em-based per SPEC 4c2f16ba). Then the closing sequence: reviewer per constitution, fix findings, commit, finish.
