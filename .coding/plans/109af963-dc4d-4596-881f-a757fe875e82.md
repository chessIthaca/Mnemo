# Plan: Snapshot fallback, UI gray fixes, forgiving base_url + per-model endpoint config

## Goal
(1) Fix game_snapshot/game_screenshot so they fall back to the app's own UI when the Browser tab isn't open (enables seeing + verifying the UI bugs); (2) fix the two visual bugs: the too-bright "white little square" and the too-dark horizontal resize bar; (3) make base_url trailing-slash forgiving (auto-normalize instead of rejecting); (4) support per-model configuration inside an endpoint — each model its own max_context, max_output_tokens, and reasoning-effort list — end to end (endpoints.toml, wire DTOs, provider caps, effort resolution, Settings UI, StatusBar dropdown, docs, tests).

## Kind
implementation

## Context


## Steps
- [x] 1. **Game snapshot falls back to the app's own UI** — In src/browser/mod.rs: split webview_page into webview_page(require_child: bool) — keep the existing child-only path for the mutation tools (webview_eval/webview_navigate/webview_click/webview_type) and add a fallback for the READ-ONLY pair: webview_screenshot/webview_snapshot try the child target first, then fall back to the app's own page (the main WebView2 target: tauri://localhost / http://localhost:5179) when the Browser tab isn't open, so the agent can see the app's UI. Update the error text ('no page target' -> mention the fallback), the doc comments in src/browser/mod.rs and the tool schema/description strings in src/tool/browser/mod.rs (game_screenshot ~line 729, game_snapshot ~line 841), and adapt the select_child_target / webview_page tests. Run `cargo test` and confirm green.
- [x] 2. **Fix the white square + horizontal resize bar (verified via live screenshot)**
- [x] 3. **Forgiving base_url: normalize instead of reject**
- [x] 4. **Backend: per-model caps + per-model effort lists (endpoints.toml)**
- [x] 5. **Frontend: per-model caps + effort fields in Settings/StatusBar**
- [x] 6. **Update README.md + PLAN.md for per-model endpoint config**
- [x] 7. **Final test pass + commit**
