# Plan: search/search_read: pin the literal escape-hatch advertisement + pattern-param recommendation

## Goal
Complete and pin the literal escape-hatch advertisement: add the literal:true recommendation to the `pattern` param descriptions of search and search_read (the item's point 1 — the only un-landed description piece), and add description-pinning tests for both tools (the acceptance criterion, mirroring the read_files collapse test from plan 9e0b266a) so the ESCAPE-HATCH text, recovery rule, example, and RECOMMENDED wording can never silently disappear. No behavior change to the search implementations.

## Kind
implementation

## Context
INVESTIGATION (code-verified this turn): most of this item ALREADY LANDED via plan 481be538 (commit 4be19df, backlog 38040f12 — the escaped-regex incident this item complements): both src/tool/agent/search.rs (description lines 1206-1209) and src/tool/agent/search_read.rs (lines 116-118) carry the ESCAPE-HATCH note ("for text with regex metacharacters or backslashes prefer literal:true (it avoids escaping)"), search's carries the inline example ("e.g. a search for the literal (?< is pattern (?< with literal true") and the recovery rule ("if a call is rejected as malformed, do NOT resend it — reformulate"), and both `literal` param descriptions carry "RECOMMENDED for text with regex metacharacters or backslashes — avoids escaping." search_read exposes the `literal` flag (verified — the item's point 4 ask).

REMAINING GAPS vs the item's fix direction:
1. Point 1 — the `pattern` param descriptions are BARE: search.rs:1220 "The pattern to search for." and search_read.rs:128 "The pattern to search for (regex by default, or literal)." Neither mentions the literal recommendation; the item asks for it in the `pattern` description specifically (the param a model reads when formulating the pattern). During 481be538's budget trim the pattern-param addition was dropped as redundant with the main description + literal param — this item's literal ask restores it.
2. Acceptance criterion — "a schema/description test asserts the text is present (mirror the read_files collapse test from plan 9e0b266a)": NO description-pinning test exists for search/search_read. The read_files precedent (src/tool/agent/read_files.rs:515-545) is the shape to mirror: construct the tool, schema(), then schema.description.contains(...) assertions per load-bearing phrase + param-property checks.

BUDGET MATH: adding ~75 chars to each tool's `pattern` param description = ~150 total on every filter (both tools ride all six). Planning/Complete: 18,727 + 150 = ~18,877 < 18,900 (23 headroom — tight but under; no raise expected). Executing 33,507/33,900, PlanFrozen 34,793/35,200, ExecutingResearch 27,632/28,000, Reviewing 28,771/29,200 — all have 490+ headroom. Measure after the edit; raise only if actually over.

DESIGN DECISIONS:
(a) The pattern-param wording, compact: "Prefer literal:true for text with regex metacharacters or backslashes." appended to both tools' `pattern` descriptions — the item's own suggested phrasing, trimmed to fit the tight Planning/Complete headroom.
(b) NO example added to search_read's note (the item asks for ONE inline example; search's description carries it — search_read's note references the same remedy; adding a second copy would burn ~70 chars of the 23-char headroom and force a ceiling raise for no new information).
(c) The pinning tests assert the phrases that must never silently disappear: the ESCAPE-HATCH marker, "prefer literal:true", the recovery rule ("do NOT resend"), the example (search only), the `literal` param's RECOMMENDED wording, and the new `pattern` param recommendation — mirroring the read_files collapse test's shape (schema.description.contains + param-property assertions).
(d) NO behavior change to the search implementation itself (the item's explicit constraint) — description + tests only.

CLASSIFICATION: kind=implementation — the remedy text already exists (landed via 481be538); the remaining work is a param-description tweak + pinning tests, not a contained defect fix (nothing fails today). The live 5-failure incident (plan 263a9e31) is the motivation, already addressed by the landed text; this item completes and pins it.

VERIFICATION: cargo test --workspace UNPIPED then "exit=$LASTEXITCODE" (agent.md: a piped exit code is the pipe's, not cargo's) — all green, warning-free under #![deny(warnings)]; the budget test (tools_array_stays_within_context_budget) passes with all six filters under ceiling.

## Steps
- [x] 1. **Add the pattern-param literal recommendation** — In src/tool/agent/search.rs (line 1220) and src/tool/agent/search_read.rs (line 128): append the literal recommendation to the `pattern` param descriptions — search: "The pattern to search for. Prefer literal:true for text with regex metacharacters or backslashes."; search_read: "The pattern to search for (regex by default, or literal). Prefer literal:true for text with regex metacharacters or backslashes." (~75 chars each; the item's point 1 — the param a model reads when formulating the pattern).
- [x] 2. **Add the description-pinning tests** — Add description-pinning tests mirroring the read_files collapse test (src/tool/agent/read_files.rs:515-545): in src/tool/agent/search.rs's tests module, a test asserting schema().description contains "ESCAPE-HATCH", "prefer literal:true", "do NOT resend" (the recovery rule), and "pattern (?< with literal true" (the inline example), plus schema().parameters["properties"]["literal"].description contains "RECOMMENDED" and ["pattern"].description contains "Prefer literal:true". In src/tool/agent/search_read.rs's tests module, the same minus the example (its note carries the remedy + recovery rule; the example lives in search's description). These pin the item's acceptance: the advertised text can never silently disappear.
- [x] 3. **Budget check** — Run `cargo test --workspace tools_array_stays_within_context_budget -- --nocapture` to measure: expected Planning/Complete ~18,877 < 18,900 (23 headroom, no raise), the other four filters unchanged under their ceilings. If actually over, raise the Planning/Complete ceilings with a dated cause comment per factory.rs's measured+headroom pattern (backlog f4d5e053, the measured figure).
- [x] 4. **Verify** — Run `cargo test --workspace` UNPIPED then `"exit=$LASTEXITCODE"` (agent.md: a piped exit code is the pipe's, not cargo's) — all green, warning-free under #![deny(warnings)]. Confirm: the two new description tests pass; the existing search/search_read tests still pass; the budget test passes with all six filters under ceiling; no behavior change (description + tests only).
