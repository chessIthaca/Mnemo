# Plan: Log-on-Err for memory-subsystem swallowed durable writes (93eee4a3, LOW 6 class-closure)

## Goal
Close the LOW 6 swallow class in the memory subsystem: apply the log-on-Err pattern (fire-and-forget semantics unchanged) to the seven durable-state-relevant swallow sites, document the two genuinely best-effort keeps, and pin both with regression tests.

## Kind
bug_fixing

## Context
LOW 6 class-closure follow-up (backlog 93eee4a3). The established pattern from commit 4e1bae9: `if let Err(e) = <call>.await { eprintln!("mnemo: ...") }` with fire-and-forget semantics unchanged (no `?`, no propagation change) and a brief LOW 6-citing comment. Site assessments (verified by reading each site): (1) agent.rs:573 record_tool_event — same store method as the fixed turn.rs:1242 site; a lost user-correction is a lost lesson for consolidation → log. (2) consolidation.rs:182/183 extraction calls — LLM/store failures are invisible (consolidate_session returns Ok(id)) → log each. (3) consolidation.rs:192 delete_working_for_session — failure leaves unbounded working-tier growth (the comment's own stated concern) → log. (4) consolidation.rs:419/529 store.write — LLM-extracted semantic/procedural knowledge permanently lost → log, loop continues. (5) indexer.rs:1092 delete_memory — failure leaves a zombie duplicate row (permanent if the marker latches) → log; keep report.migrated += 1 (the file write succeeded — the file is the truth). KEEP as documented best-effort: indexer.rs:1097-1098 marker (self-healing via row-level idempotency; adjacent comment already documents it — no change) and memory/mod.rs:1269 batch_access (advisory recency bump, self-correcting — extend the comment with the swallow rationale). Verified complete: repo sweep shows all other `let _ =` in src/memory + src/runtime are fanin_tx UI-event sends, handle.await spawn-detaches, test code, or embedder.rs:348's deliberate unused-marker; all other record_tool_event/record_request_stats call sites handle their Results. Test approach: source-contract test (include_str! needles — the repo's run_all.rs precedent) + FailingLlm behavioral test (MockLlm machinery exists in consolidation.rs tests; synthesize_with_llm already falls back to synthetic on Err at consolidation.rs:147-149).

## Steps
- [x] 1. **Reproduce with failing regression test** — Write a regression test that reproduces the defect (constitution: every defect gets a regression test that fails without the fix and passes with it). Run it and confirm it FAILS.
- [x] 2. **Document root cause** — Investigate and document the root cause. memory_write a BUG: record (symptom → root cause → fix + regression test name, ≤600 chars).
- [x] 3. **Minimal fix** — Apply the minimal fix that makes the regression test pass. Do not refactor unrelated code.
- [x] 4. **Verify** — Run the regression test + the full test suite (cargo test unpiped, warning-free). Record the regression test name via update_plan (regression_test field) — finish is blocked without it.

## Bug
Durable-write failures in memory-subsystem background/maintenance paths vanish silently — `let _ =` swallows the Result on store.record_tool_event (src/runtime/agent.rs:573, user-correction capture), extract_semantic_facts/extract_procedural_workflows/delete_working_for_session/store.write×2 (src/memory/consolidation.rs:182/183/192/419/529), and store.delete_memory (src/memory/indexer.rs:1092, migrated-row cleanup) — the same defect class as the fixed turn.rs sites (quality review LOW 6, plan 8b5f37fb, commit 4e1bae9; round-2 verification observation).

## Regression test
durable_write_failures_log_instead_of_vanishing
