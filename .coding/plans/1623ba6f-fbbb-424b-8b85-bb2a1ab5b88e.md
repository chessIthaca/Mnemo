# Plan: Adopt thinking-bar resize style for tools/agent divider

## Goal
Restyle the tools/agent ResizeHandle in App.tsx to match the thinking-bar resize handle's style: transparent bar + centered grip pill + subtle translucent cyan hover.

## Context
Two resize handles exist in the frontend:
1. InflightBar.tsx:107-113 (the "thinking bar" handle the user likes): transparent `h-1.5` bar, `cursor-ns-resize`, `hover:bg-cyan-500/20` (subtle translucent cyan), centered grip pill `h-0.5 w-8 rounded-full bg-border`.
2. App.tsx ResizeHandle:448-460 (the tools/agent divider): `w-1` bar, solid `bg-border`, solid `hover:bg-cyan-500`, no grip pill, plus a wider invisible hit area (`absolute inset-y-0 -left-1 -right-1`).

Goal: make #2 adopt #1's visual style — transparent bar, centered vertical grip pill (w-0.5 h-8), subtle translucent cyan hover — while keeping the existing pointer-capture drag logic and the wider hit area. The pill is vertical because this is a column (left/right) divider, vs the thinking bar's horizontal (up/down) pill.

## Steps
- [x] 1. In App.tsx ResizeHandle's returned div, change the className from solid `bg-border ... hover:bg-cyan-500` to a flex-centered transparent bar with `hover:bg-cyan-500/20`, and add a vertical grip pill (`w-0.5 h-8 rounded-full bg-border`) alongside the existing invisible hit-area div. Keep pointer-capture logic and role=separator intact.
- [x] 2. Run `cargo test` (no Rust changed, but constitution requires it before step completion) and `npx tsc --noEmit` in frontend to confirm no type errors.
- [x] 3. Spawn a read-only reviewer subagent to review all uncommitted changes; write findings to .coding/reviews/. Fix any findings, re-run checks.
- [x] 4. Commit to the current feature branch with a clear message.
