# Plan: Backlog 41f39672 close-out: verify expandable memory-search hits + fix the dir-scoped brace-glob regression

## Goal
Close backlog 41f39672 (expandable memory-search matched-hits — verify the shipped implementation end-to-end, run its tests, mark done) and fix the live F7 regression found during verification (dir-scoped recursive brace globs silently matching 0 files).

## Kind
implementation

## Context
Backlog 41f39672 dispatch (run-all, unattended). VERIFIED ALREADY SHIPPED (the F5 stale-grounding lesson applied — evidence first): parseMemoryEntry (frontend/src/hooks/agentEventReducer.ts:350-437) parses the full per-hit list for memory_search results via a global `[tier] title (… score: N)` regex (comment cites backlog 41f39672) + the matched count; MemoryEntryCard (frontend/src/components/chat/Message.tsx:115-207) gates expansion on hits.length, renders tier/title/score rows, click+Enter/Space toggle; dedicated test file frontend/src/hooks/agentEventReducer.memory.test.ts (219 lines, imports parseMemoryEntry); recalled SPEC 2026-08-29-steering-round-3 confirms this shipped 2026-09-15 with the note "backlog 41f39672 — close after rebuild". Item is a close-out. DEFECT FOUND DURING VERIFICATION (live, reproducible): search with glob `frontend/src/**/*.{ts,tsx}` walked 0 files while `frontend/src/**/*.tsx` walked 61 in the same tree, same session — a dir-scoped RECURSIVE brace shape broken in the fresh F7 work (src/tool/agent/search.rs expand_braces/compile_globs + walk_searchable; the F7 tests covered root-level braces and single-star dir-scoped braces, NOT dir+`**`+brace). Note walk_searchable errors are silently unwrap_or'd to an empty walk by both callers — a compile failure would present exactly as "searched 0 files". DECISIONS (documented for review): (1) fold the glob fix into this round rather than leave a known-broken shape shipped; (2) close the backlog item with a shipped-earlier note; (3) reviewer on deepseek-v4-pro (z.ai dropped a reviewer earlier this session). Frontend test command unknown — read frontend/package.json scripts in step 1. Branch: wt/agenticcoder (reused; run-all checkpoint c5afee3 already on it).

## Steps
- [x] 1. **Verify expandable cards end-to-end** — Read the wiring the SPEC cites but this session hasn't seen yet: reduceToolResult's memory merge in frontend/src/hooks/agentEventReducer.ts (how parsed hits attach to the transcript entry — search "hits" in the file) and the auto-recall path (memory_recalled carrying its hit list). Read frontend/package.json to find the test command, then run the frontend memory tests (filter to agentEventReducer.memory) and confirm green. If ANY gap in the wiring: fix it (that becomes the item's real work); expected outcome per evidence: all present + green.
- [x] 2. **Glob defect: red test → root cause → fix** — In src/tool/agent/search.rs tests: a red unit test — tempdir with frontend/src/a.ts, frontend/src/hooks/b.tsx, frontend/src/deep/c.ts; walk_searchable(dir, Some("frontend/src/**/*.{ts,tsx}")) must return 3 (currently 0 live). Run it to confirm red, then diagnose in order: (a) print/assert expand_braces("frontend/src/**/*.{ts,tsx}") == ["frontend/src/**/*.ts", "frontend/src/**/*.tsx"] (unit-level); (b) glob::Pattern::new on each expansion — if a branch fails to compile, compile_globs errors and walk_searchable's Err is silently unwrap_or'd to empty by BOTH callers (search.rs + search_read.rs) — that silent-swallow is part of the defect: make walk-glob compile failures surface as a tool error instead of a silent empty walk; (c) if the patterns compile, the bug is in matching (probe Pattern::matches_path for the nested path). Fix root cause, test goes green, and add the shape to the zero-file-hint regression coverage if relevant. cargo test before completing.
- [x] 3. **Close-out: backlog, knowledge, commit** — backlog_status 41f39672 → done (note: already shipped in steering round 3 — parseMemoryEntry hits[] + MemoryEntryCard expansion, verified + tests green this session). If step 2's diagnosis contradicts the corrected glob-shapes knowledge file (.coding/knowledge/how/2026-08-29-search-glob-shapes-extension-anchored-only-bare.md), memory_update it with the new verified shape. memory_write a BUG record for the glob defect (symptom → root cause → fix → regression test name). Full cargo test + the frontend memory tests once more; commit everything (backlog.jsonl flip rides along) to wt/agenticcoder with a clear message. Then the closing sequence: reviewer (deepseek-v4-pro) → fix findings → re-run tests → commit → finish.
