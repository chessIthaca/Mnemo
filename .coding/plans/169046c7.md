# Plan: Verify + close out backlog b804012f (search auto-delegation already shipped)

## Goal
Close out backlog b804012f (search/search_read auto-delegation): verify the shipped implementation's test family is green, supersede the stale 2026-09-15 nudge SPEC memory with the auto-delegation spec (the task's one unmet constraint), mark the backlog item done, and commit the bookkeeping. Research kind — no source-code changes; the implementation cycle (plan 7b77cf0c, commit 6cf10d5, round-2 PASS review) already completed under a previous dispatch.

## Kind
research

## Context
Backlog b804012f's implementation is ALREADY SHIPPED: commit 6cf10d5 (plan 7b77cf0c), round-1 review FINDINGS (1 high, 3 low) all fixed, round-2 PASS (.coding/reviews/2026-12-28-auto-delegate-search-review-round2.md), PLAN: digests captured (c1200d18, 6b7e3be0), docs synced (README.md:53, PLAN.md:134-138, module docs search.rs:24-25 / search_read.rs:65-68), and the feature verified LIVE this session (AUTO-DELEGATED blocks with escape line; re-issues fell back to plain search). Verified against the task contract in planning: symbol delegation (symbol_delegation_block, codegraph.rs:106; wired search.rs:1196-1216, search_read.rs:200-211; walk skipped when glob-less or memory-delegated, else prepended), memory delegation (memory_delegation_block, search.rs:1057; conservative detection — knowledge/reviews globs, path-referencing patterns, typed prefixes; plain patterns and src globs never delegate, tests 2578-2617), markers + escape line, escape hatch (DelegatedKey pattern+glob+literal, sticky until a different query delegates, search.rs:353-358/1153-1158/2800-2823 + search_read twins), best-effort fallbacks (empty recall never delegates, 2692-2710), byte-pinned 2026-09-15 nudge retained on non-delegated paths (search.rs:627-630), SEARCH_NUDGE_MARK decision documented (steering_stats.rs:447 — a delegated answer IS the steering acted; C5 gate never punishes), full test matrix in both files. ONE GAP: the 2026-09-15 nudge SPEC memory (1c73bfdc-85be-52be-9f9a-ff7c39ea2dcd, 'SPEC: symbol nudge carries the symbol id — graph_context(id=...) (b7150a3, superseded 2026-08-29 wording)') is still LIVE and stale — the task explicitly required superseding the 2026-08-29/2026-09-15 nudge spec files (2026-08-29 is already superseded history in that chain). Plus the backlog item close-out (still pending/in_flight).

## Steps
- [x] 1. **Verify the delegation test family green**
- [x] 2. **Supersede the 2026-09-15 nudge SPEC with the auto-delegation spec**
- [x] 3. **Close out the backlog item + commit the bookkeeping**
