# Plan: Verify + close: [models.reviewing] is reviewer-spawn-only (landed at 29da51a, tree intact)

## Goal
Verify the reviewer-spawn-only [models.reviewing] contract is fully implemented, tested, and live in the current tree (main agent stays on [models.executing] through Reviewing; reviewer pin intact), reconcile backlog item 86c90ff6 with done-status and pointers, and commit the accumulated sidecar bookkeeping (this item's + the prior item's). No source-code changes expected (research).

## Kind
research

## Context
Backlog item 86c90ff6-e0d5-4995-ac46-077e7e9bfca9 ("The main agent must NOT switch to the [models.reviewing] model... [models.reviewing] applies ONLY to the spawned reviewer subagent", status pending — 'interrupted by the user, returned to queue'). Planning-state verification established the work is ALREADY LANDED AND MERGED: plan 0d124d58 ("[models.reviewing] is reviewer-spawn-only — main agent stays on [models.executing]") merged into main at 29da51a (2026-09-01; merge touched model_resolver.rs +145, spawn_agent.rs +150, loop_impl.rs, general.rs, settings.rs, ModelsSection.tsx, README.md, PLAN.md + knowledge records). Current-tree confirmation (all read this session): (1) src/model_resolver.rs:263-274 — the main-agent workflow-state override chain's Reviewing arm resolves models.executing.as_ref() with the exact decision-2026-12-06 comment ("[models.reviewing] is reviewer-spawn-only... The reviewing slot itself is consumed solely by resolve_reviewer_model"); the task's suggested "Reviewing -> None" was implemented as "Reviewing -> executing slot", behaviorally identical to the user's ask ("it stays on [models.executing]"); (2) resolve_reviewer_model (model_resolver.rs:278-293) — the reviewer-spawn-only chain reviewing -> executing -> subagent, each link validated independently; (3) the spawn_agent reviewer pin (src/tool/agent/spawn_agent.rs:245-262, the task's cited 253-263 site) — role=="reviewer" && forced_model.is_none() -> resolve_reviewer_model(), documented as the reviewing slot's SOLE consumer; (4) dedicated tests at model_resolver.rs:580-615+ (main agent in Reviewing resolves the EXECUTING slot; reviewing-set/executing-unset variants; reviewer chain fallbacks); (5) module docs (model_resolver.rs:17, 60, 100) state the contract. The task's "secondary benefit" (fewer tools for the reviewer spawn) was already covered — the reviewer is read-only by construction (verified extensively in the aa653afc task this session: strict ToolFilter::Reviewer allow-list, set_plan_mutations_allowed(false) for all subagents, dispatch gate). "Executing then goes to complete with the same gates" — descriptive of the unchanged finish gates, no work implied. Item pattern: done-but-unmarked requeue artifact (same as 7ba83b5b and 8b8f40d2). Also pending hygiene: the PREVIOUS task's sidecar closure (.coding/backlog.jsonl done-note for 8b8f40d2 + untracked .coding/plans/8bce7a41.md) is still uncommitted on wt/agenticcoder — sweep it into this task's bookkeeping commit (last session's disclosed read-loop failure left it dangling).

## Steps
- [x] 1. **Run the reviewer-model tests** — Run the resolver + pin tests as fresh confirmation: `cargo test model_resolver` and `cargo test reviewer_pin` (adjust filters to the actual test names found at src/model_resolver.rs:580-615+ and spawn_agent.rs tests); read the test result lines. Confirm no later change regressed the contract (the ffe59699/8be3c30 work and this session's commits all postdate the 29da51a merge — the code read this session already confirms the arms, this run proves the tests still pass).
- [x] 2. **Spot-check docs + knowledge records** — Spot-check doc-sync in the current tree (read-only): README.md [models.reviewing] mention (merge updated it 2026-09-01) and PLAN.md's reviewer-model section still state the reviewer-spawn-only contract; confirm the knowledge record from the merge (models-reviewing-is-reviewer-spawn-only spec) is reflected in memory (SPEC f1119862 already confirmed current). If any doc contradicts the contract, STOP the research framing and report the gap.
- [x] 3. **Close item + commit sidecar bookkeeping** — Reconcile + commit bookkeeping: mark backlog 86c90ff6 done with pointers (merge 29da51a, plan 0d124d58, SPEC f1119862, model_resolver.rs:271 + resolve_reviewer_model:278, spawn_agent.rs:256 pin, tests green). Then ONE git commit on wt/agenticcoder sweeping the sidecar: the new done-note + plan file 8bce7a41's closure + this task's .coding/ artifacts (backlog.jsonl + plans). Emit the commit call DIRECTLY after the reads — no re-reading loops (last task's disclosed failure mode).
