# Plan: Show images from image commands in chat + Chat settings toggle

## Goal
Show the analyzed/captured image inline in the agent chat for image commands (7 image_* tools + browser_screenshot/game_screenshot), gated by a new Chat settings toggle (default ON), with a sandbox-validated read_image_data_url IPC.

## Kind
implementation

## Context
Backlog item 73: "when you use an image command, always show the image in the agent window too. Make that an option in the chat config section too." Research: (1) image_* tools (src/tool/agent/image_tools/tools.rs run_single:69-108) take an `image` path arg and return ToolResult data {confidence,rounds,regions,warnings} — path only in args. (2) browser_screenshot/game_screenshot (src/tool/browser/mod.rs:713-746 + ScreenshotTool nearby) write PNGs to .coding/browser/screenshots/ and return the path in text — add structured data.path instead of parsing text. (3) ToolResult.data reaches the frontend: SerializableAgentEvent::ToolResult (src/runtime/channels.rs:344-346,479-483); reducer consumes data (agentEventReducer.ts:300). (4) Chat config section = frontend/src/components/settings/sections/ChatSection.tsx; the config.toml-backed toggle pattern to copy is show_token_usage: config ui field → getSettings ui payload → App.tsx:244 store.setShowTokenUsage at startup → ChatSection draft/save (saveSettings patch) → ChatSection.test.ts source-contract assertions. Types in frontend/src/components/settings/types.ts (ChatDraft:290, serializeChat). Settings IPC: src-tauri/src/ipc/settings.rs ui section + SettingsPatch; fixture frontend/src/lib/ipc-fixtures/dto-get-settings.json + frontend/src/lib/ipc-contract.test.ts:369. (5) No IPC reads an image as data URL — add read_image_data_url mirroring the browser screenshot base64 pattern (src-tauri/src/ipc/browser.rs:69-80) with sandbox validation + spawn_blocking + extension allowlist. (6) Toggle default ON per "always show ... make that an option". (7) Rendering: Message.tsx ToolCard renders completed calls; add a small ToolImage component that lazily loads via the new IPC (fail-quiet). Windows/PowerShell; cargo test unpiped ($LASTEXITCODE); frontend vitest is node-env (pure-function/source-contract tests only; colocated tests MUST be registered in frontend/vitest.config.ts).

## Steps
- [x] 1. **Branch + config field ui.show_tool_images end-to-end (Rust + settings IPC + fixture)** — create_plan branch param already forks it. Find where show_token_usage lives in src/config/general.rs (Ui config struct) and add `show_tool_images: bool` (serde default true) with doc comment: gates inline display of image-command results in the chat. In src-tauri/src/ipc/settings.rs: include show_tool_images in the getSettings ui payload (next to show_token_usage ~:589) and in the SettingsPatch + write-back + validation (mirror show_token_usage lines). Update frontend/src/lib/ipc-fixtures/dto-get-settings.json ui object (add "show_tool_images": true) and frontend/src/lib/ipc-contract.test.ts (:369 area) to assert typeof dtoGetSettings.ui.show_tool_images === "boolean". Run `cargo test` unpiped, read test-result lines + $LASTEXITCODE.
- [x] 2. **New IPC read_image_data_url (sandbox-validated image → base64 data URL)** — In src-tauri/src/ipc/files.rs (next to get_git_branch hardening pattern): `#[tauri::command] pub async fn read_image_data_url(state: State<'_, IpcState>, path: String) -> Result<String, IpcError>` — validate the path against the project sandbox (reuse the existing read_file validation helper), allowlist extensions png/jpg/jpeg/gif/webp/bmp/ico, cap at 20 MiB, read bytes in tokio::task::spawn_blocking, return `data:<mime>;base64,<STANDARD.encode(bytes)>` (base64::Engine pattern like ipc/browser.rs:69-80). Doc comments on the pub fn. Register `ipc::files::read_image_data_url,` in src-tauri/src/main.rs invoke_handler next to other files commands. Unit tests in files.rs test module using a temp project: write a tiny png → returns data:image/png;base64,...; .rs/.txt path → Err; missing file → Err; path outside sandbox → Err. `cargo test` unpiped.
- [x] 3. **Screenshot tools: expose the PNG path in structured data** — In src/tool/browser/mod.rs: find ScreenshotTool and GameScreenshotTool execute fns (~:713-746) where the PNG is written to .coding/browser/screenshots/ and the path returned in the output text. Add `.with_data(json!({"path": <project-relative path with forward slashes>}))` to the success ToolResult (keep the output text unchanged — data is additive). Update/extend existing tests asserting the success result to also assert result.data["path"] ends with .png. `cargo test` unpiped.
- [x] 4. **Frontend store + Chat settings toggle (showToolImages)** — frontend/src/hooks/useAgentStore.ts: add `showToolImages: boolean` (init true) + `setShowToolImages(show: boolean)` (mirror showTokenUsage field+setter). frontend/src/App.tsx:244 area: `store.setShowToolImages(settings.ui.show_tool_images !== false)` after the showTokenUsage line (tolerate absent field as ON). frontend/src/components/settings/types.ts: ChatDraft += `showToolImages: boolean`; serializeChat covers it. ChatSection.tsx: readDraftFromStore += showToolImages; save: s.setShowToolImages(draft.showToolImages) + saveSettings patch show_tool_images; add a third checkbox row 'Show images from image tools in chat' (same markup pattern as the memory-activity checkbox :124-125, label + description 'Render the analyzed or captured image inline below image tool results'). ChatSection.test.ts: update base ChatDraft (:73) + assertions to cover show_tool_images patch + setShowToolImages call.
- [x] 5. **Frontend: ToolImage component + ToolCard rendering + tests** — New frontend/src/components/chat/ToolImage.tsx: `export function toolImagePath(name: string, argsJson: string, result: {success: boolean; output: string; data?: unknown} | null): string | null` — null unless result?.success; name starts with 'image_' → parse argsJson, return string .image (null on parse failure); name === 'browser_screenshot' || 'game_screenshot' → (result.data as any)?.path string else parse output for /\.coding\/browser\/screenshots\/[\w.-]+\.png/ (null if none); else null. Component `ToolImage({ path }: { path: string })`: useEffect → readImageDataUrl(path) (new wrapper in frontend/src/lib/tauri.ts: `export async function readImageDataUrl(path: string): Promise<string> { return await invoke("read_image_data_url", { path }); }` with doc comment) → useState dataUrl/error/loading; render <img src={dataUrl} alt={path} title={path}> with className 'mt-1 max-h-40 rounded border border-border object-contain' inside a block container; loading → nothing (or tiny muted text); error → nothing (fail-quiet — the tool result text still shows). In Message.tsx ToolCard: after the result block for each completed call, `const imgPath = toolImagePath(name, call.args, call.result);` and when `useAgentStore((s) => s.showToolImages)` && imgPath render <ToolImage path={imgPath} />. New ToolImage.test.ts (node-env, pure toolImagePath tests: image_* good/bad JSON, screenshot data.path, screenshot text fallback, wrong tool, failed result) — REGISTER it in frontend/vitest.config.ts include list. `cd frontend; npx tsc --noEmit; npm test` unpiped.
- [x] 6. **Docs + full verification + review + commit + finish** — Full matrix: root `cargo test` unpiped (green, zero warnings under #![deny(warnings)] — no #[allow]); `cd frontend; npx tsc --noEmit`; `npm test`. git status --short — expect only: src/config/general.rs, src-tauri/src/ipc/settings.rs, src-tauri/src/ipc/files.rs, src-tauri/src/main.rs, src/tool/browser/mod.rs, frontend fixtures + ipc-contract.test.ts, useAgentStore.ts, App.tsx, settings/types.ts, ChatSection.tsx/test, tauri.ts, Message.tsx, ToolImage.tsx/test, vitest.config.ts, docs (+ .coding bookkeeping). README.md: add/adjust a feature bullet (tool cards show images from image commands inline; toggle in Settings → Chat). Spawn reviewer (role:"reviewer") over ALL uncommitted changes with a self-contained task: goal (inline images for image commands + Chat toggle + read_image_data_url IPC + screenshot data.path), changed-file list, focus: sandbox validation on read_image_data_url (no traversal), size cap, base64 mime correctness, data.path additive-only (no output contract change), toolImagePath null-safety, ToolImage fail-quiet + no unbounded loads, toggle default ON + absent-field tolerance, vitest registration, doc comments, no dead code, multi-platform neutrality (pure TS + portable Rust; game_screenshot stays behind its existing Windows gate). END TURN after spawning. Read the report, fix EVERY finding, re-run the matrix. Commit all incl. the review report to feat/tool-image-display (never main). finish with the report path; final summary ≤3 lines + memory_write (semantic: shipped feature + branch + commit; procedural note: read_image_data_url reusable for future image display needs).
