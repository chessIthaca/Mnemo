# Plan: Implement all link-time levers (profiles, Defender, clean, feature gates, test-bin merge, CGU)

## Goal
Implement all six link-time levers from .coding/knowledge/link-time-research.md: profile overrides, Defender exclusions, target clean, feature-gate chromiumoxide + fastembed, merge integration test binaries, CGU keep-or-revert — then re-measure and document.

## Kind
implementation

## Context
Research (plan eec03b8d, .coding/knowledge/link-time-research.md): warm loop healthy post-lld, but full rebuilds pay ~33.8s full lib recompile + 6-7 binary links vs ~480MB PDB each; target\debug = 158GB stale variants; Defender real-time ON with no exclusions. Ranked levers to implement: (1) deps debug=false profile override; (2) Defender exclusions; (3) cargo clean; (4) feature-gate chromiumoxide (src/browser/mod.rs) and fastembed/ort (src/memory/embedder.rs BundledEmbedder, src-tauri/src/ipc/embeddings.rs app-side); (5) merge 4 integration test bins (tests/contract_fixtures.rs, ipc_bridge.rs, provider_integration.rs, workflow_integration.rs) into one tests/integration/main.rs binary; (6) codegen-units keep-or-revert experiment. GOTCHAS: `cargo build`/`cargo test` at workspace root default to the mnemo package ONLY — always verify with `cargo test --workspace` and `cargo build -p mnemo-app`; cargo Compiling lines are noise-filtered (time via Stopwatch, not perceived output); mtime touches don't dirty fingerprints. Browser state-visibility regression tests (plan bb8f0ca9) reference browser tools and must be cfg-gated with the feature. fastembed gating also removes transitive rav1e from light builds (memory: rav1e not cleanly removable otherwise). App (src-tauri) must keep full behavior by opting into both features. Convention: warning-free build (deny(warnings)), doc comments on public fns, preserve line endings.

## Steps
- [x] 1. **Profile overrides: deps lose debuginfo** — Root Cargo.toml: under the existing [profile.dev] and [profile.test] sections (which set debug = "line-tables-only"), add `[profile.dev.package."*"] debug = false` and `[profile.test.package."*"] debug = false`. Workspace code keeps line-tables; dependency crates compile without debuginfo → smaller link input and PDBs for every binary. Verify: `cargo check --workspace` clean.
- [x] 2. **Defender exclusions (UAC)** — Elevated one-shot via `Start-Process powershell -Verb RunAs -ArgumentList '-NoProfile','-Command','Add-MpPreference -ExclusionPath "C:\AgenticCoder\AgenticCoder\target","C:\Users\carst\.cargo","C:\Users\carst\.rustup"; Set-Content C:\AgenticCoder\AgenticCoder\target\defender-exclusions.done ok'` — the user must accept the UAC prompt. Verify the marker file exists afterwards; if declined/failed, record the skip in the knowledge doc and continue (lever is optional).
- [x] 3. **cargo clean + rebuild + baseline re-measure** — `cargo clean` (reclaims ~158GB, wipes stale myharness_app relics). Then time (Stopwatch, PowerShell) a full `cargo build --workspace` and `cargo test --workspace`. Record both numbers under a new "After levers 1+3" heading in .coding/knowledge/link-time-research.md. Expect one multi-minute full rebuild — that is the cost of applying the new profile settings to all dependency artifacts.
- [x] 4. **Merge 4 integration test bins into one** — git mv tests/contract_fixtures.rs, tests/ipc_bridge.rs, tests/provider_integration.rs, tests/workflow_integration.rs → tests/integration/<name>.rs, and create tests/integration/main.rs declaring `mod contract_fixtures; mod ipc_bridge; mod provider_integration; mod workflow_integration;` (make contract_fixtures `pub mod` if the others reference its fixtures — fix any `mod contract_fixtures;` cross-imports to use the shared module). This turns 4 link targets into 1. Preserve line endings. Verify: capture test count before (`cargo test --workspace -- --list`) and after the move — counts must match exactly, suite green.
- [x] 5. **Feature-gate chromiumoxide behind `browser`** — Root Cargo.toml: make chromiumoxide `optional = true`, add `[features] browser = ["dep:chromiumoxide"]` (default stays empty). lib.rs: `#[cfg(feature = "browser")]` on the browser module decl. Find browser-tool registration + every `crate::browser` use in the root crate (search tool) and cfg-gate them; wrap the browser state-visibility regression tests (from plan bb8f0ca9, in src/tool/) in `#[cfg(feature = "browser")]`. src-tauri/Cargo.toml: mnemo dependency gets `features = ["browser"]` so the app is unchanged. Verify BOTH configs: `cargo test --workspace` (light — browser tests cfg'd out, suite green) and `cargo test -p mnemo --features browser` (browser tests pass); `cargo build -p mnemo-app` still builds full-featured.
- [x] 6. **Feature-gate fastembed behind `embeddings`** — Root Cargo.toml: fastembed `optional = true`, `embeddings = ["dep:fastembed"]`. src/memory/embedder.rs: keep the embedder trait/config parsing ungated; gate the fastembed-backed BundledEmbedder impl + model catalog helpers behind `#[cfg(feature = "embeddings")]`; when the feature is off, init returns a clear "built without embeddings feature" error and recall falls back to keyword-only (existing semantic-fallback path already handles absent embedder). Gate embedder tests. src-tauri/Cargo.toml: mnemo features += "embeddings". Bonus: light builds drop transitive rav1e. Verify: `cargo test --workspace` green (no-embeddings fallback covered by a small test asserting the error message), `cargo test -p mnemo --features embeddings` green, `cargo build -p mnemo-app` green.
- [x] 7. **CGU keep-or-revert experiment** — Temporarily add `codegen-units = 64` to [profile.dev] in root Cargo.toml. Measure `cargo test --workspace` and `cargo build -p mnemo-app` vs the step-6 numbers (Stopwatch). KEEP only if measurably faster (≥1s on either), otherwise remove the line. Record the decision + numbers in the knowledge doc.
- [x] 8. **Final re-measure, docs, memory** — Re-run the measurement table from the research doc (warm no-op build; lib edit→build; app edit→`cargo build -p mnemo-app`; `cargo test --workspace` after lib edit — real reverted edits, mtime touches do NOT dirty fingerprints). Update .coding/knowledge/link-time-research.md with the final after-table. Docs: README.md gains an optional-features note (browser/embeddings; app builds include both by default), PLAN.md gets a short technical-decision entry (feature-gated heavyweight deps for dev-link speed). memory_write the outcome (tier semantic, HOW:/DECISION:) pointing at the doc. Ensure `cargo test --workspace` is green and warning-free before finishing.
