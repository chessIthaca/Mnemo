# Plan: Review git workflow: inventory lingering branches & stashes

## Goal
Inventory all local git branches and stashes, classify each branch as merged-into-main (safe to delete) or unmerged (keep), and report any stashes. Produce a clear status report so the user can decide on cleanup.

## Kind
research

## Context
Branch topology is wt/* → main (develop tier removed per DECISION c8fd9d60). A prior housekeeping plan (41bafaed) deleted merged feature branches once before. User now wants a fresh review of the git workflow state: which local branches linger, which are merged into main (safe to delete), which are unmerged, and whether any stashes exist. Pure read-only investigation — no source changes.

## Steps
- [x] 1. **Inventory local branches + merge status + stashes + worktrees + remotes + current branch + ahead/behind vs origin/main. Capture full output for the report. Do NOT delete anything — this step is read-only inventory only. Commands to run (PowerShell, Windows project root C:\AgenticCoder\AgenticCoder):
- `git branch -vv` (local branches with tracking + ahead/behind)
- `git branch --merged main` (branches fully merged into main — safe to delete)
- `git branch --no-merged main` (branches with unmerged commits — keep)
- `git stash list` (any stashes)
- `git worktree list` (worktrees, since this project uses per-worktree wt/* branches)
- `git remote -v` (remotes)
- `git rev-parse --abbrev-ref HEAD` (current branch)
- `git log --oneline -5 --all --decorate` (recent history across branches, to see where branch tips sit)
Run each unpiped and read $LASTEXITCODE where relevant. Record every result verbatim for the report.

Note: this project root is C:\AgenticCoder\AgenticCoder. Use Windows/PowerShell syntax. Do not pipe through cmdlets and trust the exit code.**
- [x] 2. **Classify and report. For each local branch, state: name, whether merged into main (git branch --merged main), tracking info + ahead/behind (from git branch -vv), and a recommendation: merged non-main branches are candidates for `git branch -d` cleanup; unmerged branches should be kept (note their last commit subject from the log). List any stashes with their index + message. List worktrees. Summarize the overall git hygiene state and propose concrete next steps (e.g. delete specific merged branches, drop stashes) as options for the user — but take NO action, just report. Write a concise summary to memory (semantic tier) capturing the inventory snapshot + recommendations so future sessions recall it.**
