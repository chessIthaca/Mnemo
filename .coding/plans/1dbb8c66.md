# Plan: Finish gate: force a real code-graph re-index before "regression test symbol not found" errors

## Goal
The finish gate's regression-test symbol check never errors on a symbol that exists on disk: when the incremental (mtime/hash-based) refresh misses, the gate escalates to a GUARANTEED re-index — a forced re-parse of every source file whose on-disk content contains the test name, regardless of stored mtime/hash — and only errors when the symbol is absent after that forced re-index, with a message that names which case it is (absent everywhere = wrong test name; present as text but no symbol; refresh failed = inconclusive, do not rename the test).

## Kind
bug_fixing

## Context
Backlog 648e0ad5. Related prior work: the F10 pattern (graph_search self-healing, plan 55163f1e / backlog 95f21af0 — landed: stale_source_files + reindex_stale_files at src/codegraph/mod.rs:397/:500) is mtime-based and shares the same blind spot (fresh meta + missing symbol rows is NOT mtime-stale). Key API facts: reindex_stale_files(rel_paths) re-parses the given files UNCONDITIONALLY (hash → containment guard → reindex_one → rebuild_edges) with an indexing-flag compare_exchange bail; reindex_one(path, rel, hash, bytes) is the per-file worker (:520); store() is PRIVATE (mod.rs:140) so test modules outside codegraph cannot corrupt the DB directly — a #[cfg(test)] pub(crate) simulator on CodeGraph is the sanctioned route; upsert_file(rel, hash, mtime, &FileData, source) is pub on Store (store.rs:196) and FileData { symbols, refs, contains } is pub (store.rs:85). The gate tests live in src/tool/workflow/plan.rs's test module (finish_blocks_bug_plan_when_test_symbol_missing_from_graph :3606, finish_refreshes_stale_graph_and_finds_new_regression_test :3634 — the #91 incremental-refresh case, finish_passes_bug_plan_with_test_in_graph_and_captures :3669); the pattern: write tests.rs with "fn <name>() {}" into the graph's tempdir root, CodeGraph::open_in_memory, FinishTool::new(wf, reviews).with_codegraph(graph). The existing missing-symbol test asserts contains("not found in the code graph") — the new 0-files message must keep that substring. The codegraph test module (mod.rs:578+) has the fixture helper + use super::*. deny(warnings) at both crate roots — zero warnings required. src-tauri is a binary crate: cargo test WITHOUT --lib, one filter argument only; piped exit codes can lie — trust test result: lines.

## Steps
- [x] 1. **Reproduce with failing regression test** — Write a regression test that reproduces the defect (constitution: every defect gets a regression test that fails without the fix and passes with it). Run it and confirm it FAILS.
- [x] 2. **Document root cause** — Investigate and document the root cause. memory_write a BUG: record (symptom → root cause → fix + regression test name, ≤600 chars).
- [x] 3. **Minimal fix** — Apply the minimal fix that makes the regression test pass. Do not refactor unrelated code.
- [x] 4. **Verify** — Run the regression test + the full test suite (cargo test unpiped, warning-free). Record the regression test name via update_plan (regression_test field) — finish is blocked without it.

## Bug
A bug_fixing plan's finish gate rejected the verify step with "regression test symbol 'checkFillContract' not found in the code graph (even after a refresh) — the verify step must record the actual test function name" even though the test function exists in the source (live case 2027-01-08) — the dispatched agent then "fixes" a non-problem (renames the test, records a wrong name) or stalls. Root: the gate's one incremental refresh (src/tool/workflow/plan.rs:1425, spawn_blocking(|| g.index(None))) is mtime/hash-based — src/codegraph/mod.rs:287-294 (the mtime fast-path skips the read entirely when stored mtime == disk mtime && has_content && stored_hash.is_some()) and :312 (needs_reindex only when the hash differs or there is no content row) — so when the DB's meta rows are fresh but the SYMBOL rows are stale/missing (partial write, schema drift, a prior best-effort skip at :341-343), the pass never re-parses that file, the retry misses, and the gate errors on a symbol that exists on disk.

## Regression test
finish_force_reindexes_when_meta_fresh_but_symbols_missing
