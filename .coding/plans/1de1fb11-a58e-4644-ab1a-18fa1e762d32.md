# Plan: Rename browser tool namespaces (headless → offscreen_browser_*, live → browser_*)

## Goal
Rename the agent-facing tool namespaces: headless Chromium tools browser_* → offscreen_browser_*, and the live WebView2 (Browser tab) tools game_* → browser_*, so the interactive browser gets the primary namespace. Update registration, tests, docs.

## Kind
implementation

## Context
Backlog item: "rename browser* to offscreen_browser_* and game_* to browser_*". Mapped blast radius: 16 tools in src/tool/browser/mod.rs — headless browser_navigate/list_pages/close_page/screenshot/console/snapshot/eval/click/type/switch_page (names at lines ~60,108,156,194,255,302,509,560,605,661) → offscreen_browser_*; live-WebView2 game_screenshot/eval/snapshot/navigate/click/type (lines ~721,783,833,873,923,972) → browser_*. Each tool has fn name(), ToolSchema::new(name, desc, schema), error strings mentioning the name, and doc comments. Structs: headless tools are NavigateTool/ListPagesTool/… (bare names), live tools are GameScreenshotTool/GameEvalTool/GameSnapshotTool/GameNavigateTool/GameClickTool/GameTypeTool — rename to Offscreen*Tool and Browser*Tool for coherence (compile-checked). factory.rs register_browser_tools (:754, registers structs; game block at ~782 under cfg(windows)) + parity test expected lists (:1350-1361) + comments (:772-773). .coding/browser-debugging.md has tool-name tables + narrative (:45-119). src/browser/mod.rs:1898 doc comment names game_click/game_type. NO references in skills (.coding/skills/*.toml clean), approval rules (category-based), or frontend agent-tool names (tauri.ts browser_* are IPC commands — NOT agent tools, intentionally left unchanged). Historical .coding/ plans/reviews keep old names (no history rewriting).

## Steps
- [x] 1. **Rename all 16 tool names + structs in src/tool/browser/mod.rs** — In src/tool/browser/mod.rs: for each of the 10 headless tools rename the tool name browser_X → offscreen_browser_X (fn name() return, ToolSchema::new first arg, error strings like "browser_navigate failed: {e}" → "offscreen_browser_navigate failed: {e}", doc comments); for each of the 6 live tools rename game_X → browser_X (same sites). Rename structs for coherence: headless NavigateTool/ListPagesTool/ClosePageTool/ScreenshotTool/ConsoleTool/SnapshotTool/EvalTool/ClickTool/TypeTool/SwitchPageTool → OffscreenNavigateTool/OffscreenListPagesTool/OffscreenClosePageTool/OffscreenScreenshotTool/OffscreenConsoleTool/OffscreenSnapshotTool/OffscreenEvalTool/OffscreenClickTool/OffscreenTypeTool/OffscreenSwitchPageTool; live GameScreenshotTool/GameEvalTool/GameSnapshotTool/GameNavigateTool/GameClickTool/GameTypeTool → BrowserScreenshotTool/BrowserEvalTool/BrowserSnapshotTool/BrowserNavigateTool/BrowserClickTool/BrowserTypeTool. Update the shared safety note comment (~:706-713) and any description text that self-references old names; keep descriptions' semantics (headless = "headless browser", live = "the live Browser tab's child webview"). Run `cargo test --lib tool::browser` unpiped, read test-result lines + $LASTEXITCODE.
- [x] 2. **Update factory registration + parity test + remaining name references** — In src/agent/factory.rs: register_browser_tools (:754) — update struct names (Offscreen*Tool for headless, Browser*Tool for the cfg(windows) block); update comments at :772-773 to the new names; update the parity test expected lists (:1350-1352 → offscreen_browser_navigate/offscreen_browser_list_pages/offscreen_browser_close_page/offscreen_browser_screenshot/offscreen_browser_console/offscreen_browser_snapshot/offscreen_browser_eval/offscreen_browser_click/offscreen_browser_type/offscreen_browser_switch_page; :1360-1361 cfg(windows) → browser_screenshot/browser_eval/browser_snapshot/browser_navigate/browser_click/browser_type). Then search the whole repo (glob **/*.rs) for any remaining old-name strings (browser_navigate|browser_list_pages|browser_close_page|browser_screenshot|browser_console|browser_snapshot|browser_eval|browser_click|browser_type|browser_switch_page|game_screenshot|game_eval|game_snapshot|game_navigate|game_click|game_type) and update src/browser/mod.rs:1898's doc comment + any other live references — EXCEPT .coding/ history (plans/reviews/analysis are historical records, never rewritten). Run `cargo test` (workspace, unpiped, read test-result + $LASTEXITCODE).
- [x] 3. **Docs + full verification matrix** — .coding/browser-debugging.md: update the tool tables + narrative (:45-68, :86-119) to the new names (offscreen_browser_* for headless, browser_* for live), keeping semantics. README.md: adjust the browser bullet under **Agents & interface** to name the namespaces (headless Chromium via offscreen_browser_* tools; on Windows the live Browser tab via browser_* tools). Full matrix: `cargo test` (workspace, unpiped), `cd frontend; npx tsc --noEmit`, `cd frontend; npm test` — all green, zero warnings. Fix everything found before review.
