# Plan: Fix DeepSeek reasoning_content 400 recurrence (provider-errors id 1042): tail-owner turn still escapes the age-0 guarantee

## Goal
Find why the DeepSeek thinking-mode tail contract still escapes the b23463c guarantees (evidence: provider-errors id 1042, singleton 400 after a Claude thinking-block failure burst in the same session) and close the hole with a fails-without-fix regression test, a minimal source fix, a BUG memory record, and a green warning-free suite — reproducing the exact failing request shape first.

## Kind
bug_fixing

## Context
State: Complete, HEAD ec0d794 (GLM-5.3 stop boundaries) on wt/agenticcoder. The b23463c fix lives in src/provider/openai.rs build_request_json: (1) raw-echo branch (~:1401-1455) injects reasoning_content into the age-0 assistant echo when policy.reasoning_required && reasoning_field == "reasoning_content" && key absent (live-verified DeepSeek tail contract, probes A/D/G2/T/T2/R5: with reasoning_effort present the tail-owner assistant turn — last message or issuer of trailing tool results — must carry the reasoning_content KEY, any value incl ""; historical turns tolerate missing keys; no reasoning_effort → no validation); (2) synthetic path (~:1529-1532) keys ALL synthetic assistant messages unconditionally; (3) retention strip never removes age-0 text. Regression test: raw_echo_injects_reasoning_content_when_required_but_absent. Related context in provider-errors.jsonl: the same session window also shows a DISTINCT older bug shape (ids 147-160, "insufficient tool messages following tool_calls message" — an open tool loop hitting the wire) and Claude "each thinking block must contain thinking" bursts (ids 879-888, 1022-1030) — sibling serialization-bug shapes, NOT this bug, but evidence that conversation-history echo corruption happens across providers in this app. Error log is at .coding/logs/provider-errors.jsonl (always-on, capped 8MiB); traces.jsonl (opt-in, per-record request_json capped 256KiB) may or may not hold the id-1042 request body — extraction must be truncating (never dump a whole multi-hundred-KB row into context). Windows PowerShell; cargo test unpiped; #![deny(warnings)] — zero warnings.

## Steps
- [x] 1. **Reproduce with failing regression test** — Write a regression test that reproduces the defect (constitution: every defect gets a regression test that fails without the fix and passes with it). Run it and confirm it FAILS.
- [ ] 2. **Document root cause** — Investigate and document the root cause. memory_write a BUG: record (symptom → root cause → fix + regression test name, ≤600 chars).
- [ ] 3. **Minimal fix** — Apply the minimal fix that makes the regression test pass. Do not refactor unrelated code.
- [ ] 4. **Verify** — Run the regression test + the full test suite (cargo test unpiped, warning-free). Record the regression test name via update_plan (regression_test field) — finish is blocked without it.

## Bug
DeepSeek thinking-mode 400 "The reasoning_content in the thinking mode must be passed back to the API" RECURRED after the b23463c fix (plan c9b5cbe4, age-0 raw-echo injection + synthetic-path unconditional key). New evidence: .coding/logs/provider-errors.jsonl:482, record id 1042, ts_ms 1788508751217, deepseek-v4-flash @ api.deepseek.com — a SINGLETON failure (no burst — non-retryable classification holds), immediately following in the same session run a Claude-opus-5 burst of "messages.3.content.0.thinking: each thinking block must contain thinking" 400s (ids 1022-1030, ts 1788508444631-1788508460358). Working hypothesis: a conversation carrying foreign (Claude) assistant raw turns — no reasoning_content key in the raw — switched to DeepSeek and the request's tail-owner assistant turn still lacked the key, meaning some guard (age-0 gating, policy consult, raw-echo branch, or a request path that bypasses build_request_json's message loop) still lets the tail escape.
