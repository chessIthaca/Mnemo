# Plan: Bug-fix skeleton: fix step pushes a minimal-change sub-plan

## Goal
Change the bug_fixing plan's locked step 3 from directly applying a minimal fix to pushing a sub-plan whose steps implement the fix with the least amount of change possible (self-contained, written for a lesser model to execute), keeping the skeleton lock and all existing gates intact.

## Kind
implementation

## Context
The locked bug_fixing skeleton (BUG_FIXING_SKELETON, src/tool/workflow/plan.rs:35-47) forces 4 steps on every bug plan: reproduce → root cause → minimal fix → verify. The user wants step 3 ("Minimal fix — apply the minimal fix...") changed to instead PUSH A SUB-PLAN that implements the fix with the least amount of change possible — some bug fixes are too big to implement directly and benefit from a self-contained plan executed by a lesser model. Facts: (1) create_plan called while a plan is active pushes a sub-plan automatically and pops back when it completes — no plumbing needed, just the step text. (2) No test pins step-3 text: plan.rs:1547 asserts steps[0] contains "Reproduce with failing regression test", plan.rs:1552 asserts steps[3] contains "regression_test"; tests/workflow_integration.rs:528 asserts steps[0] too. (3) Docs: PLAN.md:215 describes the skeleton as "document root cause → minimal fix → verify"; README.md:74 and prompt.rs:75 use the arrow shorthand "reproduce→root-cause→fix→verify" which stays valid (the fix step still exists, as a sub-plan). (4) Crate is #![deny(warnings)] — build must stay warning-free; doc comments required on public items; cargo test run unpiped on Windows PowerShell (check "test result:" lines, not piped exit codes). (5) Workflow: implementation kind → reviewer subagent before finish; commit to the wt/* working branch, never main.

## Steps
- [x] 1. **Rewrite skeleton step 3 in plan.rs** — In src/tool/workflow/plan.rs edit the const BUG_FIXING_SKELETON (lines 35-47): replace step 3 ("**Minimal fix** — Apply the minimal fix that makes the regression test pass. Do not refactor unrelated code.") with: "**Minimal fix via sub-plan** — Push a sub-plan (create_plan — it becomes the active plan and pops back to this one when it completes) whose steps implement the fix with the LEAST amount of change possible; do not refactor unrelated code. Write it for a lesser model to execute alone: every step self-contained (explicit file paths, exact edits, the root cause + failing test name as context)." Keep the `\` line-continuation style used by the other array entries. Also update the const's doc comment (lines 30-34): "minimal fix" → "minimal fix via sub-plan".
- [x] 2. **Pin the new step-3 text in tests** — In src/tool/workflow/plan.rs tests module, extend create_plan_bug_fixing_forces_skeleton_and_persists_symptom (line ~1525) with an assertion that plan.steps[2].text contains "sub-plan" and "least amount of change" (mirroring the existing steps[0]/steps[3] assertions at :1546-1555). Do not touch tests/workflow_integration.rs (it only pins step 0).
- [x] 3. **Sync docs** — In PLAN.md (~line 215) update the skeleton description "document root cause → minimal fix → verify" to mention the fix step pushes a sub-plan that implements the fix with the least amount of change (e.g. "document root cause → minimal fix via sub-plan → verify"). README.md and src/agent/prompt.rs:75 use the arrow shorthand reproduce→root-cause→fix→verify, which stays accurate — leave them. Check .coding/knowledge/spec/2026-08-27-run-all-dispatch-flips-complete-planning-bug-fix.md and src-tauri/src/ipc/run_all.rs:109 for the same shorthand (also leave — shorthand still true).
- [x] 4. **Run the test suite** — PowerShell, from C:\AgenticCoder\AgenticCoder: `cargo test` UNPIPED followed by `"exit=$LASTEXITCODE"`; verify the test result lines are all ok and the build is warning-free (deny(warnings)). If only skeleton-related tests fail, fix and re-run. Also re-run the two pinned tests by name if the full run is too slow.
- [x] 5. **Reviewer + close** — Spawn a read-only reviewer (spawn_agent role:"reviewer") over all uncommitted changes: plan goal (skeleton step 3 now pushes a sub-plan implementing the minimal fix for a lesser model), changed files (src/tool/workflow/plan.rs, PLAN.md), focus on correctness of the new step text, doc-sync, multi-platform neutrality, no #[allow]. Fix findings, re-run cargo test, commit to the working branch with the review report, then finish.
