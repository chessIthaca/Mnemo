# Plan: Adopt the Mnemo app icon (regenerate Tauri icon set from SVG)

## Goal
Adopt the user's mnemo-app-icon.svg as the app icon: keep it in-repo as the source of truth and regenerate the full Tauri icon set (PNG/ICO/ICNS + store variants) from it, replacing the default Tauri icons.

## Kind
implementation

## Context
The user provided C:\Users\carst\Downloads\mnemo-app-icon.svg (unreadable from Planning — file tools are project-root-sandboxed; copy first in Executing). Current state: src-tauri/tauri.conf.json:22-27 references the standard Tauri icon set in src-tauri/icons/ (32x32.png, 128x128.png, 128x128@2x.png, icon.icns, icon.ico — the 2026-04-20 review counted 17 binary files there, i.e. also the Square*/StoreLogo Windows-store variants). .gitattributes already guards *.png/*.ico/*.icns as binary. No frontend favicon references exist (no link rel=icon in the webview HTML); the in-app Sidebar/About "logo" is the lucide Code2 icon — out of scope unless the user asks. The Tauri CLI is available (frontend devDependency @tauri-apps/cli ^2, run via npx from frontend/). `npx tauri icon <svg> -o <dir>` generates the full cross-platform set from a square-ish SVG (Windows .ico + macOS .icns generation works on any host OS in CLI v2). Verification that matters: src-tauri `cargo build` (bin crate — tauri-build embeds icon.ico into the Windows resource; a malformed ico fails HERE, not in the root lib build) and frontend `npm run build` for completeness; root cargo test per constitution (code unchanged, expect green). Plan kind: implementation (asset change + possible config touch) — full review/commit closing sequence. The wt/* branch will be forked fresh from main by create_plan (wt/agenticcoder was deleted after the d0438848 merge; the dirty .coding knowledge-file from the merge session carries forward and gets committed on this branch).

## Steps
- [x] 1. **Copy the SVG in-repo as icon-source.svg and validate it** — Copy the SVG into the repo and inspect it: `Copy-Item C:\Users\carst\Downloads\mnemo-app-icon.svg src-tauri\icons\icon-source.svg` (PowerShell). Read src-tauri/icons/icon-source.svg with read_files: confirm it is well-formed XML, has a viewBox (tauri icon rasterizes from it — a missing viewBox breaks scaling), uses no external references (no <image href> to files outside the SVG, no external CSS/fonts — the rasterizer renders the SVG standalone), and note its aspect ratio (tauri icon expects square; a non-square viewBox still works but is padded). If the SVG references external assets, STOP and report to the user instead of generating broken icons.
- [x] 2. **Regenerate the icon set with tauri icon** — From frontend/ run: `npx tauri icon ../src-tauri/icons/icon-source.svg -o ../src-tauri/icons` (the CLI v2 icon command renders every platform variant: 32x32.png, 128x128.png, 128x128@2x.png, icon.ico, icon.icns, plus the Square*Logo.png/StoreLogo.png Windows-store set and ios/android/ subdirs it may create). Then `git status --short` and `Get-ChildItem src-tauri\icons` to enumerate what changed/was added: the 5 conf-referenced files MUST all be modified (non-zero size, larger than the old defaults is a good sanity signal); new untracked variants (Square*/StoreLogo/ios/android) are committed too — they replace whatever variants the previous set shipped (the old set had 17 binaries). If the command errors (unsupported SVG feature, missing CLI), fix the SVG or the invocation and re-run. Do NOT edit tauri.conf.json — the regenerated files keep the exact same names.
- [x] 3. **Verify builds (src-tauri bin crate embeds the new .ico) + closing sequence** — Run the verification matrix: (1) `cd src-tauri; cargo build` UNPIPED reading $LASTEXITCODE — the bin crate embeds icon.ico via tauri-build; a malformed ICO/ICNS fails here (also eyeball that build.rs/tauri-build emits no icon warnings); (2) `cd frontend; npm run build` (tsc + vite — expected unaffected, proves the tree is still coherent); (3) root `cargo test` (code untouched — expect green, zero warnings under deny(warnings)); (4) frontend `npx vitest run` (no test touches icons — expect green). Then the standard closing sequence: spawn the read-only reviewer over the diff (icon binaries + icon-source.svg + plan/knowledge bookkeeping; ask it to verify conf references unchanged, .gitattributes coverage for new file types, and multi-platform neutrality), fix findings, commit everything on the working branch with the review report, finish. The in-app Sidebar Code2 logo and any webview favicon are intentionally unchanged (out of scope — desktop app icon only); note this in the final summary.
