# Plan: Confirm entryId stable-keys already landed; queue run-all done-orphan guard

## Goal
Backlog item 35b94671 (stable entryId keys, mem-perf review HIGH 3) was already implemented, tested, reviewed, and committed by a prior session (plan 225e0dad, commit eac25dc) that died before finishing its plan — the orphaned item was requeued by the run-all drain and re-dispatched to this session as duplicate work; the user confirmed "we did this already". This plan verifies the landed work is green by running the tests, queues a backlog item so run-all stops re-dispatching items whose work already landed, consolidates the duplicate plan-digest memories, and closes the dispatched item out as done. No source-code changes — research kind.

## Kind
research

## Context
Verification evidence gathered during planning: (1) Commit eac25dc "Stable entryId keys for Conversation's turn/run/entry lists — no row-state misattribution at the transcript cap (plan 225e0dad, backlog 35b94671)" is an ancestor of HEAD (visible in git log -- frontend/src/lib/types.ts); git diff HEAD shows only .coding/backlog.jsonl modified (the run-all adopt note) — the fix is committed and merged. (2) frontend/src/lib/types.ts:469-478 defines entryId?: number; agentState.ts:519-559 has nextEntryId/allocEntryId/stampEntryIds (monotonic counter, /load foreign-id sync). (3) Conversation.tsx:256-286 keys turns by turn[0][0].entryId, runs by run[0].entryId, entries by e.entryId (position fallback only for id-less legacy entries). (4) Regression tests present: useAgentStore.test.ts ~2190-2315 (monotonic ids, stable across capTranscript, replacements keep id, stampEntryIds sync), Conversation.test.ts:85-95 (keys are entry ids), agentState.test.ts:167-186, agentState.images.test.ts:154-168. (5) Review exists: 2027-01-07-entryid-stable-keys-review (memory 4dced083) — "core fix is correct and well-tested". Root cause of the duplicate dispatch: the plan-225e0dad session died after committing eac25dc but before finish, so item 35b94671 stayed InFlight (orphaned, dispatch pointer 9fdbc6be); the run-all start drain requeued it to Pending and it was re-dispatched. The user confirmed "we did this already" and asked to (a) confirm, (b) queue an item so run-all stops scheduling finished/done work.

## Steps
- [x] 1. **Run the regression tests** — In frontend/ run `npm test` (vitest run; PowerShell: cd C:\AgenticCoder\AgenticCoder\frontend; npm test; "exit=$LASTEXITCODE") and confirm green — the suite includes plan 225e0dad's entryId regression tests (frontend/src/hooks/useAgentStore.test.ts ~2190-2315, frontend/src/components/chat/Conversation.test.ts:85-95, frontend/src/hooks/agentState.test.ts:167-186, frontend/src/hooks/agentState.images.test.ts:154-168). Then run `cargo test` at the repo root (constitution requirement; no Rust files changed, expect green). If any test fails, investigate before proceeding — a failure would mean the landed work is not green and the confirmation changes.
- [x] 2. **Queue the run-all done-orphan guard item** — backlog_add with the headline+body shape (headline ≤100 chars). Headline: "Run-All re-dispatches items whose work already landed — detect done orphans at the drain". Body: the run-all start drain requeues orphaned InFlight items to Pending without checking whether the dead session already finished the WORK — only plan completion (finish) marks an item done, so a session dying between committing and finishing leaves the item looking unfinished and run-all re-dispatches it as duplicate work. Live case 2027-01-07: item 35b94671 (stable entryId keys, mem-perf HIGH 3), plan 225e0dad, dispatch pointer 9fdbc6be — work implemented, reviewed, committed (eac25dc, merged to main), session died pre-finish, item orphaned InFlight, drain requeued it, run-all re-dispatched it (caught by the user before duplicate edits). Fix direction: at the drain (and/or adopt-on-dispatch), detect landed work — plan file (.coding/plans/<plan_id>) shows all steps complete, or commits referencing the item id exist after the pre-item checkpoint — and auto-resolve the orphan as done instead of requeuing; consider a 'work landed' marker stamped right after the closing-sequence commit (before review/finish) so a death between commit and finish is detectable.
- [x] 3. **Consolidate the duplicate plan-digest memories** — Read the two duplicate PLAN digests (memory ids dac0efad-c1c2-5810-b6c7-d30af17f2e31 and 96be91ba-e780-51ca-9c21-46144fe09858, both titled "PLAN: Index-based React keys misattribute row state at the transcript cap — stable entryId keys (mem-perf HIGH 3)"). Supersede one with a CONFIRMED COMPLETE record (fix landed in eac25dc, merged to main; plan 225e0dad session died pre-finish; the 2027-01-07 re-dispatch was duplicate work caught by the user; run-all guard item queued — include its backlog id from step 2) and memory_delete the other as an exact duplicate. Then memory_write a QUEUED: semantic record for the new backlog item (id + gist), mirroring the existing QUEUED: convention.
- [x] 4. **Close out the plan** — complete_step the finished steps, then finish the research plan (no review — no source-code changes; nothing to commit — the only tree change is .coding/backlog.jsonl, dispatcher-managed state: the adopt note plus the newly queued item, picked up by the dispatcher's closing stamp). Finishing resolves dispatched item 35b94671 as done.
