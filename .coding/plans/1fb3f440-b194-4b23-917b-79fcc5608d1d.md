# Plan: Edit-marker protocol: every edit persisted as a crash-continuation memory

## Goal
Establish the edit-marker protocol in persistent memory so every future session records each edit as a crash-continuation marker: a PROCEDURAL memory (the protocol, exact entry shape + resume recipe), a SEMANTIC decision memory (interpretation + infrastructure facts), and a live WORKING-tier demonstration marker.

## Kind
research

## Context
Backlog: "make sure to put to memory every edit - mostly as a marker as to what you need to continue after a crash." Interpretation (documented): an AGENT-BEHAVIOR protocol persisted via the memory system — not a harness code change, not an agent.md constitution amendment. Grounding facts: (1) the backend already auto-records durable tool calls (file_write/file_edit/file_append, plan/skill lifecycle, memory_write, write_review_report, spawn_agent, git commit/merge/push) as working-tier rows via record_tool_event (src/agent/turn.rs:1264-1279, is_durable_tool:1565) — but only the raw call args/output, no plan context or next-action, and shell-based file edits are NOT covered (is_durable_tool excludes shell); (2) a plan file with checkboxes is already the coarse crash marker (persisted per complete_step + load_latest resumes it) — the gap is edits WITHIN an uncompleted step; (3) working-tier rows surface via the session primer in-session and get consolidated to episodic (recallable by default) once a session ends — recall_excludes_working_by_default means a continuation may need tier:"working" or the consolidated episodic copy; (4) a proto-version of this practice already exists (old "ACTIVE ... crash-resume note" memories) — this formalizes it as a per-EDIT protocol; (5) memory writes are .coding bookkeeping, NOT source code → research plan (no review needed by design). Branch: none needed (no source changes; the .coding churn rides the current branch and run-all's commit_success lands it on Done).

## Steps
- [x] 1. **Write the procedural edit-marker protocol memory** — memory_write tier=procedural, title 'PROTOCOL: EDIT MARKER — write one after EVERY file edit (crash continuation)'. Content must specify: (a) TRIGGER — after every file_edit/file_write/file_append AND any shell command that changes files (the auto-log misses shell); (b) ENTRY SHAPE — tier working, title 'EDIT MARKER — <plan title> step <i>/<n>', body = plan id + step header, files just changed with one-line what/why, EXACT next action (explicit file paths), remaining files, test commands run/pending, branch name; (c) RESUME RECIPE — on continuation after a crash, read the newest EDIT MARKER (session primer, or memory_recall query 'EDIT MARKER' with tier working; older dead-session markers arrive as episodic after consolidation), verify already-done edits with git status/diff instead of redoing, resume from 'next action'; (d) notes — markers are 3-6 lines, newest marker per plan is the live one (older = audit trail); plan-file checkboxes already mark step-level progress so markers are only for WITHIN-step edits; durable file-edit tool calls are additionally auto-recorded by the backend (record_tool_event) as raw-call corroboration.
- [x] 2. **Write the semantic decision memory + live demonstration marker** — (a) memory_write tier=semantic, title 'DECISION (user requirement, 2026-08-20): every edit gets a crash-marker memory'. Content: the backlog item verbatim; the interpretation chosen (behavioral protocol via procedural memory — NOT a harness code change: the auto-log already records durable edit tool calls; NOT an agent.md constitution rule: would tax every agent/subagent turn with token overhead while auto-recall already surfaces procedural memories) + why; the infra facts (record_tool_event coverage incl. the shell gap the protocol closes; recall_excludes_working_by_default → tier:'working' or post-consolidation episodic); pointer to the protocol entry title for lookup. (b) The demonstration marker: memory_write tier=working, title 'EDIT MARKER — edit-marker protocol plan 1/3 done', body following the protocol's own shape (this task edits only memories; next = the semantic decision entry; no branch, no tests pending) — proving the loop end-to-end and leaving a live example row.
- [x] 3. **Hygiene: cargo test, then close (research plan auto-completes)** — Run `cargo test` unpiped with $LASTEXITCODE (constitution: run before marking a step complete; no Rust changes expected — pure hygiene, deny(warnings) gate). Then complete the final step: research-kind plans skip the review sequence and auto-transition to Complete on the last step (no finish gate). The .coding bookkeeping (backlog flip #58, plan file) is committed by run-all's commit_success on Done — no manual commit needed since no source changed; verify with git status --short that only .coding churn is dirty and note it in the final summary.
