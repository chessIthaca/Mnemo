# Plan: Fix v0.1.1 release build failures (windows tauri build + macOS cargo test)

## Goal
Diagnose and fix both build legs so the v0.1.1 release publishes: reproduce the windows `npx tauri build` failure locally, root-cause it, fix; diagnose the macOS `cargo test --workspace` failure (exit 101) via aarch64 crosscompile check + code reading; fix; then land on main and re-cut the v0.1.1 release (delete the unpublished tag, re-tag the fixed merge commit, push).

## Kind
bug_fixing

## Context
Facts gathered (2026-09-20): run 35514715816 (tag v0.1.1 @ c6b5b88). WINDOWS job: all steps green through "Frontend production build" (14:02:16), then "Tauri build (unsigned installers)" = `npx tauri build` failed 14:02:16→14:02:18 (exit 1, ~2s — instant CLI/startup error, NOT a compile failure; annotation "Process completed with exit code 1"). MACOS job: steps 1-6 green, then "Rust tests (workspace, macOS compile validation)" = `cargo test --workspace` failed 13:51:31→13:54:24 (exit 101 — Rust compile error or test panic; ~3 min ≈ compile-then-early-failure). The SAME `cargo test --workspace` PASSED on the windows runner (9 min). Local suite green (2489+17). CLI installed: @tauri-apps/cli ^2 in frontend/package.json devDependencies (npm workspace-hoisted to root node_modules/.bin — npx tsc/vitest resolved fine in earlier CI steps). Root package.json HAS "build": "npm run build --workspace frontend". src-tauri/tauri.conf.json: frontendDist=../target/frontend-dist, beforeBuildCommand="npm run build", bundle.targets="all", icons listed under src-tauri/icons/. Job logs are NOT anonymously accessible (jobs API 403 admin-only; annotations thin; the Actions UI lazy-load never fires logged-out — verified via headless browser) — diagnosis must come from LOCAL reproduction. Windows-leg candidates: tauri CLI startup error (config schema validation, frontendDist existence check, beforeBuildCommand CWD resolution, bundler prerequisite e.g. missing icon files under src-tauri/icons/). macOS-leg candidates: aarch64 crosscompile error (the toolchain step installs the aarch64-apple-darwin target) or a macOS/unix-specific test failure. REGRESSION-TEST DESIGN: once the root cause is known, add a guard that fails pre-fix and passes post-fix (pattern: the tests/integration/ci_workflow.rs textual guard over workflow/config files; for a code fix, a unit test pinning the fixed invariant). RE-RELEASE DESIGN: after the fix lands on main, delete the unpublished remote tag (git push origin :refs/tags/v0.1.1), re-create the annotated tag on the new merge commit, push — the release job then publishes v0.1.1 with installers; verify via the public API (actions/runs + releases/tags/v0.1.1). The failed run + old tag stay as history.

## Steps
- [x] 1. **Reproduce with failing regression test** — Write a regression test that reproduces the defect (constitution: every defect gets a regression test that fails without the fix and passes with it). Run it and confirm it FAILS.
- [x] 2. **Document root cause** — Investigate and document the root cause. memory_write a BUG: record (symptom → root cause → fix + regression test name, ≤600 chars).
- [x] 3. **Minimal fix** — Apply the minimal fix that makes the regression test pass. Do not refactor unrelated code.
- [ ] 4. **Verify** — Run the regression test + the full test suite (cargo test unpiped, warning-free). Record the regression test name via update_plan (regression_test field) — finish is blocked without it. If the fix turned out FEATURE-SCALE (new pub types or cross-module surface): amend the plan context via update_plan append with a paragraph starting "Landed design —" (architecture, deviations from the sketch, measured numbers, invariants to preserve) and record landed_design=true BEFORE completing this step; at finish write a SPEC memory and amend the BUG record with the landed outcome (agent.md 'Documentation expectations'; the finish gate blocks landed_design without the context amendment).

## Detailed steps
- [x] **Reproduce the windows leg locally** — Run `npx tauri build` at the repo root (C:/mnemo) via shell — capture the exact error (expect a fast exit-1 mirroring CI's ~2s; if it instead succeeds, capture where the installers land under target/release/bundle/). Also run `cargo test --workspace` (expect green — documents that the macOS leg is NOT locally reproducible on this Windows box).
- [ ] **Root cause** — From the repro error, identify the windows-leg cause (candidates: tauri CLI startup error — config schema validation / frontendDist existence check / beforeBuildCommand CWD resolution / missing bundler prerequisites e.g. icon files under src-tauri/icons/). For the macOS leg: run `rustup target add aarch64-apple-darwin` (if missing) then `cargo check --workspace --target aarch64-apple-darwin` to catch a crosscompile error; if crosscompile is clean, read cfg(unix)/macOS-specific code paths for test-failure candidates. memory_write the BUG record covering BOTH legs with the found cause(s).
- [ ] **Fix** — Apply the minimal fix per the found cause(s) in the offending file(s) (.github/workflows/build.yml, src-tauri/tauri.conf.json, package.json, or src-tauri code — wherever the repro points). Add the regression guard: fail-pre-fix, pass-post-fix (pattern: extend tests/integration/ci_workflow.rs with a textual guard over the workflow/config files, or a unit test pinning the fixed invariant for a code fix).
- [ ] **Verify + re-release** — Verify: `npx tauri build` completes locally producing target/release/bundle/msi/*.msi + nsis/*.exe; `cargo test --workspace` green; actionlint on .github/workflows/build.yml if touched. Then land on main (merge_to_main skill), delete the unpublished remote tag (`git push origin :refs/tags/v0.1.1`), re-create annotated tag v0.1.1 on the new merge commit, push (approval-gated), verify the new run goes green via the public API (Invoke-RestMethod actions/runs?event=push) and the release publishes at releases/tag/v0.1.1 with all four asset kinds (.msi/.exe/.dmg/mnemo.app.zip).

## Bug
The v0.1.1 tag build run (35514715816) failed on both platform legs and the release was never published: windows job step "Tauri build (unsigned installers)" (`npx tauri build`) exited 1 in ~2 seconds; macos job step "Rust tests (workspace, macOS compile validation)" (`cargo test --workspace`) exited 101 after ~3 minutes; the release job (needs both) was skipped. Both steps had NEVER executed before — the workflow never loaded until the L52 + secrets-if fixes (56132d3, b5d8ef8) — these are latent pre-existing breakages the fixes exposed, not regressions (nothing in the landed changes affects the Rust compile or the bundler).
