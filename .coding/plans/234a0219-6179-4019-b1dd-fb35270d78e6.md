# Plan: Diagnose the 2026-08-22 crash while researching the crash

## Goal
Find the cause of the most recent app crash (reported 2026-08-22, "crashed again while researching the crash"), produce a ranked diagnosis report, and log every step + finding to memory per the crash-research logging protocol.

## Kind
research

## Context
User reported the app crashed again (2026-08-22) while the agent was researching earlier crashes, and asked why. Evidence sources: (1) always-on provider-error log at .coding/logs/provider-errors.jsonl (mirrors every failed LLM request), (2) opt-in full trace log .coding/logs/traces.jsonl, (3) the memory store (working-tier events, crash markers, episodic summaries), (4) plan files under .coding/plans/ showing what the agent was executing at crash time. Known historical failure modes to correlate against: F1 blocking git-branch poll (freeze), F3 unbounded frontend transcript (slowdown) — both fixed + merged 2026-08-17; R1/R2 RDP desktop-switch freeze (fixed 2026-08-18); memory-cleanup LLM storms (~101 sessions x 3 LLM calls); Grammarly-for-Windows desktop injection; LLM-trace file-mirror hot path. Earlier markers say the freeze research itself was resolved, so this is a NEW incident: gather fresh evidence, do not re-diagnose old ones.

## Steps
- [x] 1. **Collect crash evidence from app logs** — List .coding/logs/ (PowerShell Get-ChildItem, note file sizes + LastWriteTime). Read the tail of provider-errors.jsonl and traces.jsonl if present (file_read with start_line near end). Record: timestamp of the LAST entry in each log, any error pattern (HTTP >= 400, transport errors, timeouts), and whether the last entry time matches the reported crash time 2026-08-22. Write a crash-research-protocol memory (working tier) with findings before moving on.
- [x] 2. **Reconstruct last pre-crash agent activity** — List .coding/plans/ by LastWriteTime; read the newest plan file and note its step completion state. memory_recall("session events immediately before crash 2026-08-22") to see what tool/step the agent was mid-way through when it crashed (expected: memory_recall/search/file reads during crash research). Correlate plan timestamps with log timestamps. Persist a working-tier step marker + any learning.
- [x] 3. **Correlate with known failure modes** — Check each known historical cause against the evidence: F1 blocking git poll and F3 unbounded transcript (verify fixes still present at HEAD — search for the spawn_blocking git poll and transcript cap code), R1/R2 RDP resize fixes, memory consolidation LLM storms, Grammarly injection, trace hot-path mirroring. Rule out what can't match the log evidence; rank remaining hypotheses. Persist learnings (semantic tier) as they are confirmed/ruled out.
- [x] 4. **Write diagnosis report + memorize** — Write .coding/reviews/2026-08-22-research-crash-diagnosis.md with: what the agent was doing at crash time, ranked hypotheses with evidence for/against, and a recommended next step (e.g. enable traces.jsonl, add a crash log, or fix). Write final semantic memory summarizing the diagnosis + supersede the 'new incident' note with the answer. Respond to the user with the top hypothesis in <=3 lines.
