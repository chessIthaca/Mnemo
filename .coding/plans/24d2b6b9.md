# Plan: Fix stuck running tool cards after mid-stream steer/interrupt (backlog 63cbc20f)

## Goal
Every announced tool call gets a terminal ToolResult even when the turn is cut mid-stream (steer/interrupt/compact) or retried for bad JSON; the frontend never leaves a tool/memory card spinning after finished/final-error.

## Kind
bug_fixing

## Context
Root cause confirmed (2026-09-18 session): (a) src/agent/turn.rs ~1253 mid-stream stop block (Steer/Interrupt/Compact) returns BEFORE acc.finalize() and the tool loop, so a call already announced via AgentEvent::ToolCallStart (forwarded at turn.rs:1085) never gets a ToolResult — the tool-loop stop paths (turn.rs:1499, 1820) synthesize "interrupted: not run (turn stopped)" but this earlier path does not. (b) turn.rs:1321 bad-JSON retry branch pushes Role::Tool error messages into history but emits no ToolResult events → same orphaning while the turn continues. (c) Frontend reduceFinished (frontend/src/hooks/agentEventReducer.ts:1163) sweeps nothing (reduceError:1203 sweeps vision cards only), so nothing finalizes the orphaned card; its amber border is ToolCard's running color (border-yellow-600/40, Message.tsx:703). Sibling bug fixed earlier: 300eddf (memory entry stealing tool_result). Precedent tests: src/agent/tests.rs:3726 mid_turn_steer_soft_stops_and_carries_pending_steer (PausingProvider pattern); frontend useAgentStore.test.ts:1233 vision sweep test.

## Steps
- [x] 1. **Reproduce with failing regression test** — Write a regression test that reproduces the defect (constitution: every defect gets a regression test that fails without the fix and passes with it). Run it and confirm it FAILS.
- [x] 2. **Document root cause** — Investigate and document the root cause. memory_write a BUG: record (symptom → root cause → fix + regression test name, ≤600 chars).
- [x] 3. **Minimal fix via sub-plan** — Push a sub-plan (create_plan — it becomes the active plan and pops back to this one when it completes) whose steps implement the fix with the LEAST amount of change possible; do not refactor unrelated code. Write it for a lesser model to execute alone: every step self-contained (explicit file paths, exact edits, the root cause + failing test name as context).
- [x] 4. **Verify** — Run the regression test + the full test suite (cargo test unpiped, warning-free). Record the regression test name via update_plan (regression_test field) — finish is blocked without it.

## Bug
read_files tool card never leaves "running" and keeps the amber running border after a mid-stream steer lands (backlog 63cbc20f) — announced tool call gets no ToolResult

## Regression test
mid_stream_steer_emits_not_run_result_for_announced_tool_call
