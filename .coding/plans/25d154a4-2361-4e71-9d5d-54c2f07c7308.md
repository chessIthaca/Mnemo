# Plan: Phase 2a implementation breakdown (perf H3/M1 + scroll)

## Goal
Implement Phase 2a FE streaming perf quick wins exactly: 1) rAF-batch reasoning_delta + tool_call_arg_delta (extend existing text batching, never touch non-delta path); 2) narrow Zustand selectors in App.tsx + MainPanel.tsx (active state by id, tab metadata via useShallow minimal shape); 3) smart stick-to-bottom in Conversation (only auto-scroll when near bottom; keep throttle; update on scroll). Scope strictly limited to these; preserve all existing behavior, tests, and line-ending style. Add useShallow import. Keep reducers for tests. Add batched append actions to store. Update clear for new buffers. After edits: cargo test, then complete the step.

## Context


## Steps
- [x] 1. Read full source of useAgentEvents.ts, useAgentStore.ts (relevant parts), App.tsx, MainPanel.tsx, Conversation.tsx, Message.tsx (for context only), types if needed.
- [x] 2. Extend BufferHandle + dispatch/flush/clear in useAgentEvents.ts to also rAF-batch reasoning_delta (accumulate text) and tool_call_arg_delta (accumulate per index). Flush the new buffers on any non-delta event before handle. Schedule single rAF. Call new store batched appends on flush. Update clearStreamingBuffer to drain all.
- [x] 3. Add to useAgentStore.ts: appendStreamingReasoning(id, text) implementing the net effect of reduceReasoningDelta for a coalesced batch; applyToolCallArgDeltas(id, deltas) that applies multiple arg fragments for the agent in a *single* store update (clone transcript once). Keep existing reduce* untouched (used by tests and non-stream paths). Export types if needed.
- [x] 4. Narrow subscriptions: in App.tsx remove broad `agents` select, compute activeState via narrow selector `s => s.activeAgent!=null ? s.agents[s.activeAgent] : undefined`. In MainPanel.tsx replace broad `agents` + `agentNames` selects with useShallow selector returning only minimal tab metadata array `[{id,name,running,needsApproval}]`; derive otherApprovals and tab rendering from it. Keep setActiveAgent. Add `import { useShallow } from 'zustand/shallow';` where used.
- [x] 5. Make scroll smart in Conversation.tsx: add scrollContainerRef to the overflow div; track stickToBottomRef (initial true); attach passive scroll listener that sets flag based on distance-to-bottom <= threshold (e.g. 80px); gate the existing throttled doScroll so it only runs when stick flag is true; after programmatic scroll set flag true. Keep 100ms throttle and deps. Also clean up.
- [x] 6. Verify no other files touched. Run `cargo test` (unpiped, capture exit) to satisfy step rule. If passes, mark step complete via complete_step. (FE tests optional but can run `cd frontend; npm test -- --run` if needed for confidence, but not required for this micro-step close).
