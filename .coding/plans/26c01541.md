# Plan: Retry backoff: equal jitter + turn-level trace attribution

## Goal
Desynchronize retry backoff with equal jitter via a shared helper used by both retry loops (request-level complete_with_retry and turn-level run_turn_attempt), and attribute the turn-level backoff sleeps to the trace graph via record_backoff_ms — completing the observability work from plan 6e734ccb.

## Kind
implementation

## Context
User chose "Attribution + jitter" for the turn-level retry backoff (follow-up to plan 6e734ccb). Current state: complete_with_retry (src/agent/dispatch.rs:686-777) sleeps fixed 1s/2s (delay_ms state, doubles) and parks via record_backoff_ms; run_turn_attempt (src/runtime/agent.rs:352-449) sleeps fixed 1000*2^(attempt-1) (line 432) with NO attribution — invisible in the trace graph. MAX_PROVIDER_TURN_ATTEMPTS=3 (agent.rs:25). Error classification lives in src/error.rs (is_non_retryable:102, is_rate_limited:170) — the backoff helper belongs there. No `rand` dependency — use SystemTime subsec_nanos (non-crypto, fine for desynchronization). AgentLoop::provider() -> Arc<dyn LlmClient> exists (loop_impl.rs:1256); run_turn_attempt has self.agent_loop. The 429-switch path doesn't sleep (nothing to attribute); non-retryable fails fast. Existing paused-clock test complete_with_retry_attributes_backoff_sleeps_to_next_attempt (src/agent/tests.rs) asserts exact vec![1000, 2000] — must become band assertions.

## Steps
- [x] 1. **Add jittered retry_backoff_ms helper to src/error.rs**
- [x] 2. **Use the helper in complete_with_retry (dispatch.rs)**
- [x] 3. **Jitter + attribution in run_turn_attempt (runtime/agent.rs)**
- [x] 4. **Tests: jitter bands + turn-level attribution regression**
- [x] 5. **SPEC memory update + full cargo test**
