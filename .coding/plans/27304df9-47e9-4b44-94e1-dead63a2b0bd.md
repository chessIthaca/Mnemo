# Plan: Agent backlog_add tool + queue the supersession item

## Goal
Deliver (A) an agent-facing `backlog_add` tool (brain crate, Workflow category, AutoRun) sharing the app's single BacklogStore Arc via a factory setter wired in main.rs, with unit + factory + meta tests; (B) add the memory-supersension backlog item (id 68) to .coding/backlog.json via a validated, user-authorized external write.

## Kind
implementation

## Context
User request 2026-08-20: "just implement what you want to the backlog and also create a command that lets you add things to the backlog." Two deliverables: (A) an agent tool `backlog_add` so future agent sessions can add backlog items through the sanctioned store API (the file tools correctly refuse .coding/backlog.json as protected state); (B) actually add the memory-supersession backlog item (drafted last session, blocked by the file guardrail) — user has now explicitly authorized this one external write. VERIFIED FACTS: BacklogStore lives in the brain at src/backlog.rs — `BacklogStore::open(PathBuf)` (missing/corrupt → empty, never panics), `add(&mut self, text: String, images: Vec<String>) -> BacklogItem` (allocates monotonic id, sets Pending + created_at, persists atomically every mutation); BacklogItem {id, text, images, status, created_at, note}. The IPC layer (src-tauri/src/ipc/backlog_cmds.rs:85-95) locks `state.backlog.store` (an Arc<tokio::sync::Mutex<BacklogStore>>) and calls add — the agent tool must share THIS SAME Arc (created in main.rs setup at ~:203 BEFORE the brain_result match, then moved into IpcState) so UI + agent see one store. Factory wiring precedents: `set_spawner(&self, Arc<dyn AgentSpawner>)` interior-mutability setter called in the Ready branch of main.rs setup BEFORE the main agent is built (set_spawner at main.rs:245); conditional tool registration precedent `register_memory_tools` (factory.rs:680-697, `if let Some(store)`); workflow tools registered via `register_workflow_tools(&self, registry, workflow)` (factory.rs:584) — that fn has &self so it can read an Option field. Tool module layout: src/tool/workflow/{mod?,ask_user,plan,skill}.rs — check the module registration file during impl (search `pub mod ask_user` / re-exports). Tool traits: name/category()/safety()/schema()/execute like tool::workflow::ask_user. Category choice: ToolCategory::Workflow (create_plan precedent — workflow tools are callable in Planning, which is exactly when backlog items are captured; test planning_gate_hides_write_tools_until_a_plan_exists passes with create_plan). Safety: SafetyLevel::AutoRun (create_plan_never_needs_approval precedent; backlog add is a benign, UI-deletable note — user explicitly wants frictionless). Tool needs NO workflow/loop deps — only the store Arc. NOTE: the agent tool cannot emit the Tauri backlog://changed event (no AppHandle in the brain) — document in the schema that items appear in the UI after the backlog view refreshes/restart; disk state is always correct (write-through). Text cap 4000 chars (protective). Deny(warnings); doc comments on all pub items. For deliverable (B): .coding/backlog.json currently has next_id=68, max created_at=1787211566 (machine clock is BEHIND file timestamps — use max+1=1787211567, never DateTime.Now). External write is user-authorized for THIS item; must round-trip via PowerShell ConvertFrom-Json/ConvertTo-Json -Depth 10 and validate; the live app holds the store in memory so the item becomes visible on next app restart (next session) — note to user not to edit the backlog in the UI before restarting. Test plan: tool unit tests (tempdir store: success persists + returns id/pending; empty + whitespace text error; >4000 chars error), factory tests (expected_tool_names_registered_when_fully_wired gains backlog_add + with_backlog wiring; conditional_tools_absent_without_their_wiring gains an absent-without-backlog assert), meta test (name/category/safety like tool::browser::tests::safety_levels_are_set). Closing sequence per implementation kind: tests → reviewer → fix → commit (include report) → finish.

## Steps
- [x] 1. **Brain: BacklogAddTool module** — Create src/tool/worklog — NO, src/tool/workflow/backlog.rs (match the existing workflow tool module dir). First check how workflow tools are exported (search `mod ask_user` / `pub use` in src/tool/workflow/ or src/tool/mod.rs) and register `pub mod backlog;` the same way. Implement `pub struct BacklogAddTool(pub Arc<tokio::sync::Mutex<myharness::backlog::BacklogStore>>)` (import from crate::backlog — the tool lives IN the myharness crate, so `crate::backlog::BacklogStore`). Implement Tool: name "backlog_add"; category ToolCategory::Workflow; safety SafetyLevel::AutoRun; schema: properties {text: string, required, "The backlog item text — a self-contained task/prompts for a future session (max 4000 chars)"}, required ["text"] — description states: adds a Pending item to the user's backlog (the same list the Backlog tab shows); items surface in the UI after the backlog view refreshes/app restart (disk is write-through); use it to capture user-requested follow-ups instead of editing .coding/backlog.json (protected). execute: parse args (text: String); reject empty/whitespace-trimmed text and >4000 chars with clear errors; `let item = self.0.lock().await.add(text, Vec::new());` return ToolResult::success with output `added backlog item #<id> (pending): <first 120 chars>…` and data Some(json!({"id": item.id, "status": "pending"})). Doc comments on struct + impl items. #[cfg(test)] mod tests with tempdir (dev-dep exists at root): (1) add_success_allocates_id_and_persists — open store at tempdir/backlog.json, run tool execute, assert success, id==1, reopen store from disk and assert the item text matches + status Pending; (2) empty_and_whitespace_text_error; (3) oversize_text_errors (4001 chars). Also a meta test name_category_safety asserting name/category/safety (Workflow + AutoRun). Run `cargo test backlog` unpiped at root + $LASTEXITCODE.
- [x] 2. **Factory: backlog dep + conditional registration** — src/agent/factory.rs: (a) add field `backlog: std::sync::RwLock<Option<Arc<tokio::sync::Mutex<crate::backlog::BacklogStore>>>>` (mirror the set_spawner interior-mutability pattern — read its exact field type around :186-200 first) initialized None in both constructors if there are two; (b) add `pub fn set_backlog(&self, store: Arc<tokio::sync::Mutex<crate::backlog::BacklogStore>>)` with doc comment (called by the IPC layer once the shared store exists, before the first agent build — set_spawner precedent); (c) in register_workflow_tools (has &self — :584), after the existing registrations, add a gated block `if let Some(store) = self.<read backlog field> { registry.register(Box::new(crate::tool::workflow::backlog::BacklogAddTool(Arc::clone(store)))); }` with a short comment (memory-tools precedent). Check how set_spawner's field is read (expect()/read().clone().unwrap pattern) and copy it. Update tests: expected_tool_names_registered_when_fully_wired — wire the store into the factory (`let store = Arc::new(tokio::sync::Mutex::new(crate::backlog::BacklogStore::open(dir.path().join("backlog.json")))); factory.set_backlog(store);` near the set_spawner line ~:1152) and add "backlog_add" to the expected Vec (unconditional — the test factory now wires it); conditional_tools_absent_without_their_wiring — add `assert!(registry.get("backlog_add").is_none(), "backlog_add should NOT be registered without the store wired")`. Run cargo test agent::factory unpiped.
- [x] 3. **Wire the shared store in main.rs setup** — src-tauri/src/main.rs: in the Ready branch of the brain_result match (where set_spawner/set_descendant_tracker are called, ~:245), add BEFORE the main agent is built: `brain.factory.set_backlog(Arc::clone(&backlog));` with a 2-3 line comment: shares the ONE BacklogStore Arc between the IPC commands (state.backlog.store) and the agent's backlog_add tool — both go through the same mutex, so agent adds and UI mutations stay consistent; no backlog on the NeedsProject/Err paths (no factory). `cargo build -p myharness-app` + `cargo test -p myharness-app` unpiped, check $LASTEXITCODE.
- [x] 4. **Add the supersession backlog item (user-authorized external write)** — PowerShell (NOT file tools — backlog.json is protected state; the user explicitly authorized this single write): read .coding/backlog.json via ConvertFrom-Json, append a PSCustomObject item { id = 68; text = "Memory store: supersession support — memory_write accepts a supersedes/superseded_by field, status-changing writes (SHIPPED / MERGED / ABANDONED / STATUS UPDATE) name the entry they supersede, and recall deprioritizes or annotates superseded rows. Rationale: stale state currently recalls alongside its superseder (e.g. 'plan 7a86564c 5/11 crashed' next to '11/11 done'); see the 2026-08-20 memory-protocol decision (PROTOCOL v2)."; images = @(); status = "pending"; created_at = 1787211567; note = $null }, set next_id = 69, ConvertTo-Json -Depth 10, write UTF-8. Validate: ConvertFrom-Json re-parse + assert items.Count is 9 and next_id 69; print the new item's id. GOTCHA: the live app holds the store in memory — tell the user the item appears after the next app restart and not to mutate the backlog in the UI before then (an in-memory UI mutation would overwrite the file).
- [x] 5. **Full matrix, then closing sequence** — Run unpiped + $LASTEXITCODE: `cargo test --workspace` at root (expect existing suites + new backlog tool/factory tests green, zero warnings). Frontend untouched — skip tsc/vitest, note why. `git status --short` to confirm scope (expected: src/tool/workflow/{backlog.rs,mod-or-tool-mod.rs}, src/agent/factory.rs, src-tauri/src/main.rs, .coding/backlog.json + .coding bookkeeping). Then the closing sequence runs automatically: spawn reviewer over ALL uncommitted changes, fix every finding, re-run cargo test --workspace, commit to feat/backlog-add-tool (include the review report), finish with the report path. Final summary ≤3 lines + Decisions: AutoRun+Workflow rationale (create_plan precedent), shared-Arc single-store design, no Tauri event from the brain (UI refresh note), 4000-char cap.
