# Plan: Run-all/unattended sessions must expose backlog_add — close the Skill allow-list gap (66c65db9)

## Goal
Make a direct user request to queue a backlog item never impossible in ANY surface a run-all/unattended session can be in (backlog 66c65db9). The run-all dispatch provably rides the main agent's normal state-filtered surface (pinned by a new source-contract test on run_all_dispatch_next); all base states already expose backlog_add (the 2026-12-30 reversal, 61cc0ab); the one remaining hole — the exact reproduction of the reported symptom (status+list present, add absent) — is the Skill allow-list branch (tool/mod.rs:505-521). Add backlog_add to that branch's always-available set, update the codifying test doc comments, and pin both layers with regression tests: the flipped Skill assertion (backlog_add visible under Skill(vec![])) and the run-all source-contract test (the dispatch is a plain Prompt with no tool-surface restriction). backlog_list/backlog_status remain present (existing tests keep passing); the interactive base surface is unchanged (existing base-state tests keep passing); the reviewer ROLE filter stays read-only.

## Kind
bug_fixing

## Context
Investigation findings (all verified by reading current code + the 61cc0ab diff): (1) There is NO run-all-specific tool surface — run_all_dispatch_next (src-tauri/src/ipc/run_all.rs:1433-1441) sends a plain AgentCommand::Prompt to the MAIN agent; the run-all session's surface = the main agent's registry (backlog_add registered via factory set_backlog wiring, always in the IPC app) + the workflow-state filter. (2) All base states already expose backlog_add — Planning (tool/mod.rs:328), Executing (:367), ExecutingResearch (:393-395), Reviewing (:399, the 2026-12-30 reversal commit 61cc0ab, itself triggered by this incident: "one silently-lost backlog_add"), Complete (:499). (3) The ONE remaining hole — and the exact reproduction of the reported symptom (backlog_list + backlog_status present, backlog_add absent) — is the Skill allow-list branch (tool/mod.rs:505-521): under ToolFilter::Skill(allowed), backlog_status + backlog_list ride the always-available set but backlog_add does not (unless the skill's list names it). The 61cc0ab reversal EXPLICITLY left this unchanged as a scope guard ("backlog_add stays allow-list-only under skills") and plan 8a16bc91's context named this item (66c65db9) as the related follow-up. Reachable from run-all whenever an item invokes a skill mid-run (e.g. merge_to_main after completing work on a branch) and the user steers to queue an item. (4) The Skill-state prompt fragment (src/agent/prompt.rs) does NOT enumerate the always-available set — no prompt change needed. (5) The reviewer ROLE filter (ToolFilter::Reviewer, spawn.rs REVIEWER_BASE_TOOLS) stays read-only — backlog_add must NOT be added there. Fix decision (the item's option 1, "simply add it", per the codebase's design): the filter is state-derived with no run-all special-casing (run-all is just a dispatch pattern), and the loop-prevention concern (an agent queueing work for itself) is already accepted for every base state — the run-all agent has backlog_add in Planning/Executing/Reviewing/Complete today. The widening applies uniformly to interactive and run-all skill surfaces; the interactive BASE surface is unchanged (all base-state arms untouched). Existing tests to consult: backlog_add_visible_in_all_states (~mod.rs:1747-1762, has the Skill(vec![]) assertion to flip), backlog_status_visible_in_all_states_including_skills (~:1778), backlog_list_visible_in_all_states_and_skills (~:1806); the fn_body source-contract pattern in run_all.rs tests (~:526-536).

## Steps
- [x] 1. **Reproduce with failing regression test** — Write a regression test that reproduces the defect (constitution: every defect gets a regression test that fails without the fix and passes with it). Run it and confirm it FAILS.
- [x] 2. **Document root cause** — Investigate and document the root cause. memory_write a BUG: record (symptom → root cause → fix + regression test name, ≤600 chars).
- [x] 3. **Minimal fix** — Apply the minimal fix that makes the regression test pass. Do not refactor unrelated code.
- [x] 4. **Verify** — Run the regression test + the full test suite (cargo test unpiped, warning-free). Record the regression test name via update_plan (regression_test field) — finish is blocked without it.

## Bug
During the 2026-12-30 unattended run-all batch (backlog 569b5922, plan 593f4a4e), the user asked mid-run to add a backlog item ("plan shows a weird odd id number... add to the backlog of course"); the run-all session's tool surface had NO backlog_add (backlog_list and backlog_status were present) — the agent could not comply and had to stash the drafted item in memory for a later interactive session to queue.

## Regression test
backlog_add_visible_in_all_states
