# Plan: Whole-app review: quality, security, perf, build, memory, architecture

## Goal
Run three read-only review agents over the whole codebase (quality+architecture, security, performance+build+memory) and consolidate their findings into a single review report file. No code changes.

## Kind
research

## Context
User request: "spawn a couple of review agents focusing on quality, security, performance of the app, the build system efficiency, memory efficiency and overall architecture. Write a single report file to review. Do not change code." Working tree is clean (all prior work committed on feat/provider-error-log). Review scope is the codebase at HEAD: Rust lib (src/), Tauri shell (src-tauri/, incl. ipc/), frontend (frontend/src/), build config (Cargo.toml workspace, frontend package.json, tauri.conf.json). Deliverable: ONE consolidated report file under .coding/reviews/ — no source-code changes. Research plan (kind: research) so completion skips the review gate.

## Steps
- [x] 1. **Spawn quality + architecture reviewer** — call spawn_agent (role:"reviewer", name "reviewer-quality-arch", default model). Task must be self-contained: review the myharness codebase at C:\AgenticCoder\AgenticCoder (working tree is CLEAN — git diff HEAD is empty; read the code with file_read/search, do not run shell). Assess (a) code quality: naming, dead code, duplication, error handling, test quality/coverage gaps, doc comments; (b) overall architecture: module boundaries (src/ config, provider, agent, tool, workflow, memory, browser, skill, safety_rules, model_resolver, project, error, app), coupling between layers, the Tool/tool-registry/workflow-state design, plan lifecycle (create_plan/update_plan/complete_step), Tauri IPC contract (src-tauri/src/ipc/), frontend state management (frontend/src/hooks/useAgentStore, zustand), extension points. Write findings by severity (critical/high/medium/low, correctness/bugs/design) with file:line refs to .coding/reviews/2026-08-13-app-review-quality-arch.md via write_review_report (bare filename). State "no findings" for any area that is clean. Be thorough but prioritize actionable findings; note trade-offs, not just nits.
- [x] 2. **Spawn security reviewer** — call spawn_agent (role:"reviewer", name "reviewer-security", default model). Self-contained task: threat-model the myharness app at C:\AgenticCoder\AgenticCoder (clean working tree; use file_read/search; no shell). Focus: path-sandbox confinement (src/tool/agent/sandbox.rs — traversal, symlinks, absolute paths, drive letters), shell tool injection (src/tool/agent/shell*), SSRF surface (provider base_url, browser normalize_url allow-list in src/browser/mod.rs, describe_image data URLs), secrets handling (keys.toml DACL on Windows, key echo in logs/traces), IPC input validation (src-tauri/src/ipc/*), webview hardening (tauri.conf.json CSP, additionalBrowserArgs --disable-extensions), prompt-injection/agent-safety surface (safety rules, approval gating, SafetyLevel, never_auto_for), memory-store SQL (rusqlite FTS), temp-file handling. Write findings by severity with file:line refs to .coding/reviews/2026-08-13-app-review-security.md via write_review_report. State "no findings" for areas that are clean.
- [x] 3. **Spawn performance + build + memory reviewer** — call spawn_agent (role:"reviewer", name "reviewer-perf-build-memory", default model). Self-contained task: review the myharness app at C:\AgenticCoder\AgenticCoder (clean working tree; use file_read/search; no shell). Cover: (a) runtime performance: async lock contention (tokio Mutex scopes in src/browser/mod.rs, src/agent/*), blocking calls on the async runtime (std::fs, rusqlite, tiktoken-rs, syntect in async paths), per-turn overhead (context assembly, memory recall, provider streaming), hot paths (console event forwarding, streaming UI updates); (b) build system efficiency: Cargo workspace layout (root + src-tauri member), dependency bloat vs. what's actually used (reqwest features, chromiumoxide, syntect, tiktoken-rs, rusqlite bundled), feature flags, incremental build impact, frontend build (vite config, tsc+vite, bundle size, dev vs prod CSP), tauri.conf.json build hooks; (c) memory efficiency: unbounded growth (ring buffers, caches, logs, trace ring), clone-heavy hot paths, leak-prone patterns (tempdirs, spawned tasks), context/session growth. Write findings by severity with file:line refs to .coding/reviews/2026-08-13-app-review-perf-build-memory.md via write_review_report. State "no findings" for areas that are clean.
- [x] 4. **Consolidate into a single report** — after all three reviewer finish notifications arrive, read the three report files (.coding/reviews/2026-08-13-app-review-quality-arch.md, -security.md, -perf-build-memory.md) and write ONE consolidated review file .coding/reviews/2026-08-13-full-app-review.md: title, date, scope, a summary verdict table (area × count by severity), then findings grouped by severity (critical/high/medium/low) across all areas with file:line refs and a one-line suggested fix each, then a short 'per-area detail' section pointing at the three individual files. No code changes; do not modify the three per-area files. Report the consolidated file path to the user.
