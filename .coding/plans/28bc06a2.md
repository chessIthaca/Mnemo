# Plan: Fix the Run-All intervention strand — interrupted items never close (b83e891f)

## Goal
Fix the Run-All intervention strand: an interrupt/steer mid-plan must leave the item resolvable by the turn that completes its plan. Mirror the approval-halt design (keep the item InFlight and the run state — stop flag set — while the plan is active), pin the interrupt → resume → complete → done cycle with a red-without-fix regression test, and keep the never-terminal / never-auto-continue intervention guarantees intact.

## Kind
bug_fixing

## Context
Backlog b83e891f (user green-lit "lets tackle the root fix"). Exploration done 2027-01-07: the strand path is confirmed in src-tauri/src/ipc/run_all.rs — the steer arm of halt_run_all ends the run after filling the latch (:1935); the soft-stop resolution consumes the latch, closed_loop=false (plan open) → handle_user_intervention requeues InFlight→Pending (:2029) and ends the run (:2070); the continuation turn completes the same plan but its resolution finds run_all=None, single_in_flight=None, latch consumed — every path blind. The approval-halt path solved this exact shape by KEEPING the run state ("the post-approval turn resolution still sees the item"; stop flag prevents the next dispatch) — the steer/interrupt path must mirror it. Open question for the reproduce step: whether the Executing-entry stamp (events.rs:453-465 → stamp_backlog_in_flight :2145, linkage refresh :2186) fired on the re-dispatch — item 45a4eb88's plan_id in .coding/backlog.jsonl settles it. The b83e891f item text's fix direction (re-link on re-dispatch) is superseded by this sharpened root cause; the closure note records that.

## Steps
- [x] 1. **Reproduce with failing regression test** — Write a regression test that reproduces the defect (constitution: every defect gets a regression test that fails without the fix and passes with it). Run it and confirm it FAILS.
- [x] 2. **Document root cause** — Investigate and document the root cause. memory_write a BUG: record (symptom → root cause → fix + regression test name, ≤600 chars).
- [x] 3. **Minimal fix** — Apply the minimal fix that makes the regression test pass. Do not refactor unrelated code.
- [x] 4. **Verify** — Run the regression test + the full test suite (cargo test unpiped, warning-free). Record the regression test name via update_plan (regression_test field) — finish is blocked without it.

## Bug
A Run-All item interrupted mid-plan (steer or interrupt) re-dispatches forever: the intervention requeues it to Pending and ends the run, the continuation turn completes the SAME plan, but no resolution path can see the item (run state destroyed, single_in_flight never set, latch consumed) — item 45a4eb88 was dispatched three times (checkpoints bd754a2, 4ba30e3, 2158138) despite two completed plans (024a06e1/568d405 round-2 PASS; 30e7db62/72ed3a2 verification) and only closed via a manual backlog_status correction.

## Regression test
intervention_keeps_a_run_all_item_in_flight_with_its_run
