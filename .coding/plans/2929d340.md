# Plan: Remove #[allow(dead_code)] on extract_checkpoint_sha — expose checkpoint_sha in the backlog payload

## Goal
Remove the tree's only #[allow(dead_code)] (on extract_checkpoint_sha) by restoring a production caller: surface the run-all checkpoint sha as a parsed, machine-readable checkpoint_sha field in the backlog UI payload (backlog_list, backlog_add, and the backlog://changed event), keeping the build warning-free under deny(warnings) with tests on both sides of the IPC contract.

## Kind
implementation

## Context
Backlog item 212c14af (from the 2026-12-29 four-axis review): the tree's first #[allow(dead_code)] sits on extract_checkpoint_sha (src-tauri/src/ipc/run_all.rs:852) — callerless since backlog 45dcf577 removed the rollback arm; only its #[cfg(test)] extract_tests call it. The constitution forbids #[allow] to silence warnings. DECISION: option (b) — restore a real caller by exposing the parsed sha as a machine-readable checkpoint_sha field in the backlog UI payload (arch-perf review's preferred option; turns the manual resume/rollback anchor into a feature). Design: IPC-layer view struct BacklogItemView (#[serde(flatten)] item: BacklogItem + checkpoint_sha: Option<String>, always serialized for a stable wire shape) — the persisted BacklogItem model in src/backlog.rs stays pure (no derived field, no core-crate changes; avoids the 6 struct-literal sites). The delta is already committed in HEAD (working tree clean except .coding/backlog.jsonl) — this removes it from HEAD. No UI change: the note (which contains the sha) already renders on the backlog card; the field serves programmatic consumers.

## Steps
- [x] 1. **Backend: BacklogItemView + wiring + drop the #[allow]** — In src-tauri/src/ipc/backlog_cmds.rs: (1) add `BacklogItemView` — `#[derive(Debug, Clone, Serialize)] pub struct BacklogItemView { #[serde(flatten)] pub item: BacklogItem, pub checkpoint_sha: Option<String> }` with doc comments (wire shape = stored item fields + the parsed run-all checkpoint sha, the manual resume/rollback anchor; always serialized, null when never checkpointed) and a `pub(crate) fn new(item: BacklogItem) -> Self` that parses the note head via `extract_checkpoint_sha` (import from crate::ipc::run_all). (2) Replace `resolve_item_images` with `fn resolve_item_view(store: &BacklogStore, item: &BacklogItem) -> BacklogItemView` (same image resolution, then wrap via BacklogItemView::new) and update its three call sites: `emit_backlog_changed`, `backlog_add` (return type BacklogItemView), `backlog_list` (return type Vec<BacklogItemView>); retype `BacklogChangedPayload.items` to Vec<BacklogItemView>. In src-tauri/src/ipc/run_all.rs: delete the callerless comment block + `#[allow(dead_code)]` (lines 849-852), rewrite the doc comment (caller is now the backlog UI payload via BacklogItemView), change `pub fn` to `pub(crate) fn` (matches run_all_dispatch_next's cross-module visibility). In src-tauri/src/ipc/contract_fixtures.rs: build the dto-backlog-changed-payload items via `BacklogItemView::new(BacklogItem {...})`, changing the in-flight item's note to `Some("a1b2c3d4e5f6789012345678901234567890abcd | approval requested — halted".into())` so the fixture locks the parsed field. Update frontend/src/lib/ipc-fixtures/dto-backlog-changed-payload.json to match (item 1: "checkpoint_sha": null; item 2: the sha; keys alphabetical — serde_json's sorted-map output). Run `cargo test` (PowerShell, unpiped, check $LASTEXITCODE) — must be green, which also proves zero warnings under deny(warnings).
- [x] 2. **Rust tests: backlog_list surfaces checkpoint_sha** — In src-tauri/src/ipc/backlog_cmds.rs `mod tests`: (1) unit test `backlog_item_view_parses_checkpoint_sha` — a note "a1b2c3d4e5f6789012345678901234567890abcd | approval requested — halted" yields checkpoint_sha Some(sha) with the note preserved verbatim on view.item; a non-sha note ("approval requested") and no note both yield None. (2) source-contract test following the existing include_str! slice pattern (`backlog_retry_routes_through_guarded_requeue`): assert the bodies of `backlog_list`, `backlog_add`, and `emit_backlog_changed` each route through `resolve_item_view` so checkpoint_sha actually reaches the payload (the commands need Tauri State, hence source-contract). Keep run_all.rs extract_tests unchanged (parser tests still valid). Run `cargo test` — green.
- [x] 3. **Frontend: type the field + contract assertions** — In frontend/src/lib/types.ts: add `checkpoint_sha?: string | null` to the BacklogItem interface with a doc comment (run-all checkpoint sha parsed from the note head — the manual resume/rollback anchor, machine-readable; null when the item was never checkpointed). In frontend/src/lib/ipc-contract.test.ts: extend the dtoBacklogChangedPayload assertions — the checkpointed fixture item (items[1]) carries a string checkpoint_sha, items[0] carries null. No UI change (decision: the note already renders the sha on the card; the field serves programmatic consumers). Quick check of README.md / PLAN.md for backlog-payload docs needing a sync (update if a natural spot exists). Run `npm test --workspace frontend` (vitest run) — green.
- [x] 4. **Final verification + memory** — Run the full suites once more — `cargo test` and `npm test --workspace frontend` — both green. Search the tree for any remaining `#[allow` (expect zero). Write the DECISION memory (option (b) chosen; BacklogItemView design; why no UI chip). Then the closing sequence: reviewer subagent, fix findings, re-run tests, commit to the working branch.
