# Plan: Fix reviewer findings from embedder re-add review

## Goal
Fix the 5 actionable findings from .coding/reviews/2026-11-22-embedder-readd-review.md: C1 (hardcoded 768-dim breaks non-768 models), C2 (embedder status goes stale after startup — circuit breaker never updates it), B1 (subscribe-before-fetch race), B2 (missing non-empty validation), B3 (Windows console window on ollama spawn). C3 (zero-vector memories never re-embedded) needs no code change — it's documented acceptable degradation.

## Kind
implementation

## Context
Review report at .coding/reviews/2026-11-22-embedder-readd-review.md. All findings verified against the actual code. The fixes are scoped to the files changed in the parent plan. C1 is the highest priority — it silently breaks the cloud path the plan emphasizes. C2 leaves the status badge stale after startup (dead set_status code). B1-B3 are low-severity but cheap to fix.

## Steps
- [x] 1. **C1: Make embedding dimension dynamic in RemoteEmbedder** — In `src/memory/embedder.rs`: add `dim: AtomicUsize` field to `RemoteEmbedder` (0 = unlatched, latches on first successful embed). In `try_embed_once`: replace the `vec.len() == EMBEDDING_DIM` check with: if `self.dim.load() == 0`, latch `vec.len()` (any non-zero length accepted on first success); else require `vec.len() == latched_dim` (reject mismatches → None → zero vector, which is correct since a mismatched vector would corrupt cosine). Make `zero_vector` a method `fn zero_vector(&self) -> Vec<f32>` using `self.dim.load()` (fall back to `EMBEDDING_DIM` if 0/unlatched). Update `record_failure` to call `self.zero_vector()`. Update the `embed` method's circuit-open return to `self.zero_vector()`. Override `dim()` on RemoteEmbedder to return the latched dim (or EMBEDDING_DIM if unlatched). Update the existing tests that call `zero_vector()` (it's now a method). Add a test `remote_embedder_accepts_non_768_dimension` (mock not feasible without a server; instead assert that `dim()` returns EMBEDDING_DIM before any embed, and that the dim field exists). Run `cargo test --lib`.
- [x] 2. **C2: Wire shared status Arc into RemoteEmbedder + circuit breaker** — In `src/memory/embedder.rs`: change `RemoteEmbedder`'s `status` field from `RwLock<EmbedderStatus>` to `Arc<RwLock<EmbedderStatus>>` (shared with IpcState). Change `new()` to take `status: Arc<RwLock<EmbedderStatus>>` as a 4th parameter. In `record_failure`: when the circuit opens (failures >= threshold), set the shared status to `EmbedderStatus::Fallback`. In `record_success`: set the shared status to `EmbedderStatus::Ready`. Remove the now-redundant `set_status` method (the status is updated internally by the circuit breaker; the probe writes to the same shared Arc via main.rs). Keep `status()` as a read-only accessor. In `src/provider/client_factory.rs`: change `build_embedder(config: &Config)` to `build_embedder(config: &Config, status: Arc<RwLock<EmbedderStatus>>)` — pass the Arc into `RemoteEmbedder::new`; for the HashEmbedder path, set `*status.write() = EmbedderStatus::Ready` before returning. Update imports (`EmbedderStatus`, `RwLock`). In `src-tauri/src/main.rs`: create `let embedder_status = Arc::new(RwLock::new(EmbedderStatus::Checking));` BEFORE `build_embedder`, pass `embedder_status.clone()` to `build_embedder`, and use the same Arc in IpcState's `embedder_status` field (all 3 construction sites — for the fallback sites that don't build a brain, use a fresh `Ready` Arc). In `src-tauri/src/ipc/rewire.rs`: pass `state.runtime.embedder_status.clone()` to `build_embedder`. Update the 2 `build_embedder` tests in `client_factory.rs` to pass a fresh `Arc::new(RwLock::new(EmbedderStatus::Checking))`. Run `cargo test`.
- [x] 3. **C2 (frontend): Add embedder status poll interval** — In `frontend/src/App.tsx`: add a `useEffect` (mirroring the git-branch poll at :263) that polls `getEmbedderStatus()` every 10s and calls `setEmbedderStatus`. This catches post-startup circuit-breaker transitions (the event subscription only catches the startup probe emit). Clean up the interval on unmount.
- [x] 4. **B1: Subscribe before fetch (race fix)** — In `frontend/src/App.tsx` (the startup IIFE ~:246) and `frontend/src/components/layout/StatusBar.tsx` (~:58): reorder so `onEmbedderStatus(...)` subscription is set up BEFORE the `getEmbedderStatus()` fetch. The fetch fills the current value; the listener catches future emits — no gap where an emit is missed.
- [x] 5. **B2: Add non-empty validation for embedding_model** — In `src-tauri/src/ipc/settings.rs` (the embedding_model validation block ~:1020): add `if em.endpoint.trim().is_empty() { return Err("embedding_model.endpoint must not be empty".into()); }` and `if em.model.trim().is_empty() { return Err("embedding_model.model must not be empty".into()); }` inside the `!patch.clear_embedding_model` guard, mirroring the vision_model validation at :973-976. Add a test `save_settings_rejects_empty_embedding_model` (deserialize a patch with empty endpoint, verify the validation would reject — since validation is in the command not the DTO, test the DTO shape + document). Run `cargo test -p myharness-app`.
- [x] 6. **B3: Add CREATE_NO_WINDOW to ollama spawns on Windows** — In `src/memory/embedder_probe.rs`: for both `Command::new("ollama")` spawns (the `ollama serve` at ~:76 and the `ollama pull` at ~:96), add `#[cfg(windows)] { use std::os::windows::process::CommandExt; cmd.creation_flags(0x08000000); }` (CREATE_NO_WINDOW = 0x08000000) before `.spawn()`. Use a local `let mut cmd = Command::new(...)` binding so the cfg block can mutate it. This prevents a flashing console window on Windows when the Tauri GUI app spawns the ollama subprocess. Run `cargo test --lib`.
- [x] 7. **Re-run all test suites + verify green** — Run `cargo test --lib` (check $LASTEXITCODE, all green, zero warnings), `cargo test -p myharness-app` (all green), `cd frontend; npx tsc --noEmit` (clean), `cd frontend; npx vitest run` (all green). Fix any failures before the parent plan's commit step.
