# Plan: git branch list: allow the read-only --show-current flag (backlog 41cd5ad0)

## Goal
Allow the read-only `--show-current` flag (and the equally read-only `--points-at=` filter) through the git tool's branch-list allowlist, with the doc comment and rejection message kept in sync, the refusal path still pinned, and the HOW knowledge record (fc736d96) amended after landing.

## Kind
bug_fixing

## Context
Backlog 41cd5ad0 (user report, live 2026-09-19 during plan 995436c2's closing sequence): `git branch --show-current` refused by the branch-list allowlist. Anchors (src/tool/agent/git.rs): validate_branch_list_args (:278-318) — safe_exact (:279-297) + safe_prefix (:298, ["--format=", "--sort=", "--color="]); the doc comment's Allowed list (:274-277); the rejection message's flag enumeration (:310-313); branch_list_argv (:324-329) builds ["branch"] + validated args; the schema's args description (:503) says "branch list: read-only listing flags only (positionals refused — they create branches)" with NO flag enumeration (no sync needed there, nor in the module doc :26). Tests: branch_list_allowlist_rejects_positionals_and_write_flags (:1436-1461 — positional + -d/-m/--force/--set-upstream-to= refusals), branch_list_allowlist_accepts_read_only_flags (:1463-1496 — the accept list + prefix matches), branch_list_argv_builds_correct_argv (:1498+). AUDIT (fix direction 2): --show-current is exactly as read-only as --list (prints the checked-out branch's name) — ADD as exact. --points-at=<object> is a read-only listing filter — ADD as prefix (the = form keeps the value off the positional path; the bare `--points-at HEAD` two-arg form stays refused by the existing positional check, same as --merged's value form today). Staying excluded, documented: --column/--no-column and --omit-empty (display-only formatting, no listing value beyond defaults — keep the surface minimal), -l (deprecated synonym of --list); all mutating flags (-d/-D/-m/-M/-c/-C/-f/--set-upstream-to=/--unset-upstream/--edit-description) stay refused — pinned by the existing refusal test. Verify: cargo test green + warning-free (#![deny(warnings)]).

## Steps
- [x] 1. **Reproduce with failing regression test** — Write a regression test that reproduces the defect (constitution: every defect gets a regression test that fails without the fix and passes with it). Run it and confirm it FAILS.
- [x] 2. **Document root cause** — Investigate and document the root cause. memory_write a BUG: record (symptom → root cause → fix + regression test name, ≤600 chars).
- [x] 3. **Minimal fix** — Apply the minimal fix that makes the regression test pass. Do not refactor unrelated code.
- [x] 4. **Verify** — Run the regression test + the full test suite (cargo test unpiped, warning-free). Record the regression test name via update_plan (regression_test field) — finish is blocked without it. If the fix turned out FEATURE-SCALE (new pub types or cross-module surface): amend the plan context via update_plan append with a paragraph starting "Landed design —" (architecture, deviations from the sketch, measured numbers, invariants to preserve) and record landed_design=true BEFORE completing this step; at finish write a SPEC memory and amend the BUG record with the landed outcome (agent.md 'Documentation expectations'; the finish gate blocks landed_design without the context amendment).

## Detailed steps
- [x] **Allowlist + message + doc sync** — In src/tool/agent/git.rs validate_branch_list_args: add "--show-current" to safe_exact (:279-297) and "--points-at=" to safe_prefix (:298); extend the doc comment's Allowed list (:274-277) with `--show-current` and `--points-at=…` plus a line documenting the audited exclusions (--column/--no-column, --omit-empty — display-only; -l — deprecated synonym of --list; all mutating flags stay refused); extend the rejection message's flag enumeration (:310-313) with --show-current and --points-at= so the message stays in sync with the allowlist.
- [x] **Regression tests** — In src/tool/agent/git.rs tests: extend branch_list_allowlist_accepts_read_only_flags (:1463-1496) with "--show-current" in the exact-flag loop and an assert that --points-at=HEAD passes (prefix form); extend branch_list_argv_builds_correct_argv (:1498+) with branch_list_argv(["--show-current"]) == ["branch", "--show-current"] (the flag reaches git); add an assertion that the refusal message for an unknown flag names --show-current (message/allowlist sync); the existing branch_list_allowlist_rejects_positionals_and_write_flags (:1436-1461) already pins the mutating path — verify unchanged.
- [x] **Verify + bookkeeping** — cargo test green + warning-free (#![deny(warnings)]); close backlog item 41cd5ad0 (backlog_status done + note naming this plan, the two added flags, and the documented exclusions); amend the HOW knowledge record fc736d96 ("git tool per-subcommand parameter reference" — find its memory id via memory_search) with the branch-list allowlist now including --show-current and --points-at=.

## Bug
`git branch --show-current` — a purely read-only flag that prints the current branch's name — is refused by the git tool's branch-list allowlist: "flag '--show-current' is not allowed for `git branch list` — only read-only listing flags are accepted: -v/-vv/--verbose, -a/--all, -r/--remotes, --list, --format=, --sort=, --color=/--no-color, --merged/--no-merged/--contains/--no-contains, -i/--ignore-case". Reproduce: git tool with subcommand=branch, action=list, args=["--show-current"] → refused (live 2026-09-19, plan 995436c2's closing sequence — the agent fell back to `git branch -v` and parsed the `*` marker just to learn the current branch).
