# Plan: Rewrite README as a marketing front door + move deep detail to docs/

## Goal
Turn README.md from a 60 KB wall of implementation minutiae (157 physical lines, single-line bullets up to ~8 KB) into an exciting, scannable marketing README that explains the key features, how the repository is organized, and how to clone & build on Windows and macOS. Preserve every byte of the deep feature/config detail by moving it into docs/FEATURES.md and docs/CONFIGURATION.md (user-chosen split, 2027-01-13), so nothing is lost while the front door gets fast.

## Kind
implementation

## Context
USER DECISION (asked 2027-01-13, answered): "Split into docs/ (recommended)" — slim marketing README + docs/FEATURES.md + docs/CONFIGURATION.md holding today's detail nearly verbatim. NOT a hard trim, NOT <details> collapsing. Also requested: "exciting", "explain the key features", "how the repository works", "clone & build instructions".

VERIFIED FACTS (from README.md + Cargo.toml + package.json + .cargo/config.toml + .github/workflows, 2027-01-13):
- Product: Mnemo (crate/package name `mnemo`, app binary `mnemo-app`), MIT, Copyright (c) 2026 Carsten Hess. "Named after Mnemosyne, the Greek titaness of memory." Existing tagline (keep, it's good): "An agentic coding harness that remembers."
- Stack: Rust core lib (`src/`, crate `mnemo`) + Tauri 2 desktop shell (`src-tauri/`, `mnemo-app`) + React 18/TS/Vite/Tailwind frontend (`frontend/`, vitest 4).
- Build: root is an npm workspace (root package.json workspaces: ["frontend"]); Tauri CLI hoisted to root node_modules and MUST run with src-tauri as cwd. Launcher `scripts/start.mjs` (cross-platform, `npm start`) modes: plain (build --no-bundle + launch), `dev` (Vite :5179 + native window), `build` (binary only), `bundle` (installers). Windows conveniences start.bat/build.bat exist; start.bat taskkills a running mnemo-app.exe first (exe lock). Manual path: `npm install`, then `cd src-tauri && npx tauri dev | npx tauri build`.
- Prereqs Windows: Rust stable (rustup), Node LTS, VS Build Tools with "Desktop development with C++", WebView2 Runtime (preinstalled on Win10/11). macOS 11+: Xcode CLT, Rust stable, Node LTS. `.cargo/config.toml` sets rust-lld for x86_64-pc-windows-msvc only.
- Tests: `cargo test` from repo root (workspace lib + app), `npm test` in frontend/, plus `npx tsc --noEmit` (vitest hides type defects). New frontend test files MUST be registered in frontend/vitest.config.ts `test.include` (guarded by src/lib/vitestInclude.test.ts).
- Library cargo features (off by default, app always selects both): `browser` (chromiumoxide headless browser stack), `embeddings` (fastembed/ONNX), plus dev-only `test-support`.
- CI: .github/workflows/build.yml tests+builds BOTH windows and macos. Windows-only by design: embedded WebView2 Browser tab + game_* on-screen tools (offscreen_browser_* works everywhere).
- Repo anatomy for the "how the repository works" section: src/ = core lib (agent/, app/, browser/, codegraph/, config/, mcp/, memory/, project/, provider/, runtime/, safety_rules/, skill/, tool/, workflow/, model_resolver.rs, backlog.rs, webview_args.rs); src-tauri/src = console.rs, main.rs, startup.rs, watchdog.rs, ipc/ + src-tauri/tests/ (incl. vendored-patch source guards wry_sso_patch.rs, wry_hard_reload_patch.rs, tao_backport.rs); frontend/ = React UI; tests/integration/; scripts/ (start.mjs + copyright/relicense ps1); vendor/ (patched tao 0.35.4 + wry 0.55.3 with PATCHES.md, wired via [patch.crates-io]); assets/workflow.svg (ONLY asset — no screenshots exist, do NOT invent one); agent.md (project constitution), PLAN.md (technical decisions, 93 KB); .coding/ side-car (plans/, reviews/, knowledge/, backlog.jsonl git-union-merged, skills/ — tracked; memory.db + codegraph.db = gitignored rebuildable caches); .config/nextest.toml, debug.config.toml, .cargo/config.toml.
- Proof numbers currently in README (must be RE-VERIFIED, not copied): "As of January 2027: 1,303 commits, 608 plans, 781 review reports, 440 knowledge records, ≈2,450 Rust tests, ≈1,070 frontend tests ... first 23 days shipped ~160,000 lines of production Rust + TypeScript".

CURRENT README STRUCTURE (line numbers, 157 lines total): 1-8 hero/tagline/badge; 9-28 "Why Mnemo exists: highly efficient spec driven iterative development" (strong copy — 5 pain bullets + the "not a model problem — an allocation problem" conviction + 4 spec-loop bullets + the self-hosting proof paragraph); 30-41 "Key ideas"; 42-97 "Key features" (Workflow & safety / Memory / Code intelligence / Agents & interface — lines 66 and 67 are single ~8 KB bullets of minutiae); 98-109 "The enforced workflow" (workflow.svg + 4 numbered states + a closing paragraph); 111-139 "Building" (Windows/macOS prereqs + npm start modes + manual sequence + test commands + library-feature notes); 141-151 "Configuration" (config.toml, endpoints.toml, `[models]` slots/effort, stop_boundary_strings/stop_token_ids, MCP, per-project .coding/ state); 153-157 License.

CONTENT SPLIT (target):
- README.md (new, ~200 lines): H1 + tagline + static shields badges (license MIT / Windows+macOS / Rust stable / Node LTS — static only, no upkeep-fragile number badges) → what it is (3-4 sentences) → Why Mnemo exists (tightened from lines 11-21, keep the pain bullets + allocation-problem line) → The conviction/spec-loop (tightened 23-26) → Key features highlights (6 grouped groups, ONE line per bullet, no implementation minutiae) → The enforced workflow (workflow.svg + 4 states + the "rails make the smaller executor viable" line) → How the repository works (anatomy tree + .coding/ side-car + vendor patches + CI both platforms) → Quickstart: clone & build (Windows + macOS prereqs, npm start modes, manual npx tauri sequence) → Development & tests (cargo test, cargo test -p mnemo-app, tsc --noEmit, vitest, library features) → Where the detail lives (docs map: docs/FEATURES.md, docs/CONFIGURATION.md, PLAN.md, agent.md, vendor/*/PATCHES.md, .coding/) → proof paragraph with DATED verified numbers → License.
- docs/FEATURES.md = README lines 30-109 (Key ideas + Key features + The enforced workflow) byte-exact, plus new H1 + intro + pointer links.
- docs/CONFIGURATION.md = README lines 141-151 byte-exact, plus new H1 + intro + pointer links.

TECHNIQUE (chosen deliberately, must be followed): to preserve the giant single-line bullets byte-exactly, create each docs file by COPYING README.md (`Copy-Item README.md docs/FEATURES.md` — a byte-exact copy, not content authoring) and then trimming with file_edit line-range deletes; ALL content changes go through file tools. Do NOT retype lines 66/67 (8 KB each — transcription drift risk). Justification if the reviewer flags the Copy-Item: it is a copy of already-authored text, the sanctioned file tools perform every subsequent content change, and byte-exactness is the requirement the user chose ("nothing is lost").

RISKS / GOTCHAS: (a) a new docs/ dir does not exist yet — create it via the copy target; (b) after Copy-Item the docs file is unread, so the stale-read gate may demand a read before file_edit — do a targeted start_line/max_lines read, not a full 60 KB read; (c) never rewrite historical records in .coding/plans/ or .coding/reviews/ that mention README.md:NN line numbers — only LIVE docs/comments get fixed; (d) Windows file tools can't read outside the project root (~/.mnemo/*), so config facts come from README/PLAN.md only; (e) the README's existing license badge `![License: MIT](https://opensource.org/MIT)` is not an image URL — replace with a real shields.io static badge.

VERIFICATION COMMANDS (project rules: unpiped, read $LASTEXITCODE; PowerShell syntax): `cargo test` and `cargo test -p mnemo-app` from repo root; `npx tsc --noEmit` and `npm test` in frontend/; proof numbers: `git rev-list --count HEAD`, counts of .coding/plans/*.md, .coding/reviews/*.md, .coding/knowledge/**/*.md.

FACT SHEET (step 1, verified 2027-01-13 from source reads — a restarted session must NOT re-derive these):
- Build/run: `npm install` at root (npm workspace, root package.json workspaces=["frontend"]); Tauri CLI hoisted to ROOT node_modules/.bin and must run with src-tauri as cwd (build.bat comment). Launcher scripts/start.mjs = `npm start` [syncs npm deps, tauri build --no-bundle, launches target/release/mnemo-app.exe]; `npm start dev` [Vite :5179 + native window]; `npm start build` [binary only]; `npm start bundle` [installers msi/nsis under target/release/bundle]. start.bat taskkills mnemo-app.exe first (running instance locks the exe). Manual: cd src-tauri; npx tauri dev / npx tauri build.
- Prereqs: Windows = Rust stable (rustup), Node LTS, VS Build Tools + "Desktop development with C++", WebView2 Runtime (preinstalled Win10/11). macOS 11+ = xcode-select --install, Rust stable, Node LTS. CI (.github/workflows/build.yml) runs BOTH windows + macos jobs.
- Tests: `cargo test` from root (workspace: lib mnemo + app mnemo-app) + `cargo test -p mnemo-app`; frontend: `npx tsc --noEmit` + `npm test` (vitest run) in frontend/; new frontend test files must be registered in frontend/vitest.config.ts test.include (guarded by src/lib/vitestInclude.test.ts). nextest config at .config/nextest.toml.
- Cargo: workspace members=["src-tauri"], exclude=["vendor"], resolver=2. Features: browser=chromiumoxide (headless offscreen_browser_*), embeddings=fastembed ONNX (MiniLM/BGE/nomic download on first use), test-support (dev only, never release); both heavy features OFF for the lib by default, src-tauri always selects them. Profiles: dev/test use debug="line-tables-only", [profile.*.package."*"] debug=false. .cargo/config.toml: linker=rust-lld for x86_64-pc-windows-msvc ONLY.
- Vendored patches ([patch.crates-io] tao/wry, vendor/ excluded from workspace): vendor/tao 0.35.4 = 0.35.3 + PR #1215 backport (Windows keyboard/IME self-deadlock, AppHangB1); vendor/wry 0.55.3 = 0.55.1 + two patches (OS-account SSO for the Browser tab; CDP Page.reload ignoreCache hard reload). Each documented in vendor/*/PATCHES.md, guarded by src-tauri/tests source-contract tests (tao_backport.rs, wry_sso_patch.rs, wry_hard_reload_patch.rs).
- STALE CLAIM SPOTTED: old README line 64 says the knowledge graph covers "(Rust + TypeScript/TSX)" — Cargo.toml actually carries 12 tree-sitter grammars (rust, typescript, javascript, python, go, java, c, cpp, c-sharp, ruby, php, html). New README says 12 languages; docs/FEATURES.md keeps the old text byte-exact (user chose verbatim preserve); discrepancy named in the final summary.
- Anatomy (verified listings): src/ = agent/ (approval, context, dispatch, factory, loop_impl, prompt, steering_stats, turn), app/, browser/, codegraph/ (extract, query, schema, store, walk, watcher), config/, error.rs, lib.rs, mcp/, memory/ (consolidation, embedder, knowledge, indexer, maintenance, schema, strength, types), model_resolver.rs, backlog.rs, project/, provider/, runtime/, safety_rules/, skill/, thread_util.rs, tool/ (agent/, browser/, memory/, workflow/, mod.rs, steering.rs), webview_args.rs, workflow/. src-tauri/src/ = main.rs, console.rs (headless REPL mode), startup.rs, watchdog.rs (hang evidence reports), ipc/. frontend/src/ = App.tsx, components/, hooks/, lib/, styles/. tests/integration/ (root), scripts/ (start.mjs, add-copyright-headers.ps1, relicense-to-mit.ps1), assets/workflow.svg (only asset — no screenshots exist), agent.md (constitution), PLAN.md (technical decisions).

## Steps
- [x] 1. **Audit the remaining source material + record the fact sheet** — Read README.md lines 70-98 (the unread tail of the "Agents & interface" bullets) and Cargo.toml lines 60-186 (workspace members, [patch.crates-io] tao/wry wiring, [profile.dev.package."*"] debug=false, feature blocks). Also list src/tool, src/memory, src/agent, src/codegraph and frontend/src (top level) to name the anatomy section truthfully. Then append a "Fact sheet" paragraph to this plan via update_plan append=true recording: exact build/run/test commands, prerequisites per platform, launcher modes, CI matrix, and the anatomy tree — so a restarted session never re-derives them.
- [x] 2. **Create docs/FEATURES.md by copy-then-trim** — Shell: `New-Item -ItemType Directory -Force docs` then `Copy-Item README.md docs/FEATURES.md` (byte-exact copy of already-authored text; all content changes after this go through file_edit — state this justification in the commit message if the reviewer asks). Read a targeted slice of docs/FEATURES.md (e.g. start_line 105, max_lines 60) so the stale-read gate is satisfied WITHOUT re-reading 60 KB. Then file_edit line-range-delete lines 111-157 (Building/Configuration/License leave the file) and lines 1-29 (hero + Why — those stay in the README), and prepend: `# Mnemo — feature reference`, one intro paragraph ("the exhaustive detail behind the README's highlights; the README is the front door"), and links to ./CONFIGURATION.md, ../README.md, ../PLAN.md. Verify nothing was lost: after the trim the file must still contain the 'Key ideas', 'Key features' (incl. the 8 KB bullets at old lines 66-67) and 'The enforced workflow' sections — grep their opening strings ('- **The plan is the tool surface', 'Tree-sitter knowledge graph', '## The enforced workflow') to confirm.
- [x] 3. **Create docs/CONFIGURATION.md by copy-then-trim** — Copy-Item README.md docs/CONFIGURATION.md (same byte-exact technique as step 2), read a targeted slice to satisfy the stale-read gate, then file_edit line-range-delete lines 1-140 and 153-157 so ONLY the old '## Configuration' section survives. Retitle it as `# Mnemo — configuration reference` (H1) with a 2-3 line intro linking back to ../README.md and ./FEATURES.md, keeping the section's five paragraphs byte-exact (config.toml + [context] + [models] routing/effort + endpoints.toml stop_boundary_strings/stop_token_ids + MCP two-scope + per-project .coding/ state). Verify: the strings `~/.mnemo/`, `endpoints.toml`, `mcp.toml` and `stop_boundary_strings` all still appear.
- [x] 4. **Write the new marketing README.md** — file_write README.md (full rewrite) in this exact order: (1) H1 `# Mnemo` + the tagline blockquote "An agentic coding harness that remembers." + Mnemosyne line + STATIC shields.io badges only (license-MIT, platforms-Windows/macOS, rust-stable, node-LTS) — never the broken `![License: MIT](https://opensource.org/MIT)`; (2) "What it is" — 3-4 sentences: Rust + Tauri desktop coding agent, enforced plan-first state machine, four-tier persistent memory, tree-sitter knowledge graph, approval gates on mutation; (3) "Why Mnemo exists" — tighten old lines 11-21: keep the 5 pain bullets, the "Not a model problem — an allocation problem." line and the spec-loop conviction + its 4 bullets; (4) "Highlights" — grouped one-liners (The workflow / Memory / Code intelligence / Safety / Multi-model routing / Agents & UI), scannable, NO implementation minutiae (that lives in docs/FEATURES.md, link it); (5) "The enforced workflow" — assets/workflow.svg image + the 4 numbered states (Planning/Executing/Reviewing/Complete) + the closing paragraph's key line that a tool-schema guardrail is what makes a smaller executor model viable; (6) "How the repository works" — a fenced tree of src/ (name the real modules: agent, app, browser, codegraph, config, mcp, memory, project, provider, runtime, safety_rules, skill, tool, workflow), src-tauri/ (+ its tests/ source guards), frontend/, tests/integration/, scripts/, vendor/ (patched tao/wry + PATCHES.md + [patch.crates-io]), assets/, agent.md, PLAN.md, .coding/ (plans/reviews/knowledge/backlog/skills tracked; memory.db + codegraph.db gitignored rebuildable caches), .github/workflows/build.yml; explicitly explain the lib-vs-app split (headless `mnemo` lib, `mnemo-app` shell) and that the app always selects the browser+embeddings features; (7) "Quickstart — clone & build": placeholders resolved in step 5, then Windows prereqs (rustup stable, Node LTS, VS Build Tools + Desktop development with C++, WebView2), macOS 11+ prereqs (xcode-select --install, Rust, Node), `npm install`, the launcher `npm start` / `npm start dev` / `npm start build` / `npm start bundle`, and the manual equivalent (`cd src-tauri` then `npx tauri dev` / `npx tauri build`, noting the Tauri CLI lives in the ROOT node_modules so it must run with src-tauri as cwd); (8) "Development & tests" — cargo test, cargo test -p mnemo-app, npx tsc --noEmit + npm test in frontend/, the vitest.config.ts include-list rule, and the light-build note for the two optional cargo features; (9) "Where the detail lives" — docs map; (10) proof paragraph: self-hosting (written by Mnemo) + the dated verified numbers (filled in step 6); (11) MIT License + Copyright (c) 2026 Carsten Hess. Every claim must be traceable to code/docs; invent no screenshots, no benchmarks, no badges with numbers.
- [x] 5. **Verify facts, links, and cross-references** — PowerShell (unpiped, check $LASTEXITCODE): recompute proof inputs — `git rev-list --count HEAD`, counts of .coding/plans/*.md, .coding/reviews/*.md, .coding/knowledge/**/*.md, `Get-Date` for the as-of date. Check every relative link in the new README resolves to a real path (assets/workflow.svg, LICENSE, docs/FEATURES.md, docs/CONFIGURATION.md, PLAN.md, agent.md, vendor/wry/PATCHES.md, .github/workflows/build.yml). Search pattern `README` across globs `**/*.md` and `**/*.{rs,ts,tsx}` (EXCLUDING .coding/plans/** and .coding/reviews/** — historical records are never rewritten) and fix any LIVE pointer or line-number reference broken by the rewrite (PLAN.md and code comments are the likely candidates). Resolve the clone URL: search `github.com` across `*.md`, `*.yml`, `Cargo.toml`, `package.json`; if the repo has no discoverable remote URL, leave an obvious `https://github.com/<owner>/mnemo` placeholder in both clone snippets and list it as an open item in the final summary instead of inventing a real URL.
- [x] 6. **Run the full test matrix and stamp the README numbers** — From the repo root: `cargo test` then `cargo test -p mnemo-app`, each unpiped, reading $LASTEXITCODE and the `test result:` lines (a piped exit code is not evidence — see agent.md). In frontend/: `npx tsc --noEmit` then `npm test`. All four must be green (this is a docs-only change, so a failure means the change broke something else — investigate, do not paper over). Capture the Rust test total and the vitest total, then file_edit the README's proof paragraph to the measured numbers with an explicit as-of date.
- [x] 7. **Review** — spawn_agent role:"reviewer", name "readme-review", OMITTING the model param (the configured reviewing model is authoritative). Task must be self-contained: plan goal = marketing README rewrite + byte-exact move of detail into docs/FEATURES.md and docs/CONFIGURATION.md; changed files = README.md, docs/FEATURES.md, docs/CONFIGURATION.md, plus any cross-reference fixes; ask for (a) accuracy of every build/test/prereq claim against the actual repo (scripts/start.mjs, build.bat, .github/workflows/build.yml, Cargo.toml features), (b) content-loss check: is anything from the pre-rewrite README missing across the new README + docs/ (use git_diff to see the old text), (c) link integrity, (d) multi-platform neutrality (no Windows-only assumption presented as universal beyond the sanctioned WebView2 surfaces), (e) factual accuracy of the proof numbers. Report must open `## Verdict: PASS` or `## Verdict: FINDINGS (n high, n low)`. Do not author the review myself.
- [x] 8. **Fix findings, re-test, commit** — Fix EVERY reviewer finding (the only permitted skip is a factually wrong finding, justified in writing). Re-run the test matrix from step 6 if any non-doc file changed. Then commit README.md, docs/FEATURES.md, docs/CONFIGURATION.md and any cross-reference fixes to the CURRENT working branch (wt/* — never main), including the reviewer's report, and finish the plan. Final summary must be <=3 short lines (what changed, how to use it, test result) and must name any open item (e.g. a placeholder clone URL).
- [x] 9. **Audit the remaining source material + record the fact sheet** — DONE — reads of README.md:70-97, Cargo.toml:60-186, and directory listings of src/tool, src/memory, src/agent, src/codegraph, frontend/src are complete; the fact sheet above (this paragraph) records everything a restart needs. Key new facts beyond the plan context: workspace members=["src-tauri"] with vendor excluded; 12 tree-sitter grammars (old README's "Rust + TypeScript/TSX" is stale); vendored tao 0.35.4 + wry 0.55.3 patches; src/agent, src/memory, src/codegraph, src/tool module names for the anatomy tree.
