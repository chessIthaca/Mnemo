# Plan: Revert bug_fixing skeleton step 3 to direct minimal fix (no sub-plan)

## Goal
Revert the bug_fixing skeleton's step 3 from "Minimal fix via sub-plan" back to a direct minimal-fix step, per user DECISION 544111c5 (fixes execute directly in the bug plan). A bug_fixing sub-plan can only itself be bug_fixing (nested bug-fixing is invalid), so the sub-plan step just re-copied the skeleton's own 4 steps into the child — a plan-loop generator (user aborted plan b840f00c over this, 2026-09-18).

## Kind
implementation

## Context


## Steps
- [x] 1. **Rewrite skeleton step 3 + doc comment in plan.rs (direct fix, no sub-plan) — restore the pre-1ef2bfa3 wording. In src/tool/workflow/plan.rs edit const BUG_FIXING_SKELETON (lines 37-53): replace step 3 (lines 44-49, the "**Minimal fix via sub-plan** — Push a sub-plan..." text) with: "**Minimal fix** — Apply the minimal fix that makes the regression test pass. Do not refactor unrelated code." Keep the existing \ line-continuation style used by the other array entries. Also update the const's doc comment (lines 32-36): change "minimal fix via sub-plan" → "minimal fix" so the one-line description matches the new step. Leave steps 0/1/3 untouched. This is the core change; the test pin in step 2 is the regression test that fails without it and passes with it. Do NOT touch src/agent/prompt.rs:75 or README.md — they use the arrow shorthand "reproduce→root-cause→fix→verify" which stays accurate (the fix phase still exists, only its mechanism reverted to direct).**
- [x] 2. **Retarget the step-2 skeleton pin test (the regression test) — In src/tool/workflow/plan.rs test create_plan_bug_fixing_forces_skeleton_and_persists_symptom (line 1686), update the assertion at lines 1712-1717 from checking plan.steps[2].text.contains("sub-plan") && contains("LEAST amount of change") to checking the new direct-fix wording: plan.steps[2].text.contains("Minimal fix") && plan.steps[2].text.contains("regression test pass"), with failure message "fix step is a direct minimal fix: {}". This mirrors the existing contains()-style pins on steps 0 and 3. Do NOT touch the two append-refused tests (plan.rs:2731 update_plan_tool_append_refused_for_bug_fixing_skeleton and src/workflow/mod.rs:1961 update_plan_append_refused_for_bug_fixing_skeleton) — both assert on the "locked 4-step skeleton" error message, not step wording, so they stay green unchanged. Do NOT touch tests/workflow_integration.rs (it only pins step 0).**
- [x] 3. **Sync PLAN.md bug_fixing bullet (restore direct-fix description) — In PLAN.md (~lines 251-256) the bug_fixing bullet currently reads "document root cause → minimal fix **via sub-plan** (the fix step pushes a sub-plan whose steps implement the fix with the least amount of change possible — written self-contained so a lesser model can execute it) → verify". Restore the direct-fix description: "document root cause → minimal fix → verify" (drop the "via sub-plan" clause and its parenthetical). Keep the rest of the bullet (locked skeleton, update_plan refuses steps replacement AND append, bug:<symptom> required, verify records regression_test name, finish validates + auto-captures PLAN:/BUG: digests + supersedes crash markers) unchanged.**
- [x] 4. **Supersede the knowledge decision record + DECISION memory with the reversal — (a) Rewrite the on-disk knowledge file .coding/knowledge/decision/2026-08-28-bug-fixing-skeleton-fix-step-pushes-a-minimal-ch.md to state the CURRENT decision: the bug_fixing skeleton's step 3 applies the minimal fix DIRECTLY (not via sub-plan), per user DECISION 544111c5 (2026-09-18). Note it REVERSES the 2026-02-13 sub-plan decision (plan 1ef2bfa3, commit f46076e): a bug_fixing sub-plan can only itself be bug_fixing (nested bug-fixing is invalid), so the sub-plan step just re-copied the skeleton's 4 steps into the child — a plan-loop generator (user aborted plan b840f00c over this). Keep the frontmatter title updated to reflect the direct-fix decision. (b) Supersede DECISION memory 8d705813 ("bug_fixing skeleton fix step pushes a minimal-change sub-plan") via memory_supersede with the reversal: fixes execute directly in the bug plan, citing DECISION 544111c5 as the authoritative rationale; the old sub-plan decision is now history.**
- [x] 5. **Verify (cargo test unpiped) + reviewer + fix + commit + finish — Run cargo test unpiped in PowerShell; confirm every "test result:" line is ok and $LASTEXITCODE is 0 (crate is #![deny(warnings)] so green = zero warnings; fix root causes, never #[allow]). Then spawn a read-only reviewer subagent (spawn_agent role:"reviewer") over ALL uncommitted changes with a self-contained task: plan goal (revert bug_fixing skeleton step 3 to a direct minimal fix, no sub-plan), changed files (src/tool/workflow/plan.rs, PLAN.md, .coding/knowledge/decision/2026-08-28-...md), focus on correctness of the new step text + doc-comment sync, the retargeted test genuinely pins the new wording (fails on revert), doc-sync (PLAN.md matches the const), no stale "sub-plan" references left in the skeleton/doc-comment, no #[allow], multi-platform neutrality. Have it write its report to .coding/reviews/. End the turn after spawning — resume on the background-agent-finished notification. Fix EVERY finding, re-run cargo test, commit to the working branch (never main) with the review report included, then finish(report path).**
