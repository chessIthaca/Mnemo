# Plan: Show fetched URL on web_fetch tool cards

## Goal
Show the fetched URL in the web_fetch ToolCard header in the main-window chat transcript, following the browser_navigate pattern (label helper in toolCardPaths.ts + unit tests, dispatched from argLabel in Message.tsx).

## Kind
bug_fixing

## Context
ToolCard (frontend/src/components/chat/Message.tsx:631) builds header chips from buildPathChips + argLabel. argLabel has per-tool label cases (search pattern, git_read op, read_files names, graph/memory/browser via helpers in frontend/src/lib/toolCardPaths.ts) but nothing for web_fetch, whose args {url, max_length} carry no path/file fields — so buildPathChips returns [] and argLabel returns null. Precedent: browserArgLabel (toolCardPaths.ts:399) shows browser_navigate's url (user report 2026-09-09). Label helpers live in toolCardPaths.ts (React-free, node-env unit tests in toolCardPaths.test.ts) and are dispatched from argLabel. web_fetch tool itself: src/tool/agent/web_fetch.rs (no change needed — display-only fix).

## Steps
- [x] 1. **Reproduce with failing regression test** — Write a regression test that reproduces the defect (constitution: every defect gets a regression test that fails without the fix and passes with it). Run it and confirm it FAILS.
- [x] 2. **Document root cause** — Investigate and document the root cause. memory_write a BUG: record (symptom → root cause → fix + regression test name, ≤600 chars).
- [x] 3. **Minimal fix** — Apply the minimal fix that makes the regression test pass. Do not refactor unrelated code.
- [x] 4. **Verify** — Run the regression test + the full test suite (cargo test unpiped, warning-free). Record the regression test name via update_plan (regression_test field) — finish is blocked without it.

## Bug
web_fetch tool cards in the main-window chat transcript show a bare "web_fetch" header with no indication of what was fetched — the URL is only visible after expanding the card's raw args (backlog 69cf7e9c-1d71-46be-aec5-0cf518a4d1db). Every other targeted tool (search shows its pattern, git_read its op, browser_navigate its url) surfaces its key argument in the collapsed header.

## Regression test
webFetchLabel
