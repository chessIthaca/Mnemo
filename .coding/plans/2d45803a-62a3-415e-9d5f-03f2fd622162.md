# Plan: Commit trace-freeze fix on fix/trace-freeze branch

## Goal
Commit the verified trace-freeze fix on a feature branch (fix/trace-freeze), with the plan's review + closing sequence, without touching main.

## Kind
implementation

## Context
The trace lock-inversion freeze fix (src/provider/trace.rs scoped path clones + regression test, src-tauri/src/ipc/trace.rs 3 sync commands → async) is verified green (1411 + 144 tests) but uncommitted. User previously said "don't commit yet" (DECISION memory f1eb5c67); now explicitly asked to commit. Repo convention: never commit to main — feature branch; the closing sequence commits after review. Dirty tree today: M .coding/plans/stack.json, M src-tauri/src/ipc/trace.rs, M src/provider/trace.rs, ?? .coding/plans/308b8490-*.md, ?? .coding/plans/e2ba07d8-*.md.

## Steps
- [x] 1. **Create feature branch fix/trace-freeze** — Run `git checkout -b fix/trace-freeze` from main (the dirty fix files carry along). Then `git status --short` — expect: M src/provider/trace.rs, M src-tauri/src/ipc/trace.rs, M .coding/plans/stack.json, ?? the two plan .md files.
- [x] 2. **Supersede the don't-commit DECISION memory** — memory_supersede f1eb5c67-77b3-433d-bbd2-de3db8658cfd (DECISION 'Don't commit yet') with successor: 'DECISION (user, 2026-08-22): trace-freeze fix now committed per user request on branch fix/trace-freeze; tests green (1411+144).'
- [x] 3. **Verify tests green on the branch** — Re-run the two suites unpiped and read the result lines + $LASTEXITCODE: root `cargo test` (expect 1411 passed) and `cd src-tauri; cargo test` (expect 144 passed). Warning-free (deny(warnings)).
- [x] 4. **Stage the two fix files** — Stage the fix: `git add src/provider/trace.rs src-tauri/src/ipc/trace.rs` — NOT the .coding bookkeeping yet (it gets its own chore commit at the end). Confirm `git diff --cached --stat` shows exactly those two files.
