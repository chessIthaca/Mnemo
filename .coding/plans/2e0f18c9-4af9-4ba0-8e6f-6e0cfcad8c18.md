# Plan: Fix per-workflow-state model overrides: surface effective model + add Reviewing slot

## Goal
Make the per-workflow-step model overrides actually visible and complete: report the effective per-turn model to the UI (AgentInfo.model + StatusBar), and add the missing Reviewing phase slot (config field + resolver arm + wire DTO + UI) so every workflow state can have its own model and an existing `reviewing` key in config.toml stops being silently dropped.

## Kind
implementation

## Context
User reports: "in the config section, we have defined which model to use in which workflow steps. That however is not happening." Verified by code reading this session: (1) The per-turn resolution chain is CORRECT and wired: turn.rs:75-82 calls resolve_turn_provider(wf.state(), skill_name) every turn; loop_impl.rs:405-437 resolves via ConfigModelResolver (skill > subagent > state > default); main.rs:618,645 wires the resolver into the factory; save_settings (settings.rs:1180-1206, 1240-1272) applies+persists+reloads+syncs the [models] patch (sync_model_resolver settings.rs:540). (2) The OBSERVABILITY HOLE: src-tauri/src/ipc/agent.rs:331 builds AgentInfo.model from `loop.provider().model()` — the DEFAULT provider slot, not the per-turn resolved override; frontend StatusBar.tsx:23,407 shows the store's global `model` (from get_config.default_model + set_model), and the agent tab shows agentModels[id] (also AgentInfo.model). So even when the override fires, every UI surface shows the default model. (3) CONFIG-SHAPE GAP: ModelsConfig (src/config/general.rs:107-124) has planning/executing/complete/subagent/skill — NO `reviewing` field; the resolver maps Reviewing->executing (model_resolver.rs:211); serde has #[serde(default)] and silently DROPS an unknown `reviewing = {...}` key from config.toml, and save_all rewrites config.toml from the in-memory struct so the key is erased on the next save. The frontend ModelsSection (FIXED_SLOTS, ModelsSection.tsx:18-23) likewise has no Reviewing slot. (4) Live config at ~/.myharness/config.toml + endpoints.toml (global_config_dir, src/config/mod.rs:358) — must be read to see what the user actually defined (endpoint-name matches? a reviewing key?).

FIX: (a) track the effective (resolved) model per agent loop and report it in AgentInfo.model so the UI shows the model actually used (StatusBar + agent tab); (b) add a `reviewing` field to ModelsConfig + resolver arm (Reviewing => reviewing.or(executing) for back-compat) + wire DTO/patch/validation + UI slot, so every workflow phase is configurable and a `reviewing` key in config.toml is honored instead of silently dropped; (c) verify against the live config + traces. Windows PowerShell; cargo test unpiped; zero warnings under #![deny(warnings)]; vitest + tsc for the frontend.

## Steps
- [x] 1. **Verify the live config + confirm root cause** — 1) Shell (PowerShell, project root): read $env:USERPROFILE\.myharness\config.toml (full) + endpoints.toml (names + models lists). Record: does [models] exist? which keys (planning/executing/complete/subagent/skill/any reviewing)? do the referenced endpoint names exactly match endpoints.toml names? Save a summary to .coding/analysis/per-state-model-diagnosis.txt. 2) Correlate with .coding/logs/traces.jsonl: for the traced session(s), list distinct model values per request (the trace records the ACTUAL model used — the override would appear there even though the UI showed the default). Note whether multiple models appear (evidence the override fires) vs a single model (evidence it never fires). 3) Re-verify the display path end-to-end: agent.rs:331 reads loop.provider().model() (default slot); StatusBar.tsx:23/407 reads store model; agentModels[id] fed by AgentInfo.model. 4) Write the verdict to .coding/analysis/per-state-model-diagnosis.txt: which of (a) override never fires (config issue), (b) override fires but UI shows default (observability), (c) both, applies — with the evidence. This step only reads; no code changes yet.
- [x] 2. **Backend: report the effective model per agent** — 1) src/agent/loop_impl.rs: add `resolved_model: RwLock<Option<String>>` field (init None in both constructors) + `pub fn set_resolved_model(&self, m: Option<String>)` + `pub fn resolved_model(&self) -> Option<String>` with doc comments (the model id actually used for the most recent turn: the per-context override when one resolved, None = default). 2) In resolve_turn_provider (loop_impl.rs:405-437): when the forced-model or resolver path returns Some((p,_)) — set resolved_model to Some(p.model().to_string()); when it returns None — set resolved_model to None. Do this in ONE place per return path (forced branch, resolver-Some, resolver-None/absent). 3) src-tauri/src/ipc/agent.rs:331: change `let model = agent_loops.get(&id).map(|l| l.provider().model().to_string());` to prefer the resolved model: `let model = agent_loops.get(&id).map(|l| l.resolved_model().unwrap_or_else(|| l.provider().model().to_string()));` and update the doc comment on AgentInfo.model (agent.rs:129) to say it is the effective (per-context resolved, else default) model. 4) Run `cargo test` unpiped (all green, zero warnings).
- [x] 3. **Backend: add the reviewing phase slot** — 1) src/config/general.rs ModelsConfig (line ~109): add `pub reviewing: Option<ModelRef>` between executing and complete with a doc comment; update the struct doc comment listing the fields. 2) src/model_resolver.rs:208-214: `WorkflowState::Reviewing => models.reviewing.as_ref().or(models.executing.as_ref())` (back-compat: unset reviewing falls back to executing). Update the module doc (line 12) + the review-related comment. 3) src-tauri/src/ipc/settings.rs: ModelsConfigWire (line ~681) add `reviewing: Option<ModelRefWire>`; models_config_wire (:774) emit it; ModelsConfigDto (:905-915) add `reviewing: Option<Option<ModelRefDto>>`; the patch application (:1188-1199) apply it; the validation loop (:1132) include reviewing in the fields array. 4) Update the fixtures/tests that assert the models wire shape: settings.rs tests (models_config_wire render at :1528-1567, dto patch semantics :1658+, save fixtures), and the golden fixture JSONs in frontend/src/lib/ipc-fixtures/ if they include models (check dto-get-settings.json — regenerate by running the Rust contract fixture test once with generate-on-missing, or hand-edit to add "reviewing": null). 5) model_resolver.rs tests: extend executing_and_complete_states_resolve or add reviewing_falls_back_to_executing (reviewing set → resolves reviewing; reviewing unset + executing set → resolves executing; both unset → None). 6) config/general.rs tests: add reviewing to models_section_parses_overrides + round-trips. 7) Run `cargo test` unpiped; all green.
- [x] 4. **Frontend: reviewing slot + effective model display** — 1) frontend/src/lib/tauri.ts: add `reviewing: ModelRefConfig | null` to the models types used by getSettings/saveSettings (mirror the wire). 2) frontend/src/components/settings/sections/ModelsSection.tsx: add `reviewing` to the Draft interface + emptyDraft + draftFromSettings + serializeDraft + handleSave's saveSettings payload, and add `{ key: "reviewing", label: "Reviewing", hint: "Closing sequence — review, fix, commit" }` to FIXED_SLOTS (after executing). 3) frontend/src/components/layout/StatusBar.tsx: make the model label show the ACTIVE agent's effective model: subscribe to `agentModels` + `activeAgent` and display `agentModels[activeAgent] ?? model` in the label span (line ~407) with a title tooltip noting it reflects per-state overrides when active; keep the picker highlight logic on the default `model` (the dropdown selects the default — unchanged). 4) frontend/src/hooks/useAgentStore.ts + agentState.ts: no change needed (agentModels already exists and is fed by registerAgents from AgentInfo). 5) Update the fixture dto-get-settings.json (add "reviewing": null to models) and any ipc-contract test asserting the settings DTO shape; run `npm run build` (tsc) + `npm test` (vitest) — all green.
- [x] 5. **Full verification + review + commit** — 1) From the repo root run `cargo test` unpiped (all test-result lines green, zero warnings). 2) `cd frontend; npm run build` + `npm test` — green. 3) `git status --short` — confirm only intended files: src/agent/loop_impl.rs, src-tauri/src/ipc/agent.rs, src/config/general.rs, src/model_resolver.rs, src-tauri/src/ipc/settings.rs, fixtures (frontend/src/lib/ipc-fixtures/*.json), frontend/src/lib/tauri.ts, frontend/src/components/settings/sections/ModelsSection.tsx, frontend/src/components/layout/StatusBar.tsx, frontend tests, .coding/analysis/per-state-model-diagnosis.txt. 4) Spawn a read-only reviewer subagent (role:"reviewer") over ALL uncommitted changes with a self-contained task: the plan goal (surface the effective per-turn model in AgentInfo/StatusBar + add the reviewing phase slot end-to-end), changed files, and focus points: resolved_model is set on EVERY return path of resolve_turn_provider (no stale value from a prior override when the next turn falls back to default), AgentInfo.model fallback semantics, serde wire shapes (ModelsConfigWire/DTO + fixtures) stay in sync with the Rust contract tests, reviewing falls back to executing when unset (back-compat), no #[allow(...)] suppressions, build warning-free. Have it write the report to .coding/reviews/2026-04-16-per-state-model-review.md. Do NOT sleep/poll — end the turn after spawning; resume on the notification. 5) Read the report, fix EVERY finding, re-run cargo test + npm build + npm test. 6) Commit everything (including the review report + diagnosis artifact) to a NEW feature branch feat/per-state-model-effective with a clear message.
