# Plan: Direct unit tests for the agentState helpers (quality review LOW 3)

## Goal
Add frontend/src/hooks/agentState.test.ts pinning the agentState helper boundary cases directly (the Maint H3 extraction's stated purpose): flushStreamingText, pushTranscriptEntry append order, transcript/activity count-cap off-by-ones, entry-id stamping + counter sync, selectMainAgentId, didMainTurnEnd, aggregateTokPerSec, getOrCreate, and the activity classifiers — registered in vitest.config.ts's include list.

## Kind
implementation

## Context
Quality review LOW 3 (.coding/reviews/2027-01-06-full-review-quality-coverage.md): frontend/src/hooks/agentState.ts (613 lines) is extracted as "fully unit-testable" (Maint H3 header, :5-12) but has no direct test file — coverage is indirect via useAgentStore.test.ts's 2,192-line table-driven suite. DECISION: add the direct test file (the substantive fix, consistent with LOW 1/LOW 2), NOT the header-amendment alternative. The header needs no change once tests exist (the claim is about design, now demonstrated). SURVEY FACTS: (a) agentState.images.test.ts (205 lines, 13 tests, registered at vitest.config.ts:45) already covers capTranscriptImages thoroughly + capTranscript's count∩image composition — do NOT duplicate; the new file covers the REST. (b) Helper map with edges: flushStreamingText (no-op on empty streamingText — same transcript ref; appends assistant entry + clears when set), pushTranscriptEntry (flush-then-append order — entry lands ABOVE the flushed text), capTranscript count edges (empty → same ref; exactly MAX_TRANSCRIPT_ENTRIES=1000 → same ref no-op; 1001 → drops exactly the oldest), capActivityLog (300 cap, same shape), allocEntryId (monotonic — module counter, use RELATIVE assertions), stampEntryIds (same array ref when all stamped; stamps only missing; syncs the counter past foreign /load ids so the next allocation can't collide — use toBeGreaterThan, order-tolerant), selectMainAgentId (smallest parentless; fallback to smallest known while parents load; null when empty), didMainTurnEnd (fires ONLY on running true→false; missing main reads as not-running), aggregateTokPerSec (null when msTotal=0; total-tokens/total-time — the 10×100tok/1000ms → 100 not 1000 inflation trap), getOrCreate (returns existing by ref; creates fresh WITHOUT inserting — insertion is the caller's job), emptyAgentState (documented defaults), isActivityEntry (tool/memory/vision/skill), isKnowledgeActivityEntry (memory + graph_* tools only — NOT vision/skill/shell). (c) TranscriptEntry union shapes for fixtures: tool {kind,name,calls}, memory {kind,name,success,running,...}, vision {kind,index,total,query,description,success,running}, skill {kind,name,prompt}, steer {kind,text}, qa {kind,question,answer}. (d) vitest isolates modules per test file, so the entry-id counter pollution stays within the file; tests must be order-independent regardless. (e) Registration: insert "src/hooks/agentState.test.ts" AFTER "src/hooks/agentState.images.test.ts" (:45 — "images" < "test" alphabetically). Frontend suite: npm test in frontend/ (vitest, node env; currently 947 tests / 67 files). Test-only change — no production code touched.

## Steps
- [x] 1. **Create agentState.test.ts + register it in vitest.config.ts** — (1) Create frontend/src/hooks/agentState.test.ts (vitest, node env; match agentState.images.test.ts's style — copyright header, module doc comment explaining the LOW 3 context and that the image budget has its own file, helper-free plain fixtures). Imports: describe/expect/it from vitest; from ./agentState import MAX_ACTIVITY_ENTRIES, MAX_TRANSCRIPT_ENTRIES, aggregateTokPerSec, allocEntryId, capActivityLog, capTranscript, didMainTurnEnd, emptyAgentState, flushStreamingText, getOrCreate, isActivityEntry, isKnowledgeActivityEntry, pushTranscriptEntry, selectMainAgentId, stampEntryIds; type-import ActivityEntry, AgentState, MainRunningSnapshot, TranscriptEntry from ./agentState. Nine describe blocks (~20 its): (a) emptyAgentState + getOrCreate — documented defaults spot-check; getOrCreate returns the existing state BY REFERENCE; creates a fresh state on first touch WITHOUT inserting it into the map (insertion is the caller's job — assert agents[7] stays undefined). (b) flushStreamingText — no-op on empty streamingText (same transcript reference, streamingText stays ""); moves streamed text into the transcript as an assistant entry and clears streamingText. (c) pushTranscriptEntry — with pending streamingText, the pushed entry lands ABOVE the flushed text (assert transcript kinds order ["assistant","tool"] and streamingText cleared); appends without flushing when nothing streams. (d) capTranscript count-cap edges — empty array → same reference; exactly MAX_TRANSCRIPT_ENTRIES entries → same reference (no-op); MAX+1 → length MAX, first entry is the SECOND original (t0 dropped), last is the newest. (e) capActivityLog — exactly MAX_ACTIVITY_ENTRIES → same ref; +1 → drops oldest, keeps newest. (f) entry ids — allocEntryId monotonic (RELATIVE assertions: b === a+1); stampEntryIds returns the SAME array when every entry is stamped; stamps only entries lacking an id (stamped one keeps its reference); syncs the counter past foreign /load ids (stamp an entry with entryId 1_000_000, then expect allocEntryId() > 1_000_000 — order-tolerant, module counter is file-local). (g) selectMainAgentId — smallest parentless id; fallback to smallest known id while parent info loads; null when both maps empty. (h) didMainTurnEnd — fires ONLY on running true→false (all four transitions asserted); a missing main agent reads as not-running (true→empty fires, empty→true doesn't). (i) aggregateTokPerSec — null when msTotal is 0; 10 requests × 100 tokens / 1000ms each → ~100 tok/s (the request-count inflation trap, toBeCloseTo). (j) activity classification — isActivityEntry true for tool/memory/vision/skill fixtures, false for assistant/user/error/steer/qa; isKnowledgeActivityEntry true for memory + graph_* tool, false for shell tool and vision. (2) Register "src/hooks/agentState.test.ts" in frontend/vitest.config.ts's include list immediately AFTER "src/hooks/agentState.images.test.ts" (:45).
- [x] 2. **Run the frontend suite** — Run `npm test` in frontend/ (unpiped; read the summary — expect all green, 68 test files, ~947 + ~20 new tests). Fix any failures before continuing.
- [x] 3. **Closing sequence: review, fix, commit, finish** — Spawn a read-only reviewer (spawn_agent role:'reviewer', complete task args) over ALL uncommitted changes: cite the plan, the quality-review LOW 3 finding, and the decision (direct tests, not the header-amendment alternative; no duplication of agentState.images.test.ts's coverage). Ask it to verify: the tests match the helpers' actual semantics (agentState.ts:290-613), the boundary cases are the finding's named ones (empty transcript, MAX_TRANSCRIPT_ENTRIES off-by-one, flushStreamingText, pushTranscriptEntry), the entry-id tests are order-tolerant (module counter — no absolute-value assertions), no production code changed, vitest registration correct, and multi-platform neutrality + doc sync. Fix every finding (re-run tests), round-2 verification if any findings, then commit on wt/agenticcoding (include review reports + plan file) and finish.
