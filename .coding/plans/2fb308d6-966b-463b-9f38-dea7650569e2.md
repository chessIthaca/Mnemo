# Plan: Shorten + tighten all system-prompt blocks in prompt.rs

## Goal
Rewrite the compiled system prompt (src/agent/prompt.rs): replace CODING_SYSTEM_PREAMBLE with the user's new core principles (memory-first, ask-when-unsure) and compress WORKFLOW_LIFECYCLE, APP_RULES, TOOL_STRATEGY, MEMORY_RECORDS, and the workflow_section state texts to short, precise instructions — keeping every rule's semantics. Update the co-located tests to the new marker strings.

## Kind
implementation

## Context
User: prompts are "all too verbose — focus on the key rules". Target preamble given verbatim (adds "Uses semantic memory system over search and shell" + "Ask questions when you are unsure unless you know the user is not available"). All prompt text lives in src/agent/prompt.rs consts (CODING_SYSTEM_PREAMBLE :23, WORKFLOW_LIFECYCLE :50, APP_RULES :102, TOOL_STRATEGY :170, MEMORY_RECORDS :233) + workflow_section() :447 (volatile tail state blocks). Tests asserting on these strings are co-located in prompt.rs's test module (:596-1283) — tests.rs constitution test uses its own markers (GLOBAL-RULE-V1), unaffected. CONSTRAINTS: stable head must stay byte-stable across workflow changes (test stable_head_byte_stable_across_workflow_changes); CONTEXT_FOOTER unchanged; every existing rule must survive the compression (only wording shrinks) — the test suite is the checklist. Working tree has only .coding/ bookkeeping changes. Windows/PowerShell; run cargo test unpiped and read test-result lines.

## Steps
- [x] 1. **Rewrite the five stable-head consts** — In src/agent/prompt.rs replace the const bodies (keep const names + doc comments, trimmed to match):
(1) CODING_SYSTEM_PREAMBLE — user's version: same first line; CORE PRINCIPLES: plan before code (create_plan); use the semantic memory system (memory_recall/context_pack) over search and shell; one step at a time (complete_step); read before write; be precise (file_edit exact match + modes per schema); batch reads via read_files, prefer file_edit over file_write, file_append for large files; explain briefly; ask questions when unsure unless the user is known unavailable.
(2) WORKFLOW_LIFECYCLE — one line per state (PLANNING read-tools+create_plan, research kind skips review; EXECUTING all tools, complete_step in order, sub-plan anytime, parent resumes, update_plan in place, abandon_plan last resort; REVIEWING = closing sequence per APP RULES, finish gated on report; COMPLETE read+create_plan, skills) + PLAN KIND para (implementation→review; research→no source changes only; bugs always bug_fixing: locked skeleton, bug:{symptom} param, BUG: memory at finish).
(3) APP_RULES — same rules, telegraphic: never commit to main (exception merge_to_main skill); merge/push always approval-gated; bookkeeping tools never prompt; never resend failed call identical args (parse [tool error], fix named param); preserve line endings, prefer file_edit; summaries ≤3 short lines. CLOSING SEQUENCE numbered 1-5: test; reviewer spawn (role:"reviewer", read-only surface, ALL uncommitted changes, self-contained task, Verdict line required, unparseable=FINDINGS, bug-plan extras: regression test/root cause/BUG memory, end turn after spawn — no sleep/poll, failed-without-report → ask_user never blind respawn); fix EVERY finding (skip only factually wrong w/ written justification) + re-run tests; commit feature branch incl. report; finish(report path).
(4) TOOL_STRATEGY — one line per tool, MANDATORY framing kept for memory_write (immediately on durable learning), memory_recall (before non-trivial work), context_pack (before planning), past_fixes (before bug diagnosis); graph tools = symbol questions first step + string literals NOT indexed; search = non-symbol text + graph fallback + chain to graph_context; spawn_agent/ask_user(≥2 concrete options)/image_*/read_files/convert_line_endings/web_fetch one-liners.
(5) MEMORY_RECORDS — prefix+budget table one line (SPEC ≤500, DECISION ≤300, BUG ≤600, PLAN ≤400, HOW none, REVIEW ≤500); pointer-first one line; hygiene one line (supersede on contradiction, update in place, delete junk only, list browses); branch-status default one line (no unmerged marker ⇒ in main).
- [x] 2. **Shorten workflow_section state texts** — In src/agent/prompt.rs workflow_section(): keep the data lines unchanged (WORKFLOW STATE header, Current state, CURRENT PLAN/GOAL/PROGRESS/CURRENT STEP, CAPABILITIES notes) and compress the prose blocks: Planning → 2 sentences (read tools only, create_plan first; write tools need a plan) + keep sub-agent restriction note; Executing → keep breadcrumb logic, trim trailing guidance to one line (complete current step then complete_step, stay in scope) + sub-agent variant; Reviewing → ~5 sentences pointing at the APP RULES closing sequence (spawn reviewer, verdict contract, fix every finding, re-run tests, commit, finish(report path); no plan tools here; failed-without-report → ask_user); Complete → 2 sentences (write tools need new plan; reads + skills available); Skill → keep goal + skill_end/abandon_skill, trim surrounding prose.
- [x] 3. **Update co-located tests to new markers** — In src/agent/prompt.rs test module, keep every test's semantic coverage but update asserted substrings to the new text: preamble test → 'Plan before you code', 'Read before you write', 'Be precise', plus NEW assertions 'semantic memory system' and 'Ask questions when unsure'; lifecycle test → 'research', 'sub-plan' (adjust or keep 'AT ANY POINT' if the new text has it); closing-rules test → update 'Never commit to the main branch'→new phrasing, 'Never resend a failed tool call'→new phrasing, keep 'CLOSING SEQUENCE', 'role:\"reviewer\"', '.coding/reviews/', 'fix EVERY finding', 'feature branch', 'never prompt'; memory-records + phase3 + bug-fixing + branch-status tests → update markers ('context_pack: MANDATORY', 'past_fixes: MANDATORY', 'BUG:', budgets, 'Branch-status default', 'ABSENCE' phrasing per new text); reviewing-block test → 'unparseable', root-cause + regression-test markers per new text; graph-order test → keep ordering assertions, update phrase markers. Run cargo test after; every failure = a marker mismatch to reconcile (either text or test).
- [x] 4. **Verify green + docs check** — Run cargo test unpiped (PowerShell) and read 'test result:' lines + $LASTEXITCODE — must be green (deny(warnings) ⇒ also zero warnings). Check PLAN.md + README.md for stale descriptions of the prompt blocks (phase names, verbosity claims) and update only if they describe the old text. Then per the edit-marker protocol: memory_write working-tier marker (prompt rewrite done, branch feat/short-prompts, tests green).
- [x] 5. **Review, fix, commit, finish** — Spawn reviewer (role:"reviewer") over ALL uncommitted changes: list changed files (src/agent/prompt.rs) + goal (terse rewrite of all prompt blocks, user's new core principles, no rule may be lost — tests are the checklist). Ask for correctness/bugs/security/constitution + docs-sync + multi-platform checks; report to .coding/reviews/. Fix EVERY finding, re-run cargo test, commit to feat/short-prompts incl. the report, finish(report path).
