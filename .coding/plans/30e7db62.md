# Plan: Verify shipped headline+body shape enforcement and close backlog item 45a4eb88

## Goal
Verify the already-shipped headline+body shape enforcement (plan 024a06e1, commit 568d405) against every requirement of backlog item 45a4eb88's task spec, re-run the full test matrix as proof, and close the item's Run-All loop — no source changes expected.

## Kind
research

## Context
Run-All re-dispatch of backlog item 45a4eb88 after a user interrupt returned it to the queue. The work shipped earlier in this same session: plan 024a06e1 (all 5 steps), commit 568d405 on wt/agenticcoding, round-1 review FINDINGS (2 low) fixed, round-2 verification PASS (.coding/reviews/2026-09-07-backlog-headline-body-shape-round2-verification.md), SPEC memory recorded. Current tree: 568d405 in history under checkpoint 4ba30e3; only uncommitted change is the harness's backlog.jsonl note update. Decision: research kind — the remaining work is verification + closure, no source changes expected (precedent: plan b5e625f8 "Verify shipped steered-images fix and close backlog item 950e81f3"). If verification finds a gap, escalate to fixes before finishing.

## Steps
- [x] 1. **Verify the shipped contract against the task spec** — Spot-check the committed state (568d405 on wt/agenticcoding): shared validator normalize_item_text + consts + image-aware normalize_new_item_text with unit tests in src/backlog.rs; const-built text-param description + execute() validation routing in src/tool/workflow/backlog.rs; IPC backlog_add validating via normalize_new_item_text(&text, &images) with the source-contract test in src-tauri/src/ipc/backlog_cmds.rs; BacklogView.tsx inline error (role=alert) + guarded draft restore; errMsg DTO tests in frontend/src/lib/tauri.test.ts registered in frontend/vitest.config.ts; README.md + PLAN.md shape-contract docs. Map each task-spec test requirement (single-line rejection naming the shape, over-long headline rejection, shape acceptance, description source-contract, IPC enforcement) to its test.
- [x] 2. **Run the full test matrix** — Root: cargo test (expect 2059+16 passed, 4+3 ignored — known git-integration gates). src-tauri: cargo test in src-tauri/ (expect 234+4 passed). Frontend: npm test in frontend/ (expect 1011 tests / 72 files) and npx tsc (clean). All green required — these tests pin every task-spec requirement, so a green matrix is the verification proof.
- [x] 3. **Close out** — Commit the side-car bookkeeping on wt/agenticcoding (the re-dispatch's .coding/backlog.jsonl note update + this plan's file in .coding/plans/) with a chore-style message referencing 45a4eb88 and the verification, then call finish. Final summary documents: the re-dispatch context (work shipped under 024a06e1/568d405 before the re-dispatch), the verification evidence, and the research-kind decision.
