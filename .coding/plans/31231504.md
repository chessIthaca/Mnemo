# Plan: Cache-hit analysis round 6: why hits are still poor (traces + request_stats) and the ranked fixes

## Goal
Diagnose why cache hits are poor in the current trace data: build a round-6 extractor over the newly-enabled `.coding/logs/traces.jsonl` plus the always-on `request_stats` (and `provider-errors.jsonl`), localize exactly where each session's prefix cache breaks (system head / tools array / a mutated earlier message / pure append / over-cliff / model switch / cold start), attribute each miss bucket a measured share, and deliver `.coding/analysis/cache-hit-6-report.md` with a ranked, executable fix list — then hand off the source fixes as a follow-up implementation plan.

## Kind
research

## Context
WHY: the user enabled the opt-in trace log and reports "so many poor cache hits" — round 6 of the cache-hit line. THIS PLAN IS ANALYSIS-ONLY (research kind, no source changes); it must end with a ranked, executable fix list, and the fixes then become a follow-up implementation plan.

INSTRUMENTS (all verified present):
- `.coding/logs/traces.jsonl` (472 KB, mtime 1789248778) — opt-in per-request trace, records carry the provider's REAL usage (cached_tokens). Record count/schema NOT yet read (needs a script; shell is only available inside a plan).
- `.coding/memory.db` `request_stats` — always-on per-request stats; round 5 read 29,759 rows / 677 sessions / 18 models, window 2026-08-10..09-10 (epoch clock, shifted vs the displayed dates). Columns: model, session_id, prompt_tokens, completion_tokens, reasoning_tokens, cached_tokens, ttft_ms, generation_ms, created_at, outcome, purpose. Open read-only via `file:.coding/memory.db?mode=ro` (uri=True) + `PRAGMA busy_timeout=3000`.
- `.coding/logs/provider-errors.jsonl` (1,303 B) — 429/fallback evidence.
- Precedent extractor: `.coding/analysis/cache-hit-5-extract.py` (341 lines) — copy/re-point it rather than writing from scratch; it already implements the R19 reporting/non-reporting tier split and the R21 error/summarize/cancelled segmentation, and writes `.coding/analysis/cache-hit-5-aggregates.txt` (347 lines).
- Prior reports: `.coding/analysis/cache-hit-analysis.md` (baseline 80.7%), `cache-hit-2-report.md` (post-R1 87.2%), `cache-hit-3-report.md` (post-R5 98.5% within-state), `cache-hit-4-report.md`, `cache-hit-5-aggregates.txt`, `cache-hit-2-diff.txt` / `cache-hit-4-diffs.txt` (the byte-diff technique).

BASELINE STATE (round 5, `.coding/analysis/cache-hit-5-aggregates.txt`): reporting-tier all-time hit 88.6% (n=27,544, resets<50% = 12.5%), post-R12 79.8% (n=14,751, resets 18.1%); NON-reporting models are 0.0% by construction (2,215 rows — gemini-vlm-gcp, glm-5.3-flash*) so they must never be averaged into the headline; per-model spread is the signal: glm-5.2 93.6%, glm-5.3 81.8%, glm-5.3-gcp 76.0%, glm-5.3-flash-gcp-h200-fp8-01 57.8%, deepseek-v4-flash 18.6%, glm-5.2-maas-gcp 17.5%, avgP 120K-300K with max 541K.

ALREADY-FIXED CAUSE (do NOT re-report as new): the R14 per-batch tool-result truncation that mutated already-sent messages and capped hits at ~46-50% at ANY context size — bug record `.coding/knowledge/bug/2026-12-31-cache-hit-terrible-at-any-context-size-r14-per-b.md`; fixed by hysteresis compaction `compact_old_tool_results(messages, keep=10, keep_high=20, summary_chars=500)` in `src/agent/context.rs` + `src/agent/turn.rs:2105` (token_accounting.reset only when truncated>0), with regression tests `compact_below_high_water_mark_mutates_nothing` / `compact_fires_at_high_water_mark`. Round 4 also fixed the heuristic that masked misses (`src/agent/turn.rs:1163-1173`: cached_tokens=0 was recorded as min(prev,curr)).

CANDIDATE CAUSES TO TEST (rank by measured share, not by plausibility): (a) cold start / first request per session; (b) model or endpoint switch mid-session (cache is per-model — 18 models in the table); (c) over-cliff prompts above the proxy's ~340-350K cache limit (round-4 observation: 99% at 341K → 4.4% at 370K, only the ~16K head cached); (d) any remaining prefix mutation — locate it by first-differing-message index vs the previous request in the same session; (e) non-reporting models (0% by construction, not a defect); (f) 429-fallback re-dispatch / cancelled-then-retried turns (cold cache on the fallback model); (g) prompt-management injections that rewrite the head or tools array between requests (state switch, memory/steering block, plan rider, CONTEXT_FOOTER stability).

FALSIFIABLE PREDICTION to reuse (round 4): if the prefix up to message k is byte-identical, provider cached_tokens ≈ tokens(messages[0..k]) + tools; a large gap means our own mutation, no gap with low cached means the provider/proxy limit.

OUTPUT: `.coding/analysis/cache-hit-6-report.md` (headline round-5→round-6 table, cause ranking with the evidence per bucket, ranked fix list naming file + function + expected win + risk). Verification commands for the analysis itself: `python .coding/analysis/cache-hit-6-extract.py` unpiped (read its own summary lines); `cargo test` only if a step ever touches source (it should not — research kind).

## Steps
- [ ] 1. **Build the round-6 extractor over traces + request_stats** — Copy `.coding/analysis/cache-hit-5-extract.py` to `.coding/analysis/cache-hit-6-extract.py` (file_write) and re-point it at the CURRENT data: keep the read-only `file:.coding/memory.db?mode=ro` + busy_timeout open, the R19 reporting/non-reporting split and the R21 error/summarize/cancelled segmentation; ADD a `--traces` section that loads `.coding/logs/traces.jsonl` line-by-line (json.loads per line), prints the record count, the sorted top-level keys of record 0, the ts range, the model set, and for each record: index, ts, model, session_id, prompt_tokens, cached_tokens (PROVIDER value — flag whether the record carries real usage or the min(prev,curr) heuristic), nMessages, nTools, system length, tail length, footer length, workflow state if present. Write the output to `.coding/analysis/cache-hit-6-aggregates.txt` and also print the section headers to stdout. Run it unpiped: `python .coding/analysis/cache-hit-6-extract.py`. If the trace log holds only a handful of records, say so explicitly — the memory.db statistics stay the backbone and the traces serve as ground truth for real cached_tokens.
- [ ] 2. **Localize the prefix break per request pair** — Extend `.coding/analysis/cache-hit-6-extract.py` with section B (prefix-break localizer), the round-4/round-5 byte-diff technique applied at message granularity: group trace records BY session_id in ts order; for each consecutive pair compute the first differing message index k by comparing the message arrays element-wise (role + content length + sha1 of content); classify k as HEAD (k==0, a system message), TOOLS (the tools array differs while messages do not), MID (0<k<len-1, i.e. an ALREADY-SENT message was mutated in place — the R14 failure shape), TAIL (k>=previous length: pure append, the healthy case), or UNRELATED (different session/model — skip). For each pair also record tokens-before-k (sum the message char lengths before k / 4 as a cheap approximation, and note the approximation) next to the record's provider cached_tokens so the falsifiable prediction `cached ≈ tokens-before-break` can be checked or refuted. Emit a per-pair table (session, ts, model, k, classification, cached, predicted, delta) to `.coding/analysis/cache-hit-6-aggregates.txt`. Re-run the script unpiped.
- [ ] 3. **Attribute every miss to a cause bucket** — In `.coding/analysis/cache-hit-6-extract.py` section C, bucket EVERY main-loop request (R21: outcome=None and purpose=None) from `.coding/memory.db` request_stats into exactly one cause, and report n + share of total misses + the hit% each bucket contributes: (a) COLD — first request of a session_id; (b) SWITCH — the session's previous request had a different `model`; (c) CLIFF ��� prompt_tokens above 340000 (the round-4 proxy limit observation; also print the hit% above vs below and the 320K-360K histogram so the cliff's exact location is measured, not assumed); (d) MUTATION — trace-confirmed non-TAIL breaks from section B (only for requests that HAVE a trace record; label the rest UNKNOWN-trace); (e) NONREPORTING — the R19 tier (0% by construction: exclude from the defect share and state that exclusion in the output); (f) RETRY — a request whose preceding row in the same session has outcome='error'/'cancelled' or a provider-errors.jsonl entry in the preceding 60s. Print a reconciliation line (buckets sum to the total, as the round-5 A0 section does). Re-run the script unpiped.
- [ ] 4. **Cross-check the mutation paths in the agent context code** — Cross-check the measured mutation buckets against the code that can rewrite an already-sent message or the stable head — read (do NOT edit): `src/agent/context.rs` (`compact_old_tool_results` + the hysteresis window), `src/agent/turn.rs` (~1163 heuristic fallback, ~2105 token_accounting.reset gating, and the request-assembly site), `src/agent/prompt.rs` (state-switch prompt swap), the steering/memory injection site, the compaction/summarize path, the plan-rider injection, and the model/endpoint + 429-fallback selection. For each bucket from step 3, decide and write down: (i) INTENDED (cold start, model switch — unavoidable, quantify the floor they impose), (ii) AVOIDABLE (dynamic content that could move LATER in the message order to keep the prefix stable), or (iii) BUG (in-place mutation of already-sent messages, or a head/tools rewrite between requests that has no functional need). Name the exact function + line for every (ii)/(iii).
- [ ] 5. **Write the round-6 findings report** — Write `.coding/analysis/cache-hit-6-report.md`: (1) headline table round-5 → round-6 (reporting-tier hit%, resets, avg/max prompt, request counts) with the non-reporting tier stated separately; (2) the cause ranking — every bucket with its measured share, the evidence that produced it (record ids / session ids / the section-B classification counts), and an explicit statement of the falsifiable prediction's outcome (confirmed or refuted); (3) the cliff's measured location if section C found one; (4) the ranked fix list — per fix: file + function + line, what changes, expected hit-% win (from the bucket share), risk, and a proposed regression test — ordered by win/cost, each executable by a weaker model from the text alone; (5) an explicit 'what is NOT the cause' section (R14 shape, heuristic inflation — already fixed) so the next round does not re-litigate it. Mark every measured number as measured vs estimated.
- [ ] 6. **Hand off the fixes as a follow-up implementation plan** — Hand off from `.coding/analysis/cache-hit-6-report.md`: summarize the ranked fixes for the user in the final reply (short, ≤3 lines plus the report path), and for any fix that changes source under `src/` (the anchors the report names, e.g. `src/agent/context.rs` / `src/agent/turn.rs` / the assembly site) create the follow-up implementation plan (kind=implementation, with the report path + the exact file/function anchors as context) — do NOT make source edits inside this research plan. If the diagnosis instead shows nothing actionable (e.g. the residue is entirely cold-start + non-reporting models), write that conclusion plainly in the report and skip the follow-up plan.
