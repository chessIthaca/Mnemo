# Plan: Test the effort select-mapping helpers (quality review LOW 1)

## Goal
Add a describe("effort select mapping") block to frontend/src/components/settings/types.test.ts covering effortToSelectValue and effortFromSelectValue — the null ↔ REASONING_EFFORT_DEFAULT round-trip where a regression would silently rewrite stored configs (persisting "__default__" into endpoints.toml, or breaking the "off" passthrough).

## Kind
implementation

## Context
Quality review LOW 1 (.coding/reviews/2027-01-06-full-review-quality-coverage.md): effortToSelectValue + effortFromSelectValue (frontend/src/components/settings/types.ts:355-372) have zero references in any registered test file. The null ↔ REASONING_EFFORT_DEFAULT round-trip is exactly where a regression silently rewrites stored configs (returning "" instead of null would persist the sentinel string into endpoints.toml; dropping the "off" passthrough would break off-mode). Facts: REASONING_EFFORT_DEFAULT = "__default__" is exported at types.ts:173 — import it from "./types" in the test (symbolic, not hardcoded, so a sentinel-value change doesn't break the test). effortToSelectValue: null/undefined/"" → sentinel, else passthrough. effortFromSelectValue: sentinel → null, else passthrough. The round-trip domain is the STORED domain (null + concrete efforts) — "" is not stored (from("") === "" ≠ null), so it is excluded from the round-trip but covered in the to-select mapping. types.test.ts is already registered in vitest.config.ts (no registration needed); it uses vitest describe/it with explanatory comments and a case-insensitive-alphabetical import order (capsAutofillPatch, dirtySectionIds, importEndpointEditable, modelConfigFor, SETTINGS_NAV, SettingsSectionId, upsertModelConfig — new names slot in: effortFromSelectValue/effortToSelectValue after dirtySectionIds, REASONING_EFFORT_DEFAULT after modelConfigFor). Test-only change — no production code touched. Frontend suite: npm test in frontend/ (vitest, node env).

## Steps
- [x] 1. **Add the effort select-mapping tests to types.test.ts** — In frontend/src/components/settings/types.test.ts: (1) extend the "./types" import to `import { capsAutofillPatch, dirtySectionIds, effortFromSelectValue, effortToSelectValue, importEndpointEditable, modelConfigFor, REASONING_EFFORT_DEFAULT, SETTINGS_NAV, type SettingsSectionId, upsertModelConfig } from "./types";` (case-insensitive alphabetical order preserved). (2) Append after the importEndpointEditable describe block (file ends at :279 with `});`): a describe("effort select mapping") block with five it() cases — (a) effortToSelectValue maps null, undefined, and "" to REASONING_EFFORT_DEFAULT; (b) effortToSelectValue passes "off", "low", "high" through verbatim (off stays off); (c) effortFromSelectValue maps REASONING_EFFORT_DEFAULT back to null — comment: returning "" instead of null would persist "__default__" into endpoints.toml, the sentinel is a select-only value; (d) effortFromSelectValue passes "off" and "high" through; (e) round-trip property: for (const x of [null, "off", "low", "medium", "high"] as const) expect(effortFromSelectValue(effortToSelectValue(x))).toBe(x) — comment: the stored domain is null + concrete effort strings; a regression here silently rewrites stored configs. Match the file's existing style (vitest describe/it, explanatory comments).
- [x] 2. **Run the frontend suite** — Run `npm test` in frontend/ (unpiped; read the summary — expect all green with +5 tests vs the pre-change count). Fix any failures before continuing.
- [x] 3. **Closing sequence: review, fix, commit, finish** — Spawn a read-only reviewer (spawn_agent role:'reviewer', complete task args) over ALL uncommitted changes: cite the plan, the quality-review LOW 1 finding (.coding/reviews/2027-01-06-full-review-quality-coverage.md), and the regression risks (sentinel persisted into endpoints.toml; off-mode passthrough). Ask it to verify the tests match the helpers' actual semantics (types.ts:355-372), the round-trip domain is correct (stored domain only — "" excluded because from("") === "" ≠ null), the sentinel is used symbolically (imported, not hardcoded), import order/style matches the file, and no production code changed. Fix every finding (re-run tests), round-2 verification if any findings, then commit on wt/agenticcoding (include review reports + plan file) and finish.
