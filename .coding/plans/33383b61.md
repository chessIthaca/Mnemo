# Plan: Backlog plan chip: human-friendly clickable plan identifier

## Goal
Replace the Backlog tab's raw checkpoint-sha status line with a human-friendly, clickable plan chip: the plan TITLE as the primary identifier + a short 8-char plan-id chip (matching the tool layer's prefix), status indication unchanged, the chip clickable to switch to the working agent's chat tab (inert when none), and the checkpoint sha demoted to a copyable detail — via structured payload fields (plan_title recorded at the stamp moment) instead of the composed note text.

## Kind
implementation

## Context
Backlog f45513b2 (user-reported; the queued item). Exploration at HEAD on wt/agenticcoding: (1) The noise: run-all dispatch sets the item note to the raw checkpoint sha (run_all.rs:1286/:1324 set_note(checkpoint)); annotations append " | <reason>" (run_all.rs:1938 format!("{n} | {suffix}")); the frontend renders item.note verbatim (BacklogView.tsx:753-755) so the 40-hex sha is the status line's headline. (2) The payload: BacklogItemView (backlog_cmds.rs:65-71) = #[serde(flatten)] BacklogItem + checkpoint_sha (parsed from the note head at :77) — so plan_id (backlog.rs:80, the 45dcf577 root-plan linkage) ALREADY rides the payload; the TS BacklogItem (types.ts:20-43) declares neither plan_id nor plan_title. (3) The recording point: set_plan_id (backlog.rs:438-446, reload→find live→set→persist) called from stamp_backlog_in_flight (run_all.rs:2057-2094) on every MAIN-agent Executing entry (events.rs:449-460, gated is_main; refresh-on-every-entry so a fresh plan re-links). (4) Title source: plan files .coding/plans/<id>.md start with "# Plan: <title>" (verified: 7369d7f3.md); the plans dir is reachable via state.project.root.lock().await.root.plans_dir (project/mod.rs:38). (5) Frontend: BacklogItemCard (BacklogView.tsx:251-787) already computes mainAgentId (:251 selectMainAgentId) and tracks workflowStates; setActiveAgent (useAgentStore.ts:683) switches the chat tab; the headline row (:685-720, status badge + headline, backlog 40763a24) and the note (:753-755) are the render sites. (6) Tests: BacklogView.test.tsx = component harness (renders BacklogItemCard with item fixtures, node env); BacklogView.test.ts = source-contract (raw imports); contract fixtures: contract_fixtures.rs:~190-202 (item has plan_id: None) + dto-backlog-changed-payload.json. DESIGN DECISIONS: (a) record plan_title on BacklogItem at the stamp moment (read from the plan file's first line) — survives plan completion so done items keep their titles; (b) the chip = plan TITLE (primary) + 8-char short-id chip (secondary, matching the tool layer's prefix, derived client-side from plan_id); (c) clickable when item is in_flight AND a main agent exists → setActiveAgent(mainId); otherwise INERT (documented choice over opening the plan panel — the panel only shows the active agent's live plan, so it can't display arbitrary/done plans); (d) the note display strips the "<sha> | " head client-side using checkpoint_sha (the stored note stays machine-readable for extract_checkpoint_sha); (e) the sha demoted to a copyable detail row ("pre-work checkpoint <8-char>…" + copy button); (f) the working-agent reference is derived client-side (mainAgentId + in_flight — backlog items are always main-agent-dispatched), no new payload field; (g) status badge + headline rendering unchanged (40763a24 semantics).

## Steps
- [x] 1. **Backend: record the plan title (backlog.rs, run_all.rs)** — (a) src/backlog.rs: add `pub plan_title: Option<String>` to BacklogItem (after plan_id ~:80) with `#[serde(default, skip_serializing_if = "Option::is_none")]` + doc comment (the plan's title at dispatch — recorded alongside plan_id so done items keep their title; None = not yet stamped / pre-change items); update Default/test_default/From impls as the compiler flags. (b) Extend set_plan_id (~:438) to `set_plan_id(&mut self, id: &str, plan_id: Option<&str>, plan_title: Option<&str>) -> bool` — set both fields (title only when plan_id is Some); update the existing test set_plan_id_records_the_item_plan_linkage (~:1269) + add title assertions (Some records, None clears, unknown id no-op). (c) src-tauri/src/ipc/run_all.rs: add helper `fn plan_title_from_file(plans_dir: &std::path::Path, plan_id: &str) -> Option<String>` — reads `<plans_dir>/<plan_id>.md`, takes the first line, strips the `# Plan: ` prefix (doc: the plan-file format contract written by create_plan); unit tests (parses the title, missing file → None, first line without the prefix → None). (d) stamp_backlog_in_flight (~:2057): read the plans dir via `state.project.root.lock().await.root.plans_dir.clone()`, compute `plan_title_from_file(...)` when top_plan_id is Some, pass both to set_plan_id. (e) Source-contract test (fn_body style): the stamp reads the title and passes it to set_plan_id alongside the plan id.
- [x] 2. **Payload + TS types + fixtures** — (a) frontend/src/lib/types.ts BacklogItem (~:20-43): add `plan_id?: string | null` (doc: the ROOT plan id recorded at Executing entry — the 45dcf577 linkage; the payload carries it via serde flatten) and `plan_title?: string | null` (doc: the plan's title at dispatch, recorded alongside plan_id). (b) src-tauri/src/ipc/contract_fixtures.rs: the fixture BacklogItem (~:190-202) gains `plan_title: None`. (c) frontend/src/lib/ipc-fixtures/dto-backlog-changed-payload.json: add `"plan_title": null` to the fixture item (verify `"plan_id": null` is present; add if missing). (d) frontend/src/lib/ipc-contract.test.ts: assert `typeof item.plan_id === "string"` or null + same for plan_title, next to the existing item assertions.
- [x] 3. **Frontend: plan chip + note cleanup + checkpoint detail (BacklogView.tsx)** — In BacklogItemCard (frontend/src/components/views/BacklogView.tsx:251-787): (a) a plan-chip row between the headline row (:685-720) and the note — rendered when `item.plan_id` is set: the plan TITLE (`item.plan_title`, truncated with line-clamp/title-attr fallback to the short id when the title is null — pre-change items) as the primary identifier + a monospace short-id chip `item.plan_id.slice(0, 8)` (the same 8-char prefix the tool layer shows, so user and agent references align). Clickable when `item.status === "in_flight" && mainAgentId` → `useAgentStore.getState().setActiveAgent(mainAgentId)` (switch to the working agent's chat tab; backlog items are main-agent-dispatched); otherwise inert (no onClick, cursor-default) — documented choice over opening the plan panel (it only shows the active agent's live plan). (b) The note rendering (~:753-755): strip the checkpoint-sha head — when `item.checkpoint_sha` is set and the note starts with it, render only the remainder (drop the leading `<sha>` and ` | ` separator; empty remainder → render nothing). (c) A checkpoint detail row when `item.checkpoint_sha` is set: subtle `pre-work checkpoint <sha.slice(0,8)>…` text + a copy affordance (navigator.clipboard.writeText(item.checkpoint_sha), with a copied confirmation) — the manual resume/rollback anchor stays copyable, just not the headline. (d) The status badge + headline rendering unchanged (40763a24 semantics).
- [x] 4. **Frontend tests (BacklogView.test.tsx)** — Extend frontend/src/components/views/BacklogView.test.tsx (the BacklogItemCard harness — mirror the existing item() fixture style): (a) renders the plan title + the 8-char short-id chip when plan_id/plan_title are set; no chip when plan_id is absent; title-null falls back to the short id alone. (b) The checkpoint sha NEVER appears in the note/headline text (the note shows only the reason after the sha head; a note that is exactly the sha renders no note block). (c) The checkpoint detail shows the truncated sha and the copy button calls navigator.clipboard.writeText with the FULL sha (mock clipboard). (d) Clicking the chip when the item is in_flight switches the active agent to the main agent (assert the store's activeAgentId); when not in_flight the chip is inert (no store change). (e) The status badge still renders as before (existing headline/status tests keep passing).
- [x] 5. **Full test run (all four suites)** — Per the HOW rule (root cargo test does NOT compile the app crate's unit tests): run `cargo test` (root, lib) AND `cargo test -p mnemo-app` (app unit tests) — both unpiped, reading $LASTEXITCODE directly; then `cd frontend; npx tsc --noEmit` and `npm test` (vitest). All four must be green with zero warnings under #![deny(warnings)] before the review step.
