# Plan: Backlog display: render line breaks in item cards + note

## Goal
Make multi-line backlog item prompts readable in the Backlog tab list (not only in the edit view / hover preview): newlines in item.text and item.note must render as line breaks instead of collapsing to spaces.

## Kind
implementation

## Context


## Steps
- [x] 1. **Add whitespace-pre-wrap to the card text + note** — In frontend/src/components/views/BacklogView.tsx: (1) the card prompt-text div (the `editing ? ... : (...)` branch, ~line 560, className `text-[0.8em] text-slate-200 ...`) — add `whitespace-pre-wrap break-words` so item.text newlines render (same classes the hover-preview portal at ~line 602 already uses); (2) the note div (~line 589, className `mt-1.5 rounded border ... text-[0.75em] text-slate-400`) — add `whitespace-pre-wrap break-words` so multi-line failure notes render. No other logic changes (truncation/expand/copy all operate on the raw string and are unaffected).
- [x] 2. **Add source-contract regression test** — Append to frontend/src/components/views/BacklogView.test.ts (static source-contract style, vitest node env — no React DOM): a describe block asserting the card display div carries `whitespace-pre-wrap` AND `break-words`, the note div carries `whitespace-pre-wrap`, and (guard against a future refactor collapsing the display into the preview-only path) that the classes appear in the non-editing display branch. Keep assertions on the raw source string like the existing tests.
- [x] 3. **Run frontend + rust tests, verify green** — Run `npm run test --workspace frontend` (vitest) and check all suites pass; then run `cargo test` from the repo root and check the `test result:` lines (unpiped, check $LASTEXITCODE or the result lines — do not trust a piped exit code). Fix any failures before completing the step.
