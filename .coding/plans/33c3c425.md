# Plan: Show tool results in chat by default — flip show_tool_activity default to on

## Goal
Flip the Settings → Chat `show_tool_activity` default from false to TRUE so tool/memory/vision/skill activity cards show in the chat transcript by default on a fresh install (user request 2027-01-13; dispatched backlog item 57687857). Existing per-user configs that explicitly set `show_tool_activity = false` keep hiding (serde absent-key → Default, explicit value wins). This is a DEFAULT-FLIP ONLY: no render/behavior change — Conversation.tsx filter logic, the ChatSection toggle UI, and show_knowledge_activity's own default are untouched; git diff after the change shows only defaults, contract fixtures, doc comments, and tests.

## Kind
implementation

## Context
USER REQUEST 2027-01-13 (dispatched backlog item 57687857): "show tool results by default — this is a config setting in the config dialog and all we ask for is change the default." The Settings → Chat toggle `show_tool_activity` (hides/shows ALL tool/memory/vision/skill activity cards in the chat transcript) currently defaults FALSE, so tool results are hidden on a fresh install unless the user opts in. Flip to default TRUE so tool results show by default; per-user configs explicitly setting `show_tool_activity = false` must keep hiding (serde Option semantics: absent key → Default; explicit value wins).

DEFAULT-FLIP ONLY — no behavior/design change. The render-time filter in frontend/src/components/chat/Conversation.tsx (:77 read, :185 filter) stays untouched; show_knowledge_activity keeps its own default; ChatSection toggle UI unchanged.

VERIFIED ANCHORS (repo-wide search 2027-01-13, at dispatch):
- src/config/general.rs:425 `show_tool_activity: false,` (Default impl) → `true`; field decl :368; doc comment :375 ("without the noise of the full `show_tool_activity` toggle); set `false`" — read :360-380 for the full text); :1030 comment "Users opt IN by setting show_tool_activity to true." → opt-OUT phrasing; unit test `show_tool_activity_defaults_false` :1026-1032 (asserts `!cfg.ui.show_tool_activity`) → rename `_defaults_true`, assert true; round-trip test :1063-1076 (TOML `show_tool_activity = true` at :1068) → round-trip an explicit `= false` so BOTH directions stay covered (default=true via the renamed default test; explicit-false via this); combined-literal test :1197 `show_tool_activity: true,` + :1241 assert — STAYS (explicit true still honored on a full config).
- src-tauri/src/ipc/contract_fixtures.rs:252 `show_tool_activity: false,` → `true` (the Rust contract test asserts the fixture).
- src-tauri/src/ipc/settings.rs: mapping :808 (NO change); wire-test literals :1286 and :1401 `false` → `true`; JSON assertion :1336 `assert_eq!(v["ui"]["show_tool_activity"], false)` → `true`.
- frontend/src/lib/ipc-fixtures/dto-get-settings.json:57 `"show_tool_activity": false` → `true` (keeps valid JSON; the frontend contract test asserts this fixture).
- frontend/src/hooks/useAgentStore.ts: seed `showToolActivity: false,` at :646 (item said ~:832 — corrected) → `true`; store field type + doc at :265 ("default off" phrasing — read and flip to default on / opt-out); setter :880 unchanged; :650 neighbor comment (show_knowledge_activity pattern pointer) — read, flip only if it pins the old default.
- frontend/src/hooks/useAgentStore.test.ts:45 full-state literal `showToolActivity: false,` → `true`; :1414-1419 test "entry enters the store even with showToolActivity off (render-time hiding)" explicitly setState(false) — KEEP (it exercises the explicit-off path, exactly the behavior we preserve).
- frontend/src/lib/ipc-contract.test.ts:111 full-state literal → `true`.
- CORRECTION: agentState.ts contains NO showToolActivity references (item's :615 claim stale — skip).
- TO CHECK while editing (read first): frontend/src/components/settings/sections/ChatSection.test.ts:89-120 (:99 base literal `showToolActivity: false` — flip to true if it's the default-serialization fixture, keep if it tests explicit-false preservation; :120 serializeChat({...base, showToolActivity: true}) already covers explicit-true); frontend/src/components/chat/Conversation.window.test.tsx:35-60 (comment "default showToolActivity=false is fine..." — if the test relies on the false DEFAULT, set the fixture explicitly or update; user/assistant entries pass under either default).
- Memory to reconcile: DECISION 602ddd77 ("show_tool_activity consolidates show_memory_activity — render-time filter, default FALSE") → memory_supersede with the new default TRUE (keep history, never delete).

PRECEDENTS: plan .coding/plans/0e71e4f9.md (commit a65862d, backlog db489070) shipped the FALSE default — this item is its inverse; plan bc09914f is the default-ON flip template (knowledge activity) whose fixture/wire steps this mirrors. Baseline matrix (post-UDF-merge, all green): root cargo test 2,328 passed/5 ignored, cargo test -p mnemo-app 310 passed, frontend tsc clean + vitest 1,104 passed/81 files.

VERIFICATION COMMANDS (agent.md: PowerShell, unpiped, read $LASTEXITCODE and the test-result lines — a piped exit code is not evidence): `cargo test`, `cargo test -p mnemo-app`, `npx tsc --noEmit` + `npm test` in frontend/. #![deny(warnings)] — a green build already proves zero warnings; never add #[allow].

DOC SYNC: debug.config.toml — check whether its sample `[ui]` block pins show_tool_activity (flip if it documents a default); README/docs/FEATURES.md only mention the toggle generically — verify no "default off" phrasing anywhere live; .coding/ knowledge record for DECISION 602ddd77 carries "default FALSE" — the supersede writes the corrected successor.

## Steps
- [x] 1. **Flip the config default + unit tests (src/config/general.rs)** — Read src/config/general.rs :360-380 (field doc comment) and :1020-1090 (the three tests + the :1030/:1082 comments). Then file_edit (batch mode, one atomic call): (1) Default impl :425 `show_tool_activity: false,` → `true`; (2) field doc Comment :365-375 — change the 'defaults to false' / opt-in phrasing to 'defaults to TRUE (user opt-out): set `false` to hide the activity cards'; (3) :1030 comment 'Users opt IN by setting show_tool_activity to true.' → 'Users opt OUT by setting show_tool_activity to false.'; (4) rename test :1026 `show_tool_activity_defaults_false` → `show_tool_activity_defaults_true` and flip the :1032 assertion to `assert!(cfg.ui.show_tool_activity);` (doc comment above the test follows); (5) round-trip test :1063-1076: change the TOML at :1068 from `show_tool_activity = true` to `show_tool_activity = false` so the round-trip exercises the EXPLICIT-FALSE path (default-true is covered by the renamed default test; the combined-literal test at :1197 `show_tool_activity: true,` + :1241 assertion STAYS untouched — explicit true on a full config must keep working). Check the :1082 comment ('like show_tool_activity. Users opt in...') — it refers to show_knowledge_activity: flip only if it cites show_tool_activity's default, else leave. Run `cargo test --quiet` from C:/Mnemo (cwd; unpiped) and read the `test result:` lines + $LASTEXITCODE — green, zero warnings.
- [x] 2. **Sync Rust contract fixtures + wire tests** — Three file_edits, one batch per file: (1) src-tauri/src/ipc/contract_fixtures.rs:252 `show_tool_activity: false,` → `true`; (2) src-tauri/src/ipc/settings.rs:1286 `show_tool_activity: false,` → `true` AND :1401 `show_tool_activity: false,` → `true` AND :1336 `assert_eq!(v["ui"]["show_tool_activity"], false)` → `true` (batch mode, one atomic call); mapping at :808 stays. Run `cargo test -p mnemo-app --quiet` unpiped — green (the contract + wire tests assert these literals, so they MUST change together).
- [x] 3. **Sync the frontend JSON fixture** — file_edit frontend/src/lib/ipc-fixtures/dto-get-settings.json:57 `"show_tool_activity": false` → `"show_tool_activity": true` (keep the JSON valid — comma/quoting intact; the frontend ipc-contract test asserts this fixture).
- [x] 4. **Flip the frontend store seed + literals + dependent tests** — file_edit batch on frontend/src/hooks/useAgentStore.ts: (1) :646 seed `showToolActivity: false,` → `true`; (2) :265 store field doc 'default off' → 'default on (opt-out): set false to hide' (read :255-275 first for the exact text). Then one edit each: frontend/src/hooks/useAgentStore.test.ts:45 full-state literal `showToolActivity: false,` → `true` (KEEP the :1414-1419 explicit-off test untouched — it exercises the preserved explicit-false path); frontend/src/lib/ipc-contract.test.ts:111 full-state literal → `true`. Then READ the two dependency-risk tests before deciding: (a) frontend/src/components/settings/sections/ChatSection.test.ts:85-125 — if :99's base literal is the default-serialization fixture, flip to `true`; if the test asserts explicit-false preservation, keep (the :120 line with showToolActivity: true already covers explicit-true); (b) frontend/src/components/chat/Conversation.window.test.tsx:35-60 — if it relies on the false DEFAULT, update the fixture/comment to set the flag explicitly (user/assistant entries pass under either default; activity-entry visibility under the default-true is not asserted there — verify). Conversation.tsx and Conversation.test.ts are NOT touched. agentState.ts has NO showToolActivity references (verified) — skip.
- [x] 5. **Stale-phrasing sweep + DECISION memory supersede** — Search (literal, scoped globs src/** src-tauri/** frontend/** debug.config.toml — never .coding/plans or .coding/reviews, historical records are not rewritten) for: `defaults to false`, `default off`, `default FALSE`, and `Users opt IN` near show_tool_activity; fix EVERY live phrase that pins the OLD default with targeted file_edits (general.rs comments already covered in step 1 — sweep for stragglers; debug.config.toml sample `[ui]` block if it documents a default). Then memory_supersede id 602ddd77 (DECISION 'show_tool_activity consolidates show_memory_activity — render-time filter, default FALSE') with successor content: same consolidation fact but 'default TRUE (flipped 2027-01-13, backlog 57687857): absent key → true; explicit `show_tool_activity = false` still hides; render-time filter only, the store keeps everything'. tier semantic, typed DECISION prefix — the old record stays as history.
- [x] 6. **Full matrix + diff audit** — Run the four-command matrix (all unpiped, read $LASTEXITCODE + test-result lines): `cargo test --quiet` and `cargo test -p mnemo-app --quiet` from C:/Mnemo; `npx tsc --noEmit` then `npm test` from C:/Mnemo/frontend. All green, zero warnings. Then the acceptance audit via git_read op=diff: (1) `git diff HEAD -- frontend/src/components/chat/Conversation.tsx frontend/src/components/chat/Conversation.test.ts` must be EMPTY (no render/code change — the item's acceptance #4); (2) `git diff HEAD --stat` must show ONLY default values, contract fixtures, doc comments, tests, .coding/ side-car (plan file + this backlog's knowledge) — no logic changes anywhere; (3) re-verify the two explicit-off paths survived: assert!(cfg2.ui.show_tool_activity == false) round-trip (general.rs) and the useAgentStore :1414 render-time-hiding test still pass.
- [ ] 7. **Review, fix, commit, finish** — Spawn role:"reviewer" (name show-tool-review, OMIT the model param — the configured reviewing model is authoritative). Self-contained task: plan goal + changed files (src/config/general.rs, src-tauri/src/ipc/contract_fixtures.rs, src-tauri/src/ipc/settings.rs, frontend/src/lib/ipc-fixtures/dto-get-settings.json, frontend/src/hooks/useAgentStore.ts + useAgentStore.test.ts, frontend/src/lib/ipc-contract.test.ts, plus any ChatSection.test.ts / Conversation.window.test.tsx / debug.config.toml adjustments); ask for (a) correctness of the serde semantics claim (absent key → Default `true`; explicit `false` wins — check Default impl + deny_unknown/field attrs), (b) that NO render/code behavior changed (Conversation.tsx + ChatSection.tsx untouched), (c) every fixture/wire literal flipped together (the contract tests assert them), (d) doc-comment accuracy (no surviving 'defaults to false' phrasing), (e) multi-platform neutrality + file-tools-first + no #[allow]. Then fix EVERY finding, re-run the matrix, commit to the current wt/* branch (never main) including the review report, finish(review_report path). Final summary ≤3 lines: what changed, how to use it (existing per-user `show_tool_activity = false` still hides), test result.
