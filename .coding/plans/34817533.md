# Plan: Audit tool-call echo coverage — answer backlog c8531c34

## Goal
Answer backlog c8531c34 — "are there any tool calls that don't echo into the console?" — with a verified, code-referenced audit of the whole echo chain (LLM emission → fan-in → GUI chat transcript, -console REPL, Output tab), documenting every exception and why graph/search calls appear rarely next to read_files. Deliverable: .coding/knowledge/tool-call-echo-map.md + SPEC memory + the direct answer in the final summary. Research plan: no source-code changes.

## Kind
research

## Context
Backlog c8531c34 asks: "are there any tool calls that don't echo into the console? I see lots of read files but not many graph calls and search calls." Pre-planning investigation (this session) already verified: turn.rs:1104-1121 forwards every LlmEvent::ToolCallStart to the fan-in unconditionally; console.rs render_event (241) renders ToolCallStart as "→ {name}" for every tool with Silent set = {Started, ToolCallArgDelta, ContextUsage, PromptDispatched, Phase}; agentEventReducer.ts reduceToolCallStart (486) suppresses only MEMORY_TOOLS behind showMemoryActivity (default true) and neverGroups (545: shell/search/search_read/browser) affects merging only; search/search_read auto-delegation (backlog b804012f, src/tool/agent/search.rs + codegraph.rs) runs graph/memory lookups internally with no separate ToolCallStart. Remaining to verify: ToolResult emission path, IPC forwarding to frontend, show_memory_activity default source, exact auto-delegation code refs.

## Steps
- [x] 1. **Verify the emission chain (Rust) — every tool call emits echo events unconditionally: src/agent/turn.rs:1104-1121 forwards each LlmEvent::ToolCallStart to the fan-in (no name/category filter); find the AgentEvent::ToolResult emission (search 'AgentEvent::ToolResult' in src/agent/ + src/runtime/) and confirm it fires for every executed call including errors; confirm the serialization (src/runtime/channels.rs:587) and the console loop (src-tauri/src/console.rs:1181 print_rendered(render_event(&event))) forward all events without filtering. Record file:line evidence for the knowledge file.**
- [x] 2. **Verify the GUI chat rules (frontend) — frontend/src/hooks/agentEventReducer.ts reduceToolCallStart (line 486): the ONLY name-based suppression is MEMORY_TOOLS gated by agent.showMemoryActivity (find the default in src/config/general.rs — show_memory_activity, expected default true); neverGroups (line 545: shell/search/search_read/browser) disables card merging only, never visibility; reduceToolResult logs every result to the Output tab; note capTranscript rolls old entries off in very long sessions and consecutive same-name calls (read_files) merge into one card with many chips.**
- [x] 3. **Verify the console REPL rules — src-tauri/src/console.rs render_event (line 241): ToolCallStart renders '→ {name}' for EVERY tool (line 252), ToolResult via render_tool_result (line 254); the Silent set is exactly {Started, ToolCallArgDelta, ContextUsage, PromptDispatched, Phase} (lines 243-336) — no tool call is Silent; render_event is pure (no config access), so show_memory_activity does NOT hide memory calls in the console.**
- [x] 4. **Map the non-echoing tool activity — document the by-design exceptions with code refs: (a) auto-delegated graph/memory lookups inside search/search_read (src/tool/agent/search.rs ~1071-1113, src/tool/agent/codegraph.rs ~78-124): the internal lookup emits no ToolCallStart — the answer rides inline prefixed 'AUTO-DELEGATED to …'; (b) sub-agent tool calls echo into the child's transcript only (parent sees spawn_agent + ChildFinished); (c) internal harness ops echo via dedicated events (MemoryRecalled, VisionDescribe/VisionDescribed, CompactStarted/Compacted), not as tool calls.**
- [x] 5. **Write the echo-map knowledge file — create .coding/knowledge/tool-call-echo-map.md: the verified echo map (emission → GUI ToolCard / memory entry, console '→ name' line, Output-tab result log), the exception list, and the direct answer: NO LLM tool call is suppressed from the echo; graph/search cards are rare because search auto-delegation absorbs symbol lookups (the graph work happens inside search calls — e.g. this session: many search calls auto-delegated, zero direct graph_* calls) and read_files is simply the workhorse tool.**
- [x] 6. **Record, test, commit, close the backlog item — memory_write 'SPEC: tool-call echo map' pointing at the knowledge file; run cargo test (constitution compliance; no source changes, expect green); mark backlog c8531c34 done in .coding/backlog.jsonl via the approval-gated shell residual (status done + note: 'Done — research: no tool call is suppressed from the echo; exceptions = memory tools behind show_memory_activity=false (GUI only), auto-delegated lookups inside search, sub-agent transcripts; see .coding/knowledge/tool-call-echo-map.md'); commit the knowledge file + backlog edit on wt/agenticcoding.**
