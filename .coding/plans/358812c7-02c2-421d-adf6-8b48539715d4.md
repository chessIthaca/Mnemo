# Plan: Patch reviewer instructions in agent.md

## Goal
Patch the reviewer instructions in agent.md so the reviewer always reviews ALL uncommitted changes (not just its own task's) and writes its findings to a report file under .coding/reviews/ for the main agent to act on and commit.

## Context
agent.md 'Standard plan closing sequence' step 2 already says the reviewer reviews ALL uncommitted changes and is read-only, but it doesn't tell the reviewer to write its findings to a file, and the reviewer agent complained about scope (it thought only its own task's changes were in scope). The user wants: (a) explicit instruction that the reviewer reviews ALL changes in the working tree regardless of which plan produced them, and (b) the reviewer writes its findings to a report file under .coding/reviews/ (the established convention, see .coding/reviews/CONSOLIDATED.md) so the parent agent reads the file, fixes, and commits. Reviewer stays read-only with one exception: writing the report file into .coding/reviews/. The eslint hang mentioned by the user was an environment hiccup (fixed by restart), not something to encode.

## Steps
- [x] 1. Update agent.md step 2 (Review): state the reviewer reviews ALL uncommitted changes in the working tree (git status / git diff HEAD) — including changes from other plans/sessions, not just its own task — and that it must write its findings to a report file in .coding/reviews/ (e.g. .coding/reviews/<plan-or-date>-review.md) as its only allowed write; otherwise read-only. Include the report's expected contents (findings by severity: correctness, bugs, security, constitution compliance, plus an explicit 'no findings' if clean).
- [x] 2. Update agent.md step 3 (Act on findings): the main agent reads the report file from .coding/reviews/, addresses real issues (or justifies skipping), re-runs cargo test if code changed, and includes the report file in the commit.
- [x] 3. Run cargo test to confirm the workspace is healthy (no code changes expected, but constitution requires it).
- [x] 4. Spawn a reviewer subagent to review all uncommitted changes per the updated instructions; act on findings.
- [x] 5. Commit to the current feature branch.
