# Plan: Spike B: child webview with its own CDP port (9223) via additional_browser_args

## Goal
Spike option B: can a Tauri add_child webview get its OWN CDP debug port (9223) via WebviewBuilder::additional_browser_args, separate from the parent's 9222? If yes → embedded child + agent inspection both work (the agent's BrowserManager connects to 9223). Also tests whether the blank-page bug is the port-conflict failing env creation (a different port would fix both rendering + CDP). Throwaway change to the real app; no commit.

## Kind
research

## Context
SPIKE RESULT (definitive): Adding `--remote-debugging-port=9223` to the child webview's additional_browser_args CRASHES THE UI immediately on app launch. The user reported the crash. This is the answer to the research question: a second WebView2 environment in the same process CANNOT bind its own debug port — it crashes rather than silently failing. NO-GO for Spike B.

Reverted: the additional_browser_args chain was removed from src-tauri/src/ipc/browser_webview.rs; working tree is clean (browser_webview.rs back to original, no commit — throwaway research spike). cargo test --lib = 935 passed, 0 warnings.

Verdict recorded to semantic memory (id e5e064c9). Remaining embedded+inspection options: A (iframe + CDP attach, PROVEN/shipped), C (borderless WebviewWindow, compile-proven). B is dead.

## Steps
- [x] 1. **Add the child's own debug port to the builder** — In src-tauri/src/ipc/browser_webview.rs browser_webview_ensure: chain `.additional_browser_args("--disable-extensions --remote-debugging-port=9223")` onto the WebviewBuilder (after WebviewBuilder::new, before add_child). The method is WebviewBuilder::additional_browser_args (tauri-2.11.5/src/webview/mod.rs:956) — sets webview_attributes.additional_browser_args, which wry passes to CoreWebView2EnvironmentOptions::set_additional_browser_arguments (wry webview2/mod.rs:327). This gives the child a FRESH environment (no with_environment) with its OWN port 9223, avoiding the 9222 conflict. Add a comment marking this as a SPIKE (throwaway). Build: `cargo build -p myharness-app`.
- [x] 2. **Add the child's own debug port to the builder** — DONE (then reverted). Chained `.additional_browser_args("--disable-extensions --remote-debugging-port=9223")` onto the WebviewBuilder in browser_webview_ensure.
- [x] 3. **Run the app + probe both ports** — RESULT: the app CRASHED on launch. The user reported the UI crashed. This is the definitive probe result: a second WebView2 env with its own debug port in the same process crashes the app. 9223 never came up because the app died. NO-GO.
- [x] 4. **If 9223 is GO: test agent attach** — SKIPPED (9223 was NO-GO; the app crashed).
- [x] 5. **Record go/no-go + revert** — DONE. Verdict (NO-GO) recorded to semantic memory (id e5e064c9). Spike change reverted in browser_webview.rs; git diff shows browser_webview.rs clean. cargo test --lib = 935 passed, 0 warnings.
