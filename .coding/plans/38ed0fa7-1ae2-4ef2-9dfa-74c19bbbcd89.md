# Plan: Merge hygiene overhaul — branch-status digests, merge-time memory cleanup, stale-marker audit

## Goal
Merge hygiene overhaul (backlog #87): (1) finish digests carry a branch-only hint when landing on a non-main branch; (2) merge_to_main skill supersedes branch-status memories after a merge; (3) compiled prompt encodes the default-assumption rule (no memory about a feature/bug ⇒ assume it is in main); (4) audit and supersede existing stale UNMERGED/ACTIVE markers.

## Kind
implementation

## Context
Backlog #87. BRANCHING DECISION (unattended): main (e21717a) does NOT contain the semantic memory system — it lives on feat/semantic-memory-system whose tip e9a0c44 IS the runner's pre-item checkpoint for #87 (the orchestrator staged this item on that branch deliberately, matching the recent 'same git branch' stacking pattern). create_plan initially forked feat/merge-hygiene from main; I repointed it via `git checkout -f -B feat/merge-hygiene feat/semantic-memory-system` (dirty .coding backlog/stack files were safe to discard: the #87 in_flight flip is committed in e9a0c44 and the workflow rewrites stack.json from memory). So feat/merge-hygiene is stacked on feat/semantic-memory-system @ e9a0c44. Anchors (valid on this tree): src/memory/finish_capture.rs capture_finish(store, plan, plan_id, plans_dir, now) line 55, pointer built lines 76-87, bug pointer 117-119, tests from 204; src/tool/workflow/plan.rs FinishTool line 772, capture callsite ~966-996, create_plan branch forking uses wf.project_root() ~line 357; src/agent/prompt.rs MEMORY_RECORDS line 230, hygiene paragraph 245-251, content tests ~814/914/938; .coding/skills/merge_to_main.toml — memory tools always available inside skills (SkillSpec doc + src/tool/mod.rs:37/166), no tools-list change needed. 19 stale 'SHIPPED ... unmerged' semantic memories (2026-08-19..22) + working-tier ACTIVE/STEP markers to audit in step 6.

## Steps
- [x] 1. **Branch-only hint in finish digests** — In src/memory/finish_capture.rs: add a `repo_root: Option<&Path>` param to capture_finish and a helper `branch_hint(repo_root) -> Option<String>` that runs `git branch --show-current` + `git rev-parse --short HEAD` in repo_root (mirror prepare_branch's git-call style in src/tool/workflow/plan.rs; failure/detached/main/master => None). When Some, append ` · branch <name> @ <sha> (unmerged — on this branch only until merged)` to BOTH the PLAN and BUG digest pointer lines (budgeted_digest keeps pointers intact, gist truncates). In src/tool/workflow/plan.rs FinishTool (~line 966-977), get the root from the workflow (project_root(), same as create_plan's branch forking at ~line 357) and pass it through. Tests in finish_capture.rs: hint present on a feature branch (git fixture like plan.rs's git_project), absent on main, absent without a git repo, digest still ≤400/≤600 with the hint.
- [x] 2. **merge_to_main skill memory-cleanup step** — Edit .coding/skills/merge_to_main.toml: insert a new step after 'VERIFY THE MERGED TREE BUILDS' / branch deletion — once the merge commit exists on main, clean up branch-status memories: use memory_list (prefix SHIPPED/ACTIVE/STEP MARKER) and memory_recall (branch name + tip commit) to find records citing the merged branch, then memory_supersede each with a short successor recording 'MERGED into main at <merge commit sha>'; skip cleanly when nothing matches. Note in the step + header comment that memory tools are always available inside a skill (SkillSpec invariant), so no tools-list change. Renumber the steps.
- [x] 3. **Default-assumption rule in compiled prompt** — In src/agent/prompt.rs, append to the 'Hygiene habits' paragraph of MEMORY_RECORDS (~line 245): a lean branch-status default rule — a feature/bug with no live memory saying otherwise is assumed to be IN main; 'unmerged' hints are branch-scoped and superseded at merge time, so absence of a marker means merged (verify with git_log/git_show when it matters). Keep it short (the stable head is prefix-cached). Run the prompt tests (lines ~814/914/938 assert content) and update/extend if needed.
- [x] 4. **Docs sync check** — Check README.md and PLAN.md for sections documenting the memory system, finish digests, or the merge_to_main skill; update them to mention the branch-only hint, the merge-time memory cleanup step, and the default-assumption rule. Skip files that don't cover these topics (reviewer checks documentation sync).
- [x] 5. **cargo test green** — Run `cargo test` unpiped at the repo root and read the result lines (constitution: do not trust piped exit codes). Must be fully green and warning-free under #![deny(warnings)] — fix any warning at its root cause, never #[allow].
- [x] 6. **Audit + supersede stale branch-status memories** — Runtime cleanup (uses memory/shell tools, no code): for each of the 19 'SHIPPED ... unmerged' semantic memories (memory_list prefix SHIPPED), extract the cited commit and run `git merge-base --is-ancestor <sha> main` (+ `git branch --merged main` for the branch names); for each MERGED one, memory_supersede with a successor titled 'SHIPPED: ... — MERGED in main' recording the verification date + that the cited commit is an ancestor of main. Also list working-tier ACTIVE:/STEP MARKER: memories (memory_list, tier working) and supersede any stale ones. Leave genuinely-unmerged records live. Report counts in the final summary.
