# Plan: Verify done-orphan: Anthropic 3rd-breakpoint caching already landed

## Goal
Confirm this re-dispatched item's work — the 3rd cache_control breakpoint on the growing conversation history + the volatile-tail relocation (backlog fe3fcd55, provider-comms review finding 3) — is already implemented, tested, and merged (plan ee33d615, commit d5cfd52, merged into main at 2769fca on 2027-01-10, round-2 review PASS), so the item closes without new code; record the done-orphan verification.

## Kind
research

## Context
Done-orphan evidence gathered during planning: (1) The SPEC record (.coding/knowledge/spec/2027-01-07-anthropic-conversation-history-caching-3rd-break.md) documents plan ee33d615 (backlog fe3fcd55 — THIS item, provider-comms review finding 3): the volatile system tail (workflow state/progress/memories + CONTEXT_FOOTER) is relocated into the final message's content as trailing text blocks, and breakpoint 3 (cache_control ephemeral) lands on the final message's last real block (walking back past thinking/redacted_thinking, respecting the 20-block tool-use lookback); the tail is re-appended only at build time, never baked into stored history (byte-stable cached prefix). (2) All four SPEC-named tests exist in the current tree: volatile_tail_relocates_into_final_message_and_breakpoint_lands_on_last_real_block (src/provider/anthropic.rs:2290), breakpoint_lands_on_last_tool_result_block_with_tail_after_it (:2348), breakpoint_lands_on_final_block_when_no_volatile_tail (:2393), breakpoint_stays_within_lookback_window_on_long_tool_history (:2416). (3) git log confirms commit d5cfd52 ("Anthropic conversation-history caching: relocate volatile tail, add 3rd breakpoint") in the current branch's history; merge commit 2769fca ("Merge wt/agenticcoding: Anthropic conversation-history caching (3rd breakpoint)", 2027-01-10) landed it in main with both review reports (round 1: 1 low fixed; round 2: PASS) and the DECISION record ...anthropic-automatic-caching-rejected-manual-brea.md — fix-instruction 3 (evaluate automatic caching) was DONE and rejected (the docs' varying-block trap); fix-instruction 2 (optional 4th breakpoint) was superseded by the chosen shape (the 3rd breakpoint already covers the current turn's real content). (4) The acceptance's runtime observation (cache_read_input_tokens > 0 growing with the conversation) became possible when the "Parse Anthropic cache usage fields" item (648051bf) landed at 6cd0ef9 (previous task this session, plan 2972d681). This is the exact re-dispatch scenario the user complained about on 2027-01-07 (backlog 6c6966b9, Run-All done-orphan guard).

## Steps
- [x] 1. **Verify the implementation + tests are present in the current tree** — src/provider/anthropic.rs: build_request_json carries the volatile-tail relocation + breakpoint 3 on the final message's last real block (walks back past thinking/redacted_thinking, respects the 20-block tool-use lookback); all four SPEC-named tests exist: volatile_tail_relocates_into_final_message_and_breakpoint_lands_on_last_real_block (:2290), breakpoint_lands_on_last_tool_result_block_with_tail_after_it (:2348), breakpoint_lands_on_final_block_when_no_volatile_tail (:2393), breakpoint_stays_within_lookback_window_on_long_tool_history (:2416). Evidence already gathered during planning (graph + search hits).
- [x] 2. **Verify the merge lineage + acceptance precondition** — commit d5cfd52 is an ancestor of the current branch via the merge 2769fca into main (2027-01-10; round-2 review PASS); the automatic-caching evaluation (fix-instruction 3) was done and rejected (DECISION record .coding/knowledge/decision/2027-01-07-anthropic-automatic-caching-rejected-manual-brea.md); the acceptance's runtime observation is now possible because the cache-fields item (648051bf) landed at 6cd0ef9 (previous task this session).
- [x] 3. **Run the Anthropic provider tests** — cargo test --lib anthropic over src/provider/anthropic.rs (unpiped, read $LASTEXITCODE per project constitution) to confirm the landed implementation is green in the current tree.
- [x] 4. **Record the done-orphan verification** — memory_amend the SPEC record (.coding/knowledge/spec/2027-01-07-anthropic-conversation-history-caching-3rd-break.md) appending a dated note: the item was re-dispatched by run-all on 2027-01-12 and verified as a done-orphan (implementation + all four tests present, merged at 2769fca, acceptance observable since 6cd0ef9); no new code needed.
