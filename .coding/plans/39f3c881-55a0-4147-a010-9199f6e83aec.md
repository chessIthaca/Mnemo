# Plan: Architecture review remediation

## Goal
Fix all Critical/High/Medium findings from the 2026-04-08 four-perspective architecture review in severity order: unattended correctness → approval contract → performance quick wins → Run-All/workflow UX → multi-agent plan policy → maintainability splits → structural hardening. No drive-by refactors outside each phase's scope.

## Context
Phases 0-4 complete and committed (Run-All exclusivity, ToolFilter dispatch, shell hardening, git auto-approval narrowing, approval preview/DenyAll/multi-agent UX, streaming perf, output/search/recall taxes, diff size guard, workflow event completeness, Run-All halt/checkpoint UX, plan/skill UI, main-agent-only plan ownership). Joint synthesis written (step 29). Phase 6a (spawn_blocking) is NOT done — was erroneously checked, corrected. Phases 5-6 remain: ipc/store splits, IPC contract hardening, structural perf, safety hardening, docs. cargo 523 + vitest 24 green.

## Steps
- [x] 1. Phase 0a — Fix Run-All terminal exclusivity (Quality C1): Error+Finished double-resolve; failing test first; exclusive terminal outcomes in turn/events/run_all
- [x] 2. Phase 0b — Enforce ToolFilter at dispatch (Quality H1): re-check workflow.allowed_tools in execute_tool_call/dispatch; unit tests for Planning/Skill denial
- [x] 3. Phase 0c — Harden shell (Quality H2/H3, Perf M3 partial): sandbox-validate cwd; timeout+kill on expiry/interrupt; tests for .. and outside paths
- [x] 4. Phase 0d — Narrow AutoApproveProject git auto-approval (Quality H4): commit/checkout/stash/branch-mutate require prompt; keep merge/push never_auto; tests
- [x] 5. Phase 0 close — cargo test, spawn read-only reviewer on ALL uncommitted changes, fix findings, commit Phase 0
- [x] 6. Phase 1a — Populate ApprovalPreview on live path (Quality H5): call prepare_for_approval for file tools in dispatch before ApprovalRequest; pure preview; tests
- [x] 7. Phase 1b — Consume ApprovalPreview in FE (UI H3, Perf H4 partial): DiffViewer/ApprovalPrompt prefer event.preview; keep args fallback; size guard on client LCS
- [x] 8. Phase 1c — DenyAll complete (UI H2, Quality M1): turn-level latch skipping remaining tool calls; FE Deny all button + shortcut; honor approve() bool; deny-on-/clear
- [x] 9. Phase 1d — Multi-agent approval visibility (UI H1/H4): tab badge when pendingApproval; optional non-active banner Switch & review; do not auto-steal focus
- [x] 10. Phase 1 close — cargo test + FE checks, reviewer on all uncommitted, fix findings, commit Phase 1
- [x] 11. Phase 2a — FE streaming perf quick wins (Perf H3/M1, UI L1/L2): rAF-batch reasoning_delta + tool_call_arg_delta; narrow Zustand selectors; smart stick-to-bottom scroll
- [x] 12. Phase 2b — Tool output + search + recall/token taxes (Perf M3/H2/H1-partial/M5): cap shell/git output; search early-exit at MAX_MATCHES; cache auto-recall per user msg; hoist query lowercase; reuse token count when possible
- [x] 13. Phase 2c — Diff size guard until Rust preview primary (Perf H4): computeDiff n*m threshold → truncated/heuristic path
- [x] 14. Phase 2 close — cargo test + FE checks, reviewer, fix, commit Phase 2
- [x] 15. Phase 3a — Workflow event completeness (Quality M2): emit WorkflowStateChanged for update_plan + skill_start/end/abandon_skill
- [x] 16. Phase 3b — Run-All halt/checkpoint UX (Quality M3/M4, UI M8): preserve checkpoint sha; PausedForApproval or explicit rollback policy; document success criterion (Finished vs Complete)
- [x] 17. Phase 3c — Plan/skill UI + safety-mode path (UI M3/M5/M2/M7): PlanProgress depth/parents/skill; auto-reveal Diff on file approval; StatusBar safety persist-or-label; backlog ▶ semantics
- [x] 18. Phase 3 close — cargo test + FE, reviewer, fix, commit Phase 3
- [x] 19. Phase 4 — Multi-agent plan ownership (Maint H2): implement chosen policy (default recommendation: main-agent-only plan mutations OR per-agent plan namespaces); align factory/workflow/PLAN.md/tests
- [x] 20. Phase 4 close — cargo test, reviewer, fix, commit Phase 4
- [x] 21. Phase 5a — Split ipc/commands.rs + unify spawn (Maint H1/M3/M4): domain modules agent/settings/files/backlog/spawn; one spawn_agent_shared used by main+UI+IpcSpawner
- [x] 22. Phase 5b — Split useAgentStore + pure reducers + vitest (Maint H3/M9, Quality M7, UI M9): runtime/appearance/backlog/chrome stores; table-test every SerializableAgentEvent; wire npm test
- [x] 23. Phase 5c — IPC contract hardening (Maint H4): golden JSON fixtures or specta/ts-rs for events+key DTOs; stop untyped Value for stable settings where practical
- [x] 24. Phase 5 close — cargo test + npm test, reviewer, fix, commit Phase 5
- [x] 25. Phase 6a — Structural perf (Perf structural): spawn_blocking for FS/search/sandbox; optional Rust delta coalesce; memory read pool if needed
- [x] 26. Phase 6b — Safety hardening leftovers (Quality M5/M6/M3-strict): expand protected .coding writes; keys.toml Windows ACL; optional Run-All Complete-gated success
- [x] 27. Phase 6c — Docs + remaining debt (Maint M1/M2/M6/M7/L*): PLAN.md as-built refresh; right-panel registry; tool registration groups+name test; optional openai/memory splits
- [x] 28. Phase 6 close — full cargo test + npm test, final reviewer on all uncommitted, fix, commit Phase 6
- [x] 29. Write joint synthesis already done — mark review plan complete after confirming synthesis file; stop only if user wants to re-prioritize before Phase 0 starts
