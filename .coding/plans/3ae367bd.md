# Plan: Per-note toggles for steering notes in the Chat config

## Goal
Every steering-note kind in tool output becomes individually enable/disable-able from Settings → Chat: a display-layer registry keyed by the 10 stable MarkerKind labels plus the auto-delegated family (which today's [ui].show_delegation_notes toggle covers, default hidden), threaded through the full config stack ([ui.steering_notes] TOML → settings_dto patch → IPC DTO → store → Chat section checkboxes → the two Message.tsx filter sites) without ever modifying the tool-result text the model sees.

## Kind
implementation

## Context
Backend truth: MarkerKind::marker() (src/agent/steering_stats.rs:113-126) gives each kind a stable output substring (public tool-output contract), label() (:166-179) the stable kebab keys: search-nudge ("is an indexed symbol" = SEARCH_NUDGE_MARK :279), shell-tip ("TIP: for file-content search"), graph-miss ("No symbols matched"), recall-rider ("RECALLED CONTEXT"), read-nudge ("SYMBOL NUDGE:"), literal-tip ("TIP: pattern has no regex metacharacters" — literal_tip() search.rs:809), known-memory-hit ("known memory hit:" search.rs:1104), consolidation-due ("working-memory events accumulated this session"), shell-redirect ("TIP: output redirection detected"), edit-stale-read ("Re-read the file" EDIT_STALE_READ_MARK :288). The two AUTO-DELEGATED display notes: graph delegation (search.rs:2433/2877 — contains the search-nudge marker) and memory delegation (memory_delegation_block search.rs:1152-1182 — NO marker substring, so the registry keeps a dedicated "auto-delegated" family entry, prefix "AUTO-DELEGATED", which is exactly what [ui].show_delegation_notes hides today). Display filter: frontend/src/lib/delegationNotes.ts (isDelegationNote/stripDelegationNotes, prefixes ["note: AUTO-DELEGATED", "AUTO-DELEGATED"]), applied at frontend/src/components/chat/Message.tsx (import :18, store read :703, strip :811; grep for other isDelegationNote sites). Plumbing: UiConfig src/config/general.rs:388 (show_delegation_notes :428 default false, save :1201), patch src/config/settings_dto.rs:228 + :551-552, src-tauri/src/ipc/settings.rs:810, src-tauri/src/ipc/contract_fixtures.rs:254, frontend/src/lib/tauri.ts:465 (UiSettings) + :533 (patch), frontend/src/lib/ipc-fixtures/dto-get-settings.json, useAgentStore.ts:283/:661/:884, App.tsx hydration (settings.ui.show_delegation_notes try block), ChatSection.tsx:172-180, ChatDraft/serializeChat settings/types.ts:379-394, tests: delegationNotes.test.ts, ChatSection.test.ts:95-135, types.test.ts, ipc-contract.test.ts, config::general round-trips. Decisions on record: display-layer filter only (DECISION 950fdba5 — backend text stays intact incl. the re-issue escape hatch); defaults preserve today's behavior exactly (only auto-delegated hidden; "note: known memory hit:" stays visible); line-based stripping like today (multi-line blocks strip their header line — documented limitation); legacy show_delegation_notes stays parse/serialize/DTO and seeds auto_delegated's default. Verify: cd frontend; npm run test; npm run build; repo root cargo test.

## Steps
- [x] 1. **Backend: [ui.steering_notes] registry + DTO + IPC** — src/config/general.rs: add pub SteeringNotesCfg struct (11 Option<bool> fields: auto_delegated, search_nudge, shell_tip, graph_miss, recall_rider, read_nudge, literal_tip, known_memory_hit, consolidation_due, shell_redirect, edit_stale_read; #[serde(default)] on the struct usage; each Option serializes only when Some, following the existing UiConfig field pattern) with doc comments; add UiConfig.steering_notes: SteeringNotesCfg; add pub fn effective_steering_note_visible(&self, key: &str) -> bool — Some(b) wins, None falls back: auto_delegated → legacy show_delegation_notes (default false), the other 10 → true; unknown key → true. Keep show_delegation_notes parse/serialize/DTO untouched (seed only). src/config/settings_dto.rs (:228 area + apply at :551): add steering_notes: Option<SteeringNotesPatch> (11 Option<bool>) applied per-field to general.ui.steering_notes. src-tauri/src/ipc/settings.rs (:810): UiSettings DTO gains steering_notes as the 11 RESOLVED booleans (call effective_steering_note_visible per key — the frontend never re-derives defaults). Update src-tauri/src/ipc/contract_fixtures.rs:254 (resolved defaults: auto_delegated false, rest true). Rust tests: config::general (absent table → defaults; show_delegation_notes=true seeds auto_delegated; explicit per-field values win; TOML round-trip), settings_dto patch (set/clear semantics), ipc mapping. Doc comments on all public items; warning-free build.
- [x] 2. **Frontend registry + filter + store + hydration** — Generalize frontend/src/lib/delegationNotes.ts IN PLACE (keep the filename): export type SteeringNoteKey and STEERING_NOTES: readonly {key, marker, label, description, defaultVisible}[] — the 11 entries: auto-delegated (marker "AUTO-DELEGATED", defaultVisible FALSE, label "Auto-delegation notes (search → graph/memory)"), search-nudge ("is an indexed symbol"), shell-tip ("TIP: for file-content search"), graph-miss ("No symbols matched"), recall-rider ("RECALLED CONTEXT"), read-nudge ("SYMBOL NUDGE:"), literal-tip ("TIP: pattern has no regex metacharacters"), known-memory-hit ("known memory hit:"), consolidation-due ("working-memory events accumulated this session"), shell-redirect ("TIP: output redirection detected"), edit-stale-read ("Re-read the file") — the 10 kinds defaultVisible true. Comment: markers mirror MarkerKind::marker() in src/agent/steering_stats.rs (prose mirror — same cross-root reasoning as windowRestore); auto-delegated is the display family the legacy toggle covered (both delegation notes incl. memory_delegation_block which carries no kind marker). Export hiddenKeysFromConfig(cfg?: Partial<Record<SteeringNoteKey, boolean>>): SteeringNoteKey[] (entries explicitly false) and stripSteeringNotes(output: string, hidden: ReadonlySet<string>): string (split \n, drop lines containing any hidden entry's marker substring, keep everything else verbatim — line-based like today). Keep isDelegationNote/stripDelegationNotes as back-compat wrappers delegating to the {auto-delegated} entry (legacy two-prefix behavior preserved). useAgentStore.ts: replace showDelegationNotes/setShowDelegationNotes (:283/:661/:884) with hiddenSteeringNotes: SteeringNoteKey[] (default ["auto-delegated"]) + setHiddenSteeringNotes(keys). App.tsx hydration (the settings.ui.show_delegation_notes try block): setHiddenSteeringNotes(hiddenKeysFromConfig(settings.ui.steering_notes)) — keep the try/catch. Message.tsx: import stripSteeringNotes; the store read (:703) → hiddenSteeringNotes selector; the strip site (:811) → stripSteeringNotes(call.result.output, hiddenSet); grep for other isDelegationNote uses and update each. Rework frontend/src/lib/delegationNotes.test.ts: unit cases for ALL 11 keys (each strips its marker line and leaves other content byte-identical), the user-reported TIP line strips under literal-tip, auto-delegated preserves the legacy two-prefix behavior ("note: AUTO-DELEGATED …" and "AUTO-DELEGATED to memory …"), back-compat wrappers, hiddenKeysFromConfig (absent → defaults; false entries only), plus the existing static-shape assertions that Message.tsx routes results through the filter (update the pinned substrings).
- [x] 3. **Chat section UI + save wiring + contract tests** — frontend/src/components/settings/types.ts: ChatDraft.showDelegationNotes → steeringNotes: Record<SteeringNoteKey, boolean> (all 11 keys present; build from STEERING_NOTES defaults when the backend field is absent — older-backend fallback); serializeChat unchanged (JSON stringify). frontend/src/components/settings/sections/ChatSection.tsx: replace the single showDelegationNotes checkbox (:172-180) with a "Steering notes" sub-block driven by STEERING_NOTES — one checkbox per entry (label = entry.label, hint = entry.description), bound to draft.steeringNotes[key] via patch({steeringNotes: {...draft.steeringNotes, [key]: e.target.checked}}); the load mapping reads the resolved settings.ui.steering_notes; the save patch sends ui.steering_notes (all 11 booleans). frontend/src/lib/tauri.ts: UiSettings type (:465) gains steering_notes: Record<SteeringNoteKey, boolean> (snake_case keys); the saveSettings patch type (:533) gains steering_notes?: Partial<...>. Update frontend/src/lib/ipc-fixtures/dto-get-settings.json (resolved defaults: auto_delegated false, the rest true) and frontend/src/lib/ipc-contract.test.ts expectations. Update ChatSection.test.ts (swap the showDelegationNotes serialize case :122-124 for a steeringNotes record case; draft-shape assertions) and types.test.ts. All suites must pass: cd frontend && npx vitest run.
- [x] 4. **Docs + full verify** — docs/CONFIGURATION.md: document [ui.steering_notes] — the 11 keys with defaults (auto_delegated false, the rest true), the legacy show_delegation_notes seeding auto_delegated, and that this is display-only (tool-result text the model sees is never modified). docs/FEATURES.md: extend the chat/tool-output bullet(s) (~line 42 tool strategy area + the chat surface section) with the per-note toggles sentence, replacing the single-toggle mention. Verify: cd frontend && npm run test (all suites green incl. reworked delegationNotes/ChatSection/ipc-contract) and npm run build (tsc warning-free); repo root cargo test (config/dto/ipc suites green, warning-free). Fix any failure before completing the step.
