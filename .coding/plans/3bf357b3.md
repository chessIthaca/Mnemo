# Plan: Backlog 2980ca67 close-out: verify provider-correct model display (serving endpoint on the wire)

## Goal
Close backlog 2980ca67 (provider-correct model display): verify the shipped serving-endpoint fix end-to-end, run its tests, mark done, land the bookkeeping with a review round.

## Kind
implementation

## Context
Backlog 2980ca67 dispatch (run-all, unattended): "model display shows the wrong provider when the name matches in two providers; shows the first provider." VERIFIED ALREADY SHIPPED — commit 0ce7c01 (backlog batch 554010fd T2, merged to main via 0e12ffb, in current branch history): the backend carries the per-agent SERVING endpoint (LlmClient::provider_name → AgentLoop resolved_provider mirror + effective_provider_name → AgentInfo.provider + ModelChanged.provider, contract fixtures byte-stable) → frontend agentProviders map with wire-first StatusBar label (wire ?? endpointForModel ?? provider; absent provider CLEARS, no stale label) — the commit message names this exact symptom. Dedicated tests landed (frontend/src/hooks/useAgentStore.test.ts +58, preview +1, ipc-contract +1, contract_fixtures.rs +16) and a full review round PASSED (2026-09-16, deepseek-v4-pro, 0 findings). Item is a verify-and-close, same class as 41f39672 last round. NOTE: live text searches with brace globs still return 0 files — the running app binary predates F7 (known, recorded BUG memory); use single-extension or no globs for live probes this session. Plan: implementation kind (verify → close → commit → reviewer round), keeping the review gate honest rather than research-skip, matching the 41f39672 close-out pattern that reviewed cleanly.

## Steps
- [x] 1. **Verify the wire-first provider fix** — Read the shipped implementation chain and confirm each link: src/agent/loop_impl.rs resolved_provider/effective_provider_name; src/runtime/channels.rs ModelChanged.provider (skip_serializing_if None); src-tauri/src/ipc/agent.rs AgentInfo.provider; frontend/src/hooks/agentEventReducer.ts + useAgentStore.ts agentProviders map (absent provider CLEARS); frontend/src/components/layout/StatusBar.tsx wire-first label (wire ?? endpointForModel ?? provider). Then run the covering tests: npm test --workspace frontend -- useAgentStore (both test files) and -- ipc-contract. Expect all green — any gap becomes the item's real work and gets fixed.
- [x] 2. **Close the backlog item + bookkeeping** — backlog_status 2980ca67 → done (note: shipped in backlog batch 554010fd T2 / commit 0ce7c01, serving endpoint on the wire; verified + tests green this session; review 2026-09-16 PASS). No new SPEC needed — the batch commit already landed .coding/knowledge/spec/...serving-endpoint-on-the-wire...; check it exists and mentions T2 (read the file). Full cargo test + the frontend suite once more as the step gate. Commit the backlog flip + plan file to wt/agenticcoder.
- [x] 3. **Review round + finish** — Spawn the read-only reviewer (deepseek-v4-pro) over the verification commit: confirm the shipped-fix claims (the chain above is genuinely wired for the duplicate-model-id scenario, tests exist and pass), the backlog close is consistent, and no tree changes beyond bookkeeping. Fix any findings, re-run tests, commit. finish with the PASS report to mark the task done.
