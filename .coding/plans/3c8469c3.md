# Plan: Announce image parsing (vision fallback) in the chat transcript with expandable query + response

## Goal
Announce the vision-model image-description phase in the chat transcript with a collapsible card that shows the query sent to the vision model and the response it returned — eliminating the silent pause on image prompts with text-only main models.

## Kind
implementation

## Context
Backlog adc689d8 (user: "There is a pause sometimes with prompts that contain images. Do we need some kind of message saying 'image parsing' and I would love to see what the query and response is when I uncollapse"). ROOT CAUSE: src/runtime/agent.rs:436-456 — text-only provider + vision fallback describes each pasted image via the vision model BEFORE run_turn; AgentEvent::Started only fires inside run_turn (src/agent/turn.rs:237), so the UI is silent for the whole vision round-trip. FIX PATTERN: mirror CompactStarted/Compacted (channels.rs AgentEvent ~306-317, SerializableAgentEvent ~436-437, into_serializable ~631, round-trip test ~814-825; console.rs render_event ~353; src-tauri/src/ipc/events.rs log-label match ~53). Frontend: types.ts event union (~109), agentEventReducer.ts switch (~1125) + reducers, Message.tsx collapsible card pattern = MemoryEntryCard (Message.tsx:101-184) with arePropsEqual case (35-85). The attachment path uses question=None → DEFAULT_DESCRIBE_PROMPT (src/tool/agent/image_tools/mod.rs:122-132). No settings toggle — one low-noise card per image (memory activity got a toggle because it is chatty; this is not). Multimodal providers send images inline and take no vision round-trip, so no card there. Pairing invariant: every VisionDescribe gets exactly one VisionDescribed (no interrupt path exists between them — Stop only acts via drive_turns/cmd_rx).

## Steps
- [x] 1. **Backend: add the two announcement events** — src/runtime/channels.rs: add `AgentEvent::VisionDescribe { index: usize, total: usize, query: String }` (before a vision-model description begins; index is 1-BASED, doc comment says so) and `AgentEvent::VisionDescribed { index: usize, total: usize, success: bool, description: String }` (the vision model's answer, or the error text with success=false), near CompactStarted (~306-317). Add matching `SerializableAgentEvent::VisionDescribe { index: u32, total: u32, query: String }` + `VisionDescribed { index: u32, total: u32, success: bool, description: String }` variants (~436) with doc comments pointing at the AgentEvent variants, and into_serializable mappings (~631). Serde tag derive gives kinds "vision_describe"/"vision_described". Add a round-trip test mirroring the CompactStarted test (~814-825) asserting {"kind":"vision_describe",...} serializes and deserializes.
- [x] 2. **Backend: emit from the attachment vision fallback** — src/runtime/agent.rs (~409-458, the Prompt arm): in the text-only+vision else-branch, before each `describe_image_data_url` call emit VisionDescribe { index: i+1, total: images.len(), query: crate::tool::agent::image_tools::DEFAULT_DESCRIBE_PROMPT.to_string() } on the event channel the same way the code around it sends events (the arm has access to the fanin/sender used by nearby emits — follow the local pattern); after the call resolves emit VisionDescribed { index, total, success, description } carrying Ok(desc) or Err(e) text. Update the 409-417 doc comment to mention the announcements. Extend the existing tests: non_multimodal_with_vision_injects_image_description_text (~2545) must also assert a VisionDescribe with index/total/query precedes the turn events and a VisionDescribed with the canned description arrives; non_multimodal_vision_failure_degrades_gracefully (~2584) must assert VisionDescribed with success=false. Run `cargo test` unpiped (read test-result lines + $LASTEXITCODE, PowerShell).
- [x] 3. **Backend: console + IPC wiring (compilation-guided)** — Under #![deny(warnings)] the new enum variants surface every exhaustive match. src-tauri/src/console.rs render_event (~353): VisionDescribe → Rendered::Lines(["— describing image {i}/{n} via vision model…"]), VisionDescribed → Rendered::Lines(["— image {i}/{n} described" or "— image {i}/{n} description failed: {e}"]); update console tests (~1867 area) if they enumerate kinds. src-tauri/src/ipc/events.rs: add log-label arms in the match at ~53 (e.g. "agent{i} VisionDescribe image {index}/{total}"). tests/contract_fixtures.rs: add ("event-vision_describe", …) and ("event-vision_described", …) cases with fixture JSON files following the existing event-started entry (check the fixture file location/naming next to it and mirror; wire the frontend-side fixtures in frontend/src/lib/ipc-fixtures/ + ipc-contract.test.ts if that is how event fixtures are shared). tests/ipc_bridge.rs (~204) AgentEvent→serial mapping list if it enumerates all kinds. `cargo test` unpiped until green.
- [x] 4. **Frontend: types + store entry + reducer** — frontend/src/lib/types.ts: add to SerializableAgentEvent `| { kind: "vision_describe"; index: number; total: number; query: string }` and `| { kind: "vision_described"; index: number; total: number; success: boolean; description: string }` with doc comments. Add a VisionTranscriptEntry `{ kind: "vision"; index: number; total: number; query: string; description: string | null; success: boolean; running: boolean }` to the TranscriptEntry union (find where transcript entry kinds are defined in useAgentStore.ts and add it there too). frontend/src/hooks/agentEventReducer.ts: reduceVisionDescribe — append the vision entry (running: true, description: null); REPLACE any still-running vision entry on the same agent first (so a fast multi-image burst collapses to one card per image, not duplicates on retry); reduceVisionDescribed — find the running vision entry with matching index (fallback: the last running vision entry) and set description/success/running=false; wire both into the applyAgentEvent switch (~1125). Tests in frontend/src/hooks/useAgentStore.test.ts mirroring the memory_recalled tests (~1053): append, multi-image, complete, failure, replace-running. `cd frontend; npx tsc --noEmit; npm test` unpiped.
- [x] 5. **Frontend: the collapsible card in Message.tsx** — New VisionEntryCard component modeled on MemoryEntryCard (Message.tsx:101-184): collapsed line = chevron + 🖼️ icon + `image parsing ({index}/{total})` + truncated query snippet + running dots / ✓ / ✗; expanded = the full query (label 'vision query') and the response (label 'vision response', whitespace-pre-wrap; when success=false show it in red with a 'description unavailable' label). Neutral slate/border styling (NOT violet — that is memory's), e.g. rounded-lg border border-border bg-bg-tertiary/50 px-3 py-1.5 text-xs. Add `case "vision": return <VisionEntryCard entry={entry} />;` in MessageImpl and an arePropsEqual case comparing index/total/query/description/success/running. Keep Message memoized (default: return false only for unknown kinds). `cd frontend; npx tsc --noEmit; npm test` unpiped.
- [x] 6. **Docs, verification, backlog close** — README.md: add one bullet near the existing tool-card/image feature list (~line 65) describing the image-parsing announcement card (query + response visible when expanded, shown when a text-only main model describes pasted images via the vision model). Run full verification: `cargo test` unpiped from repo root AND `cd frontend; npm run build` unpiped — both green, zero warnings. Mark backlog item adc689d8-22df-4dbb-8860-9082a29f58b7 done via backlog_status (note: shipped the announcement card).
