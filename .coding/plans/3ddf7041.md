# Plan: Unattended run-all Planning auto-continue — mode-gated coverage for premature turn ends

## Goal
Backlog a6a7727a: a premature turn end mid-Planning exploration must not halt a run-all batch. The agent task derives an `unattended` flag from the dispatched prompt's embedded preamble prefix (UNATTENDED_PROMPT_PREFIX — a single-source constant in the core runtime, also used by run_all_prompt to build the steer), and the auto-continue None-arm covers Planning when unattended. Interactive Planning still parks (a deliberate turn end there is usually a question-wait — auto-continuing would self-answer it), and the unattended continue note explicitly tells the model no user input is coming.

## Kind
bug_fixing

## Context
Exploration findings (2027-01-07): (1) The gate — agent.rs None-arm (run_turn_with_retry, :303-331): `streak < MAX_AUTO_CONTINUE(12) && workflow_expects_progress() && !descendants_running && !subagent`; failure parks with Parked evidence (NoWorkExpected when !expects_progress). workflow_expects_progress (loop_impl.rs:1835-1841) = Executing|Reviewing only; single caller run_turn_with_retry (graph-verified). (2) The dispatch context IS harness-visible: run_all_prompt (src-tauri/src/ipc/run_all.rs:243-245) builds the ONE-Prompt dispatch as format!("{RUN_ALL_STEER}\n\n---\n\n{item_text}") — the unattended preamble rides INSIDE the Prompt text (by design since the 2026-08-20 double-turn fix). RUN_ALL_STEER (:210) is used ONLY by run_all_prompt (+ doc refs + the :444 prefix test). run_all.rs:444 already prefix-checks "[backlog run-all — unattended mode]". (3) DECISION — fix direction (b), mode-gated: (a) rejected (no harness-visible question signal exists; a last-message heuristic misfires both ways); (c) rejected (unattended = nobody watching; visible ≠ resolved). In unattended mode the preamble already forbids questions ("Do NOT ask the user any questions... use your best judgment"), so the self-answer concern is moot there; interactive Planning parks stay (user present). (4) The dispatch transitions Complete→Planning BEFORE the prompt (enter_planning_if_complete, SPEC 2026-08-27), so run-all exploration is in Planning. (5) Crate layering: src-tauri depends on the core (src/), not vice versa → the prefix constant lives in the core (channels.rs, beside ParkReason), run_all.rs imports it. (6) Test idiom (agent.rs tests): CountingMockProvider + Arc<AtomicUsize> calls, Workflow::new(tempdir) (fresh = Planning — no create_plan needed), mpsc channels, deadline loops draining fanin_rx, cleanup select!; Parked-event assertion pattern from interrupted_turn_emits_parked_event. (7) Docs to sync: agent.rs None-arm comment + Prompt arm + field doc; loop_impl.rs method doc; channels.rs const doc; run_all.rs merged docs; the HOW record (.coding/knowledge/how/2026-08-31-re-add-dropped-auto-continue-budget-test-via-sma.md — its gate description is policed by prior reviews; update file + memory row). README/PLAN.md: precedent (plan bbd712c9, 2026-09-07 review) — internal mechanism → doc comments only; PLAN.md's only auto-continue mention (Subagent state) unaffected. (8) Tests: root cargo test does NOT compile src-tauri — run BOTH (cargo test; cargo test --manifest-path src-tauri/Cargo.toml), unpiped, read test-result lines. Frontend untouched. (9) Working tree: only the .coding/backlog.jsonl dispatch stamp (expected run-all bookkeeping).

## Steps
- [x] 1. **Reproduce with failing regression test** — Write a regression test that reproduces the defect (constitution: every defect gets a regression test that fails without the fix and passes with it). Run it and confirm it FAILS.
- [x] 2. **Document root cause** — Investigate and document the root cause. memory_write a BUG: record (symptom → root cause → fix + regression test name, ≤600 chars).
- [x] 3. **Minimal fix** — Apply the minimal fix that makes the regression test pass. Do not refactor unrelated code.
- [x] 4. **Verify** — Run the regression test + the full test suite (cargo test unpiped, warning-free). Record the regression test name via update_plan (regression_test field) — finish is blocked without it.

## Bug
A premature turn end mid-Planning exploration parks the session — the auto-continue None-arm gates on workflow_expects_progress() = Executing|Reviewing only — and in run-all/unattended mode nothing resumes it, so the whole run halts (live 2027-01-07, backlog a6a7727a: the session parked until the user nudged "why did you stop?").

## Regression test
unattended_planning_auto_continues
