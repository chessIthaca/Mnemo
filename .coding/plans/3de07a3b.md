# Plan: Tighten tool-card display: frameless, tighter spacing, keep affordances

## Goal
Make the agent-chat tool invocation cards frameless and tighter so more fits on screen, while preserving every interaction — expand/collapse, copy, file-path chips, state indicators, inline images. Frontend-only presentation change in frontend/src/components/chat/Message.tsx + Conversation.tsx.

## Kind
implementation

## Context
Edit sites verified this session against the current tree: frame div Message.tsx:748; header 761; search notes 817; error summary 830; expanded 835; images 853; borderColor/bgColor declared ~725-745 (textColor at 741 stays). Two corrections to the dispatched prompt: (1) its "line 277 container" is the vision card's expanded detail, not the tool list — tool cards are separate transcript entries; (2) the real inter-card gap is Conversation.tsx:97 space-y-4. CallDetail (863-995) already tight, no change needed. Backlog item deee3b8f marked in_flight.

## Steps
- [x] 1. **Remove the tool-card frame** — frontend/src/components/chat/Message.tsx ToolCard, line 748: outer div className `rounded-lg border ${borderColor} ${bgColor} p-[0.5em]` → `py-0.5` (frameless). Read the borderColor/bgColor declarations just above (~lines 725-745) and delete them — they become unused and tsc fails the build on unused locals. Keep textColor (the header span at 761 uses it). State stays scannable via header text color + running dots + ✓/✗.
- [x] 2. **Tighten ToolCard internal spacing** — same file: line 761 header row gap-[0.4em] → gap-[0.25em]; line 817 search notes mt-[0.3em] → mt-[0.15em]; line 830 error summary mt-[0.3em] → mt-[0.15em]; line 835 expanded detail mt-[0.5em] space-y-[0.75em] → mt-[0.25em] space-y-[0.4em]; line 853 images mt-[0.4em] → mt-[0.2em]. CallDetail (lines 863-995) verified already tight (space-y-[0.4em], mb-[0.15em]) — no change there.
- [x] 3. **Tighten the transcript stack gap** — frontend/src/components/chat/Conversation.tsx line 97: space-y-4 → space-y-2 (the real gap between consecutive tool cards; the dispatched prompt's line-277 target was the vision card). This affects all transcript message gaps — flag to the user in the summary; trivially revertible.
- [x] 4. **Affordance sweep** — verify all keep-list items intact in Message.tsx: expand/collapse (role=button span 751-761 incl. keyboard handler), file chips + deep links (771-792, Files tab; Diff tab for file_edit), running dots (800-808), ✓/✗ ×N (810-813), search notes (816-824), one-line error summary (829-833), expanded CallDetail (834-846), image thumbnails (852-857). CodeBlock.tsx and ToolImage.tsx must be untouched. Update any frontend test that asserts the removed frame classes.
- [x] 5. **Test** — cd frontend; npm test (vitest) and npm run build (tsc + vite) must pass; cargo test per project constitution (Rust untouched, should stay green).
