# Plan: Status-bar reasoning effort tracks the effective (wire-reported) effort of the serving model

## Goal
The status bar's reasoning-effort indicator tracks the effective reasoning effort of the model actually serving the current context — the same resolution the request builder uses (per-context ModelRef.reasoning_effort override, else the model's default chain ModelSpec.reasoning_effort → endpoint default → "max", gated by supports_reasoning_effort and clamped into the model's allow-list) — reported wire-first: ModelChanged and AgentInfo gain a reasoning_effort field the backend fills at every model-report site, and the StatusBar prefers the wire value over the toolbar echo / endpoint default. Regression test: a model_changed event carrying reasoning_effort populates the per-agent wire effort the status bar displays (fails pre-fix).

## Kind
bug_fixing

## Context
Key sites verified: StatusBar.tsx:76-160 (effectiveEffort = agentEfforts[activeAgent] ?? effortForEndpoint(endpoint); effortForEndpoint at ~:204 returns "off" when supports_reasoning_effort===false else ep.reasoning_effort ?? "max"; label clamp ~:408-417 into the target model's allow-list; selectReasoningEffort :472 calls setModel(agent, endpoint, model, effort) + setAgentEffort optimistic echo). Request-builder path: src/provider/client_factory.rs:220-225 resolve_effort (ModelRef.reasoning_effort → normalize_reasoning_effort_for; else effective_reasoning_effort_for) — returns the WIRE-encoded value (off → off_wire encoding or None=omit), so display-space needs its own resolver that keeps "off" as "off". Backend resolver: src/config/endpoints.rs:381-456 (effective_reasoning_effort, effective_reasoning_effort_for, normalize_reasoning_effort_for with gate/clamp/off-wire). Loop mirrors: loop_impl.rs:826 resolved_model() + set_resolved_model/set_resolved_provider maintained at every branch of resolve_turn_provider (:942-1157: skill override, picker pin, forced, subagent, state, default fallback) — the IPC layer reads them for AgentInfo. ModelChanged emission sites: src/agent/turn.rs:1405 (resolve_iteration_provider flip), src-tauri/src/ipc/agent.rs:612 (set_model), src-tauri/src/ipc/config_io.rs:318 (save_endpoints rebuild); serializable variant channels.rs:516-520 + to_serializable :724 + roundtrip test model_changed_roundtrips_json (:1221). AgentInfo: frontend/src/lib/types.ts:272-290 (model?, provider? — no effort), backend construction in src-tauri/src/ipc/agent.rs. Frontend store: useAgentStore.ts agentEfforts (:213 type, :606 init, :740 setReasoningEffort) + setAgentEffort; reduceModelChanged (agentEventReducer.ts:1158). Precedent: backlog 2980ca67 (model display went wire-first via AgentInfo.provider/ModelChanged.provider — same pattern, None = unknown → frontend falls back to local resolution; mock providers report empty/None).

## Steps
- [x] 1. **Reproduce with failing regression test** — Write a regression test that reproduces the defect (constitution: every defect gets a regression test that fails without the fix and passes with it). Run it and confirm it FAILS.
- [x] 2. **Document root cause** — Investigate and document the root cause. memory_write a BUG: record (symptom → root cause → fix + regression test name, ≤600 chars).
- [x] 3. **Minimal fix** — Apply the minimal fix that makes the regression test pass. Do not refactor unrelated code.
- [x] 4. **Verify** — Run the regression test + the full test suite (cargo test unpiped, warning-free). Record the regression test name via update_plan (regression_test field) — finish is blocked without it.

## Bug
User-reported (2027-01-07): the reasoning-effort shown in the status bar does not reflect the effort level of the model chosen automatically. When the active model is auto-selected / resolved per-context (or carries a per-model reasoning_effort override), the status bar's effort indicator shows a stale or wrong value — the toolbar's last manual choice or the ENDPOINT-level default — instead of the effective effort of the model actually in use. Repro: configure models with different reasoning_effort values (per-model ModelSpec.reasoning_effort in endpoints.toml, or per-context [models.*] ModelRef.reasoning_effort), let auto-selection pick one, observe the status bar effort. Root cause: StatusBar.tsx effectiveEffort (~:157-160) reads agentEfforts[activeAgent] (toolbar choice) ?? effortForEndpoint(ep) (endpoint-level ep.reasoning_effort ?? "max") — it never consults the resolution path the request builder uses (client_factory.rs resolve_effort: ModelRef.reasoning_effort → normalize_reasoning_effort_for; else effective_reasoning_effort_for: ModelSpec.reasoning_effort → endpoint → "max", gated/clamped), and the wire (ModelChanged { model, provider } / AgentInfo) carries no effort field at all — the same gap the model display had before backlog 2980ca67 went wire-first.

## Regression test
mid_turn_skill_model_switch_emits_model_changed_events
