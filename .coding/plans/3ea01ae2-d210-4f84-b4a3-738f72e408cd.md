# Plan: Fix deferred-success bug in TurnResolveLatch (backlog items marked failed when work is done)

## Goal
Fix the deferred-success bug in TurnResolveLatch so backlog items resolve at the correct time — when the main agent's plan reaches Complete and all descendants have drained — instead of sliding to a later turn where the workflow state is wrong.

## Kind
bug_fixing

## Context


## Steps
- [x] 1. **Reproduce with failing regression test** — Write a regression test that reproduces the defect (constitution: every defect gets a regression test that fails without the fix and passes with it). Run it and confirm it FAILS.
- [x] 2. **Document root cause** — Investigate and document the root cause. memory_write a BUG: record (symptom → root cause → fix + regression test name, ≤600 chars).
- [x] 3. **Minimal fix** — Apply the minimal fix that makes the regression test pass. Do not refactor unrelated code.
- [x] 4. **Verify** — Run the regression test + the full test suite (cargo test unpiped, warning-free). Record the regression test name via update_plan (regression_test field) — finish is blocked without it.

## Bug
Backlog items are marked failed ("plan loop did not close (workflow: Skill)") even though the work is done and merged into main. Root cause: TurnResolveLatch defers success resolution when the main agent finishes while descendants (reviewers) are running, but never flushes the deferred success when descendants drain — only deferred failures are flushed. The resolution slides to a later turn (e.g. a merge skill) where the workflow state is wrong.

## Regression test
deferred_success_is_flushed_when_descendants_drain
