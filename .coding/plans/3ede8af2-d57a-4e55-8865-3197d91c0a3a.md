# Plan: Fix N1–N6 + configurable in-memory trace budget

## Goal
Fix all six review findings (N1–N6) and add a configurable in-memory trace memory limit (Advanced setting): global raw-response byte budget with oldest-first eviction + per-record request-body cap, wired from config.toml through main.rs/save_settings to the live LlmRequestLog and exposed in the Settings → Advanced UI.

## Kind
implementation

## Context
Fixes for the 6 findings in .coding/reviews/2026-06-14-mem-perf-threading-review.md (N1 trace-tab poll, N2 sync fs in async commands, N3 unbounded error_logged_ids, N4 unrotated provider-errors.jsonl, N5 sync corpus_digest, N6 blocking set_trace_logging) PLUS a user-requested configurable in-memory trace limit. User decisions: (a) budget approach = global ~16 MiB cap on total response_raw bytes across the 32-record ring, evicting OLDEST records' raw payloads first, plus a 256 KiB per-record request-body cap (structure-preserving string-leaf truncation); (b) it must be a SETTING in the Advanced settings section (config.toml). Existing surfaces verified: AdvancedSection.tsx reads getSettings()/saves via saveSettings(patch); save_settings in src-tauri/src/ipc/settings.rs applies Option-patches to config and can push to live state (IpcState.trace is Arc<LlmRequestLog>); trace created main.rs:651 (fallback branches :367, :433), paths set :703-715; LlmRequestSummary already carries finish_reason+error. Windows/PowerShell env; cargo test must be green and warning-free (deny(warnings)); regression test per defect where deterministic.

## Steps
- [x] 1. **Add `[trace]` config section (budget + request cap)** — In src/config/general.rs add `pub struct TraceConfig` with `memory_budget_mb: usize` (serde default 16, clamped 1..=512 at load) and `request_body_cap_kb: usize` (default 256, clamped 16..=8192), with doc comments (in-memory ring budget; evicts oldest raw payloads first; request-body cap preserves JSON structure, truncates long string leaves). Add `pub trace: TraceConfig` to GeneralConfig + its Default impl. Re-export TraceConfig from src/config/mod.rs. Add a round-trip test in general.rs mirroring the existing summarize_at_fill_rate tests (~lines 393-412).
- [x] 2. **Enforce budget + request cap in LlmRequestLog** — In src/provider/trace.rs: (1) Shared gains `raw_budget: AtomicUsize` + `request_body_cap: AtomicUsize` initialized to new consts DEFAULT_TRACE_MEMORY_BUDGET_BYTES (16 MiB) / DEFAULT_TRACE_REQUEST_CAP_BYTES (256 KiB); add `set_memory_budget(&self, bytes)` / `set_request_body_cap(&self, bytes)` (store relaxed). (2) Helper `cap_request_json(value: &mut serde_json::Value, cap_bytes) -> bool` — repeatedly truncate the longest string leaf (char-boundary safe, `…(+N chars)` marker) until to_string fits; in `start()` apply to request_json and set new `request_truncated: bool` on LlmRequestRecord (surface on LlmRequestDetail, not the summary). (3) In `append_response()` after the existing per-record MAX_RAW_RESPONSE_BYTES cap, enforce the global budget: while sum of all records' response_raw lengths exceeds raw_budget, replace the OLDEST record's Some(raw) with None + set its new `response_evicted: bool = true`; add response_evicted to LlmRequestSummary + LlmRequestDetail so the UI can show 'payload evicted (memory budget)'. (4) Add computed `is_complete: bool` to LlmRequestDetail = finish_reason.is_some() || error.is_some(). Unit tests: oldest-first eviction leaves newest intact; flags round-trip through get; request cap preserves JSON keys; is_complete only on terminal records.
- [x] 3. **Wire config → live trace (main.rs + save_settings)** — (1) src-tauri/src/main.rs after set_error_log_path (~line 715): call trace.set_memory_budget(config.general.trace.memory_budget_mb * 1024 * 1024) + set_request_body_cap(request_body_cap_kb * 1024); fallback branches (:367, :433) keep Shared defaults. (2) src-tauri/src/ipc/settings.rs: add `trace_memory_budget_mb: Option<usize>` + `trace_request_body_cap_kb: Option<usize>` to SettingsPatch (~line 779) with clamp validation next to summarize_at_fill_rate (~line 823); apply to general.trace in the write-back (~line 994) AND push live via state.trace.set_memory_budget/set_request_body_cap; include a trace section in the getSettings response (~line 589). Tests mirroring settings.rs:1257-1273 (None no-op, Some applies + validates).
- [x] 4. **Advanced UI: trace-budget fields** — (1) frontend/src/lib/tauri.ts: add `trace: { memory_budget_mb: number; request_body_cap_kb: number }` to the getSettings return type + the two optional patch fields on saveSettings. (2) frontend/src/components/settings/sections/AdvancedSection.tsx: add traceBudget/traceBudgetSnap + reqCap/reqCapSnap state, load from s.trace in load(), include in dirty, set patch.trace_memory_budget_mb/trace_request_body_cap_kb in handleSave() when changed (finite and >= 1); render a 'Trace memory' group with two labeled numeric inputs (MiB / KiB) + hint ('Total in-memory raw-response budget; oldest payloads are evicted first') following the fill-rate input styling. (3) Update the section doc comment (line 20).
- [x] 5. **N1: stop trace-detail poll on terminal records** — (1) src-tauri/src/ipc/trace.rs: make get_llm_request async; clone the Arc<LlmRequestLog> out of State, then run the get/clone in tokio::task::spawn_blocking (the up-to-2 MiB clone + serialize must leave the main thread). (2) Frontend LlmRequestDetail type: add is_complete, response_evicted, request_truncated. (3) frontend/src/components/views/LlmTraceView.tsx detail effect (~612-635) and compare effect (~640-658): after fetching, if the record is_complete, stop the interval (keep the initial fetch; pattern: a done-flag the cleanup clears). No JS test infra — verify behavior manually and note in the summary.
- [x] 6. **N2: spawn_blocking for fs IPC commands** — src-tauri/src/ipc/files.rs: wrap blocking fs in tokio::task::spawn_blocking with owned paths (never State across await): read_file (:30, clone validated PathBuf), browse_markdown_file post-dialog read (:311, move path_buf), save_conversation (:405 create_dir_all + :409 write — move resolved + content in, return the path string from the closure), load_conversation (:428). Same error strings, sandbox validation BEFORE the spawn; doc-comment style of get_git_branch (:354-359). Keep files.rs mod tests green; add a save→load round-trip test through the wrapper.
- [x] 7. **N3+N4: bound error dedup set + rotate error log** — src/provider/trace.rs: (1) in maybe_log_error (:434-493) before seen.insert, `if seen.len() > 10_000 { seen.clear(); }` with a comment. (2) Add `pub const MAX_ERROR_FILE_BYTES: u64 = 8 MiB`; in writer_main's error-append block (:651-676) check metadata len first — over cap → open write(true).truncate(true) instead of append (mirrors the traces over-cap rule at :744); note it in the module tradeoffs doc (:696-712). Tests: seed an over-cap provider-errors.jsonl, queue an ErrorLine, flush, assert the file shrank to the new line; error_logged_ids cap behavior after >10k failures.
- [x] 8. **N5: corpus_digest off the async runtime** — src/runtime/agent.rs spawn_consolidation (:444-470): move the corpus_digest(...) call INSIDE the tokio::spawn closure; compute plans_dir + reviews_dir PathBufs before the spawn and move them in (keeps the 'Never blocks the caller' doc promise true). No behavior change; existing consolidation tests stay green.
- [x] 9. **N6: non-blocking set_trace_logging** — src-tauri/src/ipc/trace.rs: make set_trace_logging async; clone the Arc<LlmRequestLog> out of State and wrap the set_logging_enabled call in tokio::task::spawn_blocking (the disable path's blocking ack_rx.recv() drain must leave the main thread). Do NOT change the RunEvent::Exit caller (main.rs:539-547) — it must stay blocking; comment why. Frontend invoke call sites need no change.
- [x] 10. **Verify: cargo test + frontend build + regression sweep** — Run cargo test in PowerShell reading $LASTEXITCODE directly (never piped); must be green AND warning-free (deny(warnings); fix root causes, never #[allow]). Run npm run build at the repo root for the frontend type-check. Re-check each N1-N6 + the budget setting end-to-end (config default → main.rs wiring → save_settings live push → Advanced UI) against the diff. Then the closing sequence: spawn reviewer, fix findings, commit to the current feature branch with the review report included.
