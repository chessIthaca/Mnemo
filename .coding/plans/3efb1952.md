# Plan: Close re-dispatched item 45a4eb88 (stamp done) + queue the re-dispatch-loop defect

## Goal
Close the stuck Run-All loop for item 45a4eb88: stamp it done directly (its work is verifiably complete across two plans), queue the re-dispatch-loop defect (interrupted items never re-stamp/re-link, so they re-dispatch forever) as a backlog item for a proper bug_fixing pass, and commit the bookkeeping — no source changes.

## Kind
research

## Context
Third Run-All dispatch of item 45a4eb88. The work shipped under plan 024a06e1 (commit 568d405: shared validator normalize_item_text + image-aware normalize_new_item_text, const-built tool description, IPC validation, BacklogView inline error + guarded restore, docs, tests; round-1 findings fixed, round-2 verification PASS) and was re-verified under plan 30e7db62 (commit 72ed3a2, full matrix green: root 2059+16, src-tauri 234+4, frontend 1011/72, tsc clean). The item nonetheless stays pending: its plan_id remained 024a06e1 across the second dispatch (never re-linked to 30e7db62), so the turn-resolution done-stamp — which requires the linked plan to transition during the turn — never fired; the Executing-entry in_flight stamp also never fired on re-dispatch. Decision: research kind (no source changes; the loop is closed by a direct status correction per the status contract, precedent the 2027-01-07 audit corrections of 6d56943a/42302c2d), plus queueing the underlying re-dispatch-loop defect for a dedicated bug_fixing pass.

## Steps
- [x] 1. **Stamp item 45a4eb88 done**
- [x] 2. **Queue the re-dispatch-loop defect**
- [x] 3. **Commit the bookkeeping and close**
