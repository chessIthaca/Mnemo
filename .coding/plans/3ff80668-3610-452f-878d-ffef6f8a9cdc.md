# Plan: Reasoning UI fixes + config dialog settings placement

## Goal
Fix two reasoning-block UI bugs (missing expand/collapse triangle when reasoning text is empty; weird rendering on resize in that state — should be an empty gray space) and re-home reasoning-related settings to a better section than "Appearance" in the config dialog.

## Kind
implementation

## Context
User feedback (unattended backlog run, no questions possible): (1) "where should these go in the config dialog. I think maybe appearance is the wrong thing" — reasoning-related settings currently live under Appearance and should move somewhere more fitting (e.g. a Chat/Conversation/Agent section); (2) when reasoning text is still empty the little triangle to open/close is missing; (3) when resizing during that empty state it renders weird instead of an empty gray space. Project: Tauri app at C:\AgenticCoder\AgenticCoder (Rust backend + frontend), #![deny(warnings)], must run cargo test before completing steps, add regression tests for defects.

## Steps
- [x] 1. **Explore: config dialog + reasoning settings** — Find the config/settings dialog component in the frontend, list its sections (esp. Appearance), identify the reasoning-related settings currently placed there, and check what other sections exist to pick a better home. Also check settings persistence (keys/defaults) so moves don't break stored prefs.
- [x] 2. **Explore: reasoning block component** — Find the chat reasoning block renderer (chevron/triangle, expand/collapse, streaming empty state, resize handling). Identify why the triangle is hidden when reasoning text is empty and what renders on resize in that state.
- [x] 3. **Move reasoning settings out of Appearance** — Relocate the reasoning-related settings to the chosen section (likely Chat/Conversation or Agent), update any labels/keys/migrations needed, keep persistence compatible.
- [x] 4. **Fix missing triangle on empty reasoning** — Render the expand/collapse chevron whenever a reasoning block exists, even with empty content (e.g. streaming placeholder state), with correct a11y attributes.
- [x] 5. **Fix resize rendering in empty state** — Ensure the collapsed/empty reasoning block renders as a clean empty gray space on resize (no artifact); likely a height/canvas/DOM-measurement guard.
- [x] 6. **Add regression tests** — Tests: chevron present with empty reasoning; resize in empty state yields empty gray placeholder; settings render in the new section. Plus a Rust-side test if any keys changed.
- [x] 7. **Run full test matrix** — Root cargo test, src-tauri cargo test + cargo build, frontend tests/build per project convention; must be warning-free under #![deny(warnings)].
