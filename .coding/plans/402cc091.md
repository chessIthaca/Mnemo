# Plan: load_tools allowed everywhere — lift the mcp.* reveal gate + skill membership (77efc1b7)

## Goal
Make load_tools callable in every workflow state and agent flavor (backlog 77efc1b7): lift the mcp.* reveal state gate (delete mcp_reveal_allowed + its dispatch enforcement) and add load_tools to the ToolFilter::Skill always-available set — so any state can reveal/list a group's tools (the revealed tools stay filter-gated), and a skill whose allow-list names deferred tools can actually load them.

## Kind
implementation

## Context
Verified by exploration (2027-02-05): (a) mcp_reveal_allowed (src/tool/agent/load_tools.rs:83-101, pub fn) gates mcp.* reveals via filter.allows(Agent, NeedsApproval, "mcp__probe__probe"); its ONLY caller is the dispatch block at src/agent/dispatch.rs:263-279 (inside the workflow-lock section, right after the filter.allows re-check), denying with "tool group '{group}' is not available in the current workflow state ({state})". Unit test pinning it: mcp_reveal_gate_matches_the_tool_filter (load_tools.rs:375-396). (b) load_tools itself (Agent + AutoRun) is ALREADY allowed in every base filter (Planning :570-573 AutoRun && !mcp__; Complete :793-796 same; Executing/PlanFrozen/Reviewing Agent => true; ExecutingResearch :667-672 not in the denied list) — only the mcp.* group ARG was gated. (c) The Skill arm (src/tool/mod.rs:831-867): always-available set = ask_user, current_plan, skill_reload, skill_end, abandon_skill, backlog_add, backlog_status, backlog_list + the skill's allow-list; load_tools missing → a skill listing deferred tools (browser/image/mcp) in its allow-list cannot reveal them (they stay deferred → unusable mid-skill). (d) Reviewer arm (:868-878): strict allow-list + current_plan, load_tools never named → stays excluded (the non-goal preserves itself). Non-goals per the backlog item: hidden_groups_for + the empty-skip stay (the index keeps advertising only filter-usable groups; the schema enum still lists all); revealed McpTools stay filter-hidden in Planning/Complete/research (mcp__ name exclusion) — nothing unusable is dispatched; the user accepts the spawned connection. Docs: the MCP SPEC memory (2026-08-28-mcp-servers-v1…) says "dispatch denies the REVEAL itself in blocked states (review LOW 2)" — amend after landing; PLAN.md:983's tool-visibility sentence stays true. Verify with cargo test + cargo test --features browser (budget test must stay green — no schema growth; load_tools already rides every filter).

## Steps
- [x] 1. **Lift the mcp.* reveal state gate** — Delete `mcp_reveal_allowed` (src/tool/agent/load_tools.rs:83-101, fn + doc comment) and the dispatch enforcement block (src/agent/dispatch.rs:263-279 — the `if let Some(group) = parsed_call.arguments.get("group")…` block with its comment, keeping the filter.allows re-check above and the plan-mutations gate below). Delete the test `mcp_reveal_gate_matches_the_tool_filter` (src/tool/agent/load_tools.rs:375-396). Then search the repo for any other reference to the old denial (pattern "not available in the current workflow" / mcp_reveal_allowed) and update it.
- [x] 2. **Add load_tools to the Skill always-available set** — In src/tool/mod.rs ToolFilter::Skill arm's `_ =>` set (:844-866), add `|| name == "load_tools"` with a comment: a skill whose allow-list names deferred tools (browser/image/mcp) needs the loader to reveal them — the loaded tools still pass through the skill's allow-list, so this widens nothing. Extend the arm's leading comment (:832-842) to mention load_tools riding the set.
- [x] 3. **Filter-matrix + dispatch tests** — In src/tool/mod.rs tests, add a test mirroring `backlog_list_visible_in_all_states_and_skills` (:2491): assert `filter.allows(ToolCategory::Agent, SafetyLevel::AutoRun, "load_tools")` is true for Planning, Executing, ExecutingResearch, PlanFrozen, Reviewing, Complete, Skill(vec![]), and Skill(vec!["offscreen_browser_navigate"]); assert Reviewer(vec![]) stays false (read-only surface). Check src/agent/dispatch.rs tests for any load_tools group-gating test (search "load_tools" in that file): if one pins the old denial, invert it to assert the reveal succeeds in Planning; otherwise the matrix test is the pin.
- [x] 4. **Docs + memory sync** — memory_amend the MCP SPEC record (.coding/knowledge/spec/2026-08-28-mcp-servers-v1-config-lazy-manager-deferred-grou.md): the "dispatch denies the REVEAL itself in blocked states (review LOW 2)" clause is lifted (user request 2027-02-05, backlog 77efc1b7) — reveals are allowed in every state; the revealed tools stay filter-gated (mcp__ exclusion in Planning/Complete/research). Check PLAN.md:983, docs/CONFIGURATION.md, and frontend/src/components/settings/sections/McpSection.tsx for reveal-gate claims (tool-visibility sentences stay true — only reveal-gate claims change).
- [x] 5. **Verify** — Run `cargo test` and `cargo test --features browser` (unpiped, read $LASTEXITCODE) over the changed files (src/tool/agent/load_tools.rs, src/agent/dispatch.rs, src/tool/mod.rs) — both green and warning-free; confirm tools_array_stays_within_context_budget (src/agent/factory.rs) passes with no schema growth (load_tools already rides every filter).
