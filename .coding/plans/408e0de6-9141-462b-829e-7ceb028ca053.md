# Plan: Hairlines around remaining resize bars + verify scrollbar thumb gap

## Goal
Complete the resize-bar hairline motif: add the 1px flanking highlight lines (border-border token) to the two resize bars that still lack them (InflightBar, FileViewer splitter), and confirm the scrollbar thumb already has the requested 1px gap on all four sides.

## Kind
implementation

## Context
Backlog item (written 2026-08-19, before plan b33b4b95 landed) asks for: (1) the small highlight line around resize bars, (2) scrollbar thumb 1px smaller on all four sides. Current tree: globals.css already insets the scrollbar thumb 1px via transparent border + background-clip: padding-box (imported by main.tsx:5, committed). App.tsx ResizeHandle (agent|tools splitter) already has border-x border-border hairlines. The two remaining resize bars — InflightBar drag handle and FileViewer tree/content splitter — still lack flanking hairlines; those are the real remaining work. Only 3 resize handles exist in the app (verified by cursor-*-resize search). Frontend has vitest but no React component-test infra, so no render-level assertion is feasible for className-only changes; cargo test + npm test + tsc build must be green.

## Steps
- [x] 1. **Ensure a feature branch is checked out** — Run `git branch --show-current` (PowerShell). If the output is `main`, create and switch to a feature branch: `git checkout -b fix/resize-handle-hairlines`. Never commit to main. If already on a non-main branch, stay on it and record its name.
- [x] 2. **Add flanking hairlines to InflightBar resize handle** — In frontend/src/components/chat/InflightBar.tsx: (a) line 124, change the container `<div className="border-t border-border bg-bg-secondary">` to `<div className="bg-bg-secondary">` (the hairline moves onto the handle to avoid a double 1px line); (b) on the drag handle (line 129) add `border-y border-border` to the className so it reads `flex h-1.5 shrink-0 cursor-ns-resize items-center justify-center border-y border-border hover:bg-cyan-500/20`; (c) update the comment above the handle to note it now carries the 1px border-y hairlines flanking the grip pill, matching the App.tsx ResizeHandle motif (handle owns the region-separation lines).
- [x] 3. **Add flanking hairlines to FileViewer splitter** — In frontend/src/components/views/FileViewer.tsx line 564, add `border-y border-border` to the splitter handle className so it reads `group flex h-1.5 shrink-0 cursor-row-resize items-center justify-center border-y border-border transition-colors hover:bg-cyan-500/20`, and extend the `{/* Drag handle between the tree and the content. */}` comment to mention the 1px border-y hairlines flanking the grip pill (same motif as the App.tsx ResizeHandle). With border-box sizing the 6px strip = 1px line + 1px gap + 2px pill + 1px gap + 1px line; neither neighbour pane draws a border, so no double lines.
- [x] 4. **Verify scrollbar thumb-gap rule is committed and live** — Run `git log --oneline -3 -- frontend/src/styles/globals.css` to confirm the thumb-inset rule (globals.css lines 99-112: `border: 1px solid transparent` + `background-clip: padding-box` on ::-webkit-scrollbar-thumb) is committed on this branch — it already implements the requested '1px smaller on each of the four sides' gap (thumb 6px inside the 8px track). No code change expected; record the confirming commit for the final summary.
- [x] 5. **Run cargo test + frontend tests + typecheck** — PowerShell, unpiped, checking $LASTEXITCODE after each: `cargo test` (project constitution; must be green and warning-free under #![deny(warnings)]), then `npm test --prefix frontend` (vitest run), then `npm run build --prefix frontend` (tsc && vite build) to prove the TSX still compiles. Fix any failure before proceeding.
