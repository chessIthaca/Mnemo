# Plan: Browser tab: add Stop and Reload buttons

## Goal
Give the Browser tab's URL bar Stop and Reload buttons beside Open (backlog 3b395d27). Backend: two new IPC commands in src-tauri/src/ipc/browser_webview.rs following the browser_webview_navigate pattern (lock state → heal_deferred → watchdog note BEFORE the controller call → safe no-op when no webview exists): browser_webview_stop uses eval("window.stop()") (tauri 2.11.5 Webview has no stop(); window.stop() is the JS stop-button equivalent, standard in Chromium/WebView2) and browser_webview_reload uses Webview::reload(). Register both in src-tauri/src/main.rs invoke_handler beside browser_webview_navigate (~:757). Frontend: browserWebviewStop/browserWebviewReload wrappers in frontend/src/lib/tauri.ts + two icon buttons (lucide X and RotateCw) in the URL-bar row before Open in BrowserView.tsx, always enabled (the agent can navigate via browser_* tools without the frontend's loadedUrl updating, so do not gate on it), errors surfaced via the existing error state. Tests: source-contract test pinning the command wiring (include_str! slice pattern like backlog_retry_routes_through_guarded_requeue); full matrix green (root cargo test, src-tauri cargo test, frontend tests + tsc).

## Kind
implementation

## Context


## Steps
- [x] 1. **Add stop/reload IPC commands** — In src-tauri/src/ipc/browser_webview.rs, after browser_webview_navigate (ends ~line 558), add two #[tauri::command] async fns following navigate's exact pattern:

pub async fn browser_webview_stop(state: State<'_, IpcState>) -> Result<(), IpcError> — lock `state.browser_webview.state.lock().await`, then `state.browser_webview.heal_deferred(&mut bw);`, then `if let Some(wv) = &bw.webview { ... }` (safe no-op when no webview exists). Inside the gate: `note(state.inner(), "webview stop")` BEFORE the controller call (watchdog contract: the ring names the in-flight op), then `wv.eval("window.stop()")` — tauri 2.11.5 has no Webview::stop(); window.stop() is the JS stop-button equivalent — with `.map_err(|e| IpcError::msg(format!("stop failed: {e}")))?` if eval returns Result (verify the actual signature; adjust to match).

pub async fn browser_webview_reload(state: State<'_, IpcState>) -> Result<(), IpcError> — identical shape; note text "webview reload"; controller call `wv.reload()` with map_err "reload failed: {e}" (verify signature).

Both need doc comments (project rule: all public functions). Static note strings match the overlay enter/exit pattern ("webview overlay enter") since neither command takes a URL.
- [x] 2. **Register both commands in main.rs** — In src-tauri/src/main.rs, in the tauri::generate_handler![...] invoke_handler list, add `ipc::browser_webview::browser_webview_stop,` and `ipc::browser_webview::browser_webview_reload,` directly beside `ipc::browser_webview::browser_webview_navigate` (~line 757), keeping the browser_webview block contiguous.
- [x] 3. **Frontend wrappers in tauri.ts** — In frontend/src/lib/tauri.ts, after browserWebviewNavigate (~line 1728), add:

export async function browserWebviewStop(): Promise<void> { await invoke("browser_webview_stop"); }
export async function browserWebviewReload(): Promise<void> { await invoke("browser_webview_reload"); }

with doc comments matching the neighbors' style (what it does + the no-op-when-no-webview behavior).
- [x] 4. **Stop + Reload buttons in BrowserView.tsx** — In frontend/src/components/views/BrowserView.tsx: import X and RotateCw from lucide-react (extend the existing import) and browserWebviewStop/browserWebviewReload from ../../lib/tauri. Add two async handlers following openUrl's error pattern: try { await browserWebviewStop(); setError(null); } catch (e) { setError(errMsg(e)); } (same for reload). In the URL-bar row (the flex items-center gap-1.5 div), BEFORE the Open button, add two icon-only buttons: X with title "Stop loading the page", RotateCw with title "Reload the current page". Always enabled — do NOT gate on loadedUrl (the agent can navigate via browser_* tools without the frontend's loadedUrl updating). Style like the Open button but icon-only (flex items-center rounded border border-border bg-bg-tertiary px-2 py-1 text-slate-300 hover:text-cyan-400, with aria-label matching the title).
- [x] 5. **Source-contract tests** — In the #[cfg(test)] mod tests of src-tauri/src/ipc/browser_webview.rs, add a test following the backlog_retry_routes_through_guarded_requeue include_str! slice pattern (see src-tauri/src/ipc/backlog_cmds.rs:484): let src = include_str!("browser_webview.rs"); slice each command body from `pub async fn browser_webview_stop` / `pub async fn browser_webview_reload` to the next `\n}\n`; assert the stop body contains `eval("window.stop()")` and the reload body contains `.reload()`; assert both bodies contain heal_deferred, the `if let Some(wv)` no-op gate, and a note( call BEFORE the controller call (note line appears before the eval/reload line). Also pin registration: let main_src = include_str!("../main.rs"); assert it contains `browser_webview::browser_webview_stop` and `browser_webview::browser_webview_reload` (the commands need Tauri State, so the wiring is pinned as a source contract). Doc comment the test explaining why (commands need Tauri State → source contract, mirroring backlog_cmds.rs).
- [x] 6. **Full test matrix green** — Run the full matrix on Windows (PowerShell, check $LASTEXITCODE unpiped): `cargo test` at the repo root (mnemo crate), `cargo test` in src-tauri, and the frontend test + typecheck scripts (read frontend/package.json for exact script names, e.g. npm test / npm run typecheck or tsc). The build is #![deny(warnings)] — zero warnings required. Fix any failures before completing the step.
