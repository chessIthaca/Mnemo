# Plan: Cache-hit + context optimization analysis round 3

## Goal
Analyze the current LLM trace log + provider-error log to measure cache-hit rates, identify context waste, and produce a findings report with concrete recommendations for saving context, compacting better, and increasing cache hits.

## Kind
research

## Context


## Steps
- [x] 1. **Parse trace log + provider-errors with PowerShell** — Run PowerShell to parse .coding/logs/traces.jsonl and .coding/logs/provider-errors.jsonl: count lines, file sizes, date range, distinct models/base_urls. For each trace with usage: emit {id, ts, model, prompt, cached, hitPct, completion, reasoning, nMessages, max_completion_tokens}. Compute aggregates: overall hit%, count >=95%, count <50% (resets), tokens wasted. For provider-errors: list each error with id, ts, model, http_status, error snippet. Save to .coding/analysis/cache-hit-3-inventory.txt and .coding/analysis/cache-hit-3-summary.txt. Note: the trace log currently has only 1 line (id 181, glm-5.2) — the analysis will be limited but the structure + provider-errors will reveal the max_completion_tokens bug pattern.
- [x] 2. **Analyze max_completion_tokens bug** — The trace shows max_completion_tokens=131072 (line 1203 of src/provider/openai.rs: `let max_completion = self.caps.max_output_tokens.max(4096)`). This is sent as-is without capping to the model's context window. Provider-errors.jsonl shows TWO failure patterns: (1) qwen-3.6: max_completion_tokens=131072 > max_model_len=65536 → HTTP 400; (2) glm-5.2: 131072 output + 131073 input > 262144 max → HTTP 400/502. Document the root cause (no dynamic capping), the failure count from provider-errors, and propose a fix: cap max_completion_tokens to min(max_output_tokens, max_context - estimated_prompt_tokens - safety_margin). Note that build_request_json doesn't currently receive the prompt token count — the fix needs to either pass it in or cap to max_context - fixed_margin.
- [x] 3. **Analyze context waste + compaction opportunities** — From the trace, identify: (1) The stuck-loop repetitive output — the assistant produced thousands of tokens repeating 'Let me commit the uncommitted work...' dozens of times. This wastes output tokens AND input tokens on every subsequent request until summarization compacts it. Propose: detect repetitive output and stop generation early, or truncate before entering conversation history. (2) The conversation summary (messages[1]) — assess its size and whether the 8-section handoff format could be more compact. (3) Large tool outputs in conversation history (file reads, search results, git diffs) — assess whether they could be truncated before entering history. (4) The volatile tail size — the RECALLED MEMORIES section with 5 full digests + WORKFLOW STATE + CAPABILITIES. Assess whether the tail could be more compact (shorter digests, fewer recalled memories).
- [x] 4. **Verify cache-hit structure + write findings report** — Confirm the R1/R5 cache structure is correct: (1) messages[0] = stable head (preamble + lifecycle + rules + tool strategy + memory records + constitution) — byte-stable across turns. (2) PROJECT MEMORY primer (format_primer, prompt.rs:416) — cached per session via session_primer, so byte-stable within a session. (3) Volatile tail (RECALLED MEMORIES + WORKFLOW STATE + CAPABILITIES) — appended as trailing system message after conversation history (cache-immune per R1). (4) CONTEXT_FOOTER — final message, byte-stable sentinel per R5. Write .coding/analysis/cache-hit-3-report.md with: headline numbers (from the 1 trace + provider-errors), the max_completion_tokens bug (root cause + fix proposal), context waste findings (stuck-loop, compaction, tail size), cache structure verification, and ranked recommendations (R9: dynamic max_completion_tokens cap, R10: repetitive-output detection, R11: more aggressive tail compaction).
