# Plan: Reasoning bar counters render per event — drop the count-up interpolation

## Goal
Backlog item d9b560a0: the inflight bar's four token counters (↑ prompt, ↓ completion, 🧠 reasoning, ctx used) render their raw values directly — updating exactly when streaming data arrives, like the trace graph (e20b1df precedent) — instead of animating toward targets via the useCountUp rAF interpolation. With the sole caller gone, delete the dead hook.

## Kind
bug_fixing

## Context
Exploration findings (2027-01-07): useCountUp (frontend/src/hooks/useCountUp.ts:34-79, rAF ease-out interpolation, respects prefers-reduced-motion) has exactly ONE caller — InflightBarPanel — with 4 call sites at frontend/src/components/chat/InflightBar.tsx:157-160 (promptDisplay, completionDisplay, reasoningDisplay, contextUsedDisplay). The underlying values are ALREADY event-driven: liveCompletionTokens/liveReasoningTokens update per stream chunk (chars/4), tokenUsage.* lands on the Usage event, contextUsage.used on ContextUsage events — the count-up is purely cosmetic. frontend/src/components/chat/InflightBar.test.ts:118-124 pins the OLD contract via the ?raw source-contract pattern (asserts `useCountUp(tokenUsage.completion + liveCompletionTokens)` etc.) and must be rewritten; the file is already registered in frontend/vitest.config.ts (no config change, no new test files — the include allow-list guard). No React DOM test infra (node env) — the ?raw source-contract test is the established regression pattern (plan 00d88e13 precedent). No README/PLAN.md references to the count-up (search-verified) — no doc sync needed beyond code comments. Gates: from frontend/ run `npx tsc --noEmit` + `npm test` (vitest run); `cargo test` from repo root for the constitution gate (Rust untouched). Working tree clean except .coding/backlog.jsonl (run-all dispatch stamp). Reference behavior precedent: trace graph updates discretely per streaming event (e20b1df, merged).

## Steps
- [x] 1. **Reproduce with failing regression test** — Write a regression test that reproduces the defect (constitution: every defect gets a regression test that fails without the fix and passes with it). Run it and confirm it FAILS.
- [x] 2. **Document root cause** — Investigate and document the root cause. memory_write a BUG: record (symptom → root cause → fix + regression test name, ≤600 chars).
- [x] 3. **Minimal fix** — Apply the minimal fix that makes the regression test pass. Do not refactor unrelated code.
- [x] 4. **Verify** — Run the regression test + the full test suite (cargo test unpiped, warning-free). Record the regression test name via update_plan (regression_test field) — finish is blocked without it.

## Bug
The inflight bar's token counters (↑ prompt / ↓ completion / 🧠 reasoning / ctx used) animate toward their targets via a ~400ms rAF count-up interpolation, which is distracting during streaming — the user expects the counters to update per event as streaming data arrives (like the trace graph), not count up (user report 2027-01-07, backlog item d9b560a0).

## Regression test
countersRenderPerEventNoInterpolation
