# Plan: StatusBar: fixed-width truncation for model/provider names (no multiline bar)

## Goal
Force fixed-width single-line model/provider display in the StatusBar bottom bar: clamp each name span with max-w-* + truncate (ellipsis cut-off) so the bar never grows multi-line, matching the fixed-size Complete/max/main badges.

## Kind
bug_fixing

## Context
User (with screenshot of the bottom bar: glm-5.3-flash · Ollama Cloud · max · Complete · main): "this multiline resize is wrong, instead just do what you do with complete, max and main, force a certain size by cutting things off for the model and provider name". Root cause: StatusBar.tsx:551 bar is a flex row; spans at 561-574 (model "font-medium text-cyan-400", provider "text-blue-400") lack max-w + truncate, so their text wraps when squeezed. Fix pattern precedent: EndpointCard.tsx:313 "max-w-40 shrink-0 truncate". Test precedent: MainPanel.tabStyle.test.ts (?raw source-contract style; vitest runs node env, no DOM). Frontend tests: cd frontend; npm run test (vitest run); build/typecheck via npm run build (tsc && vite build). Project rule: cargo test runs before step completion (PowerShell, unpiped, check $LASTEXITCODE). Also found in git diff: .coding/backlog.jsonl clobbered by the known stale-state bug (memory d02c2ede) — working tree dropped 3 done/failed entries + pending 24e1c98e; agent re-queued the pending text as new id 5d4bcd26; file must be restored from HEAD before commit.

## Steps
- [x] 1. **Reproduce with failing regression test** — Write a regression test that reproduces the defect (constitution: every defect gets a regression test that fails without the fix and passes with it). Run it and confirm it FAILS.
- [x] 2. **Document root cause** — Investigate and document the root cause. memory_write a BUG: record (symptom → root cause → fix + regression test name, ≤600 chars).
- [x] 3. **Minimal fix** — Apply the minimal fix that makes the regression test pass. Do not refactor unrelated code.
- [x] 4. **Verify** — Run the regression test + the full test suite (cargo test unpiped, warning-free). Record the regression test name via update_plan (regression_test field) — finish is blocked without it.

## Bug
StatusBar bottom bar grows multiline when squeezed: the model name span ("glm-5.3-flash", StatusBar.tsx:562) and provider span ("Ollama Cloud", StatusBar.tsx:572) have no max-width or truncation, so flex squeezes wrap their text across multiple lines (at hyphens/spaces), growing the bar height — unlike the short fixed badges (Complete / max / main).

## Regression test
StatusBar.truncate.test.ts
