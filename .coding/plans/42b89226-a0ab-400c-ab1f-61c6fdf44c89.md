# Plan: Phase 4 — Multi-agent plan ownership (Maint H2)

## Goal
Implement Phase 4 (Maint H2): decide and enforce a multi-agent plan ownership policy. Default recommendation from review: main-agent-only plan mutations (sub-agents may not call create_plan/update_plan/complete_step/abandon_plan). Align factory (already per-agent Workflow), tool dispatch (filter in planning/executing), prompt, tests, and PLAN.md. No drive-bys; run cargo test before close steps; follow full constitution close (reviewer on all uncommitted at end).

## Context
Phase 3 (3a/3b/3c + close) just completed and committed. Parent plan now at step 19 (Phase 4). Factory already creates independent per-agent Workflow instances (each agent has its own plans_dir-backed Workflow). The finding was lack of explicit ownership policy for plan mutations across agents (who can create/update/complete plans?). Review default: main-agent-only mutations. Must be scoped, preserve existing per-agent workflows, no drive-bys, full test + reviewer close sequence.

## Steps
- [x] 1. Read current plan ownership code: factory (per-agent Workflow), tool/workflow/plan.rs + skill.rs, agent/turn.rs dispatch filters, runtime/agent.rs registration, prompts, and existing tests (factory two_builds, workflow_integration).
- [x] 2. Read the architecture review finding for Maint H2 and any prior notes on the chosen policy (main-only vs namespaced).
- [x] 3. Decide policy: main-agent-only mutations (simplest, matches 'main agent drives the work'). Sub-agents inherit read-only or no plan tools. Update allowed_tools + dispatch gates.
- [x] 4. Implement the filter: in tool dispatch (or Workflow allowed_tools for sub-agents), deny plan-mutating tools when agent is not the main agent. Keep per-agent Workflow instances (already in factory).
- [x] 5. Update prompts if they mention plan tools for sub-agents; add tests (factory test for sub-agent denied, turn test).
- [x] 6. Update PLAN.md with the policy decision and rationale.
- [x] 7. Run cargo test (unpiped) after changes. Prepare for close: git status/diff, spawn reviewer on ALL uncommitted, fix findings, commit on feature branch including report.
