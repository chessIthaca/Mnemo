# Plan: bug_fixing create_plan discards caller steps — persist as Detailed steps

## Goal
bug_fixing create_plan must not discard the caller's detailed steps: persist them as a "## Detailed steps" section in the plan file (alongside the locked skeleton), state plainly in the result what was persisted, strengthen the tool + compiled-prompt descriptions with the crash-resumption-document framing, and surface Bug + Context in the resume footer so a fresh session picks up an in-flight bug plan with the full detail.

## Kind
bug_fixing

## Context
DEFECT SITE (verified): src/tool/workflow/plan.rs:381-404 — the bug_fixing branch forces BUG_FIXING_SKELETON (plan.rs:37-49, the 4 locked steps) and IGNORES args.steps ("provided steps are ignored — the bug protocol is not negotiable"). The plan is persisted via wf.create_plan_with_kind(&title, &goal, &context, steps, kind, bug_symptom) (plan.rs:483-489; Workflow::create_plan_with_kind at src/workflow/mod.rs:482, the create_plan wrapper at :451). The result text (plan.rs:492-511) says only "created plan '{title}' with {n} steps (id, kind)" — n=4 skeleton steps, no mention the caller's steps were dropped.

PLAN FILE (src/workflow/plan_file.rs): serialize() at :282-301 writes "# Plan: {title} / ## Goal / ## Kind / ## Context / ## Steps" (checklist) + optional "## Bug" + "## Regression test". parse() at :171-235 is a section state machine (Section enum: Preamble/Goal/Kind/Context/Bug/RegressionTest/Steps/Other) — unknown "## " headers fall to Section::Other and their content is DROPPED. PlanFile fields: title/goal/context/kind/bug_symptom/regression_test/steps.

RESUME SURFACE (src/agent/prompt.rs): workflow_section :642-691, the Executing arm shows CURRENT PLAN / GOAL / PROGRESS / CURRENT STEP ("← resume here", :687) — NOT the Bug or Context sections. The volatile-tail architecture (:498-535, :558-567): the tail is re-processed per step bump, so additions must stay compact (CURRENT STEP caps at 120 chars; memory entries cap at 160). A fresh session loads the plan via Workflow::load_latest (factory.rs:689-707) — the tail is its only plan-derived injection.

COMPILED PROMPT (prompt.rs:77-81): the PLAN KIND section describes bug_fixing as "locked reproduce→root-cause→fix→verify skeleton, required bug:{symptom} param" — no resumption-document framing.

FIX DESIGN (decided): (1) PlanFile gains `detailed_steps: Option<String>` (the raw section body) + Section::DetailedSteps (header "## Detailed steps"); serialize writes it right after the ## Steps checklist (before ## Bug); parse accumulates it like Bug/RegressionTest. (2) plan.rs bug_fixing branch: when args.steps is non-empty, render each step as a markdown bullet ("- {full step text}") into the body and pass it through create_plan_with_kind (new param; the :451 wrapper passes None); the result text + data JSON state plainly: "4 locked skeleton steps + {m} detailed steps persisted as '## Detailed steps'". The skeleton lock itself is UNCHANGED (update_plan still refuses steps replacement — the discipline; the detail section is immutable like the skeleton). (3) Descriptions: the skeleton doc (plan.rs:32-36), the steps param doc, the kind doc (:329), and the main tool description gain the crash-resumption-document framing — bug + context + steps together must carry everything a fresh session needs (symptom, root cause, code locations, fix design, test design, verification commands); for bug_fixing the provided steps are persisted as the Detailed steps section, not discarded. (4) prompt.rs Executing arm: after GOAL add "BUG: {symptom}" (when present) and "CONTEXT: {context}" (when non-empty, truncated to ~240 chars, single line, ellipsized — the tail is paid per step bump); the detailed steps stay in the plan file (too big for the tail). (5) prompt.rs:77-81 PLAN KIND text: add that bug plans must detail out the bug (symptom, root cause, locations, fix + test design) so a mid-fix restart continues smoothly — the plan file is the resumption document.

TESTS: plan_file.rs round-trip (serialize→parse preserves detailed_steps; absent → None; old plans without the section parse fine); plan.rs tool tests — create_plan(kind=bug_fixing, bug, steps=[detailed]) → the plan FILE on disk contains the detailed steps + the 4-step skeleton, the result states the persistence (extend the existing create_plan_bug_fixing_forces_skeleton_and_persists_symptom at :1751 or add a sibling); prompt.rs tests — the Executing tail contains "BUG:" and "CONTEXT:" lines, truncation holds the budget; the simulated-resume test — a fresh Workflow::load_latest on the written plan file → the tail surfaces bug + context (the full detail readable from the file).

NOTE: this plan's own detailed steps are discarded by the CURRENT code (the defect being fixed) — this context param is the resumption carrier for this plan; the fix makes future bug plans carry their steps in the file.

## Steps
- [x] 1. **Reproduce with failing regression test** — Write a regression test that reproduces the defect (constitution: every defect gets a regression test that fails without the fix and passes with it). Run it and confirm it FAILS.
- [x] 2. **Document root cause** — Investigate and document the root cause. memory_write a BUG: record (symptom → root cause → fix + regression test name, ≤600 chars).
- [x] 3. **Minimal fix** — Apply the minimal fix that makes the regression test pass. Do not refactor unrelated code.
- [x] 4. **Verify** — Run the regression test + the full test suite (cargo test unpiped, warning-free). Record the regression test name via update_plan (regression_test field) — finish is blocked without it.

## Bug
A bug_fixing create_plan call carrying detailed, self-contained steps (exact paths, line anchors, test designs, commands) silently REPLACES them with the locked 4-step skeleton — the plan file keeps the bug + context params but loses every step-level recipe. A crash/restart mid-fix then resumes on generic step titles ("Reproduce", "Minimal fix") and must re-derive the whole fix + test design from scratch. User report 2027-01-07 (plan 96e2862a): 6 detailed steps silently lost this way. The premise of restarting mid-work requires the plan file to be the COMPLETE resumption document.

## Regression test
create_plan_bug_fixing_persists_detailed_steps_section
