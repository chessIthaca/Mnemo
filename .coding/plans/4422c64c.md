# Plan: Review round: provider comms vs public docs, perf/memory-loss, cache hit rate 5, security

## Goal
Produce four reviewer reports plus an executive summary covering: (1) provider communication flags/workarounds (LiteLLM/OpenAI/Anthropic) verified against current public docs and community sources, (2) performance bottlenecks and message/data-loss paths in the agent communication pipeline, (3) cache hit rate analysis from fresh log data with ranked concrete improvement suggestions, (4) a whole-app security review identifying what could be tightened. No source-code changes — reports and analysis files only.

## Kind
research

## Context
Research/review round requested by user 2027-01-09: "spin up reviewers to verify our whole communication, flags, and workarounds with LiteLLM and OpenAI and Anthropic and compare them to the newsgroups/internet; look for additional performance and memory loss in the agent communication (goal: fastest agent out there); use the data in the log files to analyze once more how our cache hit rate could be improved — make suggestions; another security review of the whole app — what could be tightened; create report files and prepare an executive summary."

Baseline: HEAD c18b5d0 on wt/agenticcoding, clean tree. Prior work this builds on: cache-hit rounds 1-4 (.coding/analysis/cache-hit-*-report.md; R1 stable head, R5 CONTEXT_FOOTER, R9 dynamic output cap, R12 heuristic fix, R13 fill_rate 0.3, R17 output budget — R12/R13/R17 batch landed 2026-12-31 per SPEC 2026-08-31-r12-r17); the ~340K-token litellm proxy cache cliff (cache-hit-4-report.md); vendor-policy work ROLLED BACK 2027-01-09 (main reset to e0ac5dd, reasoning-leak bug OPEN — see knowledge/bug/2027-01-07-deepseek-glm-reasoning-tokens-in-main-chat-open.md); GLM stop boundaries (e20b1df) and 429-fallback (e0ac5dd) still in. Prior full review rounds: 2026-09-08 (ccff0138), 2026-12-17, 2026-12-29 (72329f2c) — this round re-examines with fresh data and a provider-verification focus.

Key constraint: role:"reviewer" agents are READ-ONLY (no shell) — they cannot run Python/sqlite3 themselves, so step 1 pre-extracts compact aggregates they can read with file tools. Data sources: .coding/logs/traces.jsonl (1.3MB, opt-in full traces), .coding/logs/provider-errors.jsonl (332KB), .coding/memory.db request_stats table (always-on per-request stats; post-R12 rows carry REAL provider-reported cached tokens, not the heuristic). Reviewers have web_fetch for public-doc verification. Reports land in .coding/reviews/ (reviewer-authored via write_review_report); the executive summary is an analysis artifact I author in .coding/analysis/ (not protected).

## Steps
- [x] 1. **Extract fresh log aggregates** — Run a Python script (shell, read-only inputs) over .coding/logs/traces.jsonl, .coding/logs/provider-errors.jsonl, and .coding/memory.db request_stats (sqlite mode=ro). First inspect the request_stats schema (SELECT * LIMIT 1), then produce: per-request real cache hit rates over time (post-R12 rows carry provider-reported cached tokens), per-model aggregates (count, avg/max prompt tokens, hit rate, resets <50%), reset clustering vs prompt size (does the ~340K litellm cliff still bite after R13 fill_rate 0.3?), error histogram by (http_status, model) with time clustering, ttft/generation/send latency percentiles, and the trend since 2026-12-31 (post R12/R13/R17). Write compact text aggregates to .coding/analysis/cache-hit-5-aggregates.txt (plus a per-request CSV if useful) so the read-only cache reviewer has clean data without shell access.
- [x] 2. **Spawn 4 reviewers in parallel** — spawn_agent role:reviewer x4 in one message (parallel), each with a self-contained task (they see no conversation): (a) provider-comms → .coding/reviews/2027-01-09-provider-comms-review.md — verify src/provider/* request flags, headers, and workarounds against current OpenAI/Anthropic/LiteLLM public docs and community sources via web_fetch; explain the glm-5.3-flash* zero-cache signal. (b) perf-comm → .coding/reviews/2027-01-09-perf-comm-review.md — latency bottlenecks + message/data-loss paths in the agent communication pipeline; explain ttft_ms=0 (28,267/28,506 rows) and 'stream aborted — consumer dropped'. (c) cache-hit → .coding/reviews/2027-01-09-cache-hit-5-review.md — analyze .coding/analysis/cache-hit-5-aggregates.txt + code; ranked R18+ suggestions. (d) security → .coding/reviews/2027-01-09-security-review.md — whole-app tightening review. Each report opens with the verdict header; every bug finding carries symptom → root cause → location so the executive summary can consolidate a bug list.
- [x] 3. **Synthesize executive summary** — Read all four reports from .coding/reviews/, synthesize cross-cutting themes, a ranked top-actions list, AND a consolidated bug list with explanations (user-requested 2027-01-09: every bug-related finding across the four reviews, each explained — symptom, root cause, location, suggested fix). Write .coding/analysis/2027-01-09-review-round-executive-summary.md and present the summary to the user in the final response.
- [x] 4. **Commit artifacts** — Commit the four reviewer reports, the cache-hit-5 aggregates + extraction script, and the executive summary to wt/agenticcoding (never main). Keep the commit scoped to .coding/ artifacts only — no source changes in this plan.
