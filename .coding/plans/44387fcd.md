# Plan: Search/memory findings round F1–F13 (folded backlog 70f5b248 + b68121e8)

## Goal
Ship the folded findings round: F1 (memory_update successor-reindex bug) + F12 (supersede pointer stamping), F7 glob shapes, F10 index freshness, F2 filename fallback + current_plan path, F8 rider title boost, F9 known-memory note, F11 consolidation nudge, F13 read-only steering verification; explicitly skip F3/F4/F5/F6 (convention-only).

## Kind
implementation

## Context
Folded backlog items 70f5b248 (F1–F9) + b68121e8 (F10–F13) per the addendum's instruction. Key verified sites: F1 — src/memory/indexer.rs reindex_knowledge_files:215 (build_knowledge_metas:605 resolves supersedes only within the passed subset; tool/memory/mod.rs:301 passes only the touched rel; symptom "supersedes 'slug' not found"); KnowledgeStore::resolve (knowledge.rs:723) probes predecessor files by type+slug. F12 — knowledge.rs::supersede:639 computes the successor slug from fmt_date(now()) — so the finding's "predecessor's created date" claim must be VERIFIED first (the observed 2026-08-29-prefixed successor was plausibly hand-named; git log --follow it). Unambiguous gap: hand-written "File: <path>" pointer lines in successor bodies are never validated vs new_rel. F7/F2/F9/F10 — src/tool/agent/search.rs (validate_glob:126, walk_searchable:175, try_index:446, execute:564; zero-match output ~:704), search_read.rs shares walk_searchable/try_index; glob crate ignores braces; bare-** tail failure cause unverified (knowledge file 2026-08-29-search-glob-shapes-extension-anchored-only-bare.md). F10 — cg_content_meta(path, content_hash) has no mtime (codegraph/schema.rs:93, writer store.rs:216) → add mtime column + migration. F8 — src/tool/steering.rs recalled_context_block:60 (broad limit 3, no title input; caller plan.rs recall_rider:513 has title). F9 — SearchTool::new(sandbox, graph) at factory.rs:736; deterministic hit via title_prefix("PLAN: backlog <uuid>") (MemoryFilter.title_prefix exists, types.rs:402). F11 — record_tool_event src/memory/mod.rs:1944 (trait :363) tags working rows with session; dispatch choke point src/agent/dispatch.rs:393; consolidation filters by source_session_ids (consolidation.rs:114-118). F13 — steering_stats.rs MARKERS:194, in-process counters via get_steering_stats IPC (Trace tab; browser group can read it). New finding this session (record as F14, no code): regex-shaped symbol hunts ("fn .*watcher|struct.*Watcher") get no graph nudge. F3 skipped (low value per finding); F4/F5/F6 convention-only, already HOW memories — no code, record explicit skip decision. F6 constraint: schema description changes must respect factory tools_array char budgets. Project rules: cargo test before each step completes; #![deny(warnings)] — zero-warning build; Windows/PowerShell.

## Steps
- [x] 1. **F1 regression test (red)** — In src/memory/indexer.rs tests (near reindex_knowledge_files_updates_just_the_given_files:1705): tempdir knowledge dir with a predecessor file (spec/a.md, front matter title/created) and a successor (spec/b.md with supersedes = "<pred slug>"). Full index first, then call reindex_knowledge_files(store, dir, &["spec/b.md"]) after editing b.md's body. Assert: no report errors, b's row digest updated. It FAILS today with "supersedes 'slug' not found" (the F1 symptom from tool/memory/mod.rs:301) — that red run is the reproduce step.
- [x] 2. **F1 fix: predecessor context** — reindex_knowledge_files (src/memory/indexer.rs:215-324): after parsing requested rels, for each record with supersedes=Some(slug) whose slug is NOT in the parsed subset, probe disk for the predecessor file — successor's type dir first, then other type dirs (mirror build_knowledge_metas' preference order; same probe KnowledgeStore::resolve does at knowledge.rs:723) — and parse found files into the records+sources set as context. Keep the existing "supersedes not found" error only when the file is absent on disk too. Post-loop, mirror index_derived's meta reconciliation (indexer.rs:173-188): re-assert set_derived_metadata for every expanded-set row that exists, so an unchanged predecessor's superseded_by still flips. Step 1's test goes green. Add a tool-level regression in src/tool/memory/mod.rs tests (reuse the with_knowledge harness): memory_update on a successor's knowledge id succeeds and the digest row reflects the new content.
- [x] 3. **F12: supersede pointer stamping** — VERIFY FIRST (F5 discipline): git log --follow + git show on .coding/knowledge/decision/2026-08-29-recall-rider-vs-explicit-memory-search-mid-sessi.md to learn how it was created — knowledge.rs::supersede:639 already dates the slug fmt_date(now()), so only fix naming if the evidence shows a real bug. Unconditional fix in MemorySupersedeTool (src/tool/memory/mod.rs:813-867): after Ok(new_rel), scan args.content for lines matching "File: .coding/knowledge/<...>.md"; if any path differs from new_rel, rewrite those lines to the canonical ".coding/knowledge/{new_rel}" via KnowledgeStore::update(new_rel, None, corrected_body) + targeted reindex [new_rel] (works post-F1), and mention the rewrite in the success message. Tests in src/tool/memory/mod.rs + knowledge.rs: stale hand-written pointer is rewritten to the real path; a correct pointer is left untouched; successor slug carries the pinned clock's date (guards the naming claim).
- [x] 4. **F7: glob shapes** — src/tool/agent/search.rs: (1) write characterizing tests FIRST — tempdir with frontend/src/a.ts + b.tsx; walk_searchable with glob "frontend/src/**" and "**/*.{ts,tsx}" must return 2 files (currently 0). Run them to observe the actual glob-crate failure cause (knowledge file says unverified). (2) Fix: brace expansion — expand_braces(glob) -> Vec<String> (recursive, capped ~32 patterns); walk_searchable:175 and try_index:464 match a path if ANY expanded glob::Pattern matches; bare-** tail fix per the diagnosed cause (likely normalize a trailing "/**" to "/**/*" if the crate requires a following component). (3) Zero-file hint: in search execute (~line 703) and search_read, when the walk searched 0 files AND args.glob.is_some(), output "no matches found — glob matched no files (unsupported shape? extension-anchored patterns like **/*.rs are safest)" instead of the plain no-matches line. search_read reuses the shared helpers — verify both call paths with tests.
- [x] 5. **F10: index freshness check** — src/codegraph/schema.rs: add column mtime INTEGER NOT NULL DEFAULT -1 to cg_content_meta (CREATE for new DBs + idempotent ALTER TABLE for existing ones, guarded by a pragma table_info check — follow the file's existing migration style). src/codegraph/store.rs content writer (~lines 177-216): stamp the file's fs mtime alongside content_hash. src/tool/agent/search.rs try_index:446: gain the sandbox root param; after the glob post-filter, stat the DISTINCT displayed-hit paths (cap ~100) — any on-disk mtime differing from the stored row (stored -1 = unknown → trust) means stale: return a Stale{files} outcome instead of hits; callers (search execute ~617-647, search_read's index path) then fall through to the walk and prepend a note "note: content index stale for N file(s) — serving tree-walk results". Regression test (make_indexed_tool harness): index a file, rewrite it with new content + bump mtime via std::fs::FileTimes::set_modified, literal search must return the NEW content.
- [x] 6. **F2: filename fallback + plan path** — src/tool/agent/search.rs walk path: when results.is_empty(), match the pattern against the walked files' project-relative paths (regex: re.is_match(rel); literal: case-insensitive contains) — if any match, append "no content matches; N file path(s) match the pattern: <first 5 rel paths>". Factor a small helper so search_read gets the same fallback. CurrentPlanTool (src/tool/workflow/plan.rs:1398): add a plans_dir field (wire it in src/agent/factory.rs register_workflow_tools), and when a plan is active include the plan file ".coding/plans/<id>.md" (fs existence-checked; omit silently when absent) in both output text and data JSON. Tests: filename fallback fires on a path-only match and stays silent otherwise; current_plan output includes the path (and the existing current_plan tests keep passing).
- [x] 7. **F8: rider title-term boost** — src/tool/steering.rs: recalled_context_block / recalled_context_block_at gain a title: Option<&str> param (query stays as-is). Broad pass: recall_peek limit 3 → 8 (new CANDIDATES const); after the age-gate, stable-partition hits whose lowercase title contains any significant title term (term = lowercase alphanumeric run, len ≥ 4, split on non-alphanumeric), then truncate the broad hits to 3 — bug pass, dedupe, RIDER_HITS cap unchanged. Callers: plan.rs recall_rider (~513) passes Some(title); spawn_agent (src/tool/agent/spawn_agent.rs:285) passes None (behavior unchanged). Tests: a SPEC-titled row sharing title terms but ranked below 3 noise rows still rides; None-title ordering is unchanged (existing steering tests must keep passing).
- [x] 8. **F9: known-memory-hit note** — SearchTool + SearchReadTool (src/tool/agent/search.rs / search_read.rs): add a with_memory(Option<Arc<dyn MemoryStoreTrait>>) builder; wire the memory store in src/agent/factory.rs:736+ (same Arc the memory tools get). Gate: pattern is uuid-shaped (cheap regex, no store touch otherwise) → store.list_filtered(MemoryFilter title_prefix = format!("PLAN: backlog {id}").limit(1)) → on hit, prepend via merged_note: "known memory hit: '<title>' — a queued/shipped backlog item; memory_search for detail". Add MarkerKind::KnownMemoryHit to src/agent/steering_stats.rs (fired-only: detect on search/search_read first line, targets [], MARKERS array + Inner::counts size + index()). Commit-sha-shaped patterns are out of scope (no cheap deterministic lookup) — record that residual in the SPEC note. Tests: uuid pattern with a matching derived row gets the note; miss and non-uuid patterns stay silent (and never touch the store).
- [x] 9. **F11: consolidation-due nudge** — Read src/agent/dispatch.rs:393 (dispatch_with_interrupt) + record_tool_event (src/memory/mod.rs:1944, trait :363) first. Add a once-per-session check at the dispatch choke point: after observe_result, count this session's working rows (list_by_tier(Working) filtered by source_session_ids contains session — same shape as consolidation.rs:114-118; cache the count per dispatch, it is advisory) and when the count FIRST crosses CONSOLIDATION_NUDGE_THRESHOLD = 15, append one line to that tool result's output: "NOTE: {n} working-memory events accumulated this session — consider memory_consolidate(session_id) to distill them". Add MarkerKind::ConsolidationDue (fired-only, any tool, first-line detect on "consider memory_consolidate"). The note must never fail or delay a tool call (best-effort, errors swallowed). Tests: crossing 15 fires exactly once (16th, 20th events stay silent); below threshold silent.
- [x] 10. **F13: steering verification (read-only)** — Run cargo test (full suite, zero warnings). Then load_tools(browser) and read the app's Trace tab (get_steering_stats surface): record fired/switched numbers for all markers incl. the two new ones (known-memory-hit, consolidation-due), verify counters moved for markers this session exercised (search-nudge, literal-tip at minimum; recall-rider fired at plan creation). If any numbers look wrong (e.g. a marker that fired visibly but counts 0), open a focused fix; otherwise record the numbers in the SPEC note. Also fold in the fresh steering-coverage finding from this session as F14 in the round's knowledge file: regex-shaped symbol hunts ("fn .*watcher|struct.*Watcher", alternation patterns) get no graph nudge — candidate for a future round (extend symbol_nudge to alternation-regexes that reduce to identifiers); NO code this round.
- [x] 11. **Docs, knowledge, close-out** — Update README.md + PLAN.md for changed tool behavior (glob shapes incl. braces, zero-file glob hint, filename fallback, stale-index note, known-memory-hit note, consolidation nudge, rider title boost, supersede pointer stamping) — keep tool schema descriptions within the factory char budgets (F6; pay for additions with trims). Update the now-stale knowledge file .coding/knowledge/how/2026-08-29-search-glob-shapes-extension-anchored-only-bare.md to reflect the fixed shapes. Write the round's knowledge: SPEC for the shipped changes (paths + evidence from F12 verification + F13 numbers), DECISION recording the explicit skips (F3 low-value, F4/F5/F6 convention-only — already HOW memories). Mark backlog items 70f5b248 and b68121e8 done via backlog_status. Commit everything (including the riding .coding/backlog.jsonl in_flight change) to the working branch.
