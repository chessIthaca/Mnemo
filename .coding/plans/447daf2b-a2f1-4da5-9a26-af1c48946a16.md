# Plan: R9 max_completion_tokens cap + R10 repetition detector + model-switch summarization

## Goal
Fix the max_completion_tokens bug (R9), add a repetition detector to the stream parser (R10), and summarize before switching to a model with a smaller context window.

## Kind
implementation

## Context


## Steps
- [x] 1. **R9: Dynamic max_completion_tokens cap** — In src/provider/openai.rs build_request_json (~line 1203): replace `let max_completion = self.caps.max_output_tokens.max(4096);` with a dynamically capped value. Add a private `fn estimate_prompt_tokens(messages: &[Message], tools: &[ToolSchema]) -> usize` that sums content char lengths / 4 + 4 per message + tool schema JSON char lengths / 4 (conservative overestimate, same chars/4 fallback ContextManager uses). Then: `let prompt_est = estimate_prompt_tokens(messages, tools); let max_completion = self.caps.max_output_tokens.max(4096).min(self.caps.max_context.saturating_sub(prompt_est).saturating_sub(1024)).max(1024);` — the final .max(1024) ensures we never send 0. Apply the SAME fix in src/provider/anthropic.rs build_request_json (find its max_tokens line, ~line 230). Also lower the default in src/provider/mod.rs Capabilities::openai() max_output_tokens from 64_000 to 32_000 (R9b). Tests in openai.rs: (a) prompt small → cap = max_output_tokens (unchanged); (b) prompt near max_context → cap shrinks to fit; (c) prompt exceeds max_context → cap floors at 1024. Run cargo test unpiped.
- [x] 2. **R10: Repetition detector in stream parser** — In src/provider/openai.rs, find the SSE stream parsing loop inside the `complete` method (where LlmEvent::TextDelta chunks are accumulated). Add a repetition guard: track the accumulated response text in a String; after each TextDelta append, check whether the last N=200 chars of the accumulated text appear 3+ times consecutively at the end (i.e., the string ends with the same 200-char window repeated 3x). If detected, break the stream loop and emit a single LlmEvent::Error { message: "repetition detected — stream aborted to prevent token waste" } then LlmEvent::Done. Extract the detection into a pure `fn detect_repetition(text: &str, window: usize, threshold: usize) -> bool` so it is unit-testable. Tests: (a) detect_repetition returns true for a 600-char string of the same 200-char window x3; (b) returns false for normal prose; (c) returns false for text shorter than window*threshold. Run cargo test unpiped.
- [x] 3. **Model-switch summarization before swap** — When set_model switches to a provider with a SMALLER max_context than the current one, defer the swap so the turn loop can summarize using the OLD provider first. Changes: (1) src/agent/loop_impl.rs: add `pub(crate) pending_swap: Arc<tokio::sync::Mutex<Option<PendingSwap>>>` to AgentLoop where PendingSwap = struct { provider: Arc<dyn LlmClient>, context_manager: ContextManager, model: String }. Init None in both constructors. Add `pub fn set_pending_swap(&self, provider, context_manager, model)` storing into the mutex. (2) Modify set_explicit_provider: if new caps.max_context >= current caps.max_context, swap immediately (as today). If smaller, call set_pending_swap instead of set_provider — the swap is deferred. (3) src-tauri/src/ipc/config_io.rs swap_provider_into_loop: after calling set_explicit_provider, check if pending_swap was set (add a pub fn take_pending_swap on AgentLoop returning Option<PendingSwap>); if so, emit an AgentEvent::Error { error: "Switching to a smaller-context model — conversation will be summarized on the next turn", retrying: false } so the UI shows the user what happened. (4) src/agent/turn.rs run_turn: at the very top (before the summarize check), take pending_swap; if Some: compute current token count via ContextManager::count_tokens(&messages); if count > new_max_context * 0.8, call summarize_with_interrupt using the CURRENT (old) provider; after summarization (or if not needed), call self.set_provider(pending.provider, pending.context_manager) to complete the swap; emit a log/note. If None, proceed normally. Tests in tests.rs: (a) set_explicit_provider with larger context → immediate swap, pending_swap stays None; (b) set_explicit_provider with smaller context → pending_swap set, provider unchanged; (c) run_turn with pending_swap + oversized conversation → summarize called with old provider, then swap completes. Run cargo test unpiped.
- [x] 4. **Full verification + review + commit** — Run cargo test unpiped from repo root (all test-result lines green, zero warnings under deny(warnings)). Run npm run build (frontend tsc + vite — no frontend changes expected but confirms nothing broke). Run cd src-tauri && cargo build (bin crate compiles). Then spawn a read-only reviewer subagent (role:reviewer) over ALL uncommitted changes with a self-contained task: plan goal (R9 dynamic max_completion_tokens cap, R10 repetition detector, model-switch summarization), changed files, focus points: R9 cap is conservative (overestimates prompt → tighter cap, never looser), never floors below 1024, applied to both openai + anthropic; R10 detector is pure + tested, threshold/window tuned to avoid false positives on legitimate repeated content (code patterns), stream is cleanly broken with Error+Done; model-switch pending_swap defers correctly, summarization uses OLD provider, swap completes after, no deadlock on the pending_swap mutex, AgentLoop constructors init None. Fix EVERY finding, re-run cargo test. Commit to the feature branch with a clear message.
