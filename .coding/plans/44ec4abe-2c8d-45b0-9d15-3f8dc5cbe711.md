# Plan: Reasoning bar: non-cached sent tokens, output-only tok/s, shared number format

## Goal
Rework the reasoning bar (InflightBar): drop the input tok/s rate, show "tokens sent" as prompt−cached (non-cached only), and apply a unified hybrid number format (≤1 decimal, commas under 10K, K suffix to 1M, M suffix above) to every token display except the Trace view.

## Context
InflightBar.tsx: local formatTokens/formatRate; ↑ counter uses tokenUsage.prompt (line 74, 190); input rate span at lines 211-217 (sessionInputRate computed at 64-67); ctx popup and counts reuse formatTokens. StatsView.tsx: local fmtTokens/fmtRate/fmtInt/fmtCost (lines 29-54) used for token counts and tok/s rates. LlmTraceView.tsx keeps its own fmtInt (excluded from the rule). TokenUsage has a `cached` field (subset of prompt), accumulated per Usage event in agentEventReducer.ts (line 343). User chose the hybrid format: <10K raw comma-grouped ("1,234"), 10K-1M "12.3K", ≥1M "1.2M", ≤1 decimal everywhere.

## Steps
- [x] 1. **Add shared formatter lib/format.ts** — create frontend/src/lib/format.ts exporting fmtTokens(n: number): string and fmtRate(n: number): string (both with doc comments). Both delegate to one internal hybrid formatter: sign handling, then if |n| >= 1_000_000 → round1(n/1e6).toLocaleString("en-US", {maximumFractionDigits:1}) + "M"; else if |n| >= 10_000 → same with /1e3 + "K"; else → round1(n).toLocaleString("en-US", {maximumFractionDigits:1}) (commas). round1 = Math.round(v*10)/10. Module doc comment states the hybrid rule and that the Trace view is exempt.
- [x] 2. **Rework InflightBar counters** — in frontend/src/components/chat/InflightBar.tsx: delete the local formatTokens/formatRate functions (lines 11-22) and import { fmtTokens, fmtRate } from "../../lib/format". Change the ↑ target: const promptDisplay = useCountUp(Math.max(0, tokenUsage.prompt - tokenUsage.cached)); and change its title to "Prompt tokens sent (excludes cached)". Delete sessionInputRate (lines 64-67) and its display span (lines 211-217) — keep avgTtftMs for the tooltip and sessionOutputRate for the ↓ rate. Trim the comment block at lines 52-55 (drop the avgInputRate sentence). Replace remaining formatTokens/formatRate call sites with fmtTokens/fmtRate.
- [x] 3. **Unify StatsView formatters** — in frontend/src/components/views/StatsView.tsx: delete the local fmtTokens (lines 29-32) and fmtRate (lines 39-44) functions, keep fmtInt/fmtCost/fmtTime, and import { fmtTokens, fmtRate } from "../../lib/format". Update the module doc comment (lines 23-26) to describe the new hybrid rule. All existing fmtTokens/fmtRate call sites keep working (same names).
- [x] 4. **Add formatter unit tests + run tests** — check frontend/package.json for the test script (vitest). If vitest exists, add frontend/src/lib/format.test.ts covering: 999→"999", 1000→"1,000", 1234.5→"1,234.5", 9999→"9,999", 10000→"10K", 12345→"12.3K", 999949→"999.9K", 1e6→"1M", 1234567→"1.2M", 1234567890→"1,234.6M". Run the frontend test command (unpiped, read $LASTEXITCODE) and then cargo test; fix any failures.
- [x] 5. **Review, fix findings, commit** — standard closing sequence: spawn a reviewer subagent (role reviewer) covering ALL uncommitted changes with a self-contained task (files: frontend/src/lib/format.ts + format.test.ts, InflightBar.tsx, StatsView.tsx; goal: non-cached sent tokens, output-only tok/s, hybrid formatting; check correctness, bugs, constitution compliance incl. warning-free build). After it finishes, read the .coding/reviews/ report, fix every finding, re-run tests, then commit to the current feature branch with a clear message.
