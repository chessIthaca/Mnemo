# Plan: Merge-to-main banner: drop build-verify and steer mentions

## Goal
Trim the merge_to_main skill banner (MergeToMainDialog.tsx): remove the "Verify both builds" mention from the third summary bullet and the "You can interrupt or steer the agent mid-skill to help resolve conflicts" sentence, per user feedback. Text-only UI change — the skill procedure in .coding/skills/merge_to_main.toml and the developer-facing comments stay intact.

## Kind
implementation

## Context
User feedback on the merge_to_main banner: it need not narrate build verification or the interrupt/steer affordance. The banner text lives in frontend/src/components/layout/MergeToMainDialog.tsx (:74-81); search confirms "interrupt or steer" occurs only there, and "Verify both builds" also appears in StatusBar.tsx:192 as a developer-facing comment mirroring .coding/skills/merge_to_main.toml step 4 — that comment and the TOML procedure stay untouched (step 4 keeps both-build verification with recorded rationale: npm run build catches TS-only breaks; cd src-tauri && cargo build compiles the bin crate the root build never sees; post-merge breaks shipped repeatedly before the rule). Only the user-facing narration shrinks: bullet :76 loses "Verify both builds, " and the paragraph :78-82 loses the sentence "You can interrupt or steer the agent mid-skill to help resolve conflicts." while keeping the approval sentence. Verify with npm run build (frontend/) then cargo test at the repo root, unpiped, reading $LASTEXITCODE per the constitution. Pre-existing .coding/ bookkeeping (modified .coding/plans/506b85e2.md step-4 check-off, untracked .coding/knowledge/bug/506b85e2.md) rides along in the closing commit.

## Steps
- [x] 1. **Trim the two banner mentions in MergeToMainDialog.tsx** — In frontend/src/components/layout/MergeToMainDialog.tsx: (a) line 76 bullet "• Verify both builds, delete the merged branch, return to Planning" → "• Delete the merged branch, return to Planning"; (b) in the approval paragraph (lines 78-82) replace "Autonomous mode. You can interrupt or steer the agent mid-skill to\n              help resolve conflicts." with "Autonomous mode." — keeping "Core git operations (merge, push) always require approval, even in Autonomous mode." and the never-stashes bullet at line 74. No changes to .coding/skills/merge_to_main.toml (procedure unchanged) or StatusBar.tsx:189-193 (developer-facing flow comment mirrors the real procedure).
- [x] 2. **Verify both builds** — Verify the changed file frontend/src/components/layout/MergeToMainDialog.tsx compiles clean: from frontend/ run npm run build (typechecks the TSX change); from the repo root run cargo test unpiped and read $LASTEXITCODE directly (never trust a piped exit code). Both must be green and warning-free before proceeding to the closing sequence.
