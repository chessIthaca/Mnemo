# Plan: Remove #[allow(dead_code)] — gate len/is_empty with #[cfg(test)]

## Goal
Remove the #[allow(dead_code)] suppressions on PendingQuestions::len/is_empty and PendingApprovals::len/is_empty. Gate all four methods with #[cfg(test)] so they exist only in the test build (where they're used) and are absent from the production binary — no dead code, no suppression.

## Context
User rejects #[allow(dead_code)] as a matter of principle ("we never allow dead code"). Verified via search: in both questions.rs and approval.rs, the len()/is_empty() methods are called ONLY from their own #[cfg(test)] modules — never from production code. The map.len()/before-map.len() calls are on the inner HashMap, not these methods. So they are genuinely test-only accessors. Fix: gate them with #[cfg(test)] so they don't exist in the production binary (no dead code, no suppression) and remain used in tests. Apply to both questions.rs (already has #[allow(dead_code)] added this session — replace it) and its twin approval.rs (has pre-existing #[allow(dead_code)] at lines 53,59 — replace those too).

## Steps
- [x] 1. **Gate questions.rs len/is_empty with #[cfg(test)]** — in src-tauri\src\ipc\questions.rs, replace the `#[allow(dead_code)]` attribute above `pub fn len` (line ~45) with `#[cfg(test)]`, and the one above `pub fn is_empty` (line ~50) with `#[cfg(test)]`. Keep the doc comments. This removes them from the production binary while keeping them available to the test module below.
- [x] 2. **Gate approval.rs len/is_empty with #[cfg(test)]** — in src-tauri\src\ipc\approval.rs, replace the `#[allow(dead_code)]` at line 53 (above `pub fn len`) with `#[cfg(test)]`, and the one at line 59 (above `pub fn is_empty`) with `#[cfg(test)]`. Keep the doc comments. Same rationale: test-only accessors.
- [x] 3. **Run cargo test and confirm zero warnings** — run `cargo test` in src-tauri; confirm no `dead_code` warnings on either file and all tests pass. Check `$LASTEXITCODE`.
