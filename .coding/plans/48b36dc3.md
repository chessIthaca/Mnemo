# Plan: Harden file_edit / tool-emission pipeline: delivered-args tap, artifact validation, freshness contract, gate telemetry, size warning

## Goal
The five hardening asks of backlog e8b39d72: (H1) raw delivered tool-call args captured per trace record as generation-vs-harness discrimination evidence; (H2) file_edit pre-write artifact validation with escape-hatch error; (H3) freshness contract — gate threshold 1, any drift failure forces a read before the next edit; (H4) gate-interception telemetry surfaced in the Trace panel; (H5) an emission-fragility size warning at the observed ~700-char threshold.

## Kind
implementation

## Context
Root cause already pinned (knowledge record .coding/knowledge/bug/2026-12-20-code-bearing-tool-payload-emissions-degrade-unde.md, plan d0084b1d): model double-escaping \\n, transport verified clean (parse_sse_chunk verbatim, DeltaAccumulator push_str, normalize_line_endings passes literal \\n through, regex replacen amplifies); the denormalize_literal_newlines safety net already landed (file_edit.rs:201/502) plus regex-mode warnings. THIS task is the second hardening round (backlog e8b39d72-2328-4f5a-8f87-a327be2c4af2) — the asks are engineering features, hence implementation kind. Key sites: trace.rs owns LlmRequestRecord (:118; created :414 with response_raw:None; with_record :669; enforce_raw_budget :892 evicts response_raw under pressure; snapshot :1074) — response_raw (SSE reassembly) exists but is evictable, so a dedicated raw_tool_calls field (id/name/arguments VERBATIM as delivered, per-call + total capped) populated at provider stream completion is the discriminating tap (H1): future incidents compare delivered-args vs executed-args to separate generation decay from harness mutation. file_edit prepare_edit is pure/testable — the artifact validation (H2) runs on the computed new_content BEFORE Ok: markdown-heading lines ('## ' line starts) in non-markdown code files (.rs/.ts/.tsx/.json/.toml — Rust has no markdown headings; escape hatch in the message for legit cases: file_write full-file replacement), sentinel whole-values (trimmed new_string exactly 'unused'/'placeholder' — the fragment-replacement artifact), adjacent byte-identical doc-lines ('///' duplicated — the duplication artifact), delimiter-balance smoke-parse (unbalanced {}/[]/() or unterminated quotes — truncation) — rejected as Error::InvalidInput with an EMISSION-ARTIFACT message (re-emit carefully / smaller fragments / file_write) — deliberately NOT the drift nudge (EditStaleRead) and NOT counted by that marker: a re-read cannot fix a corrupted emission. H3: should_gate_file_edit threshold ESCALATION_THRESHOLD(2) → 1 (any drift failure gates the next edit until a read; C3 escalation stays at 2) — completes backlog 714196da's stricter intent; update my prior tests (should_gate_file_edit_arms_after_threshold_and_lifts_after_a_read asserts Some(2) at 2 fires → now Some(1) at 1) + README + the SPEC memory. H4: steering gains a per-agent gated counter incremented in file_edit_redirect; SteeringMarkerCounts gains a third field (gated) in snapshot + frontend types (lib/types.ts ~:403) + the LlmTraceView by_marker title (~:976) — the panel renders the marker array dynamically so the label update is the only UI work. H5: advisory warning appended to the file_edit result when max(old_string,new_string).len() >= 700 (the incident's observed fragility point): large-payload emission-fragility note steering toward smaller fragments/file_write. Frontend untouched except types + LlmTraceView label.

## Steps
- [x] 1. **H1: delivered-args tap in the trace record** — src/provider/trace.rs: add pub raw_tool_calls: Option<Vec<RawToolCallRecord>> to LlmRequestRecord (new struct: id, name, arguments — the arguments string VERBATIM as the model delivered it, post-JSON-parse, pre-any-normalization; each call capped e.g. 4KiB, the total capped e.g. 64KiB, NOT subject to response_raw eviction — the point is surviving budget pressure). Populate at provider stream completion: find the with_record site where finish_reason/usage land (openai.rs stream end / the provider layer that owns the DeltaAccumulator; the finalized tool_calls' raw arguments strings go in verbatim). Add a setter mirroring the existing record-updater style (e.g. set_raw_tool_calls(id, Vec<(id,name,args)>)). Snapshot/serialize the field. Frontend: lib/types.ts LlmRequestRecord mirror gains raw_tool_calls; the trace detail view (frontend traceStats.ts / the detail rendering component) shows the delivered args section per tool call. Tests: trace record population from a synthetic finalized accumulator (verbatim args preserved incl. literal backslash-n), per-call/total caps, snapshot inclusion; frontend type/UI minimal (render label).
- [x] 2. **H2: pre-write emission-artifact validation** — src/tool/agent/file_edit.rs: new fn validate_emission_artifacts(path, old_string, new_content/new_string) -> Result<(), Error::InvalidInput> run in prepare_edit BEFORE Ok(PreparedEdit): (a) markdown-heading lines — for non-markdown code files (by extension: .rs/.ts/.tsx/.js/.json/.toml/.css/.html? keep: .rs .ts .tsx .js .json .toml), any line in the EDITED REGION starting with '## ' (or '# ' for .rs? scope: '## ' only — the observed artifact) → reject; (b) sentinel whole-values — trimmed new_string exactly 'unused' or 'placeholder' → reject; (c) adjacent duplicate doc-lines — any two byte-identical ADJACENT lines within the edited region where both start with '///' or '//!' → reject; (d) delimiter-balance smoke-parse — for .rs/.ts/.tsx files, brace/bracket/paren balance + no unterminated quote across the computed new_content → reject (truncation detection). Error message shape: 'edit rejected: emission artifact detected (<class>: <detail>) — the payload looks corrupted (model emission decay, see .coding/knowledge/bug/2026-12-20-code-bearing-tool-payload-emissions-degrade-unde.md). Do not blind-retry: re-emit carefully in SMALLER fragments, or use file_write for full-file replacement. (This is not a content-drift failure — a re-read will not help.)' Tests: each artifact class (markdown heading in .rs, sentinel whole-value, duplicate adjacent doc-line, unbalanced brace) rejected with the message; legit cases pass (a '## ' line in a .md file; 'unused' as a substring not whole-value; balanced content; duplicate non-doc lines e.g. blank lines; existing tests unaffected).
- [x] 3. **H3: freshness contract — gate threshold 1** — src/agent/steering_stats.rs: should_gate_file_edit threshold ESCALATION_THRESHOLD → 1 (fired >= 1 && switched == 0 → Some(fired); document that the C3 escalation note stays at threshold 2 — the gate is now the strict freshness contract: ANY drift failure forces a read before the next edit; backlog 714196da's stricter intent, completed per e8b39d72 H3). src/agent/dispatch.rs: no change (the gate call already consults should_gate_file_edit). Update tests: should_gate_file_edit_arms_after_threshold_and_lifts_after_a_read now asserts Some(1) after ONE failure and None before; add an escalation-stays-at-2 assertion if cheap (the C3 note path is generic — verify via the existing escalation machinery if a test exists, else note in docs). Update README.md:53 steering paragraph (threshold semantics: the gate now intercepts after a single failed edit without a read) and the dispatch gate comment.
- [x] 4. **H4: gate-interception telemetry** — src/agent/steering_stats.rs: add per-agent gated counter for EditStaleRead (HashMap<AgentId, u64> or an [u64; MARKERS.len()] third array — pick the array for symmetry: agent_gated), incremented by a new pub(crate) fn note_edit_gated(agent_id) called from dispatch.rs file_edit_redirect when it returns Some; surfaced in snapshot(): SteeringMarkerCounts gains a third field gated (serialize: label/fired/switched/gated). Frontend: lib/types.ts SteeringMarkerCounts mirror + the LlmTraceView by_marker title/labels (~:976) render the gated count. Tests: gating increments the counter (redirect → snapshot shows gated 1), per-agent isolation.
- [x] 5. **H5: large-payload advisory warning** — src/tool/agent/file_edit.rs execute path: after a successful edit, when max(old_string.len(), new_string.len()) >= 700, append an advisory NOTE to the result output: 'NOTE: large edit payload (~N chars) — emission fragility has been observed near this size in long sessions (backlog e8b39d72 H5); prefer smaller fragments or file_write for full-file replacement.' Advisory only (no rejection, no steering marker). Test: a 700+ char edit result contains the note; a small edit does not.
- [x] 6. **Verify** — cargo test full suite green (test result: lines are the evidence, not piped exit codes — baseline 1877 + 16 passed); npm test --prefix frontend green (baseline 51 files / 727 passed). Zero warnings under deny(warnings).
- [x] 7. **Docs + close** — Docs: README.md:53 steering paragraph updated (H3 threshold + the artifact validation + the tap + size note); file_edit module doc + steering_stats module doc updated (the new marker semantics: gated field, threshold 1); the SPEC memory for the threshold-1 contract supersedes the threshold-2 SPEC (memory_supersede on 'SPEC: file_edit stale-read steering'). Closing sequence: spawn role:reviewer over all uncommitted changes (correctness: tap verbatim-vs-normalized ordering, artifact-validation false-positive audit incl. markdown files and legit substrings, threshold semantics, telemetry wiring, cross-platform neutrality, docs sync, scope) → fix every finding → re-run tests → commit on wt/agenticcoder (include the review report) → finish. Close backlog e8b39d72-2328-4f5a-8f87-a327be2c4af2 done. Document decisions in the final summary.
