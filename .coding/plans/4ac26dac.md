# Plan: Extract save_endpoints' re-sync into resync_runtime_state + delegate provider plan to the tested helper (quality review LOW 5)

## Goal
Make save_endpoints a readable four-step orchestrator by extracting the step-4 re-sync block (settings.rs:228-312 — the riskiest live-state mutation) into a named, documented resync_runtime_state(app, state) -> bool helper, with the provider-rebuild decision (endpoint/model fallback) delegated to the already-tested startup::resolve_startup_provider — removing the fourth inline copy of that chain.

## Kind
implementation

## Context
Quality review LOW 5 (.coding/reviews/2027-01-06-full-review-quality-coverage.md): save_endpoints (src-tauri/src/ipc/settings.rs:179-319 at current HEAD; the review's :179-638 range predates later landings) is organized with numbered section comments, but the step-4 re-sync block (:228-312 — live-state mutation, the riskiest part) is the bulk of the body, making the validate → persist → re-sync flow hard to follow. Fix: extract the re-sync into a named, testable helper, leaving the command a four-step orchestrator. SURVEY FACTS (full file read): the block does (a) provider rebuild + swap — if factory exists: lock config, resolve default endpoint + model (inline fallback chain :238-249), build_client (mnemo::provider::client_factory, imported :18), context_manager_for, swap_live_provider (crate::ipc::config_io, async), eprintln; (b) vision/embedder + model-resolver rewire (:303-312) via rewire_vision_and_embedder/sync_model_resolver (crate::ipc::rewire, imported :21). KEY INSIGHT: the inline endpoint/model chain (:238-249) is a FOURTH copy of the fallback chain already encapsulated + tested as crate::startup::resolve_startup_provider (LOW 4, plan 531fc221, 5 provider tests). Equivalence verified case-by-case: default_provider-named→default_endpoint() ≡ cfg.default_endpoint() (proved in LOW 4 — the or_else chain is redundant with default_endpoint()'s internal fallback); model chains identical (default_model → endpoint's first → "gpt-4o"); when endpoint is None the model is discarded in both shapes. DECISIONS: (1) helper is a private async fn in settings.rs placed right after save_endpoints — signature resync_runtime_state(app: &tauri::AppHandle, state: &State<'_, IpcState>) -> bool (the finding's suggested &new_config param is wrong for the actual code: the block re-reads the RELOADED config from state.project.config, which persist_and_reload already swapped in; keeping the re-lock is the verbatim-safe motion); (2) delegate the endpoint/model resolution to startup::resolve_startup_provider — removes the 4th inline copy, makes the "testable" ask real (the pure decision is the tested helper), and aligns with the LOW 2 backlog direction (that item's two cited mirrors, console.rs + memory_maintenance.rs, are untouched and remain valid); (3) NO new unit tests: the delegated decision is already pinned by startup.rs's five provider tests; the remaining wiring is Tauri-bound (AppHandle + State + factory + live agents), matching this file's established DTO-only test posture — documented for the reviewer to assess; (4) save_settings' different re-sync tail (safety/factory/trace pushes, :836-877) is OUT of scope — the finding names only save_endpoints; (5) add the missing "── 3. Persist + reload ──" header comment for four-step symmetry, and update the command doc comment's step-4 text to reference the named helper. Call-site adjustments from &State/&AppHandle params: swap_live_provider(state, app, ...), rewire_vision_and_embedder(state, &cfg), sync_model_resolver(state, &cfg) — pass the references directly. Both suites required (constitution); src-tauri currently 216 + 4, root 2004 + 16.

## Steps
- [x] 1. **Extract resync_runtime_state + rewire save_endpoints** — (1) In src-tauri/src/ipc/settings.rs, insert a private async fn resync_runtime_state(app: &tauri::AppHandle, state: &State<'_, IpcState>) -> bool immediately AFTER save_endpoints (after :319, before the wire-struct section comment at :321). Doc comment: 'Re-sync the live runtime after an endpoint/key save (step 4 of [`save_endpoints`]).' + the moved rationale (unconditional, not gated on a name change — in-place edits to base_url/kind/multimodal/reasoning effort/API key must take effect live; rebuild from the *reloaded* config; returns whether a swap actually happened — false when no factory or no default endpoint) + a note that the endpoint/model resolution delegates to crate::startup::resolve_startup_provider so the re-sync picks the same endpoint + model the next app start would. Body = the verbatim :228-312 block with exactly these adjustments: (a) the match subject becomes `let (endpoint, model) = crate::startup::resolve_startup_provider(&cfg); match endpoint {` replacing the cfg.default_endpoint() match + inline model chain (:238-249) — keep the kind_label = format!("{:?}", ep.kind), build_client call (with the trace-log wiring comment), context_manager_for, the (Some(provider), Some(context_manager), kind_label, Some(model)) tuple, and the None branch's no-endpoints comment; adapt the validation-guarantee comment to 'The validation in `save_endpoints` guarantees a model resolves (default_model or the endpoint's first model), so the "gpt-4o" terminal fallback is never hit in practice.'; (b) swap_live_provider(state, app, provider, context_manager, model).await — pass the &State/&AppHandle params directly (no & prefix); (c) rewire_vision_and_embedder(state, &cfg) and sync_model_resolver(state, &cfg) — same &State pass-through; (d) keep the eprintln!("save_endpoints: re-synced live provider ({kind_label})") verbatim; (e) return provider_swapped at the end. (2) Rewire save_endpoints: add the `// ── 3. Persist + reload ──` header comment above the persist_and_reload line (:226) for four-step symmetry, replace the whole :228-312 block with `// ── 4. Re-sync the live runtime (provider + vision/embedder/resolver) ──` + `let provider_swapped = resync_runtime_state(&app, &state).await;`. (3) Update the command's doc comment step-4 text (:166-171) to reference [`resync_runtime_state`] by name. No import changes needed (tauri::State already imported :13; build_client :18; rewire fns :21; crate::startup is crate-root reachable).
- [x] 2. **Both Rust suites green** — Run src-tauri `cargo test -- --quiet` unpiped (expect 216 + 4 doc, 0 failed, exit=0 — #![deny(warnings)] means green = zero warnings) and root `cargo test -- --quiet` unpiped (expect 2004 + 16, 0 failed — the library is untouched but is the regression net). Fix any failures before continuing.
- [x] 3. **Closing sequence: review, fix, commit, finish** — Spawn a read-only reviewer (spawn_agent role:'reviewer', complete task args) over ALL uncommitted changes: cite the plan, the quality-review LOW 5 finding, and the design decisions (helper signature without &new_config — the block re-reads the reloaded config; delegation to startup::resolve_startup_provider with the case-by-case equivalence argument; no new unit tests — the delegated decision is pinned by startup.rs's five provider tests and the remaining wiring is Tauri-bound per the file's DTO-only test posture; save_settings' tail out of scope). Ask it to verify: the move is behavior-identical (especially the resolve_startup_provider equivalence: default_provider-named→default_endpoint() ≡ cfg.default_endpoint() in all four cases, model chains identical, None-endpoint model discarded in both shapes), the &State/&AppHandle pass-through adjustments are correct, the four-step orchestrator reads cleanly, the doc comments moved faithfully, no dead code/unused imports, multi-platform neutrality, doc sync. Fix every finding (re-run tests), round-2 verification if any findings, then commit on wt/agenticcoding (include review reports + plan file) and finish.
