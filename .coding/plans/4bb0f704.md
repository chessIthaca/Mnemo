# Plan: Evaluate mitigations for agent mid-task stopping

## Goal
Produce a grounded evaluation of the four proposed mitigations for the agent stopping mid-task instead of chaining obvious consecutive steps, with a recommendation on which to implement and exact proposed text for the cheap wins. No source changes — evaluation only.

## Kind
research

## Context
System prompt lives in src/agent/prompt.rs. CODING_SYSTEM_PREAMBLE (lines 26-42) holds CORE PRINCIPLES — has "Explain briefly" + "Ask when unsure" but no positive chaining instruction. APP_RULES (lines 92-126) error-retry bullet (101-103) says WHAT but no CADENCE. Tool results are role:Role::Tool (turn.rs:1402/1540/1699/1865) — already structurally continuations, not role:user. No throughput signal exists. Project heavily optimizes cache-stable prompt sections (stable head, byte-stable) — favors stable-head text edits over volatile per-turn injections. Co-located tests assert preamble substrings (prompt.rs:721,780) so any text change needs marker reconciliation. An after-the-fact HOW memory (0af9a3a2) already says "retry immediately, don't stop to explain" — factor 4's fix is already informally identified.

## Steps
- [x] 1. **Assess each mitigation against the source** — For each of the 4 mitigations (positive chaining instruction; per-session throughput signal; mark tool results as continuations; retry-cadence clause), evaluate: root-cause fit, risk of side effects, implementation cost grounded in the actual prompt.rs/turn.rs locations, and cache-stability impact. Use the finding that tool results are already role:Tool (not role:user) to judge whether mitigation 3 is a real structural gap or collapses into mitigation 1.
- [x] 2. **Write evaluation + recommendation as a semantic memory** — Write a semantic DECISION memory capturing the evaluation: per-mitigation verdict (recommend/fold/defer), the two cheap wins with exact proposed prompt text (a chaining bullet for CODING_SYSTEM_PREAMBLE near line 33, and a cadence clause appended to the APP_RULES error-retry bullet at lines 101-103), the cache-stability rationale for preferring stable-head edits, and the test-marker reconciliation note (prompt.rs:721,780). Note this formalizes the already-identified HOW memory 0af9a3a2.
- [x] 3. **Present evaluation and offer follow-up implementation** — Present the evaluation to the user concisely: which mitigations to do (1+4), which to fold (3 into 1), which to defer (2). Offer to implement the recommended stable-head text changes as a follow-up implementation plan (with review + test-marker updates) on approval.
