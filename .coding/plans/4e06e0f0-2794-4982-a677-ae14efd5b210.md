# Plan: Phase 6c — Docs + remaining debt (Maint M5/M6/M7; defer M1/M2)

## Goal
Address the Phase 6c maintainability/debt findings from the 2026-04-08 architecture review: (M5) group tool registrations in the factory + add a unit test asserting the expected tool name set is present; (M6) consolidate the right-panel's duplicated tab metadata into a single view registry ({ id, label, icon, component }[]) driving the store defaults + UI render; (M7) refresh PLAN.md's status/architecture section as an as-built doc reflecting shipped reality (multi-agent, skills, backlog/run-all, plan stack, safety hardening, spawn_blocking, current test counts). The optional openai.rs/memory/mod.rs splits (M1/M2) are deferred with written justification (cross-cutting hot-path refactors outside this docs/registry/test phase's scope). No drive-by refactors. cargo test + npm test + tsc must stay green.

## Context
Parent: Architecture review remediation, step 27 (Phase 6c). Findings from .coding/reviews/2026-04-08-architecture-maintainability.md:

M5 (line 187): Tool registration is a centralized manual list in src/agent/factory.rs build_registry (lines 295-355). ~15 tools registered in one flat sequence. Recommendation: "Keep the explicit list (clarity is good) but group registrations (register_agent_tools, register_workflow_tools, register_memory_tools) and add a unit test that expected names are present." Current groups visible in build_registry: file/shell/search/git (agent tools, lines 305-311), plan tools (312-315), skill tools (322-329, conditional on skills), describe_image (333, always), memory tools (338-339, conditional), spawn_agent (347-352, conditional on spawner). ToolRegistry has .get(name) + .iter() (src/tool/mod.rs:260,265) so a name-set test is straightforward. Existing tests: describe_image_tool_always_registered_against_swappable_slot (577), spawn_agent_tool_registered_only_when_spawner_wired (616) — both use factory.build_registry(&workflow, None) + registry.get("name").is_some(). make_factory/make_factory_with helpers exist.

M6 (line 197): Right-panel extensibility is multi-touch. Tab metadata is duplicated in THREE places: (1) RightPanelTab type union (frontend/src/hooks/agentState.ts:84-92, 8 ids), (2) ALL_RIGHT_PANEL_TABS array (agentState.ts:95-104, same 8 ids in order), (3) TABS array in RightPanel.tsx:14-23 (adds label+icon per id). Adding a view requires editing all three + the render switch (RightPanel.tsx:129-136). Recommendation: "Single view registry ({ id, label, icon, component }[]) driving store defaults and UI." The component field lets the render switch be derived too. The RightPanelTab type union + ALL_RIGHT_PANEL_TABS must stay (the store uses them for disabledTabs state + persistence), but the label/icon/component can be consolidated into one registry that RightPanel.tsx imports. Approach: create a RIGHT_PANEL_VIEWS registry ({ id, label, icon, component }[]) in a new frontend/src/hooks/rightPanelViews.tsx (or in agentState.ts), drive RightPanel.tsx's TABS + render from it, keep RightPanelTab type + ALL_RIGHT_PANEL_TABS derived from the registry (or kept in sync with a test). Must keep disabledTabs default (all except "plan") working.

M7 (line 208): PLAN.md drift from shipped reality. Status block (PLAN.md:21-34) still frames work as "UI swap only" with "184 lib tests" (now 520+) and "no brain changes required." Skills, backlog/run-all, plan stack, ParentAwareSpawner, conversation save/load, multi-agent per-agent workflow, safety hardening, spawn_blocking — all production features not reflected. Recommendation: "Refresh PLAN.md architecture section as an as-built doc." This is a docs-only change: update the status block + add an as-built architecture overview reflecting the current channel contract, multi-agent model, workflow/skill/backlog subsystems, and the safety/approval model. No code.

OPTIONAL M1 (openai.rs 1557 LOC) + M2 (memory/mod.rs 1278 LOC) splits: these are larger refactors. M1 recommends splitting openai.rs into request.rs/sse.rs/capabilities.rs. M2 recommends extracting store.rs/stats.rs from memory/mod.rs. Per the no-drive-by-refactors constitution guard + the plan step's "optional" wording, these should be DEFERRED with written justification unless trivial — they're cross-cutting splits that risk behavior drift in the provider/memory hot paths, and the review's own recommendation calls them "direction" not requirements. The joint synthesis (line 80) lists only "Right-panel view registry; tool registration grouping + name-set test" as the 6c deliverables — M1/M2 are NOT in the synthesis's must-do list. Defer both with explicit justification.

cargo 520+49+5, vitest 78, tsc green at start. Branch: feat/bookkeeping-tools-autorun.

## Steps
- [x] 1. M5 — Group tool registrations + name-set test: refactor build_registry in src/agent/factory.rs into grouped helper methods (register_agent_tools, register_workflow_tools, register_skill_tools, register_memory_tools, register_vision_tool, register_spawn_tool) that build_registry calls in order. Keep the exact same registration set + conditionals (skills/spawner/memory). Add a unit test expected_tool_names_registered asserting the full name set is present (file_read, file_edit, file_write, file_append, shell, search, git, create_plan, complete_step, update_plan, abandon_plan, skill_start, skill_end, abandon_skill, describe_image, memory_write, memory_recall, spawn_agent) with a fully-wired factory (skills + memory + spawner), and a second test asserting the conditional tools are absent without their wiring (no spawn_agent without spawner already exists; add: no skill tools without skills, no memory tools without memory). Run factory tests.
- [x] 2. M6 — Right-panel view registry: create a single RIGHT_PANEL_VIEWS registry ({ id: RightPanelTab, label: string, icon, component: React.FC }[]) in frontend/src/hooks/rightPanelViews.tsx. Derive ALL_RIGHT_PANEL_TABS from it (map to ids) so the store defaults stay in sync. Refactor RightPanel.tsx to import the registry and drive both the TABS render + the content switch from it (the component field replaces the {shownTab === "x" && <X/>} ladder). Keep the disabledTabs default (all except plan) + the close-button behavior working. Add a vitest asserting every RightPanelTab id has a registry entry (no missing view). Run vitest + tsc.
- [x] 3. M7 — PLAN.md as-built refresh: update the status block (test counts: 520+ lib, 49 tauri, 5 integration, 78 vitest) and replace the 'UI swap only' framing with an as-built architecture overview reflecting: multi-agent per-agent Workflow+ToolRegistry, plan stack + skills, backlog/run-all with checkpoint/rollback + strict-success gate, safety hardening (protected .coding writes, keys.toml ACL, approval contract/DenyAll), spawn_blocking for FS tools, IPC module split + golden contract fixtures, FE store split into pure reducers. No code changes — docs only. Keep it accurate + concise.
- [x] 4. Run cargo test --workspace + npm test (vitest) + npx tsc --noEmit; confirm all green; check for new warnings.
- [x] 5. Spawn read-only reviewer subagent over ALL uncommitted changes; it writes its report to .coding/reviews/. Read the report and fix every finding (correctness/bugs/security/constitution) — especially: did the factory grouping change any registration set/conditional, does the right-panel registry render identically (no missing views, disabled-tabs default intact), is PLAN.md accurate. Re-run cargo test + npm test + tsc after fixes. Commit Phase 6c to feat/bookkeeping-tools-autorun with a clear message noting the M1/M2 deferral justification; include the reviewer report file.
