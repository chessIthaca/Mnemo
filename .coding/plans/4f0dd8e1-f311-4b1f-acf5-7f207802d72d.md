# Plan: Fix skill-model regression: [models.skill.&lt;name&gt;] must beat the picker pin

## Goal
Make a configured [models.skill.<name>] override actually apply to skill turns even when a per-agent picker pin is set, while keeping the 2026-08-22 picker-pin fix intact (pin beats state/subagent overrides).

## Kind
bug_fixing

## Context
Root cause found by code inspection (2026-09-04 session): loop_impl.rs resolve_turn_provider returns the explicit_provider pin (set_explicit_provider, "Never cleared... agent's lifetime") BEFORE any resolver lookup, so [models.skill.<name>] never applies once the user has ever used the per-agent model picker (the 2026-08-22 picker fix overcorrected). The resolver itself (model_resolver.rs:186-193) already ranks skill #1, and turn.rs:183-207 already re-resolves per iteration with wf.active_skill() — both correct. Fix = reorder: configured skill override beats the pin; pin still beats forced/subagent/state (2026-08-22 bug stays fixed). Plan steps must stay in the locked reproduce→root-cause→fix→verify skeleton.

## Steps
- [x] 1. **Reproduce with failing regression test** — Write a regression test that reproduces the defect (constitution: every defect gets a regression test that fails without the fix and passes with it). Run it and confirm it FAILS.
- [x] 2. **Document root cause** — Investigate and document the root cause. memory_write a BUG: record (symptom → root cause → fix + regression test name, ≤600 chars).
- [x] 3. **Minimal fix** — Apply the minimal fix that makes the regression test pass. Do not refactor unrelated code.
- [x] 4. **Verify** — Run the regression test + the full test suite (cargo test unpiped, warning-free). Record the regression test name via update_plan (regression_test field) — finish is blocked without it.

## Bug
The merge_to_main skill turn ran on the model-picker-pinned model instead of the configured [models.skill.merge_to_main] deepseek model — skill model assignment regression (user report 2026-09-04).

## Regression test
skill_override_beats_explicit_picker_pin
