# Plan: Backlog housekeeping: mark 52/58/73 done

## Goal
Housekeeping: flip backlog items 52, 58, 73 to status "done" with cleared notes (the file is protected state; use the sanctioned PowerShell JSON round-trip + validation), then commit the bookkeeping on the current branch.

## Kind
research

## Context
Housekeeping: backlog items 52 ("reasoning effort ... dynamically determine it", note = commit sha) and 58 ("make sure to put to memory every edit", note = commit sha) are marked in_flight but their work shipped in prior sessions (per-model reasoning-effort config end-to-end; the edit-marker memory protocol). Item 73 ("when you use an image command, always show the image in the agent window too...") was just completed by plan 1c50659f (committed c2a16c5 on feat/tool-image-display) but is still marked cant_resolve with the stale Kimi-403 note from the interrupted batch. The .coding/backlog.json file is protected state for the file tools; the sanctioned precedent for an authorized external write is a PowerShell ConvertFrom-Json/ConvertTo-Json -Depth 10 round-trip with validation (plan 27304df9 deliverable B). The harness itself flips statuses via BacklogStore::set_status (src/backlog.rs:165-170) — the store API semantics: set_status(id, status, note). No source-code changes: research kind.

## Steps
- [x] 1. **Confirm current backlog state for items 52/58/73** — Read current .coding/backlog.json items 52, 58, 73 — confirm statuses: 52 in_flight, 58 in_flight, 73 cant_resolve (403 note). Confirm 73's work shipped (plan 1c50659f complete, commit c2a16c5 on feat/tool-image-display). Record the exact current status/note strings for post-edit comparison.
- [x] 2. **Flip 52/58/73 to done via PowerShell + validate** — PowerShell round-trip (precedent: plan 27304df9): `$p='.coding/backlog.json'; $b=Get-Content -Raw $p | ConvertFrom-Json; foreach($id in 52,58,73){ $i=$b.items | Where-Object id -eq $id; $i.status='done'; $i.note=$null }; $b | ConvertTo-Json -Depth 10 | Set-Content -Encoding UTF8 $p` — then RE-PARSE with ConvertFrom-Json and assert items 52/58/73 have status 'done' and note null, and that item count + all other ids/statuses are unchanged (validate before proceeding). Print the final statuses of 52/58/73.
- [x] 3. **Diff-check + commit + memory note** — git diff .coding/backlog.json — confirm ONLY the intended changes (3 status flips + note clears). Commit the bookkeeping on the current branch (feat/tool-image-display, never main) with message 'Backlog housekeeping: mark items 52, 58, 73 done (work shipped in prior plans)'. Then memory_write a short semantic note recording the housekeeping.
