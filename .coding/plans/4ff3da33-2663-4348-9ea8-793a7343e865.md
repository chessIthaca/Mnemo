# Plan: Resolve remaining whole-app review findings (steps 6-11): error enum, IPC DTO, cleanups, module split, frontend

## Goal
Resolve the remaining 6 steps (findings Q-H3, Q-M1, Q-M2/M3/M4/M5, Q-L1/L3/L4/L5, Q-H1, Q-H2, B4, M2-perf) of the 2026-08-13 whole-app review. Promote the error enum to structured variants, add a typed IPC error DTO, do six quality cleanups, split the settings.rs god-module, and do the frontend pass — then final review + commit.

## Kind
implementation

## Context
Master plan e036ad5a (on disk) tracks all 31 findings from the 2026-08-13 whole-app review (.coding/reviews/2026-08-13-full-app-review.md). Steps 1-5 shipped via plan f3125fe3 (Complete): dead deps removed (B1/B2/B3), describe_image gated + magic-byte sniff (H1), log permissions + secret redaction (H2), BPE tokenizer cached (R1), case-insensitive protected-write-target (M1-sec). Working tree clean (only stack.json bookkeeping flip).

VERIFIED ANCHORS (current source, post steps 1-5):
- src/error.rs:11-36 — 9 string-bucket (String) variants: Config/Project/Provider/Tool/Workflow/Memory/Safety/Runtime/Browser. NotFound(String) + InvalidInput(String) already exist at :56-60.
- Error::Workflow( call sites: 16 matches, 12 in src/workflow/mod.rs. Patterns: "no plan exists" (:410,:509,:577), wrong-state (:403,:451,:558,:603), "no skill is active" (:625,:638), serialize (:659). Tests at :1387,:1393,:1426,:1507 assert matches!(Error::Workflow(_)).
- src-tauri/src/ipc/settings.rs:27 — get_config returns Result<GetConfigResponse, String>; files.rs:24 — read_file returns Result<String, String>. Other Result<_,String> commands likely exist (search needed).
- src-tauri/src/ipc/mod.rs:15-28 — module list (settings, files, agent, backlog_cmds, browser, projects, questions, run_all, spawn, state, trace, approval, backlog, events).
- src/tool/mod.rs:126-128 — dead never_auto() returns false, doc says "Today no tool overrides this". :142-144 never_auto_for (the real guard). :191-198 from_state maps Skill=>ToolFilter::Planning (defensive default). :385 dispatch clones call.arguments.
- src-tauri/src/ipc/settings.rs:114-124 — kind parsing accepts to_ascii_lowercase() of both "openai"/"OpenAI". frontend types.ts:131-134 kindFromConfig duplicates normalization.
- frontend/src/components/settings/types.ts:105-110 makeUid (Math.random fallback), :224-225 double blank line.
- frontend/vite.config.ts:24 chunkSizeWarningLimit:1500 suppression.
- src/workflow/mod.rs:699 unwrap_or_default() on bare-array sidecar parse. :762-778 latest_plan_id uses entry.ok()? chains.
- streamingText concat: agentEventReducer.ts:130 (agent.streamingText + event.text) + useAgentStore.ts:855 (same). useAgentStore.ts is ~958 lines.

PARALLELIZATION: Step 10 (frontend) is disjoint from all Rust work (steps 6-9) — different files, different toolchain. Can spawn as a parallel background agent early. Final review (step 11) covers ALL uncommitted changes including the agent's.

SUB-PLANS needed during execution: step 6 (error enum — many call sites), step 7 (IPC DTO — cross-cutting), step 9 (settings.rs split — pure move but large), step 10 (frontend — disjoint toolchain).

## Steps
- [x] 1. **Step 6 — Promote error-enum string buckets to structured variants (Q-H3)** [SUB-PLAN] — In `src/error.rs:11-36` add dedicated variants for the most-branched-on Workflow failures: `WorkflowNoPlan` (no plan exists) and `WorkflowWrongState { current: String, expected: String }` (update outside Executing / wrong state). Keep `Workflow(String)` as the free-form fallback. `search` for `Error::Workflow(` (16 matches, 12 in src/workflow/mod.rs) and update the no-plan constructors (:410,:509,:577 — "no plan exists") and wrong-state constructors (:403,:451,:558,:603) to the structured variants; leave "no skill is active" (:625,:638) and serialize (:659) as `Workflow(String)`. Update the 4 test assertions at workflow/mod.rs:1387,1393,1426,1507 that match `Error::Workflow(_)` — split them to assert the specific structured variant where the test exercises no-plan/wrong-state. Ensure `NotFound`/`InvalidInput` are used consistently. Run `cargo test`. (Unlocks step 7.)
- [x] 2. **Step 7 — Typed IPC error DTO (Q-M1)** [SUB-PLAN] — Add `struct IpcError { kind: String, message: String }` (serde Serialize, `#[serde(into)]` or a `From<myharness::Error>` impl mapping kind=variant name via `Error`'s Debug/variant name, message=Display). Change IPC commands from `Result<T, String>` to `Result<T, IpcError>`: `src-tauri/src/ipc/settings.rs:27` (get_config) and `src-tauri/src/ipc/files.rs:24` (read_file), plus any other `Result<_, String>` commands found via `search "Result<.*String>"` in src-tauri/src/ipc/. Verify the frontend's error handling still works: Tauri serializes the DTO to `{kind, message}`; if frontend call sites read the error as a bare string they need `.message` — check `useAgentStore.ts` / IPC invoke catch blocks via `search ".catch"` or `search "invoke("`. Run `cargo test`.
- [x] 3. **Step 8 — Quality cleanups (Q-M2, Q-M3, Q-M4, Q-M5, Q-L1, Q-L3)** — Six fixes: (a) Q-M2 `src/tool/mod.rs:126-128`: remove the dead `never_auto()` method + its doc block (re-add when a real blanket-prompt tool appears); confirm no caller references it via `search "never_auto("` (only `never_auto_for` should remain). (b) Q-M3 `src/tool/mod.rs:191-198`: make `ToolFilter::from_state` return `Option<ToolFilter>` for `WorkflowState::Skill` (return `None`) instead of silently mapping to `Planning`; update all callers (search `from_state`) to handle the `None` — likely the caller already knows the skill's real filter. (c) Q-M4: in `src-tauri/src/ipc/settings.rs:114-124` delete the Debug-form lenient parsing (the `to_ascii_lowercase()` accepting "OpenAI"); emit only the serde form ("openai"/"local") from get_config so the round-trip is clean. Delete `kindFromConfig` in `frontend/src/components/settings/types.ts:131-134` and its callers (search `kindFromConfig`) since get_config now always sends lowercase. (d) Q-M5 `src/workflow/mod.rs:699`: replace `serde_json::from_str(&text).unwrap_or_default()` with a path that logs a warning (e.g. `tracing::warn!` or `eprintln!`) when the sidecar exists but fails both the StackSidecar and bare-array parses, then defaults to empty. (e) Q-L1 `src/tool/mod.rs:385`: add a one-line comment noting the per-call clone is intentional (cheap, avoids borrow issues) — no behavior change. (f) Q-L3 `src/workflow/mod.rs:762-778`: replace the `entry.ok()?` chains in `latest_plan_id` with `filter_map` so one bad dir entry doesn't abort the whole scan. Run `cargo test`.
- [x] 4. **Step 9 — Split settings.rs god-module (Q-H1)** [SUB-PLAN] — Split `src-tauri/src/ipc/settings.rs` (1750 lines) along its header seams into new files under `src-tauri/src/ipc/`: `endpoints.rs` (endpoint DTOs like EndpointEditable/EndpointWire + `save_endpoints`), `keys.rs` (`get_api_keys` + key handling), `pricing.rs` (pricing DTOs + commands), `rewire.rs` (runtime vision/embedder rewire after config change); keep `settings.rs` for `get_config`/`get_settings`/`save_settings` + shared wire structs (GetConfigResponse, GetConfigGeneral, PricingWire). Register the new modules in `src-tauri/src/ipc/mod.rs:15-28`. PURE FILE MOVE — no behavior change, no logic edits. Run `cargo test`; verify the golden fixture test still passes (it pins the get_config payload shape).
- [x] 5. **Step 10 — Frontend pass (Q-H2, Q-L4, Q-L5, B4, M2-perf)** [SUB-PLAN, spawn as parallel background agent — disjoint from Rust] — (a) Q-H2 `frontend/src/hooks/useAgentStore.ts` (~958 lines): extract the steer/backlog action block into its own hook module `frontend/src/hooks/useSteerBacklog.ts`; re-export from useAgentStore so callers are unchanged. (b) Q-L5 `frontend/src/components/settings/types.ts:224-225`: remove the double blank line (one blank line only). (c) Q-L4 `types.ts:104`: add a comment to `makeUid` that it is for React keys only, never security-adjacent. (d) B4 `frontend/vite.config.ts`: remove `chunkSizeWarningLimit: 1500` (:24) and instead configure `build.rollupOptions.output.manualChunks` to split `react-markdown` + `rehype-highlight` into a separate chunk (lazy-loaded via dynamic import where Message.tsx renders markdown). (e) M2-perf: in the streamingText concat path (`agentEventReducer.ts:130` and `useAgentStore.ts:855` — both `agent.streamingText + event.text`), this is already one concat per rAF flush (mitigated); per the review it's a micro-opt — join-once is N/A since it's already single-concat-per-flush. Confirm via reading that the reducer batches (text_delta accumulates in a buffer, flushed once per frame) and add a one-line comment documenting the O(n) per flush / O(n²) total tradeoff is accepted. Run `npm run build` + `npm run lint` in `frontend/`.
- [x] 6. **Step 11 — Investigate + fix stale recalled memories (sub-plan)** — The `memory_recall` tool keeps surfacing stale working-tier snapshots from *before* `.claude/` was deleted (e.g. `tool: git` / `tool: search` entries whose bodies reference `.claude/plans/...` paths that no longer exist). There is no `memory_delete` tool, so investigate the available remediation paths: (1) check whether a `memory_consolidate` tool exists (referenced in the constitution's bookkeeping-tools clause) and whether it can prune/merge stale working-tier entries; (2) if no deletion/consolidation exists, determine whether writing stronger semantic-tier deprecation notes (already started — the `.claude/ deleted` note) is the only lever, and document that; (3) inspect the memory store implementation (`src/memory/mod.rs`) to understand how recall ranking works and why stale working-tier entries outrank relevant semantic ones — is it a strength/recency issue? Produce a short written finding (in the sub-plan context or a memory entry) of the root cause + the remediation taken. This is investigation that may touch the memory store's recall logic — if it changes source code, the sub-plan must be `implementation` kind so it gets reviewed.
- [x] 7. **Step 12 — Final: verify accept-only findings + cargo test + review + commit** — Verify the accept/note-only findings need no code change: Q-L2 (schemas sort at tool/mod.rs must stay — confirm `out.sort_by` present), L3 (browse_markdown_file native picker — by design), R3 (navigate lock — acceptable), R4 (screenshot base64 — fine), Q-H2 (useAgentStore 940 lines — accepted as-is per review's own 'if it grows' guidance; file shrank from 958→940 since the review). Run `cargo test` (must pass, warning-free under `#![deny(warnings)]` — no `#[allow(...)]` added). Run `npm run build` in `frontend/` (tsc + vite). Spawn a reviewer subagent (role:reviewer) to review ALL uncommitted changes (git diff HEAD) against the 31 review findings + constitution compliance; give it the changed file list and the review doc path `.coding/reviews/2026-08-13-full-app-review.md`. Read its report from `.coding/reviews/`, fix every finding, re-run `cargo test`. Commit to the feature branch with a clear message; include the review report file.
