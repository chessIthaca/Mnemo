# Plan: Drop the resize-divider highlight line

## Goal
Remove the visible vertical highlight/grip line on the right-panel resize divider while keeping the drag-to-resize behavior working via the (now fully invisible) w-1 hit area.

## Kind
implementation

## Context
User flagged the thin vertical highlight line between the left tools sidebar and the right (Graph) panel as unwanted. It comes from the ResizeHandle component in frontend/src/App.tsx: the full-height hover wash `hover:bg-cyan-500/20` and the centered grip pill `<div className="pointer-events-none h-8 w-0.5 rounded-full bg-border" />` at line 673. The handle's outer div is `w-1` with an invisible wider hit area (`absolute inset-y-0 -left-1 -right-1`), so removing all visual styling still leaves a grabbable resize zone — the space alone is enough, per the user. No other component draws a border in that gap (RightPanel.tsx has no left border; Sidebar.tsx has border-r but that's the far-left icon rail).

## Steps
- [x] 1. **Remove visible styling from ResizeHandle** — In frontend/src/App.tsx, ResizeHandle's returned JSX (~line 659-675): (1) remove `hover:bg-cyan-500/20` and the now-pointless `transition-colors` from the outer div's className (keep `relative flex w-1 shrink-0 cursor-col-resize items-center justify-center` — cursor feedback stays); (2) delete the grip pill div (line 673, `pointer-events-none h-8 w-0.5 rounded-full bg-border`) and its comment block (lines 670-672); (3) remove `group` from the outer className since nothing inside uses group-hover anymore; (4) keep the invisible hit-area div (line 669) unchanged. Also update the component doc comment (lines 580-588) to note the handle is intentionally invisible (space + cursor only).
- [x] 2. **Verify frontend checks pass** — Run the frontend test suite (npm test / vitest run in frontend/) and ensure a clean typecheck/build for the touched file. The project constitution's cargo test rule covers Rust; this change is TSX-only, so run the frontend checks. Confirm no test asserts on the removed grip pill or hover class (grep for 'grip' / 'bg-cyan-500/20' in frontend/src if a snapshot fails).
