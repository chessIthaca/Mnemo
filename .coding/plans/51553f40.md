# Plan: Run-All stalls after finish→Complete — next item never dispatches (e33a07fd + 5bb1e4cd guard)

## Goal
Fix the run-all stall: a turn end with the run active whose workflow is already Complete must resolve the item and dispatch the next pending item — no manual restart. Root cause: the gate requires loop_evidence=true, but a resolution that slides to a notification-resumed turn sees (Complete, false) and the non-closure arm returns without dispatching; Complete is a resting state so nothing ever re-checks. Fix: (1) a landed arm — at (Complete, false), an InFlight item whose plan verifiably landed (orphan_work_landed: plan file all-checked + commit after the pre-item checkpoint — the drain's done-orphan predicate) resolves Done + dispatches (the live mirror of the drain's guard); (2) in the same pass, the 5bb1e4cd guard — every Done transition (run-all gate arm + single-dispatch Done row) requires the item to be currently InFlight AND own the completing plan (item.plan_id == the workflow's top_plan_id, kept after finish); a re-queued Pending item or a mismatched/absent linkage must not flip (the note is preserved, no commit). Acceptance: ≥2 pending items + Run-All active → finishing item 1 dispatches item 2 automatically; regression tests pin the dispatch arm for a Complete-state turn end and the guard matrix (pending+X vs Y stays pending; in_flight+X vs X → done); cargo test green (root + src-tauri); the drain's orphan recovery unregressed (existing drain tests stay green).

## Kind
bug_fixing

## Context
Backlog e33a07fd (run-all batch, unattended) + related pending 5bb1e4cd (same arm, fixed in one pass per the task). ROOT CAUSE (traced): the run-all arm's gate in on_main_turn_resolved (src-tauri/src/ipc/run_all.rs:2570) requires (workflow==Complete, loop_evidence==true) to stamp Done + dispatch; the non-closure arm (:2611-2639) annotates + RETURNS — no dispatch. Complete is a resting state (no transition ever fires again; no auto-continue — Complete ∉ workflow_expects_progress), so a resolution that slides to (Complete, false) stalls the run forever. The slide mechanism: the finish turn's Finished arrives while a descendant still runs → on_finished defers (pending_finished) → the descendant's completion notification starts a new main-agent turn → on_started CLEARS the deferred entry (the 2026-08-26 deferral class) → the notification turn ends with loop_evidence=false → gate fails → stall. KEY FACTS verified: top_plan_id() = the ROOT plan, and finish() keeps the stack (src/workflow/mod.rs:797-804,319-321) so main_agent_top_plan_id still reports the completed plan after finish; orphan_work_landed (run_all.rs:3029) = plan file all-checked + commit after the pre-item checkpoint (the drain's done-orphan predicate); stamp_backlog_in_flight stamps item.plan_id at plan creation; the done-counter bump block (run_all.rs:2686-2700) runs for non-returning arms; the single-dispatch arm (2749-2844) computes (status, note) rows with the pointer-restore machinery for None rows. The dispatch pre-transitions Complete→Planning (enter_planning_if_complete), so a pre-create_plan turn end lands at (Planning, false) — covered by the a6a7727a auto-continue fix, NOT this defect. DECISIONS: (a) landed arm uses orphan_work_landed (reuses the drain's proven predicate; conservative — a missing checkpoint sha falls back to today's wait, drain still recovers); (b) the landed arm needs no plan-linkage guard (the predicate is self-evidentiary — the item's OWN plan verifiably completed) but does require InFlight; (c) the gate arm + single-dispatch Done row get the 5bb1e4cd guard (InFlight + plan_id linkage; blocked → no commit/transition, note preserved); (d) blocked gate case: clear the stale current_item + dispatch next (the re-queued item is Pending again — eligible), no done-counter bump; (e) single-dispatch blocked case → the existing (None, note) row (annotate + pointer restore + auto-feed suppressed); (f) the single-dispatch slid case (no checkpoint → landed predicate unusable) is OUT OF SCOPE — noted as a known adjacent gap; (g) async match guards are illegal in Rust → closed_earlier/may_flip computed before the match.

## Steps
- [x] 1. **Reproduce with failing regression test** — Write a regression test that reproduces the defect (constitution: every defect gets a regression test that fails without the fix and passes with it). Run it and confirm it FAILS.
- [x] 2. **Document root cause** — Investigate and document the root cause. memory_write a BUG: record (symptom → root cause → fix + regression test name, ≤600 chars).
- [x] 3. **Minimal fix** — Apply the minimal fix that makes the regression test pass. Do not refactor unrelated code.
- [x] 4. **Verify** — Run the regression test + the full test suite (cargo test unpiped, warning-free). Record the regression test name via update_plan (regression_test field) — finish is blocked without it.

## Bug
Run-All stalls after finish→Complete: with ≥2 pending items and Run-All active, completing the first item's plan (finish → Complete) never dispatches the second item — the session sits idle with the run active, requiring a manual restart (live 2027-01-08, plan 4354296c, commit 08a9295; the [done] stamp came from the manual restart's drain, not the resolution).

## Regression test
complete_state_turn_end_with_landed_plan_dispatches_next
