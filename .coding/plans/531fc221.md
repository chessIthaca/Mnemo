# Plan: Extract main.rs startup decision logic into startup.rs + tests (quality review LOW 4)

## Goal
Extract the pure startup decision logic from src-tauri/src/main.rs into a new src-tauri/src/startup.rs module with cross-platform unit tests — console-mode detection, the --project flag parse, the endpoint/model fallback ordering, the re-embed-at-startup gate, and the window-title decision — keeping main()/build_brain_inner as wiring only.

## Kind
implementation

## Context
Quality review LOW 4 (.coding/reviews/2027-01-06-full-review-quality-coverage.md): src-tauri/src/main.rs is a monolith — main() :124-954 (~830 lines), build_brain_inner :954-1746 (~792 lines); the only tests in the 91.7KB file are the two path-adoption helpers (:1746-1763, in a #[cfg(all(test, unix))] module — unix-only). The startup wiring (provider construction, memory store, codegraph, safety rules, fallback ordering) is untested and unextracted. Fix: extract pure decision logic into helpers with unit tests; keep main() as wiring only. SURVEY FACTS (full file read): (1) Console-mode detection main :139 — args.any(|a| a == "-console" || a == "--console"). (2) --project flag parse build_brain_inner :1000-1005 — args.windows(2).find(|w| w[0] == "--project").and_then(|w| w.get(1)).map(PathBuf::from); only the space-separated form is honored. (3) Endpoint/model fallback ordering :1050-1061 — endpoint = default_provider-named config.endpoint(name) → config.default_endpoint(); model = default_model → endpoint's first model_id → "gpt-4o" terminal. (4) Re-embed gate :1363-1385 — configured_bundled.is_some() && !case-insensitive-sentinel-hash && pending_download.is_none() && pending_load.is_none() && status == Ready (the comment block documents why each gate exists). (5) Window title main :292-304 — NeedsProject → "Mnemo", else "Mnemo — {folder}" with file_name fallback ".". DECISIONS: extract into a NEW src-tauri/src/startup.rs module (not inline helpers) — follows the codebase's extraction pattern, starts the monolith's decomposition, and avoids the unix-only tests-module complication (the new tests are cross-platform); the five helpers are pure (no Tauri state, no I/O) so they unit-test directly. Scope is the finding's named decision logic; the remaining main() middle (IpcState wiring, event forwarders, invoke_handler, run loop) is wiring per the finding's own framing. Fixtures via Config::default() + Endpoint { ..Endpoint::test_default() } (the HIGH 2 pattern — src-tauri's dev-dep enables mnemo's test-support feature). mod registration: main.rs :32-34 has mod console; mod ipc; mod watchdog; — add mod startup; alphabetically. Both Rust suites required (constitution); src-tauri currently 196 + 4 tests, root library 2004 + 16.

## Steps
- [x] 1. **Create startup.rs + rewire main.rs** — (1) Create src-tauri/src/startup.rs: copyright header + module doc (pure startup decision logic extracted from main.rs by quality review LOW 4 — no Tauri state, no I/O, unit-testable directly). Five pub(crate) helpers, each with its doc comment moved/adapted from main.rs: (a) console_mode_requested(args: impl IntoIterator<Item = String>) -> bool — args.any(|a| a == "-console" || a == "--console") (from main :139). (b) project_flag_from_args(args: impl IntoIterator<Item = String>) -> Option<std::path::PathBuf> — the windows(2) find for "--project" + its value (from :1000-1005; doc: only the space-separated form is honored, matching --console's spelling). (c) resolve_startup_provider(config: &Config) -> (Option<&mnemo::config::Endpoint>, String) — endpoint = default_provider-named → config.default_endpoint(); model = default_model.clone() → endpoint's first model_id → "gpt-4o" (from :1050-1061; doc: the fallback ordering for the startup provider — the dummy path at the call site handles the None case). (d) should_reembed_at_startup(configured: Option<&str>, pending_download: bool, pending_load: bool, status: &mnemo::memory::embedder::EmbedderStatus) -> bool — configured.is_some() && !configured.is_some_and(|v| v.eq_ignore_ascii_case(mnemo::config::EMBEDDING_MODEL_SENTINEL_HASH)) && !pending_download && !pending_load && *status == EmbedderStatus::Ready (from :1363-1378; move the comment block explaining each gate — the case-insensitive sentinel check, background-task ownership of re-embed). (e) window_title(needs_project: bool, root: &std::path::Path) -> String — needs_project → "Mnemo".to_string(), else format!("Mnemo — {folder}") with folder = root.file_name().and_then(|n| n.to_str()).unwrap_or(".") (from main :292-304; em dash preserved). (2) Add a #[cfg(test)] mod tests (CROSS-PLATFORM — not the unix-only cfg of the path-adoption tests) with ~25 tests: console_mode_requested ("-console" true, "--console" true, absent false, other args false); project_flag_from_args ("--project /p" → Some, absent → None, trailing "--project" with no value → None, first of multiple wins, "--project=x" → None); resolve_startup_provider (named endpoint wins + default_model wins; named-but-missing endpoint falls back to default_endpoint; no default_provider → default_endpoint; no endpoints → None + "gpt-4o"; default_model unset → endpoint's first model; neither → "gpt-4o") — fixtures via Config::default() + Endpoint { name, ..Endpoint::test_default() } inserted into the endpoints map + config.general.general.default_provider/default_model set; should_reembed_at_startup (configured+Ready+no pendings → true; "hash"/"HASH" opt-out → false; pending_download → false; pending_load → false; status Failed/Checking → false; unconfigured → false); window_title (needs_project → "Mnemo"; root /home/u/myproject → "Mnemo — myproject"; root "/" → "Mnemo — ."). (3) Rewire main.rs: add `mod startup;` to the mod block (:32-34, alphabetical); replace the five inline sites with startup:: calls — main :139 console check, :292-304 title (compute needs_project = matches!(&brain_result, Ok(BrainOutcome::NeedsProject(_))) then startup::window_title(needs_project, &backlog_root)), build_brain_inner :1000-1005 flag parse, :1050-1061 let (endpoint, model) = startup::resolve_startup_provider(&config), :1363-1385 the gate (read the status lock at the call site, pass &snapshot). Behavior byte-identical — pure code motion.
- [x] 2. **Both Rust suites green** — Run src-tauri `cargo test` unpiped (expect 196 + ~25 new + 4 doc, 0 failed, exit=0 — #![deny(warnings)] means green = zero warnings) and root `cargo test` unpiped (expect 2004 + 16, 0 failed — the library is untouched but is the regression net). Fix any failures before continuing.
- [x] 3. **Closing sequence: review, fix, commit, finish** — Spawn a read-only reviewer (spawn_agent role:'reviewer', complete task args) over ALL uncommitted changes: cite the plan, the quality-review LOW 4 finding, and the extraction-scope decision (the five named decision points; the rest of main() is wiring per the finding's framing). Ask it to verify: the moves are pure code motion (each helper's logic byte-equivalent to the inline site it replaced — especially the fallback ordering and the re-embed gate's case-insensitive sentinel check), the call-site rewiring preserves behavior (the status-lock read moving before the cheap gates is side-effect-free), the tests genuinely pin the boundary cases (fallback chains, gate combinations, flag-parse edges), the tests are cross-platform (no unix-only cfg), fixtures use the test-support feature correctly, mod registration is right, multi-platform neutrality (no platform-specific code in startup.rs), and doc sync. Fix every finding (re-run tests), round-2 verification if any findings, then commit on wt/agenticcoding (include review reports + plan file) and finish.
