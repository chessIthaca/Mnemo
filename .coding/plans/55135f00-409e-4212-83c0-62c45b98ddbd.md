# Plan: Remove prompts.toml override machinery

## Goal
Delete the user-editable prompts.toml override layer (~/.mnemo/prompts.toml: PromptBlocks/PromptSection/WorkflowStateBlocks serde structs, load_str, PromptSource/PromptHolder mtime-reload, apply_tool_descriptors, default_prompts_toml*/ensure_prompts_file* seeding, factory tool_descriptor_map) so system prompts and tool descriptors are always the compiled constants; update docs (README, PLAN, module docs).

## Kind
implementation

## Context
HEAD 5d38585 on clean tree (only stray untracked .coding/tmp-user-prompts.toml). The feature shipped via plans 2eb558c6 + 79997137. Machinery spans: src/agent/prompt.rs (PromptSection, WorkflowStateBlocks, PromptBlocks+serde, load_str, PromptSource, apply_tool_descriptors, PROMPTS_FILE_HEADER, default_prompts_toml[with_descriptors], ensure_prompts_file[with_descriptors]); src/agent/loop_impl.rs (PromptHolder, prompts field, with_prompt_source); src/agent/factory.rs (prompt_source field+ctor param, .with_prompt_source chain, tool_descriptor_map + its test); src/agent/turn.rs (prompt_blocks fetch, apply_tool_descriptors call); src-tauri/src/main.rs (factory arg ~1496, seeding block ~1535-1548, comment ~940); src-tauri/src/console.rs:2242, src-tauri/src/ipc/config_io.rs:505, tests/workflow_integration.rs:407 (factory arg). Tests inside prompt.rs cover TOML parsing/reload/descriptors/seeding — remove them. ~25 test call sites in prompt.rs pass &PromptBlocks::default()/&WorkflowStateBlocks::default() — simplify signatures instead: build_stable_head(constitution), build_volatile_tail(caps, wf, memories), build_system_prompt(caps, wf, constitution, memories); workflow_section uses the STATE_* consts directly. Docs: README.md:114 (~/.mnemo list) and PLAN.md:~308 (Editable system prompts section). use std::path::Path in prompt.rs becomes unused — drop it. Frontend has no references.

## Steps
- [x] 1. **Strip prompts.toml machinery from src/agent/prompt.rs** — In src/agent/prompt.rs remove: PromptSection, WorkflowStateBlocks + default fns, PromptBlocks + serde defaults, impl PromptBlocks load_str, PromptSource + mtime_of + read_optional, apply_tool_descriptors, PROMPTS_FILE_HEADER, default_prompts_toml, default_prompts_toml_with_descriptors, ensure_prompts_file, ensure_prompts_file_with_descriptors. Change signatures: build_stable_head(constitution: &Constitution) -> String (push the five consts directly); build_volatile_tail(caps, workflow, memories) (drop states param; workflow_section uses the STATE_* consts via push_state_text); build_system_prompt(caps, workflow, constitution, memories) (drop blocks param). Rewrite the module doc comment and per-section docs to remove prompts.toml override language. Drop `use std::path::Path;` (now unused). Keep push_section, push_state_text, short_step_text, format_recall_context, format_primer, CONTEXT_FOOTER unchanged.
- [x] 2. **Update prompt.rs tests to the simplified API** — In the tests module of src/agent/prompt.rs: DELETE the prompts.toml tests (load_prompts_partial_file_falls_back_to_consts, load_prompts_empty_section_stays_empty, load_prompts_malformed_returns_defaults, prompt_source_missing_file_uses_defaults, write_until_mtime_advances helper, prompt_source_reload_picks_up_edits_and_deletion, apply_tool_descriptors_overrides_matching_names_only, apply_tool_descriptors_empty_map_is_noop, workflow_states_override_and_empty_semantics, load_prompts_parses_new_v2_sections, prompt_source_malformed_edit_falls_back_to_defaults, default_prompts_toml_round_trips_to_const_defaults, seeded_descriptors_round_trip_and_apply, ensure_prompts_file_with_descriptors_seeds_and_never_clobbers, build_stable_head_uses_custom_blocks, ensure_prompts_file_seeds_once_and_never_clobbers). UPDATE remaining call sites: build_stable_head(&PromptBlocks::default(), &constitution) -> build_stable_head(&constitution); build_volatile_tail(&caps, &wf, &WorkflowStateBlocks::default(), X) -> build_volatile_tail(&caps, &wf, X); build_system_prompt(&caps, &wf, &PromptBlocks::default(), &constitution, X) -> build_system_prompt(&caps, &wf, &constitution, X). Verify no test references PromptBlocks/PromptSection/PromptSource/workflow_states.
- [x] 3. **Strip src/agent/loop_impl.rs PromptHolder plumbing** — In src/agent/loop_impl.rs: delete the `prompts` field + its doc (lines ~95-97), the PromptHolder enum + impl (lines ~239-269), and the `with_prompt_source` builder (lines ~506-513). Remove both `prompts: PromptHolder::Default(...)` initializers (in new and with_constitution_source). Keep ConstitutionHolder untouched.
- [x] 4. **Strip src/agent/factory.rs prompt_source plumbing** — In src/agent/factory.rs: remove the prompt_source field + doc (lines ~108-110), the ctor param and init, the .with_prompt_source(self.prompt_source.clone()) chain + comment (lines ~604-606), the tool_descriptor_map fn + doc (lines ~680-696), and the test tool_descriptor_map_contains_core_tools (lines ~1123-1141). In make_factory_with (line ~1111) drop the PromptSource::new(...) argument.
- [x] 5. **Strip src/agent/turn.rs prompt-blocks usage** — In src/agent/turn.rs: remove `let prompt_blocks = self.prompts.prompt_blocks();` (line ~512), pass only &constitution to build_stable_head, drop the &prompt_blocks.workflow_states arg to build_volatile_tail, and delete the apply_tool_descriptors call + comment (lines ~652-657). Keep the schemas/plan-mutation filtering.
- [x] 6. **Strip src-tauri + tests call sites** — In src-tauri/src/main.rs: remove the first-run installer comment (lines ~940-944), the PromptSource::new(config_dir.join("prompts.toml")) factory arg (lines ~1492-1496), and the seeding block (lines ~1535-1548). In src-tauri/src/console.rs:2242, src-tauri/src/ipc/config_io.rs:505, and tests/workflow_integration.rs:407 remove the PromptSource::new(...) argument from AgentLoopFactory::new.
- [x] 7. **Docs sync (README, PLAN) + full test run** — README.md line ~114: remove `prompts.toml` (system-prompt blocks...) from the ~/.mnemo/ config list. PLAN.md ~308-312: delete the 'Editable system prompts (~/.mnemo/prompts.toml)' section. Then run `cargo test` unpiped at C:\AgenticCoder\AgenticCoder (read test-result lines + $LASTEXITCODE; deny(warnings) => zero warnings), then `cargo test` in src-tauri (unpiped, $LASTEXITCODE). Fix failures until green. Verify no remaining references: search for prompts.toml / PromptBlocks / PromptSource / PromptHolder / with_prompt_source / apply_tool_descriptors / tool_descriptor_map across *.rs + README.md + PLAN.md.
- [x] 8. **Review, fix, commit, finish** — Closing sequence: spawn_agent role:"reviewer" over ALL uncommitted changes (list changed files + plan goal; ask for correctness, bugs, security, docs-sync, macOS/Windows neutrality — no OS-specific code added), END the turn and resume on notification. Fix EVERY finding, re-run cargo test (root + src-tauri), commit to the working branch wt/remove-prompts-toml including the review report (never main), then finish(report path).
