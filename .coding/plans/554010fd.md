# Plan: Backlog batch: expandable memory-search matches, provider-correct model display, selectable transcript

## Goal
Ship the three stacked backlog asks: expandable memory-search matched-hits display, provider-correct model display when model ids collide across endpoints, and selectable/copyable transcript text — with tests, review, and one commit on wt/agenticcoder.

## Kind
implementation

## Context
Batch of 3 stacked backlog items (dispatched during a provider 429 outage that killed earlier unattended turns; user also asked "what is going on?" — answered in the final summary, not code). T1 feature: the "memory search: 12 matched …" output has no way to expand and see the 12 hits. NOTE: expandable memory_search chat cards already shipped in c40d6b7 — investigation must identify the surface the user means (likely a plain-text tool-result rendering or the Memory tab / recall summary line that never got the expansion). T2 (defect): the model display shows the wrong provider when the same model id appears under two endpoints — apparently resolved by first-match over the endpoints list instead of the actually-serving endpoint (trace rows and status bar show "model · provider"). T3 (feature): transcript text in the agent window must be selectable/copyable (likely a user-select: none / select-none class in the chat styles). Repo state: wt/agenticcoder branch, plan 25431c3 committed and clean except one untracked knowledge file (.coding/knowledge/how/2026-08-29-search-usage-tally-analysis-2026-09-15-search-op.md from the crashed previous session) which must be swept into this round's commit (LOW-3-style straggler risk). Constraints: #![deny(warnings)], doc comments on public fns, unpiped npm test + npm run build (frontend changed) + cargo test, multi-platform neutral, never commit to main, tools-array char budgets (only Rust schema strings if any — plan expects NO backend tool changes except possibly none), review required before finish. Kind decision: implementation (mixed batch of two features + one display-accuracy defect; the defect is a wrong label, not a crash/regression — bug_fixing's locked reproduce skeleton fits a single defect, not a 3-item batch; T2 still gets a repro-asserting test).

## Steps
- [x] 1. **Investigate all three surfaces** — (T1) search frontend for the 'matched' hit rendering and the existing expandable memory_search card shipped in c40d6b7 (Message.tsx / toolCardPaths) to find which surface shows '12 matched' WITHOUT expansion; (T2) locate the model→provider display mapping (frontend trace rows / status bar; check whether Rust returns provider with the record or the frontend resolves it by scanning endpoints); (T3) grep user-select/select-none across frontend chat components + globals.css.
- [x] 2. **Fix provider-ambiguous model display (T2)** — the display must name the endpoint that actually resolved/served the model, not the first endpoint whose model list contains the id; prefer passing the resolved provider through from the backend/trace record; otherwise disambiguate deterministically (resolved-endpoint first, tooltip naming the collision) + regression test.
- [x] 3. **Fix memory-search matched-hits expansion (T1)** — wire the same expand/collapse affordance the chat cards use onto whatever surface shows 'N matched' without it; frontend test in the matching *.test.ts(x).
- [x] 4. **Enable transcript text selection (T3)** — allow user-select on messages/tool output (keep interactive chrome excluded), document any deliberate exclusion.
- [x] 5. **Tests + docs + matrix + closing sequence** — npm test + npm run build (TS files changed), cargo test (unpiped, read exit codes), README/UI-behavior doc touch if warranted, sweep in the untracked .coding/knowledge/how/2026-08-29-search-usage-tally-analysis file from the crashed session, spawn reviewer over the changes, fix all findings, commit to wt/agenticcoder, finish.
