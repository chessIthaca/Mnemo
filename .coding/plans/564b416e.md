# Plan: Full architecture, quality & performance review of Mnemo

## Goal
Produce a comprehensive three-axis review of the Mnemo codebase at HEAD: (1) architecture & design, (2) code quality & maintainability, (3) performance & resource efficiency. Verify prior review findings were resolved, assess all changes since the last review (2026-09-15), and deliver prioritized findings. No source-code changes — deliverable is review reports + a synthesized summary.

## Kind
research

## Context
Mnemo is a Rust + Tauri desktop agentic coding harness: an enforced plan-first state machine, four-tier persistent memory (SQLite+FTS5+ONNX embeddings), a tree-sitter code knowledge graph, multi-agent runtime, OpenAI/Anthropic/local providers, MCP support, and a React/TS frontend. Workspace = `mnemo` lib (src/, the brain) + `src-tauri` (app shell + IPC) + `frontend/` (React). Prior reviews: 2026-04-08 (architecture), 2026-08-13 (perf/build/memory — found dead deps syntect+markdown, tiktoken re-init, memory blocking on async runtime, browser temp sweep), 2026-09-15 (performance — 8 findings, mostly strengths + 3 low). Cargo.toml already dropped syntect+markdown. Many features shipped since (steering enforcement C1-C5, backlog images, 429 cross-provider fallback, delta batching, symbol-lookup redirect gate). A fresh full review is warranted.

## Steps
- [x] 1. **Verify prior review findings resolved** — Targeted reads to confirm the 2026-08-13 + 2026-09-15 findings were addressed, so the new review doesn't repeat them:
- B1/B2 (dead deps syntect+markdown): already confirmed gone from Cargo.toml.
- R1 (tiktoken BPE re-init per count_tokens): read src/agent/context.rs around try_tiktoken_count / count_tokens — is the CoreBPE cached in a OnceLock/Lazy now?
- R2 (memory rusqlite calls on async runtime without spawn_blocking): read src/memory/mod.rs recall/write/batch_access/project_stats — are blocking SQLite calls wrapped in spawn_blocking, or still documented as a small-DB trade-off?
- M1 (browser sweep_stale_profiles blocking read_dir under lock): read src/browser/mod.rs ensure_browser/sweep — moved off the locked async path?
- F1 (is_project_scoped blocking canonicalize on async runtime): src/agent/approval.rs — still sync by design?
- F8 (complete_with_retry fixed backoff, no jitter): src/agent/dispatch.rs — jitter added?
Record each as RESOLVED / PARTIAL / STILL-OPEN with file:line evidence.
- [x] 2. **Map current architecture** — Use graph tools + targeted reads to chart the as-built architecture so the synthesis is grounded, not hand-wavy:
- graph_search + graph_context for the core abstractions: AgentLoop, AgentManager, Workflow, ToolRegistry, ContextManager, MemoryStore, CodeGraph, Sandbox, the IPC event forwarder (DeltaBatcher), AgentLoopFactory.
- Read entry points: src/agent/loop_impl.rs (the turn loop), src/runtime/mod.rs (AgentManager), src/workflow/mod.rs (state machine + allowed_tools), src-tauri/src/ipc/events.rs (delta batching), src/tool/ mod.rs (tool registry/dispatch).
- Identify: module boundaries & layering (brain vs IPC vs frontend), coupling hotspots, the agent↔UI channel contract, multi-agent isolation (memory WAL split, approval keying, plan-ownership), error-recovery strategy, state persistence (plans on disk, memory SQLite).
Produce a concise architecture map (modules, data flow, key seams) to anchor the synthesis.
- [x] 3. **Spawn 3 parallel deep-dive reviewers** — Spawn three role:'reviewer' (read-only) sub-agents in parallel, each with a self-contained mandate (they do NOT see this conversation). Each writes a report to .coding/reviews/ via write_review_report with a ## Verdict line. Point each at the prior reviews to verify-not-repeat.

(a) ARCHITECTURE & DESIGN reviewer — assess module boundaries, separation of concerns, coupling/cohesion, layering (brain/IPC/frontend), the plan-first state machine, multi-agent runtime isolation, abstraction quality, error-handling strategy, extensibility (MCP, skills, providers), config scoping. Prior review: .coding/reviews/2026-04-08-architecture-performance.md.

(b) CODE QUALITY & MAINTAINABILITY reviewer — assess code clarity, doc comments (public fns must have docs per project rules), test coverage & quality, consistency, dead code, error handling robustness, the #![deny(warnings)] discipline, naming, module sizes, duplication, unsafe usage. Run `cargo test` mentally from source; note test gaps.

(c) PERFORMANCE & RESOURCE EFFICIENCY reviewer — assess hot paths (agent turn loop, SSE streaming, memory recall, codegraph indexing, IPC delta batching), blocking I/O on the async runtime, allocations, caching, concurrency (locks, WAL), memory bounds (ring buffers, unbounded growth), build times (deps, features). Prior reviews: .coding/reviews/2026-08-13-app-review-perf-build-memory.md + .coding/reviews/2026-09-15-performance-review.md — VERIFY those findings are fixed and assess NEW code since.

Give each: project description (Mnemo = Rust+Tauri agentic harness, workspace src/ + src-tauri/ + frontend/), their axis, prior-review pointers, instruction to ground every finding in file:line, severity-tag each (CRITICAL/HIGH/MEDIUM/LOW), and write the report. End turn after spawning; resume on each ChildFinished.
- [x] 4. **Synthesize unified review** — Once all three reviewer reports land in .coding/reviews/, read all three. Merge their findings with (a) the prior-findings verification from step 1 and (b) the architecture map from step 2. Produce a single unified review:
- Overall verdict per axis (architecture / quality / performance).
- Consolidated, de-duplicated, severity-ordered findings (CRITICAL > HIGH > MEDIUM > LOW), each with file:line evidence + concrete fix direction.
- Explicit 'prior findings status' table (resolved/partial/open).
- Top strengths.
- Prioritized remediation roadmap.
Present the synthesized review in the chat response. Note the three durable reports in .coding/reviews/ for detail. Write a REVIEW: memory record pointing at the reports.
