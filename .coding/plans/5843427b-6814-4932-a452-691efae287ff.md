# Plan: Phase 2c — verify diff size guard (Perf H4)

## Goal
Verify the existing n*m threshold guard in client computeDiff (LCS_CELL_BUDGET + fallback to truncated remove/add) is present, correct, tested, and used as fallback only (Rust UnifiedDiffView preferred). Run all required tests. No new code unless a bug is found. Then mark parent Phase 2c step complete. This fulfills Perf H4 without drive-by changes.

## Context
The LCS quadratic guard (LCS_CELL_BUDGET=2e6, early return to capped side-by-side on n*m exceed) + test already exist in DiffView.tsx per prior landing. This step exists to formally verify it against the review finding, ensure it is not bypassed, run the verification tests per constitution, and advance the remediation plan. Prefer Rust preview path (ApprovalPreview) for real edits; client path is fallback only. No changes to FE streaming, store, or backend.

## Steps
- [x] 1. Read DiffView.tsx implementation, constants, guard logic and fallback path.
- [x] 2. Read call sites in ApprovalPrompt.tsx and DiffViewer.tsx to confirm prefer-Unified + fallback usage.
- [x] 3. Read the unit test in DiffView.test.ts and confirm the budget-exceed test.
- [x] 4. Run frontend vitest for the diff tests (npm test or equivalent, focused).
- [x] 5. Run full `cargo test` (unpiped) as required before step close.
- [x] 6. If all clean, complete the parent plan's Phase 2c step (0-based index 12).
