# Plan: Resize-handle highlight line + inset scrollbar thumb

## Goal
Two UI polish fixes from the backlog: (1) the small highlight line (grip pill + hover wash) used on the InflightBar/FileViewer resize handles is missing on the main tools-panel ResizeHandle in App.tsx — restore it there so all resize bars share the motif; (2) the webkit scrollbar thumb fills its whole track — inset it by 1px on all four sides so a thin track-colored gap shows around the thumb (also on hover).

## Kind
implementation

## Context
Current state (verified by reading the files): frontend/src/App.tsx ResizeHandle (~:663-677) is fully invisible — outer div className "relative flex w-1 shrink-0 cursor-col-resize items-center justify-center" plus an invisible wider hit-area div; plan 50af2cd3 removed its grip pill + hover wash, and the doc comment at :589-591 says "intentionally invisible". The other two handles keep the motif the user calls the "small highlight line": InflightBar.tsx:129-133 (h-1.5 bar, hover:bg-cyan-500/20, pill "h-0.5 w-8 rounded-full bg-border") and FileViewer.tsx:558-567 (same motif). Pre-removal App handle (per .coding/reviews/2026-04-20-resize-handle-invisible-review.md) had hover:bg-cyan-500/20 + pill "h-8 w-0.5 rounded-full bg-border" — restore exactly that (vertical pill, no `group` class since nothing uses group-hover). Scrollbar CSS: frontend/src/styles/globals.css:91-105 — ::-webkit-scrollbar 8px, track var(--bg-secondary), thumb `background: var(--bg-tertiary); border-radius: 4px;`, hover `background: var(--text-muted)`. Inset trick: transparent 1px border + background-clip: padding-box shows the track through as a 1px gap on all four sides. GOTCHA: the `background` shorthand resets background-clip to border-box, so BOTH thumb rules (base + :hover) must switch to the `background-color` longhand or the gap disappears (on hover). Tests: frontend vitest is pure-node (no DOM/testing-library), colocated *.test.ts must be registered in frontend/vitest.config.ts include list (precedent: textareaAutosize.test.ts). A CSS-contract test (fs-read globals.css, assert the thumb rules) is a genuine regression test: fails without the fix, passes with it. JSX class changes have no harness (precedent: earlier handle restyles shipped without JSX tests). Constitution: Windows 11 + PowerShell, run cargo test UNPIPED and read test-result lines + $LASTEXITCODE (piped exit codes lie); #![deny(warnings)]; doc comments on pub fns; never commit to main; commit includes the review report. Branch: check `git branch --show-current` + `git status --short` first — recent branches stacked unmerged; commit only this task's files to the current feature branch (create feat/resize-scrollbar-polish off the current branch if the tree is clean; if there are unrelated pending changes, leave them untouched and document).

## Steps
- [x] 1. ****Restore highlight line on main ResizeHandle (App.tsx)**** — In frontend/src/App.tsx ResizeHandle return JSX (~:663-677): (1) outer div className "relative flex w-1 shrink-0 cursor-col-resize items-center justify-center" → add "transition-colors hover:bg-cyan-500/20" (match InflightBar/FileViewer hover wash). (2) After the invisible hit-area div (<div className="absolute inset-y-0 -left-1 -right-1" />), add the grip pill: <div className="pointer-events-none h-8 w-0.5 rounded-full bg-border" /> (the pre-removal pill, vertical). (3) Update the hit-area comment (:672-674) — it currently says the handle is invisible; now the pill + hover wash are the visible affordance and the absolute div only widens the grab zone. (4) Update the component doc comment (:580-592): replace the "intentionally invisible" paragraph with one stating the handle shows the app's standard resize-affordance motif — centered grip pill + subtle cyan hover wash — matching the InflightBar and FileViewer handles, and that the w-1 column plus invisible wider hit area keep it grabbable. Do NOT re-add the `group` class (nothing uses group-hover). Keep role/aria/title attributes and all drag logic untouched.
- [x] 2. ****Inset the scrollbar thumb by 1px on all sides (globals.css)**** — In frontend/src/styles/globals.css (:99-105): rewrite the thumb rules so the thumb is visually 1px smaller than the track on all four sides. Base rule becomes: ::-webkit-scrollbar-thumb { background-color: var(--bg-tertiary); border: 1px solid transparent; border-radius: 4px; background-clip: padding-box; } — the transparent border ring lets the track show through as the gap. Hover rule becomes: ::-webkit-scrollbar-thumb:hover { background-color: var(--text-muted); } (longhand, NOT the `background` shorthand: the shorthand resets background-clip to border-box and would silently erase the gap — add a brief comment saying exactly that so nobody 'simplifies' it back). Leave ::-webkit-scrollbar (8px) and the track rule unchanged. Preserve the file's existing line-ending style.
- [x] 3. ****Add scrollbar CSS-contract regression test**** — Create frontend/src/styles/scrollbar.test.ts (vitest, node env, fs-based): read globals.css via readFileSync(new URL("./globals.css", import.meta.url), "utf8"). Extract rule bodies with a regex like /::-webkit-scrollbar-thumb(:hover)?\s*\{([^}]*)\}/g. Assert (the regression contract for 'thumb fills the whole track; wants a 1px gap on each side'): (a) the base thumb rule contains "border: 1px solid transparent" and "background-clip: padding-box"; (b) BOTH the base thumb rule and the :hover rule use background-color and do NOT use the `background` shorthand (regex /(^|;)\s*background\s*:/ on the rule body must not match) — the shorthand resets background-clip to border-box and would remove the gap, including on hover. Doc comment at top explaining the bug + why the contract matters. Register the file in frontend/vitest.config.ts include list (add "src/styles/scrollbar.test.ts", follow the existing entries). Verify the test FAILS against the pre-fix CSS (run it before applying step 2's edit if convenient, or trust the contract: it asserts properties added in step 2) and passes after.
- [x] 4. ****Verify builds/tests**** — PowerShell, unpiped, read test-result lines + $LASTEXITCODE (piped exit codes lie per the constitution): (1) cd frontend; npm test (vitest run — new scrollbar.test.ts green plus all existing suites). (2) cd frontend; npm run build (tsc + vite — proves App.tsx edit compiles). (3) Root cargo test unpiped (constitution requires it before completing steps; no Rust changed, must stay green with zero warnings). Fix anything that fails before proceeding.
- [x] 5. ****Review, fix, commit, finish**** — (1) git status --short + git branch --show-current to confirm scope: only frontend/src/App.tsx, frontend/src/styles/globals.css, frontend/src/styles/scrollbar.test.ts, frontend/vitest.config.ts (+ review report) should be committed; leave any unrelated pending changes out (document if found). (2) Spawn reviewer (spawn_agent role:"reviewer") over ALL uncommitted changes (git status / git diff HEAD) with a self-contained task: goal = restore the grip-pill/hover-wash highlight line on the App.tsx main ResizeHandle (matching InflightBar.tsx:129-133 and FileViewer.tsx:558-567) + inset the webkit scrollbar thumb 1px on all four sides via transparent border + background-clip: padding-box (globals.css) with a CSS-contract regression test (src/styles/scrollbar.test.ts registered in vitest.config.ts); changed files = the four above; focus = background-shorthand-resets-background-clip correctness (base + hover), test genuinely fails pre-fix, App.tsx edit doesn't disturb drag logic/aria, comment accuracy, no dead code. END THE TURN after spawning — resume on the background-finished notification. (3) Read the report; fix EVERY finding (never defer); re-run step 4's checks. (4) Commit the four files + the review report to the current feature branch (never main). (5) Call finish with the review report path. Final summary <=3 lines + a Decisions section documenting: interpretation of 'highlight line missing around the resize bars' as the App.tsx handle (superseding plan 50af2cd3's removal), the background-clip approach, and the CSS-contract-test choice (no DOM harness).
