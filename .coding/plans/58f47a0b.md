# Plan: Add .worktrees to IGNORED_DIRS (backlog 39b3bbfb)

## Goal
Add ".worktrees" to IGNORED_DIRS in src/tool/agent/search.rs so `.worktrees/runall-<item>/**` is neither indexed (content index + codegraph walkers + search/search_read) nor watched (no debounced main-tree pass triggered by a worktree's .coding/codegraph.db writes) — one const entry, all four root-relative consumers follow from is_ignored_component.

## Kind
bug_fixing

## Context
ROOT CAUSE (verified by exploration): `.worktrees` is absent from IGNORED_DIRS (src/tool/agent/search.rs:56-71). `is_ignored_component` (:74-76) is the single shared predicate behind exactly four ROOT-RELATIVE consumers: `should_search` (:118, call :128), the pruned walk `descend` (:317, call :332, counts pruned dirs), the codegraph walkers' `visit` (src/codegraph/walk.rs:179, call :192 — backs walk_searchable AND walk_project), and the watcher's `under_base` (src/codegraph/watcher.rs:55, call :62 — is_indexable_path:54-91 is the SOLE gate for the debounced pass). One const entry therefore repairs index coverage + watcher triggers + both walkers; no new predicate, no drift risk. `.worktrees/` is already gitignored (.gitignore:67) and app-managed (src/project/worktrees.rs:51-52 — main_root.join(".worktrees"), branch wt/runall-<item8>, agent.md branch policy). REGRESSION-TEST DESIGN (must be RED pre-fix, GREEN post-fix): extend the ignore fixtures in src/tool/agent/search.rs::skips_ignored_dirs (~:2019, needle must not match) and ::pruned_walk_matches_the_index_engine_and_never_searches_ignored_dirs (~:2613 — add .worktrees to the fixture and re-pin `skipped 3 ignored dirs` -> `skipped 4 ignored dirs`; read the test tail ~:2652 first to keep the index-engine equivalence asserts aligned), src/codegraph/walk.rs::fixture() (~:219) consumed by the two exact-list asserts ::walks_source_files_sorted_and_skips_ignored (:241-259) and ::walks_every_searchable_file_for_the_content_index (:262-291) — a `.worktrees/runall-abcd1234/src/dup.ts` fixture addition makes both lists go RED pre-fix without editing the expected lists, src/codegraph/watcher.rs::is_indexable_path_accepts_any_file_and_rejects_ignored (~:274-330 — add the worktree DB + a worktree source file, both must be rejected), src/tool/agent/search_read.rs::skips_ignored_dirs (~:769-781). ROOT-RELATIVE SAFETY PIN (stays green pre- AND post-fix — a semantics guard, not regression evidence): a project root that IS itself a worktree path (`<tmp>/.worktrees/runall-abcd1234` as ROOT) must stay fully indexed/searched, because both predicates strip the root before walking components — the predicate must never be applied to absolute paths, or the run-all item agent would lose its own search index. Docs: no README/PLAN/docs file enumerates IGNORED_DIRS (verified by search; hits were code only) — the const doc comment is the documentation. VERIFY: `cargo test --quiet` unpiped from C:/AgenticCoder/AgenticCoder, read the `test result:` lines + `"cargo-exit=$LASTEXITCODE"`; pre-change baseline is 2717 passed / 5 ignored, then 19, then 1 (test counts unchanged — the edits extend existing tests, no new test fns). Frontend untouched; the src-tauri crate is unbuildable on this Windows box (Application Control policy — standing, unrelated). Pointer: reviewer LOW-1 in .coding/reviews/2026-09-25-watcher-coding-artifacts-2a2ae0aa-review.md; coverage/trigger invariant in .coding/knowledge/bug/2027-01-11-content-index-staleness-root-causes-coding-never.md.

## Steps
- [x] 1. **Reproduce with failing regression test** — Write a regression test that reproduces the defect (constitution: every defect gets a regression test that fails without the fix and passes with it). Run it and confirm it FAILS.
- [x] 2. **Document root cause** — Investigate and document the root cause. memory_write a BUG: record (symptom → root cause → fix + regression test name, ≤600 chars).
- [x] 3. **Minimal fix** — Apply the minimal fix that makes the regression test pass. Do not refactor unrelated code.
- [x] 4. **Verify** — Run the regression test + the full test suite (cargo test unpiped, warning-free). Record the regression test name via update_plan (regression_test field) — finish is blocked without it. If the fix turned out FEATURE-SCALE (new pub types or cross-module surface): amend the plan context via update_plan append with a paragraph starting "Landed design —" (architecture, deviations from the sketch, measured numbers, invariants to preserve) and record landed_design=true BEFORE completing this step; at finish write a SPEC memory and amend the BUG record with the landed outcome (agent.md 'Documentation expectations'; the finish gate blocks landed_design without the context amendment).

## Detailed steps
- [x] **Reproduce — pin the defect in tests first (regression asserts must be RED)** — (a) src/tool/agent/search.rs::skips_ignored_dirs (~:2019): add `.worktrees/runall-abcd1234/src/dup.rs` containing `fn findme() {}`; assert the output contains no `.worktrees`. (b) same file ::pruned_walk_matches_the_index_engine_and_never_searches_ignored_dirs (~:2613): add `.worktrees/runall-abcd1234/src/x.js` with the needle, re-pin `skipped 3 ignored dirs` -> `skipped 4 ignored dirs` (read the ~:2652 tail first so the index-engine equivalence asserts stay aligned). (c) src/codegraph/walk.rs::fixture() (~:219): add `.worktrees/runall-abcd1234/src/dup.ts`; the exact-list asserts in ::walks_source_files_sorted_and_skips_ignored (:241) and ::walks_every_searchable_file_for_the_content_index (:262) stay UNCHANGED (dup.ts must be absent). (d) src/codegraph/watcher.rs::is_indexable_path_accepts_any_file_and_rejects_ignored (~:274): add `.worktrees/runall-abcd1234/.coding/codegraph.db` and `.worktrees/runall-abcd1234/src/b.tsx`; assert !is_indexable_path for both. (e) src/tool/agent/search_read.rs::skips_ignored_dirs (~:769): add a `.worktrees/runall-abcd1234/src/dup.rs` needle tree; assert no `.worktrees` in the output. Then run each targeted test (`cargo test --quiet <name>`, unpiped, check $LASTEXITCODE): (a),(b),(c)x2,(d),(e) must all FAIL — that is the reproduction.
- [x] **Root cause — confirm the single shared predicate** — Re-read src/tool/agent/search.rs:56-76 (IGNORED_DIRS + is_ignored_component), :118-138 (should_search), :317-334 (descend + pruned count), src/codegraph/walk.rs:179-200 (visit), src/codegraph/watcher.rs:54-91 (under_base + is_indexable_path + canonicalize fallback). Confirm `.worktrees` is absent and that all four consumers are root-relative users of the ONE const — no second ignore list, no absolute-path application. If anything deviates from the plan's sketch, append the finding via update_plan.
- [x] **Fix — add the one-line entry (+ const comment)** — In src/tool/agent/search.rs IGNORED_DIRS add after `.claude` (~:70): `".worktrees",  // app-managed run-all worktrees (wt/runall-<item8>) — duplicate tree copies; their .coding/ DB writes must not churn the main index/watcher`. Extend the const's doc comment (~:54-55, currently 'build output, dependencies, or VCS metadata') to also name app-managed worktrees. No other source change.
- [x] **Verify — targeted tests green, then the full suite** — Re-run the six targeted tests: all must now PASS (the pre-fix reds are the repro evidence). Then `cargo test --quiet` from C:/AgenticCoder/AgenticCoder (unpiped); read the `test result:` lines and `"cargo-exit=$LASTEXITCODE"` — 0 failed, totals equal to the pre-change baseline (2717 passed / 5 ignored; 19; 1). Frontend untouched; src-tauri is unbuildable on this Windows box (Application Control policy — standing, unrelated).
- [x] **Bookkeeping — amend the coverage/trigger knowledge record** — memory_search for the record behind .coding/knowledge/bug/2027-01-11-content-index-staleness-root-causes-coding-never.md, then memory_amend it: the coverage invariant is index = search = watcher triggers, minus IGNORED_DIRS (now incl. `.worktrees`) and the `.coding` SQLite store family; note the root-relative safety (a worktree instance whose root IS `.worktrees/runall-*` stays fully covered). The BUG: digest auto-captures at finish.

## Bug
`.worktrees/runall-<item>/**` is not in IGNORED_DIRS (src/tool/agent/search.rs:56), so the main content index has ALWAYS ingested duplicate worktree trees as content rows; since plan 2a2ae0aa narrowed the watcher's blanket `.coding` exclusion, each worktree's own `.coding/codegraph.db` writes (continuous index passes, seeded once by snapshot_db's VACUUM INTO in src-tauri/src/ipc/spawn.rs:146-177 — dest is the WORKTREE's .coding, not the main one) now also fire debounced main-tree index passes: unbounded-ish churn while a run-all is in flight, on top of the duplicate index rows. NOT a loop (a main-tree pass never writes under .worktrees). Reported as LOW-1 in .coding/reviews/2026-09-25-watcher-coding-artifacts-2a2ae0aa-review.md (found 2027-01-25).

## Regression test
is_indexable_path_accepts_any_file_and_rejects_ignored
