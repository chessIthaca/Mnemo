# Plan: Descendant tracker lags session completion (2c406d72) — verify landed fix and close out

## Goal
Backlog 2c406d72's fix is already committed (194d925: notify_parent_on_completion clears the child's running flag BEFORE sending the completion Suggestion — the tracker and the notification agree by construction; 3 A/B-verified regression tests; trait/module docs) and reviewed (round-1: 0 high, 1 low → fixed; round-2 verification: PASS), but the prior session was steered/halted before plan completion: the original plan's step 4 (verify) was never checked off, the backlog item was returned to the queue and re-dispatched, and the stale "live" BUG memory record (93aaf51b) was never superseded by the fixed record (1d39a5eb). This session closes the item: verify the fix in the working tree, run both Rust suites green, reconcile the memory store, mark the backlog item done, and commit the bookkeeping.

## Kind
bug_fixing

## Context
Root cause (traced by the prior session, plan c8c49338): in the IPC event forwarder (src-tauri/src/ipc/events.rs) the notification match ran BEFORE the running-state match for a child's Finished/final-Error event — notify_parent_on_completion sent the completion Suggestion (waking the parent immediately) and only afterwards did the forwarder emit ChildFinished and clear the running flag; under a saturated frontend that window stretched across a whole parent turn, so the woken parent hit the descendant gate (has_running_descendants) on a still-marked child. Fix (commit 194d925, already on the working branch): notify_parent_on_completion clears set_running(agent_id, false) FIRST (events.rs ~:800); the forwarder's later running-state match clears again (idempotent). Regression tests: child_finish_notification_leaves_no_running_descendant, child_final_error_notification_leaves_no_running_descendant, tracker_agrees_at_notification_receipt (src-tauri/src/ipc/events.rs mod tests; A/B-verified — all three fail with the fix line removed). Knowledge file: .coding/knowledge/bug/2027-01-07-descendant-tracker-lagged-the-finish-notificatio.md. Reviews: .coding/reviews/2027-01-07-descendant-tracker-finish-notification-order-review.md (round-1, 0 high 1 low — LOW-1 BUG-memory fixed) + round-2 verification PASS (commit 926fc00). Tree state: clean except .coding/backlog.jsonl (the run-all dispatcher's pre-item checkpoint stamp for this re-dispatch). This session produces no source-code changes — verification and closure only.

## Steps
- [x] 1. **Reproduce with failing regression test** — Write a regression test that reproduces the defect (constitution: every defect gets a regression test that fails without the fix and passes with it). Run it and confirm it FAILS.
- [x] 2. **Document root cause** — Investigate and document the root cause. memory_write a BUG: record (symptom → root cause → fix + regression test name, ≤600 chars).
- [x] 3. **Minimal fix** — Apply the minimal fix that makes the regression test pass. Do not refactor unrelated code.
- [x] 4. **Verify** — Run the regression test + the full test suite (cargo test unpiped, warning-free). Record the regression test name via update_plan (regression_test field) — finish is blocked without it.

## Bug
Descendant tracker lags session completion: complete_step stays refused ("cannot change workflow state while spawned subagents are still running") for a turn or more after every spawned child has verifiably ended (finish notification received, report on disk) — live-observed twice in the 2026-12-30 full-review session (plan 72329f2c).

## Regression test
child_finish_notification_leaves_no_running_descendant
