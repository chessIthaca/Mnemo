# Plan: MCP follow-ups: per-server trust + connection health UX

## Goal
Close the last two MCP follow-ups: (1) per-server auto-approve trust — `trusted` flag on the server def + Settings toggle; a trusted server's tools become AutoRun for APPROVAL purposes while the plan-first state gates stay intact (mcp__ names excluded from the AutoRun visibility arm); default deny untouched. (2) connection health UX — manager status tracking (connected/tool count/last error/restart count), optional per-server idle-timeout (lazy expiry), an agent-window "server restarted after a crash" note on the reconnecting call, and a live per-server status indicator on the Settings MCP page.

## Kind
implementation

## Context
Two remaining MCP follow-ups (dispatched run-all; both queued pending after the merge of plans b277ebfc + 844180a8 at 54aa0e9). Current code (all merged): McpServerDef (src/config/mcp.rs) with name/enabled/transports/env-names/auth/client_id/scopes + validation; McpManager (src/mcp/mod.rs) with lazy connections, call() retry loop (re-ensure on vanish), meta methods, from_config default token store; McpTool (src/mcp/tool.rs) kinds Tool/GetPrompt/ReadResource, safety() ALWAYS NeedsApproval, reveal registers capability-gated meta tools via ToolSlot; ipc/mcp.rs (mcp_list_servers source-tagged, mcp_save_servers split+shadowed-restore, mcp_oauth_start, mcp_test); Settings McpSection with source/auth/test/connect. DESIGN DECISIONS (documented for review): (1) TRUST = per-server `trusted: bool` on McpServerDef; McpTool.safety() returns AutoRun when the owning server is trusted (default deny preserved). Because the Planning/Complete filters admit Agent tools on `safety == AutoRun`, trusted MCP tools would leak into read-only states — FIX: exclude `mcp__`-prefixed names from that AutoRun visibility arm (trust relaxes ONLY the approval prompt, never the plan-first gate; research arm already excludes mcp__). Implementation via safety() rather than auto-injecting safety_rules: rules are user-owned/persisted in safety.toml and auto-managing them from mcp.toml would create two sources of truth (deviation from the backlog's literal "feeds safety_rules" wording — the intent "run without per-call prompts, default deny" is met). (2) HEALTH UX: manager tracks per-server status (connected_at, tool_count, last_error, restarts, last_used) in a tokio Mutex map; idle-timeout = optional per-server `idle_timeout_secs` on McpServerDef, enforced LAZILY in ensure() (a cached connection idle beyond the timeout is dropped and re-established — no background task); restart note = new `McpManager::call_with_info` returning (outcome, reconnected: bool) — the vanish-retry path sets reconnected, McpTool prefixes the result text with a "server restarted after a crash" note (the agent-window surface); `call` keeps delegating (tests unchanged). IPC `mcp_status` returns per-server {name, connected, tool_count, last_error, restarts, idle_timeout_secs}; McpSection shows a per-card status line + refresh; idle_timeout input + trusted toggle in the card/edit form. Constraints: Windows/PowerShell, cargo test UNPIPED, deny(warnings) no #[allow], doc comments on public items, multi-platform (tokio time only), regression tests for each change, four suites green before commit, reviewer subagent, never commit to main.

## Steps
- [x] 1. **Config: trusted + idle_timeout_secs fields** — Add `#[serde(default)] pub trusted: bool` + `#[serde(default, skip_serializing_if = "Option::is_none")] pub idle_timeout_secs: Option<u64>` to McpServerDef (src/config/mcp.rs, after scopes). Validation: idle_timeout_secs must be >= 1 when set (reject 0). Tests: both fields default correctly in parse + struct literals, round-trip save/load, validation rejects idle_timeout_secs = 0. Fix all McpServerDef struct literals across the repo (src/mcp/mod.rs server() helper, src/mcp/tool.rs reveal test + fake_manager_with, src/tool/agent/load_tools.rs two test literals) by adding the two fields.
- [x] 2. **Manager: status tracking + lazy idle expiry + call_with_info** — src/mcp/mod.rs: (1) new `pub struct ServerStatus { pub connected: bool, pub tool_count: usize, pub last_error: Option<String>, pub restarts: u64 }` + `status: tokio::sync::Mutex<HashMap<String, ServerStatus>>` on McpManager; ServerConn gains `connected_at` and `last_used` (std::time::Instant). (2) ensure(): on cache hit, if the def has idle_timeout_secs and last_used.elapsed() > the timeout → drop the conn (remove + reconnect, counted as a restart with a note); on fresh connect set connected_at + tool_count + connected=true. (3) New `pub async fn call_with_info(&self, name, tool, args) -> Result<(CallToolOutcome, bool)>` — the vanish-retry path returns reconnected=true and bumps restarts + records last_error on failures; `call` delegates to it and drops the flag. (4) `pub async fn statuses(&self) -> Vec<(String, ServerStatus)>`. Tests (fake client): idle expiry (idle_timeout_secs=1 + short sleep → respawn counted), status after connect (connected/tool_count), status after a failed call (last_error set), vanish-retry sets reconnected (existing concurrent test asserts call still works).
- [x] 3. **Trust: McpTool AutoRun + state-gate exclusion** — src/mcp/tool.rs: McpTool + new_meta gain `trusted: bool` (new field; constructors take it); `fn safety()` returns AutoRun when trusted else NeedsApproval (doc: approval-only — the state gates never widen, see tool/mod.rs). McpReveal::reveal_group reads the def's trusted via a new `McpManager::server_def(name) -> Option<McpServerDef>` accessor (public, clones) and passes it to new/new_meta. src/tool/mod.rs ToolFilter: the Planning AND Complete arms' Agent visibility (`safety == SafetyLevel::AutoRun || name == "spawn_agent"`) must exclude mcp__ names — `(safety == AutoRun && !name.starts_with("mcp__")) || name == "spawn_agent"` — so a trusted MCP tool is AutoRun for approval but NEVER visible in read-only states (research arm already excludes mcp__; Executing/Reviewing unchanged). Tests: McpTool trusted→AutoRun / untrusted→NeedsApproval (both kinds), reveal passes trust through (fake manager with trusted def), filter pins: trusted mcp tool visible under Executing, hidden under Planning + Complete (extend existing filter tests).
- [x] 4. **IPC mcp_status + Settings status indicator** — src-tauri/src/ipc/mcp.rs: `#[tauri::command] pub async fn mcp_status(state) -> Result<Vec<McpServerStatusWire>, IpcError>` where McpServerStatusWire { name, connected, tool_count, last_error: Option<String>, restarts, idle_timeout_secs: Option<u64> } (def + manager.statuses merged); register in src-tauri/src/main.rs invoke_handler. Frontend: lib/types.ts McpServer gains trusted?: boolean + idle_timeout_secs?: number | null + new McpServerStatus interface; tauri.ts wrapper `mcpStatus()`; settings/types.ts canonicalMcp + blankMcpServer + serializeMcpServers include the new fields; McpSection: trusted checkbox on each card, idle-timeout number input in the edit form (remote+stdio both fine), a per-card status line (connected · N tools / error: … / not connected) + restarts badge, fetched via mcpStatus on active + after Test/Save/Connect + a manual refresh button; validateMcpDraft: idle_timeout_secs >= 1 when set. Tests: frontend helper/validation updates + a status-shape test.
- [x] 5. **Docs + verification + reviewer + close** — README mcp.toml paragraph gains the trusted toggle + idle-timeout knob (one sentence each); PLAN.md MCP subsection gains two short paragraphs (trust = approval-only with the state-gate note; health UX = lazy idle expiry, status, restart note). Run all four suites green: root cargo test UNPIPED (expect ~1625+), frontend npm test, npm run build, cd src-tauri cargo build. spawn_agent role:reviewer over ALL uncommitted changes (goal + changed files; focus: trust cannot widen state visibility, idle expiry lock discipline, status map never leaks secrets, approval-only semantics, docs sync, multi-platform, no #[allow]). Fix findings, re-run suites, commit to the working branch with the report (+ round-2 PASS if needed), finish. Then backlog_status fd9b3517 + 25b5b710 → done with notes.
