# Plan: Fix: browser_navigate auto-opens the Browser tab + readable browser tool cards

## Goal
browser_navigate bootstraps the Browser tab by itself (creates the child webview + reveals the tab) when none exists, and browser/offscreen browser tool cards show readable one-line summaries (URL, selector, expression, landed URL/page title) in the chat UI.

## Kind
bug_fixing

## Context
Bug 1 root cause: the child WebView2 is created lazily by frontend BrowserView.tsx openUrl() → browser_webview_ensure; the agent-side BrowserManager (src/browser/mod.rs, lib crate, no Tauri deps) only attaches to an existing CDP target (webview_page ~:756 polls select_child_target ~:852). A tab toggled on but never navigated has no child → "no page target to attach to". BrowserWebviewState.rect (~:129 ipc/browser_webview.rs) already stores the last frontend-reported rect even when no webview exists (apply_rect ~:267). revealRightPanelTab store action exists (frontend/src/hooks/useAgentStore.ts ~:385 area). Bug 2 root cause: frontend/src/components/chat/Message.tsx argLabel (~:423) has branches for shell/git/spawn_agent/search/graph but nothing for browser tools; ToolImage already renders browser_screenshot PNGs inline from result.data.path.

## Steps
- [x] 1. **Reproduce with failing regression test** — Write a regression test that reproduces the defect (constitution: every defect gets a regression test that fails without the fix and passes with it). Run it and confirm it FAILS.
- [x] 2. **Document root cause** — Investigate and document the root cause. memory_write a BUG: record (symptom → root cause → fix + regression test name, ≤600 chars).
- [x] 3. **Minimal fix** — Apply the minimal fix that makes the regression test pass. Do not refactor unrelated code.
- [x] 4. **Verify** — Run the regression test + the full test suite (cargo test unpiped, warning-free). Record the regression test name via update_plan (regression_test field) — finish is blocked without it.

## Bug
(1) browser_navigate fails with "the child WebView2 has no page target to attach to — is the Browser tab open?" when the tab has never loaded a URL — the agent cannot open the tab itself; the user had to manually type a URL first. (2) Browser tool calls render in chat with no human-readable summary — browser_navigate shows no URL, browser_click no selector, browser_screenshot's PNG inline (already works via ToolImage) is the only visual; cards read "browser_navigate ✗" with raw JSON hidden behind expand.

## Regression test
webview_navigate_auto_ensures_missing_child
