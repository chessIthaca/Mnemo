# Plan: Fix `****bold****` plan-step headers (double-wrap + extraction + audit)

## Goal
Fix `****bold****` plan-step headers: stop create_plan/update_plan from double-wrapping already-bold headers, make bold-header extraction robust to 2+ asterisk runs (fixes display of existing plan files), fix the frontend step-body strip for legacy `****` text, and add a markdown renderer-contract test that audits what formatting works.

## Kind
implementation

## Context
Diagnosis (verified by reading code + the live plan file .coding/plans/5866e160-1143-40e1-bfe0-f27f196b8d6a.md lines 13-17): (1) src/tool/workflow/plan.rs:83-94 StepInput::to_text formats object steps as `**{header}** — {body}` — when the model passes a header already containing `**` (encouraged by schema wording "short bold header" + string-step example at :182), the file gets `****Verify builds/tests****`. (2) src/workflow/plan_file.rs:401-428 extract_bold_header_impl strip_prefix("**") + rest.find("**") finds the closer at offset 0 for `****foo****` → empty header → None. (3) frontend/src/components/views/PlanProgress.tsx:256-272 + StatusBar.tsx:658 then render the FULL raw step.text as plain text (literal asterisks, not bold). The body-strip regex at PlanProgress.tsx:266 `/^\*\*[\s\S]*?\*\*/` strips only 2 leading `*` → stray `**` left in body for legacy files. The shared renderer (MarkdownImpl: react-markdown v9 + remark-gfm + rehype-highlight, micromark/CommonMark-compliant) renders `****x****` as nested <strong> = bold, so chat/FileViewer are NOT affected — the defect is entirely in the plan-step data + display path. DECISIONS: keep plan-step headers as plain text + font-semibold (perf: plan tab re-renders on 2s poll; header is a one-liner; after fix it is guaranteed asterisk-free); do NOT rewrite existing plan files on disk (parse-time extraction fixes their display; step.text stays the verbatim source of truth); answer "what other formatting is failing?" with an executable renderer-contract test (markdownRendering.test.ts) rather than prose. Frontend tests: colocated *.test.ts, node env, pure helpers — must be registered in frontend/vitest.config.ts include list.

## Steps
- [x] 1. ****Unwrap already-bold object-step headers (plan.rs)**** — Edit src/tool/workflow/plan.rs StepInput::to_text (~:83-94): before formatting, strip ONE layer of surrounding `**` from `header` — if header.trim_start() starts with `**` AND trim_end ends with `**` AND the inner (trimmed) is non-empty, unwrap to the inner; use the unwrapped header in the format!. Update the `Map { header }` field doc comment (~:70) to say PLAIN text, bold markers are added automatically and already-bold headers are unwrapped. Update the `steps` schema description at :182 (create_plan) AND the equivalent in update_plan's schema (search plan.rs for the second occurrence of that description text) with one clause: 'the header field is PLAIN text — do not include ** markers (they are added automatically; an already-bold header is unwrapped)'. Add a regression test in plan.rs tests module: create_plan with a map step {header: "**Verify builds/tests**", body: "run tests"} → plan file step text is exactly `**Verify builds/tests** — run tests` and the parsed header is Some("Verify builds/tests") — must fail without the fix.
- [x] 2. ****Robust bold-header extraction (plan_file.rs)**** — Rewrite extract_bold_header_impl (src/workflow/plan_file.rs:401-428): (1) measure the leading run of `*` on trim_start()'ed text — if < 2, return None; (2) scan for the first subsequent `*` run of length >= 2 (that is the closer start); header = text between (trimmed, non-empty else None); (3) consume the ENTIRE closing run (so `after` starts clean); (4) keep the existing leading-token check on the text after the closer (empty / — / – / - / : / \n). Keep all existing semantics: `**a*b**` → Some("a*b") (single `*` inside is not a closer), "Use **bold** for emphasis" → None (mid-sentence), plain step → None. Update the fn doc comment (~:380-390) with `****Header****` examples. Add tests to plan_file.rs tests: `****foo**** — body` → Some("foo"); `****foo**` (asymmetric, real legacy case) → Some("foo"); `**foo**` → Some("foo") stays; and a parse-level regression test parsing the exact legacy line `- [x] 1. ****Verify builds/tests** — run them` → step.header == Some("Verify builds/tests").
- [x] 3. ****Frontend step-body strip helper (planSteps.ts)**** — Create frontend/src/lib/planSteps.ts exporting `stepBody(text: string): string` with a doc comment: strips a leading bold header (2+ asterisks, tolerant of the legacy double-wrapped `****` form) + optional separator (—/–/-/:) + whitespace, returning the body; plain text passes through unchanged. Implementation: `text.replace(/^\*{2,}[\s\S]*?\*{2,}\s*[—–\-:]?\s*/, "")`. Use it in frontend/src/components/views/PlanProgress.tsx (~:266) replacing the inline regex, updating the comment to note the `\*{2,}` tolerance for legacy `****` steps. Create frontend/src/lib/planSteps.test.ts (vitest, node): `**H** — body` → "body"; `****H**** — body` → "body"; `****H** — body` → "body"; `**H**` → ""; `Plain step` → unchanged; `**a*b** — body` → "body" (single `*` inside header); body starting with a hyphenated word NOT swallowed when separator absent? (case: `**H** body` → "body"). Register "src/lib/planSteps.test.ts" in frontend/vitest.config.ts include list.
- [x] 4. ****Markdown renderer contract test (formatting audit)**** — Create frontend/src/lib/markdownRendering.test.ts — the executable answer to 'what other formatting is failing?': import { MarkdownImpl } from "../components/chat/MarkdownImpl" (static, not the lazy wrapper) + createElement from react + renderToStaticMarkup from react-dom/server; for each corpus case assert the rendered HTML: `****double-bold****` contains <strong>; `**bold**` contains <strong>; `*italic*` contains <em>; `~~strike~~` contains <del>; GFM table (| a | b |\n|---|---|\n| 1 | 2 |) contains <table>; task list (`- [x] done`) contains checkbox/checked attr; fenced ```rust code contains class with hljs; bare URL (https://example.com on its own line) renders an <a>; hard-break line (two trailing spaces) produces <br>. Doc comment at top: this is the renderer contract — the chat/FileViewer/Goal path; plan-step headers are intentionally plain text. Register "src/lib/markdownRendering.test.ts" in frontend/vitest.config.ts. Run it: if any case fails, investigate and fix within the md stack (or document the limitation with justification in the test + final summary).
- [x] 5. ****Run all checks**** — PowerShell, unpiped, read test-result lines + $LASTEXITCODE (piped exit codes lie per the constitution): (1) cd frontend; npm test — new planSteps.test.ts + markdownRendering.test.ts green plus all existing suites. (2) cd frontend; npm run build — tsc proves the PlanProgress.tsx edit compiles. (3) Root cargo test — Rust plan.rs/plan_file.rs tests green, zero warnings. Fix anything that fails before proceeding.
- [x] 6. ****Review, fix, commit, finish**** — (1) git status --short + git branch --show-current to confirm scope (expected: src/tool/workflow/plan.rs, src/workflow/plan_file.rs, frontend/src/lib/planSteps.ts + planSteps.test.ts + markdownRendering.test.ts, frontend/src/components/views/PlanProgress.tsx, frontend/vitest.config.ts, + review report). (2) Spawn reviewer (spawn_agent role:"reviewer") over ALL uncommitted changes with a self-contained task: goal = fix `****bold****` plan-step headers (double-wrap at creation in plan.rs StepInput::to_text, extraction failure in plan_file.rs extract_bold_header_impl, frontend body-strip regex in PlanProgress) + renderer-contract audit test; list the changed files; ask for correctness/regression-risk/security/constitution findings. END THE TURN after spawning; resume on the background-finished notification. (3) Read the report; fix EVERY finding; re-run step 5's checks. (4) Commit the files + review report to fix/step-header-double-bold (never main). (5) Call finish with the report path. Final summary <=3 lines + Decisions section.
