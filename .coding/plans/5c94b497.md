# Plan: Thought-signature round-trip support for OpenAI-compatible endpoints

## Goal
Implement Gemini 3 thought_signature support generically across all OpenAI-compatible endpoints/proxies this app talks to: capture opaque signature metadata verbatim from streamed chat.completions responses into an optional metadata bag on assistant messages and individual tool calls, persist it through stored history serialization unchanged, and merge it back verbatim into subsequent request bodies so Google stateless-mode rules hold exactly as received never modified even after switching models mid-conversation while non-signature endpoints see byte-identical requests because provenance-gating makes fabrication impossible

## Kind
implementation

## Context
Verified via ai.google.dev thinking+openai-compat docs this session and stored as SPEC memory .coding/knowledge/spec/2026-12-20-gemini-thought-signature-wire-contract-round-tri.md. Signature scopes = per-tool-call entry fields and assistant-message-level fields on chat.completions wire objects captured during streaming near stream end. In-repo precedent for required echo fields is DeepSeek reasoning_content handling at openai.rs ~1323 builder plus turn.rs reasoning accumulation/packaging ~1028/~1254 including partial-stop path at turn.rs 1254 which must also keep signatures for interrupted turns

## Steps
- [x] 1. **Shared opaque metadata bag** — In src/provider/mod.rs add field provider_meta of type Option<serde_json::Map<String serde_json::Value>> with serde default plus skip_serializing_if None to both Message (~line 148) and ToolCall (~line 239), doc comments stating the provenance-gated verbatim echo invariant and pointing at the knowledge spec file .coding/knowledge/spec/2026-12-20-gemini-thought-signature-wire-contract-round-tri.md ; add pub const PROVIDER_PASSTHROUGH_KEYS holding thought_signature plus two helpers with doc comments : capture_provider_meta copying any passthrough key found on a response Value verbatim into an optional map returning None when nothing matched , apply_provider_meta merging every key of an optional map into an outgoing Value untouched ; fix every struct-literal construction site of Message and ToolCall under src including tests so cargo build compiles ; run cargo test green
- [x] 2. **Capture events from SSE stream** — Add variant ProviderMeta { tool_call_index Option<usize>, meta serde_json::Map } to LlmEvent enum src/provider/mod.rs ~328 documented as opaque provider metadata riding outside standard event kinds ; in src/provider/openai.rs streaming parse path run capture_provider_meta over each chunk choices[0].delta object emitting ProviderMeta with tool_call_index None for message-level keys , and over each delta.tool_calls[i] entry emitting ProviderMeta with Some(i) using that entry index ; concatenation rule when a key repeats across chunks : concatenate string values preserving order since Google streams signatures as their own delta near stream end where split-delivery keeps concat identical to passthrough ; anthropic parser untouched
- [x] 3. **Accumulate and package into stored history** — Extend DeltaAccumulator used by src/agent/turn.rs : feed() stores ProviderMeta events merging string values by key per scope like existing arg accumulation ; finalize() attaches per-call maps onto packaged ToolCall.provider_meta entries by accumulated index order , attaches message-level map onto final assistant Message.provider_meta ; cover BOTH packaging paths : normal completion packaging region ~1231 onward AND partial-output stop path pushing Message at turn.rs ~1254 so interrupted turns still carry captured signatures
- [x] 4. **Echo verbatim in request builder** — In src/provider/openai.rs request builder where messages_json objects are assembled (~1300-1335 region next to reasoning_content echo) : after building each assistant message object merge apply_provider_meta(message_obj m.provider_meta) then inside its tool_calls array merge apply_provider_meta(entry tc.provider_meta) preserving insertion position right after function block exactly where captured scopes map back ; no model-name or endpoint-config gating anywhere provenance alone decides presence so non-emitting endpoints produce identical bodies ; Local kind inherits via same builder unchanged ; Anthropic builder untouched
- [x] 5. **Test matrix** — In openai.rs tests module add : capture fixture shaped like Google compat stream where a delta.tool_calls entry carries thought_signature plus a later delta carries message-level thought_signature asserting bags land on right scopes , repeated-key concatenation across two chunks yields ordered joined value , round-trip test running parsed messages through build_request_json asserting signature strings appear byte-identical inside correct objects , no-fabrication regression asserting same fixture minus signature keys yields request body byte-equal to current expected output proving zero diff for endpoints that never emit , partial-interruption test mirroring turn packaging stop path retains bag ; verify existing anthropic unsigned-thinking regression still passes unchanged
- [x] 6. **Docs sync** — Update PLAN.md provider strategy section documenting the generic thought_signature round-trip mechanism its two wire scopes three invariants verbatim fidelity provenance-gated echo conversation-scoped survival across model switches , README feature bullet mentioning Gemini 3 thought signatures supported on OpenAI-compatible endpoints automatically , module doc comment cross-reference already added on provider_meta fields
