# Plan: Deduplicate list_models/list_vision_models + kind_wire (quality review HIGH 2)

## Goal
Deduplicate list_models/list_vision_models: extract resolve_endpoint_credentials + fetch_by_kind so both commands are thin wrappers, delete kind_wire in favor of endpoint_kind_wire, and add the missing test module covering all five resolution paths — behavior unchanged, drift risk eliminated.

## Kind
implementation

## Context
Quality review HIGH 2 (.coding/reviews/2027-01-06-full-review-quality-coverage.md): list_models and list_vision_models (src-tauri/src/ipc/models.rs:73-105 vs :149-181) copy-paste the 33-line credential-resolution block verbatim (override → saved endpoint → kind-aware env fallback → "dummy"); the delegation block (:110-118 vs :186-194) likewise; kind_wire (models.rs:22-28) duplicates endpoint_kind_wire (src-tauri/src/ipc/settings.rs:638-644, private fn with a test at settings.rs:1208). Drift risk on a user-facing path (the Vision tab picker keeps stale resolution); models.rs has NO test module. Exact current structure verified by reading the file: the credential block is a scoped expression `let (base_url, api_key, kind) = { let config = state.project.config.lock().await; ...; (base_url, api_key, kind) };` — the config guard DROPS at the block close, before the HTTP fetch (preserve this scoping in the wrappers). Resolution logic per field: base_url = override (filtered non-empty) → saved endpoint's base_url → Err("endpoint '{endpoint_name}' has no base_url and does not match a saved endpoint"); kind = override (filtered) → kind_wire(saved.kind) → "openai"; api_key = override (filtered) → config.key_for(&e.name) → kind-aware env (anthropic: ANTHROPIC_API_KEY→ANTHROPIC_AUTH_TOKEN; else OPENAI_API_KEY→ANTHROPIC_AUTH_TOKEN) → "dummy". Delegation: kind=="anthropic" → fetch_models_anthropic else fetch_models_with_vision, both .map_err(IpcError::from). API surface for tests: Config::default() (derives Default), config.endpoint(name) -> Option<&Endpoint>, config.key_for(name) -> Option<&str>, KeyStore::insert(endpoint_name, key) (src/config/keys.rs:76), Endpoint { name, kind, base_url, models, ..Endpoint::test_default() } (test_default is a plain pub fn at src/config/endpoints.rs:259 — verify it is not #[cfg(test)]-gated during implementation; if gated, spell out all 17 fields). Env-mutating tests must use the ENV_TEST_LOCK static-Mutex pattern from src/provider/client_factory.rs:486-506 (cargo test runs in parallel). IpcError: From<String> exists (the current code's `?` on a String error proves it). Editing technique: PowerShell here-string .Replace with Contains guards + @EM@ tokens for em-dashes (file_edit unavailable this session); newline-anchor IndexOf searches to avoid substring matches inside deeper-indented lines (lesson from the run_turn decomposition).

## Steps
- [x] 1. **Extract resolve_endpoint_credentials + fetch_by_kind, delete kind_wire** — In src-tauri/src/ipc/models.rs: (a) delete kind_wire (:22-28); (b) in src-tauri/src/ipc/settings.rs make endpoint_kind_wire pub(crate) (it stays private-to-crate, still used by endpoint_wire at :672); (c) add `use super::settings::endpoint_kind_wire;` to models.rs's imports; (d) add two private helpers to models.rs — `fn resolve_endpoint_credentials(config: &mnemo::config::Config, endpoint_name: &str, base_url: Option<String>, api_key: Option<String>, kind: Option<String>) -> Result<(String, String, String), IpcError>` containing the verbatim credential block (with kind_wire → endpoint_kind_wire in the kind chain) and `async fn fetch_by_kind(kind: &str, base_url: &str, api_key: &str) -> Result<Vec<mnemo::provider::models::ModelWithVision>, IpcError>` containing the verbatim delegation if/else — both with doc comments noting they are shared by the two commands so the pickers cannot drift (quality review HIGH 2); (e) rewrite both commands as thin wrappers: `let (base_url, api_key, kind) = { let config = state.project.config.lock().await; resolve_endpoint_credentials(&config, &endpoint_name, base_url, api_key, kind)? }; fetch_by_kind(&kind, &base_url, &api_key).await` — PRESERVING the block scoping so the config guard drops before the HTTP fetch; keep each command's doc comment (list_vision_models's "Resolution is identical to [list_models]" note is now literally true via the shared helper). Update the module doc if it describes the duplication. cargo test green (both crates: cargo test in src-tauri runs the app crate; also run the library suite from the repo root).
- [x] 2. **Add the models.rs test module (five resolution paths)** — Add a #[cfg(test)] mod tests to src-tauri/src/ipc/models.rs covering resolve_endpoint_credentials (the five paths from the review): (1) override_wins_over_saved — config with a saved endpoint (base_url X, stored key K), override (base_url Y, api_key Z) → (Y, Z, default kind "openai" or the override kind); (2) saved_endpoint_fallback — no override, saved endpoint with base_url + keys.toml key → (saved base_url, stored key, endpoint_kind_wire(kind)); (3) kind_aware_env_fallback — saved endpoints without stored keys, env vars set: anthropic kind → ANTHROPIC_API_KEY preferred, openai kind → OPENAI_API_KEY preferred (use the ENV_TEST_LOCK static-Mutex pattern from client_factory.rs:486-506; remove ANTHROPIC_AUTH_TOKEN first); (4) empty_base_url_errors — no override and no matching saved endpoint → the "has no base_url and does not match a saved endpoint" error; also an empty-string override base_url with no saved endpoint → same error (the filter treats empty as absent); (5) dummy_terminal_fallback — saved endpoint, no stored key, env vars removed → api_key == "dummy" (ENV_TEST_LOCK). Fixtures: `let mut config = mnemo::config::Config::default(); config.endpoints = vec![mnemo::config::Endpoint { name: "ep".into(), kind: EndpointKind::OpenAI/Anthropic, base_url: "https://x/".into(), models: vec![], ..Endpoint::test_default() }]; config.keys.insert("ep".into(), "sk-saved".into());` — verify Endpoint::test_default() is not #[cfg(test)]-gated (it is a plain pub fn at src/config/endpoints.rs:259; if gated, spell out all fields). cargo test green.
- [x] 3. **Test, review, fix findings, commit, finish** — Closing sequence: run both test suites (library cargo test from the repo root + the src-tauri crate's tests — one cargo test invocation from src-tauri, or the workspace command the repo uses); spawn the read-only reviewer (role:"reviewer", no model param) over all uncommitted changes with a self-contained task (the duplication being removed, the pure-code-motion claim for the extraction, the lock-scoping preservation requirement, the kind_wire→endpoint_kind_wire consolidation, the five new tests, and the constitution checks: doc comments on the new helpers, multi-platform neutrality — env-var access is platform-neutral, documentation sync). Read the report; fix every finding (re-run tests after); if the verdict is FINDINGS, fix → re-test → round-2 verification reviewer → PASS; commit to the current working branch including the review report(s); finish(report path).
