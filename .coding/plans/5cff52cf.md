# Plan: Fix five macOS cargo test --workspace failures (run 35521221353)

## Goal
Make the macOS CI leg's `cargo test --workspace` green — the five failing tests from release run 35521221353 fixed or platform-gated per the user's 2026-09-20 decision — so the v0.1.1 release can complete.

## Kind
bug_fixing

## Context
Diagnosis (all five read at source): (1) src/webview_args.rs:242 — production fn secondary_webview_data_dir (:137) is a pure join, no production bug; the TEST's expected PathBuf hardcodes backslashes. Fix: structure assertions (component-wise ends_with("WebView2/mnemo-4242") + parent-chain == base). (2)+(3) src/codegraph/watcher.rs:47 is_indexable_path does a plain strip_prefix(root); on macOS FSEvents resolves symlinks in delivered paths so a tempdir root (/var/folders/…) never matches its events (/private/var/folders/…) — the watcher filters EVERY event and both tests' polls time out. Deterministic on macOS, inert on Windows (notify delivers the watched root's form; raw prefix matches first). Fix: retry the component check against std::fs::canonicalize(root) on mismatch; event_is_indexable (:225) routes through it. Regression test: is_indexable_path_accepts_canonical_event_paths, cfg(unix) (symlink alias of a real root; canonical-form event must be indexable; fails without the fix on Unix incl. the macOS CI leg — fail-first evidence is run 35521221353 itself). Also double the two watcher tests' poll budgets (0..100 → 0..200 × 50ms, watcher.rs:313/:342) as FSEvents delivery-latency insurance. (4) src/browser/mod.rs:2829 — launches real Chromium; macOS root cause not diagnosable from Windows. USER DECISION 2026-09-20: platform-gate rather than widen — #[cfg(windows)] + dated comment. (5) src/agent/factory.rs:1698 — the failing assertion is the deferral-savings check inside the #[cfg(feature = "browser")] block (:1675), NOT the ceilings loop (passes on macOS today — keep it ungated so macOS keeps ceiling coverage). cargo test --workspace unifies features with src-tauri (browser+embeddings), compressing the deferral delta below 12k on every platform; the macOS CI leg is the only --workspace invocation. USER DECISION 2026-09-20: platform-gate rather than recalibrate — block becomes #[cfg(all(feature = "browser", windows))]; MockDescriber/make_factory_with have other callers (factory.rs:2133/:2222) so no dead-code warnings on macOS. MEASUREMENT GATE in step 1: if the deferral test already fails under plain --features browser on Windows, the gate would leave a red local command — STOP and report to the user (recalibrate vs disable are the options to re-present). Verification: cargo test + cargo test --features browser on Windows, both green; cargo test --workspace on Windows will still fail the deferral assertion BY DESIGN (documented residual — the gate is macOS-skip, not unification-aware); the macOS CI leg is the real --workspace gate. Post-plan (outside these steps, skills cannot run in Executing): merge via merge_to_main, then re-cut the v0.1.1 tag on the new main HEAD so the release job re-runs.

MEASUREMENT (step 1, Windows): the deferral test PASSES under plain --features browser — c1=47496, c0=32796, delta 14700 >= 12000; webview_args (7 passed) and codegraph::watcher (4 passed) green locally — macOS-only manifestation confirmed for (1)-(3). CORRECTED ROOT CAUSE for failure (5): NOT workspace feature unification — the browser deferred group is platform-conditional: the six on-screen WebView2 tools (factory.rs:1127-1135, #[cfg(windows)] — they attach to the app's WebView2 via CDP, absent on macOS) contribute ~3454 chars missing from the macOS all-loaded array (44042 vs 47496; c0 identical at 32796 on both sides). The 12k deferral threshold is calibrated to the Windows browser group; on macOS the delta is inherently 11246. Consequence: the #[cfg(all(feature = "browser", windows))] gate is exactly right AND leaves NO residual — Windows passes under any feature set (the six tools are always registered there), so a Windows cargo test --workspace passes too. Step 5's code comment must state the platform-conditional-group cause with the measured numbers, not the unification theory.

## Steps
- [x] 1. **Reproduce with failing regression test** — Write a regression test that reproduces the defect (constitution: every defect gets a regression test that fails without the fix and passes with it). Run it and confirm it FAILS.
- [x] 2. **Document root cause** — Investigate and document the root cause. memory_write a BUG: record (symptom → root cause → fix + regression test name, ≤600 chars).
- [x] 3. **Minimal fix** — Apply the minimal fix that makes the regression test pass. Do not refactor unrelated code.
- [x] 4. **Verify** — Run the regression test + the full test suite (cargo test unpiped, warning-free). Record the regression test name via update_plan (regression_test field) — finish is blocked without it. If the fix turned out FEATURE-SCALE (new pub types or cross-module surface): amend the plan context via update_plan append with a paragraph starting "Landed design —" (architecture, deviations from the sketch, measured numbers, invariants to preserve) and record landed_design=true BEFORE completing this step; at finish write a SPEC memory and amend the BUG record with the landed outcome (agent.md 'Documentation expectations'; the finish gate blocks landed_design without the context amendment).

## Detailed steps
- [x] **Reproduce + measure on Windows** — Run `cargo test --features browser tools_array_stays_within_context_budget -- --nocapture` and record the printed deferred-default/all-loaded char counts (the deferral delta). Run `cargo test webview_args` and `cargo test codegraph::watcher` to confirm they are green locally (the defects manifest on macOS only). BRANCH: if the deferral test FAILS under plain --features browser on Windows, STOP and report the measurement to the user — the platform gate would keep a local command red; do not proceed to step 5.
- [x] **Fix the webview_args separator test** — src/webview_args.rs:241-253 (secondary_udf_is_per_pid_under_webview2): replace the hardcoded-backslash assert_eq! with separator-agnostic structure assertions — assert dir.ends_with(std::path::Path::new("WebView2/mnemo-4242")) (component-wise, works on both separators) and dir.parent().and_then(|p| p.parent()) == Some(base); keep the distinct-pids assert_ne!. Add a comment naming macOS CI run 35521221353.
- [x] **Fix the watcher canonical-path filter** — src/codegraph/watcher.rs:47 is_indexable_path: extract the component check into a private helper; try the raw root first (Windows fast path unchanged), then on mismatch retry against std::fs::canonicalize(root) (FSEvents resolves symlinks in delivered paths; macOS tempdirs sit under /var → /private/var). Update the fn's doc comment. Add regression test is_indexable_path_accepts_canonical_event_paths (cfg(unix): create a real root under tempdir, symlink an alias, assert is_indexable_path(root.join("src/b.rs"), alias) is true and the plain form still matches). Double the poll budgets in watcher_spawns_and_indexes_outside_runtime (watcher.rs:313) and watcher_reindexes_on_new_source_file (watcher.rs:342) from 0..100 to 0..200 as FSEvents-latency insurance.
- [x] **Platform-gate the profile test** — src/browser/mod.rs:2828: add #[cfg(windows)] above the #[tokio::test] on profile_dir_is_removed_on_close, with a dated comment recording that it fails on the macOS CI leg (run 35521221353) and was gated per the 2026-09-20 user decision — Windows, where the app ships, keeps the coverage.
- [x] **Platform-gate the factory deferral block** — src/agent/factory.rs:1675: change #[cfg(feature = "browser")] to #[cfg(all(feature = "browser", windows))] on the block containing the deferral-savings assertion (:1698); extend the block's comment to record why (workspace feature unification compresses the deferral delta below 12k on every platform; the macOS CI leg is the only --workspace invocation; residual: a Windows cargo test --workspace run would still fail this assertion — recalibrating the threshold is the follow-up if that ever becomes a run command). Leave the assertion text and the ceilings loop untouched.
- [x] **Verify** — Run `cargo test` (full plain suite) and `cargo test --features browser` (browser suite, incl. the profile test and the factory block on Windows) — both must be green, zero warnings (deny(warnings) is enforced by the build). Do NOT treat a Windows `cargo test --workspace` deferral failure as a regression — it is the documented residual.
- [ ] **Review + commit** — Closing sequence: spawn the reviewer over all uncommitted changes (correctness, multi-platform neutrality — the cfg gates must not orphan helpers on macOS, regression tests exercise the changed paths), fix every finding, re-run tests, commit to the wt/* working branch, finish. Post-plan, outside these steps: merge via the merge_to_main skill, then re-cut the v0.1.1 tag on the new main HEAD so the release job re-runs.

## Bug
Five tests fail on the macOS CI leg's `cargo test --workspace` (release run 35521221353, v0.1.1): (1) webview_args::tests::secondary_udf_is_per_pid_under_webview2 — expected path hardcodes `\`, Path::join uses the host separator; (2) codegraph::watcher::tests::watcher_spawns_and_indexes_outside_runtime and (3) watcher_reindexes_on_new_source_file — "watcher must auto-index…" (FSEvents delivers symlink-resolved event paths that never prefix-match a symlinked root, e.g. every macOS tempdir /var → /private/var); (4) browser::tests::profile_dir_is_removed_on_close — profile dir outlives the 15s poll on macOS; (5) agent::factory::tests::tools_array_stays_within_context_budget — deferral-savings assertion c1 >= c0 + 12_000 fails under workspace feature unification (44042 vs 32796 = 11246).
