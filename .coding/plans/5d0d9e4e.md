# Plan: Codegraph scan polish: str::find + re-parse cap note

## Goal
Polish the finish-gate escalation's byte scan (src/codegraph/mod.rs reindex_files_containing): replace the naive O(n*m) windows() scan with str::find (Two-Way) via from_utf8 with a byte-scan fallback, and cap the total re-parse count (REPARSE_CAP) so a common identifier cannot re-parse the world — matches beyond the cap are still counted, the outcome gains a `capped` flag, and the gate error notes the partial refresh (the absence verdict is inconclusive for un-re-parsed files). The existing regression test stays unchanged and green.

## Kind
implementation

## Context
Backlog af1e76ec, polish from the 2026-09-08 performance review (LOW 1's non-source-reads half was already refuted — the extension gate skips non-source files before reading since 3d13884; this is pure scan-speed polish). Verified code: (1) the naive scan at src/codegraph/mod.rs:620-625 — `bytes.windows(needle.len()).any(|w| w == needle.as_bytes())`, O(n*m) per file; the needle is an ASCII identifier so str::find (Two-Way) is exact and faster; UTF-8 is self-synchronizing so an ASCII needle can only match as itself in valid UTF-8, and a from_utf8 failure (non-UTF-8 source file — rare/nonexistent) falls back to the byte scan keeping semantics identical. (2) The re-parse loop (:626-652): matched += 1 BEFORE the skips (containment guard, re-parse failure) so matched>0 && reparsed==0 stays the inconclusive signal; reindex_one per match — a common identifier would re-parse every matching file. (3) ReindexOutcome (:100, pub struct { matched, reparsed }) — constructed at the empty-needle early return (:587-592) and the final return (:657); consumed only by the gate at src/tool/workflow/plan.rs:1455-1524 (three error paths: matched==0 → wrong-test-name; reparsed==0 → inconclusive; else → "not found even after a forced re-index of {reparsed} file(s)" at :1502-1508 — where the cap note lands). (4) The regression test reindex_files_containing_reparses_despite_fresh_meta (:807) must stay unchanged and green; walk_searchable picks up root-level .rs files (the fixture writes app.ts at the root).

## Steps
- [x] 1. **str::find scan + REPARSE_CAP in codegraph**
- [x] 2. **str::find scan + REPARSE_CAP in codegraph** — src/codegraph/mod.rs: (a) Replace the scan at :620-625 — `let contains = match std::str::from_utf8(&bytes) { Ok(source) => source.contains(needle), Err(_) => bytes.windows(needle.len()).any(|w| w == needle.as_bytes()) }; if !contains { continue; }` with a comment explaining the Two-Way win, the ASCII-needle exactness (UTF-8 self-synchronizing), and the byte-scan fallback for non-UTF-8 files. (b) Add `const REPARSE_CAP: usize = 16;` (module level, doc comment: the escalation exists to refresh a MISSING symbol, not to re-parse the world; 16 tree-sitter parses bound the worst case sub-second). (c) Add `let mut capped = false;` next to matched/reparsed; in the loop after the containment guard passes and before the hash/reindex_one, add `if reparsed >= REPARSE_CAP { capped = true; continue; }` with a comment (matches stay counted — the scan is cheap — only the re-parse is skipped). (d) ReindexOutcome (:100) gains `pub capped: bool` with a doc comment (true when the budget was exhausted — matches beyond the cap were counted but not re-parsed, so a not-found verdict is inconclusive for them); update the empty-needle early return (:588, capped: false) and the final return (:657, capped). (e) New test after the regression test (~:850): reindex_files_containing_caps_its_reparse_budget — write REPARSE_CAP+4 root-level .rs files each containing the needle "run_all_dispatch_next", assert matched == REPARSE_CAP+4, reparsed == REPARSE_CAP, capped == true.
- [x] 3. **Gate error notes the cap**
- [x] 4. **Gate error notes the cap** — src/tool/workflow/plan.rs (:1502-1508, the third error path — matched > 0, reparsed > 0, still not found): when outcome.capped, append to the message a parenthetical noting the partial refresh — e.g. " (re-parse budget capped — {reparsed} of {matched} matching file(s) re-parsed; the absence verdict is inconclusive for the rest)" — built with an if/else on outcome.capped producing either the parenthetical or an empty string. No other error path changes (matched==0 and reparsed==0 keep their exact semantics).
- [x] 5. **Verify: cargo test (lib) green, regression test unchanged**
- [x] 6. **Verify: cargo test (lib) green, regression test unchanged** — Run `cargo test` at the repo root (mnemo lib — both changed files live there); check the `test result:` lines. Confirm reindex_files_containing_reparses_despite_fresh_meta passes UNCHANGED (git diff must show no edit to it) and the new cap test passes. Then the closing sequence (reviewer → fix findings → commit) runs automatically.
