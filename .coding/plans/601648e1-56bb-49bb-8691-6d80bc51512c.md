# Plan: Safety rules file — regex-based auto-approve + editor tab

## Goal
Add a "Mark Safe" option to approval prompts that saves a regex rule to `.coding/safety.toml`. Matching tool calls auto-approve (skip the prompt). Add a right-panel "Safety" tab to edit the file.

## Context
Approval flow: AgentLoop::execute_tool_call (src/agent/mod.rs:708) checks needs_approval() then sends ApprovalRequest. The ApprovalPrompt.tsx has Approve/Deny buttons calling the `approve` IPC command. SafetyLevel is per-tool (NeedsApproval/AutoRun). ConstitutionSource (src/project/agent_md.rs) is the mtime-checked file-backed pattern to mirror. RightPanel.tsx has a TABS array + RightPanelTab union in useAgentStore.ts. regex + toml crates are already deps. Sandbox (src/tool/agent/sandbox.rs) protects .coding/memory.db — must also protect the safety file. AgentLoop constructors (new/with_constitution_source) are used by ~10 tests; use a builder method with_safety_rules() to avoid breaking them.

## Steps
- [x] 1. Create src/safety_rules.rs: SafetyRules struct (file-backed, mtime-checked) with Rule{tool,pattern,regex}, signature(tool,args) extraction, is_safe(), rules(), add_rule(tool,args)->pattern, read_raw(), write_raw(). TOML [[rule]] format. Include tests. Add `pub mod safety_rules` to lib.rs.
- [x] 2. Wire SafetyRules into AgentLoop (src/agent/mod.rs): add `safety_rules: Option<Arc<SafetyRules>>` field (None in both constructors), add `with_safety_rules(self, rules)` builder + `safety_rules_handle()` accessor. In execute_tool_call, after needs_approval()==true, check is_safe(); if safe, skip approval and execute directly. Add a test: a matching rule auto-approves in ApproveEachAction mode.
- [x] 3. Protect the safety file in Sandbox::is_protected_write_target (src/tool/agent/sandbox.rs) — add `.coding/safety.toml`.
- [x] 4. Add IPC commands (src-tauri/src/ipc/commands.rs): get_safety_rules()->String, save_safety_rules(content:String)->Result<()>, add_safety_rule(tool:String,args:String)->Result<String>. Add `safety_rules: Arc<SafetyRules>` to IpcState (src-tauri/src/ipc/state.rs). Register commands in main.rs invoke_handler. Wire SafetyRules construction in build_brain() + fallback path, pass via .with_safety_rules() and store in IpcState.
- [x] 5. Frontend: add getSafetyRules/saveSafetyRules/addSafetyRule to tauri.ts + types to types.ts.
- [x] 6. Frontend: create SafetyRules.tsx view (raw TOML editor with save/reload, like MdViewer edit mode). Add 'safety' to RightPanelTab union in useAgentStore.ts. Add Safety tab to RightPanel.tsx TABS + content switch.
- [x] 7. Frontend: add 'Mark Safe' button to ApprovalPrompt.tsx — calls addSafetyRule(toolName, JSON.stringify(args)) then approve(). Show generated pattern feedback.
- [x] 8. Run cargo test + npm build to verify everything compiles and tests pass.
