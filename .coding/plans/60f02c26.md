# Plan: Close out backlog 1a5bffcf: verify the landed sandbox bypass fixes on main and finish the plan's last two steps

## Goal
Close out backlog item 1a5bffcf (Sandbox: hardlink / 8.3-alias / ladder-fallback path-protection bypasses) against the current main tree: verify on main that each of the three bypasses is closed by its named regression test (and that docs + module docs state the rules), run the full suite unpiped to green and warning-free, tick the plan file's two remaining steps (.coding/plans/b4812291.md — Docs sync, Full verification) via the sanctioned shell fallback, then mark the backlog item done with the merge sha and record the verification in the SPEC/BUG memories. No source-code changes: any failed check escalates to a new bug_fixing sub-plan instead of a patch here.

## Kind
research

## Context
WHY THIS PLAN EXISTS: backlog item 1a5bffcf was re-dispatched, but its work is already LANDED. Plan b4812291's three fixes were committed on wt/mnemo (work commit 78d8329), passed three review rounds (.coding/reviews/2026-09-15-sandbox-path-protection-bypasses-review.md + -round2.md + -round3.md), and came into main with the wt/mnemo merge at 630ec31b2072de843284d79b5ebfd7a632be1341 (2027-01-15, branch deleted, pushed). The PLAN/SPEC records for it are already marked MERGED. What is NOT done is the plan file's last two steps: .coding/plans/b4812291.md still shows `- [ ] Docs sync` (:34) and `- [ ] Full verification` (:35) unchecked — the owning session was interrupted (the item's note says "interrupted by the user, returned to queue"), so no session ever ran the closing pass. This plan supplies that pass. It is `research` because verification + .coding bookkeeping produce NO source-code changes; if any check FAILS, the fix is a new bug_fixing sub-plan (do not patch under this plan).

ALREADY VERIFIED IN PLANNING (read-only): the three fixes are present in src/tool/agent/sandbox.rs — the hardlink guard inside `is_protected_write_target` (:289-298, with `link_count` at :571-594), the 8.3 handling and the canonical-ancestor check before `create_dir_all` (:646-649, :419, :452), and the fail-closed ladder step 5 (:507); the five regression tests exist — `protected_hardlink_inside_coding_refused` (:1579), `#[cfg(windows)] protected_win32_8dot3_alias_refused` (:1456, with a visible `SKIP:` marker when the volume has 8dot3 disabled), `validate_for_write_refuses_in_root_link_into_protected_tree` (:1626), `validate_for_write_fails_closed_on_out_of_root_link` (:1515), `canonical_existing_ancestor_pins_the_alias_layer` (:1414); the docs are synced — PLAN.md:128-141 (hardlink refusal, canonical re-run that kills the 8.3 alias, ladder fail-closed + the one re-checked lexical fallback, attributed "2027-01-15, plan b4812291; review rounds 2-4 §Q5 of plan ee65fd4b"), docs/FEATURES.md:22, the sandbox.rs module doc's syscall paragraph (:55-63), `is_protected_write_target`'s doc (:210-217, incl. the accepted residual: a hardlink OUTSIDE .coding pointing at a protected .coding file is not caught). NOTE: the plan's own doc pointers "PLAN.md:438" / "PLAN.md:490-505" were written before the docs edit and have shifted (they now hold the resumability-gate / review-verdict text) — check doc sites by CONTENT, not by those line numbers.

ACCEPTANCE (from the backlog item) TO CONFIRM ON MAIN: one regression test per bypass; `cargo test` green and warning-free (#![deny(warnings)] at src/lib.rs:5 and src-tauri/src/main.rs); (2) Windows-gated, (1) and (3) platform-neutral. VERIFICATION COMMANDS: `cargo test` (full, unpiped — read the `test result:` lines and `$LASTEXITCODE`; a piped exit code is the pipe's, per agent.md), targeted `cargo test --lib sandbox` and `cargo test --lib file_write`. FILE-TOOLS NOTE: `.coding/plans/**` is refused by the file tools BY DESIGN (PLAN.md:128-130), so ticking the two boxes is the constitution's sanctioned last-resort shell fallback, and the step states that justification. The two knowledge files this plan amends: .coding/knowledge/spec/2027-01-11-sandbox-write-protection-hardlink-8-3-alias-ladd.md (SPEC 23902096) and the BUG record f5bf8008's file under .coding/knowledge/bug/.

## Steps
- [x] 1. **Verify the three fixes + five regression tests on main** — Run, UNPIPED: `cargo test --lib sandbox` then `cargo test --lib file_write`, printing `$LASTEXITCODE` after each (never trust a piped exit code). Confirm these five tests are present and PASS: `protected_hardlink_inside_coding_refused`, `validate_for_write_refuses_in_root_link_into_protected_tree`, `validate_for_write_fails_closed_on_out_of_root_link`, `canonical_existing_ancestor_pins_the_alias_layer`, and `#[cfg(windows)] protected_win32_8dot3_alias_refused` — the last one either passes or prints its visible `SKIP: …` marker (the read-only `fsutil 8dot3name query C:` say whether this volume generates aliases; a SKIP is the accepted outcome per the acceptance list, but the marker must be visible in the output). Also confirm the fix anchors are still in place by reading src/tool/agent/sandbox.rs: the hardlink guard + `link_count` (is_protected_write_target, ~:289-298 / :571-594), the canonical-ancestor check before `create_dir_all` (~:646-649), and the narrowed step-5 fallback (~:507). Record the exact output lines as the verification evidence.
- [x] 2. **Verify the docs sites (by content, not the shifted line numbers)** — Read and confirm each doc site states the three rules by CONTENT (the plan file's own `PLAN.md:438` / `:490-505` pointers are pre-edit line numbers and have shifted): PLAN.md:128-141 (the approval-contract/safety bullet: hardlink refusal, the canonical re-run that closes the Win32 8.3 alias, the ladder's fail-closed link gate + the one re-checked lexical fallback, with the 2027-01-15 / plan b4812291 attribution); docs/FEATURES.md:22; src/tool/agent/sandbox.rs:55-63 (module doc syscall paragraph) and :210-217 (`is_protected_write_target` doc: the hardlink rule + the accepted residual — a hardlink OUTSIDE .coding pointing at a protected file is not caught). If any site is stale or missing, STOP: a research plan cannot write non-.coding docs — create_plan an implementation sub-plan naming the exact file and the missing sentence, and finish this plan after it lands.
- [x] 3. **Full verification on main** — Run `cargo test` from the repo root UNPIPED and read the `test result:` lines plus `$LASTEXITCODE`: all suites green, `0 failed`, and warning-free — `#![deny(warnings)]` at src/lib.rs:5 and src-tauri/src/main.rs means a green run proves zero warnings, so never add `#[allow(...)]`. Call out explicitly that the sandbox protection family, the research-write dispatch tests (`cargo test --lib dispatch`), and the file-tool tests (`cargo test --lib file_write`) did not regress. Record the counts (expected ~2380 passed / 0 failed / 5 ignored on the lib suite) as the plan's verification evidence.
- [x] 4. **Close out the plan file's last two steps** — Tick the two remaining boxes in .coding/plans/b4812291.md: `- [ ] **Docs sync**` and `- [ ] **Full verification**` → `- [x]`. The file tools refuse `.coding/plans/**` BY DESIGN (PLAN.md:128-130), so this is the constitution's sanctioned last-resort shell fallback — state that justification in the shell call's purpose. Use a single targeted PowerShell replacement of exactly those two lines (no whole-file rewrite), then `read_files` the file back and confirm ONLY those two lines changed (the four skeleton steps and the five other detailed steps stay `[x]`). If the write cannot be made surgically, leave the file untouched and record the state in a memory instead — never hand-edit the file wholesale.
- [x] 5. **Close out the backlog item + record the verification** — No-code research step (bookkeeping only). (a) `backlog_status(id="1a5bffcf-6e82-4a64-9dba-16fe52bec53d", status="done", note=…)` — it rewrites .coding/backlog.jsonl; the note names the outcome: all three bypasses closed and verified on main at 630ec31 (work commit 78d8329), five regression tests green, the 8.3 test exercised or visibly SKIPped. (b) `memory_amend` on .coding/knowledge/spec/2027-01-11-sandbox-write-protection-hardlink-8-3-alias-ladd.md (SPEC id 23902096-0277-52af-9f3f-8003281f6148) with a dated verification paragraph: the five test names, the suite result, and that the plan file's two remaining steps were satisfied by this close-out pass. (c) `memory_amend` the BUG record (id f5bf8008-b8c0-577c-a738-bdd9c8b0bb8d; its file lives under .coding/knowledge/bug/ and is named in the tool's own result — read it back to confirm the paragraph landed) with the landed outcome: fixed by plan b4812291, merged and verified at 630ec31, per-bypass regression tests named. Then finish the plan (research kind — no review round).
