# Plan: Graph-vs-search tool-choice guidance (prompt + empty-result hint)

## Goal
Make tool choice self-correcting: prompt distinguishes symbol questions (graph tools) from text questions (search) plus a search→graph_context chaining rule, and graph_search returns an actionable hint on zero matches instead of failing silently.

## Kind
implementation

## Context
Session discussion: agent used `search` for string-literal lookups (correct) but didn't escalate to graph_context once symbols surfaced (miss). Root causes: (1) TOOL_STRATEGY in src/agent/prompt.rs (lines ~161-168) headlines "TRY THESE FIRST" for graph tools with the string carve-out buried, and has no chaining rule; (2) graph_search in src/tool/agent/codegraph.rs (execute at ~line 118-136) returns {"count":0,"symbols":[]} with no guidance; (3) the tool description (~lines 104-107) doesn't state that string literals are unindexed. Existing tests: src/agent/prompt.rs asserts TOOL_STRATEGY contents (~lines 683-740, incl. "TRY THESE FIRST"); src/tool/agent/codegraph.rs has graph_search_unknown_name_returns_empty_not_error (~line 417). Windows 11, PowerShell, project root C:\AgenticCoder\AgenticCoder; run cargo test unpiped and check $LASTEXITCODE; #![deny(warnings)] so green test = warning-free; never commit to main.

## Steps
- [x] 1. **Create feature branch** — Use the git tool to create and switch to branch feat/graph-search-guidance from the current main HEAD (app rule: never commit to main). Verify with git status/branch.
- [x] 2. **Rewrite TOOL_STRATEGY graph/search lines in src/agent/prompt.rs** — Rework the graph_search line (~161-165) and search line (~166-168) to be question-typed: symbol questions (where defined / who calls / blast radius / path) → graph tools first; text questions (tool-name string literals, config keys, log messages, comments) → search. Add a chaining rule sentence: when any result surfaces a symbol you need to understand (definition or callers), use graph_context instead of reading whole files. Then update the prompt tests near lines 683-740 that assert TOOL_STRATEGY contents (e.g. contains "TRY THESE FIRST") to match the new wording.
- [x] 3. **Add empty-result hint + description scope note in src/tool/agent/codegraph.rs** — In GraphSearchTool::execute (~line 127-134), when matches is empty, add a `hint` field to the returned JSON: no symbols matched; graph indexes symbol definitions only — for string literals (tool names, config keys, log text) use `search`, for a symbol fragment retry a shorter query. Omit the field on non-empty results. Also extend the ToolSchema description (~104-107) with one clause: indexes symbol definitions only — string literals (tool names, config keys) are not indexed; use search for those. Update test graph_search_unknown_name_returns_empty_not_error (~line 417) to assert the hint field is present, and add a test asserting a successful search result has no hint field.
- [x] 4. **Run cargo test (root crate)** — In PowerShell at C:\AgenticCoder\AgenticCoder run `cargo test` unpiped, then `"exit=$LASTEXITCODE"` — must be 0. Under #![deny(warnings)] a green run also proves warning-free; fix any failure at the root cause (no #[allow]).
