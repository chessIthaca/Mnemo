# Plan: Architecture multi-agent review

## Goal
Produce four focused architecture review reports (maintainability, UI wiring, performance, quality), then stop so we can review findings together before any implementation.

## Context
myharness is a Tauri 2 + React/TS + Rust agentic coding harness. Brain lives in src/ (agent, runtime, workflow, tool, memory, provider, config, project, skill). UI bridge is src-tauri/src/ipc/ (commands, events, approval, backlog, run_all, state). Frontend is frontend/src/ (React + Zustand + Tailwind). PLAN.md is the design doc. Multi-agent runtime via AgentManager with channel decoupling (AgentCommand/AgentEvent). Workflow is plan-first with stack + skills. User wants a detailed architecture review via parallel agents, then a stop-and-discuss — no fixes yet.

## Steps
- [x] 1. Spawn four parallel read-only review agents covering maintainability, UI wiring, performance, and code quality
- [x] 2. Collect their reports from .coding/reviews/ once all finish
- [x] 3. Synthesize findings into a joint architecture brief and stop for human review — no code changes
