# Plan: Token Dashboard: savings aggregates + get_savings_stats IPC + Dashboard view before Memory

## Goal
Ship the Token Dashboard for backlog item 652ae094: per-project savings AGGREGATION over the already-landed `savings_events` ledger, exposed through a new `get_savings_stats` IPC command, and rendered in a new Dashboard view positioned immediately before the Memory tool in the left toolbar — showing metered total saved tokens, per-kind and per-day breakdowns, the recent events, and a cache-efficiency section whose pricing-derived dollar figure is labeled ESTIMATED and kept strictly out of the metered totals. Reuse the existing ledger, SavingsEvent type, record_savings_event writer and instrumentation; build only the aggregate read, the IPC surface, the frontend binding + view, the registry entry, the docs and the tests.

## Kind
implementation

## Context
Backlog item 652ae094 (user request 2027-01-11): per-project token-savings logging + a Dashboard view immediately BEFORE the memory tool in the left toolbar, absorbing every lever from roadmap e4a50d22. Full original recipe: .coding/plans/a990cf74.md.

CRITICAL — ALREADY LANDED, REUSE, DO NOT RECREATE. Plan b1fa7963 (committed on wt/mnemo as 1a37d2f) already shipped the ledger and the instrumentation this item's step 1 described:
- `savings_events` table: src/memory/schema.rs:139 — (id TEXT PRIMARY KEY, session_id TEXT, kind TEXT NOT NULL, detail TEXT, tokens_before INTEGER NOT NULL, tokens_after INTEGER NOT NULL, tokens_saved INTEGER NOT NULL, measured INTEGER NOT NULL DEFAULT 1, created_at INTEGER NOT NULL) + indexes idx_savings_events_session(:151) and idx_savings_events_kind(:153).
- `SavingsEvent` type: src/memory/types.rs:558 (Debug+Clone+Serialize+Deserialize).
- Store: trait `record_savings_event` (src/memory/mod.rs:390) and `savings_events_rows` (:395); impls :2130 and :2158, both following the house pattern — `self.conn.clone()` / `self.read_conn()`, `tokio::task::spawn_blocking`, `conn.lock().expect("conn lock poisoned")`, `.map_err(|e| Error::Memory(format!("<fn> task failed: {e}")))`.
- Round-trip test: src/memory/tests.rs:2015 `savings_events_round_trip_and_negative_expansions`.
- Instrumentation: `AgentLoop::record_savings_event` (src/agent/turn.rs, fire-and-forget tokio::spawn + log-on-Err) is the SINGLE ledger writer, fed from the turn loop's `data.savings {kind, detail, tokens_before, tokens_after, measured:false}` extraction. Kinds already written: truncation, compaction, delta_read, skeleton, compression, archive, archive_expand, compaction_checkpoint. So step 2 of the old recipe (instrument truncation + compaction) is DONE — do not redo it, and do not touch src/agent/context.rs.
- Also already present: `tool_result_archive` + FTS5 + the `expand_result` tool.

REMAINING SCOPE (what this plan builds): (a) the aggregate read `savings_stats()` + its types; (b) the `get_savings_stats` IPC command + registration; (c) the frontend binding + `DashboardView`; (d) the toolbar/view registry entry before Memory; (e) docs; (f) tests.

ANCHORS VERIFIED BY READING (2027-01-25):
- Types style to mirror: `RequestStats` at src/memory/types.rs:514-516 (doc comment + `#[derive(Debug, Clone, Serialize, Deserialize)]`).
- Aggregate to mirror: the `project_stats` impl in src/memory/mod.rs (search `async fn project_stats`) — it already does the per-day bucket (created_at / 86400) and the NULL-safe cache sums this plan needs.
- IPC to mirror: src-tauri/src/ipc/agent.rs:936-951 `get_project_stats` (factory → memory_handle → store method → serde_json::to_value); registered in src-tauri/src/main.rs's invoke_handler beside `ipc::agent::get_project_stats`.
- Frontend binding to mirror: frontend/src/lib/tauri.ts:1527-1530 `getProjectStats` (`invoke("get_project_stats")`).
- Registry: frontend/src/hooks/rightPanelViews.tsx — order plan/diff/git/files/stats/trace/backlog/browser/graph/memory, with `memory` LAST at :64; the lucide import is at :22 and view components are imported at :25-34. frontend/src/hooks/agentState.ts — `RightPanelTab` union at :323-333 and `ALL_RIGHT_PANEL_TABS` at :336.
- Sync guard: frontend/src/hooks/rightPanelViews.test.ts asserts every RightPanelTab has a registry entry, that the registry references no unknown id, and that the two lengths are EQUAL — it must pass UNMODIFIED.
- VITEST INCLUDE TRAP: frontend/vitest.config.ts holds an explicit `include` list naming each test file, and frontend/src/lib/vitestInclude.test.ts guards it — a new test file MUST be added to that list or it will never run.
- Frontend render-test precedent: frontend/src/components/views/BacklogView.test.tsx / the node-env `renderToStaticMarkup` pattern; StatsView.tsx holds the fmtTokens/fmtCost/modelCost pricing idioms the ESTIMATED-$ section must reuse.

VERIFICATION: `cargo test` from the repo ROOT, run UNPIPED (piped exit codes lie — a filtered invocation can report 1 on success); read the `test result:` lines. Warning-free is mandatory (`#![deny(warnings)]` at both crate roots). Then `cargo test -p mnemo-app --no-run` from src-tauri/ (the app crate DOES compile on this machine — verified in this session; a bad config path there is a real bug class). Then `npx tsc --noEmit` and `npx vitest run` in frontend/ (expect the current baseline: 91 files / 1281 tests, which grows by the new DashboardView test). Branch: wt/mnemo (already current — no branch arg).

NOTE: item e4a50d22 remains the LEVERS roadmap and will keep writing savings_events rows as further phases land; this plan only reads them.

## Steps
- [x] 1. **SavingsStats aggregate types** — REUSE, do not redefine: `SavingsEvent` already exists at src/memory/types.rs:558 and the `savings_events` table at src/memory/schema.rs:139 — touch neither. In src/memory/types.rs add, next to SavingsEvent and following the `RequestStats` style at :514-516 (doc comment + `#[derive(Debug, Clone, Serialize, Deserialize)]`), public types:\n- `SavingsKindBreakdown { kind: String, event_count: i64, saved_tokens: i64 }`\n- `SavingsDayBreakdown { day: i64, event_count: i64, saved_tokens: i64 }` (day = epoch DAYS, i.e. created_at/86400, so the UI can format it)\n- `CacheEfficiency { prompt_tokens: i64, cached_tokens: i64, cached_not_null_requests: i64, request_count: i64 }`\n- `SavingsStats { saved_tokens_total: i64, event_count: i64, per_kind: Vec<SavingsKindBreakdown>, per_day: Vec<SavingsDayBreakdown>, recent: Vec<SavingsEvent>, cache: CacheEfficiency }`\nEvery field needs a doc comment (public items are doc-commented in this repo). Export them from src/memory/mod.rs's re-export list beside `SavingsEvent` (src/memory/mod.rs:38). Then `cargo test` (root, unpiped).
- [x] 2. **savings_stats() store method + tests** — Read `async fn project_stats` in src/memory/mod.rs first — it already implements the per-day bucket (created_at / 86400) and the NULL-safe cache sums (`unwrap_or(0)`), which this method mirrors. Add to the `MemoryStore` trait beside `savings_events_rows` (src/memory/mod.rs:395): `async fn savings_stats(&self) -> Result<SavingsStats>;` with a doc comment naming it as the dashboard's read path. Implement it beside the other savings/archive methods (~:2158+), following the house pattern exactly: clone `self.read_conn()`, `tokio::task::spawn_blocking`, `conn.lock().expect("conn lock poisoned")`, `.map_err(|e| Error::Memory(format!("savings_stats task failed: {e}")))`. Four queries:\n1. totals: `SELECT COALESCE(SUM(tokens_saved),0), COUNT(*) FROM savings_events`\n2. per-kind: `SELECT kind, COUNT(*), COALESCE(SUM(tokens_saved),0) FROM savings_events GROUP BY kind ORDER BY SUM(tokens_saved) DESC, kind`\n3. per-day: `SELECT created_at/86400 AS day, COUNT(*), COALESCE(SUM(tokens_saved),0) FROM savings_events GROUP BY day ORDER BY day`\n4. recent: `SELECT id, session_id, kind, detail, tokens_before, tokens_after, tokens_saved, measured, created_at FROM savings_events ORDER BY created_at DESC, id DESC LIMIT 50` (row→SavingsEvent mapping mirrors savings_events_rows at :2158)\n5. cache (from `request_stats`, NULL-safe): `SELECT COALESCE(SUM(prompt_tokens),0), COALESCE(SUM(COALESCE(cached_tokens,0)),0), COUNT(cached_tokens), COUNT(*) FROM request_stats`\nAn EMPTY store must return zeros with empty vectors, never an error. Tests in src/memory/tests.rs mirroring `savings_events_round_trip_and_negative_expansions` (:2015): record several events across two kinds and two distinct days (pass explicit created_at values), then assert per-kind sums/ordering, per-day buckets, recent ordering, the totals, the cache sums, and the empty-store zero case. `cargo test` (root, unpiped).
- [x] 3. **get_savings_stats IPC command + registration** — In src-tauri/src/ipc/agent.rs add `get_savings_stats` immediately after `get_project_stats` (:936-951), mirroring it verbatim in shape: `#[tauri::command] pub async fn get_savings_stats(state: State<'_, IpcState>) -> Result<serde_json::Value, IpcError>` → `state.factory()?` → `factory.memory_handle().ok_or_else(|| "no memory store configured".to_string())?` → `store.savings_stats().await.map_err(|e| format!("failed to read savings stats: {e}"))?` �� `serde_json::to_value(&stats).map_err(|e| format!("serialize: {e}"))?`. Then register it in src-tauri/src/main.rs's invoke_handler as ONE additive line beside `ipc::agent::get_project_stats` — use an exact anchor and do not reformat the surrounding handler. Verify from src-tauri/: `cargo test -p mnemo-app --no-run` (this crate does compile on this machine; a typo'd store/config path here is a real bug that the root suite cannot catch).
- [x] 4. **Frontend binding + DTO types** — In frontend/src/lib/tauri.ts, immediately after `getProjectStats` (:1527-1530), add the DTO interfaces mirroring the Rust types field-for-field (`SavingsKindBreakdown`, `SavingsDayBreakdown`, `CacheEfficiency`, `SavingsStats`) and `export async function getSavingsStats(): Promise<SavingsStats> { return await invoke("get_savings_stats"); }`. Purely additive — do not reorder or restyle neighbouring bindings. `npx tsc --noEmit` in frontend/ must stay clean.
- [x] 5. **DashboardView component + test** — Read frontend/src/components/views/StatsView.tsx FIRST and reuse its idioms: the `useEffect` + errMsg fetch pattern, `fmtTokens`/`fmtRate` from lib/format, and the pricing math (`modelCost`/`fmtCost`) that bills cached tokens at the cached rate vs the input rate. Create frontend/src/components/views/DashboardView.tsx with: (1) summary cards — total saved tokens (METERED) + event count; (2) a per-kind table (kind, events, saved tokens); (3) per-day savings bars from `per_day`; (4) a recent-events list (time, kind, detail, before→after, saved); (5) a cache-efficiency section (cached vs prompt totals, hit rate, and the pricing-derived $ figure) whose dollar value is EXPLICITLY labeled ESTIMATED and MUST NOT be added into the metered totals — render the metered and estimated numbers as visually distinct figures. Handle the loading/error/empty-store states. Then a new frontend/src/components/views/DashboardView.test.ts using the node-env `renderToStaticMarkup` pattern (BacklogView.test.tsx precedent) with a stubbed `getSavingsStats`, asserting the metered total, a per-kind row, and the ESTIMATED label render. ALSO add that test file to the explicit `include` list in frontend/vitest.config.ts (frontend/src/lib/vitestInclude.test.ts guards that every test file is listed, so an unlisted test silently never runs). `npx vitest run` in frontend/ must be green.
- [x] 6. **Registry: Dashboard immediately before Memory** — frontend/src/hooks/rightPanelViews.tsx: add `Gauge` to the lucide import (:22), import `DashboardView` beside the other view imports (:25-34), and insert `{ id: "dashboard", label: "Dashboard", icon: Gauge, component: DashboardView }` IMMEDIATELY BEFORE the memory entry (:64) — the left toolbar order derives from this registry, so this is what puts Dashboard directly before Memory. frontend/src/hooks/agentState.ts: add `"dashboard"` to the `RightPanelTab` union (:323-333) and to `ALL_RIGHT_PANEL_TABS` (:336+) in the SAME before-memory position. Do not weaken frontend/src/hooks/rightPanelViews.test.ts: it asserts union↔registry parity and equal length, and it must pass UNMODIFIED (it is the designed drift catcher). `npx tsc --noEmit` + `npx vitest run` green.
- [x] 7. **Docs + full verification + land** — docs/FEATURES.md and README.md: additive mention of the Dashboard view (immediately before Memory in the left toolbar) and the per-project `savings_events` ledger it reads — note that the ledger is per-project by construction (each project owns its memory DB) and that the dollar figure is an ESTIMATE derived from the pricing table, never mixed with the metered token totals. Use exact anchors; append in a distinct region rather than rewriting existing paragraphs. Then the FULL verification: `cargo test` from the repo root UNPIPED (read the `test result:` lines; warning-free is mandatory under #![deny(warnings)]); `cargo test -p mnemo-app --no-run` from src-tauri/; `npx tsc --noEmit` and `npx vitest run` in frontend/ (baseline 91 files / 1281 tests, plus the new DashboardView test). Then run the closing sequence: spawn the read-only reviewer subagent over ALL uncommitted changes (omit the model param), fix every finding, re-run the suites, commit to wt/mnemo (never main) including the review report, and finish. If the reviewer returns FINDINGS rather than PASS, fix and re-review — finish is fail-closed on a non-PASS verdict.
