# Plan: Phase 3b — Run-All halt/checkpoint UX (preserve sha, pause/rollback policy, Finished vs Complete)

## Goal
Phase 3b (Quality M3/M4, UI M8): preserve checkpoint sha on Run-All halt; introduce or confirm PausedForApproval (or explicit rollback) policy; document and enforce the success criterion (Finished vs Complete) for Run-All. Make the behavior consistent so that halting mid-Run-All keeps the git checkpoint and the UI can resume or roll back cleanly. No drive-by refactors. Tests + cargo test required.

## Context


## Steps
- [x] 1. Inspect current Run-All implementation (src-tauri/src/ipc/run_all.rs) and how it drives turns and handles terminal events (Error/Finished).
- [x] 2. Inspect turn.rs terminal emission and how checkpoints are created/stored (look for checkpoint, git stash or similar in run_all or agent).
- [x] 3. Inspect workflow state machine and any PausedForApproval or equivalent states.
- [x] 4. Identify where halt/interrupt on Run-All should snapshot the current checkpoint sha and surface a clear state.
- [x] 5. Implement minimal change to preserve checkpoint on halt; ensure UI-visible state reflects pause/rollback choice.
- [x] 6. Update or add tests (integration or unit) for halt preserving checkpoint and Finished-vs-Complete distinction.
- [x] 7. Run cargo test (unpiped) and confirm pass. Fix any breakage.
- [x] 8. If clean, mark the parent remediation plan step 16 complete.
