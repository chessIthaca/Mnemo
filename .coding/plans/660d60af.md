# Plan: Land the spawn-cancellation + event-delivery delta (quality fixes Q1-Q4)

## Goal
Apply the four quality-review fixes (Q1 latch store order, Q2 CWD-independent diag sink, Q3 registration-rejection source-contract test, Q4 cosmetics) to the uncommitted spawn-cancellation + UI-event-delivery delta on wt/agenticcoding, verify with the full test matrix and the live emit-diag chain, and commit the whole delta including the BUG knowledge file — closing backlog item f20bff16 (UI event blackout) on the way.

## Kind
implementation

## Context
Backlog item b18ef70d (top of queue), dispatched from the 2026-09-08 four-axis review round. EXECUTION UPDATE: the run-all dispatcher committed the ENTIRE delta as a pre-item checkpoint BEFORE dispatching this item — commit a8546f2 on wt/agenticcoding (source files + BUG knowledge file + stack.json.bak; tree now clean except .coding/backlog.jsonl + this plan file). Consequences: (a) the "uncommitted delta" is already landed — this plan's final commit covers ONLY the Q1-Q4 diffs + the new registration test + plan file + backlog.jsonl; (b) the attribution baseline is a8546f2 (git diff against it isolates this item's changes); (c) test invocation pinned: the root Cargo.toml is BOTH the mnemo package AND the workspace root ([workspace] members = ["src-tauri"]) so `cargo test --workspace` covers both crates; frontend via `npm test` (vitest run) in frontend/. Original planning context: the delta contains (1) the spawn-cancellation race fix — has_ever_started latch on AgentHandle (src/runtime/channels.rs:1041-1055), cleanup filter change + cleanup_tests (src-tauri/src/ipc/events.rs:1235-1258, 1465+), bookkeeping test (src-tauri/src/console.rs), latch test (src/runtime/mod.rs:355-379); (2) the UI-event-delivery hardening — emit diagnostics + WEBVIEW_PROBE_JS + diag_line (src-tauri/src/ipc/events.rs:1030-1065), ui_diag command (src-tauri/src/ipc/agent.rs + registration in src-tauri/src/main.rs), bounded buffering + attach/detach + registration surfacing (frontend/src/hooks/useAgentEvents.ts), uiDiag (frontend/src/lib/tauri.ts), buffering tests (frontend/src/hooks/useAgentEvents.dispatch.test.ts). The quality review (.coding/reviews/2026-09-08-full-review-quality.md) verified the delta and found four LOW findings (Q1-Q4) to fix. Key facts: the filter evaluates !is_running() && has_ever_started() (events.rs:1254-1255); the latch test pins only sequential semantics; the repo has a source-contract test pattern (readFileSync + import.meta.url, see frontend/src/lib/toolCardPaths.test.ts); tauri.ts:32 already names its constant AGENT_EVENT_CHANNEL. Decisions: (a) Q1 gets the swap PLUS Release/Acquire ordering and a latch-first filter evaluation — Relaxed stores to different atomics can reorder on ARM64 (macOS is a first-class target); (b) Q2 uses a OnceLock-resolved sink keeping .coding/logs/emit-diag.log when a .coding side-car exists under the CWD (dev) and falling back to std::env::temp_dir().join("mnemo-emit-diag.log") in packaged builds (the watchdog's anchoring pattern, main.rs:545/629); (c) the acceptance's literal fresh-app-restart test is impossible from inside the running app — verified instead via the current process's emit-diag chain plus the green matrix on the fixed delta; (d) the emit*.log debris at the repo root is left alone (outside scope; emit-stderr.log may be a live sink); (e) commit stages files explicitly, never git add -A.

## Steps
- [x] 1. **Verify baseline + pin test invocation** — Branch wt/agenticcoding confirmed; the delta is COMMITTED as the dispatcher's pre-item checkpoint a8546f2 (source + BUG file; tree otherwise clean — verified via git show --stat a8546f2). Test invocation pinned: root Cargo.toml is the mnemo package AND workspace root with members=["src-tauri"] → `cargo test --workspace` covers both crates; frontend `npm test` (vitest run) in frontend/. Run the baseline matrix NOW (pre-change, unpiped, check $LASTEXITCODE) to confirm the checkpointed delta's tests are green — if red, fix before applying Q1-Q4. The latch test (src/runtime/mod.rs:355-379) and cleanup tests (src-tauri/src/ipc/events.rs:1465+) are the pinned behavior.
- [x] 2. **Q1 — swap the latch store order + strengthen ordering** — src/runtime/channels.rs:1046-1055: in set_running, store `running` FIRST, then latch `has_ever_started` with Ordering::Release (currently latch-first, both Relaxed). Change has_ever_started() (1041-1044) to load with Ordering::Acquire. Update set_running's doc comment: between the two stores the handle must read "pending" or "running", never "started + idle" (the cancel signature); Release/Acquire stops weakly-ordered targets (ARM64) publishing the latch before the running store it follows. In src-tauri/src/ipc/events.rs:1254-1255, reorder the filter's last two conditions to `h.has_ever_started() && !h.is_running()` (latch first — the Acquire load guarantees the subsequent running load sees the pre-latch store) with a short comment. Sequential semantics are unchanged; the existing latch test must still pass.
- [x] 3. **Q2 — anchor diag_line's sink CWD-independently** — src-tauri/src/ipc/events.rs:1036-1059: add a module-level `static DIAG_SINK: std::sync::OnceLock<std::path::PathBuf>` resolved by a helper: if `.coding` exists under the current working directory (dev launches), use `.coding/logs/emit-diag.log`; else `std::env::temp_dir().join("mnemo-emit-diag.log")` (packaged builds — the same anchor the hang watchdog uses for its reports, src-tauri/src/main.rs:545,629). diag_line writes to the resolved path (create_dir_all on its parent as now). Extend the doc comment to explain the resolution. Dev behavior is unchanged — every existing reference to .coding/logs/emit-diag.log keeps working.
- [x] 4. **Q3 — registration-rejection source-contract test** — New file frontend/src/hooks/useAgentEvents.registration.test.ts following the repo's source-contract pattern (readFileSync + new URL(..., import.meta.url), see frontend/src/lib/toolCardPaths.test.ts): slice the `function ensureListenerStarted` body out of ./useAgentEvents.ts (bound at the next `\nfunction `), then assert (1) it registers via `onAgentEvent(` and chains `.then(` and `.catch(`; (2) the .catch body reports via `console.error` AND `uiDiag` with the `FATAL` marker; (3) the .then body reports via `uiDiag`. Rationale (quality review Q3): a void-discarded registration rejection leaves the window with NO agent-event listener — the exact "app works but the UI never updates" blackout (live 2026-09-08); the registration only runs inside a real webview, so the contract is asserted against the source text.
- [x] 5. **Q4 — cosmetics** — src-tauri/src/ipc/events.rs:1478: collapse the ~14-space run in the cleanup_tests assertion message ("it is the              agent" → "it is the agent"). frontend/src/hooks/useAgentEvents.ts:141-144 and :259: rename AGENT_EVENT_LABEL → AGENT_EVENT_CHANNEL (matching tauri.ts:32 for cross-side diagnostic comparison) and adjust its doc comment ("Channel reported to the backend...").
- [x] 6. **Run the full test matrix** — `cargo test --workspace` (both crates) and `npm test` in frontend/ (vitest run). Run commands unpiped and check $LASTEXITCODE (a piped non-zero code is not evidence of failure — rely on the test result lines). Fix any failure before continuing. The build must stay warning-free: #![deny(warnings)] at both crate roots means a green cargo test already proves zero warnings — never add #[allow].
- [x] 7. **Verify the event chain + close f20bff16** — Read the tail of .coding/logs/emit-diag.log and confirm the current process shows the full chain: forwarder RUNNING → emit result=ok → probe eval ok → listener registered → first event DELIVERED. Documented decision: a literal fresh-app-restart test is impossible from inside the running app (a restart kills this agent); the running app IS built from this delta, so the live chain plus the green matrix on the fixed delta satisfy the acceptance — the user's next restart runs the committed code. Then backlog_status: mark item f20bff16 done with a note (verified via the emit-diag chain + full test matrix; delta landed by this item).
- [x] 8. **Review — spawn the reviewer subagent** — spawn_agent role:"reviewer" (NO model param — the configured reviewing model is authoritative) with a self-contained task: the delta landed as commit a8546f2 plus this item's Q1-Q4 changes (git diff a8546f2..HEAD + uncommitted), the plan goal, and asks for correctness, bugs, security, and constitution checks (docs sync: module doc comments match the new behavior; multi-platform neutrality: the Q1 Release/Acquire reasoning is ARM64-relevant, no Windows-only assumptions in the sink resolution). The report must open "## Verdict: PASS" or "## Verdict: FINDINGS (n high, n low)". END the turn after spawning — the finish notification resumes this plan.
- [x] 9. **Fix findings + re-run tests** — Read the review report; fix EVERY finding (the only permitted skip is a factually wrong finding, with written justification). Re-run the full test matrix after fixes.
- [x] 10. **Commit the Q1-Q4 fixes + finish** — Stage exactly this item's changes via explicit git add (NEVER git add -A — keep debris out): src/runtime/channels.rs, src-tauri/src/ipc/events.rs, frontend/src/hooks/useAgentEvents.ts, frontend/src/hooks/useAgentEvents.registration.test.ts, .coding/backlog.jsonl, this plan's .coding/plans/660d60af.md — plus anything else the fixes touched (verify with git status; the delta itself is already committed as a8546f2). Commit on wt/agenticcoding (never main) with a message naming the Q1-Q4 quality fixes. Then finish with the review report path.
