# Plan: Reviewer prompts must be clean — skip the recalled-context rider for reviewer spawns (backlog 1d0332ca)

## Goal
Reviewer prompts must be clean: skip the RECALLED CONTEXT rider for role:"reviewer" spawns — the reviewer task passes through to the spawner byte-identical to the task argument even when the memory store has matching hits — while unrestricted sub-agents keep the rider (existing behavior preserved). The skip is role-based only (documented decision in the rider's comment).

## Kind
bug_fixing

## Context
Root cause (verified): the RECALLED CONTEXT rider block in src/tool/agent/spawn_agent.rs execute() (~:308-326) calls crate::tool::steering::recalled_context_block(self.memory.as_ref(), None, &args.task, None) and prepends the rider via format!("{}{rider}", args.task) for EVERY spawn with no role check. The role is already resolved and validated earlier in execute (:221-235 — Option<String>, only "reviewer" or None), so the fix is a one-line guard around the rider.

CODE MAP: rider block :308-326; role validation :221-235; reviewer pin (role.as_deref() == Some("reviewer")) :288; module doc :12-15 ("the task is best-effort seeded with a passive RECALLED CONTEXT rider"); struct field doc :102-105; with_memory doc :137-141; the schema description carries NO rider text (verified — no budget impact). recalled_context_block (src/tool/steering.rs:155-162): Option<&Arc<dyn MemoryStoreTrait>>, title, query, bug_query → Option<String>; None on no store/no hits/error; age-gated (14 days), capped, "RECALLED CONTEXT (...)" header.

TEST FIXTURE PATTERN (existing task_seeded_with_recalled_context_rider, :1099-1160): HashEmbedder + MemoryStore::open_in_memory + a seeded Semantic memory with a FRESH now timestamp (the 14-day recency gate drops old seeds) whose title matches the task text; MockSpawner::ok(3); SpawnAgentTool::new(spawner).with_memory(store); calls[0].1 is the task the spawner received.

DESIGN DECISION (to document in the rider's comment): the skip is role-based ONLY, not an exposed spawn option — the reviewer protocol is the one consumer that needs a clean prompt (its task is self-contained by contract), and an opt-out knob would grow the schema for no current consumer; unrestricted sub-agents keep the rider (its original purpose: seed a worker's task with prior knowledge).

VERIFICATION: cargo test (full suite green, warning-free under #![deny(warnings)]); acceptance = the reviewer spawn's task reaches the spawner byte-identical (assert_eq! with the exact task string) even with matching hits, a non-reviewer spawn still carries the rider, and the existing rider tests stay green.

## Steps
- [x] 1. **Reproduce with failing regression test** — Write a regression test that reproduces the defect (constitution: every defect gets a regression test that fails without the fix and passes with it). Run it and confirm it FAILS.
- [x] 2. **Document root cause** — Investigate and document the root cause. memory_write a BUG: record (symptom → root cause → fix + regression test name, ≤600 chars).
- [x] 3. **Minimal fix** — Apply the minimal fix that makes the regression test pass. Do not refactor unrelated code.
- [x] 4. **Verify** — Run the regression test + the full test suite (cargo test unpiped, warning-free). Record the regression test name via update_plan (regression_test field) — finish is blocked without it. If the fix turned out FEATURE-SCALE (new pub types or cross-module surface): amend the plan context via update_plan append with a paragraph starting "Landed design —" (architecture, deviations from the sketch, measured numbers, invariants to preserve) and record landed_design=true BEFORE completing this step; at finish write a SPEC memory and amend the BUG record with the landed outcome (agent.md 'Documentation expectations'; the finish gate blocks landed_design without the context amendment).

## Detailed steps
- [x] **Reproduce: regression test fails pre-fix** — In src/tool/agent/spawn_agent.rs tests (after task_seeded_with_recalled_context_rider, ~:1160): add `reviewer_task_skips_the_recalled_context_rider` — same fixture (HashEmbedder + MemoryStore::open_in_memory + a fresh-timestamp Semantic seed titled to match the task text, e.g. "SPEC: the kettle safety valve opens above 2 bar" + task "work on the kettle safety valve"; MockSpawner::ok(3); SpawnAgentTool::new(spawner).with_memory(store)). Two spawns on the same store: (1) role:"reviewer" → assert the spawner-received task is BYTE-IDENTICAL (assert_eq! with the exact task string — no "RECALLED CONTEXT", no matched-hit text); (2) a normal spawn (no role) → assert its task carries the rider (contains "RECALLED CONTEXT" and the seed title). Run it and CONFIRM IT FAILS pre-fix (the reviewer spawn's task currently carries the rider).
- [ ] **Root cause: rider runs for every spawn, no role check** — Root cause (verified during planning, record in the plan): the rider block (src/tool/agent/spawn_agent.rs ~:308-326) computes and prepends recalled_context_block for EVERY spawn with no role check; the role is already resolved and validated earlier in execute (:221-235, Option<String>, only "reviewer" or None) — the fix is a guard around the rider.
- [x] **Fix: role check skips the rider for reviewer spawns** — In src/tool/agent/spawn_agent.rs execute(): wrap the rider in the reviewer skip — `let task = if role.as_deref() == Some("reviewer") { args.task } else { match crate::tool::steering::recalled_context_block(...) ... }` (both arms move args.task; the reviewer arm passes through byte-identical, skipping the store round-trip entirely). Extend the rider block's comment with the decision: reviewer spawns are exempt (backlog 1d0332ca) — the reviewer protocol hands the spawned reviewer a self-contained task, and the rider would pollute it with parent-project memories (noise at best, review bias at worst) plus prompt bloat; the skip is role-based ONLY, not a spawn option (the reviewer protocol is the one consumer needing a clean prompt; an opt-out knob would grow the schema for no current consumer; unrestricted sub-agents keep the rider — its original purpose). Sync the doc comments: module doc (:12-15), struct field doc (:102-105), with_memory doc (:137-141) — each gains the reviewer exemption.
- [ ] **Verify: full suite green + warning-free** — cargo test — full suite green, warning-free (#![deny(warnings)]). The new regression test passes (reviewer task byte-identical; normal spawn still carries the rider); the existing rider tests (task_seeded_with_recalled_context_rider, task_unchanged_without_store_or_hits) and the reviewer-pin tests stay green. No schema change (verified: the spawn_agent schema description carries no rider text — no budget impact).

## Bug
Every spawned reviewer's first prompt (the task) arrives with an embedded "recalled context" block — memories semantically matched to the task text — polluting the self-contained reviewer protocol task with parent-project memories (noise at best, review bias at worst: the reviewer should judge the diff/plan, not be steered by recalled context) plus needless prompt bloat on every review. Reproduce: spawn any role:"reviewer" agent whose task text matches existing memories (e.g. mentions "review", "diff", a file path, or a plan topic) with the memory store populated — the spawned agent's first prompt carries the recalled-context rider; visible in the reviewer's transcript/task echo (observed on the reviewers spawned during plan 995436c2's closing sequence, user report 2026-09-19).
