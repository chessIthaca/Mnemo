# Plan: Install @tailwindcss/typography so markdown prose classes render

## Goal
Install and register the @tailwindcss/typography plugin so the `prose` classes already used across all 5 markdown surfaces (chat, file view, edit preview, code preview, plan progress) generate CSS. This restores heading sizes/weights/margins (currently stripped by Tailwind Preflight with nothing replacing them) and makes tables/lists/links/blockquotes render fully.

## Kind
implementation

## Context


## Steps
- [x] 1. **Install @tailwindcss/typography (MIT) into frontend deps and register it in tailwind.config.ts plugins array (currently empty []). The `prose prose-invert prose-sm` classes + modifiers (prose-h1:*, prose-pre:*) are already in the JSX across Message.tsx:214, SourceEditor.tsx:513/519/525, PlanProgress.tsx:229 — they just have no CSS behind them. Pin a version compatible with tailwindcss ^3.4.15 (use @tailwindcss/typography ^0.5.15). Run: cd frontend; npm install --save-dev @tailwindcss/typography@^0.5.15. Then edit frontend/tailwind.config.ts: change `plugins: []` to import the plugin and register it: `import typography from '@tailwindcss/typography';` at top, and `plugins: [typography],` in the config object. Preserve the existing license header comment and all other config (content, darkMode, theme.extend). Do NOT touch the hand-written .prose rules in globals.css — they layer on top of the plugin and are still needed (e.g. .prose { font-size: inherit } overrides prose-sm's 0.875rem so the user's app font size applies). Verify the plugin is registered by confirming the generated CSS includes .prose rules (npm run build succeeds and the bundle contains prose heading styles). Run: cd frontend; npm run build. Read the build output for success (tsc + vite build, exit 0). If build fails, fix the error before proceeding. Do NOT add @tailwindcss/typography to the About dialog's dependencies list (frontend/src/components/about/dependencies.ts) — that file lists runtime deps shown to users; this is a build-time devDependency. Check: is @tailwindcss/typography already referenced anywhere that would conflict? (search showed only PLAN.md mentions it in passing). Confirm package-lock.json updated with the new entry. Capture: the exact version installed, and confirm tailwind.config.ts plugins array now contains the typography plugin.**
- [x] 2. **Run frontend tests + build to confirm nothing regressed. Run: cd frontend; npm test (vitest run) — expect all existing tests green (the markdownRendering.test.ts contract test, language.test.ts, etc.). Then: cd frontend; npm run build — expect tsc + vite build to succeed with exit 0. The typography plugin is pure CSS generation; no runtime behavior change, so tests should pass unchanged. If any test fails, investigate — it may indicate the test was asserting against the absence of prose styles (unlikely). Read $LASTEXITCODE unpiped per the Windows rule (do not trust piped exit codes). Capture: test count (passed/failed) and build success/failure.**
- [x] 3. **Spawn a read-only reviewer (spawn_agent role:'reviewer') with a self-contained task: review ALL uncommitted changes (git diff HEAD) on branch wt/fix-md-typography. Goal: install + register @tailwindcss/typography so markdown headings render. Focus: (a) the plugin is in devDependencies (not dependencies) in package.json and the version is compatible with tailwindcss ^3.4.15; (b) tailwind.config.ts plugins array now contains the typography plugin and the license header + all other config is preserved; (c) no JSX changes were needed (the prose classes were already there) — confirm no unintended edits to the .tsx files; (d) the hand-written .prose rules in globals.css are untouched (they layer on top of the plugin); (e) build is warning-free and tests pass; (f) multi-platform neutrality — this is pure CSS, no platform-specific code; (g) documentation sync — check whether README.md or PLAN.md need a note about the typography dependency (PLAN.md:127 lists the markdown stack as react-markdown + remark-gfm + rehype-highlight; the typography plugin is a Tailwind concern, not part of the markdown parsing stack — likely no doc change needed, but the reviewer decides). It writes a report to .coding/reviews/. Do NOT poll — end turn after spawning; when notified, read the report, fix EVERY finding, re-run npm test + npm run build, commit all (including the review report) to branch wt/fix-md-typography (NEVER main), then finish with the report path.**
