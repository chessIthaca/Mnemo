# Plan: Verify merged main: build + full test suite green

## Goal
Verify merged main is fully green: Rust workspace builds warning-free and all tests pass; frontend typechecks, builds, and tests pass; fix anything broken in this same pass.

## Kind
implementation

## Context
Just merged fix/local-system-message-sanitizer into local main (origin push declined by user). Workspace = root crate (myharness) + src-tauri member. Frontend in frontend/ with npm scripts: build = "tsc && vite build", test = "vitest run". Build is warning-free by construction (#![deny(warnings)] at both crate roots) — a green cargo test proves zero warnings. Windows/PowerShell environment: run commands unpiped and read $LASTEXITCODE, don't trust piped exit codes.

## Steps
- [x] 1. **Run cargo test on workspace** — From repo root run `cargo test` (workspace: myharness + src-tauri) UNPIPED, then `"exit=$LASTEXITCODE"`. Green = builds warning-free (deny(warnings) at both crate roots). If failures: note them for step 3.
- [x] 2. **Run frontend checks** — In frontend/: `npm test` (vitest run) and `npm run build` (tsc && vite build), each unpiped, checking $LASTEXITCODE. If node_modules missing, `npm install` first. Note any failures for step 3.
- [x] 3. **Fix any failures** — For each failure from steps 1-2: read the failing code, fix the root cause (no #[allow]) following existing style, doc-comment new public fns, re-run the failing command until green. If everything already passed, skip.
- [x] 4. **Close out: review + commit** — Run cargo test once more (and npm test if frontend changed) to confirm green. Spawn reviewer subagent on all uncommitted changes (git status / git diff HEAD), fix every finding, re-run tests, commit to current branch, include review report.
