# Plan: Implement prompt caching optimizations: Anthropic ephemeral cache_control, split system blocks, and state-stable tool anchoring

## Goal
Optimize prompt cache hit rates across providers by (1) splitting system prompt blocks in anthropic.rs into structured blocks and applying cache_control: {"type": "ephemeral"} to the stable head; (2) attaching cache_control: {"type": "ephemeral"} to the last tool definition in Anthropic; (3) attaching cache_control: {"type": "ephemeral"} to the turn boundary in multi-turn conversation history; (4) ordering tool schemas so always-visible base tools precede state-dependent workflow tools, ensuring state transitions only append to the tools array without invalidating the common prefix.

## Kind
implementation

## Context
Picking up backlog item #8814f81c ("Optimize prompt caching across providers"). Output token quantization was completed in the previous plan. This plan implements the remaining caching architecture: Anthropic prompt caching (cache_control breakpoints on stable system head, tool array, and multi-turn message history) and tool schema 3-tier prefix anchoring across workflow state transitions.

## Steps
- [x] 1. **Split system prompt into structured content blocks with cache_control in Anthropic**
- [x] 2. **Attach cache_control to tool definitions and conversation history in Anthropic**
- [x] 3. **Anchor tool schemas with 3-tier priority ordering for prefix stability**
- [x] 4. **Verification, review, and closing sequence**
