# Plan: R12-R17: Cache-hit + context optimization batch

## Goal
Implement the 5 cache-hit + context optimization recommendations (R12-R15, R17) from the round-4 analysis: fix the cache-hit heuristic that masks real misses, lower the summarization threshold, truncate consumed tool results, reduce read_files default line cap, and add a sane max_output_tokens ceiling.

## Kind
implementation

## Context
Round-4 analysis (.coding/analysis/cache-hit-4-report.md) found: R1/R5 prompt structure is working (head byte-identical, footer byte-stable), but (1) the cache-hit heuristic in turn.rs:1155-1174 records min(prev,curr) when provider reports cached=0, inflating the 95.7% to mask real misses; (2) summarization never triggered (fill_rate=0.5, max_context=1M → threshold 500K, context only reached 370K); (3) 245 tool messages in a 486-message conversation, read_files returns 2000-line slices (~16K tokens each); (4) max_completion_tokens=131072 sent because R9 cap doesn't kick in with 1M context window. All R1-R11 fixes are merged and confirmed in source. Windows PowerShell; cargo test unpiped; zero warnings under deny(warnings).

## Steps
- [x] 1. **R12: Fix cache-hit heuristic — report 0 when provider says 0** — SIMPLER FIX (not Option<u32>): remove the min(prev,curr) heuristic entirely. (1) In src/agent/turn.rs:1155-1174: replace the 3-branch `effective_cached` with a simple `let effective_cached = cached_tokens;` — when the provider reports 0, report 0. Remove the `did_summarize` branch and the `else` heuristic branch. Update the comment at :1140-1146 to explain: the provider's reported cached_tokens is trusted as-is; 0 means not cached (the old min(prev,curr) heuristic masked real cache misses like the litellm proxy's ~340K cache limit). (2) Remove the last_prompt_tokens write at turn.rs:1175-1182 (it was only used by the heuristic). (3) Search for `last_prompt_tokens` (graph_search or search) to find the session struct field definition — remove the field + its initialization. Under deny(warnings) a write-only field is dead code. (4) Check src/agent/tests.rs for any test asserting the heuristic behavior (search for `last_prompt_tokens` or `min(prev` or `heuristic`) — remove or update. (5) Run `cargo test` unpiped from repo root; read test-result lines + $LASTEXITCODE. The frontend AgentEvent::Usage already carries cached_tokens as a number — 0 is a valid value, no frontend change needed.
- [x] 2. **R13: Lower summarization fill_rate default 0.5 → 0.3** — (1) In src/config/general.rs:272: change `summarize_at_fill_rate: 0.5` to `0.3`. With max_context=1M this sets summarize_at=300K (under the litellm proxy's ~340K cache-reset cliff); with a 200K context window it's 60K (reasonable). (2) Update the test at general.rs:644 that asserts the default is 0.5 — change to 0.3. (3) Update the doc comment at src/agent/context.rs:68 (fill_rate example 0.5 → 0.3). (4) Search for any other test or doc referencing 0.5 as the fill_rate default and update. (5) Run `cargo test` unpiped.
- [x] 3. **R14: Truncate consumed tool results — compact_old_tool_results** — (1) Add a function in src/agent/context.rs: `pub fn compact_old_tool_results(messages: &mut [Message], keep: usize, summary_chars: usize)` with a doc comment: 'Truncate old tool-result messages to a compact summary, keeping only the most recent `keep` tool results intact. Tool results the model has already acted on are replaced with their first `summary_chars` chars plus a truncation marker. Preserves tool_call_id and name so the conversation structure stays valid. Idempotent: messages already containing the marker are skipped.' Implementation: collect indices of Role::Tool messages; keep the last `keep` intact; for older ones, if content char-count > summary_chars + 100 AND content does not already end with the marker `[... truncated for context efficiency]`, replace with `first summary_chars chars + '…\n[... truncated for context efficiency]'`. Use chars().take() for UTF-8 safety. (2) In src/agent/turn.rs: after `provider.record_tools_phase_ms(...)` at line ~1929 (after the tool batch for-loop, before the stop_reason check at :1936), add: `crate::agent::context::compact_old_tool_results(messages, 10, 500);` then `token_accounting.reset();` (messages changed — force a full recount on the next iteration). Add a comment explaining: tool results the model has already acted on are truncated to a 500-char summary, keeping only the 10 most recent intact; this keeps context under the provider's cache limit without waiting for the summarization threshold. (3) Tests in context.rs: (a) old tool results beyond `keep` are truncated to summary_chars + marker; (b) recent `keep` tool results are intact; (c) idempotent — calling twice produces the same result; (d) short tool results (< summary_chars + 100) are not truncated; (e) tool_call_id and name are preserved. Run `cargo test` unpiped.
- [x] 4. **R15: Reduce read_files + file_read default max_lines 2000 → 500** — (1) In src/tool/agent/read_files.rs:30: change `pub(crate) const DEFAULT_MAX_LINES: usize = 2000;` to `500`. Update the doc comment at :28-29. (2) Search src/tool/agent/file_read.rs for the same constant (the read_files comment says 'Mirrors file_read') — if it has its own DEFAULT_MAX_LINES, change it to 500 too. (3) Search for any test asserting the default is 2000 (search `2000` in read_files.rs + file_read.rs test modules) — update to 500. (4) Run `cargo test` unpiped.
- [x] 5. **R17: Add sane max_output_tokens ceiling (32K absolute cap)** — (1) In src/provider/openai.rs near line 1315: add `const SANE_MAX_OUTPUT_TOKENS: usize = 32_000;` with a doc comment: 'Absolute ceiling on output tokens regardless of endpoint config. Most coding tasks need 2-8K; 32K is generous. Prevents wasteful 128K output budgets on large-context endpoints where the R9 context-window cap never triggers.' (2) In the R9 formula at :1320-1325, add `.min(SANE_MAX_OUTPUT_TOKENS)` before the final `.max(1024)`: `let max_completion = self.caps.max_output_tokens.max(4096).min(self.caps.max_context.saturating_sub(prompt_est).saturating_sub(1024)).min(SANE_MAX_OUTPUT_TOKENS).max(1024);`. (3) Mirror in src/provider/anthropic.rs: find its max_tokens line (search for `max_tokens` or `max_output_tokens` in anthropic.rs build_request_json), add the same constant + cap. (4) Tests in openai.rs: add `max_completion_tokens_capped_at_sane_ceiling` — a config with max_output_tokens=131072 and max_context=1_000_000 (large context so the R9 window cap doesn't trigger) → assert body max_completion_tokens == 32_000 (the sane ceiling kicks in). Update the existing `max_completion_tokens_uncapped_when_prompt_small` test if it uses max_output_tokens > 32K — it should now expect 32_000 not the raw config value. Run `cargo test` unpiped.
- [x] 6. **Verify + review + commit** — (1) Run `cargo test` unpiped from repo root — confirm all test-result lines green + zero warnings (deny(warnings) enforces it). (2) `git status --short` — confirm only src/ files changed (no .coding/ except bookkeeping). (3) Spawn a read-only reviewer subagent (role:reviewer) over ALL uncommitted changes with a self-contained task: plan goal (5 cache-hit + context optimizations: R12 fix heuristic, R13 lower fill_rate, R14 truncate tool results, R15 reduce read_files default, R17 sane output cap), changed files, focus points: R12 — heuristic removed, last_prompt_tokens field removed (no dead code), 0 is reported when provider says 0; R13 — fill_rate default 0.3, tests updated; R14 — compact_old_tool_results is idempotent, preserves tool_call_id/name, only truncates old results (keeps 10 recent), token_accounting reset after compaction; R15 — default 500 in both read_files + file_read; R17 — sane cap 32K applied to both openai + anthropic, doesn't break existing cap tests, .max(1024) floor preserved. Ask for correctness, bugs, security, constitution findings (warning-free, multi-platform neutral, doc comments on public items). Have it write the report to .coding/reviews/. END TURN after spawning. (4) Read the report, fix EVERY finding, re-run cargo test. (5) Commit to wt/agenticcoding with a clear message + include the review report.
