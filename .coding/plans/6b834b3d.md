# Plan: Verify LlmTraceView version-skip already landed (done-orphan re-dispatch)

## Goal
The dispatched backlog item (Skip LlmTraceView re-fetch when version unchanged) describes work already shipped: plan 0a54dcd5, commit ccb568d, round-2 review PASS, SPEC record written. This plan verifies every acceptance criterion in the CURRENT tree (Rust version field + both regression tests, frontend shouldSkipRefetch + vitest suite registered, commit/reviews/plan in history), records the re-verification in the SPEC knowledge record, and closes the item as a done-orphan with zero source changes. If any verification check fails, the plan is abandoned and reclassified as an implementation plan for the actual gap.

## Kind
research

## Context
Done-orphan re-dispatch (third this session): the item's work already shipped — plan 0a54dcd5, commit ccb568d "perf(trace): skip LlmTraceView detail re-fetch when version unchanged (L5)" (10 files, +308/−3), round-1 review 1-LOW-fixed + round-2 PASS (.coding/reviews/2026-09-10-llmtrace-version-skip-review.md, -round2-review.md), SPEC record .coding/knowledge/spec/2027-01-07-trace-record-mutation-version-llmtraceview-detai.md. Verified present in the current tree: shouldSkipRefetch (frontend/src/components/views/LlmTraceView.tsx:141, wired :810), regression test version_bumps_on_every_mutation_and_stays_stable_when_idle (src/provider/trace.rs:1606), LlmTraceView.versionSkip.test.ts registered in frontend/vitest.config.ts. This plan re-verifies acceptance in the current tree (cargo test version_bumps; cd frontend; npm test) and amends the SPEC record; zero source changes. If any check fails → abandon and reclassify as an implementation plan for the gap.

## Steps
- [x] 1. **Verify the Rust trace-version side** — read src/provider/trace.rs: confirm the `version` field (u64, starts 0) is bumped in with_record and enforce_raw_budget, and carried by both LlmRequestDetail and LlmRequestSummary; run `cargo test version_bumps` (matches both regression tests version_bumps_on_every_mutation_and_stays_stable_when_idle at :1606 and version_bumps_on_payload_eviction) and confirm both pass.
- [x] 2. **Verify the frontend skip side** — read frontend/src/components/views/LlmTraceView.tsx (shouldSkipRefetch at :141, wired into the detail + compare polls at :810), frontend/src/components/views/LlmTraceView.versionSkip.test.ts, and its registration in frontend/vitest.config.ts; run the frontend suite (`cd frontend; npm test`) and confirm green.
- [x] 3. **Verify history + bookkeeping** — confirm commit ccb568d ("perf(trace): skip LlmTraceView detail re-fetch when version unchanged (L5)") is in the current branch history with both review reports (.coding/reviews/2026-09-10-llmtrace-version-skip-review.md and .coding/reviews/2026-09-10-llmtrace-version-skip-round2-review.md) and the completed plan .coding/plans/0a54dcd5.md. If ANY check in steps 1-3 fails, stop and reclassify as an implementation plan for the actual gap; otherwise this step is verification-only (no-code research).
- [x] 4. **Record the re-verification** — memory_amend the SPEC record (id be5a62f5-dd31-5d15-b9d9-44e6ac25494e, file .coding/knowledge/spec/2027-01-07-trace-record-mutation-version-llmtraceview-detai.md) with a dated paragraph: the 2027-01-12 run-all re-dispatch of this item verified the work landed (commit ccb568d in the current tree, cargo + vitest suites green) — done-orphan closed with no code changes.
