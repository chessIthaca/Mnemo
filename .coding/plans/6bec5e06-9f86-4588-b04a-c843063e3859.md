# Plan: Align agent tab bar with tool tab style

## Goal
Restyle the MainPanel agent tab bar to match the RightPanel tool tab bar's flat rail style (fixed h-10 triggers, cyan 2px bottom border on the active tab, no raised pill), so the "model tab" and the "tool tabs" are aligned in height and style. Keep the StatusBar toolbar change from the previous session untouched.

## Kind
implementation

## Context
User's earlier request was to align the model/agent tab (MainPanel, top of the main column) with the tool tabs (RightPanel rail) in height and style. A previous session changed the StatusBar toolbar instead — the user likes that change and wants it KEPT, but the tab alignment itself is still the open "last one".

Current code (confirmed from live DOM + source):
- MainPanel.tsx line 90: TabsList `flex items-center gap-1 overflow-x-auto border-b border-border bg-bg-secondary px-2 py-1`; line 105: TabsTrigger `group flex items-center gap-2 rounded-t-md px-3 py-1.5 text-sm transition-colors`, active = `bg-bg-primary text-slate-200`, inactive = `text-slate-400 hover:bg-bg-tertiary hover:text-slate-200`. This renders as a raised pill tab (the dark rounded box in the user's screenshot).
- RightPanel.tsx lines 50-95: TabsList `flex` inside an `h-10`-row container; TabsTrigger `group flex h-10 items-center gap-1.5 border-b-2 px-3 text-xs font-medium transition-colors`, active = `border-cyan-500 text-cyan-400`, inactive = `border-transparent text-slate-500 hover:text-slate-300`; plus a hover-revealed close-x. This is the flat rail style ("Plan" tab in the screenshot).
- Sidebar tool toggles (lines 55-68) already use the same `border-b-2 border-cyan-500` motif as the right-panel rail.
- The StatusBar (bottom toolbar) was restyled in the previous session — DO NOT TOUCH it.

Target style for agent tabs: match the rail — `flex h-10 items-center gap-2 border-b-2 px-3 text-xs font-medium`, active `border-cyan-500 text-cyan-400`, inactive `border-transparent text-slate-500 hover:text-slate-300`; keep the status dot, two-line name+model layout, approval badge, and subagent close-x semantics unchanged. The TabsList should keep its scrollable row but drop the pill-specific padding/background so the rail sits flush on the border-b.

Testing pattern: this project's vitest runs in `node` env (no jsdom), so component styling is asserted via static source-contract tests using `?raw` imports (see frontend/src/components/chat/InflightBar.test.ts and src/lib/ipc-contract.test.ts). Create frontend/src/components/layout/MainPanel.tabStyle.test.ts asserting: (1) active tab carries border-cyan-500 text-cyan-400, (2) inactive carries border-transparent, (3) the trigger is h-10 and text-xs (height/style parity with RightPanel rail), (4) no leftover rounded-t-md raised-pill styling, (5) the model second line + close-x + approval affordances are preserved. Also assert RightPanel keeps its rail classes so drift between the two is caught.

Docs: README.md describes features; the agent tab bar is not documented there (no per-feature bullet about the tab bar found — searched "tab" in README: only tool/output tabs mentioned). PLAN.md's layout section (line 435) says "Tab bar shows name + status (● running / idle / ! error)" — the model second-line and the rail style are UI details; a one-line sync of PLAN.md's main-panel row to mention "name + model + status" keeps docs honest. Verify nothing in README/PLAN claims pill tabs.

## Steps
- [x] 1. **Restyle agent tabs to rail style in MainPanel.tsx** — Edit frontend/src/components/layout/MainPanel.tsx. TabsList (line ~90): change `flex items-center gap-1 overflow-x-auto border-b border-border bg-bg-secondary px-2 py-1` → `flex items-stretch overflow-x-auto border-b border-border` (drop the pill padding/background, keep the rail's bottom border). TabsTrigger (line ~105): replace `rounded-t-md px-3 py-1.5` with `h-10 px-3`, change active classes `bg-bg-primary text-slate-200` → `border-b-2 border-cyan-500 text-cyan-400`, inactive `text-slate-400 hover:bg-bg-tertiary hover:text-slate-200` → `border-b-2 border-transparent text-slate-500 hover:text-slate-300`. Keep `text-sm` for the agent name (the tab is two-line: name + model at text-[0.625em]; text-sm keeps the name primary and legible — the h-10 rail height is the alignment driver, not the font size). Keep gap-2, the status dot, the two-line name+model block, the approve badge, and the close-x unchanged. Do NOT touch StatusBar.tsx.
- [x] 2. **Add tab-style contract test** — Create frontend/src/components/layout/MainPanel.tabStyle.test.ts modeled on InflightBar.test.ts (import source via `import source from "./MainPanel.tsx?raw";`). Assert: (1) active-trigger classes contain `border-cyan-500 text-cyan-400`; (2) inactive classes contain `border-transparent` and the trigger no longer carries `rounded-t-md` or `bg-bg-primary`; (3) trigger uses `h-10` (parity with RightPanel's h-10 rail) and `text-sm` is retained for agent-name legibility; (4) the model second line `text-[0.625em]`, the running-dot `agent-running-dot`, and the close-x button are still present (regression guard: the restyle must not drop the model line or close affordances); (5) drift guard: raw-import RightPanel.tsx and assert it still has `h-10` + `border-b-2` (the rail reference stays stable). Add `"src/components/layout/MainPanel.tabStyle.test.ts"` to the include list in frontend/vitest.config.ts.
- [x] 3. **Run frontend tests + build** — Run `npm test` in frontend/ (vitest) and confirm the new test file passes and nothing else broke. Then `npm run build` (tsc + vite) to prove the `?raw` import type-checks (vite/client types) and the component changes compile. PowerShell: run commands unpiped and read $LASTEXITCODE (or rely on the command's own test result lines — never trust a piped exit code).
- [x] 4. **Docs sync (PLAN.md layout row)** — PLAN.md line ~435 main-panel row currently ends: 'Tab bar shows name + status (● running / idle / error).' Update that sentence to mention the model line + rail styling: 'Tab bar shows name + model + status (● running / ○ idle / ! error) in the same flat-rail style as the right-panel tools.' Check README.md for any stale pill-tab description (searched: none found — only tool-tab mentions; leave README alone unless a stale claim is found).
