# Plan: InflightBar reasoning panel collapsed by default

## Goal
Change the InflightBar reasoning/activity panel to start collapsed; user opens it by clicking the chevron (or drag handle, which already expands).

## Context
frontend/src/components/chat/InflightBar.tsx line 31-32 initializes `expanded` to `true` with comment 'Expanded by default — the reasoning is the useful part to watch.' Toggling already works via the chevron button (line 117) and drag handle (lines 91-92). Only the initial state and comment need to change.

## Steps
- [x] 1. Flip the initial `expanded` state in frontend/src/components/chat/InflightBar.tsx from `useState(true)` to `useState(false)` and update the adjacent comment to say it's collapsed by default.
- [x] 2. Run cargo test to confirm no regressions (frontend change; Rust tests should pass unchanged).
