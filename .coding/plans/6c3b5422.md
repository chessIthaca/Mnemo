# Plan: Consolidate endpoint/model fallback onto resolve_startup_provider (e73a290f)

## Goal
Route the two remaining mirror copies of the startup provider fallback chain — console.rs::initial_selection and the memory_cleanup provider block in ipc/memory_maintenance.rs — through src-tauri/src/startup.rs::resolve_startup_provider, keeping their test suites green and updating both self-documenting comments to point at the single source.

## Kind
implementation

## Context
Review follow-up to startup-extraction LOW 2 (plan 531fc221, report .coding/reviews/2027-01-07-startup-extraction-low4-review.md). Semantics are provably equivalent across all three copies: Config::default_endpoint() (src/config/mod.rs:121-128) itself resolves the named default_provider first, so console's `default_endpoint()?` ≡ the helper's named→default_endpoint chain; memory_maintenance is a literal copy of the helper. settings.rs::resync_runtime_state (line 256) already delegates to the helper — the precedent for call shape and comment wording. The helper is pub(crate) in src-tauri/src/startup.rs (mod startup; at main.rs:34), reachable as crate::startup::resolve_startup_provider from both console.rs and ipc/memory_maintenance.rs. Existing tests pin the semantics: console.rs initial_selection_* (lines 2146-2227, 5 tests) and startup.rs provider_* (5 tests). No other copies exist (verified: only 3 default_endpoint() chain sites in src-tauri; root-crate gpt-4o hits are all test fixtures).

## Steps
- [x] 1. **Route console.rs initial_selection through the helper** — In src-tauri/src/console.rs, replace initial_selection's inlined chain (lines 527-544: `let ep = config.default_endpoint()?;` + the default_model→first-model→gpt-4o chain) with `let (ep, model) = crate::startup::resolve_startup_provider(config); let ep = ep?;` — keep the ModelSelection construction (effort = ep.effective_reasoning_effort_for(Some(&model)), supports, requested_effort: None) unchanged. Rewrite the doc comment (lines 519-526) to say the endpoint/model fallback chain delegates to crate::startup::resolve_startup_provider (the same tested helper build_brain uses), so the console's initial selection matches the app-startup provider by construction; keep the `None` ONLY-when-no-endpoint note.
- [x] 2. **Route memory_maintenance.rs cleanup provider through the helper** — In src-tauri/src/ipc/memory_maintenance.rs memory_cleanup, replace the inlined provider-resolution block (lines 252-278: named default_provider → config.endpoint(name) → config.default_endpoint(), plus the model chain inside endpoint.map) with `let (endpoint, model) = crate::startup::resolve_startup_provider(&config);` followed by the unchanged `endpoint.map(|ep| build_client(&config, ep, &model, ep.multimodal_for(&model), ep.effective_reasoning_effort_for(Some(&model)), Some(state.trace.clone())))`. Update the comment (lines 249-251) to point at crate::startup::resolve_startup_provider as the single source, keeping the 'minus the dummy fallback: no endpoint → synthetic-only consolidation' note.
- [x] 3. **Verify both Rust suites green** — Run `cargo test` in src-tauri and at the repo root (PowerShell; trust the echoed exit=N, not the trailing [exit code]). Both suites must be green and warning-free (deny(warnings) at both crate roots). The 5 initial_selection_* console tests and the 5 provider_* helper tests pin the fallback semantics through the new delegation; no new tests needed — behavior-neutral refactor with existing suites as the pin.
