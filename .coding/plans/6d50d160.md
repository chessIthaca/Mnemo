# Plan: Approval-UI warning badge for protected-path commands

## Goal
Make the documented standing residual visible at decision time: an approved shell command can write the protected dirs (.coding/reviews/, .coding/plans/) and .git/ because shell bypasses the file-tool sandbox. Add an advisory warning badge to the approval dialog when a shell command's text mentions .coding/, safety.toml, or .git/ (static, case-insensitive string scan), with a vitest suite on the badge logic registered in frontend/vitest.config.ts. Advisory only — no approval gating change, no Rust changes.

## Kind
implementation

## Context
Security review 2026-09-09 LOW-2 (.coding/reviews/2026-09-09-security-review.md), backlog f4b7b2b5. The residual: the file tools enforce the sandbox (.git/ writes blocked since the HIGH-1 fix; .coding/reviews/ and .coding/plans/ protected from file tools), but shell cannot be sandboxed the same way — an approved shell command can write anything. The approval dialog is frontend/src/components/chat/ApprovalPrompt.tsx (args cast :43-48, header row :212-283, diff block :284+); the payload type is PendingApproval (frontend/src/hooks/agentState.ts:22-37, args: unknown); the shell tool's command field is args.command (src/tool/agent/shell.rs:180-184, required). frontend/vitest.config.ts pins an EXPLICIT include allow-list enforced by the drift guard src/lib/vitestInclude.test.ts (every *.test.{ts,tsx} under src/ must be registered or the suite FAILS) — the new test file MUST be registered there. lucide-react ^0.460.0: ShieldAlert is available (MainPanel.tsx:5, StatusBar.tsx:6). DESIGN DECISIONS: (a) scan scope = shell commands only (the documented residual; scanning file-tool args would false-positive on diff content that merely mentions ".git/"); (b) pure helper in src/lib/protectedPathWarning.ts (unit-testable, house lib-helper pattern); (c) case-insensitive match (Windows paths); (d) the ".git/" token keeps its trailing slash so ".gitignore" does NOT match; (e) the badge lists the matched tokens plus a one-line rationale, as a red-tinted advisory strip below the header row; (f) no Rust changes — the payload already carries args; the badge is computed client-side. Verification: cd frontend; npm test; cargo test (root).

## Steps
- [x] 1. **Add the pure scan helper** — create frontend/src/lib/protectedPathWarning.ts: export const PROTECTED_PATH_TOKENS = [".coding/", "safety.toml", ".git/"] and export function protectedPathTokens(command: string): string[] — case-insensitive (lowercase both sides), returns the matched tokens in PROTECTED_PATH_TOKENS order (deduped by construction), [] when clean. Doc comments: the purpose (security review 2026-09-09 LOW-2 — shell bypasses the file-tool sandbox, so an approved command can write protected dirs; the badge makes the residual visible at decision time), the case-insensitivity rationale (Windows paths), and the ".git/" trailing-slash note (".gitignore" must not match).
- [x] 2. **Wire the advisory badge into the approval dialog** — edit frontend/src/components/chat/ApprovalPrompt.tsx: extend the args cast (:43-48) with `command?: string`; import ShieldAlert from lucide-react and protectedPathTokens from "../../lib/protectedPathWarning"; compute `const protectedTokens = approval.toolName === "shell" && typeof args?.command === "string" ? protectedPathTokens(args.command) : [];` (shell-only — the documented residual; file tools enforce the sandbox); render below the header row (after the buttons div ending :283, before the diff block) when protectedTokens.length > 0: a red-tinted advisory strip (border-red-500/40 bg-red-950/30, ShieldAlert icon, text listing the matched tokens + "shell writes bypass the file-tool sandbox").
- [x] 3. **Add the vitest suite + register it** — create frontend/src/lib/protectedPathWarning.test.ts: unit tests on protectedPathTokens (command containing ".git/" → [".git/"] — the item's prescribed acceptance case; each of the three tokens matches; case-insensitive ".GIT/" and ".Coding/" match; ".gitignore" does NOT match; clean command → []; multiple tokens → canonical order) + a ?raw source-contract on ../components/chat/ApprovalPrompt.tsx pinning the wiring (imports protectedPathTokens, gates on toolName === "shell", renders the badge strip). Register "src/lib/protectedPathWarning.test.ts" in frontend/vitest.config.ts test.include (the vitestInclude.test.ts drift guard fails the suite otherwise).
- [x] 4. **Run both suites green** — verify the touched files' suites: `cd frontend; npm test` must pass frontend/src/lib/protectedPathWarning.test.ts (new), frontend/src/lib/vitestInclude.test.ts (registration guard), and all existing tests; then `cargo test` from the repo root (Rust untouched by this plan — src/tool/agent/shell.rs and the rest of the workspace must stay green; expect 2282+16 passed).
