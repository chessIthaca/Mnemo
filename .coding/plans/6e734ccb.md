# Plan: Trace graphs: split waiting out of send (connect/backoff/stall)

## Goal
Make the per-request trace graph and the inflight timeline attribute time honestly: waiting becomes its own visible state family — connect (renamed from the mislabeled "send"), backoff (new), stall (new) — distinct from actual streaming (reason/generate), with error paths stamping the same windows as success and totalMs summing to true wall time.

## Kind
implementation

## Context
AUDIT (verified against code):
- PhaseKind emission in src/agent/turn.rs: Sending (292, local prep) → Compacting (418→468) → Waiting (900, before complete_with_retry — covers connect+POST+TTFT+retry backoff; correct since plan 710eacd4/merge 4b8b27b) → Reasoning/Streaming (1071-1130, first delta) → RunningTools (1623). GAP: mid-stream stalls keep showing "answering…"/"reasoning…".
- Trace record (src/provider/trace.rs): send_ms = record_created→headers (openai.rs:1183, anthropic.rs:1518) = connect+upload+server-ack+fallback retry — network waiting mislabeled "send". ttft_ms = headers→first chunk (docs wrongly say "POST → first chunk"). generation_ms includes stalls. Retry backoff sleeps (dispatch.rs:755, 1s/2s) attributed to NO record. Error paths stamp set_ttft_ms(record_created.elapsed()) — same window lands in a different bucket than on success. Sliver: body-build+log.start (turn.rs:888 parking → record_created) in no field.
- Frontend: PHASE_SEGMENTS in frontend/src/lib/traceStats.ts (prep/compact/send/wait/reason/generate/tools), PhaseTimes chips in LlmTraceView.tsx:193+1126, totals in TraceStats.tsx:497.

ATTRIBUTION RULES (apply to every stamping arm in both providers):
- connect_ms = record_created→request_start (headers). Errors before headers (transport Err, HTTP error status): record_created→error (includes reject processing — comment why), ttft=None.
- ttft_ms = request_start→first_chunk. Stream timeout with no first chunk: keep ttft=request_start.elapsed() AND add connect=request_start−record_created. Mid-stream errors with a first chunk: ttft=fc−request_start.
- generation_ms = first_chunk→end/error (unchanged). stall_ms = byte-silence gaps >2s inside generation (+ trailing gap on timeout), subset of generation_ms.
- backoff_ms = retry sleeps preceding this attempt (parked on the provider, stamped on the NEXT record — mirrors parked prep).
- prep_ms = turn-loop start→record_created (parked + local build sliver), includes compact_ms.

send_ms blast radius (rename): src/provider/trace.rs (182, 265, 300, 459, 498-516, tests 1618/1735/1906/1938), src/provider/openai.rs (797, 1183, 1215), src/provider/anthropic.rs (1250, 1518, 1549), src/agent/turn.rs:887, frontend: types.ts (490, 536), traceStats.ts, TraceStats.tsx:497, LlmTraceView.tsx (193, 1129), traceStats.test.ts, LlmTraceView.test.ts. Also grep src-tauri + whole repo for stragglers (skip .coding/ historical analysis files).

## Steps
- [x] 1. **Rename send_ms → connect_ms (Rust backend + tests green)**
- [x] 2. **Rename send → connect in the frontend**
- [x] 3. **Attribute retry backoff: backoff_ms field + parking + dispatch measurement**
- [x] 4. **StallTracker + stall_ms field, wired into both stream loops**
- [x] 5. **Re-stamp error paths to match success attribution**
- [x] 6. **Close the prep sliver (body-build + log-start into prep_ms)**
- [x] 7. **Inflight bar: flip to Waiting on mid-stream stalls**
- [x] 8. **Frontend chart: 9 disjoint segments (backoff/connect/stall added)**
- [x] 9. **Docs sync + full test run + SPEC memory**
