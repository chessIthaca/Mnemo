# Plan: Phase 2 — derived-record indexer + bootstrap + Settings Memory section

## Goal
Phase 2 of the semantic memory system: derived-record indexer over .coding/plans/*, .coding/reviews/*, backlog.json + plan-referenced commit pointers; idempotent keys + incremental indexing via a state table; lazy bootstrap on project open; derived-only rebuild; Settings Memory section with record counts by class, last-built timestamp, Rebuild-index button with progress, and scale knobs (decay, per-query cap, per-class cap, digest budgets); graceful degradation on legacy files. Tests throughout; cargo test + frontend npm test green.

## Kind
implementation

## Context
Parent: orchestrator 1c9ba6c8 — ALL work stays on feat/semantic-memory-system (NO branch; create_plan without a branch param). Phase 1 landed (steps verified): record_class/record_type/superseded_by schema + indexes; MemoryClass/MemoryRecordType enums (SPEC:/DECISION:/BUG:/PLAN:/HOW:); store ops update/supersede/delete/list_filtered; class_weight authored>derived; typed budgets (PLAN 400/BUG 600/SPEC 500/DECISION 300 chars, reject-never-truncate); 4 hygiene tools; MEMORY_RECORDS prompt block; stable ids in recall/primer output. GROUNDING for Phase 2 (verified 2026-08-22): (1) maintenance pattern — src/memory/maintenance.rs rebuild_search (brain-side, unit-tested, progress callback) + src-tauri/src/ipc/memory_maintenance.rs streaming memory://maintenance events (started → progress → done/error, op field routes cleanup/rebuild) with busy-flag guard + PROGRESS_EVERY/should_forward_progress; codegraph_rebuild_index in src-tauri/src/ipc/codegraph_cmds.rs is the same shape. (2) Plan files: .coding/plans/<id>.md parsed by crate::workflow::plan_file (PlanFile{title, goal, context, kind, steps}) — REUSE the parser, don't re-parse markdown. Reviews dir = plans_dir.parent()/reviews. backlog.json: {items:[{id,text,status,created_at,note}],next_id}. (3) Project open: switch_project (src-tauri/src/ipc/projects.rs:143) restarts the app; build_brain consumes the marker and opens the project — lazy bootstrap hooks where the memory store opens at startup; create_project:103 shows the best-effort spawn pattern (seed_stores). (4) Frontend: SettingsDialog → sections/MemorySection.tsx (existing Memory & Search section: OpState pattern, progress listener routes by event.op at ~:101, overview refresh at ~:61/:89); invoke wrappers in frontend/src/lib/tauri.ts (maintenanceRebuildSearch :683, codegraphRebuildIndex :780). (5) uuid has only v4 — enable v5 in Cargo.toml for deterministic ids. (6) Recall scoring has hardcoded Ebbinghaus decay + tier_weight/class_weight in src/memory/mod.rs (~:1000s; strongest() doc mentions decayed strength). (7) Config home: mnemo::config (TraceConfig.memory_budget_mb referenced from src-tauri) — locate the module and follow its persistence pattern. (8) Constitution: cargo test unpiped, warning-free (#![deny(warnings)]), no #[allow], doc comments on public fns, regression tests for defects; root cargo test does NOT compile the src-tauri bin crate — verify it with cd src-tauri; cargo build. DESIGN DECISIONS baked in: review digests get a NEW MemoryRecordType::Review (prefix REVIEW:, budget 500); incremental indexing via a derived_index_state table (source_key → content_hash + memory_id) so unchanged sources skip entirely (no embed, no write); deterministic memory id = uuid v5 of the source key (re-runs update in place); rebuild wipes ONLY record_class='derived' rows + the state table (authored sacred); bootstrap is best-effort background, never blocks startup.

## Steps
- [x] 1. **Schema: derived_index_state table + MemoryRecordType::Review** — src/memory/schema.rs: add table derived_index_state(source_key TEXT PRIMARY KEY, content_hash TEXT NOT NULL, memory_id TEXT NOT NULL, indexed_at INTEGER NOT NULL) + index on memory_id via the existing apply_schema migration pattern (CREATE TABLE IF NOT EXISTS suffices for a new table; the ALTER helper is only for retrofitting columns). Tests in schema.rs tests mod: fresh DB has the table + index; apply_schema twice idempotent. src/memory/types.rs: add MemoryRecordType::Review mirroring the Phase 1 pattern (as_str 'review', from_str, from_title 'REVIEW:' prefix, content_budget Some(500), Display, serde lowercase, doc comment) — a review digest is a pointer like a plan digest but review reports run longer, hence 500. Update types.rs tests: roundtrip, from_title('REVIEW: x') → Review, budget 500. Run cargo test unpiped.
- [x] 2. **Store ops: count_by_class / delete_derived / index-state helpers / get_memory** — In src/memory/mod.rs + MemoryStoreTrait (keep object-safe async_trait pattern): (1) count_by_class() -> Result<(usize, usize)> returning (authored, derived) — the dialog's counts; (2) delete_derived() -> Result<usize> — DELETE FROM memories WHERE record_class='derived' (FTS delete trigger syncs; authored rows MUST be untouched); (3) get_memory(id) -> Result<Option<Memory>> — lightweight single-row read for content-compare; (4) index-state helpers: index_state_get(key) -> Result<Option<(content_hash, memory_id)>>, index_state_upsert(key, hash, memory_id), index_state_clear(), index_state_keys() -> Result<Vec<String>> (for removal detection), index_state_last_built() -> Result<Option<i64>> (max indexed_at). Tests: count_by_class distinguishes authored/derived; delete_derived wipes derived only (authored survives + still recalls); index-state helpers roundtrip + clear + last_built. Run cargo test unpiped.
- [x] 3. **Indexer module: plans/reviews/backlog → budgeted derived digests with idempotent incremental keys** — New src/memory/indexer.rs (add pub mod indexer; to src/memory/mod.rs). pub struct IndexReport { indexed, skipped, removed, errors: Vec<String> } with doc comments. pub async fn index_derived(store, plans_dir: &Path, backlog_path: &Path, progress: impl Fn(usize,usize)) -> Result<IndexReport> — mirror maintenance.rs rebuild_search's store param type. Sources: (a) .coding/plans/*.md parsed via crate::workflow::plan_file (reuse the parser) → title 'PLAN: <plan title>', budgeted (≤400 chars) digest: goal first line + steps done/total + 'path .coding/plans/<id>.md' + first commit-hash mention (regex [0-9a-f]{7,40}) as 'commit <hash>' pointer — NOT raw git log; key 'plan:<id>'; (b) reviews dir (plans_dir.parent()/reviews) *.md → title 'REVIEW: <file stem>', ≤500 digest: '## Verdict' line when present + findings count + path; key 'review:<stem>'; (c) backlog.json pending items → title 'PLAN: backlog #<id>', ≤400 digest: first text line + status + 'path .coding/backlog.json'; key 'backlog:<id>'. Deterministic id: enable uuid v5 in Cargo.toml (features = ['v4','v5']), Uuid::new_v5(&Uuid::NAMESPACE_URL, key.as_bytes()) — re-runs address the same row. Incremental: hash assembled digest content with a hand-rolled FNV-1a 64-bit (std DefaultHasher is NOT persist-stable; do NOT add a sha2 dep for this) — index_state_get(key): unchanged → skip (no embed/write); changed/new → store.write or update_memory with record_class=Derived + record_type from title, then index_state_upsert. Removals: index_state_keys() minus keys-seen-this-run → delete_memory + drop state row (report.removed). Graceful degradation: a file that fails structured parsing still indexes (title + path + first 200 body chars); never abort — collect into report.errors and continue. Progress callback every N files (maintenance.rs PROGRESS_EVERY pattern). Tests (tempdir .coding trees): double-run idempotent (run 2 indexes 0, skips all); changed plan re-indexes in place (same id, new content); deleted file removes record + state; legacy plan without ## Goal still indexes; backlog pending-only. Run cargo test unpiped.
- [x] 4. **Rebuild (derived-only wipe + rescan) + lazy bootstrap on project open** — indexer.rs: pub async fn rebuild_derived(store, plans_dir, backlog_path, progress) -> Result<IndexReport> = delete_derived + index_state_clear, then full index_derived (authored rows sacred — covered by step-2 test). Lazy bootstrap: in src-tauri where the project's memory store opens at startup (build_brain in src-tauri/src/main.rs — projects.rs switch_project doc says 'On restart build_brain consumes the marker and opens the project'; find the store-open site), after open: if count_by_class derived == 0, spawn index_derived on a background task — best-effort (eprintln on failure, NEVER block startup), mirroring projects.rs:103 seed_stores spawn_blocking pattern. Brain tests: rebuild_derived on a temp .coding tree replaces derived rows and preserves an authored row. Run cargo test unpiped + cd src-tauri; cargo build (bin crate compiles separately).
- [x] 5. **IPC: memory_rebuild_index + memory_index_status commands** — src-tauri/src/ipc/memory_maintenance.rs: add (1) memory_rebuild_index — streams memory://maintenance events with op:'index' (started → progress → done/error), reusing the busy-flag guard + PROGRESS_EVERY/should_forward_progress; calls rebuild_derived from step 4; (2) memory_index_status -> { authored_count, derived_count, last_built_at: Option<i64> } from count_by_class + index_state_last_built (knobs snapshot added by step 6 if it lands first, else keep minimal). Follow existing IpcError::msg('memory store unavailable') patterns. Register both in src-tauri/src/main.rs command list (near ipc::projects::create_project ~:669). cd src-tauri; cargo build green.
- [x] 6. **Scale knobs config (decay / per-query cap / per-class cap / digest budgets) wired into recall + memory_write** — Locate mnemo::config (TraceConfig with memory_budget_mb lives there; src-tauri imports mnemo::config::TraceConfig). Add MemorySearchConfig (serde + Default): decay_half_life_days (default = the hardcoded Ebbinghaus constant in src/memory/mod.rs recall scoring), per_query_cap (default = current recall default limit), per_class_cap (max one-record_type records per query, default 5), digest budgets {plan 400, bug 600, spec 500, decision 300, review 500}. Wire: MemoryStore holds a config snapshot (setter + Default); recall() reads decay/caps from the snapshot instead of hardcoded constants; memory_write's budget_violation (src/tool/memory/mod.rs) reads budgets from the store's snapshot. Persist following TraceConfig's existing pattern (locate + follow). Defaults MUST equal today's behavior. Tests: default config leaves recall ranking unchanged (existing recall tests stay green); lowered per_query_cap honored; budget override honored by memory_write. Run cargo test unpiped.
- [x] 7. **Settings Memory section: derived-index card (counts, last-built, Rebuild button + progress) + scale knobs** — frontend/src/lib/tauri.ts: add maintenanceRebuildIndex() + memoryIndexStatus() invoke wrappers + TS types mirroring maintenanceRebuildSearch (:683) / codegraphRebuildIndex (:780). frontend/src/components/settings/sections/MemorySection.tsx: extend the Memory & Search section with a Derived index card — authored/derived counts + last-built timestamp from memoryIndexStatus (refresh on active like the overview ~:61/:89); Rebuild index button through the existing OpState pattern (add op 'index' routing in the progress listener ~:101); scale-knob controls (decay half-life, per-query cap, per-class cap) reading/writing the step-6 config, following how ChatSection patches draft settings (~:84); digest budgets shown read-only with their values. Follow existing component test conventions (add/extend tests if a MemorySection test file exists). cd frontend; npm test + npm run build green.
- [x] 8. **Full verification + step marker (no commit)** — cargo test unpiped — green + warning-free (#![deny(warnings)]; fix root causes, never #[allow]). cd src-tauri; cargo build. cd frontend; npm test + npm run build. memory_write a STEP MARKER (working tier): plan 1c9ba6c8 Phase 2 done, files changed, next action = Phase 3 sub-plan (retrieval tools plans_search/reviews_search/past_fixes/context_pack + read-only git_log/git_show + prompt enforcement). Do NOT commit — the root orchestrator reviews + commits the whole feature at the end.
