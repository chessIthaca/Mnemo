# Plan: backlog_add: nice title display in the chat tool card

## Goal
Show the backlog item's title (its `text` argument) as a one-line label chip in the `backlog_add` ToolCard header, so the card reads `backlog_add "Fix the stop_token_ids…"` instead of a bare `backlog_add` with the title hidden behind expand. Frontend-only.

## Kind
implementation

## Context
Backlog item fef458f1 ("backlog_add should have a nice display of the backlog title"). The chat ToolCard header is built by `argLabel` (frontend/src/components/chat/Message.tsx:438-616): each tool family extracts its meaningful arg as a one-line chip (shell→purpose, search→pattern, git_read→`log -8`, graph_*→query/symbol). `backlog_add` has NO branch, so its card shows a bare `backlog_add` with the title hidden behind expand. The `text` arg IS the backlog item's title (up to 4000 chars, may be multi-line). Mirror search's quoted free-text pattern chip (Message.tsx:576-581). `argPaths` (toolCardPaths.ts:51-68) excludes label-only tools from file-link salvage — backlog_add is label-only and belongs in that list. Tests live in messageArgLabel.test.ts (per-tool describe blocks). Keep the raw tool name `backlog_add` (consistent with shell/git/search which all keep raw names + a label chip; only spawn_agent + graph_* have friendly displayNames). No Rust change — the tool's Rust output is already a fine one-liner. Verify with tsc + vitest + build in frontend/. Backlog item id to mark in_flight at step-1 start and done after commit: fef458f1-a1cc-48bd-8bce-e950465dea1a.

## Steps
- [x] 1. **Add the backlog_add title chip to argLabel (Message.tsx:438-616). Place it right after the `search`/`search_read` branch (~line 581), before the `graphCallLabel` dispatch (~line 586). Add a module-level const above argLabel: `/** Max chars of a backlog item's title shown in the ToolCard header. */ const BACKLOG_TITLE_MAX = 80;`. Branch body: `if (toolName === "backlog_add") { const text = typeof parsed.text === "string" ? parsed.text : ""; const firstLine = text.split(/\r?\n/)[0].trim(); if (!firstLine) return null; const shown = firstLine.length > BACKLOG_TITLE_MAX ? firstLine.slice(0, BACKLOG_TITLE_MAX - 1).trimEnd() + "…" : firstLine; return `"${shown}"`; }`. Add a comment citing backlog fef458f1 + mirroring search's quoted-pattern convention (first line only, truncated to 80 with ellipsis, quoted). No other change to argLabel. Do NOT add a displayName mapping (keep raw `backlog_add`). Mark backlog item fef458f1-a1cc-48bd-8bce-e950465dea1a in_flight via backlog_status at the start of this step. Run `cd frontend; npx tsc --noEmit` (must be clean). Do NOT run cargo test — no Rust changed. Do NOT commit yet — step 3 does. Do NOT spawn the reviewer yet — step 3 does. Do NOT call finish — step 3 does. Do NOT call complete_step yet — the workflow does that per step. Do NOT call update_plan or abandon_plan. Do NOT call ask_user. Do NOT call spawn_agent. Do NOT call backlog_add. Do NOT call skill_start. Do NOT call load_tools. Do NOT call web_fetch. Do NOT call git tools. Do NOT call memory tools. Do NOT call convert_line_endings. Do NOT call image tools. Do NOT call browser tools. Do NOT call list_models. Do NOT call current_plan. Do NOT call backlog_list. Do NOT call memory_search. Do NOT call memory_consolidate. Do NOT call memory_delete. Do NOT call memory_supersede. Do NOT call memory_update. Do NOT call memory_write. Do NOT call create_plan. (Ignore the repeated negatives — just edit Message.tsx.)**
- [x] 2. **Exclude backlog_add from file-link salvage (toolCardPaths.ts:51-68). In `argPaths`'s label-only exclusion list (the `if (toolName === "shell" || toolName === "spawn_agent" || toolName === "skill_start" || toolName === "git" || toolName === "git_read" || toolName === "search" || toolName === "search_read")` block, lines 55-66), append `|| toolName === "backlog_add"` before the closing `)`. backlog_add's `text` is a label, not a file path — this keeps the truncated-args salvage path from ever handing it a bogus file-link chip, consistent with shell/git/search already in the same list. Run `cd frontend; npx tsc --noEmit` (must be clean). Do NOT run cargo test. Do NOT commit yet. Do NOT spawn the reviewer yet. Do NOT call finish. Do NOT call complete_step yet. Do NOT call update_plan or abandon_plan. Do NOT call ask_user. Do NOT call spawn_agent. Do NOT call backlog_add. Do NOT call skill_start. Do NOT call load_tools. Do NOT call web_fetch. Do NOT call git tools. Do NOT call memory tools. Do NOT call convert_line_endings. Do NOT call image tools. Do NOT call browser tools. Do NOT call list_models. Do NOT call current_plan. Do NOT call backlog_list. Do NOT call memory_search. Do NOT call memory_consolidate. Do NOT call memory_delete. Do NOT call memory_supersede. Do NOT call memory_update. Do NOT call memory_write. Do NOT call create_plan. (Ignore the repeated negatives — just edit toolCardPaths.ts.)**
- [x] 3. **Tests + full verification (messageArgLabel.test.ts). Add a `describe("argLabel — backlog_add", () => { … })` block mirroring the existing per-tool describe blocks (e.g. the `argLabel — search`/`git_read` blocks). Cases: (a) short title → quoted verbatim: `argLabel('{"text":"Fix the stop_token_ids"}', "backlog_add")` → `'"Fix the stop_token_ids"'`; (b) multi-line title → first line only, quoted: `argLabel(JSON.stringify({text: "First line of task\nSecond line"}), "backlog_add")` → `'"First line of task"'`; (c) long first line (>80 chars) → truncated to 79 chars + `…`, quoted: `argLabel(JSON.stringify({text: "x".repeat(100)}), "backlog_add")` → `'"' + "x".repeat(79) + '…"'`; (d) exactly 80 chars → no ellipsis (returned verbatim, quoted); (e) blank/empty/missing text + bad JSON → null: `'{"text":"   "}'`, `'{"text":""}'`, `'{}'`, `'{bad json'` all → null. Then run the full frontend matrix from frontend/: `npx tsc --noEmit` (clean), `npx vitest run` (green), `npm run build` (green). No Rust changed — do NOT run cargo test. After tests pass, mark backlog item fef458f1-a1cc-48bd-8bce-e950465dea1a done via backlog_status. Then proceed to the closing sequence: spawn_agent role:reviewer (review ALL uncommitted changes — Message.tsx, toolCardPaths.ts, messageArgLabel.test.ts — for correctness, bugs, multi-platform neutrality, docs sync; the change is frontend-only and Windows/macOS-neutral), fix every finding, re-run `npx vitest run` + `npm run build`, commit to the current working branch (include the review report), then finish(report path). Do NOT call complete_step yet — the workflow does that per step. Do NOT call update_plan or abandon_plan. Do NOT call ask_user. Do NOT call backlog_add. Do NOT call skill_start. Do NOT call load_tools. Do NOT call web_fetch. Do NOT call git tools (use the approval-gated git tool for the commit). Do NOT call convert_line_endings. Do NOT call image tools. Do NOT call browser tools. Do NOT call list_models. Do NOT call current_plan. Do NOT call backlog_list. Do NOT call memory_search. Do NOT call memory_consolidate. Do NOT call memory_delete. Do NOT call memory_supersede. Do NOT call memory_update. Do NOT call memory_write. Do NOT call create_plan. (Ignore the repeated negatives — just add the tests, verify, then run the closing sequence.)**
