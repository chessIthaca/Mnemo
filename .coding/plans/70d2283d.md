# Plan: Bound the over-threshold re-compaction loop + cross-turn re-emission guard

## Goal
Stop the unbounded engine loop where a turn whose context stays over the summarize threshold re-compacts every iteration (maybe_compact, src/agent/turn.rs:1433), each cycle burning a summarizer LLM call + a model request on a nearly-identical context, which drives byte-identical re-emission of tool calls. Two guards: (A) a per-turn compaction-attempt budget with escalation (keep_recent 6/3 → 1 → turn abort with a clear error event); (B) a cross-turn repetition guard (3 consecutive byte-identical assistant responses → break the turn with an error event — the turn-level mirror of the in-stream R10 guard).

## Kind
bug_fixing

## Context
Root cause pinned from logs + code (BUG memory id e3da7716-86b8-56ce-b11a-0254cea75077, knowledge record .coding/knowledge/bug/2027-01-07-cross-turn-re-emission-tool-loop-pattern-continu.md). Key sites: maybe_compact (src/agent/turn.rs:1433-1653, called from run_turn at turn.rs:151-719) — the per-iteration fill check whose own comment admits "a turn that stays over the threshold re-compacts every iteration"; TurnState already carries compact_announced (event-spam suppression only, not a bound); summarize_with_interrupt (src/agent/context.rs:286-382) with keep_recent (6 normal, 3 over hard_ceiling); the in-stream R10 guard precedent at src/provider/openai/stream.rs:447-489 (detect_repetition). keep_recent=1 is safe — exercised by existing tests (summarize_with_interrupt_all_tool_tail_appends_sendable_continuation, context.rs:1794; ensure_sendable_tail context.rs:555). Why existing guards miss it: R10 watches within one stream only; the auto-continue budget (MAX_AUTO_CONTINUE=12) doesn't apply — the loop is inside one turn's tool cycle; workflow_expects_progress(Complete)=false parks, so auto-continue is not the driver.

## Steps
- [x] 1. **Reproduce with failing regression test** — Write a regression test that reproduces the defect (constitution: every defect gets a regression test that fails without the fix and passes with it). Run it and confirm it FAILS.
- [x] 2. **Document root cause** — Investigate and document the root cause. memory_write a BUG: record (symptom → root cause → fix + regression test name, ≤600 chars).
- [x] 3. **Minimal fix** — Apply the minimal fix that makes the regression test pass. Do not refactor unrelated code.
- [x] 4. **Verify** — Run the regression test + the full test suite (cargo test unpiped, warning-free). Record the regression test name via update_plan (regression_test field) — finish is blocked without it.

## Bug
A turn whose context stays over the summarize threshold re-compacts every iteration unboundedly — each cycle runs an extra summarizer LLM call plus a model request on a nearly-identical context, driving byte-identical re-emission of the same tool call. Observed live: ~114 LLM requests over 95 minutes with "no new work" (the session summary recorded "resumed/re-injected with this summary thirty-one times"), duplicated identical read_files messages in the transcript, and a 5,526,459-token request rejected with 400 (context limit 1,048,576).

## Regression test
over_threshold_turn_bounds_compaction_attempts
