# Plan: Complete the git pull with clean history

## Goal
Integrate origin/main (ff7a633, 13 commits) into a fresh wt/* working branch forked from main (8957b12, 23 commits ahead), resolving the 3 known conflicts (README.md, src/agent/loop_impl.rs, src/memory/mod.rs) with the already-verified resolution, validated by a passing cargo test. Policy forbids committing on main, so the merge lands on wt/*; the user can later merge_to_main (wt/* -> main) and push. The uncommitted WIP (src/memory/tests.rs, src/runtime/agent.rs) is preserved as unstaged work, not swept into the merge.

## Kind
research

## Context
A `git pull` (merge of main from the GitHub remote, commit ff7a633) is in progress with MERGE_HEAD set. Conflicts in README.md, src/agent/loop_impl.rs, src/memory/mod.rs are already resolved in the working tree (no markers) but not staged. ~58 files of local feature work are staged and would be swept into the merge commit if committed now. User wants clean history: local work as its own commit, then a clean merge commit. Approach: back up the 3 resolved files (gitignored target/), abort the merge, commit the local work, re-pull, restore the resolved content, test, commit the merge. No source code is authored by this plan (resolution already exists); cargo test validates.

## Steps
- [x] 1. **Back up resolved conflict files to gitignored target dir before aborting the merge, so the already-done resolution is not lost. Verify the 3 backups exist. Files: README.md, src/agent/loop_impl.rs, src/memory/mod.rs. PowerShell: New-Item -ItemType Directory -Path target/.merge-resolved-backup -Force; then Copy-Item each file into target/.merge-resolved-backup/ . Confirm with Get-ChildItem target/.merge-resolved-backup/ that all 3 are present. (target/ is gitignored so this never pollutes git status.)**
- [x] 2. **Back up agent.rs WIP and make it uptodate** — Copy the current worktree agent.rs (staged+unstaged WIP applied) into target/.merge-resolved-backup/agent.rs so the WIP is preserved. Then run `git checkout -- src/runtime/agent.rs` to discard only the unstaged worktree changes (worktree now matches the index = uptodate). Verify: `git status --short src/runtime/agent.rs` shows `M ` (staged-only, no second column). This unblocks the merge abort without losing any content (WIP is in the backup; staged part stays in the index).
- [x] 3. **Abort the stale pull** — Run `git merge --abort` (now unblocked since agent.rs is uptodate). Verify: exit 0; `git status --short` shows NO `UU` lines; .git/MERGE_HEAD is gone; `git rev-parse --abbrev-ref HEAD` shows `main`; the trove files are all still present in the worktree as unstaged modifications / untracked new files (abort resets the INDEX to HEAD but keeps worktree content for non-merge files). Nothing is lost.
- [x] 4. **Create wt/* working branch from main** — Run `git checkout -b wt/pull-sync` from main (8957b12). The uncommitted WIP (src/memory/tests.rs modified + .coding/plans/718c0d97.md untracked) carries over to the new branch. Verify `git rev-parse --abbrev-ref HEAD` shows wt/pull-sync. NOTE: there is no separate 'trove' to commit first — the user's local work is already committed in 8957b12; origin/main's 13 commits are what we are integrating.
- [x] 5. **Merge origin/main into the wt/* branch** — Run `git merge origin/main` (explicit ref — the fresh wt/* branch has no upstream, so bare `git pull` would fail). Expect the same 3 conflicts to reappear: README.md, src/agent/loop_impl.rs, src/memory/mod.rs (origin/main has 4 commits touching these). src/memory/tests.rs WIP is safe — origin/main does not touch it, so it stays as unstaged WIP through the merge. Verify with `git status --short` that those 3 show UU.
- [x] 6. **Restore the known-good resolved conflict files** — Copy the 3 backed-up resolved files back over the conflicted worktree files: target/.merge-resolved-backup/README.md -> README.md, loop_impl.rs -> src/agent/loop_impl.rs, mod.rs -> src/memory/mod.rs. Then sweep all 3 for conflict markers with Select-String -Pattern '<<<<<<<|>>>>>>>|=======' (7 equals). Expect ZERO matches. This restores the known-good resolution verified earlier (no markers; coherent: fallback_endpoints 429-fix field in loop_impl.rs, FtsResult enum in mod.rs).
- [x] 7. **Stage the resolved trio and validate with cargo test** — Run `git add README.md src/agent/loop_impl.rs src/memory/mod.rs`. Then run `cargo test` UNPIPED (per the Windows constitution, do not trust a piped exit code) and read `$LASTEXITCODE` plus the `test result:` lines. The build must be warning-free (#![deny(warnings)] at both crate roots), so a green cargo test also proves zero warnings. If tests fail, investigate the resolution and fix before committing.
- [x] 8. **Commit the merge on the wt/* branch** — Run `git commit --no-edit` to finalize the merge using the prepared MERGE_MSG. Verify with `git log --oneline -3` that the merge commit is present on wt/pull-sync. src/memory/tests.rs WIP remains unstaged (not in the merge commit). This completes the pull integration on the working branch — never on main.
- [x] 9. **Restore agent.rs WIP, clean up backup, verify and report** — Copy target/.merge-resolved-backup/agent.rs -> src/runtime/agent.rs to restore the WIP (the auto-merged base matches the committed version, so only the WIP shows as an unstaged modification). Remove the backup dir: Remove-Item target/.merge-resolved-backup -Recurse -Force. Final verify: `git log --oneline -5` shows the merge commit on wt/pull-sync; `git status` is clean except for the two WIP files (tests.rs, agent.rs) as unstaged modifications; no unmerged paths. Report the final state: pull integrated on wt/pull-sync; user can merge_to_main (wt/pull-sync -> main) then push; WIP files left uncommitted for the user.
