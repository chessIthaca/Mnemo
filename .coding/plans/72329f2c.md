# Plan: Full codebase review: architecture/performance, security, quality & maintainability → backlog findings

## Goal
Produce a comprehensive review of the codebase at current HEAD (bf0f71c) across four axes — architecture & performance, security, code quality, maintainability — via three parallel read-only reviewer subagents; consolidate and verify their findings against the current tree; then queue every actionable finding as a backlog item for future sessions.

## Kind
research

## Context
Prior full reviews: 2026-12-15 (arch+quality+perf) and 2026-12-17 (arch+perf+security, HEAD bdd3b42, 3 LOW findings fixed at 8957b12: repetition guard lifted to shared streaming layer, consolidation dedup, SSRF protection on web_fetch). Since 2026-12-17 ~25 commits landed: backlog soft delete + 30-day purge (f5be6cf), tool-card expanded views (4a31d1c), GLM-5.3 stop tokens + stream guard (b3b18a3), DeepSeek reasoning_effort fix (a73d38f), search auto-delegation for symbol/memory lookups (6cf10d5), claude-opus-5 empty-thinking fix (5492a6b), backlog status⇔plan-lifecycle semantics (ba392c1), frameless cards (51bbe72, bf0f71c). Backlog currently has zero pending items. Repo: Tauri app — frontend src/ (TS/TSX React), backend src-tauri/src/ (Rust, IPC commands in src-tauri/src/ipc/). Reviewers are read-only (role:"reviewer") and author their reports via write_review_report into .coding/reviews/.

## Steps
- [x] 1. **Scope the review + prep reviewer briefs** — Run git_read op=diff to check for uncommitted changes (include them in scope if present). Confirm repo layout: frontend src/ (TS/TSX React), backend src-tauri/src/ (Rust; IPC commands in src-tauri/src/ipc/). Assemble the change list since the 2026-12-17 review (bdd3b42 → bf0f71c): backlog soft delete + 30-day purge (f5be6cf), tool-card expanded views (4a31d1c), GLM-5.3 stop tokens + stream guard (b3b18a3), DeepSeek reasoning_effort fix (a73d38f), search auto-delegation (6cf10d5), claude-opus-5 empty-thinking fix (5492a6b), backlog status⇔plan-lifecycle (ba392c1), frameless cards (51bbe72, bf0f71c). Note for reviewer briefs: the three 2026-12-17 findings (repetition guard, consolidation dedup, SSRF protection) are FIXED at 8957b12 — do not re-report; verify they still hold instead. Call list_models to pick distinct reviewer models if available.
- [x] 2. **Spawn architecture + performance reviewer** — spawn_agent role:"reviewer", name "arch-perf-reviewer" (self-contained task): review module boundaries and layering (frontend ↔ IPC ↔ Rust core), async/concurrency patterns (tokio usage, lock scope, channels), hot paths (LLM streaming, transcript rendering, memory recall/embedding, graph queries), memory usage/retention, I/O batching (file writes, JSONL appends, DB), startup cost. Report findings ranked HIGH/MED/LOW with file:line evidence via write_review_report.
- [x] 3. **Spawn security reviewer** — spawn_agent role:"reviewer", name "security-reviewer" (self-contained task): review the IPC command surface (src-tauri/src/ipc/), path traversal in file/shell tools, injection (SQL/regex/command), secrets & API-key handling/storage, network fetch surface (web_fetch — SSRF protection landed 8957b12, verify it holds), unsafe code blocks, tauri.conf.json (CSP, allowlist, capabilities), backlog/memory JSONL parsing robustness (union merge, resurrection). Report findings ranked HIGH/MED/LOW with file:line evidence via write_review_report.
- [x] 4. **Spawn quality + maintainability reviewer** — spawn_agent role:"reviewer", name "quality-reviewer" (self-contained task): review duplication, dead code, error-handling consistency (Result hygiene, unwrap/expect in non-test code), doc comments on public functions, test coverage gaps (frontend vitest + cargo tests), module size/complexity hotspots, naming consistency, docs sync (README.md / PLAN.md vs shipped features). Report findings ranked HIGH/MED/LOW with file:line evidence via write_review_report.
- [x] 5. **Consolidate + verify findings** — Read all three review reports from .coding/reviews/. Deduplicate overlapping findings across axes. Spot-check each finding against the current tree (read the cited files/lines; use graph_context for symbol questions). Drop anything already fixed, factually wrong, or already tracked in a done backlog item. Normalize severity (HIGH/MED/LOW) across the consolidated set.
- [x] 6. **Queue findings in the backlog** — For each verified actionable finding, backlog_add a self-contained item: title = imperative fix summary; body = symptom, evidence (file:line), severity, concrete fix sketch, and any regression-test requirement (project rule: every bug fix ships with a regression test). Group tightly-coupled findings from one root cause into a single item. Skip pure style nits with no actionable fix.
- [x] 7. **Record the review in memory** — memory_write a REVIEW: semantic record: consolidated verdict (counts by severity), the three report paths in .coding/reviews/, the backlog item ids created, and the HEAD reviewed. Keep it pointer-first. Do not supersede the 2026-12-15/2026-12-17 review memories (historical, not contradicted).
