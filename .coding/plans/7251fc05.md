# Plan: Research: parallel run-all — multiple main agents, one branch each

## Goal
Produce a dependency-ordered gap analysis: what must be enabled for run_all to run multiple backlog items concurrently, each with its own main agent working in its own git branch — covering the dispatch loop, the runtime agent model, git worktree/branch provisioning, .coding/ shared-state coordination, UI, and safety. No source-code changes.

## Kind
research

## Context
Known from this session: run_all.rs:943-955 refuses dispatch while the main agent is running or has running descendants ("interleaving corrupts statuses and resolves the wrong item"); run_all_dispatch_next checkpoints the tree before dispatch (checkpoint_impl commits on the current branch — the MEDIUM security finding 04bd6977); item status is tied to plan lifecycle (45dcf577: set_plan_id at src/backlog.rs:438 records the ROOT plan id; the checkpoint sha is embedded in the item note and parsed by extract_checkpoint_sha); branch policy: one wt/* working branch per agent directory (worktree), reused across plans, auto-forked from main by create_plan (ensure_work_branch, git_ops.rs:588-642), landed via merge_to_main which deletes the branch; .coding/ is the mergeable side-car (backlog.jsonl has a git union merge driver; plans/stack.json, memory.db, codegraph.db are per-instance/gitignored); the descendant gate was just relaxed to review-exit transitions (commit 264ba82) — parallel SIBLING agents are not descendants of each other, so that gate is not the blocker; spawn.rs has parent_in_state and builds per-agent tool surfaces (the run-all surface lacks backlog_add — item 66c65db9). The user wants research only — the deliverable is the gap analysis, not code.

## Steps
- [x] 1. **Map the run-all dispatch loop**
- [x] 2. **Map the runtime agent model**
- [x] 3. **Map the git/branch layer**
- [x] 4. **Map shared-state contention**
- [x] 5. **Synthesize the enablement gap list**
