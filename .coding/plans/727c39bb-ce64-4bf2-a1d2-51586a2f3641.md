# Plan: Collapse provider cards to a single line

## Goal
Make collapsed endpoint/provider cards in the Settings → Providers section show only the single header line (chevron, key icon, name, DEFAULT badge, delete), hiding the kind/URL/model-count row until the card is expanded.

## Kind
implementation

## Context
In frontend/src/components/settings/sections/EndpointCard.tsx, the card always renders two rows: (1) header row with chevron toggle, key icon, name input, DEFAULT badge, trash (lines 189–234); (2) a bg-bg-secondary inner box with kind select, base URL input, and a "N models" toggle button (lines 236–273) — this shows even when `open` is false. Expanded-only content (API key, models list, etc.) is already gated on `open`. Parent (ProvidersSection.tsx) starts all cards collapsed (`setExpanded(new Set())` on load) and toggles via the chevron. No component tests exist for EndpointCard (only DiffView/JsonView/settings types/store tests), so no test updates needed. Frontend scripts: build = `tsc && vite build`, test = `vitest run`.

## Steps
- [x] 1. **Hide second row when collapsed** — in frontend/src/components/settings/sections/EndpointCard.tsx, wrap the `bg-bg-secondary` inner box (currently lines 236–273: kind select, base URL input, "N models" button) in `{open && ( ... )}` so a collapsed card renders only the header line. Keep the div contents and the "N models" toggle button (which also toggles collapse) unchanged inside the conditional.
- [x] 2. **Keep model count visible in header** — in the same file, add a small muted span `{endpoint.models.length} model{...plural}` (styled like the existing count button, `text-[0.7rem] text-[color:var(--text-muted)]`) in the header row between the DEFAULT badge and the trash button, rendered only when `!open`, so the at-a-glance count is not lost while collapsed. When expanded, the count remains where it is (second row) — no duplication.
- [x] 3. **Verify** — run `npm run build` and `npm test` in frontend/, and `cargo test` at the repo root; check `exit=$LASTEXITCODE` directly (unpiped) and confirm no new warnings/errors.
