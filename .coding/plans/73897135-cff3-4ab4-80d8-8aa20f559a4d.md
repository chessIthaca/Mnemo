# Plan: Flip resize-bar seam to bg-secondary (user wants opposite of last fix)

## Goal
Reverse the resize-bar seam color change from the 2026-09-05 fix (75e4c82): instead of painting every handle strip bg-bg-primary to match the vertical handle, paint every handle strip bg-bg-secondary so all five handles match the horizontal bars' lighter seam. Update the regression test contract and doc comments accordingly.

## Kind
bug_fixing

## Context
User: "in a recent change you changed the background of the horizontal resize bars to match the vertical resize bars. I was hoping for the opposite color change to happen." The recent change (commit 75e4c82, "Fix AppHangB1: vendored tao 0.35.4 backport + delta coalescing; resize-bar seam fix") changed all five transparent resize-handle strips to explicitly paint bg-bg-primary (#0f172a, dark) between their border hairlines, so the horizontal bars (InflightBar, FileViewer, GraphView, LlmTraceView) matched the vertical App.tsx ResizeHandle. The user wanted the vertical handle to instead match the horizontal bars' original lighter bg-bg-secondary (#1e293b) seam. Files to change:
- frontend/src/App.tsx:805 — vertical ResizeHandle: bg-bg-primary → bg-bg-secondary (also the doc comment at :813-817)
- frontend/src/components/chat/InflightBar.tsx:206 — border-y handle: bg-bg-primary → bg-bg-secondary (comment at :204-205)
- frontend/src/components/views/FileViewer.tsx:371 — splitter: same (comment at :364-367)
- frontend/src/components/views/GraphView.tsx:776 — splitter: same (comment at :769-772)
- frontend/src/components/views/LlmTraceView.tsx:976 — splitter: same (comment at :966-969)
- frontend/src/components/resizeHandleMotif.test.ts — all five seam expectations: "border-x border-border bg-bg-primary" → "border-x border-border bg-bg-secondary" (and border-y variants), plus the module doc comment (lines 17-27)
- .coding/knowledge/bug/2026-08-24-horizontal-resize-bars-showed-bg-secondary-seam.md — the knowledge file documents the previous direction; update the fix description to the new direction (or note the reversal).
Working tree is clean except an untracked .coding/plans file; current branch per git log is main (668c105). Branch policy: create_plan forks from develop; reuse existing wt/* branch if active, else fork. Check branch state first (shell) once in Executing.

Regression test: resizeHandleMotif.test.ts already asserts the seam class for all five handles — flipping the expected string to bg-bg-secondary makes it fail without the fix and pass with it. Run `cd frontend; npm test` (and `cargo test` per constitution) to verify.

## Steps
- [x] 1. **Reproduce with failing regression test** — Write a regression test that reproduces the defect (constitution: every defect gets a regression test that fails without the fix and passes with it). Run it and confirm it FAILS.
- [x] 2. **Document root cause** — Investigate and document the root cause. memory_write a BUG: record (symptom → root cause → fix + regression test name, ≤600 chars).
- [x] 3. **Minimal fix** — Apply the minimal fix that makes the regression test pass. Do not refactor unrelated code.
- [x] 4. **Verify** — Run the regression test + the full test suite (cargo test unpiped, warning-free). Record the regression test name via update_plan (regression_test field) — finish is blocked without it.

## Bug
Resize bars show the wrong seam color — the 2026-09-05 fix made the horizontal bars match the vertical handle's dark bg-bg-primary, but the user wanted the opposite: the vertical handle should match the horizontal bars' lighter bg-bg-secondary seam.

## Regression test
frontend/src/components/resizeHandleMotif.test.ts
