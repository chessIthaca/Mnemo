# Plan: Backlog: memory supersession feature

## Goal
Add the memory-supersession harness feature to .coding/backlog.json as a pending item.

## Kind
research

## Context
User decision 2026-08-20 (semantic memory "DECISION: step-granularity crash markers + discovery-first memory"): the memory store accumulates stale state — e.g. recall surfaces BOTH "search plan 7a86564c 5/11 crashed" AND its later "11/11 done" superseder at similar scores. The agreed fix is a harness feature: explicit supersession support. User approved adding it to the BACKLOG (not implementing now). This plan only appends one item to .coding/backlog.json — pure .coding bookkeeping, no source changes (research-kind precedent: plan 1fb3f440 justified memory/bookkeeping-only writes as research for exactly this reason). No branch needed.

## Steps
- [x] 1. **Append supersession backlog item to .coding/backlog.json** — Read .coding/backlog.json (confirm current next_id — expected 68 after the merge). Append one item: id = next_id, text = "Memory store: supersession support — memory_write accepts a supersedes/superseded_by field, status-changing writes (SHIPPED/MERGED/ABANDONED/STATUS UPDATE) name the entry they supersede, and recall deprioritizes or annotates superseded rows. Rationale: stale state rows currently recall alongside their superseder (e.g. 'plan 7a86564c 5/11 crashed' next to '11/11 done'); see the 2026-08-20 memory-protocol decision.", images = [], status = "pending", created_at = current epoch seconds (get via PowerShell [int]double::Parse((Get-Date -UFormat %s)) or equivalent), note = null. Bump next_id by 1. Validate the file is parseable JSON afterwards (ConvertFrom-Json).
