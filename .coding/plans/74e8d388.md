# Plan: Extract + test Message.tsx and EndpointCard.tsx inline logic (quality review LOW 2)

## Goal
Continue the codebase's extraction pattern for the two highest-value untested components named by quality review LOW 2: pull Message.tsx's memo-equality (arePropsEqual) into a new pure lib helper (frontend/src/lib/messageEquality.ts) and its tool-card branching (argLabel, BACKLOG_TITLE_MAX, displayName) into frontend/src/lib/toolCardPaths.ts; pull EndpointCard.tsx's caps-discovery / effective-caps / efforts-parse / number-input logic into frontend/src/components/settings/types.ts — each with behavior tests alongside (replacing the one source-contract check that exists).

## Kind
implementation

## Context
Quality review LOW 2 (.coding/reviews/2027-01-06-full-review-quality-coverage.md): significant components with inline logic and no tests — Message.tsx (1,042 lines), EndpointCard.tsx (925), StatsView, MemoryDebugView, FileViewer, GitView, App.tsx. Fix: continue the codebase's extraction pattern (toolCardPaths/planSteps/traceStats: logic → lib/ or settings/types.ts + behavior tests). SCOPE DECISION: this item covers the finding's two named highest-value targets only (Message.tsx memo-equality + tool-card branching; EndpointCard caps/dirty logic); the other five components remain for future items ("highest value first"). knip/ts-prune CI addition: NOT in scope — a separate tooling decision (new dev dependency + CI config), documented in the final summary. SURVEY FACTS: (a) arePropsEqual (Message.tsx:38-104) is pure React-free logic with only a source-contract string check (attachImages.test.ts:136-138 asserts messageSource contains "peEvicted !== neEvicted" — the regression-blind node-env pattern the finding criticizes; that test must be removed/replaced when the function moves, else it fails). (b) argLabel (Message.tsx:493-687, exported) + BACKLOG_TITLE_MAX (:490) + displayName (:692-707) are the tool-card branching; argLabel already has PARTIAL coverage in frontend/src/components/chat/messageArgLabel.test.ts (git, read_files, git_read, browser, backlog_add branches) — untested: shell purpose, spawn_agent name, skill_start skill, search/search_read pattern+glob, graph delegation, webFetch delegation, path/file basename fallback, malformed JSON. Its only external importer is messageArgLabel.test.ts. (c) toolCardPaths.ts (frontend/src/lib/) is the sibling label-helper home (graphCallLabel, browserArgLabel, webFetchLabel, memorySearchLabel all local) — argLabel's dependencies (basename, graphCallLabel, browserArgLabel, webFetchLabel) are all in-file there. (d) EndpointCard's caps logic: DiscoveredCaps type (:31-32), applyServerModels' mapping loop (:201-211), effCapsCtx/effCapsOut (:120-128); the number-input parse (trim → null if empty; finite > 0 → floor; else no change) is duplicated 4× (:507-517, :534-544, :818-826, :838-846); the efforts parse is comma-split/trim/filter-empty (:860-864). settings/types.ts already holds modelConfigFor/upsertModelConfig/capsAutofillPatch/effortToSelectValue/effortFromSelectValue (tested in types.test.ts) and imports types from ../../lib/tauri — VisionModelInfo (frontend/src/lib/tauri.ts:332) fits that pattern. (e) NEW test files must be registered in frontend/vitest.config.ts's include list (established batch constraint). Frontend suite: npm test in frontend/ (vitest, node env; currently 923 tests / 66 files). Pure code motion — moved bodies stay verbatim (doc comments included); Message.tsx keeps the memo() wiring and all rendering.

## Steps
- [x] 1. **Extract Message.tsx's memo-equality + tool-card branching into lib/** — (1) Create frontend/src/lib/messageEquality.ts: copyright header + module doc (pure memo-equality for the Message component — React-free so node-env unit tests can exercise it directly) + the MessageProps interface (entry: TranscriptEntry; streaming?: boolean — with its streaming-rationale doc comment) + arePropsEqual moved VERBATIM from Message.tsx:30-104 (doc comment + body unchanged; imports `import type { TranscriptEntry, ToolInvocation } from "./types"`). Export both. (2) In frontend/src/lib/toolCardPaths.ts: append BACKLOG_TITLE_MAX (with doc), argLabel (moved VERBATIM from Message.tsx:489-687, doc comment included — all its deps basename/graphCallLabel/browserArgLabel/webFetchLabel are already in-file), and displayName (moved VERBATIM from :689-707). (3) Rewire frontend/src/components/chat/Message.tsx: delete the moved definitions (MessageProps interface, arePropsEqual, BACKLOG_TITLE_MAX, argLabel, displayName); import { arePropsEqual, type MessageProps } from "../../lib/messageEquality" and add argLabel + displayName to the existing toolCardPaths import; `export const Message = memo(MessageImpl, arePropsEqual)` stays. (4) Update frontend/src/components/chat/messageArgLabel.test.ts: `import { argLabel } from "./Message"` → `from "../../lib/toolCardPaths"` (tests unchanged — the function body is identical). (5) In frontend/src/lib/attachImages.test.ts: REMOVE the source-contract test at :136-138 ("Message arePropsEqual re-renders on imagesEvicted changes" — it reads Message.tsx source for "peEvicted !== neEvicted", which fails once the function moves; its behavior coverage is replaced by real tests in messageEquality.test.ts in step 3). Keep the :129-134 placeholder-chip test (rendering stays in Message.tsx). Verify with `npx tsc --noEmit` in frontend/ if available, else the step-3 suite run catches type errors.
- [x] 2. **Extract EndpointCard's caps/input logic into settings/types.ts** — In frontend/src/components/settings/types.ts, append four pure helpers (doc comments in the file's established style): (1) `export type DiscoveredCaps = { ctx: number | null; out: number | null }` + `export function discoveredCapsById(list: VisionModelInfo[]): Record<string, DiscoveredCaps>` — the mapping loop from EndpointCard.applyServerModels (:203-209): for each m, caps[m.id] = { ctx: m.context_length ?? null, out: m.max_output_tokens ?? null }. Import `type VisionModelInfo` from "../../lib/tauri" (add to the existing import). (2) `export function effectiveCaps(ep: EndpointEditable, modelId: string): { ctx: number | null; out: number | null }` — from EndpointCard :120-128: ctx = modelConfigFor(ep, modelId).max_context ?? ep.max_context, out likewise (callers pass a non-null modelId; the component's capsRefModel-null guard stays at the call site). (3) `export function parseEffortsList(value: string): string[]` — value.split(",").map(s => s.trim()).filter(s => s !== "") (from :860-864). (4) `export function parsePositiveIntInput(raw: string): number | null | undefined` — "" → null (clear); Number(raw) finite && > 0 → Math.floor(n); otherwise → undefined (meaning no change — the caller skips onChange). Doc comment must state the three-outcome contract explicitly. Then rewire frontend/src/components/settings/sections/EndpointCard.tsx: delete the local DiscoveredCaps type (:31-32); applyServerModels calls discoveredCapsById(list) instead of its inline loop; effCapsCtx/effCapsOut (:120-128) become one effectiveCaps(endpoint, capsRefModel) call (keep the null-guard shape: capsRefModel ? effectiveCaps(...) : {ctx: null, out: null}); the four number-input onChange handlers (:507-517, :534-544, :818-826, :838-846) become `const v = parsePositiveIntInput(e.target.value); if (v !== undefined) onEndpointChange({ max_context: v })` (each with its own field); the efforts input onChange (:859-865) becomes `onChange({ reasoning_efforts: parseEffortsList(e.target.value) })`. Add the new names to the existing `from "../types"` import. Behavior must be byte-equivalent: the no-change path (invalid input) previously skipped onChange entirely — the undefined outcome preserves that.
- [x] 3. **Add behavior tests + run the frontend suite** — (1) Create frontend/src/lib/messageEquality.test.ts (vitest, `import { describe, expect, it } from "vitest"; import { arePropsEqual } from "./messageEquality"; import type { TranscriptEntry } from "./types"`) and REGISTER it in frontend/vitest.config.ts's include list (follow the existing entry pattern). Tests — behavior, not source-contract: streaming toggle → false; kind mismatch → false; assistant/user/error text equality (equal → true, differing text → false); user images reference inequality → false; user imagesEvicted difference → false (the regression the old source-contract check guarded — the transcript image budget flipping an entry to evicted must re-render); tool calls reference equality (same array ref → true, new array → false); steer text; qa question+answer; skill name+prompt; memory name/tier/title/snippet/hits-ref/success/running; vision index/total/query/description/success/running; default (unknown kind) → false. (2) Extend frontend/src/lib/toolCardPaths.test.ts with a describe("argLabel — remaining branches") block: shell purpose (present/blank/absent → null), spawn_agent name, skill_start skill, search/search_read pattern + glob + quoted forms, graph_search query / graph_context name delegation, webFetch url delegation, common path/file basename fallback, malformed JSON → null; plus a small describe("displayName") block (spawn_agent → "Spawn Agent", graph_* mappings, default passthrough). (3) Extend frontend/src/components/settings/types.test.ts with a describe("endpoint caps + input parsing") block: discoveredCapsById (maps context_length/max_output_tokens, null-coalesces absent fields), effectiveCaps (per-model override wins, falls back to endpoint-level, bare config when absent), parseEffortsList ("max, high" → ["max","high"], trims, drops empties, "" → []), parsePositiveIntInput ("" → null, "42" → 42, "3.9" → 3, "0"/"-5"/"abc" → undefined). (4) Run `npm test` in frontend/ (unpiped; read the summary) — expect all green with the new tests included; fix any failures.
- [x] 4. **Closing sequence: review, fix, commit, finish** — Spawn a read-only reviewer (spawn_agent role:'reviewer', complete task args) over ALL uncommitted changes: cite the plan, the quality-review LOW 2 finding, and the scope decision (two named targets; other five components + knip/ts-prune out of scope, documented). Ask it to verify: the moves are pure code motion (arePropsEqual/argLabel/displayName bodies verbatim; no behavior change), the EndpointCard rewiring preserves the no-change path for invalid number input (undefined outcome → no onChange), the four number-input sites are genuinely deduped, the source-contract test removal is compensated by real behavior tests (imagesEvicted case), messageArgLabel.test.ts's import update is the only change there, vitest registration is correct, no production behavior changed (memo wiring, rendering, store reads untouched), and multi-platform neutrality + doc sync. Fix every finding (re-run tests), round-2 verification if any findings, then commit on wt/agenticcoding (include review reports + plan file) and finish.
