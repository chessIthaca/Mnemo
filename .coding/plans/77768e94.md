# Plan: Steering layer: memory+graph over grep at every tool-choice point

## Goal
A steering layer that routes agents to memory + graph tools at every decision point: reverse nudge (graph empty → search for literals), shell grep nudge, recall rider on create_plan (structural enforcement of mandatory pre-plan memory_search), and an explicit cost-model line in TOOL STRATEGY.

## Kind
implementation

## Context
Anchors: existing nudge pattern symbol_nudge() src/tool/agent/search.rs:231 + merged_note():318 + tests :1088/:1132 (search/search_read prepend SYMBOL NUDGE when pattern is a bare identifier exactly naming an indexed symbol — SPEC merged 7fd1802). Graph tools: src/tool/agent/codegraph.rs — run_query():45 shapes results; unknown names yield helpful empty results, never errors (module doc :19-22) — the reverse nudge hooks there. ShellTool: src/tool/agent/shell.rs:52/:65/:144. CreatePlanTool: src/tool/workflow/plan.rs:253 holds only Arc<Mutex<Workflow>> — needs a memory handle wired from factory.rs; memory ranking must use the NON-access-bumping passive path (src/memory/mod.rs:1268 "Passive surfacing (auto-recall, debug previews) must NOT bump"). Prompt TOOL STRATEGY at src/agent/prompt.rs:152-159, pinned by tests at :997-998 (memory_search named) and :1237-1244 (graph tools listed before search). User ask: systematic force/guide toward memory+graph (cheaper than grep/search) — NOT a memory-solution feature. Missing-commands answer: none — the gap is the steering layer.

## Steps
- [x] 1. **Graph empty-result → search nudge** — src/tool/agent/codegraph.rs: when a name-only graph_search (or graph_context/graph_impact name resolution) returns ZERO symbols and the query is identifier-shaped, append to the JSON payload a note field + trailing line: literals (tool names, config keys, log text) are NOT indexed — use the `search` tool for those. Mirror of symbol_nudge but in reverse. Advisory only, never changes rows. Tests: empty identifier-shaped lookup carries the note; a hit does NOT; error results (broken DB) unchanged.
- [x] 2. **Shell grep-family nudge** — src/tool/agent/shell.rs execute() (~:144): before running, inspect the command text for grep-family invocations (rg, grep, git grep, findstr, Select-String); after execution prepend a one-line advisory note to the output: symbol wiring → graph_search/graph_context; file-content search → the sandboxed indexed `search` tool (it also skips build output/deps). Keep detection conservative (word-boundary match on the first token or anywhere for Select-String -Pattern); no behavior change to the command itself. Tests: 'grep foo src/' result carries the note; 'cargo test' does not; note still present when the command fails.
- [x] 3. **Recall rider on create_plan** — src/tool/workflow/plan.rs CreatePlanTool: add an optional memory handle (Option<Arc<...>>) to the struct + new(); wire it in factory.rs where CreatePlanTool is constructed (check ALL construction sites: main agent factory + sub-agent/spawn_agent toolsets — Option keeps call sites without a store working). After a successful plan create: best-effort ranked recall on title + goal (+ bug symptom when kind=bug_fixing, which should ALSO query record_type bug), using the PASSIVE non-access-bumping recall path (see src/memory/mod.rs:1268) — never the tool-layer path that bumps access. Append to the tool result a 'RECALLED CONTEXT (n)' block: top 3-5 hits as '- [tier] title (id) — one-line gist' + closing line 'full detail via memory_search'. Zero hits or no store or store error → append nothing (silent skip). Tests: seeded store + plan whose goal matches a memory → rider present with the hit id; empty store → result unchanged; bug_fixing plan → bug-record ranked in; rider does NOT bump access timestamps.
- [x] 4. **Cost-model line in TOOL STRATEGY** — src/agent/prompt.rs TOOL STRATEGY (~:152-159): add one line making the cost explicit, e.g. 'Cost model: memory_search and graph_* are single indexed lookups; search walks the tree and shell spawns a process — prefer the cheap path first.' Must not reorder the existing entries (tests :997-998 and :1237-1244 pin ordering + naming). Extend the pinning tests to also assert the cost-model line exists.
- [x] 5. **Test + docs sync** — Run cargo test (unpiped; read test result: lines + $LASTEXITCODE). Docs: README.md features list gains the steering-layer bullet (reverse nudge, shell nudge, create_plan recall rider); PLAN.md tool-strategy section if it exists gets a matching sentence. Check module doc comments in the three touched files stay accurate.
