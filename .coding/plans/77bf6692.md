# Plan: Fresh-eyes analysis: deepseek hello-turn failure

## Goal
Redo, from live evidence only, the analysis of why the turn failed after switching to deepseek and saying hello — cancelled request, interrupted turn, corrupted response — ignoring all former learnings per the user's fresh-eyes directive.

## Kind
research

## Context
Fresh-eyes protocol (user directive, .coding/knowledge/decision/2027-01-07-fresh-eyes-protocol-for-deepseek-bug-analysis.md): all former learnings about this bug are inadmissible; every conclusion must be re-derived from (1) the screenshot, (2) live trace logs, (3) code as it exists now. Captured symptom: after switching to deepseek and sending 'Hello' (~2:22 AM), the deepseek-v4-flash request ended CANCELLED, the turn was interrupted ('Turn interrupted — send a message to resume.'), and the assistant response was corrupted — no tool call emitted despite repeated reasoning intent, visible text truncated mid-sentence with leaked system-prompt fragments including a literal '</context footer>'. Two glm-5.3 requests completed after (2:23 AM @72% cache, 2:49 AM @86% cache); at least one has no visible assistant turn. Log tracing was enabled by the user before the repro, so file logs should cover the failure window. Verification: every claimed mechanism must cite a specific log line or code line; no memory-derived premises.

## Steps
- [x] 1. **Pin the symptom from raw evidence** — NO-CODE RESEARCH (observation only, no files touched). Enumerate only literal facts from the screenshot and the live conversation: 'Turn interrupted — send a message to resume.' alert; trace shows deepseek-v4-flash request CANCELLED (~2:22 AM) plus two completed glm-5.3 requests (~2:23, ~2:49); garbled assistant response (no tool call, truncated, leaked '</context footer>'); header model glm-5.3. State the working symptom definition and open questions (which request produced the garbled text; what the 2:49 request was; why the deepseek request was cancelled) for the user to correct.
- [x] 2. **Locate the trace logs** — NO-CODE RESEARCH (read-only searches). Find where the app writes trace logs: search '**/*.rs' for 'tracing' (log sink, tracing_appender/tracing_subscriber/tauri_plugin_log, output path) and '**/*.ts' / '**/*.tsx' for 'tracing' (settings toggle / trace UI); list the project root and .coding/ for log dirs. Also search literal 'Turn interrupted' to locate the interruption code path (likely under src-tauri/ or src/).
- [x] 3. **Read logs for the failure window** — NO-CODE RESEARCH (read-only). Read trace log entries covering ~2:22–2:50 AM: the cancelled deepseek-v4-flash request (cancel reason: user, timeout, error, or app logic?), the glm-5.3 request that produced the garbled response, and the unaccounted 2:49 glm request. Extract exact timestamps, error/cancel reasons, and response-assembly/save events.
- [x] 4. **Read the implicated code** — NO-CODE RESEARCH (read-only). From what the logs show, read the current code for the implicated paths: model switching, provider request dispatch and cancellation, response streaming/assembly/save, and the context-footer sentinel handling (expected under src-tauri/src/ and src/). No memory-derived assumptions; read the code as it exists now.
- [x] 5. **Derive and report the root cause** — NO-CODE RESEARCH (analysis deliverable). Synthesize evidence → mechanism → root cause; every claim cites a log line or code line. Deliver the analysis to the user before any fix is planned.
