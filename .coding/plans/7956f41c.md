# Plan: Hide search AUTO-DELEGATION notes from tool output display (config toggle)

## Goal
Add a `[ui] show_delegation_notes` config toggle (default false = hidden) that hides ONLY the "note: AUTO-DELEGATED …" steering line from search/search_read tool result display in the chat ToolCard — both the collapsed card's amber note chip and the expanded card's raw output — while the delegated answer (def:/callers:/full 360° lines) stays visible. Display-layer filter only: the tool result text (the model's context, including the re-issue escape-hatch hint) is untouched; console/Trace debug surfaces are not filtered.

## Kind
implementation

## Context
Exploration findings: (1) The "Output tab" does NOT exist — removed in commit 3ff4845 ("remove Output right-panel tab + dead toolOutputLog plumbing"); the echo map's claim #6 is stale and the chat ToolCard is the ONLY GUI render site for tool result text (console REPL + Trace tab are debug surfaces, not filtered by design). (2) The ToolCard has TWO note surfaces: collapsed — searchResultInfo (frontend/src/lib/toolCardPaths.ts:432-455) extracts the leading "note: …" line, Message.tsx:758-760 pushes it as an amber chip rendered at :855-863; expanded — CallDetail renders {call.result.output} in a <pre> at Message.tsx:1026-1028. (3) The note is composed by with_note (src/tool/agent/search.rs:1000): "note: {n}\n\n{body}" where n is the multi-line delegation block (header "AUTO-DELEGATED to the code graph — 'X' is an indexed symbol (re-issue this exact search to get the plain file search instead):\n  def: …\n  callers: …\n  full 360° view: …") — the FIRST line starts with "note: AUTO-DELEGATED" and the answer lines follow, so a line-level filter removes exactly the header and keeps the answer; the memory twin ("note: AUTO-DELEGATED to memory — …") matches the same prefix. (4) Merged-note edge case: when the delegation note merges with the staleness note ("; "-joined), the line filter still removes only the first line; staleness text stays visible (its own fix is queued item 42302c2d — out of scope). (5) ToolCard reads store values via useAgentStore selector (Message.tsx:703 pattern). (6) Default false (hidden) per the locked design — consistent with show_tool_activity's model-serves rationale and directly delivers the user's ask. Plumbing anchors verified: general.rs UiConfig :313-368 + Default :370-381 + tests :948-998; patch.rs :472-477 + :594-606; settings_dto.rs :207-221 + :518-529; ipc/settings.rs :535-560 + :749-754 + wire test :1155-1260; contract_fixtures.rs :227-240; dto-get-settings.json :52-65; tauri.ts :401-431 + :474-486; App.tsx :312; useAgentStore.ts :252/:442/:831; settings/types.ts ChatDraft :375-384; ChatSection.tsx :34-46/:82-100/:129+; vitest.config.ts explicit include list.

## Steps
- [x] 1. **Backend config chain — show_delegation_notes (default false)** — Follow the show_tool_activity precedent exactly. src/config/general.rs: add `show_delegation_notes: bool` field + doc comment to UiConfig after show_knowledge_activity (:338), `show_delegation_notes: false` in the Default impl (:377 area), extend the ui-defaults test + add a round-trip test (pattern: show_tool_activity_round_trips :985-998). src/config/patch.rs: SettingsPatch field (:598 area) + apply block (:475-477 area). src/config/settings_dto.rs: SettingsSaveDto field (:213 area) + apply (:527-529 area). src-tauri/src/ipc/settings.rs: GetSettingsUi field + doc (:548 area), populate (:754 area), wire test (:1177 init + :1224 assert area). src-tauri/src/ipc/contract_fixtures.rs: fixture field (:232 area, value false) AND frontend/src/lib/ipc-fixtures/dto-get-settings.json: "show_delegation_notes": false (:55 area) — both sides change together (the Rust contract test asserts the fixture).
- [x] 2. **Frontend types + store + hydration** — frontend/src/lib/tauri.ts: `show_delegation_notes: boolean` in GetSettingsUi.ui (:412 area) + `show_delegation_notes?: boolean` in SettingsSavePatch (:479 area). frontend/src/hooks/useAgentStore.ts: state `showDelegationNotes: boolean` default false (:252 area), interface `setShowDelegationNotes: (show: boolean) => void` (:442 area), impl (:831 area). frontend/src/App.tsx: `store.setShowDelegationNotes(!!settings.ui.show_delegation_notes);` (:312 area). frontend/src/components/settings/types.ts: ChatDraft.showDelegationNotes (:379 area — serializeChat is JSON.stringify, automatic) + add "delegation notes" to the chat section's SETTINGS_NAV keywords. frontend/src/components/settings/sections/ChatSection.tsx: draft init (:40 area), store commit + saveSettings mapping (:86/:95 area), checkbox UI after the knowledge-activity checkbox with label "Show auto-delegation notes in search results".
- [x] 3. **Filter helper + ToolCard render sites** — New frontend/src/lib/delegationNotes.ts (pure, React-free): `stripDelegationNotes(text: string): string` removes lines starting with "note: AUTO-DELEGATED"; `isDelegationNote(note: string): boolean` returns true when a chip-note text (no "note: " prefix) starts with "AUTO-DELEGATED". frontend/src/components/chat/Message.tsx ToolCard: add `const showDelegationNotes = useAgentStore((s) => s.showDelegationNotes);` (:703 pattern); collapsed chip — skip the searchNotes push when `!showDelegationNotes && isDelegationNote(info.note)` (:758-760); expanded output — render `showDelegationNotes ? call.result.output : stripDelegationNotes(call.result.output)` in the <pre> (:1026-1028).
- [x] 4. **Tests — Rust + frontend** — Rust: general.rs ui-defaults + round-trip tests; ipc/settings.rs wire test; contract fixture test asserts the JSON automatically (both sides changed together in step 1). Frontend: ChatSection.test.ts — source pins for the new label, `show_delegation_notes: draft.showDelegationNotes`, `setShowDelegationNotes(draft.showDelegationNotes)` + a serializeChat dirty-detection case; ipc-contract.test.ts — field-shape assert for show_delegation_notes (:413 area pattern); new frontend/src/lib/delegationNotes.test.ts — pure helper tests (strips the code-graph twin, strips the memory twin, keeps def:/callers:/full 360° lines, leaves staleness/other notes untouched, idempotent on note-free text) + Message.tsx source pins via ?raw (filter applied at both render sites) — register "src/lib/delegationNotes.test.ts" in frontend/vitest.config.ts's explicit include list.
- [x] 5. **Docs + memory** — Update .coding/knowledge/tool-call-echo-map.md: correct the stale claim #6 (the Output tab was removed in commit 3ff4845 — the ToolCard is the only GUI surface) and add the display-filter exception for the AUTO-DELEGATED note (modeled on exception (1) for show_tool_activity). Write a DECISION memory: display-layer filter (not backend suppression — the note is model guidance), default false, line-level scope (only lines starting "note: AUTO-DELEGATED"), the Output-tab-does-not-exist finding, and the merged-note edge case (staleness text stays visible; its fix is queued item 42302c2d).
- [x] 6. **Verify — cargo test + frontend vitest** — Run `cargo test` (must be green and warning-free — #![deny(warnings)] at both crate roots) and the frontend suite (`cd frontend; npx vitest run`). Fix any failures before the review.
