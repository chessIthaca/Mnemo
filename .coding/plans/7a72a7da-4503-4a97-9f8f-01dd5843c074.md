# Plan: Phase 5a — Split ipc/commands.rs + unify spawn (Maint H1/M3/M4)

## Goal
Phase 5a: Split the 2251-line ipc/commands.rs god module into domain modules (agent, settings, files, backlog commands, spawn), unify all three spawn paths (main startup, UI spawn_agent command, IpcSpawner tool) onto one spawn_agent_shared, and restructure IpcState into nested context groups (Maint H1/M3/M4). Command names stay identical so the frontend is unaffected; mod.rs + generate_handler! remain the registry.

## Context
Parent: Architecture review remediation, step 21 (Phase 5a). Findings from .coding/reviews/2026-04-08-architecture-maintainability.md: H1 (ipc/commands.rs god module ~2251 LOC / 42 commands), M3 (duplicated spawn paths: main.rs inlines ~100-114 vs spawn_agent_shared in commands.rs 290-346; must be one shared path for main startup + UI spawn_agent command + IpcSpawner), M4 (IpcState unstructured dependency bag in state.rs 32-86; group into nested contexts). Current commands.rs structure (verified): agent/safety/model/workflow/stats commands (1-717 + AgentInfo:83 + WorkflowStateInfo:95), spawn (253-421 incl. IpcSpawner), settings/config/endpoints/keys (718-1496 incl. EndpointDto:762, VisionModelDto:1272, PricingDto:1279, SettingsSaveDto:1297, rewire_vision_and_embedder:1157), files/conversations (1497-1790 + emit_prompt_dispatched:1779), backlog/run-all + callbacks (1792-2251 incl. emit_backlog_changed:1792, on_main_turn_resolved:2088, halt_run_all_for_approval:2204). Cross-module deps (verified): events.rs imports on_main_turn_resolved + halt_run_all_for_approval from commands; run_all.rs imports nothing from commands; emit_prompt_dispatched used by spawn (418) + backlog (1943, 2075); emit_backlog_changed used only within backlog command code. main.rs invoke_handler lists 41 commands via ipc::commands::*; main.rs:89-114 wires IpcSpawner + inline main-agent spawn. mod.rs currently re-exports approval/backlog/commands/events/run_all/state + spawn_event_forwarder + IpcState. Registry stays in mod.rs + generate_handler!. Frontend invokes commands by string name (invoke("backlog_add") etc.) — names must not change. Existing backlog.rs = BacklogStore (store only, no commands). events.rs already has Emitter import; emit_prompt_dispatched belongs there (event emit helper).

## Steps
- [x] 1. Create src-tauri/src/ipc/agent.rs: move agent-control/safety/model/workflow/skill/stats commands (send_prompt, send_suggestion, interrupt, cancel, approve, set_safety_mode, get_safety_mode, get_startup_error, list_agents, set_model, get_workflow_state, enter_skill, get_session_stats, get_project_stats, get_session_list, get_safety_rules, save_safety_rules, add_safety_rule) + AgentInfo + WorkflowStateInfo DTOs + their imports from commands.rs.
- [x] 2. Create src-tauri/src/ipc/settings.rs: move get_config, get_api_keys, list_models, save_endpoints, get_settings, save_settings, rewire_vision_and_embedder + EndpointDto/VisionModelDto/PricingDto/SettingsSaveDto + endpoint_kind_wire/safety_mode_wire/parse_safety_mode helpers from commands.rs.
- [x] 3. Create src-tauri/src/ipc/files.rs: move read_file, list_files, list_markdown_files, get_git_branch, save_conversation, load_conversation from commands.rs.
- [x] 4. Create src-tauri/src/ipc/backlog_cmds.rs: move backlog_add/list/remove/reorder/clear_finished/retry/set_auto_feed/dispatch_next/run_all/stop_all + emit_backlog_changed from commands.rs (BacklogItem DTO moves with them or is re-exported; run_all loop fn stays in run_all.rs).
- [x] 5. Create src-tauri/src/ipc/spawn.rs: move spawn_agent (command), spawn_agent_shared (make pub(crate)), IpcSpawner + AgentSpawner/ParentAwareSpawner impls from commands.rs; emit_prompt_dispatched moves to events.rs as pub(crate) and is imported by spawn.rs + backlog_cmds.rs.
- [x] 6. Move on_main_turn_resolved + halt_run_all_for_approval from commands.rs into run_all.rs (they are Run-All/backlog resolution callbacks; events.rs already imports them — update imports to run_all).
- [x] 7. Restructure IpcState (state.rs) into nested contexts per M4: e.g. IpcState { runtime: AgentRuntimeContext (manager, agent_loops, factory, memory_store, safety_mode, safety_rules, startup_error), project: ProjectContext (project, config, sandbox), approvals: Arc<PendingApprovals>, backlog: BacklogContext (backlog, auto_feed, run_all, single_in_flight) }; update ALL command field accesses (state.manager → state.runtime.manager etc.) in the new modules; keep the same pub fields available via the nested structs.
- [x] 8. Unify spawn paths per M3: replace main.rs's inline main-agent setup (lines ~100-114) with a call to spawn_agent_shared(factory, manager, agent_loops, "main", None, None); keep the IpcSpawner wiring (89-94) referencing ipc::spawn::IpcSpawner.
- [x] 9. Update mod.rs: declare the new modules, keep re-exports (IpcState, spawn_event_forwarder, PendingApprovals), remove `pub mod commands` (or keep a thin re-export module if main.rs references it — prefer updating main.rs). Update main.rs invoke_handler + IpcSpawner path to the new module names; delete the old commands.rs contents.
- [x] 10. Run cargo test (workspace) + npm test (vitest) + tsc --noEmit; fix all compile/test failures.
- [x] 11. Spawn a read-only reviewer subagent over all uncommitted changes; it must write its report to .coding/reviews/ (file_write bug is fixed so this now works); read report, fix every finding, re-run tests.
- [x] 12. Commit Phase 5a to the current feature branch.
