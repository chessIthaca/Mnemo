# Plan: CRLF-tolerant source-contract pins — normalize include_str! source at the read boundary

## Goal
Make every include_str!-based source-contract test CRLF-tolerant: normalize the embedded source (CRLF→LF) at the read boundary via one shared helper per crate, so an autocrlf-smudged working tree can no longer break (or silently weaken) the \n-anchored body-slice pins — no change to committed file bytes, no git config change.

## Kind
bug_fixing

## Context
Investigation (2027-01-24, unattended run-all): the \n-anchored pins are the include_str!-based source-contract tests slicing with find("\n}\n"). AFFECTED matchers: (1) fn_body (src-tauri/src/ipc/run_all.rs:1643-1653 — slices "\n}\n", ~40 test callers incl. fn_body(include_str!("backlog_cmds.rs"), ...) at run_all.rs:1920); (2) its root-crate twin fn_body (src/memory/consolidation.rs:1196-1206, same "\n}\n" slice, fed by include_str!("consolidation.rs") at :1219); (3) 18 DIRECT include_str! + find("\n}\n") sites: run_all.rs:657/813/846/880, backlog_cmds.rs:700/736/770/803/881/910/936/961, agent.rs:985/1021/1063/1124, browser_webview.rs:981, startup.rs:172. Verified CRLF-safe, deliberately NOT wrapped (minimal scope): events.rs:2527 + src/agent/turn.rs:3757 + consolidation.rs's three single-line contains reads (:1258/:1266/:1278 — single-line needles), the vendored-tree guards tao_backport.rs / wry_hard_reload_patch.rs / wry_sso_patch.rs / webview_udf.rs (single-line .contains/.find needles + RELATIVE byte-offset comparisons — both offsets shift equally under CRLF), tests/integration/ci_workflow.rs (str::lines() strips \r), src/provider/policy.rs:721 (single-line needles). FIX DESIGN: shared helper `normalize_lf(src: &str) -> String { src.replace("\r\n", "\n") }` in the EXISTING `#[cfg(test)] mod contract_fixtures;` (src-tauri/src/ipc/mod.rs:45-46) with a doc comment carrying the rationale (autocrlf smudge, include_str! embeds working-tree bytes); run_all.rs fn_body normalizes INTERNALLY (covers all ~40 callers with zero call-site edits); the 18 direct sites wrap: `let src = normalize_lf(include_str!("x.rs"));` + a `use crate::ipc::contract_fixtures::normalize_lf;` line per test module (5 files). Root crate: consolidation.rs fn_body normalizes internally (single site — no shared helper needed there). REGRESSION-TEST DESIGN: (a) run_all.rs tests — fn_body on a synthetic TWO-function CRLF source ("\r\n"-joined) must slice EXACTLY: the extracted body must NOT contain the second function's marker (pre-fix the slice over-captures to EOF and the test FAILS); (b) consolidation.rs tests — same over-capture pin for its fn_body; (c) contract_fixtures — normalize_lf unit test: CRLF→LF, LF unchanged, idempotent. NO docs/FEATURES.md change (test infrastructure only; the helper's doc comment is the documentation). NO change to committed file bytes; NO .gitattributes/eol config change. VERIFICATION: `cargo test` at the repo root AND in src-tauri (binary crate: cargo test WITHOUT --lib, ONE filter argument only; trust the `test result:` lines, not piped exit codes — run unpiped and read $LASTEXITCODE), warning-free under deny(warnings).

## Steps
- [x] 1. **Reproduce with failing regression test** — Write a regression test that reproduces the defect (constitution: every defect gets a regression test that fails without the fix and passes with it). Run it and confirm it FAILS.
- [x] 2. **Document root cause** — Investigate and document the root cause. memory_write a BUG: record (symptom → root cause → fix + regression test name, ≤600 chars).
- [x] 3. **Minimal fix** — Apply the minimal fix that makes the regression test pass. Do not refactor unrelated code.
- [x] 4. **Verify** — Run the regression test + the full test suite (cargo test unpiped, warning-free). Record the regression test name via update_plan (regression_test field) — finish is blocked without it. If the fix turned out FEATURE-SCALE (new pub types or cross-module surface): amend the plan context via update_plan append with a paragraph starting "Landed design —" (architecture, deviations from the sketch, measured numbers, invariants to preserve) and record landed_design=true BEFORE completing this step; at finish write a SPEC memory and amend the BUG record with the landed outcome (agent.md 'Documentation expectations'; the finish gate blocks landed_design without the context amendment).

## Detailed steps
- [x] **Read-before-write: exact current code** — Read src-tauri/src/ipc/contract_fixtures.rs (module shape, existing helpers, how sibling test modules import from it), the 18 direct sites' exact text (src-tauri/src/ipc/run_all.rs:655-670 + :811-826 + :844-859 + :878-893; backlog_cmds.rs:698-712 + :734-748 + :768-782 + :801-815 + :879-893 + :908-922 + :934-948 + :959-973; agent.rs:983-997 + :1019-1033 + :1061-1075 + :1122-1136; browser_webview.rs:979-995; startup.rs:170-185), run_all.rs::fn_body (:1643-1653), and consolidation.rs::fn_body (:1196-1206). Confirm each direct site's needle set (all "\n}\n" slices). No edits yet.
- [x] **Reproduce: failing CRLF over-capture tests** — (a) In src-tauri/src/ipc/run_all.rs's test module add `fn_body_slices_crlf_source_exactly`: build a synthetic two-function source joined with \r\n line endings (fn first() { MARKER_ONE } / fn second() { MARKER_TWO }, CRLF line endings, column-0 closing braces), call fn_body(&src, "first"), assert the result contains MARKER_ONE and does NOT contain MARKER_TWO (pre-fix the "\n}\n" find misses and the body over-captures to EOF → FAILS). (b) In src/memory/consolidation.rs's test module add the same over-capture pin for its fn_body (`fn_body_slices_crlf_source_exactly`). Run both with one filter argument each (src-tauri: cargo test WITHOUT --lib; root: cargo test --lib) unpiped, confirm both FAIL pre-fix.
- [x] **Fix src-tauri: shared normalize_lf + fn_body internal normalization + wrap the 18 sites** — (1) In src-tauri/src/ipc/contract_fixtures.rs add `pub(crate) fn normalize_lf(src: &str) -> String { src.replace("\r\n", "\n") }` with a doc comment: include_str! embeds working-tree bytes at compile time, so an autocrlf-smudged (CRLF) checkout breaks every "\n}-anchored body slice — normalize at the read boundary (backlog b93c0f6e); no committed bytes change. (2) In run_all.rs::fn_body (:1643) normalize internally: `let src = &src.replace("\r\n", "\n");` as the first line (covers all ~40 callers with zero call-site edits) + a one-line comment citing normalize_lf/contract_fixtures. (3) Wrap the 18 direct sites: `let src = include_str!("x.rs");` → `let src = normalize_lf(include_str!("x.rs"));` in run_all.rs (4 sites), backlog_cmds.rs (8), agent.rs (4), browser_webview.rs (1), startup.rs (1), adding `use crate::ipc::contract_fixtures::normalize_lf;` inside each affected test module (5 files) — match the existing import style. (4) In contract_fixtures.rs add the normalize_lf unit test: CRLF→LF, LF-only unchanged, idempotent (normalize twice == once).
- [x] **Fix root crate: consolidation.rs fn_body internal normalization** — In src/memory/consolidation.rs::fn_body (:1196) add `let src = &src.replace("\r\n", "\n");` as the first line with a one-line comment (same rationale, backlog b93c0f6e). The three single-line include_str! reads in durable_write_failures_log_instead_of_vanishing (:1258/:1266/:1278) stay unwrapped — single-line needles are CRLF-safe (documented in the plan context).
- [ ] **Verify both suites, review, commit** — Run `cargo test` at the repo root AND `cargo test` in src-tauri (binary crate: no --lib; when filtering use ONE filter argument), unpiped, checking $LASTEXITCODE and the `test result:` lines (warning-free under deny(warnings)). Confirm the two CRLF over-capture tests now PASS and the full matrix is green. Spawn the read-only reviewer (spawn_agent role:"reviewer", omit model) over all uncommitted changes: src-tauri/src/ipc/contract_fixtures.rs, run_all.rs, backlog_cmds.rs, agent.rs, browser_webview.rs, startup.rs, src/memory/consolidation.rs — plan goal: CRLF-tolerant include_str! source-contract pins; ask for correctness, bugs, security, constitution findings (esp.: no production-code changes, no committed source bytes touched, the deliberately-unwrapped CRLF-safe sites justified, normalize_lf used consistently, no #[allow]). Fix EVERY finding, re-run both suites, commit to the current working branch with the review report.

## Bug
Tests that pin repo source via include_str! and slice function bodies with find("\n}\n") break — or silently over-capture to EOF, weakening the pins — when git autocrlf smudges the working tree to CRLF: include_str! embeds working-tree bytes at compile time, so the "\n}\n" needle never matches "\r\n}\r\n" and src[start..].find("\n}\n") returns None → end = src.len() → the "body" is everything to EOF. Live incident 2027-01-24: a fresh checkout smudged 11 merged files to CRLF (committed blobs always identical/LF) and the source-pin tests failed although the repo content was correct; worked around once with `git -c core.autocrlf=false checkout --`.

## Regression test
fn_body_slices_crlf_source_exactly
