# Plan: Phase 3a — emit WorkflowStateChanged for remaining workflow tools (update_plan + skills)

## Goal
Implement Phase 3a (Quality M2): ensure WorkflowStateChanged is emitted after successful update_plan, skill_start, skill_end, and abandon_skill (in addition to the existing create_plan/complete_step/abandon_plan). This keeps UI, allowed tools, progress, etc. in sync for all workflow mutations. Minimal targeted change in turn.rs; update any tests/docs if needed; run cargo test before close. No unrelated changes.

## Context


## Steps
- [x] 1. Inspect current emission site in src/agent/turn.rs (the if for workflow tools after execute).
- [x] 2. Inspect tool names for update_plan and skill_* in src/tool/workflow/plan.rs, skill.rs, tool/mod.rs and how they transition state.
- [x] 3. Confirm emission contract: always emit current wf.state() on success of these (like existing).
- [x] 4. Extend the condition in turn.rs to also cover "update_plan", "skill_start", "skill_end", "abandon_skill" (after result).
- [x] 5. Check for tests that may need update (workflow_integration, tool/workflow tests, ipc events).
- [x] 6. Run cargo test (unpiped) and confirm pass.
- [x] 7. If clean, mark the remediation plan step for Phase 3a complete (the appropriate 0-based index).
