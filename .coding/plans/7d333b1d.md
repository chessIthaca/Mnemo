# Plan: Feature-scale bug-fix documentation expectations (e5a84ce9)

## Goal
Feature-scale bug fixes leave durable documentation: a constitution rule (agent.md 'Documentation expectations') + a self-reported landed_design flag on the plan frame, enforced at the bug_fixing finish gate (the same point as regression_test) — a landed_design plan whose context lacks a "Landed design" amendment cannot finish; the skeleton verify-step text and the gate's success note prompt the context amendment, the SPEC memory, and the BUG-record amendment.

## Kind
implementation

## Context
Backlog e5a84ce9 (user feedback during plan 85368a1f): bug_fixing plans under-document feature-scale fixes — the plan context is written once BEFORE implementation, the BUG memory is captured at step 2 before code exists, and nothing prompts a post-implementation amendment, so a fix that turns out feature-scale (85368a1f: parallel wave indexing — new pub types FileMeta/UpsertEntry/ParserCache, cross-module changes, concurrency invariants) leaves its landed design nowhere durable except code doc comments. Plan-time steering (041dd8d0, commit eb73f9c) already sends bug-triggered FEATURES to kind=implementation; the residual gap is the contained-at-plan-time fix that GREW mid-flight. DESIGN DECISION (documented): the item's fix direction (b) suggests a diff-threshold gate (>500 changed lines or any new pub item) — that requires git wiring in the finish tool + a plan-schema baseline commit + numstat parsing + a new class of flaky git-state tests. REJECTED as the heavy option the item itself marks "optional"; instead a SELF-REPORTED flag enforced at the same point: update_plan gains landed_design: Option<bool> (persisted like regression_test), the agent sets it when the fix turns out feature-scale (prompted by the constitution rule + the skeleton verify-step text), and the finish gate blocks landed_design=true whose plan context lacks a "Landed design" amendment. Residual (documented): an agent that forgets the flag entirely skips the gate — same exposure as the constitution rule alone; the flag catches the honest-forgetful case (sets the flag, forgets the amendment) which is the realistic failure mode. The SPEC memory + BUG-record amendment (acceptance 2+3) are NOT gate-verifiable (memory writes are invisible to the plan gate) — carried by the constitution rule + the gate's success-note reminder. CODE SITES: src/workflow/plan_file.rs (PlanFile struct :82+, regression_test field :111, Section::RegressionTest parse :279-284, serialize :333-335 — the pattern for the new landed_design: bool + Section::LandedDesign + "## Landed design" section, body yes/true → true); src/workflow/mod.rs (update_plan :629-712 — gains the 7th param landed_design: Option<bool>, sets frame.plan.landed_design both ways, counts for the no-op guard :767, doc comments :620-628/:650 list the accepted fields; the Reviewing relaxation :652-653 already allows any-field updates in Reviewing — extend the test update_plan_regression_test_allowed_in_reviewing :2344); src/tool/workflow/plan.rs (UpdatePlanArgs :443 area + schema :915-922 + call site :1017; the bug_fixing skeleton branch :590-660 — the verify-step (steps[3]) text gains the feature-scale instruction, test :2535 asserts steps[3].text; the finish gate :1754-1774 — insert the landed_design check after the regression_test presence check :1766-1771 and before the graph gate :1774: plan.landed_design && !plan.context.contains("Landed design") → blocking error naming agent.md 'Documentation expectations' + the update_plan append instruction; when set and present → notes.push the SPEC-memory + BUG-amendment reminder); agent.md (new '## Documentation expectations' section after '## Code style'). MARKER CHOICE: the context amendment is a PROSE paragraph starting "Landed design —" (NOT a "## " header — the plan-file parser treats "## " lines as section headers; regression_test has an explicit section-injection guard for exactly this). Gate check is case-sensitive contains("Landed design") — a sketch-context false positive is unlikely (documented residual). TESTS: plan_file roundtrip (landed_design=true parses back; default false); update_plan sets/unsets; landed_design-only allowed in Reviewing; skeleton verify-step text carries the instruction; update_plan tool records landed_design; finish blocks flag-without-amendment; finish passes flag-with-amendment (+ note); contained fixes (no flag) unaffected. VERIFICATION: cargo test from repo root (all suites; the plan.rs tests live in the lib suite). DOGFOODING: this plan is itself feature-scale (3 modules + a new gate) — amend its context with the landed design before verify and write a DECISION memory at finish, per the very practice it codifies.

Landed design — (dogfooding the practice this plan codifies; plan 7d333b1d, backlog e5a84ce9.) ARCHITECTURE AS LANDED: (1) agent.md gained the '## Documentation expectations' section (after '## Code style'): a bug_fixing fix that turns out feature-scale mid-flight MUST, before the verify step, amend the plan context via update_plan append with a paragraph starting "Landed design —" (architecture, deviations, measured numbers, invariants) and record landed_design=true; at finish write a SPEC memory + amend the BUG record. (2) PlanFile.landed_design: bool (serde(skip), default false) persists as the `## Landed design` section (body "yes" from serialize; parse accepts "yes"|"true" for hand-edited files) — Section::LandedDesign added to the enum + the header match. (3) workflow::update_plan gained the 7th param landed_design: Option<bool> — Some(v) sets/un-sets frame.plan.landed_design and counts for the no-op guard; allowed in Reviewing like regression_test; the skeleton-lock + no-op error texts name it. (4) The tool layer: UpdatePlanArgs.landed_design + the JSON schema property + the call site passes args.landed_design. (5) The bug_fixing skeleton's verify-step text carries the full feature-scale instruction (amend + record landed_design=true BEFORE completing the step; SPEC memory + BUG amendment at finish). (6) The finish gate, between the regression_test presence check and the code-graph gate: landed_design && !context.contains("Landed design") → blocking error naming agent.md 'Documentation expectations'; when set + present → a success note reminding the SPEC memory + BUG-record amendment (notes join into the finish output). DEVIATIONS FROM THE SKETCH: the item's fix direction (b) suggested a diff-threshold gate (>500 changed lines or any new pub item) — REJECTED as the heavy option (git wiring in the finish tool + a plan-schema baseline commit + numstat parsing + a new class of flaky git-state tests); the landed design uses a SELF-REPORTED flag enforced at the same point. Residual (documented): an agent that never sets the flag skips the gate — same exposure as the constitution rule alone; the flag catches the honest-forgetful case (sets the flag, forgets the amendment), the realistic failure mode from 85368a1f. The context marker is a PROSE paragraph "Landed design —", NOT a "## " header — the plan-file parser treats "## " lines as section starts (regression_test has an explicit section-injection guard for exactly this); the gate check is case-sensitive contains("Landed design"). MEASURED NUMBERS: 4 new tests (landed_design_flag_roundtrips, update_plan_sets_and_unsets_landed_design, finish_blocks_landed_design_without_context_amendment, finish_accepts_landed_design_with_amendment) + 2 extended (update_plan_regression_test_allowed_in_reviewing, the skeleton verify-step test); 21 update_plan call sites updated (19 in mod.rs — 13 single-line via one regex replace_all + 6 multi-line — and 2 in plan.rs); full suite 2303 + 16 passed, 0 failed, 0 warnings (plain cargo test; the --workspace count differs by feature unification). INVARIANTS TO PRESERVE: the gate fires only for bug_fixing plans with the flag set (implementation/research plans are unaffected — their PLAN digest captures the final state and they have no BUG-at-step-2 problem); the flag roundtrips through the plan file (crash-safe mid-fix restarts keep the gate armed); Some(false) un-sets (a mistaken declaration); the marker check is a plain substring — a sketch-context false positive is possible but unlikely (documented residual).

## Steps
- [x] 1. **Constitution rule (agent.md)** — agent.md: add a '## Documentation expectations' section after '## Code style' (before '## File mutation policy'): when a bug_fixing fix turns out FEATURE-SCALE mid-flight (new pub types, cross-module surface, concurrency invariants — plan-time steering already sends bug-triggered features to kind=implementation; this covers the contained-at-plan-time cases that grew), the agent MUST, BEFORE the verify step: (1) amend the plan context via update_plan append with a paragraph starting "Landed design —" carrying the architecture, deviations from the sketch, measured numbers, and invariants to preserve, and (2) record landed_design=true via update_plan. At finish, in addition to the auto-captured BUG digest: write a SPEC memory documenting the architecture + invariants, and amend the BUG record with the landed outcome. Note that the finish gate blocks a landed_design plan whose context carries no "Landed design" amendment.
- [x] 2. **The landed_design field (plan_file.rs + mod.rs)** — src/workflow/plan_file.rs: add `landed_design: bool` to PlanFile (default false — update the two struct-literal sites :181 and :298-311), a Section::LandedDesign variant (the header "## Landed design"), parse (body trimmed matches "yes" | "true" → true) + serialize (`if self.landed_design { out.push_str("\n## Landed design\nyes\n"); }` after the RegressionTest block :333-335). Tests: roundtrip with landed_design=true (the section parses back); default false when absent. src/workflow/mod.rs: update_plan gains the 7th param `landed_design: Option<bool>` — Some(v) sets frame.plan.landed_design = v and counts as a change (the no-op guard at :767 must not reject landed_design-only calls); update the doc comments (:620-628 accepted-fields list, :650, :671 error text) to name landed_design. Update the two existing call sites that pass None (the tool call site is step 3's; any test call sites in mod.rs get None). Tests: update_plan sets and un-sets landed_design; extend update_plan_regression_test_allowed_in_reviewing (:2344) to also assert a landed_design-only update succeeds in Reviewing.
- [x] 3. **Skeleton text + finish gate + tool args (plan.rs)** — src/tool/workflow/plan.rs: (1) UpdatePlanArgs gains `#[serde(default)] landed_design: Option<bool>` (after regression_test :443) + the JSON schema property (after the regression_test property :922, description: "Set true when a bug_fixing fix turned out feature-scale (new pub types / cross-module surface) — the finish gate then requires a 'Landed design' context amendment.") + pass it at the update_plan call site (:1017 area, after args.regression_test). (2) The bug_fixing skeleton's verify-step (steps[3]) text gains: when the fix turned out feature-scale (new pub types or cross-module surface), amend the plan context via update_plan append with a paragraph starting "Landed design —" (architecture, deviations, measured numbers, invariants) and record landed_design=true BEFORE completing this step; at finish write a SPEC memory and amend the BUG record with the landed outcome. (3) The finish gate, after the regression_test presence check (:1766-1771) and before the graph gate (:1774): if plan.landed_design && !plan.context.contains("Landed design") → return ToolResult::error naming agent.md 'Documentation expectations' + the exact update_plan append instruction; when landed_design && the amendment is present → notes.push a reminder to write the SPEC memory and amend the BUG record at finish. Tests: the skeleton verify-step text contains the instruction (extend the :2535-area assertions); update_plan tool records landed_design; finish blocks landed_design-without-amendment (error names the constitution section); finish passes landed_design-with-amendment and the output carries the SPEC/BUG reminder note; a contained fix (no flag) finishes unaffected.
- [x] 4. **Docs + full verify + dogfood** — README.md + PLAN.md: the bug_fixing workflow/gate descriptions (the sentences touched by the 041dd8d0 steering change — README.md ~:36, PLAN.md ~:420) gain the landed-design gate clause: a feature-scale bug_fixing fix records landed_design + a 'Landed design' context amendment before finish (the finish gate enforces it). Run full `cargo test` from the repo root — all suites green, zero warnings (deny(warnings)). DOGFOOD (the practice this plan codifies): before the verify/review, amend THIS plan's context via update_plan append with a "Landed design —" paragraph (the flag field + gate + skeleton text as landed, deviations from the sketch, test counts). Then spawn the read-only reviewer over the uncommitted changes (agent.md, src/workflow/plan_file.rs, src/workflow/mod.rs, src/tool/workflow/plan.rs, README.md, PLAN.md) — it should check the gate logic (flag → context substring), the parse/serialize roundtrip, the no-op guard, the Reviewing relaxation, the skeleton text, docs sync, and that the constitution rule matches the machinery's marker strings exactly.
