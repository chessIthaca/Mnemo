# Plan: Full review: memory leaks, performance, threading

## Goal
A full code review of the myharness codebase (Rust backend src/ + src-tauri/, TS frontend frontend/src/) focused on memory leaks, performance, and threading/concurrency problems, written up as a review report in .coding/reviews/.

## Kind
research

## Context
Prior findings exist (2026-04-19 freeze diagnosis F1–F6; 2026-04-18 deep review A1 forwarder stall). Memory says F1 (blocking git poll) + F3 (unbounded frontend transcript) were merged to main in bc210d4 (2026-08-17) — must verify at HEAD instead of re-reporting fixed issues. Deliverable is a severity-ranked review report (memory leaks / performance / threading), persisted via write_review_report to .coding/reviews/. No source changes, so kind=research.

## Steps
- [x] 1. **Map concurrency + growth surface** — grep for tokio::spawn/std::thread/blocking calls in src/ and src-tauri/, and for unbounded collections (Vec push, HashMap insert without removal) in shared state. List files to audit deeply.
- [x] 2. **Audit runtime + agent lifecycle** — read src/runtime/agent.rs (task spawn/abort paths, channel fanin, join handles, agent-id maps) and src/agent/turn.rs + context.rs for detached tasks, leaks, lock contention.
- [x] 3. **Audit provider/trace/browser subsystems** — read src/provider/openai.rs + trace.rs (writer thread, unbounded mpsc, file rewrite), src/browser/mod.rs (page/console/tasks maps, profile sweep, spawned tasks), src/memory/mod.rs + embedder.rs for boundedness.
- [x] 4. **Audit IPC + Tauri layer** — read src-tauri/src/ipc/*.rs (events.rs forwarder, prev_workflow_state map, get_git_branch, listeners) and src-tauri/src/main.rs webview lifecycle.
- [x] 5. **Audit frontend state and listeners** — read frontend/src/hooks/agentState.ts, agentEventReducer.ts, App.tsx: transcript/activityLog caps, event listener registration/teardown, polling intervals.
- [x] 6. **Write the review report** — consolidate verified findings (each with file:line, mechanism, severity) into .coding/reviews/2026-06-14-mem-perf-threading-review.md via write_review_report; mark previously-reported items as fixed-at-HEAD or still-open.
