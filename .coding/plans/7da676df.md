# Plan: Cancel/delete a scheduled steer via an "x" on each pending steer

## Goal
Add an "x" button to each pending steer in the InputBar backlog that cancels it: removes the frontend bubble AND tells the backend to drop the queued steer before it's injected, so the steer truly does not run.

## Kind
implementation

## Context


## Steps
- [x] 1. **Add CancelSuggestion command variant** — In src/runtime/channels.rs (enum AgentCommand, lines 59-83), add a new variant `CancelSuggestion(String)` right after `Suggestion(String)` (line 66) with a doc comment: cancel a queued steer whose text matches — drops it before injection (the x on a pending steer). Handled by StopReason::fold (removes the text from any Steer/InterruptWithSteers/CompactWithSteers list) and the provider-request buffer; a no-op between turns. This breaks exhaustive matches; later steps add arms. Do NOT add #[allow(...)].
- [x] 2. **Handle CancelSuggestion in StopReason::fold** — In src/agent/loop_impl.rs fold() (lines 337-379), add an arm for C::CancelSuggestion(s): for Some(Steer(texts)) | Some(InterruptWithSteers(texts)) | Some(CompactWithSteers(texts)), do texts.retain(|t| t != &s); then collapse an empty vec: Steer([]) -> set current to None (turn continues, no soft-stop); InterruptWithSteers([]) -> Interrupt; CompactWithSteers([]) -> Compact. Implement cleanly (take + reassign). Add a cancel(s) test helper next to steer() at line 1159 returning AgentCommand::CancelSuggestion(s.into()). Add unit tests in mod tests (line 1164+): fold_cancels_a_queued_steer (Steer([A]) then cancel(A) -> None); fold_cancel_preserves_other_steers (Steer([A,B]) then cancel(A) -> Steer([B])); fold_cancel_on_interrupt_with_steers_collapses_to_interrupt; fold_cancel_unknown_text_is_noop (Steer([A]) then cancel(Z) -> Steer([A])).
- [x] 3. **drop_cancelled_steers helper + turn.rs paths** — (a) Add pub fn drop_cancelled_steers(buffered: &mut Vec<crate::runtime::AgentCommand>) in src/agent/loop_impl.rs near fold: collect texts from every CancelSuggestion(s) in the vec, then retain — drop all CancelSuggestion(_), and drop any Suggestion(s)/Prompt{text:s,..} whose text is in the cancelled set (order-independent). (b) src/agent/turn.rs provider-request select! (~893-948): add arm Some(AgentCommand::CancelSuggestion(s)) => { buffered_steers.retain(|t| t != &s); } next to the Suggestion/Prompt arms. (c) Before each summarization re-injection `for cmd in buffered` loop in turn.rs (~126 and ~478) call crate::agent::drop_cancelled_steers(&mut buffered);. (d) Approval re-injection (~1628-1640): widen the fold arm to cmd @ (Suggestion(_) | Prompt { .. } | CancelSuggestion(_)) => { StopReason::fold(&mut stop_reason, cmd); }.
- [x] 4. **Drain before inject in run_turn_with_retry** — In src/runtime/agent.rs run_turn_with_retry (lines 119-165): after `let Some(outcome) = outcome else { return AfterTurn::Continue };` move the stop reason out with `let mut stop_reason = outcome.stop_reason;` then drain `while let Ok(cmd) = cmd_rx.try_recv() { StopReason::fold(&mut stop_reason, cmd); }` (cmd_rx is &mut mpsc::Receiver<AgentCommand>). Then `match stop_reason { ... }` over the same arms (Steer/InterruptWithSteers -> inject loop unchanged; CompactWithSteers -> AfterTurn::Compact(steers); Cancel -> Cancelled; Compact -> Compact(vec![]); Clear -> Clear; Interrupt|None -> Continue). This closes the window where a CancelSuggestion arrives just after the turn soft-stopped but before injection.
- [x] 5. **Between-turns no-op arm + exhaustive matches** — In src/runtime/agent.rs main loop match cmd (lines 357-358), add arm `AgentCommand::CancelSuggestion(_) => { /* nothing queued between turns — no-op */ }` near the Suggestion arm (~510). Then run cargo check and fix every non-exhaustive-match error by adding the appropriate arm. Catch-all sites (approval.rs:211, context.rs:287, dispatch.rs:427 & 566) already buffer `other` — no change. The re-injection `other =>` log arms (turn.rs ~140/500, runtime/agent.rs ~771) stay unreachable for CancelSuggestion after the drop_cancelled_steers/fold widening but must still compile. Do NOT add #[allow(...)].
- [x] 6. **cancel_suggestion Tauri command + register** — In src-tauri/src/ipc/agent.rs mirror send_suggestion (lines 223-231): add `#[tauri::command] pub async fn cancel_suggestion(state: State<'_, IpcState>, agent_id: AgentId, text: String) -> Result<(), IpcError> { send_cmd(&state, agent_id, AgentCommand::CancelSuggestion(text), "cancel suggestion").await }` with a doc comment. In src-tauri/src/main.rs generate_handler! (line 669) add `ipc::agent::cancel_suggestion,` right after `ipc::agent::send_suggestion,` (line 671).
- [x] 7. **Frontend removeSteer + cancelSuggestion IPC** — In frontend/src/hooks/useAgentStore.ts: add to the AppState interface near addSteer (~491) `removeSteer: (id: AgentId, steerId: number) => void;` with a doc comment (remove a single pending steer by id — the x on a backlog entry; caller also sends a backend cancel). Implement near clearSteers (~904): `removeSteer: (id, steerId) => set((s) => { const agent = getOrCreate(s.agents, id); return { agents: { ...s.agents, [id]: { ...agent, steers: agent.steers.filter((e) => e.id !== steerId) } } }; })`. In frontend/src/lib/tauri.ts add after sendSuggestion (line 53): `export async function cancelSuggestion(agentId: AgentId, text: string): Promise<void> { await invoke("cancel_suggestion", { agentId, text }); }` with a doc comment.
- [x] 8. **InputBar x button on pending steers** — In frontend/src/components/layout/InputBar.tsx: import cancelSuggestion from ../../lib/tauri (add to the import block lines 9-19); add `const removeSteer = useAgentStore((s) => s.removeSteer);` near line 43. In the steers.map render (lines 636-652): for pending steers (steer.status !== landed) add an X button with className ml-auto shrink-0 ... and title "Cancel this steer", onClick calls `removeSteer(activeAgent!, steer.id); cancelSuggestion(activeAgent!, steer.text).catch((e) => console.error("failed to cancel suggestion:", e));`. Keep the existing landed badge for landed steers. The X icon is already imported (line 6). activeAgent is non-null here (steers only exist for the active agent) but use activeAgent! to satisfy TS.
- [x] 9. **Tests + build green** — Rust: fold unit tests from step 2. Add an integration test in src/agent/tests.rs mirroring mid_turn_steer_soft_stops_and_carries_pending_steer (~3771): send a steer mid-stream, then send CancelSuggestion with the same text before the turn ends, assert the follow-up turn does NOT run and no SuggestionInjected event is emitted (the steer was dropped). TS: in frontend/src/hooks/useAgentStore.test.ts add a test: addSteer(ID,'a'); addSteer(ID,'b'); capture the first steer id; removeSteer(ID, firstId); assert steers length 1 and the remaining text is 'b'. Run `cargo test` then `"exit=$LASTEXITCODE"` (do NOT pipe). Run the frontend suite (check package.json scripts); fix failures. Ensure zero warnings (deny(warnings)).
