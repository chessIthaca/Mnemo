# Plan: Reviewer report failures are silent — harden write_review_report + honest finish notifications (5b46674d)

## Goal
Make reviewer report-writing failures loud and recoverable (backlog 5b46674d): (1) write_review_report's verdict rejection shows the expected format verbatim PLUS the received first line, so a reviewer fixes it in one retry; (2) the finish notification states whether a report was written — the project-relative path (".coding/reviews/<file>", no more \\?\C:\AgenticCoding alias) or an explicit "no report was written" — and the failed-reviewer protocol (distinct text + parent latch) fires for ANY report-less reviewer, not just !success ones; (3) 3 consecutive write_review_report failures abort the reviewer's turn with a distinct final error (not reset by other tools' successes, unlike tool_error_count); (4) report filenames date-prefix from the system clock (a reviewer's stale date is normalized away) and finish resolves the project-relative path.

## Kind
bug_fixing

## Context
Edit sites (all verified by reading current code): (1) src/tool/agent/write_review_report.rs — verdict_error at :228-230 (static string), append-without-verdict error at :237-241, success/with_data paths at :270-287 use target.display() (canonicalized verbatim \\?\ path), reviewer supplies the full filename incl. date prefix; tests at :291-633 (several assert dated filenames like "2026-04-04-review.md" — must be updated for normalization). (2) src/runtime/turn_resolve.rs — completion_suggestion_text :246-261 (None arm = generic "read its report"), reviewer_failure_suggestion_text :225-233; tests completion_suggestion_tests :438-484 (generic_text_when_no_report_path asserts the OLD generic text — update). (3) src-tauri/src/ipc/events.rs — notify_parent_on_completion :788+; reviewer_failed_no_report = !success && role==reviewer && no report (drop !success); tests in mod tests :1441-1568 show the harness pattern (AgentManager + AgentLoop wiring). (4) src-tauri/src/console.rs — notify_parent :737-779, same condition at :757-758. (5) src/agent/turn.rs — TurnState :60-104 (add review_report_failures), the MAX_RETRIES abort at :654-673 (insert the distinct abort BEFORE it), the tool_error_count reset at :1139-1140, the last_review_report capture at :1154-1163 (add increment/reset next to it). (6) src/agent/mod.rs — MAX_RETRIES :49 (add REVIEW_REPORT_MAX_FAILURES=3 next to it). (7) src/agent/tests.rs — max_retries_aborts_after_consecutive_tool_errors :1316 is the pattern to mirror. (8) src/tool/workflow/plan.rs — finish's path validation :1266-1282 (canonicalize as-given only; add project-root-relative + reviews-dir-basename fallbacks). (9) Reusable date helper: crate::tool::memory::format_epoch_date (src/tool/memory/mod.rs:333, pub(crate), Hinnant civil_from_days, UTC YYYY-MM-DD). The reviews dir is <project>/.coding/reviews by construction (factory.rs reviews_dir() :760-765 derives it from plans_dir exactly like FinishTool), so the display path ".coding/reviews/<file>" is always honest in the app. Design decisions: date normalization strips a leading date-shaped YYYY-MM-DD- and prepends today (appends resolve as-given first, then the normalized name, so the chunked protocol survives stale-name appends and midnight rollover); the reviewer-failure protocol fires for ANY report-less reviewer (a reviewer's only output channel is the report — finishing cleanly without one means the review did not happen); the dedicated failure counter resets ONLY on write_review_report success (not on other tools' successes) so interleaved read_files calls can't mask a stuck report writer.

## Steps
- [x] 1. **Reproduce with failing regression test** — Write a regression test that reproduces the defect (constitution: every defect gets a regression test that fails without the fix and passes with it). Run it and confirm it FAILS.
- [x] 2. **Document root cause** — Investigate and document the root cause. memory_write a BUG: record (symptom → root cause → fix + regression test name, ≤600 chars).
- [x] 3. **Minimal fix** — Apply the minimal fix that makes the regression test pass. Do not refactor unrelated code.
- [x] 4. **Verify** — Run the regression test + the full test suite (cargo test unpiped, warning-free). Record the regression test name via update_plan (regression_test field) — finish is blocked without it.

## Bug
Reviewer subagents finish without writing reports and the harness finish notification is identical either way ("read its report and combine the results into a plan", path only sometimes present) — the parent needs shell forensics to discover no report exists. write_review_report's verdict-line validation rejects reports with a static error showing neither the expected format verbatim nor the received first line, so reviewers guess; some recover on retry, some exhaust their turns and die report-less (live-observed: 2 of 5 reviewers in the 2026-12-30 full-review session, two different model families).

## Regression test
verdict_rejection_names_received_first_line
