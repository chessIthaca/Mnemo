# Plan: Close skill target_state invariant at registry load (UI enter_skill path)

## Goal
Validate target_state in SkillRegistry::load_dir so a hand-edited skill TOML with target_state = "subagent"/"skill"/"reviewing" is rejected at load (logged + skipped), closing the UI-initiated enter_skill path that plan 3fb064c4 left open. Add a regression test that fails without the gate and passes with it.

## Kind
bug_fixing

## Context


## Steps
- [x] 1. **Reproduce with failing regression test** — Write a regression test that reproduces the defect (constitution: every defect gets a regression test that fails without the fix and passes with it). Run it and confirm it FAILS.
- [x] 2. **Document root cause** — Investigate and document the root cause. memory_write a BUG: record (symptom → root cause → fix + regression test name, ≤600 chars).
- [x] 3. **Minimal fix** — Apply the minimal fix that makes the regression test pass. Do not refactor unrelated code.
- [x] 4. **Verify** — Run the regression test + the full test suite (cargo test unpiped, warning-free). Record the regression test name via update_plan (regression_test field) — finish is blocked without it.

## Bug
A hand-edited .coding/skills/*.toml with target_state = "subagent" (or "skill"), started via the UI skill button (enter_skill, src-tauri/src/ipc/agent.rs:808), passes spec.target_state straight to start_skill without validation — landing a MAIN-agent workflow in WorkflowState::Subagent (or Skill) with no tool allow-list, so allowed_tools panics on the next turn ("Subagent-state workflow must carry a tool allow-list", workflow/mod.rs:397-399). The LLM-reachable skill_start tool path was validated in plan 3fb064c4 (skill.rs:154-161), but the UI path was not. Pre-existing, not LLM-reachable, no shipped skill affected (merge_to_main targets "planning").

## Regression test
load_dir_skips_skill_with_non_lifecycle_target_state
