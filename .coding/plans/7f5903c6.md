# Plan: Fix open-existing-project landing on switch dialog instead of Mnemo UI

## Goal
Pressing Open on an existing project in the picker reliably lands the restarted app on the Mnemo agent UI for the chosen project — or, if unreproducible on the current build, hard evidence of that plus targeted hardening of the open flow's failure surfaces.

## Kind
bug_fixing

## Context
Static analysis exhausted — every link verified sound: switch_project (src-tauri/src/ipc/projects.rs:196 — validate → write marker → tauri::process::restart); marker helpers (src/config/mod.rs:479/495 + write_atomic:396, home-scoped ~/.mnemo via global_config_dir:424); reload resolution (src-tauri/src/main.rs:985-1016: marker → .coding check → Project::from_root → Ready with needs_project=false; marker absent → --project → cwd → NeedsProject picker); Tauri 2.11.5 restart verified via upstream source (spawn same exe+args then exit(0) — spawn failure means app CLOSES, not a stuck dialog); frontend one-shot getNeedsProject (App.tsx:161-265, error falls through to normal UI, never defaults to picker); Sidebar switch dialog is local useState(false) (Sidebar.tsx:34) so it cannot reappear post-restart; handleOpen (ProjectPicker.tsx:69-80) assumes "unreachable on success". Timeline: picker since 60d0001 (Aug 13), splash/overlay work 2c56c36+0ebd72b (Aug 27), bug filed Sept 3, no re-reports since. Bug is either runtime-empirical (needs live probe) or done-but-unmarked (pattern of 7ba83b5b/8b8f40d2/86c90ff6). Key probes planned: plant ~/.mnemo/.pending_project + spawn the exe from a non-project cwd and observe marker consumption/liveness/project-open side effects; check for single-instance plugin first.

## Steps
- [x] 1. **Reproduce with failing regression test** — Write a regression test that reproduces the defect (constitution: every defect gets a regression test that fails without the fix and passes with it). Run it and confirm it FAILS.
- [x] 2. **Document root cause** — Investigate and document the root cause. memory_write a BUG: record (symptom → root cause → fix + regression test name, ≤600 chars).
- [x] 3. **Minimal fix** — Apply the minimal fix that makes the regression test pass. Do not refactor unrelated code.
- [x] 4. **Verify** — Run the regression test + the full test suite (cargo test unpiped, warning-free). Record the regression test name via update_plan (regression_test field) — finish is blocked without it.

## Bug
Opening an existing project from the project picker (press Open) leaves the UI showing the switch-project/startup dialog instead of the Mnemo agent UI — the restarted app should land on the Mnemo UI for the chosen project (backlog 16e4a7f8, filed 2026-09-03).

## Regression test
switch_target_requires_coding_dir
