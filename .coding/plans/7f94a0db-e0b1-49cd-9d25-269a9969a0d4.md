# Plan: Add 'never sleep-wait for a subagent' rule to agent.md

## Goal
Add a constitution rule to agent.md: never use sleep/polling to wait for a spawned subagent — yield the turn and let the finish notification resume you.

## Context
agent.md (project constitution) at repo root. The "Standard plan closing sequence" → step 2 "Review" describes spawning a reviewer subagent via spawn_agent. This is exactly where the sleep-poll regression occurred: after spawn_agent, I blocked the turn with Start-Sleep polling instead of yielding and waiting for the "background agent finished" notification. Adding a hard rule here keeps it co-located with the instruction that triggers the pattern.

## Steps
- [x] 1. Add a concise bullet under the Review step (step 2) of the 'Standard plan closing sequence' in agent.md stating: after spawn_agent, do NOT sleep/poll for the report — end the turn and wait for the 'background agent finished' notification to resume you.
- [x] 2. Run cargo test (constitution requires it before step completion; no code changed but confirms the repo still builds).
- [x] 3. Spawn a read-only reviewer subagent to review the agent.md change; write findings to .coding/reviews/. Fix any findings.
- [x] 4. Commit to the current feature branch.
