# Plan: README: lift the deck's sharper prose (failure modes, harness argument, proof numbers)

## Goal
Lift the presentation deck's sharper copy into README.md's three narrative sections — "Why Mnemo exists" (five failure modes + allocation punchline + proof-by-construction numbers), "Key ideas" (harness-vs-chat, spec-on-disk, memory-as-defect-control one-liners + fix the two broken bullets), and "The enforced workflow" (structural-vs-suggestion argument) — with refreshed, date-attributed proof numbers. No screenshots, no changes to features/building/config sections.

## Kind
implementation

## Context
Copy source of truth: docs/deck_content.py (gitignored — the deck stays untracked; only the lifted prose lands in README.md). Screenshots explicitly SKIPPED per user request — no image work, no docs/ un-ignoring, no changes to Key features / Building / Configuration sections. Deck tone guidance: "smaller / less capable / faster" model, not "cheap" — the argument is fit for purpose, not price. The deck's numbers (421 plans, 432 reviews, 157 knowledge records, 93.9% cached, ~160k lines in 23 days) date from 2026-09 — refresh where quoted (step 1) and date-attribute. The two broken Key-ideas bullets (README.md lines 22 and 26) are pre-existing defects fixed in the same pass.

## Steps
- [x] 1. **Refresh the proof numbers** — Run the deck's own refresh commands (docs/deck_content.py slide 19 notes): `git rev-list --count HEAD`; count `.coding/plans/*.md`, `.coding/reviews/*.md`, and `.coding/knowledge/**/*.md` (PowerShell: `(Get-ChildItem .coding/plans -Filter *.md).Count` etc.); test counts from the last full run are root 2158+16, src-tauri 293+4+2 — re-run if unsure. These feed the proof paragraph in step 2, quoted with an "as of 2027-01" attribution.
- [x] 2. **Rewrite "Why Mnemo exists" (README.md:9-16)** — Open the section with the five failure modes adapted from docs/deck_content.py slide 2: one model does the thinking AND the typing and treats them the same; you re-explain every session what it forgot; ask for one function, get 400 lines across nine files to read; a defect found after merge costs multiples; one interruption and the work in flight is gone. Land the punchline: "Not a model problem — an allocation problem." Keep the existing conviction paragraph and the four bullets (they are accurate), changing only "a cheap, fast model" → "a smaller, faster model" in the routing bullet. Close with a proof-by-construction paragraph: Mnemo is written by Mnemo through its own loop (planned with a strong model, executed by a smaller one, independently reviewed — every change leaves a plan and a review on disk), quoting the refreshed numbers from step 1, date-attributed.
- [x] 3. **Sharpen "Key ideas" (README.md:18-26)** — Weave in the deck's one-liners: "A chat window can suggest this. A harness enforces it." (slide 5); "The spec is a file on disk, not a chat message" (slide 5); "A forgotten decision is a defect" — memory as defect-rate control, "the cost is not the retyping, it is the wrong decision" (slide 9); "Rework is the work you do twice" (slide 11); "Editing with a map, not a torch" (slide 15). Fix the two broken bullets: line 22 ("**Tool usage is optimized.** by implementing semantic code indexing and have optimized tools for development of software." → grammatical rewrite, e.g. "**Tool usage is optimized.** Semantic code indexing and purpose-built tools replace grep archaeology — the right file, first try.") and line 26 ("**Optimizing Context* by removing common patterns of low value from shell, build and other common commands.." → closed bold + single period, e.g. "**Context is the scarce resource.** Shell/build noise never enters the window, occasional tool families stay deferred until wanted, and the tool array is byte-stable so provider prompt caches hit.").
- [x] 4. **Sharpen "The enforced workflow" (README.md:83-93)** — After the existing diagram and 4 numbered items, add one short paragraph adapted from deck slides 5/8 notes: spec-driven and test-first are not new ideas — everybody agrees with them and everybody skips them at 2am under a deadline; a chat window can only ever suggest them, a harness makes them structural (in Planning no write tools exist — absent from the request schema, not discouraged; bug plans are locked to the reproduce → root-cause → fix → verify skeleton; finish is blocked until the verify step names a regression test, validated against the code graph). Close with: "If your guardrail is a sentence in a prompt, you cannot safely downshift the model. If your guardrail is the tool schema, you can — the rails are what make the smaller executor viable."
- [x] 5. **Verify + close** — Run `cargo test` (workspace, unpiped, read the `test result:` lines — README.md is not compiled but the constitution requires the run), eyeball the rendered markdown for broken bold/list markup, then the closing sequence: spawn reviewer (role:"reviewer", omit model) → fix every finding → re-run tests → commit to the current branch → finish.
