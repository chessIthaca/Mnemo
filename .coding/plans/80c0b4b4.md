# Plan: Backlog 8863159f close-out: verify selectable/copyable transcript (context-menu guard)

## Goal
Close backlog 8863159f (selectable/copyable transcript text): verify the shipped context-menu fix end-to-end, run its tests, mark done, land the bookkeeping with a review round.

## Kind
implementation

## Context
Backlog 8863159f dispatch (run-all, unattended): "I want to select things in the agent window so I can copy them." VERIFIED ALREADY SHIPPED as T3 of backlog batch 554010fd (commit 0ce7c01, merged to main via 0e12ffb, in current branch history): frontend/src/lib/contextMenu.ts — the module doc cites backlog 8863159f with the user's exact words; T3 added keepNativeMenu(isEditable, overSelection) (:79, pure decision), activeSelectionIncludes (:91-98, non-collapsed selection + containsNode(el, true) partial containment, Chromium+WebKit), wired into installContextMenuGuard's capture-phase listener (:112-123 — preventDefault unless editable or over-selection, so the native Copy item serves drag-selected transcript/tool-output text); contextMenu.test.ts +21 (16 tests total incl. the keepNativeMenu matrix :43-58) and the README bullet (line 69: "select transcript or tool-output text and right-click it to Copy"). Selection itself was never blocked — only the Copy menu was (the doc explains this). This is the third consecutive already-shipped close-out in this batch (41f39672, 2980ca67 — both reviewed PASS with the same pattern). DECISIONS: (1) verify-and-close, implementation kind with a reviewer round (matching the last two close-outs); (2) step 1 also confirms the one link not yet seen live — where installContextMenuGuard is installed (expected App-level useEffect) — and runs the covering tests (contextMenu + answerSelect filters); (3) avoid brace globs in live searches (stale-binary quirk, recorded BUG memory).

## Steps
- [x] 1. **Verify the context-menu fix end-to-end** — Confirm the one link not yet seen live: where installContextMenuGuard() is installed (search frontend/src for installContextMenuGuard — expected an App-level useEffect with cleanup; verify the guard is actually mounted). Sanity-check transcript selectability (no user-select:none on transcript content — search for select-none/user-select in the chat components). Then run the covering tests: npm test --workspace frontend -- contextMenu answerSelect. Expect all green — any gap becomes the item's real work and gets fixed.
- [x] 2. **Close the backlog item + gates + commit** — backlog_status 8863159f → done (note: shipped in backlog batch 554010fd T3 / commit 0ce7c01 — keepNativeMenu + activeSelectionIncludes + capture-phase guard; verified + tests green this session; the batch review 2026-09-16 PASSED). Full gates: cargo test + npm test --workspace frontend (whole suite). Commit the backlog flip + plan file to wt/agenticcoder with a clear message.
- [x] 3. **Review round + finish** — Spawn the read-only reviewer (deepseek-v4-pro) over the bookkeeping commit: confirm the shipped-fix claims (the guard is installed, the decision logic covers the select-then-right-click-Copy scenario, tests exist and pass), the backlog close is consistent, and the tree change is bookkeeping-only. Fix any findings, re-run tests, commit, then finish with the PASS report to mark the task done.
