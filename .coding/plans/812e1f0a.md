# Plan: Search optimization round: search_read index parity, literal TIP, definition-prefix nudge, pruned walk, gap-driven memory prompt

## Goal
Cut wasted search round-trips at the tool layer and make the memory prompt gap-driven: route search_read's literal queries through the FTS content index (parity with search), nudge regex-mode walks that could have been index-served, widen the symbol nudge to definition-prefixed queries (fn/struct/… X), prune ignored directories from the tree walk (107k stat calls → pruned descent), and re-balance the memory_search prompt trigger (keep the two MANDATORY triggers, make mid-session lookups gap-driven instead of reflexive) + supersede the "wait 2 weeks" DECISION record.

## Kind
implementation

## Context
Findings from the 2026-09-15 search/memory optimization analysis (working-memory tally + this session's own drift), now decided. DECISIONS (user delegated): (D1) literal-vs-regex engine gap → NUDGE, do NOT auto-route: store.rs:537 search_content builds ONE quoted FTS5 phrase (unicode61 tokenizer splits on punctuation) so a regex-mode pattern auto-routed to the index would silently change match semantics — `foo|bar` regex matches literal "foo|bar" but the phrase queries tokens foo+bar (over-match); `fn foo(` matches `foo` inside `foobar`? no — phrase under-matches non-word boundaries. Review B1 (search.rs test fallback_literal_walks_even_with_index, :663) already ruled: only walk keeps literal semantics exact for non-literal calls; the exactness argument extends to auto-routing regex-mode. So: advisory TIP when a regex-mode walk COULD have been index-served. (D2) memory prompt → keep BOTH MANDATORY triggers verbatim (bug record_type:"bug" + planning pointer-first — pinned by prompt.rs tests stable_head_carries_phase3_retrieval_habits :1072-1098, byte-stable head) but replace the reflexive mid-session clause "Also use it whenever auto-recalled context looks thin (auto-recall covers only the latest message)" (prompt.rs:164-165) with GAP-DRIVEN wording (search only when auto-recall/rider hits don't cover the question) — this session's recall batch showed 2/5 relevance and the 2026-09-15 tally showed 6/9 redundant mid-session lookups; SUPERSEDE the 2026-09-15 DECISION record "wait ~2 weeks" (the softening IS the fix; tool-scoped counters verify post-hoc). (D3) the avoidable-search finding (caller-question answered by search instead of graph_context) is behavior-level: covered by HOW memory 70f2e85e + counters, no code — BUT it exposed a real code gap: definition-prefixed symbol queries ("fn search_content", "pub fn foo") never earn the symbol nudge because is_bare_identifier (search.rs:215-222) rejects spaces. GROUNDING (verified): search.rs execute :362-531 (index path :411-441 gated args.literal && !\n; walk :443-527 uses glob::glob on the FULL tree then per-file should_search :63 → 107,206 stat calls enumerated only to be skipped); try_index pub(crate) :256-303 (None on unpopulated/untokenizable/glob-starved); with_note :307 / merged_note :318 (line-1 notes, survive end-truncation); symbol_nudge :233-246 (is_bare_identifier gate → symbol_id exact lookup, id embedded); search_read.rs has NO index path (always engine: walk, :208/:234; shares merged_note/with_note via search:: :141/:210/:232) and its own walk loop :167-205; store.rs search_content :537-566 (quoted phrase, bm25); steering_stats.rs MARKERS :121-127 (5 kinds, array fixes order; detect() tool-scoped; MarkerKind::detect per-kind rule; SEARCH_NUDGE_MARK const; marker strings are a public-output contract — pin any new one in the same commit); SteeringStatsSnapshot by_marker renders dynamically in the Trace tab (maps over the array — a 6th kind is wire-additive); prompt.rs TOOL_STRATEGY :149-174 (memory_search bullet :160-165; ORDER pinned by tool_strategy_lists_graph_tools_before_search + stable_head_byte_stable_across_workflow_changes — wording edits only, NEVER reorder); README.md :53 steering bullet + PLAN.md Agent-tools search bullets need the new clauses; search schema descriptions (search.rs :336-353, search_read.rs) state engine behavior. Constraints: #![deny(warnings)] both crates, doc comments on public items, cargo test per step (unpiped, read test result:/exit), npm test + npm run build for frontend (none planned — Rust+docs only), no #[allow], platform-neutral, never commit to main; branch wt/agenticcoder (create_plan default reuse). Behavioral residual (no code): my own drift — caller/symbol questions answered via search/read (this session: tool/mod.rs guess-read, "fn search_content" pattern, search_read.rs engine-site scan) — nudges+counters+HOW memory measure this; verify in a future session via get_steering_stats.

## Steps
- [x] 1. **search_read: route literal single-line queries through the FTS index** — src/tool/agent/search_read.rs currently ALWAYS walks (engine: walk, lines 208/234) even for literal:true — search.rs has had the FTS path since the content index landed. Mirror search.rs's routing: in execute, after glob validation, when args.literal && !args.pattern.contains('\n') && a graph is present, call the EXISTING shared helper search::try_index(g, &args.pattern, args.glob.as_deref()) (search.rs :256-303 — pub(crate), handles unpopulated index/untokenizable/glob-starve by returning None → transparent walk fallback). On Some(ix): keep search_read's value-add — READ the top matched files: reuse its existing per-file read machinery (the read loop and per-file byte/line caps) over the ix.hits files (dedup by path, cap at the existing read_count), and render the summary line '{total} matches in {files} files (engine: index); reading top {read_count}' followed by the file sections — the output shape must match its current walk rendering (search::with_note(note) wrapper, same section format). Zero index hits → same 'no matches found' message with '(engine: index; N files indexed)'. Broken-regex fallback (compile_with_fallback note) and regex mode and \n-spanning patterns MUST keep the walk path untouched (review B1 parity — exact literal semantics only on the literal:true path). Tests in search_read.rs mirroring search.rs's: literal_query_uses_index_engine (assert engine: index + the matched file's CONTENT is actually read/rendered); regex_query_walks_even_with_index (existing behavior pin); fallback_literal_walks_even_with_index (mirror of search.rs :663); empty-index store falls back to walk transparently (make_indexed_tool-style fixture helper exists in search.rs tests — replicate or reuse the pattern). Run cargo test.
- [x] 2. **Literal-steering TIP when a regex-mode walk could have been index-served (+ 6th steering marker, fired-only)** — The observed waste (tally: literal 'RECALLED CONTEXT' walked 1,481 files in REGEX mode — only literal:true uses the index). D1 decision: nudge, never auto-route (FTS phrase semantics ≠ regex semantics, store.rs :537; B1 precedent). In src/tool/agent/search.rs add `fn is_index_eligible_regex(pattern: &str) -> bool` — single line, non-empty, NO regex metacharacters (none of `\ . * + ? ( ) [ ] { } | ^ $`) and is_tokenizable (:204, at least one alphanumeric) — i.e. a pattern whose regex and literal matchers are equivalent AND the index could serve. Compute a third note component: when NOT args.literal AND is_index_eligible_regex(pattern) AND a graph is present AND its content index is populated (graph.content_coverage() → with_content > 0; two COUNT queries, cheap) AND symbol_nudge did NOT fire for this call (symbol steering wins; suppress the TIP to avoid double-nudging — doc-comment this priority) → TIP text, EXACTLY: 'TIP: pattern has no regex metacharacters — literal:true would use the content-index engine (one indexed lookup instead of a tree walk)'. Merge into the ONE line-1 note: extend merged_note (:318) or build a 3-way join — keep with_note(:307) prepending so the note survives end-truncation. Apply the SAME TIP to search_read.rs (post step 1 both tools have an index path, so the advice is true for both) via the shared helper — factor `pub(crate) fn literal_tip(graph: &Option<Arc<CodeGraph>>, pattern: &str, literal_mode: bool, symbol_nudged: bool) -> Option<String>` in search.rs. steering_stats.rs: add MarkerKind::LiteralTip (6th kind): marker() = 'TIP: pattern has no regex metacharacters' (prefix of the emitted text — contains-match), targets() = &[] (fired-only, like RecallRider: observe_call sees only tool NAMES, a 'switch' to search-with-literal:true is indistinguishable from search-regex, so do not pretend to measure switches), label 'literal-tip', index() = 5, MARKERS array grows to [.., LiteralTip] (len 6; Inner::counts [(0,0); 6]), detect(): tools search|search_read, first-line contains (the note is line-1 via with_note). Update the module doc's marker enumeration. Tests: search.rs — metachar-free single-line pattern in regex mode + indexed store → TIP present on line 1, engine: walk still; pattern WITH metachars ('foo|bar', 'search(ed)?', 'a.b') → no TIP; literal:true mode → no TIP (already index-routed); no store → no TIP; punctuation-only pattern ('->') → no TIP (untokenizable); TIP suppressed when the symbol nudge fires (bare identifier naming an indexed symbol → nudge present, TIP absent). steering_stats.rs — extend the MATRIX const with ('search', LiteralTip, 'read_files', 'TIP: pattern has no regex metacharacters — literal:true would use the content-index engine\nsome line\n[exit code: 0]') and assert fired-only (next call to search does not switch). Run cargo test.
- [x] 3. **Widen the symbol nudge to definition-prefixed queries (fn X, pub struct X, …)** — The avoidable-search gap: 'fn search_content' (a symbol query) earned NO nudge because symbol_nudge gates on is_bare_identifier (search.rs :237), which rejects the space. In src/tool/agent/search.rs add `fn strip_definition_prefix(s: &str) -> Option<&str>`: strip leading modifier keywords ('pub ', 'pub(...) ' any visibility, 'async ', 'unsafe ', 'extern ') then ONE definition keyword from {fn, struct, enum, trait, impl, mod, const, static, type, class, interface, function, def} + a single space; return the remainder ONLY if it is a bare identifier (reuse is_bare_identifier on the remainder) — anything else (two words, trailing '()', generics '<...>', missing keyword) → None. Rewire symbol_nudge (:233): `let name = strip_definition_prefix(pattern).unwrap_or(pattern);` then the existing exact symbol_id lookup on name (case-sensitive precision unchanged); the note text names the SYMBOL: when a prefix was stripped, emit "the symbol '{name}' (from pattern '{pattern}') is indexed — graph_context(id=\"{id}\") gives its definition + callers in one call; prefer the graph tools for symbol lookups (search is for text)"; bare-identifier patterns keep the EXISTING sentence byte-identically ("'{pattern}' is an indexed symbol — ...") — the 2026-09-15 spec pins that wording. Shared with search_read automatically (same fn). Update is_bare_identifier's doc + symbol_nudge's doc. Tests in search.rs: 'fn search_content' with search_content indexed → nudge fires with the id; 'pub fn foo' / 'async fn foo' / 'struct Bar' → fire; 'fn foo bar' (two words) / 'fn foo()' / 'impl Foo for Bar' / bare keyword 'function' → no nudge; unknown symbol 'fn zzz' → no nudge (symbol_id None); every existing bare-identifier nudge test (bare_identifier_symbol_searches_earn_the_graph_nudge in search.rs + the search_read.rs twin) must pass UNCHANGED. NOTE: step 2's TIP suppression interacts — 'fn search_content' is metachar-free so it would also be literal-eligible; the suppression rule (symbol nudge wins) must cover the WIDENED nudge too (add one test: 'fn <indexed-symbol>' → nudge present, TIP absent). Run cargo test.
- [x] 4. **Prune ignored directories during the tree walk (both tools, shared helper)** — search.rs's walk (:443-505) and search_read.rs's walk (:167-205) use glob::glob on root+'/**/*' then per-file should_search (:63) — this session's tally output showed 'skipped 107206': 107k stat calls under node_modules/target/.git/dist ONLY to skip them. Read src/codegraph/walk.rs first — its content-index pass already walks every SEARCHABLE file pruning ignored dirs (see walks_every_searchable_file_for_the_content_index); reuse its ignore-dir set/mechanism if pub, else extract the basename set from should_search into a shared const. Add ONE shared walker in src/tool/agent/search.rs: `pub(crate) fn walk_searchable(root: &Path, glob: Option<&str>) -> std::io::Result<(Vec<std::path::PathBuf>, u64)>` — recursive descent from root, SKIP descent into ignored dir basenames entirely (never stat their children), sort entries per directory for deterministic order, filter files through the validated glob (glob::Pattern::matches_path, default '**/*') AND should_search (kept as defense-in-depth for files inside non-ignored dirs), return (paths, skipped) where skipped now counts ignored DIRECTORIES pruned (doc-comment the semantic change — the old per-file count required enumerating the very tree we prune). Rewire BOTH walk paths (search.rs :443-505, search_read.rs :167-205) onto it — keep the early-exit-at-MAX_MATCHES + per-file caps + CRLF-safe matching byte-for-byte (only the FILE ENUMERATION changes). Output line: '(searched {files_searched}, skipped {skipped} ignored dirs, engine: walk)' — update the two tests that assert the old 'skipped {n}' phrasing (search.rs no_matches :592 asserts 'no matches' only — check; search_read tests around :456 assert 'engine: walk') and the schema/README/PLAN wording in step 6. Tests: fixture tree with node_modules/x.js, target/y, dist/z + a real match file → the matches are identical to the old glob walk (equivalence pin), node_modules content NEVER searched (a planted needle in node_modules must not match), skipped-count reports pruned DIRS (assert the new phrase), determinism (two calls, same order), glob filter still honored on the pruned walk. Run cargo test.
- [x] 5. **Gap-driven memory prompt + supersede the wait-2-weeks DECISION record** — D2 decision (user delegated). src/agent/prompt.rs TOOL_STRATEGY memory_search bullet :160-165: KEEP both MANDATORY triggers VERBATIM ('MANDATORY before planning non-trivial work (no record_type — a pointer-first bundle...' and 'MANDATORY before diagnosing any bug (record_type:"bug" ...') — pinned by stable_head_carries_phase3_retrieval_habits (:1072) which must pass UNCHANGED. REPLACE the trailing clause 'Also use it whenever auto-recalled context looks thin (auto-recall covers only the latest message).' with gap-driven wording, e.g.: 'Mid-session, search memory only when the auto-recalled hits and the plan rider do not cover the question (auto-recall covers only the latest message) — an explicit lookup that returns what auto-recall already carried is a wasted round-trip; the search is for a GAP, not a reflex.' FIRST grep prompt.rs's test module for 'looks thin' — if any test pins the old clause, update that assertion in the same edit (documented); the two MANDATORY pins and tool_strategy_lists_graph_tools_before_search must stay green untouched. NEVER reorder TOOL_STRATEGY entries (byte-stable head constraint — wording change only, same position). Then memory_supersede the 2026-09-15 DECISION record 'Recall-rider vs explicit memory_search — measurement plan (do NOT de-emphasize yet)' (semantic tier): successor records the FINAL decision — both MANDATORY triggers stay (cheap indexed lookups, high-value: bug root-cause reuse + pointer-first planning bundle); mid-session memory_search is now GAP-DRIVEN per the new prompt wording (evidence: 6/9 redundant mid-session lookups in the 2026-09-15 tally + 2/5 relevance in this session's recall batches); the tool-scoped counters (get_steering_stats, recall-rider fired + the new literal-tip marker) verify effectiveness post-hoc instead of gating the change; rider age-gate (14d, c40d6b7) already removed the stale-digest noise. Keep the pointer to the knowledge file. Run cargo test (prompt tests).
- [x] 6. **Docs + schema descriptions + full matrix + closing sequence** — (1) README.md :53 steering bullet: extend the tool-result-steering clause — search_read now serves literal single-line queries from the content index (engine parity with search); regex-mode walks of metachar-free patterns carry a TIP pointing at literal:true (counted as the fired-only literal-tip steering marker); definition-prefixed symbol-shaped patterns ('fn X') earn the same id-embedded graph nudge as bare identifiers; the walk prunes ignored directories instead of enumerating them. (2) PLAN.md Agent-tools section: the search/search_read bullets' engine wording (literal→index in BOTH tools; regex→walk with the TIP; pruned walk) and the memory_search TOOL_STRATEGY summary if PLAN.md carries one (grep 'memory_search' — :124 area mentions the rider rule only; update if the prompt change is reflected there). (3) search.rs (:336-353) + search_read.rs schema descriptions: search_read's description gains 'Literal queries use the content index when populated (engine: index), walking otherwise'; search's description mentions the literal TIP. (4) memory_write nothing more — the SPEC for this round lands at finish. (5) FULL matrix UNPIPED reading test result:/exit codes: cargo test at root; cd src-tauri; cargo build; (frontend untouched — skip npm unless any TS file changed, then npm test + npm run build). (6) Closing sequence: spawn role:'reviewer' over ALL uncommitted changes (self-contained task: goal + changed files; focus = TIP text ↔ marker string parity (any drift silently zeroes the literal-tip metric), merged-note line-1 survival under truncation, search_read index-path output parity with its walk rendering, B1 exactness preserved (fallback/regex still walk), nudge-widening wording vs the pinned 2026-09-15 spec sentence (bare-identifier text must be byte-identical), walk-pruning equivalence + skipped-semantics truthfulness, prompt MANDATORY pins untouched + no TOOL_STRATEGY reorder, docs sync, platform neutrality, no #[allow]); fix EVERY finding; re-run the matrix; commit to wt/agenticcoder including the review report; if FINDINGS, round-2 verify reviewer (PASS required) before finish.
