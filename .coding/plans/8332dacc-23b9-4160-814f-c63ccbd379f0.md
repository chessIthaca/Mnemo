# Plan: Diagnose 2026-08-23 4:52 AM freeze from hang-watchdog evidence

## Goal
Diagnose the 2026-08-23 4:52 AM app freeze from the hang-watchdog report + provider logs, and pin down where the main thread hung (code location + mechanism). No source changes — produce a findings report + fix recommendations.

## Kind
research

## Context
User reports app froze again. Newest hang watchdog report: .coding/logs/hang-1787475154907.txt (8/23/2026 4:52:34 AM), traces.jsonl + provider-errors.jsonl last write 4:59:45 AM. HEAD = 1fc6ce8 (includes fix/trace-freeze lock-inversion fix merged 2026-08-22) and the hang watchdog (f4f1caf, merged) — so this incident has watchdog evidence for the first time. Prior freezes: 2026-04-19 (F1 blocking git poll — fixed), 2026-08-18 (R1 set_size / R2 rAF buffering — fixed), 2026-08-22 (trace.rs lock inversion — fixed). Watchdog report fields: fired_at unix ms, stall ms, episode, main-thread CPU busy% over the stall window (≈100% = busy loop on main thread; ≈0% = blocked in lock/native call), 64-entry activity ring of agent events.

## Steps
- [x] 1. **Read the new watchdog report + 8/22 comparison set** — Read .coding/logs/hang-1787475154907.txt (the 8/23 4:52 AM incident) plus 2-3 of the 8/22 reports (hang-1787423496567, hang-1787422305857, hang-1787420974274). Extract: stall duration, episode number, main-thread CPU busy%, and the last ~10 activity-ring entries before each fire. The CPU% discriminates busy-loop vs blocked.
- [x] 2. **Correlate provider logs around the hang** — Tail .coding/logs/traces.jsonl and .coding/logs/provider-errors.jsonl around 8/23 4:40–5:00 AM: which provider/model was streaming, were there 429s/aborts, what was the last completed LLM request. Compare with the activity ring to see what the app was doing when the main thread stalled.
- [x] 3. **Map the stall signature to code** — Use the CPU busy% + last activity notes to rank hang mechanisms: (a) ≈0% busy → main thread blocked in a lock/sync IPC/native call — check run_on_main_thread callers, mutexes on the main thread, WebView2 sync calls; (b) ≈100% busy → spin loop in frontend/re-render storm or a busy Rust loop. Check Windows event log (AppHangB1 event 1002, ~4:52 AM 8/23) for corroboration. Cite exact files/lines.
- [x] 4. **Write findings report + memory** — Write .coding/reviews/2026-08-23-freeze-diagnosis.md: verdict, evidence, ranked findings with file:line, recommended fix plan (smallest first). Then memory_write a semantic DIAGNOSIS record (or supersede the 2026-08-20 one if it generalizes) pointing at the report.
