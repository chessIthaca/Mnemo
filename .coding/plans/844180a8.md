# Plan: MCP follow-ups batch: per-project config, prompts/resources/sampling, OAuth

## Goal
Close the three dispatched MCP follow-ups in one batch: (1) per-project .coding/mcp.toml union-merged over the global file (project wins; source-tagged Settings list + split save), (2) MCP prompts + resources exposed as capability-gated per-server agent tools and sampling/createMessage declined cleanly (never hang a server), (3) OAuth 2.1 authorization-code + PKCE flow for remote servers with loopback redirect, tokens in keys.toml, and automatic 401 refresh.

## Kind
implementation

## Context
Unattended run-all batch closing THREE queued MCP follow-ups (04ae22ef in_flight; 414a85bc + f51abac2 cant_resolve from a prior attempt that died in Planning — rolled back, no work done). Current code: src/mcp/{mod,stdio,http,tool}.rs from MCP v1 (McpManager with RwLock<Vec<McpServerDef>> servers + tokio Mutex connections + replace_servers; StdioMcpClient with reader task that currently DROPS server→client requests; HttpMcpClient with static headers only; McpTool/McpReveal; deferred groups mcp.<server>; Settings → MCP page complete). Key facts: (1) Project struct (src/project/mod.rs:54-68) computes paths in from_root — add mcp_toml. (2) KeyStore (src/config/keys.rs) is name→String (get/insert/save user-only perms, load_or_default) — usable for OAuth tokens by storing a JSON blob under a sanitized key. (3) stdio reader task owns stdout only; stdin is owned by the client struct — server→client requests need a shared writer handle to be answered (else servers hang). (4) Manager is built in main.rs build_brain (~line 1698) via with_mcp from config.mcp (GLOBAL only today); project root + config both in scope there. (5) IPC mcp.rs uses McpServerDef directly as wire type. (6) base64 crate present; NO sha2 — needed for PKCE S256. (7) Frontend Settings → MCP section complete with cards/edit/save/Test; lib/types.ts McpServer shape mirrors the Rust serde. SCOPE DECISIONS (unattended, best judgment, to document): Phase 1 per-project = union-merge by name, project file wins; save splits by per-server source tag; new servers default source "global". Phase 2 prompts/resources/sampling = manager methods for prompts/list+get and resources/list+read; capability-gated per-server meta tools (mcp__<server>__get_prompt, mcp__<server>__read_resource) registered at reveal with names/URIs embedded in descriptions (reveal-time snapshot — documented limitation); sampling/createMessage DECLINED with JSON-RPC -32601 via a shared-writer fix so servers never hang (full sampling = future work, documented); HTTP transport opens no server→client stream (documented). Phase 3 OAuth = authorization-code + PKCE S256 with loopback redirect listener (background task; IPC returns the authorize URL; frontend opens it), RFC 8414 discovery, token exchange + refresh, tokens in keys.toml as JSON blobs under sanitized keys (KeyStore reuse), auto-refresh on 401 + retry once; NO dynamic client registration — client_id required in the server def (documented limitation). Non-goals stay non-goals (trust flags, health UX — still pending backlog items). Workflow: implementation kind; reviewer subagent before finish; four suites green before commit; never commit to main.

## Steps
- [x] 1. **Phase 1: per-project mcp.toml (merge + source tagging)** — (a) src/project/mod.rs: add `pub mcp_toml: PathBuf` (coding_dir.join("mcp.toml")) to Project/from_root; update from_root_computes_paths test. (b) src/config/mcp.rs: `pub fn merge_servers(global: &[McpServerDef], project: &[McpServerDef]) -> Vec<McpServerDef>` — project defs win by name, non-colliding globals kept (order: project defs first then remaining globals); tests (project wins, global kept, both enabled states preserved). (c) main.rs build_brain: build the manager from the MERGED set: load project file via mcp::load_or_default(&project.mcp_toml) (forgiving — missing file = empty) then merge with &config.mcp. (d) src-tauri/src/ipc/mcp.rs: wire DTO `McpServerWire { #[serde(flatten)] def: McpServerDef, source: String }` ("global"|"project"); mcp_list_servers derives source per name from the project file (read via the Project in state — state.project.root lock → mcp_toml path); mcp_save_servers takes Vec<McpServerWire>, validates merged uniqueness (validate_set on merged), splits by source, writes BOTH files (global → global_config_dir/mcp.toml, project → project.mcp_toml via the Project in state), replace_servers(merged), syncs config.mcp to the GLOBAL subset. (e) frontend: lib/types.ts McpServer gains `source?: "global" | "project"`; McpSection cards show a source chip + a per-card source select (global/project, default global for new servers); save passes source through. (f) lib tests: merge_servers; frontend test: source round-trips through serializeMcpServers (extend canonicalMcp).
- [x] 2. **Phase 2: prompts, resources, sampling** — (a) src/mcp/stdio.rs: share the child stdin with the reader task (Arc<tokio::sync::Mutex<ChildStdin>>); reader task answers any server→client REQUEST (has id + method, no pending entry) with a JSON-RPC error response (code -32601, "method not supported by this client") — sampling/createMessage and every other server request is DECLINED cleanly instead of hanging the server; keep routing responses by id as today. (b) src/mcp/mod.rs: store the server's advertised capabilities from the initialize result on ServerConn (capabilities.prompts / capabilities.resources booleans); add `list_prompts(server)`, `get_prompt(server, name, args: Value)`, `list_resources(server)`, `read_resource(server, uri)` (requests prompts/list, prompts/get, resources/list, resources/read; parse text content). (c) src/mcp/tool.rs: generalize McpTool with a kind enum `McpToolKind::Tool(name) | GetPrompt | ReadResource` (execute routes per kind; GetPrompt takes {name, arguments?}, ReadResource takes {uri}); McpReveal: after tools, if capabilities.prompts → list_prompts and register `mcp__<server>__get_prompt` whose description embeds the available prompt names (cap ~20); if capabilities.resources → list_resources and register `mcp__<server>__read_resource` embedding available URIs (cap ~20); document the reveal-time snapshot limitation. (d) tests: stdio server-request decline (pure: build the -32601 response fn), manager prompt/resource request shapes via the fake client, kind routing in McpTool, reveal registers meta tools only when capabilities advertise. (e) http.rs: leave server→client stream unopened — add a module-doc line documenting that (sampling/resources over HTTP wait for the GET stream; backlog note).
- [x] 3. **Phase 3: OAuth 2.1 for remote servers** — (a) Cargo.toml: add sha2 = "0.10" (small pure-Rust; base64 already present) for PKCE S256. (b) src/config/mcp.rs: McpServerDef gains `#[serde(default, skip_serializing_if = "Option::is_none")] pub auth: Option<String>` ("oauth"; remote-transport only — validate rejects auth=oauth on stdio servers and unknown auth values) + optional `client_id: Option<String>` (pre-registered; NO dynamic client registration — documented limitation). (c) NEW src/mcp/oauth.rs: pkce() → (verifier, challenge) with challenge = base64url(sha256(verifier)); discover(origin) → RFC 8414 well-known (authorization_endpoint, token_endpoint); authorize_url(...) builder (response_type=code, client_id, redirect_uri, scope, state, code_challenge, method=S256); build_token_request body builder (pure, tested); loopback redirect: bind 127.0.0.1:0, wait for ?code+state (validate state), respond 200 and shut down. (d) Token store: `McpTokenStore { keys_path: PathBuf }` using KeyStore (load_or_default/insert/save — user-only perms preserved): key = "mcp-" + sanitized server name (non-alphanumeric → '-'); value = serde_json {access_token, refresh_token, expires_at_epoch}. (e) HttpMcpClient: when auth=oauth → Authorization: Bearer <access> from the store at request time; on 401 → refresh (grant_type=refresh_token) → persist → retry once; refresh failure surfaces the original 401. (f) Manager/factory: thread an `Option<Arc<McpTokenStore>>` into the manager (from the global keys.toml path at startup) so the default factory constructs OAuth-aware clients. (g) IPC `mcp_oauth_start(state, name) -> {authorize_url}`: resolves the def (must be remote+oauth), starts the spawned flow (listener + exchange + store), returns the URL for the frontend to open. (h) frontend: McpServer gains `auth?`/`client_id?`; remote cards get an auth select (none/oauth) + client_id field + "Connect (OAuth)" button (disabled unless saved; opens the returned URL via window.open). (i) tests: pkce shape/determinism, authorize_url params, build_token_request body, sanitize fn, auth-validation cases; network legs documented as manual.
- [x] 4. **Verification + docs** — Run all four suites green: root `cargo test` UNPIPED (expect ~1620+), `cd frontend; npm test`, `cd frontend; npm run build`, `cd src-tauri; cargo build`. Docs: README mcp.toml paragraph gains the per-project file + merge rule + auth fields (one short block); PLAN.md MCP subsection gains one paragraph per phase (merge, prompts/resources/sampling-decline, OAuth flow + limitations: no DCR, reveal-time snapshots, sampling declined). backlog: after finish, set 04ae22ef → done, 414a85bc → done, f51abac2 → done with notes pointing at this plan (25b5b710 + fd9b3517 stay pending — NOT dispatched).
- [ ] 5. **Reviewer + fixes + commit + finish** — spawn_agent role:"reviewer" over ALL uncommitted changes with a self-contained task (three-phase goal, changed-file list, focus: secret hygiene in the OAuth path (tokens never logged), merge correctness (project wins, no dup names), capability gating of the meta tools, sampling decline cannot hang a server, OAuth state validation + PKCE, filter gates unaffected, multi-platform, no #[allow]). Fix every finding, re-run the four suites, commit to the working branch with the round-1 report (+ round-2 PASS report if a re-review is needed), then finish. The finish gate requires a PASS verdict — if round 1 is FINDINGS, spawn the verification re-review after fixing.
