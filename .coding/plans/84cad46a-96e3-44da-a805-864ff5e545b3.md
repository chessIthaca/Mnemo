# Plan: New-project eager store seeding + phantom input scrollbar fix

## Goal
New projects get memory.db created AND the codegraph (source search) index built eagerly during create_project, so the first session after switching starts with semantic + source search ready; and the phantom gray scrollbar thumb in the prompt input textarea (and the identical Backlog inputs) is eliminated.

## Kind
implementation

## Context
Recovered after a crash lost the in-progress plan; user confirmed scope = "Eager-init at create time" (no starter semantic memories). FINDINGS: (1) Project::init (src/project/mod.rs:127-143) scaffolds only .coding/, plans/, agent.md — memory.db and codegraph.db are created lazily at app startup (MemoryStore::open src/memory/mod.rs:287; codegraph open+background-index src-tauri/src/main.rs:993-1032, gated on config.general.general.codegraph, default true src/config/general.rs:115+233). create_project IPC flow: src-tauri/src/ipc/projects.rs:62-107 (init → register in projects.toml), then frontend calls switch_project which RESTARTS the app — so seeding must happen inside create_project before the restart. MemoryStore::open only opens+applies schema; embedder is unused until writes, so seeding an empty DB with HashEmbedder is safe (no vectors/fingerprints; startup re-embed check is a no-op on empty DB). (2) Gray piece in the command enter box (user screenshot, red arrow at right edge inside the textarea) = the textarea's vertical scrollbar thumb (styled gray: globals.css:99-104 ::-webkit-scrollbar-thumb background var(--bg-tertiary)). Root cause: InputBar.tsx:75-81 auto-resize sets style.height = scrollHeight under Tailwind's global box-sizing:border-box; scrollHeight excludes the 1px border, leaving content ~2px short → permanent overflow with the UA-default overflow:auto (the textarea className at :728 has NO overflow class) → scrollbar always visible. Same pattern in BacklogView.tsx:75-81 (cap 160) and a second auto-resize at BacklogView.tsx:~347 (edit-in-place textarea — verify during execution). FileViewer/SafetySection textareas are pane editors that legitimately scroll — do NOT touch. (3) Test conventions: frontend vitest tests are PURE helpers only (no @testing-library); colocated *.test.ts must be added to frontend/vitest.config.ts include list (precedent: promptHistory.test.ts, language.test.ts). Root cargo test does NOT compile src-tauri — run cargo test + cargo build in src-tauri separately. #![deny(warnings)] at both crate roots: zero warnings, no #[allow], doc comments on all pub fns. Windows 11 + PowerShell; run cargo test UNPIPED and read test-result lines + $LASTEXITCODE (piped exit codes lie). Constitution: regression test required for the bug fix; never commit to main; commit includes the review report. Current branch feat/remove-output-tab has unrelated pending work — branch the new work off main.

## Steps
- [x] 1. **Branch + root-crate Project::seed_stores API** — git status (expect clean or only remove-output-tab commits on the branch); git checkout main; git checkout -b feat/new-project-seed. In src/project/mod.rs add `pub fn seed_stores(&self, codegraph_enabled: bool) -> Result<()>` (doc comment: creates .coding/memory.db with schema applied via MemoryStore::open with a HashEmbedder — empty DB, no vectors written, so the embedder choice is inert — and, when codegraph_enabled, opens the CodeGraph at (root, codegraph_db) and runs index(None) SYNCHRONOUSLY so source search is ready before the first launch; idempotent on already-initialized projects — existing DBs are opened, index is incremental). Map errors with context via crate::error. Tests in the existing mod tests (tempfile pattern already used): (a) tempdir + Project::init + write a tiny .rs file containing `fn seeded_symbol() {}` + seed_stores(true) → assert memory.db exists and re-opens via MemoryStore::open, and codegraph.db exists and a fresh CodeGraph::open reports the indexed symbol (use the same public stats/query API the graph tools use — check src/codegraph for the accessor); (b) seed_stores(false) → memory.db exists, codegraph.db does NOT; (c) second seed_stores(true) call on the same dir succeeds (idempotency). Run root `cargo test` UNPIPED; check test-result lines + $LASTEXITCODE; zero warnings.
- [x] 2. **Wire seeding into create_project IPC** — In src-tauri/src/ipc/projects.rs create_project (currently :62-107): read the codegraph gate FIRST (`let cg_enabled = { state.project.config.lock().await.general.general.codegraph };` — scoped lock, dropped before any await-heavy work), then after Project::init run `let proj = project.clone(); tokio::task::spawn_blocking(move || proj.seed_stores(cg_enabled)).await` — on join error OR seed error, eprintln!("warning: project store seeding failed: {e}") and CONTINUE (startup re-creates/re-indexes anyway; seeding must never fail project creation). Keep the existing registry-add lock block unchanged (re-lock after seeding). Update the command doc comment (:53-60) to state that creation also seeds memory.db + the codegraph index so first launch is search-ready. Run `cargo test` UNPIPED in src-tauri AND `cargo build` in src-tauri (bin crate not covered by root build); zero warnings.
- [x] 3. **Fix phantom textarea scrollbar (InputBar + Backlog)** — Create frontend/src/lib/textareaAutosize.ts: pure exported helper `autosizeForScrollHeight(scrollHeight: number, maxPx: number): { heightPx: number; overflowY: "auto" | "hidden" }` returning { heightPx: min(scrollHeight, maxPx), overflowY: scrollHeight > maxPx ? "auto" : "hidden" }, with a doc comment explaining the bug: under box-sizing:border-box, height=scrollHeight leaves content ~border-width short, so with UA-default overflow:auto a phantom vertical scrollbar (gray thumb) is always visible; hiding overflow unless capped removes it. In InputBar.tsx:75-81 effect apply it: after setting height, `ta.style.overflowY = autosizeForScrollHeight(ta.scrollHeight, 200).overflowY` (use the helper for both values). Same in BacklogView.tsx BacklogInput effect (:75-81, cap 160) and the edit-in-place auto-resize (~:347 — read first to confirm same pattern; apply with its cap). Add frontend/src/lib/textareaAutosize.test.ts: scrollHeight <= max → hidden + unclamped height; scrollHeight > max → auto + clamped height; boundary equality → hidden (regression: the always-visible gray scrollbar thumb in the prompt input). Register the test file in frontend/vitest.config.ts include list (follow promptHistory.test.ts entry). Run `npm test` and `npm run build` in frontend/.
- [x] 4. **Verify, review, commit** — (1) Root `cargo test` UNPIPED (test-result lines + $LASTEXITCODE, zero warnings), `cargo test` + `cargo build` in src-tauri, `npm test` + `npm run build` in frontend/. (2) Spawn reviewer (spawn_agent role:"reviewer") over ALL uncommitted changes (git status / git diff HEAD) with a self-contained task: goal = eager-seed memory.db + codegraph index at project creation, and fix phantom gray scrollbar thumb in prompt/backlog textareas; changed files = src/project/mod.rs, src-tauri/src/ipc/projects.rs, frontend/src/lib/textareaAutosize.ts (+test), frontend/src/components/layout/InputBar.tsx, frontend/src/components/views/BacklogView.tsx, frontend/vitest.config.ts; focus = seed_stores error handling/idempotency on already-initialized projects, create_project lock discipline (config lock dropped before spawn_blocking; no guard held across await) + panic/error tolerance, overflow boundary logic (hidden at <= cap so the border-box 2px overflow can never show a thumb; auto above cap so long prompts still scroll), no dead code/#[allow], doc comments on pub fns, no secrets. Report path: .coding/reviews/<today from Get-Date>-project-seed-scrollbar-review.md. END THE TURN after spawning — resume on the background-finished notification. (3) Read the report; fix EVERY finding (never defer); re-run all four test/build commands. (4) Commit everything including the review report to feat/new-project-seed (never main). (5) Final summary <=3 lines.
