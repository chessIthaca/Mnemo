# Plan: Verify + close a015d2f7: strategy-changing bad-JSON correction (already landed)

## Goal
Verify that backlog a015d2f7 (the general mechanism: a repeated bad-JSON signature must produce a strategy-changing correction, not a third schema restatement) is fully satisfied by the already-landed implementation (plan 481be538, commit 4be19df), confirm the tests are green, record the item linkage in the BUG knowledge record, and close the item without duplicating work.

## Kind
research

## Context
VERIFICATION (code-verified this turn): the general mechanism item a015d2f7 asks for is ALREADY LANDED — implemented this session as plan 481be538 (backlog 38040f12, the specific escaped-regex incident), commit 4be19df on wt/macos-fix, three review rounds (final PASS at .coding/reviews/2026-09-21-481be538-bad-json-retry-loop-verify-round3.md). The item text itself anticipated the split: "this item is the general mechanism fix, 38040f12 is the specific escaped-regex incident" — and the 481be538 implementation built the GENERAL mechanism (per-tool strategy-changing correction), not a search-specific patch.

ACCEPTANCE-CRITERIA MAPPING (verified against the current code):
(a) "two identical bad-JSON failures for the same tool produce a correction that names an alternative formulation, not a third schema restatement" — turn.rs:3338-3362: the retry arm computes failed_content = bad_json_retry_message() and, when repeated_tool_failure(messages, &tc.name, &failed_content) matches a PRIOR identical (tool, error) signature, substitutes repeated_bad_json_correction(&tc.name, ...) — a harness-attributed, strategy-changing correction naming per-tool alternatives: search/search_read → literal:true / metacharacter-free class [(][?][<] / substring+glob narrowing; file_write/file_edit → chunk the body; default → rebuild the argument object from scratch. VERIFIED INTACT (read this turn).
(b) "the first bad-JSON failure still produces the schema correction" — bad_json_retry_message() (turn.rs:3508-3527) is the unchanged schema-oriented first-failure text, used verbatim when no prior identical failure exists. VERIFIED INTACT (read this turn).
(c) "a regression test extends the existing repeat-failure tests asserting the strategy change on the bad-JSON path" — src/agent/tests.rs::repeated_bad_json_for_same_tool_changes_strategy: drives three identical malformed search calls; asserts [0]≠[1], [0]≠[2] (no reversion to schema advice), [1]==[2] (documented idempotence), harness attribution ("[harness tool-call correction"), `literal` named, and the correct class [(][?][<]. Plus the sibling tests (repeat_failure_injects_schema_correction_on_second_identical_error, repeat_failure_across_an_intervening_success_still_fires, distinct_tool_errors_do_not_fire_the_correction) still cover the execution-error class.
(d) "cargo test --workspace green and warning-free" — green at the last run (2,522 passed, 0 failed, warning-free); re-run in this plan to confirm.

FIX-DIRECTION-POINT MAPPING: (1) strategy-changing correction on repeat ✓ (above); (2) schema re-injection preserved for the first failure ✓; (3) "distinguish the two classes explicitly in the correction builder" ✓ — the bad-JSON path uses repeated_bad_json_correction (strategy change) while the tool-execution circuit breaker keeps tool_call_correction (schema re-injection via pending_correction at turn.rs:1644); the detection fn (repeated_tool_failure) is shared but the builders are distinct, and bad-JSON calls never enter execute_tool_batch's failure path (they never execute), so the two corrections cannot double-fire.

DESIGN NOTE (deviation from the item's pointer, already reviewed and PASSED): the item points at "the correction/schema-injection site" (the pending_correction consumer); the landed implementation delivers the strategy change inline in the bad-JSON retry arm as a Tool-role message instead of routing through pending_correction — functionally equivalent (the correction rides the tool-result the model sees immediately) and the acceptance criteria mandate behavior, not mechanism.

CLASSIFICATION: kind=research — no source-code changes are needed (the work is landed); the remaining tasks are verification (reads + test runs) and a .coding/ knowledge-record amendment (allowed for research plans). Research skips the closing review.

VERIFICATION COMMANDS: cargo test --lib repeated_bad_json_for_same_tool_changes_strategy (the regression test), then cargo test --workspace UNPIPED with "exit=$LASTEXITCODE" (agent.md: a piped exit code is the pipe's, not cargo's).

## Steps
- [x] 1. **Verify the acceptance criteria against the landed code** — Read the landed implementation and confirm each acceptance criterion: (a) src/agent/turn.rs:3338-3362 (the bad-JSON retry arm — repeated_tool_failure detection → repeated_bad_json_correction substitution) and the repeated_bad_json_correction builder's per-tool arms (search/search_read → literal:true + metacharacter-free class; file_write/file_edit → chunk; default → rebuild-from-scratch); (b) bad_json_retry_message() (turn.rs:3508-3527) unchanged as the first-failure schema text; (c) the class distinction — repeated_bad_json_correction (bad-JSON path) vs tool_call_correction (execution-error path via pending_correction); (d) the regression test src/agent/tests.rs::repeated_bad_json_for_same_tool_changes_strategy and its assertions. The retry arm and first-failure builder were already re-read this turn and verified intact — confirm the remaining two sites.
- [x] 2. **Run the regression test + full suite** — NO-CODE RESEARCH step (runs commands, touches no files; exercises src/agent/turn.rs + src/agent/tests.rs). Run `cargo test --lib repeated_bad_json_for_same_tool_changes_strategy` UNPIPED then `"exit=$LASTEXITCODE"` — the regression test must pass. Then `cargo test --workspace` UNPIPED with the same exit-code check — all green, warning-free under #![deny(warnings)] (acceptance criterion d).
- [x] 3. **Record the a015d2f7 linkage in the BUG record** — Amend the BUG knowledge record (id 407bd11a, .coding/knowledge/bug/2027-01-11-bad-json-retry-loop-re-injected-identical-schema.md) via memory_amend with a dated paragraph recording: the general-mechanism backlog item a015d2f7 is satisfied by this same implementation (plan 481be538, commit 4be19df) — the per-tool strategy-changing correction IS the general mechanism; 38040f12 was the specific escaped-regex incident that motivated it; verified against a015d2f7's acceptance criteria (a)-(d) with the tests green. This closes the loop so a future session recalling either item id finds the linkage.
