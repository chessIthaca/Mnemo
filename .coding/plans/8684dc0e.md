# Plan: Delete the unwired parallel settings validator (quality review HIGH 3)

## Goal
Remove the dead parallel settings-validation family from src/config/patch.rs (validate_settings_patch, apply_settings_patch, apply_models_patch, SettingsPatch, ModelsPatch + their two tests + the stale TODO(F2-full), section headers, and now-unused imports), porting the two coverage points those tests carried (proxy-cache-ceiling validation, [models] double-Option semantics) to the live path in src/config/settings_dto.rs — so exactly one settings-validation implementation exists.

## Kind
implementation

## Context
Quality review HIGH 3 (.coding/reviews/2027-01-06-full-review-quality-coverage.md): validate_settings_patch (src/config/patch.rs:306-406) is dead code on a 9-month-old TODO(F2-full); the live save path is validate_and_apply_settings_patch (src/config/settings_dto.rs:258-624, called from src-tauri/src/ipc/settings.rs:822). DECISION: delete, not migrate — the live validator is strictly more complete (trace/memory/sound/browser-inspection/core_operations fields SettingsPatch doesn't carry; B1-B3 already implemented there; the TODO's B1-B5 list documents the dead copy being BEHIND the live one), migration would be a large parity refactor of the most user-facing save path for zero user-visible benefit, and the task sanctions deletion. Graph-verified dead: validate_settings_patch (only its own test), apply_settings_patch (zero callers), apply_models_patch (dead apply + own test), SettingsPatch/ModelsPatch (no refs outside patch.rs); mod.rs has only `pub mod patch;` (no re-exports); no README/PLAN.md references (only .coding/ history). STAYS in patch.rs: validate_endpoint, validate_endpoint_set, apply_endpoints, parse_safety_mode (live — settings_dto.rs:338) + their tests. The two dead tests are the ONLY coverage of the ceiling validation and the [models] double-Option semantics — both behaviors live on in the live path, so port both tests to settings_dto.rs. The knowledge file .coding/knowledge/decision/2026-12-21-vendor-reasoning-retention-policy-driven-340k-pr.md cites the dead ceiling test name — update the pointer (+ memory row af97c020 digest). PowerShell edit pitfalls: sequential guarded replaces / bottom-up line-range deletions only, throw BEFORE WriteAllText; file_edit is available this session for exact-string edits.

## Steps
- [x] 1. **Delete the dead settings-patch family from patch.rs** — In src/config/patch.rs, delete bottom-up with boundary guards (PowerShell: read lines, verify boundary content, splice, write; throw BEFORE WriteAllText on any mismatch): (a) lines 1256-1279 — blank + apply_models_patch_double_option_semantics test (guard: L1258 contains 'apply_models_patch_double_option_semantics', L1279 -eq '    }'); (b) lines 970-991 — blank + validate_settings_patch_rejects_tiny_proxy_cache_ceiling test (guard: L972 contains the test name, L991 -eq '    }'); (c) lines 568-618 — '// ── Lib-side patch structs' header + ModelsPatch + SettingsPatch structs + trailing blank (guard: L568 contains 'Lib-side patch structs', L587 contains 'pub struct SettingsPatch', L617 -eq '}'); (d) lines 291-554 — '// ── Settings (non-endpoint) save' header + validate_settings_patch + apply_settings_patch + apply_models_patch + trailing blank (guard: L291 contains 'Settings (non-endpoint) save', L306 contains 'pub fn validate_settings_patch', L553 -eq '}', L555 contains 'Parse a kebab-case safety-mode'). Result: apply_endpoints' '}' + one blank + parse_safety_mode doc; parse_safety_mode's '}' + one blank + '#[cfg(test)]'. Then exact-string edits: rewrite the module doc (lines 5-14) to state the module covers the endpoint-save path (validate_endpoint, validate_endpoint_set, apply_endpoints) + safety-mode parsing, adapter deserializes/trims/calls in, persist+reload+rewire stays in the adapter, and the non-endpoint settings save path lives in settings_dto.rs (validate_and_apply_settings_patch) — drop the stale F2 parenthetical + 'wire structs STAY in the adapter' claim; collapse the module import (lines 18-21) to 'use crate::config::{Config, Endpoint, EndpointKind, KeyStore, PricingEntry, SafetyMode};' (drop EmbeddingModel, ModelRef, VisionModel, EMBEDDING_MODEL_SENTINEL_HASH — all only used by the deleted family); change the tests-module import 'use crate::config::{Endpoint, ModelRef};' to 'use crate::config::Endpoint;' (ModelRef was only used by the deleted test). Verify: repo-wide search for 'SettingsPatch|ModelsPatch|apply_settings_patch|validate_settings_patch|apply_models_patch' returns zero .rs hits.
- [x] 2. **Port the two coverage points to the live path in settings_dto.rs** — In src/config/settings_dto.rs, append two tests inside mod tests (after chat_readability_flags_persist_through_the_save_patch, before the closing brace at :652): (1) proxy_cache_ceiling_validation_rejects_tiny_and_accepts_disable_or_sane — SettingsSaveDto { proxy_cache_ceiling_tokens: Some(1024), ..Default::default() } → unwrap_err containing 'proxy_cache_ceiling_tokens'; Some(340_000) → is_ok(); Some(0) → unwrap + assert next.general.context.proxy_cache_ceiling_tokens == None (0 clears the guard). Comment: ported from the removed patch.rs twin (quality review HIGH 3). (2) models_patch_double_option_semantics — start from Config::default() with current.general.models.planning = Some(ModelRef { endpoint: 'ep', model: 'old' }); Some(Some(ModelRefDto { endpoint: 'ep2', model: 'm2' })) via SettingsSaveDto { models: Some(ModelsConfigDto { planning: ..., ..Default::default() }), ..Default::default() } → next.general.models.planning == Some(ModelRef { 'ep2', 'm2' }); Some(None) → None; outer None (ModelsConfigDto::default()) → unchanged Some('ep','old'). All types in scope via use super::* (Config, ModelRef imported at :20; DTOs defined in-file). Then update the knowledge-file pointer: .coding/knowledge/decision/2026-12-21-vendor-reasoning-retention-policy-driven-340k-pr.md — replace 'patch.rs (validate_settings_patch_rejects_tiny_proxy_cache_ceiling)' with 'settings_dto.rs (proxy_cache_ceiling_validation_rejects_tiny_and_accepts_disable_or_sane)'; memory_update row af97c020-db7e-5b3b-ae47-d2045d452cb7's digest with the same pointer fix.
- [x] 3. **Both Rust suites green + zero remaining references** — Run root 'cargo test' unpiped (read test-result lines + $LASTEXITCODE; expect 2004 + 16 doc — net count unchanged: -2 dead tests, +2 ported) and src-tauri 'cargo test' unpiped (expect 196 + 4). #![deny(warnings)] means green = zero warnings. Re-run the repo-wide family-name search to confirm zero references. Fix any failures before continuing.
- [x] 4. **Closing sequence: review, fix, commit, finish** — Spawn a read-only reviewer (spawn_agent role:'reviewer', complete task args) over ALL uncommitted changes: cite the plan, the quality-review HIGH 3 finding (.coding/reviews/2027-01-06-full-review-quality-coverage.md), the delete-vs-migrate decision + rationale, and the coverage port; ask it to verify the deletion is complete + behavior-preserving (nothing live removed: parse_safety_mode/validate_endpoint/validate_endpoint_set/apply_endpoints intact; settings_dto.rs live path untouched apart from the two new tests), the ported tests actually exercise the live path, imports/doc are clean, and docs/knowledge pointers updated. Fix every finding (re-run tests), round-2 verification if any findings, then commit on wt/agenticcoding (include review reports + plan file) and finish.
