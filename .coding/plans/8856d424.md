# Plan: Push main to origin so .coding/ sidecar syncs across machines

## Goal
Publish local main (merge a0ad7d8 + prior commits) to origin so .coding/backlog.jsonl, plans, reviews, and knowledge files are available on other machines; verify post-push sync shows clean/ahead=0 with backlog present at pushed tip.

## Kind
research

## Context
User wants .coding/backlog.jsonl available on another machine. Backlog file is git-tracked with union merge driver (.gitattributes line 52). All changes committed locally on main via merge a0ad7d8; last observed state was "ahead of origin/main by ~11 commits"; nothing pushed this session. memory.db/codegraph.db are gitignored by design; plans/reviews/knowledge/backlog travel with git.

## Steps
- [x] 1. Verify divergence and remotes - run git status -sb, git log --oneline origin/main..main (list unpushed commits), and git remote -v via shell; confirm local main is ahead of origin/main before pushing.
- [x] 2. Push main to origin - run approval-gated shell command git push origin main; if rejected because remote drifted, report findings instead of force-pushing.
- [x] 3. Verify post-push sync - re-run git status -sb expecting branch line without ahead marker or ahead=0 after fetch semantics refresh; confirm .coding/backlog.jsonl exists at pushed tip via git show HEAD:.coding/backlog.jsonl head lines.
