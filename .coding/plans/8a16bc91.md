# Plan: Expose backlog_add/backlog_status in Reviewing — all workflow states

## Goal
Reverse the 2026-09-04 decision: expose backlog_add and backlog_status in the Reviewing state (ToolFilter arm + state-description prompt + tests) so the main agent can queue items and update statuses during the closing sequence — a direct user request to queue an item must never be impossible. Every workflow state then exposes both tools; the reviewer role stays read-only.

## Kind
implementation

## Context
The Reviewing-state exclusion of backlog mutations is a DELIBERATE, thrice-documented user decision from 2026-09-04 ("no backlog work during the closing sequence"): the Reviewing arm comment (src/tool/mod.rs:441-444), the Reviewing-surface test (~:1709-1712), and the two visibility tests' doc comments (:1734-1741, :1764-1771). On 2026-12-30 the user reversed it after two blocked requests mid-review (the plan-steps display item, and the request to plan this very change; one earlier backlog_add — 46038722 — silently never landed, and db489070's done-flip needed a shell workaround). All other state arms already expose both tools — Planning (:322-324), Executing (:361-363), ExecutingResearch (:393-395), Complete (:487-489) — Reviewing (:434-445) is the only gap. The Reviewing state's prompt fragment at src/agent/prompt.rs:301 enumerates the surface and needs the matching update. Scope guards: the Skill filter treatment stays unchanged (backlog_status rides the always-available set inside skills; backlog_add stays allow-list-only under skills); the Reviewer ROLE filter (src/tool/workflow/backlog.rs:333 — "backlog_add / backlog_status stay out of the reviewer surface") is correct and must NOT change — reviewer subagents stay read-only. Related but separate: backlog item 66c65db9 (run-all/unattended surface exposure) — out of scope here.

## Steps
- [x] 1. **Reviewing arm: add both tools + rewrite the comment (backlog exposure, step 1/4) — in src/tool/mod.rs, ToolFilter::Reviewing's Workflow arm (around lines 434-446), add `|| name == "backlog_add"` and `|| name == "backlog_status"` alongside the existing backlog_list line. Replace the comment block "(backlog_add and backlog_status — both mutations — are NOT available in Reviewing: no backlog work during the closing sequence.)" with the reversal rationale: backlog mutations are user-facing queue operations, not plan mutations — a direct user request to queue an item or update a status must never be impossible mid-closing-sequence (2026-12-30 reversal of the 2026-09-04 decision, after two blocked user requests and one silently-lost add); the reviewer subagent still never sees these tools (ToolFilter::Reviewer's allow-list excludes them).**
- [x] 2. **Update the Reviewing state-description prompt (backlog exposure, step 2/4) — in src/agent/prompt.rs around line 301, the Reviewing state fragment enumerates the available surface ("...update_plan IS available (fix-findings scope...)"). Add a terse note that backlog_add/backlog_status are available too (queue/update user requests mid-review), matching the new ToolFilter arm. Read the surrounding fragment first and keep the wording style consistent.**
- [x] 3. **Flip the three test blocks (backlog exposure, step 3/4) — (a) The Reviewing-surface test (~:1709-1713): the two `assert!(!names.contains(&"backlog_add".to_string()))` / `assert!(!names.contains(&"backlog_status".to_string()))` become `assert!(names.contains(...))`; rewrite the "user decision 2026-09-04" comment to cite the 2026-12-30 reversal. (b) `backlog_add_visible_in_all_base_states` (:1734-1762): flip `!visible(ToolFilter::Reviewing)` to `visible(ToolFilter::Reviewing)` with the message "visible in Reviewing (2026-12-30 reversal)", drop the EXCEPT clause from the doc comment (cite the reversal), and rename the test to `backlog_add_visible_in_all_states`. (c) `backlog_status_visible_in_all_states_including_skills` (:1764-1789): same flip + doc-comment update. Run `cargo test tool::` (root, unpiped) to confirm the flipped tests pass before moving on.**
- [x] 4. **Records, sweep, full verify (backlog exposure, step 4/4) — (1) memory_search for the 2026-09-04 decision record (query e.g. "backlog mutations closing sequence Reviewing 2026-09-04") and memory_supersede it with the reversal (or memory_write a DECISION: record if none exists): backlog_add/backlog_status now available in ALL workflow states including Reviewing; reviewer role unchanged. (2) Search .coding/knowledge/** for stale references to the exclusion ("2026-09-04", "no backlog work during the closing sequence", "not available in Reviewing") and update any that describe it as current behavior. (3) Run cargo test (root, unpiped) and cargo test --manifest-path src-tauri/Cargo.toml — all green, warning-free. Then the closing sequence runs: reviewer subagent (omit the model param) over all uncommitted changes → fix findings → re-run tests → commit on the current wt/* branch → finish.**
