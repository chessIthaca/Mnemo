# Plan: Swallowed errors on durable writes — log + collect (quality review LOW 6)

## Goal
Stop swallowing errors on durable writes: (a) log the plan-completion memory-write failure in turn.rs's spawned task (fire-and-forget stays, failures become visible); (b) collect config save_all rollback failures — log each restore error and enrich the returned error so the user sees the half-committed state — with the rollback extracted into a testable helper and regression tests pinning the collection.

## Kind
bug_fixing

## Context
Quality review LOW 6 (.coding/reviews/2027-01-06-full-review-quality-coverage.md): swallowed errors on durable writes. (a) src/agent/turn.rs — the finding cites :2061 but lines drifted (LOW 2-5 landings); the actual site is :952-954 inside emit_workflow_state's plan-completion handler: `tokio::spawn(async move { let _ = store.write(memory).await; })` — the episodic "Completed plan" memory write. The comment above (:905-907) says fire-and-forget must never block/break the turn — the fix keeps that but logs on Err. The file's logging pattern is eprintln! (:759, :1122, :1461). (b) src/config/mod.rs:211-227 — save_all's rollback branch: two `let _ = restore_from_backup(...)` (committed paths + the failed one) + a staging cleanup loop, then `return Err(e)` with only the commit error. restore_from_backup (config/mod.rs:323-355): if bak missing → Ok; rename(bak, path); on failure with path existing → aside dance (remove_file(aside), rename(path, aside)?, rename(bak, path), on failure rename aside back). Result = anyhow::Result. SURVEY FACTS: existing tests cover save_all happy paths + restore_from_backup success (save_all_restores_from_bak_if_second_rename_fails :885-898 uses the file+bak setup directly); no test covers restore FAILURE collection (it doesn't exist). DECISIONS: (1) bug_fixing kind — the task describes a defect (errors on durable writes silently dropped; the batch rule lists "error" as a defect class); (2) fix (a) is logging-only inside a spawned task — NOT unit-testable (eprintln + tokio::spawn, no capture seam); verified by inspection + review, documented for the reviewer; (3) fix (b) extracts the rollback into a testable helper so the collection behavior IS testable: rollback_partial_commit(paths: &[PathBuf], committed: &[&Path], failed: &Path) -> Vec<(String, anyhow::Error)> (restore committed + failed from .bak, eprintln each failure with the "mnemo:" prefix, clean leftover temps, return the failures) + describe_rollback_failure(commit_err: anyhow::Error, failures: &[(String, anyhow::Error)]) -> anyhow::Error (empty → the original error unchanged; non-empty → "{commit_err}; additionally, rollback failed — the config set may be half-committed: {path: err; ...}"); save_all's failure branch becomes `let failures = rollback_partial_commit(&paths, &committed, path); return Err(describe_rollback_failure(e, &failures));`; (4) CROSS-PLATFORM failure injection for the regression test (worked out case-by-case): live path = file, path.bak = DIRECTORY, path.bak.restore = DIRECTORY → restore_from_backup: rename(bak_dir, path_file) fails on both platforms (dir→file); path.exists() → aside branch: remove_file(aside_dir) fails silently (it's a dir, remove_file can't clear it) → rename(path_file, aside_dir) fails (file→existing dir) → `?` returns Err — collected. The clean-rollback test uses the existing file+bak pattern (rename file→file succeeds both platforms). (5) Scope discipline: only the two named sites — record_request_stats/record_tool_event fire-and-forget writes stay (the finding explicitly blesses fire-and-forget for non-named sites; the reviewer can flag if it disagrees). Root cause: `let _ =` discards Results on durable writes with no logging channel and no error-collection path in the rollback.

## Steps
- [x] 1. **Reproduce with failing regression test** — Write a regression test that reproduces the defect (constitution: every defect gets a regression test that fails without the fix and passes with it). Run it and confirm it FAILS.
- [x] 2. **Document root cause** — Investigate and document the root cause. memory_write a BUG: record (symptom → root cause → fix + regression test name, ≤600 chars).
- [x] 3. **Minimal fix** — Apply the minimal fix that makes the regression test pass. Do not refactor unrelated code.
- [x] 4. **Verify** — Run the regression test + the full test suite (cargo test unpiped, warning-free). Record the regression test name via update_plan (regression_test field) — finish is blocked without it.

## Bug
Durable-write failures are silently swallowed with no trace. (a) The plan-completion semantic-memory write (src/agent/turn.rs:953, store.write inside tokio::spawn) discards its Result — a failed memory write vanishes with no log. (b) The config multi-file save rollback (src/config/mod.rs:215+219) discards restore_from_backup Results — a failed rollback leaves a possibly half-renamed config set with no trace, and the returned error tells the user nothing about the half-committed state. Quality review LOW 6 (.coding/reviews/2027-01-06-full-review-quality-coverage.md).

## Regression test
rollback_collects_restore_failures
