# Plan: Verify + close: graph-first discipline is landed (nudge + redirect gate + HOW memories)

## Goal
Verify the graph-tool usage discipline mechanism is fully implemented, tested, and live in the current tree — covering both remedies the task suggested (HOW memory + steering-stats nudge/intercept) and all its concrete instances — then reconcile the backlog item with done-status and pointers. No source-code changes expected (research).

## Kind
research

## Context
Backlog item 8b8f40d2-86e3-4006-8e74-2abda844494d ("Improve graph-tool usage discipline: ... lead with graph_search → graph_context instead of falling back to search/grep", note pointer 100fa5ba). Investigation (Planning state) established the task's work is ALREADY LANDED in the tree and live: (1) symbol_nudge — src/tool/agent/search.rs:583-617 (plan 08eb13d8 "Graph-first discipline: symbol-intent nudge", MERGED into main at c1daf7f): the search tool's result embeds an id-carrying note whenever the pattern resolves to an indexed symbol (definition-prefix or bare identifier shapes — covering all five named instances: send_suggestion/send_prompt/on_main_turn_resolved/run_all_dispatch_next/backlog_add, plus 'steer' whose ignored nudge the task cites); (2) the graduated redirect gate — src/agent/dispatch.rs:734-753 symbol_search_redirect + steering_stats (plan a41a0d82 "Graduated symbol-lookup redirect gate", review PASS 2026-12-04, .coding/reviews/2026-12-04-c5-symbol-lookup-redirect-gate-review.md): after 2 ignored nudges, a symbol-shaped search is INTERCEPTED with "SEARCH INTERCEPTED: the pattern 'X' names an indexed symbol..." + the embedded graph_context(id=...) + the text-occurrence escape hatch; 6 unit tests in dispatch.rs (fires after ignored nudges / lifts after switch / also gates search_read / ignores non-search tools / no graph / below threshold / non-symbol pattern); (3) both suggested remedies from the task text exist: the HOW memory bcea5de4 ("SEARCH INTERCEPTED redirect — call graph_context immediately") + a146c39d ("graph_search is lexical substring — retry name fragments on zero", the record the task itself references), and the steering-stats nudge; (4) LIVE PROOF this session: two symbol-shaped searches (tool_filter|token_accounting|tool_schemas) produced the nudge note and then the full SEARCH INTERCEPTED redirect — and the session correctly switched to graph_context. Full suite has run green repeatedly this session (latest: 1872 passed / 0 failed +16, frontend 709/709) — includes all steering tests. The item is done-but-unmarked (same pattern as 7ba83b5b): plans 08eb13d8/a41a0d82 delivered it, but the backlog row was never reconciled.

## Steps
- [x] 1. **Run the steering test set** — Run the targeted steering tests as fresh confirmation in the current tree: `cargo test symbol_nudge symbol_search_redirect steering` (adjust filter to cover search.rs symbol_nudge tests + dispatch.rs symbol_search_redirect tests); read the suite result lines. Also `git show --stat 100fa5ba` to confirm the item's note pointer is a pre-item checkpoint commit (bookkeeping, not unlanded work).
- [x] 2. **Verify wiring + records end to end** — Confirm the mechanism wiring end to end in the tree (read-only): search.rs symbol_nudge pattern shapes cover the task's five named bare identifiers + 'steer' (definition-prefix/bare-identifier resolution); dispatch.rs symbol_search_redirect wiring in execute_tool_call (search/search_read, threshold 2, lifts after a graph_context switch); steering_stats counters. Confirm the two HOW memories are current in recall (bcea5de4, a146c39d) and the two delivering plans are recorded (08eb13d8 merged c1daf7f; a41a0d82 review PASS 2026-12-04).
- [x] 3. **Close the backlog item with pointers** — Reconcile: mark backlog item 8b8f40d2 done with a note carrying the pointers (symbol_nudge search.rs:583, symbol_search_redirect dispatch.rs:734, plans 08eb13d8/a41a0d82, HOW memories bcea5de4/a146c39d, live-proof note from this session's intercept). If steps 1-2 surface any GAP (a pattern shape not covered, a broken test, an unlanded piece), STOP the research framing: report the gap and switch to an implementation plan before closing. Then finish the plan.
