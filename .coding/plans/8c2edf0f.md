# Plan: Backlog resolution contract — finished vs done, Clear-finished & Run-all

## Goal
One consistent, documented backlog-resolution contract, pinned by tests: (1) the exact resolution path end-to-end in one place — the run_all.rs module doc gains a consolidated "finished" section (finished = terminal = Done|Failed|CantResolve, one notion everywhere; Clear-finished soft-deletes exactly those, keeps Pending+InFlight, 30-day startup purge, union-merge-safe; Run-all dispatches live Pending only and never touches terminal, its done/total counters are run-scoped and unaffected by mid-run clearing, the adoption sweep reads live items only so soft-deleted InFlight can never be resurrected, and an agent-stamped terminal status wins over the automatic Done stamp — the escape hatch); (2) the README backlog section carries the user-facing contract; (3) pin tests for the load-bearing gaps (adoption's live-only read, the clear-finished IPC's isolation from run state, the UI's hasFinished terminal notion). The audit found NO divergence — no behavior changes, only documentation + pins.

## Kind
implementation

## Context
Audit verified 2027-01-07 (no divergence found — the deliverable is the consolidated contract + pins): terminal = Done|Failed|CantResolve ("finished") consistently at every consumer: store transition table (src/backlog.rs:478-486, allowed_destinations :221 — terminal→Pending only), requeue :594 (accepts all three), clear_finished :696 (soft-deletes exactly the three, keeps Pending+InFlight — tested at :1625), items() :721 (filters soft-deleted), next_pending :640 / next_pending_eligible :655 / pending_item :683 (live Pending only); UI canRetry BacklogView.tsx:602-605 + hasFinished :997-999 + clear-finished button title :1055 (all three terminal; done-specific retry title = UX nicety); indexer scan_backlog :593 (live pending only — terminal = history); run-all dispatch (live Pending non-deferred only, never touches terminal); auto-feed (next_pending); adopt_orphaned_in_flight :2370 (reads items() — live-only, cannot resurrect soft-deleted InFlight); RunAllState (state.rs:214) counters are run-scoped in-memory — clear_finished cannot touch them (mid-run clearing safe); backlog_clear_finished IPC :222 (store + emit only). Resolution path verified end-to-end in on_main_turn_resolved :1936-2042: plan-loop gate (Complete + ≥1 transition) → Done + commit_success; plan_abandoned → Failed; other turn ends → annotate + halt (status kept); terminal Error → annotate + halt (left InFlight); done counter bumps after terminal resolution; an agent-stamped terminal status (e.g. CantResolve via backlog_status) survives the automatic Done stamp (guarded table refuses terminal→terminal) and the run still counts it resolved — the escape hatch (HOW d899e4f7). run_all.rs module doc :106-142 carries the resolution arms + dead-owner exceptions but NOT the consolidated finished notion / Clear-finished / Run-all treatment. Scope boundary: the queued done-orphan guard (6c6966b9 — run-all re-dispatching already-finished items) is a SEPARATE task; this plan documents the contract, it does not implement that guard.

## Steps
- [x] 1. **Contract doc: run_all.rs module doc** — Extend the run_all.rs module doc (after the resolution arms at :106-142) with a consolidated section: "Finished (the one notion)" — finished = terminal = Done|Failed|CantResolve, identical at every consumer (the guarded transition table, requeue, clear_finished, the Backlog tab's retry/clear-finished affordances, the memory indexer's pending-only scan, run-all/auto-feed dispatch). Document Clear-finished: soft-deletes exactly the three terminal statuses (deleted_at stamp, JSONL + image sidecars kept, 30-day startup purge, union-merge-safe), keeps Pending AND InFlight (a mid-flight item is never cleared). Document Run-all's treatment: dispatches live Pending (non-deferred) only, never touches terminal items; the run's done/total counters are run-scoped in-memory state — clearing terminal items mid-run does not touch them (the progress line stays accurate for the run's own history); the start-time adoption sweep snapshots store.items() (live-only) so a soft-deleted InFlight item can never be resurrected. Document the escape hatch: an agent-stamped terminal status (e.g. CantResolve via the backlog_status tool) wins over the automatic Done stamp — the guarded table refuses terminal→terminal, the item keeps the agent's stamp, and the run still counts it resolved (HOW d899e4f7: check the item's status at closure and stamp directly when the automatic stamp missed).
- [x] 2. **README backlog section** — Extend the README's Backlog tab paragraph with the user-facing contract: how an item resolves (Done = the plan reached Complete via the turn-resolution gate; Failed = the plan was abandoned; CantResolve = an explicit dead end; InFlight = a plan is active and survives interruptions; a main-agent exit or crash requeues orphans to Pending), "finished" = done/failed/can't-resolve treated identically everywhere (retry re-queues any of them; Clear finished soft-deletes exactly those and never a mid-flight item; Run-all dispatches pending items only and its progress counters are unaffected by mid-run clearing). Keep it compact — the module doc carries the detail.
- [x] 3. **Pin tests for the load-bearing gaps** — The store contract is already tested (requeue_moves_terminal_statuses_back_to_pending :1330, clear_finished_drops_terminal_statuses :1625 — asserts pending+inflight kept, terminal→terminal transitions refused :1441). Add the missing pins: (1) src-tauri/src/ipc/run_all.rs tests — a source-contract test that adopt_orphaned_in_flight's snapshot iterates store.items() (the live-only read — a soft-deleted InFlight item can never be resurrected; use the rfind pattern — the tests module precedes the functions in this file); (2) src-tauri/src/ipc/backlog_cmds.rs tests — a source-contract test that backlog_clear_finished routes through BacklogStore::clear_finished + emit_backlog_changed and touches NOTHING else (no run-state interaction — mid-run clearing cannot corrupt the run's counters/pointer); (3) frontend/src/components/views/BacklogView.test.ts — a source-contract pin that hasFinished (the clear-finished gating) checks all three terminal statuses (mirroring the existing canRetry pin at :22-24).
- [x] 4. **Full matrix + closing sequence** — Run the full matrix: cargo test (root), cargo test --manifest-path src-tauri/Cargo.toml, npm test + npx tsc in frontend/. All green (doc + test changes only — no behavior changes). Then the closing sequence: reviewer subagent → fix findings → re-run tests → commit to the working branch → finish. Write a compact SPEC memory recording the consolidated contract (the finished notion + Clear-finished/Run-all treatment + the escape hatch) so it is durable beyond the module doc.
