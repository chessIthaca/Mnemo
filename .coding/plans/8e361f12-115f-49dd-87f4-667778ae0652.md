# Plan: Add debug TOML config sample

## Goal
Create a debug-oriented sample `config.toml` for myharness that matches the real serde schema in src/config/general.rs so it parses with `toml::from_str`.

## Context
Sandbox is C:\AgenticCoder\AgenticCoder (project `myharness`). Global config lives in ~/.myharness/ as config.toml, endpoints.toml, keys.toml, projects.toml. GeneralConfig (src/config/general.rs) has [general] (default_provider, default_model, safety: SafetyMode kebab-case, vision_model), [context] (summarize_at_fill_rate), [memory] (embedding_model, embedding_provider), [ui] (theme, show_token_usage). All structs are #[serde(default)], so partial files parse.

## Steps
- [x] 1. Write debug.config.toml at project root with debug-oriented settings matching the GeneralConfig schema
- [x] 2. Validate the file parses (cargo test, per constitution)
