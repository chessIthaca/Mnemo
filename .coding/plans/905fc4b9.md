# Plan: Kill wedged offscreen headless Chromium + verify self-healing coverage

## Goal
Kill the wedged offscreen (launch-mode headless) Chromium instance (web-ifc WASM sync parse loop saturating the CDP channel) and verify the landed self-healing code reaps this wedge class — no source changes expected.

## Kind
research

## Context
Backlog f653ec07 (user-queued 2026-09-20). The wedge: a page ran the in-page IFC import; the browser's web-ifc WASM entered a synchronous parse loop (the recorded GetSetArgument error flood); the page acknowledges nothing (evals/close/navigate all time out) and the wedged renderer saturates the browser process/CDP channel — even list_pages (a browser-level query) hangs. It will not self-recover; the process must die. Cleanup: find by remote-debugging + headless flags (NEVER a user Chrome window), Stop-Process. Verification: the regression test wedge_renderer_times_out_and_self_heals (cargo test --features browser wedge_renderer -- --ignored at the root crate — the browser feature is off by default) pins force-reap + respawn; the landed SPEC (main 752164e, plan ec425270) says every launch-mode CDP round-trip in src/browser/mod.rs rides the cdp() wrapper (30s ops / 60s navigate) -> force_reap_wedged -> respawn on next op, and the watchdog probe (Browser::version, 5s, 2 strikes) catches idle-wedged browsers. Contingency: if the regression test fails or code reading shows browser-level queries bypass the wrapper, STOP and file a bug_fixing plan instead of patching. The running app must be restarted from a build >= 752164e to benefit — note that in the report.

## Steps
- [x] 1. **Locate the wedged instance** — NO-CODE RESEARCH (operational process enumeration — no files touched). Shell: Get-CimInstance Win32_Process filtering CommandLine matching 'remote-debugging' AND 'headless' (chrome/headless_shell). Print PID + full command line for each. Confirm the match is the offscreen launch-mode instance (headless + remote-debugging flag + user-data-dir profile), never a user Chrome window. If nothing matches, the wedge already cleared (app restart or process death) — record that and skip to step 4.
- [x] 2. **Kill it** — NO-CODE RESEARCH (operational process kill — no files touched). Stop-Process -Id <pid> for the identified instance(s) ONLY (the filter from step 1 already excludes user browsers — headless + remote-debugging). Record the killed PID(s) and start time if available.
- [x] 3. **Confirm cleanup** — NO-CODE RESEARCH (operational re-enumeration — no files touched). Re-run the enumeration from step 1: assert zero wedged/orphaned headless Chromium processes with a remote-debugging flag remain.
- [x] 4. **Verify the self-healing code path** — Code + test verification (read-only on src/browser/mod.rs). Run cargo test --features browser wedge_renderer -- --ignored at the root crate (the ignored regression test wedge_renderer_times_out_and_self_heals — single test, no parallel-flake risk; expect ~5s: 3s CDP-timeout override fires, force-reap, respawn). Also confirm via src/browser/mod.rs that browser-level queries (list_pages) ride the cdp() wrapper — graph_context or a targeted read of the list_pages implementation in src/browser/mod.rs.
- [x] 5. **Report + wrap up** — NO-CODE RESEARCH (report only — no files touched). Report: cleanup result (killed PID(s) or already-cleared), regression-test verdict, list_pages wrapper coverage confirmation, and the note that the running app must be restarted from a build >= 752164e (self-healing landed there) to benefit. Mark backlog item f653ec07 done if not automatic at plan completion.
