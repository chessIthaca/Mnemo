# Plan: Shell output preview: fixed height + on/off toggle

## Goal
Stop the live shell-output preview from jumping the chat layout while a command streams — render it at a FIXED height (reserved from the first paint of a running shell card, overflow-auto, stick-to-bottom), and add a persisted Chat-settings toggle (default on) that disables the preview entirely.

## Kind
implementation

## Context
Anchors: the preview block is frontend/src/components/chat/Message.tsx:671-685 (inside ToolCard :435-731; `running` already computed at :450) — `<pre aria-live="polite" className="... max-h-[12em] overflow-auto ...">` behind `{liveTail !== "" && (...)}`; the tail comes from liveTailPreview (frontend/src/lib/toolCardPaths.ts:1055-1060 — last 6 lines / 400 chars); stick-to-bottom effect at :562-565. The source-contract test pinning the CURRENT wiring: frontend/src/lib/toolCardPaths.test.ts:1163-1177 — asserts `{liveTail !== "" && (` and `max-h-[12em]`; both change with this fix, so the contract must be updated to the new wiring (the assertions on calls.find/el.scrollTop/liveTailPreview/aria-live stay). Toggle pattern to copy (showTokenUsage): appearance.ts:360-364 (reader, default true) + LS key; useAgentStore.ts:271/:491/:679/:927-929 (state, setter decl, init, impl); ChatSection.tsx:35-50/:87/:137-145 (draft capture, apply, checkbox UI); settings/types.ts (ChatDraft + serializeChat). FIX DESIGN (fixed height): gate the block on `showShellPreview && name === "shell" && running` (only shell streams — Message.tsx:551-553 comment) and render it at a FIXED `h-[7.75em]` (6 lines × 1.5 line-height × 0.75em text + 2 × 0.5em padding — fits liveTailPreview's 6-line window), overflow-auto + the existing stick-to-bottom; reserved from the FIRST paint of a running shell card (renders empty until the first chunk) so the card height never changes while streaming. TOGGLE DESIGN (localStorage-only, per the backlog: "persisted like the other appearance options in frontend/src/hooks/appearance.ts"): appearance.ts — LS_SHOW_SHELL_PREVIEW = "mh.showShellPreview" + readShowShellPreview() (default true, mirroring readShowTokenUsage); useAgentStore.ts — showShellPreview: boolean + setShowShellPreview (writeLs + set) + import/re-export entries; settings/types.ts — ChatDraft.showShellPreview + serializeChat; ChatSection.tsx — draft capture + apply via the setter + the checkbox ("Stream shell output live in the tool card"), NOT in the saveSettings config.toml payload (localStorage-only persistence — comment the deviation from the showTokenUsage sibling; the config.toml/Rust SettingsUi field is out of the backlog's scope). TEST DESIGN: (1) update the source contract (toolCardPaths.test.ts:1163-1177) to pin the new wiring — the toggle gate, h-[7.75em], reservation-while-running (no liveTail condition); (2) NEW frontend/src/components/chat/Message.preview.test.tsx (node-env SSR, renderToStaticMarkup — ToolCard takes props {name, calls: ToolInvocation[]}; store reads fall to initial state, so the toggle-off case seeds localStorage via a vi.hoisted window stub, the RightPanel.width.test.tsx pattern): (a) a running shell call renders the fixed-height block even with NO output yet (reserved from first paint) and the class carries h-[7.75em] not max-h; (b) with mh.showShellPreview="false" seeded, no preview block renders; (c) a non-shell running call renders no block; register the file in frontend/vitest.config.ts (the include allowlist — vitestInclude.test.ts guards it); (3) appearance.test.ts — readShowShellPreview unit cases (default true; "false" → false). VERIFICATION: cd frontend; npm test and npm run build; cargo test at the root UNPIPED (no Rust changes expected — read its test result: lines per agent.md). Docs: check docs/FEATURES.md for a streaming-preview line to update (fixed height + toggle); update ChatSection's doc comment (:19-26) which lists the section's toggles.

Review round 1 (.coding/reviews/2026-09-18-shell-preview-fixed-height-toggle-review.md): FINDINGS (0 high, 2 low) — the core fix verified correct and all backlog acceptance criteria hold. L1: the h-[7.75em] math conflated parent-em with element-em (em below font-size resolves against the element's own 0.75em font) — the box fit 4.5 lines, not the claimed 6; fixed to h-[10em] (6 × 1.5em line-height + 2 × 0.5em padding = exactly six lines at the element's own em), with the comment math corrected and the test assertions (toolCardPaths contract, Message.preview.test.tsx, Message.preview.off.test.tsx) updated to pin h-[10em]. L2: the comment misattributed streaming to neverGroups (the merge policy); corrected to "only shell emits ToolOutputDelta (the shell.rs sink), and shell never merges (the reducer's neverGroups)". FEATURES.md's "six lines" claim is now true. Re-verified after the fixes: full vitest suite green, tsc+vite build clean, cargo test 2410+16 green.

Review round 2 (.coding/reviews/2026-09-18-shell-preview-fixed-height-toggle-review-round2.md): FINDINGS (0 high, 1 low) — both round-1 LOWs verified genuinely fixed (the h-[10em] arithmetic independently recomputed as exactly six visible lines; the L2 attribution corrected verbatim in Message.tsx), no regression in the round-1-verified surface, and a consistency sweep found no stale 7.75em/12em values. The residual LOW: the same L2-class misattribution ("Only shell streams (the reducer's neverGroups)") survived in Message.preview.test.tsx:61 — the sibling comment was not corrected with the Message.tsx instance. Fixed: the comment now reads "Only shell emits ToolOutputDelta (the shell.rs sink) — no reservation for any other tool, even with a stray liveOutput"; a repo-wide sweep for "Only shell streams|neverGroups" confirms no other misattributed instance remains (the other matches correctly attribute neverGroups to merging). Targeted tests re-run green (116/116).

## Steps
- [x] 1. **Fixed-height reserved preview block** — frontend/src/components/chat/Message.tsx (ToolCard): add the `showShellPreview` store selector next to showToolImages (:445); replace the `{liveTail !== "" && (...)}` max-h block (:677-685) with a block gated on `showShellPreview && name === "shell" && running`, rendered at a FIXED height — `<pre ref={liveTailRef} aria-live="polite" className="mt-[0.15em] h-[7.75em] overflow-auto whitespace-pre-wrap break-words rounded bg-bg-primary p-[0.5em] text-[0.75em] text-slate-400">{liveTail}</pre>` — keeping the stick-to-bottom effect (:562-565); update the block comment (:671-676): fixed height reserved from the first paint of a running shell card, so streaming never changes the card height; gated by the Chat-settings toggle.
- [x] 2. **The persisted on/off toggle** — frontend/src/hooks/appearance.ts: add LS_SHOW_SHELL_PREVIEW = "mh.showShellPreview" + readShowShellPreview() (default true, mirroring readShowTokenUsage :360-364). frontend/src/hooks/useAgentStore.ts: `showShellPreview: boolean` state (near :271), `setShowShellPreview: (show: boolean) => void` decl (near :491), init `showShellPreview: readShowShellPreview(),` (near :679), impl `setShowShellPreview: (show) => { writeLs(LS_SHOW_SHELL_PREVIEW, String(show)); set({ showShellPreview: show }); }` (near :927) + import/re-export entries. frontend/src/components/settings/types.ts: ChatDraft.showShellPreview + include it in serializeChat. frontend/src/components/settings/sections/ChatSection.tsx: capture `showShellPreview: s.showShellPreview` in readDraftFromStore, apply `s.setShowShellPreview(draft.showShellPreview)` in save, add the checkbox after the showKnowledgeActivity label ("Stream shell output live in the tool card"); do NOT add it to the saveSettings config.toml payload — localStorage-only per the backlog, with a comment noting the deviation from the showTokenUsage sibling.
- [x] 3. **Tests — contract update + preview render cases** — Update the source contract in frontend/src/lib/toolCardPaths.test.ts:1163-1177 to the new wiring (keep the calls.find/el.scrollTop/liveTailPreview/aria-live assertions; replace `{liveTail !== "" && (` and `max-h-[12em]` with the toggle gate + `h-[7.75em]` + reservation-while-running). Add frontend/src/components/chat/Message.preview.test.tsx (node-env SSR, renderToStaticMarkup; ToolCard props {name, calls}; toggle-off seeds localStorage via a vi.hoisted window stub — the RightPanel.width.test.tsx pattern): (a) running shell call renders the fixed-height block even with no output yet; (b) mh.showShellPreview="false" → no block; (c) non-shell running call → no block. Register the file in frontend/vitest.config.ts. Add readShowShellPreview cases to frontend/src/hooks/appearance.test.ts.
- [x] 4. **Verify + docs** — cd frontend: npm test + npm run build (both green). cargo test at the root UNPIPED (read its test result: lines). Docs: update docs/FEATURES.md if it describes the streaming preview (fixed height + the new toggle); update ChatSection's doc comment (:19-26) to list the new toggle. Manual check: run a streaming shell command (e.g. cargo test) in the chat — the card height must not change after the preview appears; toggle Settings → Chat → "Stream shell output live in the tool card" off, restart, confirm no preview block and the preference persisted.
