# Plan: Round 2: remove rav1e + fix browser tests + optimize git_ops + install nextest + enforce test-success gate

## Goal
Round 2 of build/test optimization: (1) remove the transitive rav1e AV1 encoder (~5s cold-compile win), (2) mark browser integration tests #[ignore] so the default suite is always green (fixes the tolerated-failures anti-pattern), (3) optimize git_ops TestRepo setup (7→3 git spawns per test), (4) install cargo-nextest for parallel test execution, and (5) enforce that no plan closes without 100% test success — zero tolerated failures, no coverage deterioration.

## Kind
implementation

## Context


## Steps
- [x] 1. **Investigate + remove rav1e (AV1 encoder, ~5s cold-compile win). rav1e is pulled transitively by ravif ← image v0.25.10, despite Cargo.toml having image = { default-features = false, features = ["png","jpeg","gif","webp","bmp"] } (no avif). Step 1a: run `cargo tree -e features -i rav1e` (or `cargo tree -i ravif`) to find the exact enablement path — is image 0.25.10 compiling ravif unconditionally (a known image-crate annoyance), or is another dep (fastembed? chromiumoxide?) enabling the `avif` feature on image transitively? Step 1b: based on the finding, apply the fix: if image 0.25.10 always compiles ravif, check whether pinning to a slightly different 0.25.x patch version gates it properly, OR add an explicit `[patch]` / feature override to exclude it, OR if another dep enables avif, gate that dep's image feature. If no clean fix exists (image bakes ravif in), document why and skip — don't force a broken config. Step 1c: verify with `cargo build --lib --timings` (cold or touch Cargo.toml to force re-resolve) — confirm rav1e/ravif no longer appear in the timing report, and `cargo test --lib` still passes. Record the before/after cold-compile delta.**
- [x] 2. **Mark browser integration tests #[ignore] — fix the tolerated-failures anti-pattern. In src/browser/mod.rs test module: the 14 integration tests that spawn/attach a real Chromium or WebView2 (browser_connect_attaches_to_running_chromium, webview_connect_screenshot_eval_snapshot, webview_frame_and_input_cdp_surface, webview_page_selects_child_target, webview_read_falls_back_to_app_page_when_child_missing, webview_click_and_type_drive_child_webview, webview_click_without_child_says_to_navigate_first, webview_navigate_auto_ensures_missing_child, and any other test that calls Browser::launch/Browser::connect/spawns Chromium) must be marked #[ignore] with a clear reason comment: '// #[ignore] — requires a graphical Chromium/WebView2 environment unavailable in the headless test runner; run with `cargo test -- --ignored`'. KEEP the pure-logic tests (URL normalization, first_line_truncates_and_strips, any test that doesn't spawn a browser process) in the default suite. Read each test to categorize correctly — a test is 'integration' if it calls Browser::launch, Browser::connect, BrowserManager::new + a real navigate, or asserts on page content/screenshots; it's 'pure-logic' if it only tests normalize_url/validate_url/first_line or similar helpers. Verify: `cargo test --lib browser` — zero failures (ignored tests don't count as failures), pure-logic tests pass. Confirm `cargo test --lib browser -- --ignored` runs the integration tests (they may still fail without a graphical env — that's fine, they're opt-in now).**
- [x] 3. **Optimize git_ops TestRepo setup (7→3 git spawns per test, ~5s win). In src/project/git_ops.rs TestRepo::new (line ~610): currently 7 git subprocess calls: init, config user.email, config user.name, config commit.gpgsign, config core.autocrlf, add, commit, branch -m main. Optimize to 3: (1) `git init -q -b main` (the -b flag sets the initial branch name, eliminating the separate `branch -m main` call — requires git 2.28+, universally available now); (2) `git add -A`; (3) `git -c user.email=test@example.com -c user.name=Test -c commit.gpgsign=false -c core.autocrlf=false commit -q -m initial` (the -c flags set config for THIS command only, eliminating the 4 separate config calls). This cuts 7→3 spawns × 24 tests = 96 fewer subprocess spawns. Verify: `cargo test --lib project::git_ops` — all 24 tests still pass (the behavior is identical: same repo state, same assertions). Measure before/after: record the module test time (before ~9s, target ~5-6s).**
- [x] 4. **Install cargo-nextest + verify speedup. Run `cargo binstall cargo-nextest --no-confirm` (downloads a prebuilt binary, faster than `cargo install` which compiles from source). If binstall isn't available, fall back to `cargo install cargo-nextest`. Verify: `cargo nextest --version`. Then run `cargo nextest run --lib` and compare wall time to the `cargo test --lib` baseline (17.1s pre-rav1e-removal, expected lower after steps 1-3). Record the measured time. NOTE: nextest is a drop-in test runner — `cargo nextest run` replaces `cargo test`. It runs tests in parallel with lower per-test overhead. Do NOT change the project constitution's test command yet (it says `cargo test`) — just install nextest, verify the speedup, and note the recommendation. The closing sequence of THIS plan uses `cargo nextest run --lib` for the test run.**
- [x] 5. **Enforce test-success gate + full verification + review + commit. (1) Run the FULL test suite: `cargo nextest run --lib` (or `cargo test --lib` if nextest had issues) — confirm ZERO failures (ignored tests are fine, failures are not). If ANY test fails, FIX IT before proceeding — no tolerated failures (the decision recorded 2026-09-19: all failing tests must be fixed). (2) Verify no coverage deterioration: the removed/ignored tests were either (a) testing an external tool we don't own (git tool tests — already removed in round 1), (b) environment-dependent integration tests now #[ignore] (browser — still runnable via --ignored, just not in the default suite), or (c) optimized but still present (git_ops — same assertions, faster setup). No pure-logic test was removed. Confirm the test COUNT didn't drop unexpectedly (record before/after counts). (3) `git status --short` + confirm branch is wt/* (not main). (4) Spawn a read-only reviewer (spawn_agent role:reviewer) over ALL uncommitted changes: changed files = Cargo.toml (rav1e fix), src/browser/mod.rs (#[ignore] marks), src/project/git_ops.rs (TestRepo optimization), plus any Cargo.lock changes. Goal = round-2 build/test optimization (rav1e removal + browser test ignore + git_ops setup optimization + nextest install). Ask for: correctness (rav1e truly removed not just hidden; #[ignore] only on integration tests not pure-logic; TestRepo optimization produces identical repo state — same assertions pass; nextest run is green), the TEST-SUCCESS GATE (verify `cargo test --lib` / `cargo nextest run --lib` has ZERO failures — not 'mostly passing', ZERO; #[ignore] tests have clear reason comments; no flaky tests in the default suite), coverage (no pure-logic test removed; test count stable or explained), constitution (warning-free; multi-platform neutral — rav1e removal doesn't break macOS; #[ignore] is standard Rust not a hack), docs sync. END TURN after spawning. (5) Read report, fix EVERY finding, re-run tests. (6) Commit to wt/* with a clear message + include the review report. (7) finish with the report path. Final summary ≤3 lines.**
