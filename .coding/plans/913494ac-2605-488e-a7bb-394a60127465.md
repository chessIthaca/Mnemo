# Plan: Restore bg-bg-secondary to the agent tab rail

## Goal
Give the MainPanel agent tab rail its own surface again by restoring bg-bg-secondary on the TabsList, so the tab bar reads as a distinct chrome band above the bg-bg-primary conversation — matching the rest of the app (Sidebar, RightPanel, InflightBar, StatusBar, InputBar all sit on bg-bg-secondary). Keep the flat-rail trigger styling (h-10, cyan 2px bottom border) from the previous restyle untouched.

## Kind
implementation

## Context
The previous restyle (plan 6bec5e06, "Align agent tab bar with tool tab style") changed MainPanel.tsx TabsList from `flex items-center gap-1 overflow-x-auto border-b border-border bg-bg-secondary px-2 py-1` to `flex items-stretch overflow-x-auto border-b border-border` — dropping bg-bg-secondary entirely. The review at the time flagged this ("Lost bg-bg-secondary on the rail", observation #3, deemed deliberate parity with RightPanel). But the user now says the background color is off: the agent tab bar blends into the bg-bg-primary conversation area, whereas every other chrome band (Sidebar, RightPanel, InflightBar, StatusBar, InputBar) is bg-bg-secondary. The RightPanel tool rail sits on a bg-bg-secondary panel, so its rail reads as a distinct surface; the MainPanel rail now does not.

Fix: restore `bg-bg-secondary` on the TabsList (full-bleed, no pill padding — keep items-stretch and the border-b hairline). Also extend MainPanel.tabStyle.test.ts to assert the TabsList carries bg-bg-secondary (it currently asserts nothing about the list background, so no test needs loosening). PLAN.md:435 already describes the flat-rail style and needs no change. README.md has no tab-bar claim. The git working tree is clean except .coding bookkeeping.

## Steps
- [x] 1. **Restore bg-bg-secondary on the agent tab rail** — In frontend/src/components/layout/MainPanel.tsx line 90, change the TabsList className from `flex items-stretch overflow-x-auto border-b border-border` to `flex items-stretch overflow-x-auto border-b border-border bg-bg-secondary` (add bg-bg-secondary; keep items-stretch + border-b hairline — no pill padding). Leave the TabsTrigger styling (h-10, cyan 2px bottom border) untouched.
- [x] 2. **Extend the tab-style contract test** — In frontend/src/components/layout/MainPanel.tabStyle.test.ts, add a test asserting the TabsList keeps `border-b border-border bg-bg-secondary` (locks the rail's distinct chrome-band background so a future restyle can't silently drop it again). Keep all existing assertions unchanged.
- [x] 3. **Run frontend tests + build** — In frontend/: `npm test` (vitest) must pass, then `npm run build` (tsc + vite) must succeed. PowerShell: run commands unpiped and read $LASTEXITCODE (never trust a piped exit code).
