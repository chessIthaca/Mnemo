# Plan: Anthropic Messages API endpoint + agent.md review-expectations (docs + multi-platform)

## Goal
Add a native Anthropic Messages API endpoint kind (works with any Anthropic-compatible provider via configurable base_url + stored key), and amend agent.md so the reviewer checks documentation-update needs and multi-platform (macOS + Windows) neutrality.

## Kind
implementation

## Context
Crashed mid-planning — no plan was persisted (verified: no plan file, no memory marker exists). User confirmed the Anthropic endpoint must be the NATIVE Messages API and must work for non-Anthropic providers (OpenRouter/gateways) via base_url + stored key. Architecture (verified): EndpointKind {OpenAI,Local} (src/config/endpoints.rs:121) → ProviderKind {OpenAI,Local} (src/provider/mod.rs:86) → Capabilities; single client src/provider/openai.rs implements LlmClient (trait at mod.rs:318: capabilities/kind/model/complete); client_factory.rs centralizes key resolution (stored → OPENAI_API_KEY → ANTHROPIC_AUTH_TOKEN → dummy, line 29) + build_openai_client (used by main.rs:901, settings.rs save re-sync:203, model_resolver); vision.rs wraps OpenAiClient (OpenAI-compat only); list_models/list_vision_models (src-tauri/src/ipc/models.rs) resolve key the same way and GET {base}/models with Bearer; frontend KIND_OPTIONS=["openai","local"] (frontend/src/components/settings/types.ts:158), EndpointCard fetch calls listModels(endpoint.name, base_url, apiKey); validation in src/config/patch.rs:54 (kind match) + endpoint_kind_wire (settings.rs:499) + EndpointDto. Anthropic API contract: POST {base}/messages, headers x-api-key + anthropic-version: 2023-06-01, required max_tokens, single hoisted system field, content blocks (text/image/tool_use/tool_result), SSE events (message_start, content_block_start/delta/stop, message_delta, message_stop), stop_reason values end_turn/tool_use/max_tokens/stop_sequence, usage in message_start.input_tokens + message_delta.output_tokens. GET {base}/models returns {data:[{id,display_name}]} (parse_models_with_vision already reads data[].id). Vision/embedder are OpenAI-compat-only — Anthropic kind must error clearly there. agent.md currently holds only project-specific policy (universal review rules live in the compiled APP_RULES) — the two new review expectations (docs-check + multi-platform) belong in agent.md. README.md:45/90 and PLAN.md:122-124/154 mention endpoints/providers and need a line for the anthropic kind. Working tree has only .coding/ bookkeeping changes (backlog.json, stack.json) — clean for branching.

## Steps
- [x] 1. **Kind plumbing: EndpointKind::Anthropic + ProviderKind::Anthropic + capabilities** — Add the Anthropic variant end to end: (1) src/config/endpoints.rs — `EndpointKind::Anthropic` in the enum (serde rename_all=lowercase gives wire form "anthropic"); update doc comment. (2) src/provider/mod.rs — `ProviderKind::Anthropic` + `Capabilities::anthropic()` (supports_tool_choice: true, supports_strict_schema: false, supports_parallel_tools: true, reliable_finish_reason: true, max_context: 200_000, max_output_tokens: 32_000, multimodal: false) + wire into capabilities()/capabilities_with_overrides match. (3) src/config/patch.rs validate_endpoint kind match (~line 54): accept "anthropic" → EndpointKind::Anthropic; update the error message listing. (4) src-tauri/src/ipc/settings.rs endpoint_kind_wire (~line 499): "anthropic". Update affected tests: endpoints.rs parses_two_endpoints area (add an anthropic case), patch.rs validate_endpoint_rejects_unknown_kind still uses "claude" — keep it ("claude" remains unknown) but add an accepts-anthropic test, settings.rs rejects_unknown_kind currently uses "anthropic" — CHANGE it to "claude" since anthropic is now valid; endpoint_kind_wire_matches_serde add assert. Run `cargo test` (unpiped, read test result lines).
- [x] 2. **AnthropicClient: config + request builder (src/provider/anthropic.rs)** — New file src/provider/anthropic.rs (copyright header: `// Copyright (c) 2026 Carsten Hess` + SPDX line matching openai.rs). Contents: (a) `AnthropicClientConfig` {base_url, api_key, model, max_context, max_output_tokens, multimodal} — mirror OpenAiClientConfig. (b) `AnthropicClient` struct {config, caps, http_client (reqwest with 30s connect timeout, gzip/brotli/deflate, redirect Policy::none — copy the OpenAiClient::new_with_trace builder pattern), trace: RwLock<Option<Arc<LlmRequestLog>>>}. (c) `new`/`new_with_trace` ctors. (d) `build_request_json(messages, tools, tool_choice) -> Result<serde_json::Value>` — Anthropic shape: top-level {model, max_tokens (caps.max_output_tokens.max(4096)), stream: true, system: <hoisted concatenation of all Role::System message texts>, messages: [non-system messages], tools: [{name, description, input_schema: parameters}], tool_choice when Some(ToolChoice) (Auto→omit, Function(name)→{type:"tool",name}, Required→{type:"any"})}. Message mapping: user → content array [{type:"text",text}] or image blocks [{type:"image",source:{type:"base64"|"url",...}}] — parse ContentPart::ImageUrl url: data:image/png;base64,xxx → base64 block (media_type from prefix, fallback image/png), else url block; strip images when !multimodal (match openai.rs behavior). assistant with tool_calls → content [{type:"text",text}] + [{type:"tool_use",id,name,input: parsed arguments JSON (fallback {} on parse error)}]; echo reasoning_content as thinking block when present. tool role → [{type:"tool_result",tool_use_id: tool_call_id, content: text, is_error: false}]. (e) complete() impl: build request, POST {base_url}/messages with headers x-api-key + anthropic-version: 2023-06-01 + content-type, error mapping via check_response-style helper (reuse pattern from openai.rs — copy the status→Error::Provider mapping with body suppression for 401), stream the SSE body through the parse loop (step 3's parser). Unit tests in-file: build_request_json system hoisting (two system messages concatenated, none left in messages), tool_use/tool_result round-trip, image base64 + url blocks, multimodal=false strips images, max_tokens field, tool_choice variants, tools input_schema. Run cargo test.
- [x] 3. **AnthropicClient: SSE stream parser → LlmEvent** — In src/provider/anthropic.rs: (a) `parse_sse_buffer(&mut String) -> Vec<SseOutcome>` — copy the line-splitting/drain pattern from openai.rs:1305-1332 but keep `event:` lines: track current event name across lines within one chunk; only `data:` lines carry payload. Event map: message_start → capture message.usage.input_tokens (+ model id for later); content_block_start → index+content_block{type,id,name} — if type==tool_use emit ToolCallStart{index,id,name}, if text store placeholder; content_block_delta → delta.type: text_delta→TextDelta, thinking_delta→ReasoningDelta, input_json_delta→ToolCallArgumentDelta{index,fragment:delta.partial_json}; content_block_stop → (no-op, tool blocks close); message_delta → capture usage.output_tokens + stop_reason; message_stop → emit Finish{reason} + Usage{prompt:input, completion:output, reasoning:0, cached:0, ttft_ms/generation_ms captured at loop level}; error event → emit LlmEvent::Error. FinishReason mapping: end_turn/stop_sequence→Stop, tool_use→ToolCalls, max_tokens→Length, other→Other(s). Unknown event types ignored. (b) complete(): reqwest response → bytes_stream, accumulate into buffer, feed parse_sse_buffer, map SseOutcome::Done→Finish, buffer SseOutcome::Error, emit Usage once (at message_stop, using captured input/output + loop timings). Unit tests (in-file, mirroring openai.rs parse tests): full canonical stream fixture (message_start → content_block_start(text) → text_delta x2 → content_block_stop → content_block_start(tool_use) → input_json_delta x2 → content_block_stop → message_delta(usage, stop_reason=tool_use) → message_stop) asserting exact LlmEvent sequence; thinking_delta; error event; incomplete trailing line retention; multi-line chunk. Run cargo test.
- [x] 4. **Factory + wiring: client_factory.rs, main.rs, settings.rs re-sync** — (1) client_factory.rs: `provider_kind` (line 39) becomes total — replace with match returning Option or extend enum mapping Anthropic→ProviderKind::Anthropic, but build_openai_client must NOT be used for anthropic (OpenAiClient can't speak Messages): add `anthropic_client_config(...)` + `build_anthropic_client(config, endpoint, model, multimodal, trace)` mirroring the openai ones. Add a new `build_client(config, endpoint, model, multimodal, trace) -> Arc<dyn LlmClient>` dispatching on endpoint.kind: OpenAI/Local → build_openai_client, Anthropic → build_anthropic_client. `resolve_api_key` gains the endpoint kind: Anthropic → stored → ANTHROPIC_API_KEY → ANTHROPIC_AUTH_TOKEN → dummy; OpenAI/Local → stored → OPENAI_API_KEY → ANTHROPIC_AUTH_TOKEN → dummy. Vision: build_vision_client (and embedder path if it uses provider_kind) must return a clear Err for EndpointKind::Anthropic ("vision endpoint must be OpenAI-compatible") — grep all `EndpointKind::` match sites for exhaustiveness breakage (config_io.rs:379, console.rs:2023, factory.rs:839 are ProviderKind — check each still compiles). (2) src-tauri/src/main.rs provider build (~line 896): call build_client instead of build_openai_client (same args). (3) src-tauri/src/ipc/settings.rs save re-sync (~line 203): same swap to build_client. (4) Update client_factory tests: endpoint() helper + config_resolves_key_and_maps_kind extended with Anthropic case; add resolve_api_key test asserting ANTHROPIC_API_KEY preference for anthropic kind. Run cargo test (root) + cargo build --manifest-path src-tauri/Cargo.toml (src-tauri compiles separately).
- [x] 5. **Model listing: kind-aware list_models/list_vision_models** — Anthropic-kind endpoints need x-api-key + anthropic-version headers on GET {base}/models (response {data:[{id,display_name,created_at}]} — parse_models_with_vision already reads data[].id so vision_capable=false, caps=None; acceptable). (1) src/provider/openai.rs or new helper: add `fetch_models_anthropic(base_url, api_key) -> Result<Vec<ModelWithVision>>` (same client builder pattern, headers x-api-key + anthropic-version: 2023-06-01, parse via parse_models_with_vision). (2) src-tauri/src/ipc/models.rs: both list_models and list_vision_models take a new `kind: Option<String>` param; after resolving base_url/api_key, when kind (or the saved endpoint's kind) == "anthropic" call fetch_models_anthropic, else fetch_models_with_vision. Update doc comments. (3) frontend/src/lib/tauri.ts: listModels/listVisionModels signatures gain `kind: string` param passed to invoke. (4) frontend/src/components/settings/sections/EndpointCard.tsx: fetchServerModels + testConnection pass `endpoint.kind`. Run cargo test + npm test + npm run build.
- [x] 6. **Frontend: KIND_OPTIONS + kindFromConfig passthrough** — frontend/src/components/settings/types.ts: (a) KIND_OPTIONS (line 158) → ["openai", "local", "anthropic"] as const; (b) kindFromConfig (line 203) → `return k === "local" ? "local" : k === "anthropic" ? "anthropic" : "openai";` with updated doc comment (get_config always emits serde form, this validates). EndpointCard kind dropdown auto-renders from KIND_OPTIONS (line 337) — no change needed there. EndpointEditable kind doc comment in tauri.ts (line 866) → "openai", "local", or "anthropic". Run npm test + npm run build.
- [x] 7. **agent.md amendments + docs (README/PLAN.md)** — (1) agent.md — add a new `## Review expectations` section (project-specific; universal review mechanics live in the compiled prompt) with two bullets: (a) the reviewer checks whether the change requires documentation updates (README.md, PLAN.md, module doc comments, config examples) and reports missing/needed updates as findings; (b) the reviewer verifies the change is multi-platform — no Windows-only APIs, paths, or shell syntax in library/app code (must build + behave on both macOS and Windows; cfg(windows)-only additions are findings). (2) README.md: line 45 feature list + line 90 config sentence — mention native Anthropic Messages API endpoints alongside OpenAI-compatible (one phrase each). (3) PLAN.md: provider strategy rows (~122-124) + ProviderKind enum (~154) — note Anthropic kind added (native /v1/messages, no strict schemas, tool_use blocks). Keep edits minimal. No code changed — cargo test unchanged, run it anyway before proceeding.
- [x] 8. **Full verification, memory marker, review, commit** — (1) Run cargo test (root, unpiped) + cargo build --manifest-path src-tauri/Cargo.toml + cd frontend && npm test && npm run build — all green, zero warnings (deny(warnings)). (2) memory_write tier=semantic STEP MARKER: 'Anthropic endpoint plan — steps 1-7 done, in review' with plan id, branch feat/anthropic-endpoint, key file list, per protocol v2. (3) Spawn reviewer (role:reviewer) over ALL uncommitted changes (git diff HEAD): list changed files + plan goal (native Anthropic Messages client, vendor-agnostic, agent.md review-expectations + multi-platform). Ask for correctness/bugs/security/constitution findings AND explicitly: docs-update check (README/PLAN.md/module docs) + multi-platform neutrality (no Windows-only code). Reviewer writes report to .coding/reviews/. (4) Read report, fix EVERY finding (justify only factually-wrong ones), re-run cargo test. (5) git add + commit to feat/anthropic-endpoint including the review report. (6) finish with the report path.
