# Plan: Fix vision model picker hiding the user's vision model

## Goal
Stop the Vision settings model picker from hiding the user's vision model. Show ALL models (sorted vision-capable-first, with a badge) instead of filtering to only flagged-capable models when modality is known.

## Context


## Steps
- [x] 1. **Extend ModelPickerDropdown with an optional vision badge** — In frontend/src/components/settings/sections/ModelPickerDropdown.tsx: add an optional prop `visionCapableIds?: Set<string>` to the props type (with a doc comment). Destructure it. In the `status === "ready"` option map, render each button with `gap-2`; after the truncated model name span, when `visionCapableIds?.has(m)` render a small badge `<span className="ml-auto shrink-0 rounded bg-[color:var(--accent-color)]/15 px-1.5 py-0.5 text-[0.6rem] font-medium text-[color:var(--accent-color)]">vision</span>`. Backward-compatible: EndpointCard doesn't pass the prop so it's unaffected.
- [x] 2. **Show all models, capable-first, in VisionSection** — In frontend/src/components/settings/sections/VisionSection.tsx replace lines 134-139 (the `capable`/`modalityKnown`/`pickerModels`/`filteredModels` block) with: `const capable = visionModels.filter((m) => m.vision_capable);` then `const capableIds = new Set(capable.map((m) => m.id));` then `const pickerModels = [...visionModels].sort((a, b) => Number(b.vision_capable) - Number(a.vision_capable)).map((m) => m.id);` (capable sorted to top, never hidden) then keep `filteredModels` unchanged (filter by pickerQuery). Pass `visionCapableIds={capableIds}` to the `<ModelPickerDropdown>` (line ~269).
- [x] 3. **Update the vision note text** — In VisionSection.tsx replace the note paragraph (lines 287-295) so it no longer references `modalityKnown`: when `capable.length > 0` show `${capable.length} of ${visionModels.length} model(s) are vision-capable (shown first)`; else when `visionModels.length > 0` show `No models reported as vision-capable — pick your vision model`; else `No models available`.
- [x] 4. **Build + test** — Run `cd frontend && npm run build` (confirm tsc passes) and `cargo test` (no backend change, but constitution requires it before completing). Fix any breakage.
