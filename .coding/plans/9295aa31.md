# Plan: Laya foundation — opt-in Classifier trait + Laya backend + status + Settings (backlog bb54bdcc)

## Goal
Ship the Laya foundation (backlog bb54bdcc): a Classifier abstraction mirroring Embedder (src/memory/classifier.rs) + a Laya HTTP backend (POST /v1/systemone) + config-driven build_classifier (src/provider/client_factory.rs) + status via Tauri IPC + a new Settings section (enable toggle, endpoint URL, status, install hints). Disabled/absent ⇒ zero behavior change; enabled ⇒ stub-server round-trip test (acceptance). Docs synced, warning-free, cargo test green.

## Kind
implementation

## Context
Source: user-dispatched backlog item bb54bdcc ('Laya classifier foundation: opt-in sidecar + Classifier trait') — item 1 of the 5-item Laya chain; items 2-5 (091e694d pre-prompt model routing, e2c47d5f tool-choice steering, 1a4049c1 error/flaky-test triage, a147b63c memory auto-typing) depend on this foundation and will consume it later — NO consumer wiring in this plan. Laya = convaiinnovations/laya (Apache 2.0, ~33 ms non-autoregressive calibrated text classifier, 'System 1'), served by laya-serve (pip install laya[serve]) as POST /v1/systemone (TypeSafe-Jev-compatible shape); never generates text; typed questions choice/score/noul. HARD REQUIREMENT: opt-in, disabled by default — when disabled/not installed the app behaves exactly as today (no classifier calls, no startup cost, no new failure modes). Acceptance: cargo test green with Laya disabled + behavior unchanged; enabled ⇒ a test classification round-trips end-to-end against a stub HTTP server; Settings shows enabled/disabled + endpoint. Mirror targets: src/memory/embedder.rs (Embedder trait / HashEmbedder / EmbedderStatus / shared Arc<RwLock<..>>), build_embedder + EmbedderStartupPlan at src/provider/client_factory.rs:313, call sites src-tauri/src/main.rs:1066 (build_brain_inner, ~:1228-1287) and src-tauri/src/ipc/rewire.rs:21/38, runtime field + accessor src-tauri/src/ipc/state.rs:150/:323, command src-tauri/src/ipc/settings.rs:30 (registered at src-tauri/src/main.rs:804), snapshot src-tauri/src/ipc/startup.rs:40/:145/:157, UI EmbeddingSection.tsx + SettingsDialog.tsx:43/:391 + tauri.ts. Config-field checklist (from bundled_embedding_model): src/config/general.rs (:108/:252/:279/:1012-1060), src/config/settings_dto.rs (:253/:536), src-tauri/src/ipc/settings.rs (:558/:853), src-tauri/src/ipc/contract_fixtures.rs (:240), frontend/src/lib/ipc-fixtures/dto-get-settings.json. HTTP test-stub pattern: one-shot tokio::net::TcpListener at 127.0.0.1:0 (src/provider/openai/tests.rs:3321 etc.) — no mock crates in deps. Env: Windows 11 / PowerShell (chain with ';' not '&&'); run cargo test unpiped, read 'test result:' lines; #![deny(warnings)] at both crate roots — zero warnings; src-tauri test leg has a known Application Control caveat (memory d67ac741) — attempt it, follow that memory's documented fallback if blocked, record it. Scope: (a) sidecar install automation DEFERRED for v1 — Settings ships enable toggle + endpoint URL + install hints ('pip install laya[serve]; laya-serve'); (b) fine-tuning/temperature calibration explicitly out of scope (base checkpoints near-chance zero-shot on custom tasks; probabilities over-confident until refit — later work); (c) no auth field for v1 (laya-serve LAYA_API_KEY bearer can follow). Keep everything cross-platform (no cfg(windows)-only additions).

## Steps
- [x] 1. **Config: Laya section, disabled by default (Rust + contract fixtures)** — Add an opt-in Laya config to GeneralSection in src/config/general.rs — struct LayaConfig { enabled: bool (serde default false), endpoint: Option<String> } — doc-commenting the hard requirement: disabled ⇒ zero behavior change. Wire the Default impl (:252-266) and serde defaults like bundled_embedding_model (:108-109, :279). Settings save path: src/config/settings_dto.rs (patch field ~:253, apply ~:536) and src-tauri/src/ipc/settings.rs (DTO ~:558, get_settings mapping ~:853). Contract fixtures MUST change together: src-tauri/src/ipc/contract_fixtures.rs (~:240) + frontend/src/lib/ipc-fixtures/dto-get-settings.json. Tests mirroring general.rs:1012-1060: defaults to disabled; explicit enable round-trips; absent key = disabled.
- [x] 2. **Classifier trait + status + no-op backend (src/memory/classifier.rs)** — New src/memory/classifier.rs (export from src/memory/mod.rs): Classifier trait (async_trait, Send+Sync) with the Embedder-style failure-protected contract — classify(&self, q: &Question) -> Option<Classification> (None on failure; never panics), plus typed-question types for Laya's three shapes (choice/score/noul) and Classification { label, probability }. ClassifierStatus enum: Disabled (default) / Ready / Failed, serde lowercase, shared via Arc<RwLock<ClassifierStatus>> (mirror src/memory/embedder.rs:83-118). Doc comments on all public items (constitution). Unit tests: serde shapes, Disabled default, None-path contract.
- [x] 3. **Laya HTTP backend + stub-server round-trip test** — LayaClassifier in src/memory/classifier.rs (or src/memory/laya.rs): reqwest POST {endpoint}/v1/systemone with the Laya typed-question JSON; parse the calibrated probability into Classification; bounded timeout; failure ⇒ None + status Failed (no retries, no panics, never blocks startup). Pin the wire shape from the Laya docs (https://huggingface.co/convaiinnovations/laya; laya-serve README) — keep the mapping in one serializer/parser fn so a shape correction is a one-place change. Tests: one-shot tokio::net::TcpListener::bind("127.0.0.1:0") stub (pattern: src/provider/openai/tests.rs:3321) — choice-question round-trip (the acceptance test); malformed response + timeout ⇒ None + Failed; nothing is called when the backend is never constructed.
- [x] 4. **build_classifier + construction tests** — src/provider/client_factory.rs next to build_embedder (:313): pub fn build_classifier(config: &Config, status: Arc<RwLock<ClassifierStatus>>) -> Option<Arc<dyn Classifier>> — disabled or no endpoint ⇒ None + status Disabled (NO client built, no calls — the hard requirement); enabled + endpoint ⇒ Some(LayaClassifier). Tests mirroring client_factory.rs:596-926: disabled ⇒ None (regression pin for 'zero behavior change'); enabled ⇒ Some + status; enabled-but-no-endpoint ⇒ None (logged, not fatal).
- [x] 5. **Runtime + IPC: status field, command, snapshot, rewire, startup** — Mirror the embedder plumbing: runtime field classifier_status: Arc<RwLock<ClassifierStatus>> + accessor in src-tauri/src/ipc/state.rs (:150/:323); init + build_classifier call in src-tauri/src/main.rs (build_brain_inner ~:1228-1287; IpcState wiring ~:519); command get_classifier_status in src-tauri/src/ipc/settings.rs (~:30), registered in the handler list (src-tauri/src/main.rs:804); snapshot field in src-tauri/src/ipc/startup.rs (:40/:145/:157) + the frontend snapshot type (frontend/src/lib/tauri.ts ~:254); rebuild on settings save in src-tauri/src/ipc/rewire.rs (~:21/38) with a status event mirroring 'embedder://status' (src-tauri/src/main.rs ~:560-595) if straightforward. Store the built classifier (Option<Arc<dyn Classifier>>) on the runtime for items 2-5.
- [x] 6. **Settings UI: Laya section** — New frontend/src/components/settings/sections/ClassifierSection.tsx mirroring EmbeddingSection.tsx (forwardRef + active + onDirtyChange save contract): enable toggle, endpoint URL input, status line ('disabled' / 'ready @ host:port' / 'failed'), install hints ('pip install laya[serve]'; run 'laya-serve'). Register in frontend/src/components/settings/SettingsDialog.tsx (:43 import, :391 render). Bindings: frontend/src/lib/tauri.ts (getClassifierStatus -> invoke('get_classifier_status'); snapshot/listener types per the previous step). Draft/serialize the new config field in frontend/src/components/settings/types.ts (+ seeds if the pattern requires). Frontend tests (vitest) mirroring the Embedding section's: renders, dirty/save flow, disabled-state display.
- [x] 7. **Docs sync** — Update README.md (feature list) and docs/FEATURES.md + docs/CONFIGURATION.md (new [general] Laya keys + how to run laya-serve) — check PLAN.md for provider/integration notes needing a line. Module doc comments in src/memory/classifier.rs covering the opt-in contract + failure-protected semantics.
- [x] 8. **Full verification** — cargo test at the repo root (Cargo.toml; unpiped — read 'test result:' lines): existing suite green with Laya disabled = no behavior change, plus the new tests in src/memory/classifier.rs, src/provider/client_factory.rs and src/config/general.rs pass; zero warnings under #![deny(warnings)] (src/lib.rs). src-tauri leg: cargo test under src-tauri/Cargo.toml — new command tests in src-tauri/src/ipc/settings.rs + fixtures in src-tauri/src/ipc/contract_fixtures.rs; the known Application Control caveat is memory d67ac741 (follow its documented fallback if blocked; record the residual). Frontend: cd frontend; npx tsc --noEmit; npm test per frontend/package.json (new ClassifierSection tests); npm run build if the config/UI contract changed (frontend/src/lib/ipc-fixtures/dto-get-settings.json). Confirm acceptance: disabled path adds zero calls; enabled path round-trips (stub test); status surfaces enabled/disabled + endpoint.
