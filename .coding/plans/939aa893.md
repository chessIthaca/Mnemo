# Plan: Full architecture, performance & security review

## Goal
Produce a comprehensive three-axis review report covering architecture, performance, and security of the Mnemo codebase at HEAD, identifying findings with severity ratings and actionable recommendations.

## Kind
research

## Context


## Steps
- [x] 1. **Architecture axis** — Examine module structure, dependency graph, separation of concerns, coupling/cohesion, the agent factory + multi-agent runtime design, the workflow state machine, the config layering (global vs project), the memory subsystem (4-tier + knowledge files + derived index), the codegraph subsystem, and the IPC boundary between Rust and the Tauri frontend. Identify architectural debt, overly coupled modules, missing abstractions, and design risks. Compare against the 2026-10-15 architecture review to find what's been addressed and what's new.
- [x] 2. **Performance axis** — Examine hot paths (turn loop, token counting, context compaction, memory recall, codegraph indexing), allocation patterns, lock contention (RwLock/Mutex usage across factory + agent loops + memory store + backlog), DB query efficiency (SQLite FTS5 + codegraph queries), streaming efficiency, and the file watcher / indexer. Identify bottlenecks, unnecessary allocations, lock-hold concerns, and scaling risks. Compare against the 2026-12-15 performance review.
- [x] 3. **Security axis** — Examine the full attack surface: API key storage/handling (keys.toml, Windows DACL/0o600), path traversal protections (sandbox, protected .coding/ state), shell command safety (cmd classification, core-operations gate, flag injection rejection), MCP server security (env-var-only secrets, approval gating, OAuth PKCE), the IPC boundary (what commands expose what data, secrets isolation), prompt injection risks, the browser tooling surface, and the review-report protection mechanism. Identify security gaps, missing validation, privilege escalation paths, and data leakage risks.
- [x] 4. **Synthesize + write report** — Combine all three axes into a single review report at .coding/reviews/2026-12-17-full-arch-perf-security-review.md with a verdict line, findings grouped by axis and severity (high/medium/low), and actionable recommendations. Cross-reference prior reviews for resolved vs new findings.
