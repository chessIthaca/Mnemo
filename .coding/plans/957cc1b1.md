# Plan: Review remediation sweep — all findings (M1-M3, L1-L6)

## Goal
Remediate every actionable finding from the 2026-12-15 full three-axis review: 3 medium (M1 adapter extraction, M2 codegraph mtime, M3 module split) and 6 low (L1–L6), each landing green on cargo test.

## Kind
implementation

## Context
Consolidated findings from three parallel reviewer reports in .coding/reviews/ (2026-10-15 architecture, 2026-12-15 quality, 2026-12-15 performance). Zero critical/high across all axes. Nine actionable items remain: M1 domain-logic-in-adapter (events.rs 1757 lines + settings.rs 1917 lines, grew across 3 reviews), M2 codegraph re-reads every file per pass (codegraph/mod.rs:272), M3 oversized memory/mod.rs (4046 lines), L1 dead MockGit helpers + #[allow(dead_code)] (git_ops.rs:762,790), L2 #[allow(clippy::too_many_arguments)] on 9-arg AgentLoop::new (loop_impl.rs:486,531), L3 blocking std::fs::metadata stat on async runtime (agent_md.rs:74 + safety_rules.rs:253), L4 build_stable_head rebuilds String every turn (prompt.rs:392), L5 response_text unbounded within a turn (openai.rs:878), L6 heavy native deps not feature-gated (Cargo.toml). Branch wt/agenticcoding carries the uncommitted review reports.

## Steps
- [x] 1. **Delete dead MockGit test helpers (L1)** — In src/project/git_ops.rs, delete the MockGit::set_sha method (~:762) and MockGit::calls method (~:790) along with their #[allow(dead_code)] annotations — both have zero callers (verified by the quality reviewer). Run `cargo test`; the no-#[allow] rule is now fully complied in project code. Commit.
- [x] 2. **Bound the repetition-detector buffer (L5)** — In src/provider/openai.rs (~:878), response_text accumulates every TextDelta for the whole stream but detect_repetition (:1543) only inspects the last ~600 bytes (REPETITION_WINDOW×threshold). After each detect_repetition call, truncate response_text to the last ~1.2 KB so the buffer stays bounded. Add a regression test asserting the buffer does not exceed the bound after many deltas. cargo test green, commit.
- [x] 3. **Codegraph mtime fast-path (M2)** — In src/codegraph/mod.rs index() (~:272), before std::fs::read(path), compare the stored mtime (already in cg_content_meta) to mtime_of(path); skip the read+hash when unchanged. The content hash remains the authoritative change signal when mtime did advance. Add a test that indexing twice with no file change skips the re-read. cargo test green, commit.
- [x] 4. **AgentLoopConfig struct (L2)** — In src/agent/loop_impl.rs introduce an AgentLoopConfig struct grouping the 9 constructor params of AgentLoop::new (:486) and with_constitution_source (:531); remove both #[allow(clippy::too_many_arguments)] annotations; update AgentLoopFactory (factory.rs) and other callers to build the config struct. cargo test green, commit.
- [x] 5. **Cache the stable system-prompt head (L4)** — In src/agent/prompt.rs build_stable_head (~:392), cache the built head String so it is not rebuilt every turn; invalidate only when the constitution actually reloads (wire through ConstitutionHolder's reload signal / a generation counter at loop_impl.rs:266). The volatile tail continues to rebuild each turn. Add a test that the head is built once across turns unless the constitution changes. cargo test green, commit.
- [x] 6. **Move blocking stat probes off the async runtime (L3)** — Two sites do a blocking std::fs::metadata on a tokio worker: ConstitutionHolder reload_if_changed (src/project/agent_md.rs:74, called every loop iteration via turn.rs:663→loop_impl.rs:266) and safety_rules is_safe (src/safety_rules.rs:253, per tool call). Evaluate whether spawn_blocking is net-positive (a stat is microseconds; task-spawn overhead may exceed it). If spawn_blocking is net-negative, apply the least-invasive correct alternative (e.g. throttle the mtime check to once-per-turn rather than once-per-iteration, or accept with a clearer inline rationale). Document the decision in code. cargo test green, commit.
- [x] 7. **Feature-gate heavy native deps (L6)** — In Cargo.toml add a [features] section with default = ["embeddings", "codegraph", "browser"] and gate fastembed/ONNX, chromiumoxide, and tree-sitter (+grammars) behind those features with cfg gates at usage sites. The default build must remain unchanged (all features on). Verify cargo build (default) and a --no-default-features build both compile. Lowest-priority item; if the cfg-gating surface is too invasive/risky, document and defer. cargo test green, commit.
- [x] 8. **Extract EventCoordinator into the brain (M1a)** — Move the domain orchestration currently in src-tauri/src/ipc/events.rs (1757 lines) into src/runtime/: TurnResolveLatch (:110), notify_parent_on_completion (:829), the failed-reviewer protocol (:867-884), and workflow-transition tracking (:253). Define a brain-side trait the IPC forwarder calls; events.rs keeps only Tauri-emit + oneshot-storage. Preserve all existing behavior and tests. cargo test green, commit.
- [x] 9. **Extract SettingsAssembler into config (M1b)** — Move config assembly + domain validation from src-tauri/src/ipc/settings.rs (1917 lines) into src/config/: endpoint/key/pricing persistence, the non-endpoint settings patch with inline validation, and runtime provider rewires. settings.rs becomes a thin Tauri-command shim delegating to the brain. Preserve all behavior and tests. cargo test green, commit.
- [x] 10. **Split memory/mod.rs into submodules (M3)** — Split src/memory/mod.rs (4046 lines) along cohesion seams: memory/store.rs (schema+DDL+open+conn split), memory/recall.rs (FTS5 fetch + cosine KNN + scoring weights), memory/embeddings.rs (BLOB encode/decode). Re-export the public API from memory/mod.rs so external callers are unaffected; move the #[cfg(test)] block to memory/tests.rs. cargo test green, commit. Optionally split src/tool/workflow/plan.rs (3353 lines) by tool group if time permits.
