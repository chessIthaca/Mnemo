# Plan: Project open/create progress feedback (486955d5)

## Goal
Close the progress-feedback gaps across the project open/create flow (backlog 486955d5): no white flash while the app restarts into a project (a static pre-React splash in index.html), a visible "Starting Mnemo…" boot state during the pre-index phases (BootSplash early return in App), and phase-aware picker buttons ("Preparing project…" / "Restarting…" instead of a bare "Working…").

## Kind
implementation

## Context
Gap analysis (verified in code): the codegraph index pass is covered by IndexingOverlay + codegraph://index-progress (and is now fast, plan 85368a1f), but the rest of the open/create flow is silent. (1) RESTART WINDOW: switch_project (src-tauri/src/ipc/projects.rs:201) writes the pending-project marker and restarts the process; frontend/index.html has NO styling (empty #root, class="dark") — the relaunched window shows a WHITE flash, then a dark empty screen, until the CSS+JS bundle loads and React mounts. (2) PRE-INDEX PHASES: App.tsx has NO early return for !checkedStartup — while getNeedsProject/getStartupSnapshot wait on the backend (build_brain blocks the setup hook at main.rs:288; IpcState is managed only after), App renders its full main UI shell with empty data. (3) PICKER FEEDBACK: ProjectPicker.tsx:225 `{busy ? "Working…" : "Create & open"}` is the only feedback during handleCreate (scaffold → seed index [overlay-covered] → register → restart) and handleOpen (validate → marker → restart; the Open buttons don't even change text). DESIGN (user chose "Full polish"): (a) a static pre-React splash inside #root in index.html — inline-styled (background #0f172a = --bg-primary dark, matching the app default; the light-mode override is a user setting the pre-JS splash cannot know — documented decision), lowercase "mnemo" text wordmark + "Starting…" + a tiny CSS-animated spinner via an inline <style> in <head> (zero JS, zero bundle dependency); React's createRoot(#root).render() replaces it on mount. (b) frontend/src/components/common/BootSplash.tsx rendering the SAME visual (text wordmark, not MnemoLogo — the static splash can't use React components, so both use the text wordmark for a seamless handoff) + an `if (!checkedStartup) return <BootSplash />` early return in App.tsx placed before the picker/error returns. (c) frontend/src/components/projects/projectPickerPhases.ts — `type BusyPhase = "preparing" | "restarting"` + pure `createButtonLabel(phase)`/`openButtonLabel(phase)` (the vitest node-env pattern has no DOM testing, so the phase logic is exported pure functions per the messageArgLabel/applyIndexProgress precedent); ProjectPicker.tsx wires them: handleCreate sets "preparing" before createProject, "restarting" before switchProject; handleOpen sets "restarting"; error paths reset to null; `busy = phase !== null`. TESTS: BootSplash.test.tsx (renderToStaticMarkup — wordmark + "Starting" present) + projectPickerPhases.test.ts (label mapping table) — BOTH registered in frontend/vitest.config.ts test.include (new test files are invisible to vitest without registration). VERIFICATION: cd frontend; npm test; npx tsc --noEmit (frontend-only change — no Rust surface touched, so the frontend suites are the project's tests for this change). Docs sync: check README.md for an open/create flow description that needs the splash/phase mention.

## Steps
- [x] 1. **Static pre-React splash in index.html** — frontend/index.html: add an inline <style> in <head> with a minimal spinner keyframes animation, and inside <div id="root"> a static splash div — position:fixed inset:0, background #0f172a (the dark --bg-primary), centered flex column: lowercase "mnemo" wordmark (letter-spaced, #e2e8f0), "Starting…" (#94a3b8), and the CSS spinner. All inline-styled (no CSS bundle dependency) so the window never flashes white between first paint and React mount — on every startup AND every switch_project restart. React's createRoot(#root).render() replaces this content on mount. Include the rationale comment (why dark-only, why text wordmark).
- [x] 2. **BootSplash component + App early return** — Create frontend/src/components/common/BootSplash.tsx — the React twin of the static splash (same layout/colors/text wordmark so the static→React handoff is seamless; a doc comment explaining it covers the pre-index boot phases while getNeedsProject/getStartupSnapshot wait on the backend). In frontend/src/App.tsx, add `if (!checkedStartup) { return <BootSplash />; }` immediately BEFORE the `if (checkedStartup && needsProject)` picker return (~:594) so the empty main-UI shell never flashes during boot. Add frontend/src/components/common/BootSplash.test.tsx (renderToStaticMarkup: asserts the wordmark + "Starting" text render) and register it in frontend/vitest.config.ts test.include.
- [x] 3. **Phase-aware picker buttons** — Create frontend/src/components/projects/projectPickerPhases.ts — `export type BusyPhase = "preparing" | "restarting"` + `export function createButtonLabel(phase: BusyPhase | null): string` ("Preparing project…" / "Restarting…" / "Create & open") + `export function openButtonLabel(phase: BusyPhase | null): string` ("Restarting…" / "Open"), with doc comments. In frontend/src/components/projects/ProjectPicker.tsx: replace the `busy` boolean state with `const [phase, setPhase] = useState<BusyPhase | null>(null)` + `const busy = phase !== null`; handleCreate sets "preparing" before createProject and "restarting" before switchProject; handleOpen sets "restarting" before switchProject; every error path resets phase to null; the create button label becomes createButtonLabel(phase) and each Open button label becomes openButtonLabel(phase). Add frontend/src/components/projects/projectPickerPhases.test.ts (table-driven label mapping) and register it in frontend/vitest.config.ts test.include.
- [x] 4. **Verify — suites, tsc, docs sync** — Run `cd frontend; npm test` (all suites green including the two new test files) and `npx tsc --noEmit` (clean). Check README.md for an open/create-flow description that needs updating for the splash/phase feedback (docs sync is a review expectation); update if stale. Then spawn the read-only reviewer over the uncommitted changes (changed files: frontend/index.html, frontend/src/App.tsx, frontend/src/components/common/BootSplash.tsx + .test.tsx, frontend/src/components/projects/projectPickerPhases.ts + .test.ts, frontend/src/components/projects/ProjectPicker.tsx, frontend/vitest.config.ts).
