# Plan: Markdown editor upgrade: live preview, formatting toolbar, real save

## Goal
Give the Files-tab Markdown editor real formatting support: a live split-pane preview (headers/sections rendered as you type), a markdown formatting toolbar, and true persistence via a sandboxed write_file IPC — plus dirty tracking, unsaved-changes guard, and line-ending preservation.

## Kind
implementation

## Context
Investigation findings: (1) The md editor is frontend/src/components/views/FileViewer.tsx:357-362 — a bare <textarea>; the Edit/Done toggle never persists (header comment line 9-10 admits Save did not persist); browsed files (absolute paths from the native picker, outside the sandbox) silently lose edits. (2) NO write_file IPC exists — src-tauri/src/ipc/files.rs has read_file/list_files/browse_markdown_file/save_conversation/load_conversation only. save_conversation shows the sandbox-validate → spawn_blocking-write pattern with IpcError. Sandbox has refuse_if_protected (used by file_edit.rs:544) — protected live-state files (.coding/plans/*.md, memory DB, backlog.json, safety.toml) must stay protected from this path too. (3) The shared renderer frontend/src/components/chat/Markdown.tsx (react-markdown + remark-gfm + rehype-highlight, lazy chunk) renders md; MarkdownImpl passes components through. (4) Frontend tests are vitest in NODE env, PURE helpers only (no @testing-library) — colocated *.test.ts files must be added to frontend/vitest.config.ts include list (pattern: language.test.ts, promptHistory.test.ts). (5) Rust: src-tauri is a separate package — root cargo test does NOT compile it; must run cargo test in src-tauri too. #![deny(warnings)]; all pub fns need doc comments; no #[allow]. Windows/PowerShell; run cargo test unpiped and read test-result lines/$LASTEXITCODE. (6) tauri.ts wrappers at frontend/src/lib/tauri.ts:952+ (readFile/listFiles); errMsg at :104. Wire convention: invoke("write_file", { path, content }) — Tauri 2 camelCase conversion. DECISIONS (unattended, best judgment): interpret "md file edit does not do well with formatting headers and sections. Can we have a better md formatting?" as the md EDIT experience in the Files tab: add (a) a live split-pane preview so headers/sections render formatted while typing, (b) a formatting toolbar (H1/H2/H3, bold, italic, code, fence, link, lists) as pure text transforms, (c) REAL persistence via a new sandboxed write_file IPC with dirty tracking + unsaved-changes guard + line-ending preservation, (d) clearer heading separation in view mode. Protected files (.coding/plans, etc.) stay non-editable via the shared sandbox refusal (consistent with agent-side rules — avoid desyncing live workflow state). Browsed outside-project files keep the editor but show an explicit "local preview only — cannot save outside the project" hint instead of silently losing edits. New feature branch feat/md-editor (never main). No new npm/cargo deps.

## Steps
- [x] 1. **Add sandboxed write_file IPC (backend)** — From repo root C:\AgenticCoder\AgenticCoder run `git checkout -b feat/md-editor` (never commit on main). Then in `src-tauri/src/ipc/files.rs`: (1) add a doc-commented pure helper `fn write_sandboxed(sandbox: &myharness::tool::agent::sandbox::Sandbox, path: &str, content: &str) -> Result<String, String>` — sandbox.validate(Path::new(path)) (map_err "path rejected: {e}"), then sandbox.refuse_if_protected(&validated) (map_err to its message; import via the crate path used by file_edit.rs — check how files.rs already imports myharness types), then std::fs::write, return Ok(validated forward-slash string). Keep it SYNC so it is unit-testable; the command wraps it in spawn_blocking with owned clones. (2) `#[tauri::command] pub async fn write_file(state: State<'_, IpcState>, path: String, content: String) -> Result<String, IpcError>` — clone sandbox from state (same as read_file), `tokio::task::spawn_blocking(move || write_sandboxed(&sandbox, &path, &content))`, await with the same error-string mapping pattern as read_file, map_err(IpcError::from). Doc comment: persists FileViewer md edits; sandbox-validated; protected live-state files refused with the shared message. (3) Register `ipc::files::write_file` in src-tauri/src/main.rs invoke_handler next to `ipc::files::browse_markdown_file`. (4) Tests in files.rs `#[cfg(test)] mod tests` using tempfile::tempdir + Sandbox::new (check the exact Sandbox constructor used by src/tool/agent/file_edit.rs tests — `Sandbox::new(dir).unwrap()`): writes_content_round_trips (write then read_to_string equals content; returned path is forward-slash); rejects_path_escape ("../x.md" → Err containing "path rejected"); refuses_protected_target (".coding/plans/p1.md" → Err; mirrors file_edit behavior). Run `cargo test` in src-tauri (PowerShell, unpiped, read test-result lines + $LASTEXITCODE) and `cargo test` at root.
- [x] 2. **Create md formatting helpers (mdEdit.ts) + tests** — Create `frontend/src/lib/mdEdit.ts` — pure, DOM-free helpers (node-env vitest can test them). Exports (all with doc comments): (1) `wrapSelection(text: string, selStart: number, selEnd: number, before: string, after: string, placeholder = "text"): { text: string; selStart: number; selEnd: number }` — wraps the selection (or inserts placeholder) and selects the inner text. (2) `setLinePrefix(text: string, selStart: number, selEnd: number, prefix: string): { text: string; selStart: number; selEnd: number }` — prefixes every selected line with `prefix` (used for H1/H2/H3 `# `/`## `/`### `, `- `, `1. `); expands a collapsed cursor to its whole line. (3) `insertBlock(text: string, selStart: number, selEnd: number, block: string): { text: string; selStart: number; selEnd: number }` — inserts `block` on its own lines at the selection (ensures blank-line separation before/after; used for fenced code blocks). (4) `normalizeForSave(original: string, edited: string): string` — if `original` contains "\r\n", collapse edited's endings to \n then expand all to \r\n (mirrors the Rust tools' line-ending preservation); else return edited unchanged. (5) `hasUnsavedChanges(original: string, edited: string): boolean` — trimmed-trailing-newline-insensitive compare (a lone trailing-newline diff is not a change). Create `frontend/src/lib/mdEdit.test.ts` with vitest describe/it covering: wrapSelection with/without selection; setLinePrefix single line, multi-line, collapsed cursor, header prefix; insertBlock mid-document blank-line separation; normalizeForSave CRLF file stays CRLF after LF-typed edits + LF file unchanged; hasUnsavedChanges true/false/trailing-newline case. Add `"src/lib/mdEdit.test.ts"` to the include array in `frontend/vitest.config.ts`. Run `npm test` in frontend/
- [x] 3. **Add writeFile wrapper in tauri.ts** — In `frontend/src/lib/tauri.ts` next to readFile (:952): `export async function writeFile(path: string, content: string): Promise<string>` calling `invoke("write_file", { path, content })` with a doc comment (persists the FileViewer md edit; sandbox-validated backend; returns the written path). No new DTO types needed (string in/out). Run `npm run build` in frontend/ to confirm types compile.
- [x] 4. **FileViewer: split-pane editor + toolbar + save** — Rewrite the md branch of `frontend/src/components/views/FileViewer.tsx`: (1) NEW STATE: `savedContent: string` (last-loaded/saved text), `saving: boolean`, `saveError: string` (""), textarea ref `taRef`. Set savedContent in loadFile, handleBrowse, and openEntry's binary arm (reset together with content). `dirty = content !== savedContent` via hasUnsavedChanges(content, savedContent) — import from ../../lib/mdEdit. (2) UNSAVED GUARD: in loadFile and openEntry, when dirty and editing, `if (!window.confirm("Discard unsaved changes to the current file?")) return;`. (3) SAVE: async `handleSave()` — only for project files (browsed === null); if browsed file, set saveError "Cannot save a file outside the project — edits are local preview only"; else writeFile(path, normalizeForSave(savedContent, content)), on success setContent(written normalized text) + setSavedContent(normalized) + clear saveError, on failure set saveError(errMsg(e)); wrap in setSaving(true/false). Keydown handler on the textarea for Ctrl+S / Cmd+S (preventDefault → handleSave). Header buttons: keep Edit toggle (label Edit/Done), show Save button (lucide Save icon + "Save", cyan when dirty, disabled while saving) only when editing && isMd && browsed === null. Dirty indicator: a small amber dot next to the filename when dirty. (4) SPLIT-PANE EDIT LAYOUT: replace the single textarea with `<div className="flex h-full min-h-0 gap-2">`: left `<textarea>` (flex-1, min-w-0, same styling as today) + right preview `<div className="flex-1 min-w-0 overflow-y-auto rounded border border-border bg-bg-primary p-2 prose prose-invert prose-sm max-w-none prose-pre:bg-bg-primary"><Markdown>{content}</Markdown></div>`. Update the file-header doc comment (lines 1-20) to describe the editor. (5) TOOLBAR: a compact row of buttons above the textarea (H1 H2 H3 | B I ` </> link | ul ol — lucide icons where natural: Bold, Italic, Code, Link, List, ListOrdered, Heading1/2/3; text labels are fine for H1-H3). Each button applies one mdEdit helper to `{ text: content, selStart, selEnd }` from taRef.current (selectionStart/End), then setContent(result.text) and restore selection AFTER the render via requestAnimationFrame(() => taRef.current?.setSelectionRange(...)) + focus. (6) VIEW MODE heading separation: on the existing view-mode md container (line 364) add `prose-h1:border-b prose-h1:border-border prose-h1:pb-1 prose-h2:border-b prose-h2:border-border/60 prose-h2:pb-1`. (7) SHOW saveError as a red text line above the content pane (same styling as the existing error div) and, when editing a browsed file, a slate hint line "Local preview only — files outside the project can't be saved". (8) A FileViewer.test.ts is NOT required (no DOM test infra); instead add the vitest include check if you created any test file. Run `npm run build` + `npm test` in frontend/ (all green, no type errors).
- [x] 5. **Full test + build matrix** — PowerShell, unpiped, read $LASTEXITCODE after each: (1) `cargo test` at repo root; (2) `cargo test` inside src-tauri/ (separate package — root test does not compile it); (3) `npm test` in frontend/; (4) `npm run build` in frontend/. All must be green and warning-free (deny(warnings) enforces zero warnings for Rust). Fix any failures before continuing.
- [x] 6. **Reviewer subagent over all changes** — Run `git status --short` + `git diff HEAD --stat` to enumerate ALL uncommitted changes (expect: src-tauri/src/ipc/files.rs, src-tauri/src/main.rs, frontend/src/lib/mdEdit.ts, frontend/src/lib/mdEdit.test.ts, frontend/src/lib/tauri.ts, frontend/src/components/views/FileViewer.tsx, frontend/vitest.config.ts). Spawn a read-only reviewer (spawn_agent, role:"reviewer") with a SELF-CONTAINED task: the plan goal (proper md editing: split-pane live preview, formatting toolbar, real save via sandboxed write_file IPC, dirty guard, line-ending preservation), the changed-file list, and focus points: (a) write_file sandbox validation + refuse_if_protected parity with the agent file tools (no escape, protected targets blocked, spawn_blocking for the blocking write, no State across await); (b) mdEdit helpers correct for multi-byte/cursor edge cases and CRLF; (c) unsaved-changes guard fires on every content-switch path (loadFile, openEntry, browse); (d) Ctrl+S doesn't trigger default browser save; (e) no new deps; (f) constitution compliance: doc comments on pub items, warning-free, no #[allow], regression tests present. Have it write the report to .coding/reviews/2026-04-20-md-editor-review.md. Do NOT poll — end the turn after spawning; resume when the background-agent-finished notification arrives.
- [ ] 7. **Fix findings, re-test, commit, finish** — Read the reviewer report from .coding/reviews/. Fix EVERY finding (a skip requires written justification that the finding is factually wrong). Re-run the full matrix from the test step (cargo test root + src-tauri, npm test, npm run build). Then commit ALL changes INCLUDING the review report to the current feature branch feat/md-editor (verify `git branch --show-current` is not main) with message like "feat: FileViewer markdown editor — live preview, formatting toolbar, real save". Then call finish with the review report path.
