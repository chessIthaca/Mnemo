# Plan: Finish interrupted bundled-embedder work

## Goal
Properly close out the interrupted bundled-embedder feature: verify it compiles, fix the dead-code constitution violation, run tests, review, and commit.

## Kind
implementation

## Context
The previous session crashed mid-closing-sequence. The workflow is marked Complete but 4 files have uncommitted changes (mid-flight compile-error fixes): embeddings.rs (IpcError::Other→msg), rewire.rs + main.rs (MemoryStoreTrait imports), main.rs (status_for_reembed→_status_for_reembed). The `_status_for_reembed` rename is a constitution violation — it silences an unused-variable warning instead of removing the dead line. No review report exists, nothing was committed. The fixes reference real types (IpcError::msg exists at error.rs:35; MemoryStoreTrait has stored_model_fingerprints + reembed_all), so they're plausible but unverified — cargo test has NOT been run.

## Steps
- [x] 1. **Verify the build compiles** — Run `cargo test` unpiped from the repo root (C:\AgenticCoder\AgenticCoder). Read the `test result:` / `error:` lines directly, not the pipe exit code. If it fails to compile, capture the exact errors before fixing. If it compiles, note any warnings (under `#![deny(warnings)]` a warning fails the build, so green = zero warnings).
- [x] 2. **Fix the dead-code constitution violation in main.rs** — In `src-tauri/src/main.rs` around line 649, DELETE the line `let _status_for_reembed = embedder_status.clone();` entirely (it clones a value never used inside the spawned task — the `_` prefix silences the warning instead of removing the dead code, which the constitution forbids). Do NOT replace it with anything; the spawned task does not use the status handle. Re-run `cargo test` to confirm it still compiles warning-free.
- [x] 3. **Run the frontend build + tests** — Run `cd frontend; npm run build` then `npx vitest run` (check `$LASTEXITCODE` directly, not via a pipe). Fix any failures. The plan's step 7 changed EmbeddingSection.tsx, tauri.ts, StatusBar.tsx, App.tsx — these must build + pass.
- [x] 4. **Spawn a read-only reviewer** — Use spawn_agent with role:"reviewer". Give it a self-contained task: list the 4 changed files (src-tauri/src/ipc/embeddings.rs, src-tauri/src/ipc/rewire.rs, src-tauri/src/main.rs, .coding/plans/921e1669...md) + the plan's goals (bundled embedder: fastembed in-process, download progress, startup re-embed check, rewire path, frontend model picker). Ask for correctness, bugs, security, constitution-compliance (esp. fastembed inference on spawn_blocking not async runtime, download callback doesn't deadlock the status lock, startup re-embed handles cross-machine git case, re-embed-all is fire-and-forget, schema migration idempotent, config field round-trips, no #[allow] suppressions, no dead code). Have it write to `.coding/reviews/<date>-bundled-embedder-review.md`. After spawning, end the turn — resume when the background agent finishes.
- [x] 5. **Act on review findings** — Read the reviewer's report. Fix EVERY finding (never defer; the only permitted skip is a factually-wrong finding with a written justification). Re-run `cargo test` + frontend build after fixes.
- [x] 6. **Commit to the feature branch** — Check `git branch --show-current` (must NOT be main — if it is, create a feature branch). Stage all changes including the review report under `.coding/reviews/`. Commit with a clear message describing the bundled-embedder feature + the interrupted-work fix.
- [x] 7. **Finish** — Call finish with the review report path to close out the plan and reach Complete.
