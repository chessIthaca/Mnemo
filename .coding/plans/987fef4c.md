# Plan: Imperative phrasing for steering notes and tool-strategy guidance

## Goal
Rewrite every advisory "prefer …" guidance sentence (steering notes riding in tool output + the prompt's tool-strategy STABLE HEAD) as short imperatives per backlog 56168c38 — search.rs symbol/alternation/delegation notes, shell redirection warning, file_write/file_edit hints, plan.rs update_plan guidance, prompt.rs tool-strategy lines, and the two codegraph doc-comment mentions — without changing which steering marker fires or its label, keeping the re-issue escape hatch intact.

## Kind
implementation

## Context
Recon 2027-01-14 (backlog 56168c38): user called out "prefer the graph tools for symbol hunts (search is for text)" — the note the search tool emits when a pattern resolves to an indexed symbol. Sites: src/tool/agent/search.rs:622-624 (alternation/graph-miss note tail), :678 and :683-684 (AUTO-DELEGATED block), :2169-2170 (symbol-nudge path); src/tool/agent/shell.rs:51 and src/agent/steering_stats.rs:802 (redirection warning); src/tool/agent/file_write.rs:81; src/tool/agent/file_edit.rs:200; src/tool/workflow/plan.rs:1514; src/agent/prompt.rs:42 and :192 (tool-strategy block — byte-stability test-pinned, see stable_head_byte_stable_across_workflow_changes); doc comments src/codegraph/mod.rs:294 and src/tool/agent/codegraph.rs:89. Constraints: keep notes SHORT (they ride in tool output; the stable head has a byte-budget test), keep the escape-hatch sentence (re-issuing the same query skips delegation) intact, and do NOT change which steering marker fires or its label (MarkerKind::marker() strings like "is an indexed symbol" stay) — the frontend delegationNotes.ts registry (plan 3ae367bd, commit e4557e9) mirrors those emission sites and keys on the stable phrases.

Acceptance: every listed site reads as a command; prompt tests (stable_head_carries_tool_strategy, tool_strategy_lists_graph_tools_before_search, stable_head_byte_stable_across_workflow_changes) and the steering_stats.rs marker-label tests pass; any test asserting the old literal text is updated; docs/FEATURES.md (~:42) quotes several of these strings — updated to match; cargo test green.

Verification: cargo test at repo root (all listed files are root-crate src/**), unpiped exit check per constitution; delegationNotes vitest file only if its fixtures are touched.

## Steps
- [x] 1. **Recon: read every listed site + grep the old phrasings repo-wide** — Read the exact sites in src/tool/agent/search.rs:615-630, :670-690, :2160-2180; src/tool/agent/shell.rs:45-60; src/agent/steering_stats.rs:795-810; src/tool/agent/file_write.rs:75-90; src/tool/agent/file_edit.rs:192-210; src/tool/workflow/plan.rs:1505-1525; src/agent/prompt.rs:35-50 and :185-200; src/codegraph/mod.rs:288-300; src/tool/agent/codegraph.rs:82-95. Then literal-search the old phrasings to catch every consumer: "prefer the graph tools", "prefer running the command", "Prefer update_plan", "prefer file_edit", "prefer smaller fragments" — across src/**, src-tauri/** (tests asserting literals), frontend/src/** (frontend/src/lib/delegationNotes.ts registry comments + frontend/src/lib/delegationNotes.test.ts fixtures may quote the tails), docs/FEATURES.md, docs/CONFIGURATION.md. Confirm which steering marker each site carries; marker strings and MarkerKind::label() are not to be touched.
- [x] 2. **Rewrite each site as a short imperative** — Imperative rewrites per the recon in: src/tool/agent/search.rs (symbol-lookup/alternation/delegation tails → e.g. "use graph_search/graph_context for symbol lookups — search is for text"), src/tool/agent/shell.rs + src/agent/steering_stats.rs (redirection warning → e.g. "run the command unredirected"), src/tool/agent/file_write.rs ("edit an existing file with file_edit"), src/tool/agent/file_edit.rs ("use smaller fragments, or file_write for a full-file replacement"), src/tool/workflow/plan.rs ("Use update_plan whenever the plan is still broadly correct"), src/agent/prompt.rs tool-strategy lines (same conversion within the stable-head byte budget), src/codegraph/mod.rs + src/tool/agent/codegraph.rs doc comments (same imperative shape). Constraints: keep the AUTO-DELEGATED block's escape-hatch sentence intact; do NOT change MarkerKind::marker() strings or labels; keep clause structure (several tails are "; "-joined inside merged notes — the frontend registry's component matching must stay valid).
- [x] 3. **Update tests + docs/FEATURES.md quoting the old text** — Update every test asserting the old literal phrasings: src/agent/prompt.rs (tests stable_head_carries_tool_strategy / tool_strategy_lists_graph_tools_before_search / stable_head_byte_stable_across_workflow_changes), src/agent/steering_stats.rs marker-label tests, plus search.rs/shell.rs/file-tool tests pinning note text. If frontend/src/lib/delegationNotes.test.ts fixtures quote the old tails, refresh them (then run npx vitest run src/lib/delegationNotes.test.ts in frontend/). Update docs/FEATURES.md (~:42) wherever it quotes the old strings.
- [x] 4. **Verify: cargo test green, zero warnings** — Verify the rewritten files compile and the suites pass: run `cargo test` at the repo root (unpiped exit check: `cargo test` then `"exit=$LASTEXITCODE"`) — the touched files are src/tool/agent/search.rs, src/tool/agent/shell.rs, src/tool/agent/file_write.rs, src/tool/agent/file_edit.rs, src/tool/workflow/plan.rs, src/agent/prompt.rs, src/agent/steering_stats.rs, src/codegraph/mod.rs, src/tool/agent/codegraph.rs, all in the root crate — and #![deny(warnings)] makes a green run prove zero warnings. If frontend/src/lib/delegationNotes.test.ts was touched, also run that vitest file. Fix any failure before continuing.
- [ ] 5. **Closing: reviewer, fix findings, commit** — Spawn the read-only reviewer (role:"reviewer", omit the model param) over the full uncommitted diff (the ~11 files listed in step 4 + docs/FEATURES.md) with the plan goal; fix every finding; re-run the affected suites (cargo test at root; the delegationNotes vitest file if frontend was touched); commit to the current working branch including the review report; finish citing the PASS report.
