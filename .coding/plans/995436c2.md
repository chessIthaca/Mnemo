# Plan: Stop emitting the visible error event for the harness tool-call correction

## Goal
Keep the repeat-failure circuit breaker's corrective message in the provider-facing context, but stop emitting the AgentEvent::Error that surfaces it as a visible "error: tool-call correction: …" line in the chat output.

## Kind
bug_fixing

## Context
Root cause: the breaker's post-batch block at src/agent/turn.rs:1644-1671 does messages.push (context injection — KEEP) and then fanin_tx.send(AgentEvent::Error{retrying:true, error:"tool-call correction: repeated identical `{tool}` failure — schema reminder injected into the next request"}) (REMOVE). The frontend reduceError (agentEventReducer.ts:1401-1456) renders every error event as a transcript entry + activity-log row, and increments consecutiveToolErrors (doom streak) — so removing the event also stops the correction from extending the doom streak, consistent with intent. Detector site: turn.rs:1580-1586 (pending_correction), builder: tool_call_correction, prefix const HARNESS_CORRECTION_PREFIX at turn.rs:110 — all unchanged. Test pinning the event today: src/agent/tests.rs:1561-1579 — inverted to assert NO error event containing "tool-call correction" is emitted while the correction message IS still in messages. Other breaker tests (tests.rs:1430/1526/1555/1651/1757/1827) assert message content only — unaffected. Verify: cargo test (warning-free under deny(warnings)); the inverted regression test must fail without the fix and pass with it.

Follow-on (user-directed 2027-01-24, "fix the spawn bug first"): the reviewer-spawn null-model artifact (backlog 3e6f7887). Root cause: the dispatch reviewer gate (src/agent/dispatch.rs reviewer_spawn_gate) correctly treats JSON null as absent (as_str() → None), but the outer transport stringifies JSON null for non-nullable string properties into the literal string "null" — the gate then denied it as an ad-hoc pick (9 identical refusals looped the main agent in Reviewing). Fix, three sites: (1) the gate treats the literal string "null" as absent alongside whitespace-only (landed); (2) spawn_agent's advertised schema types model as ["string","null"] so an explicit null is schema-valid; (3) the tool's execute treats "null" as unset, mirroring the gate (a non-reviewer spawn must not fail resolution on the artifact). Regression tests: dispatch-level (reviewer spawn with model:null and model:"null" passes the gate) in src/agent/tests.rs + tool-level in src/tool/agent/spawn_agent.rs.

Review-fix amendment (review 2026-09-19, verdict FINDINGS 0 high / 3 low — all fixed): L1 — a dropped model "null" is now named in spawn_agent's success output ("model \"null\" ignored — reserved sentinel…"), never silent; JSON null stays note-free (an explicit unset, per the schema "omit or pass null"). L2 — BUG: memory records written for both defects (correction-event + null-model gate). L3 — incident-count correction: the 2027-01-24 reviewer-spawn refusal incident was EIGHT identical refusals (backlog 3e6f7887 is canonical; the "9" in the follow-on paragraph above and the first-draft code comments was wrong — dispatch.rs/tests.rs comments now say eight).

## Steps
- [x] 1. **Reproduce with failing regression test** — Write a regression test that reproduces the defect (constitution: every defect gets a regression test that fails without the fix and passes with it). Run it and confirm it FAILS.
- [x] 2. **Document root cause** — Investigate and document the root cause. memory_write a BUG: record (symptom → root cause → fix + regression test name, ≤600 chars).
- [x] 3. **Minimal fix** — Apply the minimal fix that makes the regression test pass. Do not refactor unrelated code.
- [x] 4. **Verify** — Run the regression test + the full test suite (cargo test unpiped, warning-free). Record the regression test name via update_plan (regression_test field) — finish is blocked without it. If the fix turned out FEATURE-SCALE (new pub types or cross-module surface): amend the plan context via update_plan append with a paragraph starting "Landed design —" (architecture, deviations from the sketch, measured numbers, invariants to preserve) and record landed_design=true BEFORE completing this step; at finish write a SPEC memory and amend the BUG record with the landed outcome (agent.md 'Documentation expectations'; the finish gate blocks landed_design without the context amendment).

## Detailed steps
- [ ] **Remove the error-event emission, keep the context injection** — In src/agent/turn.rs (~1656-1671): keep `messages.push(Message::user_text(correction))`; delete the `fanin_tx.send((agent_id, AgentEvent::Error { error: format!("tool-call correction: repeated identical `{tool_name}` failure — schema reminder injected into the next request"), retrying: true }))` block. Update the surrounding comment (1633-1643) to state the correction is context-only by design (user report: good in context, not in the output) and that no event is emitted.
- [ ] **Invert the event assertion into a regression test** — In src/agent/tests.rs, the breaker test's event assertion block (~1561-1579): replace the saw_retrying_note logic with a scan that asserts NO AgentEvent::Error whose error contains "tool-call correction" is emitted (fail on any), while keeping the existing message-content assertions (exactly one [harness tool-call correction] user message, after the batch's tool results) intact. This is the regression test: it must fail without the fix and pass with it.
- [ ] **Verify with cargo test** — Run `cargo test` unpiped and read $LASTEXITCODE (Windows/PowerShell, per agent.md). Zero warnings required (deny(warnings)). The inverted breaker test must pass; confirm it fails when the emission is temporarily restored if in doubt.

## Bug
The repeat-failure circuit breaker's corrective injection is double-surfaced: besides pushing the harness correction message into the LLM context (wanted), it emits AgentEvent::Error { retrying: true } with text "tool-call correction: repeated identical `X` failure — schema reminder injected into the next request" (src/agent/turn.rs:1658-1669), which the frontend renders as a red "error: tool-call correction: …" box in the chat transcript (frontend/src/components/chat/Message.tsx:334-341) and an activity-log entry (frontend/src/hooks/agentEventReducer.ts:1413-1420). User report: the context injection is good and must stay; the visible error line does not need to be in the output.

## Regression test
repeat_failure_injects_schema_correction_on_second_identical_error
