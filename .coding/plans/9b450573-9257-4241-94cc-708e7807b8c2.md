# Plan: Phase 4 — bug_fixing plan kind + finish-step auto-capture

## Goal
Phase 4 of the semantic memory system: bug_fixing plan kind (locked 4-step skeleton + required bug:{symptom} param), reviewer verdict line enforcement (## Verdict: PASS / FINDINGS, fail closed), finish-step deterministic auto-capture (PLAN:/BUG: digests, crash-marker supersede, idempotent by plan id) with a regression-test gate validated via graph_search, and the compiled-prompt habit (bugs always use kind=bug_fixing). Tests throughout; cargo test + src-tauri build + frontend tests green.

## Kind
implementation

## Context
Parent: orchestrator 1c9ba6c8 — ALL work stays on feat/semantic-memory-system (NO branch; create_plan without a branch param). Phases 1-3 landed: typed records + hygiene tools, derived indexer + Settings Memory section, retrieval tools (plans_search/reviews_search/past_fixes/context_pack) + git_log/git_show + MANDATORY prompt habits. GROUNDING (verified 2026-08-22): (1) PlanKind at src/workflow/plan_file.rs:34-63 (serde lowercase, from_str_ci, as_str; PlanFile fields at :73-91 with #[serde(skip)] on kind; parse() section loop at :142-232 with Section enum at :288-296; serialize() at :235-243; write_to_dir at :275). (2) Workflow: create_plan_with_kind at src/workflow/mod.rs:367 (takes kind, persists ## Kind); complete_step at :534-571 (match kind: Implementation → Reviewing, Research → Complete); update_plan at :421-515 (title/goal/context/steps params); plan()/plan_id() accessors at :225-232. (3) Tools: CreatePlanTool at src/tool/workflow/plan.rs:167-334 (CreatePlanArgs at :31-52 with kind: PlanKind; execute at :251-333 calls wf.create_plan_with_kind at :308); UpdatePlanTool at :336-455 (UpdatePlanArgs at :155-165); CompleteStepTool at :457+; FinishTool at :665-773 (new(workflow, reviews_dir); execute validates report under .coding/reviews/ + non-empty at :740-757, then wf.finish() at :758); tests mod at :866 (make_workflow helper at :870; finish tests at :1315-1376 write '# Review\nno findings' — MUST be updated to the verdict format). (4) write_review_report at src/tool/agent/write_review_report.rs:40-100+ (WriteReviewReportArgs{path, content}; resolve_under_reviews guard; content non-empty check). (5) Memory: MemoryStoreTrait at src/memory/mod.rs:178+ (write/supersede_memory/delete_memory/list_filtered/memory_search_config); Memory::new at src/memory/types.rs:227-250; MemoryClass::Authored default; indexer digest builders at src/memory/indexer.rs:278-414 (plan_record at :278, budgeted_digest at :364, first_commit_hash at :388 — make budgeted_digest + first_commit_hash pub(crate) to reuse); indexer already imports crate::workflow::PlanFile (no cycle). (6) CodeGraph: view() at src/codegraph/mod.rs:154 → GraphView::resolve(query) at src/codegraph/query.rs:193 (Vec<Symbol>, empty on no match). (7) Factory: FinishTool::new at src/agent/factory.rs:707-710 (workflow + self.reviews_dir()); memory field Option<Arc<dyn MemoryStoreTrait>> at :107; codegraph field at :161. (8) Prompt: PLAN KIND paragraph at src/agent/prompt.rs:76-83; Reviewing state block at :503-517; CLOSING SEQUENCE at :119-140; write_review_report schema description. (9) Constitution: cargo test unpiped, warning-free (#![deny(warnings)]), no #[allow], doc comments on public fns, regression tests for defects; root cargo test does NOT compile src-tauri — verify with cd src-tauri; cargo build. DESIGN DECISIONS: bug_symptom + regression_test are PlanFile fields with #[serde(skip)] (frontend wire shape unchanged), persisted as '## Bug' + '## Regression test' sections; create_plan_with_kind gains a bug_symptom: Option<&str> param (call sites: mod.rs:352 + 4 tests + tool at plan.rs:308); BugFixing completes → Reviewing (bug plans get reviewed, same as Implementation); skeleton lock enforced in update_plan (steps replacement refused for BugFixing plans); finish gate order: state → report → verdict (PASS proceeds; FINDINGS/unparseable block) → bug_fixing regression_test required → graph resolve (skip with note when graph unwired) → capture (non-blocking on error, eprintln + note); capture module src/memory/finish_capture.rs with deterministic ids uuid v5('plan-finish:<id>') / uuid v5('bug-finish:<id>'), crash-marker supersede via list_filtered(Working, title_prefix 'ACTIVE:'/'STEP MARKER:') + content/title contains plan id; capture writes Authored records (class weight lifts them over the indexer's derived digests); nudge line in finish output about SPEC:/DECISION: memories.

## Steps
- [x] 1. **PlanKind::BugFixing + bug_symptom/regression_test fields + locked skeleton** — src/workflow/plan_file.rs: (1) PlanKind gains BugFixing — from_str_ci maps "bug_fixing", as_str returns "bug_fixing", doc comment (bug plans complete → Reviewing like Implementation, but with the locked skeleton + finish auto-capture). (2) PlanFile gains bug_symptom: Option<String> + regression_test: Option<String>, both #[serde(skip)] (frontend wire shape unchanged), persisted as '## Bug' + '## Regression test' sections: Section enum gains Bug + RegressionTest variants; parse() maps 'bug'/'regression test' headers; serialize() emits the sections when Some. (3) PlanFile::new keeps defaults None.
src/workflow/mod.rs: (1) create_plan_with_kind gains bug_symptom: Option<&str> param (set plan.bug_symptom before write_to_dir); create_plan passes None; update the 4 test call sites at :953/:970/:984/:1001. (2) complete_step's kind match: BugFixing → Reviewing (same arm as Implementation). (3) update_plan gains regression_test: Option<&str> param (sets the field when Some + non-empty; changed=true) AND the skeleton lock: when the active plan's kind is BugFixing and steps is Some → Err('bug_fixing plans have a locked 4-step skeleton — steps cannot be replaced; title/goal/context/regression_test updates are allowed').
src/tool/workflow/plan.rs: (1) CreatePlanArgs gains #[serde(default)] bug: Option<String>. (2) CreatePlanTool::execute: when args.kind == PlanKind::BugFixing — bug must be Some + non-empty (else error 'kind=bug_fixing requires the bug param (the symptom)'); steps are FORCED to the locked skeleton (ignore provided steps): const BUG_FIXING_SKELETON: [&str; 4] = ['**Reproduce with failing regression test** — Write a regression test that reproduces the defect (constitution: every defect gets a regression test that fails without the fix and passes with it). Run it and confirm it FAILS.', '**Document root cause** — Investigate and document the root cause. memory_write a BUG: record (symptom → root cause → fix + regression test name, ≤600 chars).', '**Minimal fix** — Apply the minimal fix that makes the regression test pass. Do not refactor unrelated code.', '**Verify** — Run the regression test + the full test suite (cargo test unpiped, warning-free). Record the regression test name via update_plan (regression_test field) — finish is blocked without it.']; pass args.bug.as_deref() to create_plan_with_kind. (3) Schema: kind enum gains 'bug_fixing' + description (bugs always use kind=bug_fixing); add bug param description. (4) UpdatePlanArgs gains #[serde(default)] regression_test: Option<String>; execute passes it to wf.update_plan. (5) UpdatePlanTool schema mentions regression_test.
Tests: plan_file — bug_fixing kind roundtrips through serialize/parse; bug_symptom + regression_test sections roundtrip; legacy plan without the sections → None. workflow — bug_fixing plan completes → Reviewing (not Complete); update_plan sets regression_test; update_plan steps replacement on a BugFixing plan errors. tool — create_plan kind=bug_fixing without bug errors; with bug forces the 4-step skeleton (plan.steps == the skeleton, bug_symptom persisted); update_plan regression_test lands on the plan. Run cargo test unpiped.
- [x] 2. **Reviewer verdict line enforcement (fail closed)** — src/tool/agent/write_review_report.rs: in execute, after the non-empty content check, validate the report's FIRST non-blank line starts with '## Verdict: PASS' or '## Verdict: FINDINGS' — otherwise error: 'review report must open with "## Verdict: PASS" or "## Verdict: FINDINGS (n high, n low)" — an unparseable verdict counts as FINDINGS (fail closed)'. Update the schema description to state the requirement.
src/tool/workflow/plan.rs FinishTool::execute: after the under_reviews + non-empty checks, parse the verdict from the report's first non-blank line: starts_with '## Verdict: PASS' → proceed; starts_with '## Verdict: FINDINGS' → error 'review verdict is FINDINGS — fix every finding, re-run the reviewer, and re-finish with a PASS report'; anything else → error 'unparseable review verdict — the report must open with "## Verdict: PASS" or "## Verdict: FINDINGS (n high, n low)" (fail closed)'. Add a small fn parse_verdict(report: &str) -> Verdict enum {Pass, Findings, Unparseable} with unit tests.
Update the existing finish tests at plan.rs:1315-1376 (they write '# Review\nno findings' — change to '## Verdict: PASS\nno findings'). New tests: FINDINGS verdict blocks finish (stays Reviewing); unparseable verdict blocks; PASS proceeds. write_review_report tests: content without the verdict line is rejected; with '## Verdict: PASS' accepted; with '## Verdict: FINDINGS (2 high, 1 low)' accepted. Run cargo test unpiped.
- [x] 3. **Finish hook: deterministic auto-capture + regression-test gate** — New src/memory/finish_capture.rs (add pub mod finish_capture; to src/memory/mod.rs). Make budgeted_digest + first_commit_hash pub(crate) in src/memory/indexer.rs and reuse them.
pub struct FinishCaptureReport { plan_memory_id: String, bug_memory_id: Option<String>, markers_superseded: usize } with doc comments.
pub async fn capture_finish(store: &Arc<dyn MemoryStoreTrait>, plan: &crate::workflow::PlanFile, plan_id: &str, plans_dir: &Path, now: i64) -> Result<FinishCaptureReport>:
(1) PLAN: digest — title 'PLAN: <plan title>', content = budgeted_digest(goal first line, pointer, 400) where pointer = '<done>/<total> steps · <kind> · path .coding/plans/<id>.md' + (' · regression test: <name>' when Some) + (' · commit <hash>' when the plan file at plans_dir/<id>.md yields first_commit_hash); memory id = Uuid::new_v5(&Uuid::NAMESPACE_URL, format!('plan-finish:{plan_id}')) — deterministic, INSERT OR REPLACE via store.write (idempotent by plan id); record_class = MemoryClass::Authored (class weight lifts it over the indexer's derived digest).
(2) When plan.kind == PlanKind::BugFixing: BUG: digest — title 'BUG: <plan title>', content = budgeted_digest('symptom: <first line of plan.bug_symptom>', 'regression test: <name> · path .coding/plans/<id>.md', 600); id = uuid v5('bug-finish:<plan_id>'); Authored.
(3) Crash-marker supersede: list_filtered(MemoryFilter::new().tier(Working).title_prefix('ACTIVE:')) + same with 'STEP MARKER:' — for each memory whose title OR content contains plan_id, supersede_memory(old_id, Memory::new(Working, 'STEP MARKER: <plan title> — COMPLETE', 'plan <id> completed <date>; superseded by finish auto-capture — see PLAN: <plan_memory_id>', now)); count into markers_superseded. Errors on individual supersedes are collected (eprintln) and do not abort the rest.
FinishTool (src/tool/workflow/plan.rs): struct gains memory: Option<Arc<dyn MemoryStoreTrait>> + codegraph: Option<Arc<crate::codegraph::CodeGraph>>; new() keeps the 2-arg shape (None/None); add with_memory(store) + with_codegraph(graph) builder methods. execute() gate order AFTER the verdict check: (a) if plan.kind == BugFixing: plan.regression_test must be Some + non-empty → else error 'bug_fixing plan has no regression test recorded — the verify step must call update_plan with regression_test before finish'; (b) if codegraph is Some AND regression_test is Some: graph.view()?.resolve(name) — empty → error 'regression test symbol <name> not found in the code graph — the verify step must record the actual test function name'; graph unwired → skip with a note in the output; (c) capture: when memory is Some, call capture_finish (plans_dir from wf.plans_dir(), now = utc_now_secs-style) — on Err eprintln + append ' · capture failed: {e}' to the output (non-blocking); when memory is None, append ' · no memory store — finish capture skipped'; (d) append the nudge: 'If this plan changed intended behavior, write a SPEC:/DECISION: memory (memory_write) so the new behavior is recorded.'; then wf.finish().
Factory (src/agent/factory.rs:707-710): FinishTool::new(workflow.clone(), self.reviews_dir()).with_memory(store).with_codegraph(graph) — only when the Options are Some (chain conditionally).
Tests in finish_capture.rs (make_store pattern): capture writes the PLAN: digest within the 400 budget with the path pointer + done/total; re-run is idempotent (same memory id, count_by_class unchanged); bug_fixing plan also writes the BUG: digest (600 budget, symptom + regression test + path); crash markers (seeded 'ACTIVE: x <plan_id>' + 'STEP MARKER: y <plan_id>' working-tier rows) are superseded (old rows excluded from default recall, superseded_by set) and unrelated markers untouched; authored rows never deleted. FinishTool tests: bug_fixing plan without regression_test → finish errors; with regression_test + codegraph wired (open_in_memory with a seeded symbol) → missing symbol errors, present symbol passes; graph unwired → passes with the skip note; capture runs on finish (memory wired → PLAN: digest exists after finish). Run cargo test unpiped.
- [x] 4. **Prompt habits: bug_fixing kind + verdict requirement + reviewer checklist** — src/agent/prompt.rs: (1) PLAN KIND paragraph (:76-83) gains: 'BUGS: always use kind:"bug_fixing" (never "implementation") — bug plans get the locked reproduce→root-cause→fix→verify skeleton, a required bug:{symptom} param, and a BUG: memory at finish.' (2) CLOSING SEQUENCE step 2 (:122-131) gains: 'The reviewer's report MUST open with "## Verdict: PASS" or "## Verdict: FINDINGS (n high, n low)" — an unparseable verdict counts as FINDINGS (fail closed). For bug_fixing plans the reviewer must additionally check: the regression test exercises the changed path, the root cause is documented, and a BUG: memory was written.' (3) Reviewing state block (:503-517) gains the same verdict + bug-checklist sentence (the reviewer task text the agent pastes). (4) create_plan schema description (plan.rs:188-207) kind sentence gains bug_fixing.
Tests in prompt.rs: stable_head_carries_bug_fixing_habit — head contains 'kind:"bug_fixing"' and 'never "implementation"'; closing_sequence_mentions_verdict — head contains '## Verdict: PASS' and 'fail closed'; reviewing_block_mentions_bug_checklist — the Reviewing state text (build via the same path the turn layer uses, or assert on the const/format helper) contains 'regression test exercises the changed path'. Run cargo test unpiped.
- [x] 5. **Full verification + step marker (no commit)** — cargo test unpiped — green + warning-free (#![deny(warnings)]; fix root causes, never #[allow]). cd src-tauri; cargo build (bin crate compiles separately). cd frontend; npm test (unchanged this phase — must stay green). memory_write a STEP MARKER (working tier): plan 1c9ba6c8 Phase 4 done, files changed, next action = orchestrator step 5 (integration verification of the whole feature). Do NOT commit — the root orchestrator reviews + commits the whole feature at the end.
