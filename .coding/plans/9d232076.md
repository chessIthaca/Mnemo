# Plan: offscreen_browser_screenshot preview missing in chat

## Goal
Every screenshot tool's captured PNG must render inline in the chat transcript — currently only `browser_screenshot`/the dead `game_screenshot` names are matched by toolImagePaths, so `offscreen_browser_screenshot` (headless, cross-platform) resolves no image and shows no preview.

## Kind
bug_fixing

## Context
User report: "offscreen_browser_screenshot [active-1788366238690.png] did not show the preview in the agent. All screenshot tools need to show it in the chat."

ROOT CAUSE (verified by reading code): frontend/src/components/chat/ToolImage.tsx `toolImagePaths` (line 49) gates PNG resolution on `name === "browser_screenshot" || name === "game_screenshot"`. The `game_*` tools were dropped in the 2026-08 browser rename (parity list src/agent/factory.rs:1868-1888; review 2026-09-09 removed every remaining game_* comparison from toolCardPaths.ts), and the headless tool registered as `offscreen_browser_screenshot` (src/tool/browser/mod.rs:213-274). Both screenshot tools return `ToolResult::success("screenshot saved: {rel}").with_data(json!({"path": rel}))` with rel = `.coding/browser/screenshots/<page>-<ts>.png` (offscreen, mod.rs:255-269) resp. `browser-<ts>.png` (Windows live tab, mod.rs:809+). toolImagePaths returns [] for the offscreen name → Message.tsx:829-835 renders no ToolImage → no preview. Stale `game_screenshot` mentions live ONLY in ToolImage.tsx:16,49 + ToolImage.test.ts:42,49. Sibling card-chip machinery (browserArgLabel/browserResultInfo, toolCardPaths.ts:399-521) already covers both families via `toolName.startsWith("browser_") || toolName.startsWith("offscreen_browser_")`, and browserResultInfo's screenshot branch keys on `endsWith("_screenshot")` — the fix should follow that convention.

FIX DIRECTION: in toolImagePaths, treat ANY tool whose name ends with `_screenshot` as a screenshot producer (structured data.path preferred, output-text scan fallback) — covers browser_screenshot + offscreen_browser_screenshot today and any future screenshot tool; drop the dead game_screenshot branches. Update the module doc comment (lines 11-24) accordingly. Frontend-only change: ToolImage.tsx + ToolImage.test.ts (already in vitest.config.ts include list, line 58). Test via `cd frontend; npx vitest run src/components/chat/ToolImage.test.ts` then full `npm test` + `npx tsc --noEmit`. No Rust change → cargo untouched (repo precedent: frontend-only plans verify with vitest + tsc only).

## Steps
- [x] 1. **Reproduce with failing regression test** — Write a regression test that reproduces the defect (constitution: every defect gets a regression test that fails without the fix and passes with it). Run it and confirm it FAILS.
- [x] 2. **Document root cause** — Investigate and document the root cause. memory_write a BUG: record (symptom → root cause → fix + regression test name, ≤600 chars).
- [x] 3. **Minimal fix** — Apply the minimal fix that makes the regression test pass. Do not refactor unrelated code.
- [x] 4. **Verify** — Run the regression test + the full test suite (cargo test unpiped, warning-free). Record the regression test name via update_plan (regression_test field) — finish is blocked without it.

## Bug
offscreen_browser_screenshot tool card in the chat transcript shows no inline PNG preview (e.g. .coding/browser/screenshots/active-1788366238690.png); only browser_screenshot produced a preview. All screenshot tools should render their captured PNG in the chat.

## Regression test
frontend/src/components/chat/ToolImage.test.ts
