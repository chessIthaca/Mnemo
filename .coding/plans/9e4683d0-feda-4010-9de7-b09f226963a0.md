# Plan: Agent visibility: effective-model display on override + memory-usage notifications

## Goal
Make agent self-learning visible and the model display truthful: (A) emit ModelChanged when the per-turn per-context model override (skill/state/subagent/forced) changes the effective model — toolbar + agent tab update mid-turn; (B) memory activity visible by default (show_memory_activity default true) PLUS a new MemoryRecalled event so auto-recall usage shows in the agent window; (C) verify the memory system is used/up-to-date/useful and record hygiene notes.

## Kind
implementation

## Context
Investigated (Planning, read-only). ISSUE A — "skill moved us to grok but toolbar/tab never updated": the plumbing EXISTS but one emission path is missing. resolve_turn_provider (src/agent/loop_impl.rs) re-resolves EVERY loop iteration (turn.rs:149-169, wf guard held for the match at :156-169) and sets resolved_model on every path; AgentInfo.model (list_agents, src-tauri/src/ipc/agent.rs) prefers resolved_model; ModelChanged event exists end-to-end (channels.rs:199/323-326/479-480 + fixture + frontend reducer agentEventReducer.ts:704/:840/:919-920 stamping agentModels; StatusBar.tsx:41 shows agentModels[activeAgent] ?? model; MainPanel.tsx:63 tabs show agentModels[id]). GAP: ModelChanged is ONLY emitted from config_io::swap_live_provider (set_model/save_endpoints). The per-turn override path NEVER emits it → a mid-turn skill model switch (e.g. merge_to_main → grok) never updates toolbar/tab. FIX: in turn.rs, after the resolution block, compare resolved_model before/after; on change send AgentEvent::ModelChanged via fanin_tx (precedent: SuggestionInjected emit at turn.rs:259-264; fanin events forward unconditionally — events.rs arms at :187-499 are side-effect routing only). When resolved_model is None (default), effective model = default_provider.model() (snapshot at turn start, turn.rs:62-66). Regression tests in src/agent/tests.rs near run_turn_switches_to_skill_model_after_midturn_skill_start: mid-turn skill override emits ModelChanged(override model); skill end / next-turn default emits ModelChanged(default model). ISSUE B — "no notification when semantic skills + memory accessed": (B2) show_memory_activity EXISTS end-to-end (UiConfig general.rs:279-284, default FALSE at :292; App.tsx:247 loads it; reducer gates MEMORY_TOOLS at agentEventReducer.ts:203/:262/:360 rendering readable memory entries; AppearanceSection checkbox) — DEFAULT OFF is why the user sees nothing → flip default to true: general.rs default + field doc, config test show_memory_activity_defaults_false (:710, rename to ..._defaults_true + assert true), round-trip test stays, settings wire literals src-tauri/src/ipc/settings.rs:1387/:1469, contract_fixtures.rs:216, frontend dto-get-settings.json:42 + any ipc-contract assertion. (B1) KNOWN-OLD-BUG check: 2026-11-22 review C1 said parseMemoryEntry recall regex never matches — CURRENT recall output (src/tool/memory/mod.rs:176-187) is "[{tier}] {title} (score: {x:.2}, strength: {y:.2})\n  {content≤200chars}\n\n" which the current regex /\[(\w+)\]\s+(.+?)\s+\(score:/ + /\n\s+(.+)/ DOES match — pin with vitest regression tests using the real format; only fix if the test proves a mismatch. (B3) AUTO-RECALL is invisible: turn.rs:343-384 recalls every turn and injects RECALLED MEMORIES into the prompt (fresh-recall branches :357-361 + :367-371; cache-reuse :353-355) with NO event. User explicitly wants a notification when memory is accessed → add AgentEvent::MemoryRecalled { hits: usize, top_title: Option<String> }: channels.rs AgentEvent + SerializableAgentEvent + into_serializable arm + roundtrip test (mirror ModelChanged); emit from turn.rs ONLY on a FRESH recall with hits>0 (both fresh branches; not on cache reuse, not on empty); frontend types.ts union member {kind:"memory_recalled"; hits:number; top_title:string|null} + agentEventReducer case → push `memory` transcript entry {kind:"memory", name:"auto-recall", tier:"recall", title:undefined, snippet:`${hits} hit(s) — top: ${top_title ?? "—"}`, success:true, running:false} gated by showMemoryActivity + fixtures: tests/contract_fixtures.rs sample, frontend/src/lib/ipc-fixtures/event-memory-recalled.json {kind:"memory_recalled",hits:3,top_title:"t"}, ipc-contract.test.ts row, useAgentStore.test.ts row. ISSUE C — verify memory used/useful: auto-recall demonstrably works (this session's per-turn recall block shows scored semantic hits); staleness note: old ACTIVE memories (6-batch plan e342c141, freeze diagnosis 3c3833d2, skill-midturn "pending merge") are superseded by shipped/merged work — write ONE hygiene memory recording supersession + run live memory_recall verification queries during execution; document results in the commit. ENV: Windows/PowerShell, cargo test unpiped (root does NOT compile src-tauri — run both), #![deny(warnings)], doc comments on pub items, no #[allow]. Branch from main (clean; merge stashes linger — ignore). Backlog: none of this is a backlog item; it's a direct user request.

## Steps
- [x] 1. **Branch fix/agent-visibility + baseline** — From repo root C:\AgenticCoder\AgenticCoder: `git checkout -b fix/agent-visibility` (main is clean post-merge). Then `cargo test 2>&1 | Select-String "test result:"` to confirm the pre-change baseline is green (expect 1058 passed). No code changes in this step beyond the branch.
- [x] 2. **A: emit ModelChanged on per-turn override** — In `src/agent/turn.rs` per-iteration resolution block (:149-169): (1) BEFORE the block, capture `let prev_resolved = self.resolved_model();` (std-Mutex getter on AgentLoop, expect-poisoned style per loop_impl). (2) Leave the wf-guard match as-is (guard drops at block end). (3) AFTER the block: `let now_resolved = self.resolved_model();` and if `now_resolved != prev_resolved` compute the effective model id: `now_resolved.clone().unwrap_or_else(|| default_provider.model().to_string())` and emit `fanin_tx.send((agent_id, AgentEvent::ModelChanged { model: effective })).await` (ignore send errors like the SuggestionInjected emit at :259). Add a comment: the per-turn override path never emitted ModelChanged, so a mid-turn skill model switch left the toolbar/tab stale (user report 2026-04-20). Regression tests in src/agent/tests.rs (mirror the fixture style of run_turn_switches_to_skill_model_after_midturn_skill_start): (a) a turn that starts a skill with a [models.skill] override mid-turn yields a fanin ModelChanged event carrying the override model; (b) when the override no longer applies (skill ended / next resolution default), a ModelChanged with the DEFAULT model is emitted (revert). If the fixture harness makes (b) hard in one turn, cover the revert by asserting no ModelChanged is emitted when nothing changed (turn with no override and prev=None → zero ModelChanged events) instead — document the choice in the test comment. Run `cargo test agent::` unpiped.
- [x] 3. **B2: show_memory_activity default true** — Flip the memory-activity default to visible. (1) `src/config/general.rs`: UiConfig::default() show_memory_activity false→true (:292) and fix the field doc (:279-284 — now 'default true; set false to hide'); update test show_memory_activity_defaults_false (:710) → rename to show_memory_activity_defaults_true asserting `cfg.ui.show_memory_activity` is true (show_memory_activity_round_trips unchanged). (2) `src-tauri/src/ipc/settings.rs`: the two wire-shape test literals at :1387/:1469 — if they render a default config, flip show_memory_activity to true (read them first; they are GetSettingsUi literals). (3) `src-tauri/src/ipc/contract_fixtures.rs:216`: show_memory_activity: false → true. (4) Frontend `frontend/src/lib/ipc-fixtures/dto-get-settings.json:42`: "show_memory_activity": false → true; check `frontend/src/lib/ipc-contract.test.ts` for an assertion on that field and update if pinned to false. Run `cargo test` (root) + `cargo test` in src-tauri/ + `npm test` in frontend/ unpiped — all green.
- [x] 4. **B3 backend: MemoryRecalled event + emission** — Add the auto-recall visibility event. (1) `src/runtime/channels.rs`: add `AgentEvent::MemoryRecalled { hits: usize, top_title: Option<String> }` with doc comment (emitted once per FRESH auto-recall that returned hits — the per-turn prompt injection path — so the UI can show memory being used; carries the hit count + top hit title) + `SerializableAgentEvent::MemoryRecalled { hits: usize, top_title: Option<String> }` + the into_serializable arm (approval_sender/question_sender = None) + a roundtrip test mirroring model_changed_roundtrips_json (assert JSON contains "kind":"memory_recalled", hits, top_title). (2) `src/agent/turn.rs` auto-recall block (:343-384): in BOTH fresh-recall branches (:357-361 and :367-371), after a successful `Ok(r)` with `!r.is_empty()`, send `AgentEvent::MemoryRecalled { hits: r.len(), top_title: r.first().map(|sm| sm.memory.title.clone()) }` via fanin_tx (ignore errors). Do NOT emit on the cache-reuse branch (:353-355) or on empty/error. Comment why (visibility of memory access; user request). (3) Confirm the events.rs forwarder needs NO new arm (display-only event — the arms at :187-499 are routing side-effects; ModelChanged already forwards with none). (4) Agent test in src/agent/tests.rs: with a memory store seeded with a relevant memory, a turn produces one MemoryRecalled event with hits>0 (reuse the fixture harness used by run_turn_records_tool_events-style tests; if seeding the store in the fixture is impractical, cover the channel roundtrip in channels.rs only and note it). Run `cargo test` root unpiped.
- [x] 5. **B3 frontend + fixtures + parse pin** — Frontend wiring. (1) `frontend/src/lib/types.ts`: add `| { kind: "memory_recalled"; hits: number; top_title: string | null }` to the SerializableAgentEvent union (near model_changed). (2) `frontend/src/hooks/agentEventReducer.ts`: add `case "memory_recalled"` — when agent.showMemoryActivity is true push a `memory` transcript entry {kind:"memory", name:"auto-recall", tier:"recall", title:undefined, snippet:`${event.hits} hit(s) — top: ${event.top_title ?? "—"}`, success:true, running:false} (capTranscript); when false, return agent unchanged. (3) parseMemoryEntry regression pin: add `frontend/src/hooks/agentEventReducer.memory.test.ts` (node env, registered in vitest.config.ts include) exporting/testing parseMemoryEntry (export it if private) against the REAL recall output shape `[semantic] Title (score: 0.73, strength: 0.72)\n  content…` from src/tool/memory/mod.rs:179 — assert tier/title/snippet parse; also memory_write 'wrote semantic memory (id: x)' tier parse. If the pin proves a mismatch, fix parseMemoryEntry. (4) Fixtures: `frontend/src/lib/ipc-fixtures/event-memory-recalled.json` = {"kind":"memory_recalled","hits":3,"top_title":"t"}; add the sample to `tests/contract_fixtures.rs` samples vec (SerializableAgentEvent::MemoryRecalled { hits: 3, top_title: Some("t".into()) }); add an ipc-contract.test.ts row (dispatch fixture → transcript gains a memory entry named auto-recall when showMemoryActivity true) and a useAgentStore.test.ts row (same + the suppressed-when-false case). Run `npm test` + `npm run build` in frontend/ + `cargo test` root (regenerates/compares fixtures) unpiped.
- [x] 6. **C: verify memory usefulness + hygiene note** — Verify the memory system is used, up to date, and useful (user ask C): (1) run `memory_recall` with queries "md editor", "line endings CRLF", "model changed event" against the live project DB — confirm fresh distilled semantic hits from THIS session return (they exist: write_file IPC + FileViewer md editor MERGED, LF/CRLF fixes). (2) Write ONE semantic hygiene memory: the stale ACTIVE entries (6-batch deep-review plan e342c141, freeze diagnosis sub-plan 3c3833d2, skill-model-midturn 'pending merge') are SUPERSEDED by shipped/merged work (freeze+CodeGraph merged 2026-08-17; skill-midturn merged 2026-08-18; batches merged per deep-review trail) so future recalls don't resurrect dead plans. (3) Note the verification results + any staleness observations in the final summary/commit message. No code changes.
- [x] 7. **Full test matrix** — Full matrix, unpiped, read test-result lines + $LASTEXITCODE: `cargo test` at repo root; `cargo test` in src-tauri/; `npm test` in frontend/; `npm run build` in frontend/. All green + zero warnings before continuing.
- [x] 8. **Reviewer subagent over all changes** — `git status --short` + `git diff HEAD --stat` to enumerate ALL changes (expect: src/agent/turn.rs, src/agent/tests.rs, src/runtime/channels.rs, src/config/general.rs, src-tauri/src/ipc/settings.rs, src-tauri/src/ipc/contract_fixtures.rs, tests/contract_fixtures.rs, frontend/src/lib/types.ts, frontend/src/hooks/agentEventReducer.ts, frontend/src/hooks/agentEventReducer.memory.test.ts (new), frontend/src/lib/ipc-fixtures/{dto-get-settings.json,event-memory-recalled.json}, frontend/src/lib/ipc-contract.test.ts, frontend/src/hooks/useAgentStore.test.ts, frontend/vitest.config.ts). Spawn a read-only reviewer (spawn_agent role:"reviewer") with a SELF-CONTAINED task: the goal (A: ModelChanged emitted on per-turn per-context model override so toolbar/tab show the effective model mid-turn; B2: show_memory_activity default true; B3: MemoryRecalled event emitted on fresh auto-recall hits + frontend memory entry), the changed-file list, and focus: (a) the turn.rs emit happens AFTER the wf guard drops (no lock across await), fires only on resolved_model CHANGE, and reports default_provider.model() when resolved is None; (b) MemoryRecalled fires only on fresh recalls with hits>0 (never on cache reuse or empty); (c) default flip covered ALL fixtures/tests on both sides (dto-get-settings.json, contract_fixtures.rs, settings wire literals, config tests); (d) serde wire shapes match between channels.rs, fixtures, and the TS unions exactly (snake_case kinds, Option<String> ↔ string|null); (e) memory entries gated by showMemoryActivity in both reducers; (f) doc comments, no #[allow], regression tests per defect. Report to .coding/reviews/2026-04-20-agent-visibility-review.md. Do NOT poll — end the turn after spawning.
- [x] 9. **Fix findings, re-test, commit, finish** — Read .coding/reviews/2026-04-20-agent-visibility-review.md; fix EVERY finding (skip only with written justification); re-run the full matrix; commit ALL changes INCLUDING the review report to fix/agent-visibility (verify `git branch --show-current` is not main) with a clear message covering the model-display fix + memory visibility default + MemoryRecalled event + verification notes; then call finish with the review report path.
