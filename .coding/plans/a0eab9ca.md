# Plan: DeepSeek echo/loop hardening: fold volatile tail for DeepSeek + generalize R10 repetition guard

## Goal
Make DeepSeek-vendor turns usable and cap any model's repetition loop: fold the volatile tail (+ skip the CONTEXT_FOOTER) into the system head for DeepSeek-vendor providers via a policy flag (reusing the is_local fold path), and generalize the R10 repetition guard to detect loops of any byte period ≤ 200, with regression tests for both.

## Kind
bug_fixing

## Context
Fresh-eyes trace analysis (plan b2172228, 2027-01-11) proved from .coding/logs/traces.jsonl record id=3: request clean (8 messages, HTTP 200, no loop text in payload — 'REMAINING THOUGHTS'/'exit note' exist in no request, no code, no memory), model echoed the trailing # RECALLED MEMORIES block then looped on the 69-char/74-byte unit "Exit note says 'DeepSeek temporarily disabled — exit notes staged. " 366× in the content channel (74 bytes: the em-dash arrives as the mojibake sequence U+00E2 U+20AC U+201D — 3 chars / 8 bytes; CORRECTION review 2026-09-11 L4 — the Bug section and Detailed steps below were written with a stale 71-byte figure, the correct figure is 74). Code anchors: src/agent/turn.rs:2122-2300 install_system_messages (is_local fold precedent at 2242/2253/2294; caller pops at 454-457); src/provider/policy.rs:179-214 ProviderPolicy + for_kind_and_model (228-255, m.contains("deepseek") → deepseek() at 397); src/provider/stream.rs:387-476 detect_repetition (REPETITION_WINDOW=200, REPETITION_THRESHOLD=3, suffix must be exactly window-periodic); src/provider/openai/stream.rs:451 call site. Regression-test design: (1) detect_repetition fires on the 74-byte exit-note unit ×10 (fails pre-fix, passes post-fix); (2) install_system_messages with a deepseek-v4-flash mock produces no trailing system messages and folds the tail into the head; GLM mock unchanged (trailing pushes preserved). IMPLEMENTATION NOTE (review 2026-09-11 L5): the shipped any-period scan is 1..=window — a safer superset of the planned 16..=window (sub-16 periods that don't divide 200 are equally degenerate loops; p=1 uniform runs were already caught by the old exact-window check). Verification: cargo test (deny(warnings) — green means zero warnings). Cache tradeoff documented: folding sacrifices DeepSeek prefix-cache reuse (empirical cache law, .coding/analysis/cache-hit-2-report.md) — accepted for a vendor that is otherwise unusable; GLM/Anthropic/OpenAI paths unchanged.

## Steps
- [x] 1. **Reproduce with failing regression test** — Write a regression test that reproduces the defect (constitution: every defect gets a regression test that fails without the fix and passes with it). Run it and confirm it FAILS.
- [x] 2. **Document root cause** — Investigate and document the root cause. memory_write a BUG: record (symptom → root cause → fix + regression test name, ≤600 chars).
- [x] 3. **Minimal fix** — Apply the minimal fix that makes the regression test pass. Do not refactor unrelated code.
- [x] 4. **Verify** — Run the regression test + the full test suite (cargo test unpiped, warning-free). Record the regression test name via update_plan (regression_test field) — finish is blocked without it.

## Detailed steps
- **Add fold_volatile_tail to ProviderPolicy** — In src/provider/policy.rs: add `pub fold_volatile_tail: bool` to ProviderPolicy (struct at ~line 179) with a doc comment explaining the DeepSeek context-echo incidents (OPEN sentinel-mirror 5cf5469c + the 2027-01-11 exit-note loop repro): trailing volatile-tail/CONTEXT_FOOTER system messages after the user message trigger echo degeneration on this vendor, so the tail must fold into the head (standard request shape) at the cost of that vendor's prefix-cache reuse. Set fold_volatile_tail: true in deepseek() (~line 397) and false in every other registry entry (claude, gemini, qwen, kimi, glm, vllm_default, openai_responses).
- **Wire the fold into install_system_messages** — In src/agent/turn.rs install_system_messages (~2122-2300): after the is_local check (2242), resolve the policy — ProviderPolicy::for_kind_and_model(provider.kind(), provider.model()) (model() is on the LlmClient trait, returns &str) — and compute `let tail_folded = is_local || policy.fold_volatile_tail;`. Use tail_folded for the head fold (2253), the push skip (2294), and the return value (2299). Update the function doc comment (2113-2121) and the caller (424-457): rename the is_local variable to tail_folded, keep the pops (454-457) gated on !tail_folded, update comments. Behavior: GLM/Anthropic/OpenAI unchanged (trailing pushes); DeepSeek-vendor requests end with the last user/tool message.
- **Generalize detect_repetition to any period** — In src/provider/stream.rs: rewrite detect_repetition (~447-476) to detect ANY period: keep the signature (text, window, threshold); work on text.as_bytes(); needed = window*threshold; fire iff the last `needed` bytes are p-periodic for some p in 16..=window (tail[i] == tail[i-p] for all i in p..needed), early-exit per period; drop the char-boundary bail (byte compare is UTF-8 safe). Update the R10 doc comments (src/provider/stream.rs:387-413 and src/provider/openai/stream.rs:106-115) to describe any-period detection and cite the 71-byte exit-note incident. Existing 200-window tests must still pass (p=200 is in the scan range).
- **Guard regression tests** — In src/provider/openai/tests.rs: add regression tests for detect_repetition: (a) the exact trace unit — "Exit note says 'DeepSeek temporarily disabled — exit notes staged. " (69 chars, 71 bytes with the em-dash) repeated 10× — must fire (this FAILS on current code: 71 does not divide 200); (b) normal prose no-fire; (c) only two repeats no-fire; (d) unrelated prefix before the repeats still fires. Run `cargo test detect_repetition` (PowerShell: run unpiped, then check "exit=$LASTEXITCODE").
- **Packaging regression tests** — In src/agent/tests.rs: (a) policy unit test — deepseek() has fold_volatile_tail true, glm()/qwen()/claude() false; (b) install_system_messages test with a mock LlmClient whose model() returns "deepseek-v4-flash": assert NO trailing system messages were pushed (last message is the user message) and the head (messages[0]) contains the volatile tail ('# RECALLED MEMORIES' / workflow state); (c) same test with a glm model: trailing pushes still happen (tail + CONTEXT_FOOTER as the last two messages). Follow the existing mock-provider pattern in this file. Run `cargo test`.
- **Docs sync + full test run** — Docs sync: PLAN.md provider-strategy section — note that DeepSeek-vendor requests fold the volatile tail into the head (echo incidents; prefix-cache tradeoff accepted) and that the R10 guard now detects any-period loops; check README.md and endpoints.toml examples for any packaging mentions to update. Then full `cargo test` (unpiped, read "exit=$LASTEXITCODE"; deny(warnings) means green = zero warnings).

## Bug
deepseek-v4-flash turns degenerate into an unbounded repetition loop: (a) the trailing volatile-tail + CONTEXT_FOOTER system messages (placed after the user message for prompt-cache reasons) trigger the model's context-echo degeneration — it echoes the recalled-memories block, fabricates sections, and loops; (b) the R10 repetition guard (detect_repetition) only fires when the accumulated tail is exactly 200-byte-periodic, so the 71-byte-period exit-note loop (366 repetitions, 6,194 tokens, 64.5 s, ~1 MB) could never be detected and ran until the user manually cancelled.

## Regression test
detect_repetition_fires_on_exit_note_loop_with_74_byte_period
