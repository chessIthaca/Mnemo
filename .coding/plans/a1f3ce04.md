# Plan: Cut provider waiting: fail-fast timeouts, 502 context guard, and cache-stabilizing token quantization

## Goal
Dramatically reduce time spent in the "Waiting" phase by reducing dead-connection timeouts (30s -> 10s), classifying proxy context-overflow 502s as non-retryable to stop burning 30s of doomed retries, and quantizing `max_completion_tokens` to 2048-token buckets to prevent proxy cache invalidation jitter.

## Kind
implementation

## Context
Analysis of `.coding/logs/provider-errors.jsonl`, `openai.rs`, and `turn.rs` shows that "Waiting" is dominated by: (1) dead connection timeouts holding for 30s per attempt (93s total for 3 attempts); (2) LiteLLM/Vertex 502 context-overflow errors retried 3 times (burning 30s of useless prefill); and (3) prompt cache misses causing 15-45s GPU prompt prefill on long conversations due to `max_completion_tokens` jittering on every turn.

## Steps
- [x] 1. **Shorten HTTP connect timeout from 30s to 10s in OpenAI and Anthropic providers**
- [x] 2. **Classify proxy-wrapped context overflow errors as non-retryable**
- [x] 3. **Quantize max_completion_tokens to eliminate per-turn parameter jitter**
- [x] 4. **Verification & closing sequence**
