# Plan: Self-describing bookkeeping tool results (d779060a)

## Goal
The three bookkeeping tool results become self-describing: memory_amend names the amended record (tier + title, not just the id), update_plan enumerates the applied changes (context appended/replaced with char counts, steps appended/replaced with counts, regression_test/landed_design values), and complete_step carries the completed step's header text — the transcript alone shows what was done.

## Kind
bug_fixing

## Context
Backlog d779060a (user feedback during plan 85368a1f's review; the sibling of e5a84ce9 which landed the feature-scale documentation expectations). INVESTIGATION (line numbers current at HEAD 57e2188): (1) memory_amend's result (src/tool/memory/mod.rs:868) — "Amended knowledge record {id} — dated paragraph appended to {rel}; the memory row re-indexed". The id is resolved via Support::rel_for_id (mod.rs:322-335) which ALREADY fetches the full Memory row (store.get_memory → title/tier/record_class/data) but returns only the rel path — the title/tier just needs surfacing. rel_for_id has 4 callers: :727 (a shared resolution helper), :855 (memory_amend), :1025 (memory_supersede), :1189 (memory_update) — the rel-only callers map mechanically to .rel. (2) update_plan's result (src/tool/workflow/plan.rs:1050) — "updated plan '{title}' (id: {plan_id}) ({completed}/{total} done, {before} → {total} steps) — state: {state}"; the call site (:1028-1059) has ALL args in scope (title/goal/context/steps/append/regression_test/landed_design — the last added by plan 7d333b1d). NO test asserts this format (the only "updated plan" occurrence is the format string itself). (3) complete_step's result (plan.rs:1291) — "Complete Step {n} ({completed}/{total}, {title}) — state: {state}{plan_id_note}{out_of_order_warning}"; after wf.complete_step the plan is re-fetched (:1284) so plan.steps[step_index].text (the completed step's text) is in scope. The DETAILED-tick branch (:1239-1257) already carries the sub-step's opening words via PlanFile::complete_detailed_step's summary ("2/5 detailed steps done — {opening words}", plan_file.rs:408-411) — verified adequate, no change needed there. TESTS ASSERTING THE CURRENT STRINGS: src/tool/memory/mod.rs:2097 (contains("Amended knowledge record")), src/tool/workflow/plan.rs:4253/:4266 (contains("Complete Step 1 (1/2") / "Complete Step 2 (2/2")) — both break with the enriched formats and get updated. FIX DESIGN: (a) rel_for_id → returns a small pub(crate) KnowledgeRef { rel, title, tier } (rename knowledge_for_id; the 3 rel-only callers map to .rel); memory_amend's output becomes "Amended [{tier}] '{title}' (id {id}) — dated paragraph appended to {rel}; the memory row re-indexed" (the item's example format). (b) update_plan's output becomes "updated plan '{title}' (id: {plan_id}): {changes} — {completed}/{total} done — state: {state}" where {changes} is the comma-joined enumeration of ONLY the changed fields: "title set" / "goal set" / "context appended (+N chars)" | "context replaced (N chars)" / "steps appended (+K)" | "steps replaced ({before} → {after} steps)" / "regression_test={name}" / "landed_design={v}" (update_plan errors on no-op, so ≥1 change always). (c) complete_step's output becomes "Complete Step {n} '{step}' ({completed}/{total}, {title}) — state: {state}{plan_id_note}{out_of_order_warning}" where {step} is the step's text truncated to 80 chars (char-boundary safe, "…" suffix). REGRESSION TESTS (fail without the fix): a memory_amend test asserting the output contains the record's tier + title; an update_plan test asserting "context appended (+N chars)" and "regression_test=..."; a complete_step test asserting the step's header text appears between "Step n '" and "(". VERIFICATION: cargo test from repo root (the tool tests live in the lib suite); the full suite must stay green (0 warnings under deny(warnings)).

## Steps
- [x] 1. **Reproduce with failing regression test** — Write a regression test that reproduces the defect (constitution: every defect gets a regression test that fails without the fix and passes with it). Run it and confirm it FAILS.
- [x] 2. **Document root cause** — Investigate and document the root cause. memory_write a BUG: record (symptom → root cause → fix + regression test name, ≤600 chars).
- [x] 3. **Minimal fix** — Apply the minimal fix that makes the regression test pass. Do not refactor unrelated code.
- [x] 4. **Verify** — Run the regression test + the full test suite (cargo test unpiped, warning-free). Record the regression test name via update_plan (regression_test field) — finish is blocked without it.

## Detailed steps
- **Reproduce — failing format assertions** — Add the regression tests that pin the enriched formats (they fail against the current opaque strings): (1) in src/tool/memory/mod.rs's test module — a memory_amend test (extend the existing amend test near :2097 or add a sibling) asserting the output contains "Amended [" + the record's tier + the record's title; (2) in src/tool/workflow/plan.rs's test module — an update_plan test asserting the output contains "context appended (+" and "regression_test=" (a call with context+append+regression_test); (3) a complete_step test asserting the output contains "Complete Step 1 '" followed by the step's text (use a plan whose step 1 has a distinctive header). Run `cargo test <filter>` for each — confirm they FAIL against the current strings (the reproduce evidence).
- **Root cause — document it** — Record the root cause in the plan context (already drafted in create_plan's context, confirm against the code): the three emission sites do not surface metadata that is already in scope at emission time — memory_amend's id resolution (Support::rel_for_id, mod.rs:322) fetches the full Memory row (title/tier) but returns only the rel path; update_plan's call site (plan.rs:1028-1059) has all the args (title/goal/context/steps/append/regression_test/landed_design) but formats only title/id/counts; complete_step's Ok branch (plan.rs:1281-1294) re-fetches the plan (:1284) whose steps[step_index].text is the completed step's text. No behavioral bug — an observability gap at the three result-string emission sites.
- **Minimal fix — surface the in-scope metadata** — (1) src/tool/memory/mod.rs: change Support::rel_for_id (:322-335) to return a pub(crate) KnowledgeRef { rel: String, title: String, tier: Tier } (rename the fn knowledge_for_id; the Memory row's title/tier fields are already fetched at :323); update the 3 rel-only callers (:727, :1025, :1189) to map to .rel; memory_amend's output (:868) becomes "Amended [{tier}] '{title}' (id {id}) — dated paragraph appended to {rel}; the memory row re-indexed". (2) src/tool/workflow/plan.rs: update_plan's output (:1050) becomes "updated plan '{title}' (id: {plan_id}): {changes} — {completed}/{total} done — state: {state}" with {changes} = the comma-joined changed-field enumeration built from args (title set / goal set / context appended (+N chars) | context replaced (N chars) / steps appended (+K) | steps replaced ({before} → {after} steps) / regression_test={name} / landed_design={v}); complete_step's output (:1291) becomes "Complete Step {n} '{step}' ({completed}/{total}, {title}) — state: {state}{plan_id_note}{out_of_order_warning}" with {step} = plan.steps[step_index].text truncated to 80 chars (char-boundary-safe helper, "…" suffix). (3) Update the stale assertions: memory/mod.rs:2097 (contains("Amended knowledge record") → the new tier+title form) and plan.rs:4253/:4266 (contains("Complete Step 1 (1/2") → "Complete Step 1 '" + the step text). The detailed-tick branch (:1239-1257) stays as-is (its summary already carries the sub-step's opening words).
- **Verify — regression tests + full suite** — Run the three regression-test filters — all pass now. Run full `cargo test` from the repo root: all suites green, 0 failed, 0 warnings (deny(warnings)). Then spawn the read-only reviewer over the uncommitted changes (src/tool/memory/mod.rs, src/tool/workflow/plan.rs) — it should check: the KnowledgeRef change compiles cleanly with all 4 call sites updated (no caller left using the old String return), the truncation is char-boundary safe, the changes enumeration covers all seven update_plan fields and never lies (e.g. "context appended" only when append=true AND context provided), the updated assertions match the new formats exactly, the detailed-tick branch is genuinely adequate (opening words in the summary), and docs sync (the tool schema descriptions — memory_amend's description at mod.rs:823-826 mentions the result shape? update_plan's description — check whether they describe the output format and need the same enrichment).

## Bug
The confirmation messages of the three plan/memory bookkeeping tools are opaque at a glance — the transcript alone cannot show what was done without opening the plan file or looking the memory id up: (1) memory_amend (src/tool/memory/mod.rs:868) names the amended record only by id + knowledge-file path, never the record's title/tier, so "Amended knowledge record 12527cd7-…" says nothing about WHICH memory changed; (2) update_plan (src/tool/workflow/plan.rs:1050) reports the plan title/id and step counts but never WHAT was updated — context appended vs replaced, steps replaced/appended, regression_test/landed_design set — so a mid-flight amendment's effect is unverifiable from the transcript; (3) complete_step (src/tool/workflow/plan.rs:1291) reports the step INDEX ("Complete Step 3 (3/4, Plan Title)") but not the step's header text — which work item completed requires opening the plan file. Reproduce: run any memory_amend / update_plan / complete_step and read the tool result — none identifies the amended memory's title, the updated fields, or the completed step's header.

## Regression test
update_plan_result_enumerates_applied_changes
