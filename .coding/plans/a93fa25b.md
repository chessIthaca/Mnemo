# Plan: Sweep merged wt/runall-* branches after PR merges

## Goal
Sweep merged wt/runall-* branches after PR merges (backlog 64662ef2, review L4 of the b52b041a landing): after the human merges a run-all PR, the next run-start deletes the now-redundant local branch (its commits are in origin/main); unmerged branches, non-runall branches, and worktree-checked-out branches are never touched.

## Kind
implementation

## Context
Findings from planning: (1) The gap (review L4 of .coding/reviews/2026-09-21-b52b041a-ruleset-aware-run-all-landing-review.md): since backlog b52b041a the PR-path landing keeps every wt/runall-<item8> branch forever — no code path deletes a merged ref, weakening agent.md's "no branches accumulate" topology goal. (2) Design: a standalone sweeper in src/project/worktrees.rs — `sweep_merged_runall_branches(main_root) -> Vec<String>` (the deleted names, for the run-start log): best-effort `git fetch origin` (a stale origin/main only under-deletes, never over-deletes), `git branch --list 'wt/runall-*'` (the app-managed namespace ONLY — never other branches), then per branch `git merge-base --is-ancestor <branch> origin/main` (exit 0 = the tip's commits are already in main → the local ref is pure redundancy; the remote PR head is untouched) → `git branch -D`. A branch still checked out in a worktree is refused by git itself ("cannot delete branch used by worktree") — the in-flight case is covered without extra code. NO gh pr view double-guard: ancestry into origin/main already implies the commits landed (documented decision). (3) Wiring: called at run-start in src-tauri/src/ipc/run_all.rs's `backlog_run_all` AFTER `adopt_orphaned_in_flight` (the run-start hygiene moment; the acceptance says "the next run-start") — separate from the adoption sweep (different concern: landed-PR leftovers vs orphaned in-flight items); log the swept names via eprintln when non-empty. Mind the source-pin at ~3019 ("backlog_run_all must call adopt_orphaned_in_flight") — untouched by an addition after it. (4) Tests (real git in tempdirs, mirroring the existing upstream-sync test setup at worktrees.rs ~276-345: bare upstream + remote add origin + push -u origin main): merged runall branch swept; unmerged runall branch kept; merged NON-runall branch (wt/other) never touched (not in the listing pattern); a worktree-checked-out merged branch kept (git refuses). (5) Docs to sync: worktrees.rs (remove_worktree_only + land_via_pull_request docs carry "no sweeper yet, review L4" notes → point at the sweeper; the module doc's two-path paragraph), agent.md's branch-policy note ("the branch kept for the human merge" → + swept at next run-start), PLAN.md line ~357 (same). Verification: cargo test --workspace green + warning-free (deny(warnings)).

## Steps
- [x] 1. **The sweeper + tests in worktrees.rs** — src/project/worktrees.rs: add `pub async fn sweep_merged_runall_branches(main_root: PathBuf) -> Vec<String>` + its spawn_blocking impl — best-effort `git fetch origin`; `git branch --list 'wt/runall-*'` (strip the '* ' current-branch prefix defensively); per branch `git merge-base --is-ancestor <branch> origin/main` (is_ok = merged) → `git branch -D <branch>`; collect the deleted names. Full doc comment: the conservative gate (ancestry into origin/main = the commits are in main; the remote PR head is untouched), the worktree refusal being git's own, the no-gh-double-guard decision, and the stale-fetch under-deletes property. Tests in the module's tests module (real git, mirroring the upstream-sync setup): a merged runall branch (branch + commit + merge --no-ff into main + push origin main) is swept; an unmerged runall branch is kept; a merged NON-runall branch (wt/other) is never touched; a provisioned (worktree-checked-out) merged branch is kept.
- [x] 2. **Wire the sweeper at run-start** — src-tauri/src/ipc/run_all.rs: in `backlog_run_all` (the run-start handler), AFTER the `adopt_orphaned_in_flight` call (~3019, whose source-pin must stay intact), call `mnemo::project::worktrees::sweep_merged_runall_branches(state.project.root.lock().await.root.clone())` and eprintln the swept names when non-empty (backlog 64662ef2 comment). Read the call site first for the exact anchor.
- [x] 3. **Docs sync** — Docs sync: src/project/worktrees.rs — remove_worktree_only's doc and land_via_pull_request's doc drop the "no sweeper yet (review L4)" notes in favor of the sweeper pointer ([`sweep_merged_runall_branches`], backlog 64662ef2); the module doc's two-path paragraph gains the sweep sentence. agent.md's Branch policy run-all note: "the branch kept for the human merge" → add that merged branches are swept at the next run-start. PLAN.md's Branch topology row (~line 357): same addition.
- [x] 4. **Verify** — Verify src/project/worktrees.rs and src-tauri/src/ipc/run_all.rs via cargo test --workspace UNPIPED then "exit=$LASTEXITCODE" (PowerShell; never trust a piped exit code) — green + warning-free (deny(warnings) makes a green run prove zero warnings). Confirm the new sweeper tests pass and the run_all source-pins still hold.
