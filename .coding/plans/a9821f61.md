# Plan: Remove the backlog hover preview — superseded by expanding details

## Goal
Remove the backlog hover preview — superseded by the cards' expanding details (user request 2027-01-07). The hover popup machinery (state/refs/fns, card wiring, portal render, the createPortal import) is deleted; the expand/collapse affordance stays untouched. No content becomes unreachable: the headline always renders, the body is reachable via expand, and images stay visible via the always-rendered small thumbnail strip — the only loss is the popup's larger (h-16) hover thumbnails, noted in the resolution. Tests and doc references updated to the new contract (a removal pin + images-reachable pin; the expand coverage stays pinned by the existing tests). tsc catches any unused import/var left behind.

## Kind
implementation

## Context
Explored (line numbers current as of this session's earlier edits): the hover preview machinery in BacklogView.tsx — cardRef/previewTimer/previewOpen/previewPos/showPreview (text>200||images)/showPreviewDelayed/hidePreview + the scroll-close effect (:389-451); the card wiring ref/onMouseEnter/onMouseLeave (:608-611); the portal block {previewOpen && createPortal(...)} rendering item.text + h-16 thumbnails (:918-943); the createPortal import (line 6, becomes unused — tsc catches). KEEP list verified: expanded state (:308, useState(!isLongBody)), isLongBody (:307), hasBody (:306), the chevron toggle (:797-813, aria-expanded + Expand/Collapse titles), the expanded body render (:861-865, Markdown+remarkBreaks). COVERAGE VERIFIED: the headline always renders in the card row (break-words, no clamp); the body is reachable via expand (collapsed by default only when >200 chars); images are ALWAYS visible as small thumbnails (h-10, :872-887, rendered when !editing regardless of expand) — no content becomes unreachable; the only loss is the popup's larger (h-16) hover thumbnails (the user judged the expand sufficient: "no longer needed now that item cards have expanding details") — noted in the resolution, no replacement added. Reference sites to update: BacklogView.test.ts :87-91 (the hover-preview pipeline test → replace with the removal pin + images-reachable pin; expand coverage already pinned at :79-85 + :99-107), messageArgLabel.test.ts :274 (doc comment "(card, hover preview, steer bubbles)"), markdownRendering.test.ts :81 (doc comment "(card + hover preview)"), README.md :79 ("the card and hover preview go through the same pipeline"). useRef/useState/useEffect all stay used (copyResetTimer, editRef, editing, expanded).

## Steps
- [x] 1. **Remove the hover preview machinery** — In frontend/src/components/views/BacklogView.tsx: (1) delete the preview block :389-451 — the comment + cardRef/previewTimer refs, previewOpen/previewPos state, showPreview, showPreviewDelayed, hidePreview, and the scroll-close effect (KEEP the copy-reset effect :428-435 that sits between them — it is unrelated); (2) strip the card wiring on the outer div :608-611 — ref={cardRef}, onMouseEnter={showPreviewDelayed}, onMouseLeave={hidePreview} (keep the className); (3) delete the portal render block :918-943 ({previewOpen && createPortal(...)} — full text + larger thumbnails); (4) remove the now-unused `import { createPortal } from "react-dom";` (line 6); (5) update the two doc comments — :252-254 "the editor, copy button, and hover preview keep using the raw text" → "the editor and copy button keep using the raw text", and :301-304 "Display-only — the editor, copy button, and hover preview keep using the raw text" → "Display-only — the editor and copy button keep using the raw text". Do NOT touch the expand/collapse machinery (expanded state, isLongBody, hasBody, the chevron toggle, the expanded body render).
- [x] 2. **Update tests + doc references to the new contract** — (1) frontend/src/components/views/BacklogView.test.ts — replace the "renders the hover preview through the same remark-breaks pipeline" test (:87-91) with two tests: (a) a removal pin — the source no longer contains the hover machinery (expect(source).not.toContain for previewOpen, showPreviewDelayed, hidePreview, createPortal), with a comment noting long text is reachable via the expand affordance (pinned above) and images via the always-rendered thumbnail strip; (b) an images-reachable pin — expect(source).toContain("{!editing && item.images.length > 0 && (") (the always-rendered small-thumbnail strip). The expand coverage stays pinned by the existing tests (:79-85 body pipeline, :99-107 expand/collapse affordance). (2) messageArgLabel.test.ts :274 — the doc comment "(card, hover preview, steer bubbles)" → drop the hover preview mention. (3) markdownRendering.test.ts :81 — the doc comment "The backlog surfaces (card + hover preview) pass remark-breaks" → "The backlog card body passes remark-breaks". (4) README.md :79 — "the card and hover preview go through the same pipeline as the plan display" → "the card goes through the same pipeline as the plan display".
- [x] 3. **Verify: tsc + vitest + Rust confirm, then closing sequence** — Run npx tsc (catches unused imports/vars — must be exit 0), npm test (all files green), and the Rust suites (cargo test + src-tauri — frontend-only change, quick confirm). Then the closing sequence: reviewer subagent → fix findings → re-run tests → commit to the working branch → finish. Note the resolution's coverage finding in the final summary: the popup's larger (h-16) hover thumbnails are lost with the popup — images remain visible as the always-rendered small (h-10) thumbnails; no replacement added (the user judged the expanding details sufficient).
