# Plan: Token Dashboard: per-project savings ledger + toolbar view before Memory

## Goal
Ship per-project token-savings logging for Mnemo's optimization levers — a savings_events SQLite ledger in the per-project memory DB, instrumenting tool-result truncation and context compaction today with a reserved kind vocabulary for the future levers from backlog e4a50d22 (delta_read, skeleton, compression, archive) — plus a new Dashboard view reachable from the left toolbar directly before the Memory tool, rendering total metered savings, per-kind and per-day breakdowns, recent events, and cache-efficiency stats with pricing-derived estimated $ savings kept strictly separate from metered numbers.

## Kind
implementation

## Context
Backlog e4a50d22 (token-optimizer parity) pulled the dashboard + savings-ledger phase forward at the user's request ("add a dashboard and per project logging ... before the memory tool to the left toolbar"). VERIFIED ANCHORS: stats pipeline = src/memory/schema.rs (CREATE TABLE request_stats ~:105, sessions ~:84 with project column; migrations idempotent PRAGMA-gated, migrate_add_column_if_missing ~:186) → src/memory/mod.rs MemoryStore trait (~:384) + impl (record_request_stats ~:2062, project_stats ~:2196, request_stats_rows ~:2094; all spawn_blocking with expect-locked conn + .map_err("... task failed")) → src-tauri/src/ipc/agent.rs get_project_stats (:940-951: factory → memory_handle → store → serde_json::to_value) → src-tauri/src/main.rs invoke_handler registration (:917) → frontend/src/lib/tauri.ts bindings (getProjectStats ~:1528) → frontend/src/components/views/StatsView.tsx (fmtTokens/fmtCost/modelCost idioms). LEFT TOOLBAR: frontend/src/components/layout/Sidebar.tsx renders per-tool buttons from ALL_RIGHT_PANEL_TABS order + VIEW_BY_ID (derived from frontend/src/hooks/rightPanelViews.tsx RIGHT_PANEL_VIEWS, order plan/diff/git/files/stats/trace/backlog/browser/graph/memory — memory is LAST; new view goes immediately before it). RightPanelTab union + ALL_RIGHT_PANEL_TABS live in frontend/src/hooks/agentState.ts (~:330); rightPanelViews.test.ts asserts union↔registry sync (designed drift catcher — keep it passing). Per-project by construction: each project owns its memory DB (ProjectPicker switches roots). Fire-and-forget recording MUST use log-on-Err (if let Err(e) = ... { eprintln!("mnemo: ...") }) per BUG: swallowed durable writes. Frontend tests: renderToStaticMarkup node-env pattern (BacklogView.test.tsx precedent); check frontend/src/lib/ipc-fixtures/ for a DTO fixture pattern. COLLISION DISCIPLINE: branch wt/mnemo carries ANOTHER AGENT's uncommitted kNN WIP (19 dirty files: failure_triage_knn.rs untracked; ClassifierSection*, settings types/DTOs, src-tauri ipc settings/rewire/contract_fixtures, main.rs, PLAN.md, README.md, docs/CONFIGURATION.md, docs/FEATURES.md, frontend/src/lib/tauri.ts are dirty). Shared files I must touch: src-tauri/src/main.rs (1-line registration), frontend/src/lib/tauri.ts (additive binding), docs/FEATURES.md + README.md (additive section). Additive-only edits with exact anchors; commit via shell `git add <this plan's paths>` then git commit (staged-only; NEVER let commit auto-stage the other agent's WIP). Reviewer task must scope review to this plan's files and explicitly ignore the kNN WIP. Verify commands: cargo test (root, warning-free under deny(warnings)); cargo test in src-tauri (mnemo-app crate); cd frontend && npx tsc --noEmit && npx vitest run.

## Steps
- [ ] 1. **Savings ledger: schema, types, store methods** — Read src/memory/schema.rs (~:105 region) and src/memory/types.rs (RequestStats ~:514) first. schema.rs: add CREATE TABLE IF NOT EXISTS savings_events (id TEXT PRIMARY KEY, session_id TEXT, kind TEXT NOT NULL, detail TEXT, tokens_before INTEGER NOT NULL, tokens_after INTEGER NOT NULL, tokens_saved INTEGER NOT NULL, measured INTEGER NOT NULL DEFAULT 1, created_at INTEGER NOT NULL) + indexes idx_savings_events_created/idx_savings_events_kind beside the request_stats ones — additive new table, no rebuild migration. Doc-comment kinds: 'truncation'|'compaction' now, reserved 'delta_read'|'skeleton'|'compression'|'archive' (backlog e4a50d22). types.rs: SavingsEvent (serde + Clone, RequestStats style) + SavingsStats { saved_tokens_total, event_count, per_kind: Vec<SavingsKindBreakdown>, per_day: Vec<SavingsDayBreakdown>, recent: Vec<SavingsEvent>, cache: CacheEfficiency { prompt_tokens, cached_tokens, cached_not_null_requests, request_count } }. src/memory/mod.rs: add trait methods (near :384) + impl (near :2062, spawn_blocking + expect-locked pattern): record_savings_event(&SavingsEvent) INSERT; savings_stats() -> SavingsStats — per-kind GROUP BY kind, per-day bucket (created_at/86400 like project_stats ~:2245), recent 50 ORDER BY created_at DESC, cache numbers from request_stats SUMs NULL-safe unwrap_or(0). Tests in src/memory/tests.rs mirroring request_stats_rows test (~:1998): record + savings_stats round-trip, per-kind/day aggregation, cache sums.
- [ ] 2. **Instrument truncation + compaction with savings events** — READ FIRST: src/agent/context.rs::truncate_tool_result_at:829, truncate_to_token_budget:1591, compact_old_tool_results:950, mechanical_compaction:1098, compact_headroom_tokens:223; src/agent/turn.rs::maybe_compact:1929; src/runtime/agent.rs::compact_context:1103. Where before/after token counts are already computed, thread the numbers out to the caller that holds the memory handle — return a small savings summary (kind, tokens_before, tokens_after, detail); do NOT give context.rs a store dependency (keep functions pure/testable). The callers (turn.rs / runtime/agent.rs) spawn fire-and-forget store.record_savings_event with the mandatory log-on-Err pattern: if let Err(e) = store.record_savings_event(&ev).await { eprintln!("mnemo: failed to record savings event: {e}") }. tokens_saved = tokens_before - tokens_after (skip when <= 0); measured=1 for truncation/compaction. Extend existing context/turn tests (MockProvider harness, src/agent/tests.rs) to assert the summaries are produced and recorded.
- [ ] 3. **IPC: get_savings_stats command + registration** — src-tauri/src/ipc/agent.rs: add get_savings_stats mirroring get_project_stats (:940-951) — factory → memory_handle → store.savings_stats() → serde_json::to_value. Register in src-tauri/src/main.rs invoke_handler beside ipc::agent::get_project_stats (:917) as a single additive line with an exact anchor — this file carries the other agent's uncommitted WIP; no reformatting. cargo test in src-tauri must stay green.
- [ ] 4. **Frontend: getSavingsStats binding + DashboardView component** — frontend/src/lib/tauri.ts: add getSavingsStats() binding + SavingsStats DTO types mirroring getProjectStats (~:1528) — additive edit, file carries the other agent's WIP. Create frontend/src/components/views/DashboardView.tsx modeled on StatsView.tsx idioms (useEffect fetch + errMsg, fmtTokens/fmtRate from lib/format, fmtCost pattern): summary cards (total saved tokens metered + event count), per-kind table (kind, events, saved), per-day savings bars, recent events list (time, kind, detail, before→after, saved), and a cache-efficiency section (cached vs prompt totals, hit rate; $ saved computed from the pricing table using StatsView's modelCost math — cached tokens billed at cached rate vs input rate — labeled ESTIMATED, never mixed into the metered totals). New DashboardView.test.ts using the renderToStaticMarkup node-env pattern (BacklogView.test.tsx precedent) with a stubbed getSavingsStats. Check frontend/src/lib/ipc-fixtures/ for a DTO-fixture pattern and add one if stats DTOs use it.
- [ ] 5. **Toolbar + view registry: Dashboard before Memory** — frontend/src/hooks/rightPanelViews.tsx: insert { id: "dashboard", label: "Dashboard", icon: Gauge, component: DashboardView } immediately BEFORE the memory entry (:64) — the user's requested position (left toolbar order derives from this registry). Add Gauge to the lucide import. frontend/src/hooks/agentState.ts: add "dashboard" to the RightPanelTab union and to ALL_RIGHT_PANEL_TABS (:330) in the same before-memory position. Verify frontend/src/hooks/rightPanelViews.test.ts passes (union↔registry sync — the designed drift catcher; do not weaken it). npx vitest run green.
- [ ] 6. **Docs + full verify + selective-staged commit** — docs/FEATURES.md + README.md: additive mention of the Dashboard view + the per-project savings ledger — both files carry the other agent's WIP, so append in a distinct region with exact anchors. Full verify: cargo test (root) warning-free, cargo test (src-tauri), cd frontend && npx tsc --noEmit && npx vitest run. Commit with collision discipline: shell `git add` ONLY this plan's files (schema.rs, memory types/mod/tests, context.rs, turn.rs, runtime/agent.rs instrumentation, ipc/agent.rs, main.rs line, tauri.ts, DashboardView.tsx + test, rightPanelViews.tsx, agentState.ts, docs) then git commit — never let the commit auto-stage the kNN agent's WIP (failure_triage_knn.rs, ClassifierSection*, settings DTOs, PLAN.md, etc.).
