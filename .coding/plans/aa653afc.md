# Plan: update_plan fully callable in Reviewing (main agent only)

## Goal
Make update_plan fully callable in the Reviewing state for the main agent (title/goal/context/steps append, matching Executing semantics minus complete_step), while the reviewer subagent remains structurally unable to see or call it — verified and regression-tested.

## Kind
implementation

## Context
Backlog item 24e1c98e-a4df-401a-9458-63cccb67aa30 (created 2026-09-03, AFTER the 2026-08-31 finish↔update_plan deadlock fix — plan 853421fe, knowledge .coding/knowledge/bug/2026-08-31-finish-update-plan-deadlock-blocks-closing-bug-f.md — which the user endorsed as "Both" paths and which landed update_plan in Reviewing as regression_test-ONLY). The item therefore asks for general callability: update_plan in Reviewing must accept title/goal/context/steps like Executing, main-agent-only. Current state of the code: (a) ToolFilter::Reviewing (src/tool/mod.rs:429-441) already names update_plan — dispatch-level availability is DONE; (b) the workflow-side guard (src/workflow/mod.rs:509-526) rejects structural fields in Reviewing ("WorkflowState::Reviewing if !has_structural_fields") — THIS is the blocker; (c) the BugFixing skeleton lock (mod.rs:534-541, steps refused for bug plans) is kind-based and orthogonal — keep; (d) reviewer exclusion is structural: ToolFilter::Reviewer(list) (tool/mod.rs:225-238) auto-grants NOTHING and the reviewer base list never names plan-mutation tools — verify the spawn-path list site (tool_allowlist → mod.rs:339-344) and encode with a regression test; (e) comments/docs carrying the old contract: tool/mod.rs:416-428 (Reviewing arm comment), workflow/mod.rs:509-514 (guard comment), error.rs:38 (WorkflowWrongState doc), factory.rs:1487 (token-budget comment), workflow/mod.rs:2048-2058 (regression_test-only test asserting the old contract — must be updated to the new one). Reviewer role: spawn_agent role:"reviewer" is read-only by construction; agent.md says the reviewer "cannot run plan tools" — must stay true after this change.

## Steps
- [x] 1. **Pin the reviewer allow-list + old-contract sites** — Locate the role:"reviewer" spawn path that seeds ToolFilter::Reviewer's allow-list (search `tool_allowlist` / spawn_agent_shared in src/agent/ and src/runtime/; workflow/mod.rs:339-344 maps it) and confirm update_plan can never be named there. Collect every test asserting the regression_test-only contract (workflow/mod.rs ~2048 update_plan_regression_test_allowed_in_reviewing; any tool/mod.rs tests) and every comment/doc carrying the old contract: tool/mod.rs:416-428, workflow/mod.rs:509-514, error.rs:38, factory.rs:1487. No code changes in this step — evidence only.
- [x] 2. **Relax the workflow guard for Reviewing** — src/workflow/mod.rs update_plan (~509-526): change the state guard so Reviewing matches Executing for ALL fields — `WorkflowState::Executing | WorkflowState::Reviewing => {}`, deleting the has_structural_fields freeze. Keep the BugFixing skeleton lock (mod.rs:534-541) untouched (kind-based, orthogonal). Update the guard comment: the closing sequence (fix findings → re-run tests → commit) may legitimately need to adjust title/goal/context or append steps for follow-on fix work; complete_step stays hidden in Reviewing so appended steps stay unchecked through the exit (finish still gates on the review report); the regression_test field remains accepted (deadlock fix preserved).
- [x] 3. **Sync comments and docs** — Update every comment/doc encoding the old contract to the new one: (a) tool/mod.rs:416-428 Reviewing arm — update_plan is now FULLY callable for the main agent, and the reviewer never sees it (strict allow-list, plan-mutation tools never in the base list); (b) workflow/mod.rs update_plan doc comment (~495-499) if it carries the Reviewing freeze; (c) error.rs:38 WorkflowWrongState doc; (d) factory.rs:1487 token-budget comment if it says regression_test-only. agent.md reviewer protocol line ("cannot run plan tools") stays true — verify no edit needed.
- [x] 4. **Regression tests + suites** — Regression tests in src/workflow/mod.rs tests (+ tool/mod.rs if the reviewer-list site is there): (a) NEW update_plan_full_edit_allowed_in_reviewing — title+goal+context update in Reviewing succeeds (fails pre-change with WorkflowWrongState); (b) NEW steps-append in Reviewing succeeds for an implementation-kind plan (bug plans still refused by the skeleton lock — assert that too in the same or an adjacent test); (c) NEW reviewer exclusion — the reviewer base list / spawn-path allowlist never contains "update_plan" (against the real construction site from step 1, not a hand-built vec); (d) UPDATE the existing regression_test-only test (~2048) to the new contract — structural fields now accepted (rename or extend its assertions so the deadlock protection — regression_test in Reviewing — stays covered). Run cargo test until green; the build must stay warning-free under #![deny(warnings)].
- [x] 5. **Verify, review, commit, finish** — Full closing sequence: cargo test + npm test (frontend untouched, but run for the gate); check doc-sync (README.md per-state tool table if one exists — search 'update_plan' there; agent.md reviewer section; module docs); commit to the current working branch; spawn reviewer role:"reviewer" with self-contained task; fix findings; re-run tests; finish with the review report.
