# Plan: Mandate test → review → commit plan closing in constitution

## Goal
Make every plan end with: run tests → code review of all changes by a spawned reviewer subagent → commit to git, by adding a hard 'Standard plan closing sequence' rule to the project constitution (agent.md) so the agent follows it on every plan.

## Context
In Executing state all tools are available (ToolFilter::Executing → Agent: true), so spawn_agent + git are usable mid-plan. agent.md is the project constitution loaded into every system prompt after the global one, labeled 'The agent cannot ignore these'. The existing 'Run cargo test before marking a workflow step complete' + 'Never commit directly to the main branch' rules live there. The robust way to make every plan end with test→review→commit is a hard constitution rule, not per-plan instruction. Reviewer subagent must be read-only (review + report findings, no edits); the parent applies fixes. Commit must respect the never-commit-to-main rule (commit to the current feature branch).

## Steps
- [x] 1. Add a 'Standard plan closing sequence' section to agent.md: (1) run cargo test as the final step, (2) spawn a reviewer subagent (spawn_agent) to review all uncommitted changes read-only and report findings, (3) address/block on findings, (4) commit with git to the current feature branch (never main, per existing rule). Keep it concise and consistent with the existing constitution tone.
- [x] 2. Run cargo test to confirm nothing broke (docs-only change).
