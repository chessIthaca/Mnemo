# Plan: debug.config.toml: name both platform copy targets (mac-readiness LOW 3)

## Goal
Make debug.config.toml's copy instruction platform-neutral: name both copy targets — %USERPROFILE%\.mnemo\config.toml (Windows) or ~/.mnemo/config.toml (macOS) — so a macOS user following the file literally lands in the right place.

## Kind
implementation

## Context
Mac-readiness review LOW 3 (.coding/reviews/2027-01-06-full-review-mac-readiness.md): debug.config.toml:3-4 — the actionable copy instruction "Copy this file to %USERPROFILE%\.mnemo\config.toml" is Windows-only; a macOS user following it literally copies to a wrong/nonexistent path (line 3's ~/.mnemo/ mention is neutral, but the actionable instruction is not). Fix per the finding: "…to %USERPROFILE%\.mnemo\config.toml (Windows) or ~/.mnemo/config.toml (macOS)". VERIFIED FACTS: (1) global_config_dir() (src/config/mod.rs:483-485) = config_dir_named(".mnemo") — resolves to %USERPROFILE%\.mnemo on Windows and ~/.mnemo on macOS, so both copy targets in the new comment are accurate; (2) no test parses debug.config.toml (repo-wide search: all matches are .coding/ bookkeeping, reviews, and the file itself) — a comment edit has zero test impact; (3) the file is a tracked example config at the repo root, 58 lines, comment-only header at :1-10. CLASSIFICATION: implementation (a tracked-file doc change requiring commit + review; not a defect — no bug/error/crash, a documentation gap; not research — it produces a repo change). The change is confined to the two instruction lines (:4 + a new macOS line); line 3 (~/.mnemo/ mention) and line 5 (merge alternative) stay unchanged.

## Steps
- [x] 1. **Fix the copy instruction to name both platforms** — In debug.config.toml, replace line 4 (`#   Copy this file to  %USERPROFILE%\.mnemo\config.toml`) with the two-platform instruction, keeping the surrounding lines (:3 and :5) unchanged: line 4 becomes `#   Copy this file to  %USERPROFILE%\.mnemo\config.toml  (Windows)` and insert a new line after it: `#   or                 ~/.mnemo/config.toml               (macOS)`. The result reads: "Global config lives in `~/.mnemo/`. To use this for a debugging session: Copy this file to %USERPROFILE%\.mnemo\config.toml (Windows) or ~/.mnemo/config.toml (macOS) (or merge the sections into an existing config.toml)." — exactly the finding's suggested fix text, split across comment lines. Comment-only change; no TOML keys touched.
- [x] 2. **Both Rust suites green** — Run root `cargo test -- --quiet` unpiped (expect 2008 + 16, 0 failed, exit=0) and src-tauri `cargo test -- --quiet` unpiped (expect 216 + 4, 0 failed, exit=0). The change is comment-only in a non-compiled example file, so this is the constitutional proof nothing broke — no test parses debug.config.toml (verified by repo-wide search).
- [x] 3. **Closing sequence: review, fix, commit, finish** — Spawn a read-only reviewer (spawn_agent role:'reviewer', complete task args) over ALL uncommitted changes: cite the plan, the mac-readiness LOW 3 finding, and the verification facts (global_config_dir() resolves to both named paths; no test parses the file). Ask it to verify: the comment now names both platforms accurately (paths match config_dir_named(".mnemo")'s resolution), the Windows target is unchanged from the original, the TOML remains valid (comment-only), no other lines drifted, multi-platform neutrality (the fix IS the neutrality), and doc sync (README/PLAN don't reference this instruction). Fix every finding (re-run tests), round-2 verification if any findings, then commit on wt/agenticcoding (include review reports + plan file) and finish.
