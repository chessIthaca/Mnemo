# Plan: Fix ctx readout basis: provider-anchored, tools+reasoning-inclusive token accounting

## Goal
Make the ctx bar reflect the same basis the provider counts: provider-reported usage.prompt_tokens anchors the bar after each response, and the between-responses local estimate (TokenAccounting) includes reasoning_content + tools-schema overhead so the two agree within <10% on a long session.

## Kind
bug_fixing

## Context
Backlog da4fc87d. Chain mapped: turn.rs:362 token_accounting.update(messages) → AgentEvent::ContextUsage (turn.rs:498/566) → agentEventReducer.ts:800 contextUsage → InflightBar ctx bar. Local basis: count_message_tokens = 4 + content.as_text() + tool-call name/args — misses reasoning_content (stored on Message, counted by provider::estimate_prompt_tokens at src/provider/mod.rs:695 but not by TokenAccounting) and the tools array (schemas at turn.rs:803 self.tools.schemas(...)). Provider basis: LlmEvent::Usage{prompt_tokens} (turn.rs:1116) → AgentEvent::Usage → reducer:785 lastContextBreakdown.prompt = 97.1k figure. Existing turn-loop test src/agent/tests.rs:511-565 asserts ContextUsage used == count_tokens — must stay green. Summarization trigger + hard_ceiling headroom (turn.rs:404-406) consume the same token_count; adding tools+reasoning makes the trigger more correct (headroom comment needs refresh). Retention (plan ffe59699): wire strips historical reasoning per policy while stored raw keeps it — provider-anchored emission self-corrects any divergence. Related-but-separate: backlog 7ba83b5b (trace graphs) — keep basis documented, do not fix here. Frontend likely needs no logic change (Rust re-emits ContextUsage); popup/tooltip text should document the basis.

## Steps
- [x] 1. **Reproduce with failing regression test** — Write a regression test that reproduces the defect (constitution: every defect gets a regression test that fails without the fix and passes with it). Run it and confirm it FAILS.
- [x] 2. **Document root cause** — Investigate and document the root cause. memory_write a BUG: record (symptom → root cause → fix + regression test name, ≤600 chars).
- [x] 3. **Minimal fix** — Apply the minimal fix that makes the regression test pass. Do not refactor unrelated code.
- [x] 4. **Verify** — Run the regression test + the full test suite (cargo test unpiped, warning-free). Record the regression test name via update_plan (regression_test field) — finish is blocked without it.

## Bug
Toolbar prompt-bar ctx readout massively underestimates real prompt size: "ctx 54.5k" while the provider-visible prompt was 97.1k usage.prompt_tokens (~43k missing) — the bar is fed by the local TokenAccounting estimate (content + tool-call args only) which excludes reasoning_content and the tools-schema array, while the provider counts system + tools + full wire history.

## Regression test
context_usage_reanchors_to_provider_prompt_tokens
