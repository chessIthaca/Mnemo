# Plan: Strengthen system-preamble workflow guidance

## Goal
Add workflow/tool-availability, research-planning, and sub-plan emphasis bullets to the CODING_SYSTEM_PREAMBLE CORE PRINCIPLES in src/agent/prompt.rs, and back them with tests.

## Kind
implementation

## Context
The system prompt lives in src/agent/prompt.rs. CODING_SYSTEM_PREAMBLE (lines 13-40) holds the CORE PRINCIPLES the model reads first. WORKFLOW_LIFECYCLE (lines 52-86) already details research planning + sub-plans, but the preamble only says "Plan before you code." The user wants the preamble itself to (1) explain the workflow gates tool availability, (2) encourage planning even for research, (3) remind that sub-plans can be created any time. Existing test lifecycle_encourages_research_planning_and_subplans asserts the full prompt contains "research", "sub-plan", "AT ANY POINT" — must keep passing. build_stable_head is byte-stable and excludes volatile sections; preamble is part of the stable head.

## Steps
- [x] 1. **Add three CORE PRINCIPLES bullets to the preamble** — In src/agent/prompt.rs, edit the CODING_SYSTEM_PREAMBLE const: after the existing `- Plan before you code.` bullet (line 18), insert three new bullets using the same `\` line-continuation style: (1) a bullet explaining the workflow gates tool availability — in PLANNING and COMPLETE only read tools + create_plan are visible, no edits/shell/commit without a plan; in EXECUTING all tools are available; this is enforced not a suggestion. (2) a bullet encouraging a plan even for research/investigation — use create_plan with kind:"research", it structures work into steps and skips review at completion, so plan before diving in. (3) a bullet reminding sub-plans can be created at any point during EXECUTING via create_plan for nested work, and the parent resumes when the sub-plan finishes. Keep wording consistent with WORKFLOW_LIFECYCLE (use "sub-plan" and "AT ANY POINT").
- [x] 2. **Add a preamble-focused test** — In src/agent/prompt.rs tests module, add a test `preamble_emphasizes_workflow_tool_gating_research_and_subplans` that calls build_stable_head with a default Constitution and asserts the head contains: a phrase about tool availability gating (e.g. "gates your tools" or "enforced"), "research", "sub-plan", and "AT ANY POINT". This locks the preamble (not just the lifecycle) to carry the three emphases.
- [x] 3. **Run cargo test** — Run `cargo test` unpiped and confirm all tests pass (including the new one and the existing lifecycle_encourages_research_planning_and_subplans). Confirm zero warnings under #![deny(warnings)].
