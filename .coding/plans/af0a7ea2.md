# Plan: Render backlog item text with the plan display's markdown pipeline

## Goal
Backlog item text renders through the same markdown stack the plan display uses (react-markdown + remark-gfm + rehype-highlight via Markdown/MarkdownImpl) on every read-only surface — Backlog tab card + hover preview, InputBar steer bubbles, backlog_add tool-card chip — while the inline editor stays a raw textarea and single-line surfaces render inline md only (bold/code), truncation-safe. Single newlines must keep rendering as line breaks (2026-08-21 regression) via remark-breaks passed through a new optional remarkPlugins prop on the shared pipeline; chat/plan/FileViewer default behavior unchanged.

## Kind
implementation

## Context


## Steps
- [x] 1. **Extend shared pipeline with remarkPlugins passthrough + add remark-breaks** — In frontend/src/components/chat/MarkdownImpl.tsx add an optional `remarkPlugins` prop (type from ReactMarkdown options) spread AFTER remarkGfm in the remarkPlugins array; in frontend/src/components/chat/Markdown.tsx forward the new prop through the lazy boundary and update both doc comments (why: backlog surfaces must keep single-newline line breaks per the 2026-08-21 regression, which plain CommonMark would collapse). Add "remark-breaks": "^4.0.0" to frontend/package.json dependencies and run `npm install` at the repo root (workspaces).
- [x] 2. **Add InlineMarkdown component + tests** — Create frontend/src/components/chat/InlineMarkdown.tsx: a minimal inline-md tokenizer rendering **bold** → <strong>, *italic* → <em>, `code` → <code> (chat-like styling), ~~strike~~ → <del>; unterminated markers render literally (truncation-safe); no block elements ever. Create frontend/src/components/chat/InlineMarkdown.test.ts (renderToStaticMarkup style): bold/code/italic/strike render, unterminated ** and backtick stay literal, plain text passes through unchanged, output contains no block elements.
- [x] 3. **BacklogView card + hover preview → Markdown** — In frontend/src/components/views/BacklogView.tsx: render the card display text (displayText, lines ~640-646) and the hover-preview portal text (line ~684) through <Markdown remarkPlugins={[remarkBreaks]}> wrapped in prose prose-invert max-w-none sized to the card's text-[0.8em] (verify against @tailwindcss/typography; adjust so the compact size is preserved). Keep click-to-expand, break-words, and the whitespace-pre-wrap fallback classes. The inline editor textarea stays raw; the item.note div stays plain (status annotation, not item text). Update the 'BacklogView line-break display' describe in frontend/src/components/views/BacklogView.test.ts to pin the new contract (card renders via Markdown + remark-breaks; expand/collapse affordance kept).
- [x] 4. **Steer bubble + backlog_add chip → InlineMarkdown** — In frontend/src/components/layout/InputBar.tsx (line ~648) render steer.text via <InlineMarkdown text={steer.text} /> inside the existing truncate span. In frontend/src/components/chat/Message.tsx: add `md?: boolean` to the ToolCardChip type, set it only on the argLabel-derived chip when name === "backlog_add" (push site ~line 695), and render non-path chips through InlineMarkdown when md is set. Add source-contract assertions (BacklogView.test.ts style) pinning both call sites.
- [x] 5. **Tests + build green** — Extend frontend/src/lib/markdownRendering.test.ts with a backlog-item-shaped case (bold header + list + code span, mirroring items 45dcf577/900fe8d8) asserting <strong>/<li>/<code> render like a plan goal does. Run `npm run test --workspace frontend` and `npm run build`; fix all failures.
