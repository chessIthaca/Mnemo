# Plan: Slid resolution with a re-queued (Pending) item still stalls the run

## Goal
At (Complete, false) with a Pending current_item, the run-all resolution recovers instead of stalling: if the re-queued item's work verifiably landed (orphan_work_landed — the drain's done-orphan evidence), resolve it Done exactly like the drain's done-orphan guard; otherwise clear the stale pointer and dispatch the next pending item (the item is already Pending — eligible for re-dispatch). Both the main path (on_main_turn_resolved) and the spawned lane (on_spawned_turn_resolved) recover — no manual restart. The 2026-08-20 never-planned hole stays plugged: a Pending item with no plan_id never resolves Done.

## Kind
bug_fixing

## Context
Review LOW-3 (2027-01-08, the e33a07fd closing review). Related prior fixes: e33a07fd (the closed_earlier landed arm — requires InFlight, run_all.rs:4460-4536), 5bb1e4cd (the status/linkage Done guards), 9836108a (the done-orphan guard — orphan_work_landed, the drain's landed predicate :5148), bba2c82d (the failed-transition guards + the note-preservation discipline: no annotate on blocked rows — the re-queue note is recovery evidence). Key structure: the main tail clears the current_item pointer UNCONDITIONALLY (:4726) and dispatches next for any arm that falls through (:4765-4767) — only the waiting arm returns early; the spawned tail mirrors (remove_spawned_run/retire unconditional, worktree removal best-effort). The 2026-08-20 never-planned hole must stay plugged: a Pending item with no plan_id must not resolve Done (orphan_work_landed requires a plan_id — anything unverifiable is false). Regression-test pattern: source-contract via fn_body(include_str!("run_all.rs"), "<fn>") with body.contains + index ordering pins (see complete_state_turn_end_with_landed_plan_dispatches_next :1935). src-tauri is a binary crate: cargo test WITHOUT --lib, one filter argument only; piped exit codes can lie — trust test result: lines.

## Steps
- [x] 1. **Reproduce with failing regression test** — Write a regression test that reproduces the defect (constitution: every defect gets a regression test that fails without the fix and passes with it). Run it and confirm it FAILS.
- [x] 2. **Document root cause** — Investigate and document the root cause. memory_write a BUG: record (symptom → root cause → fix + regression test name, ≤600 chars).
- [x] 3. **Minimal fix** — Apply the minimal fix that makes the regression test pass. Do not refactor unrelated code.
- [x] 4. **Verify** — Run the regression test + the full test suite (cargo test unpiped, warning-free). Record the regression test name via update_plan (regression_test field) — finish is blocked without it.

## Bug
Run-All stalls forever when the in-flight item is externally re-queued to Pending mid-run (the 5bb1e4cd-style .coding/backlog.jsonl edit, which cannot touch the in-memory current_item pointer) AND the completing turn's resolution slides (the e33a07fd slide: the finish turn's Finished is deferred because a descendant still runs, then discarded when the descendant's completion notification starts a new turn): the notification turn lands at (Complete, false) with the item Pending → closed_earlier=false (it requires InFlight, src-tauri/src/ipc/run_all.rs:4476-4480) → the waiting arm annotates + returns (:4601) → the run never dispatches the next item (Complete is a resting state; nothing ever re-checks) — a manual restart is required. Found as review LOW-3, 2027-01-08, in the e33a07fd closing review (.coding/reviews/2026-09-08-run-all-stall-fix-review.md).

## Regression test
slid_resolution_with_requeued_pending_item_dispatches_next
