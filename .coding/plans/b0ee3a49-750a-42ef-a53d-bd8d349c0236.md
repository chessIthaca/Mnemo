# Plan: Why start.bat fails after loop builds pass

## Goal
Identify the step the agent loop misses that causes start.bat's `tauri build --no-bundle` to fail after `cargo test` passes.

## Kind
research

## Context
start.bat runs `npm install` then `tauri build --no-bundle` (release, custom-protocol feature, beforeBuildCommand = `tsc && vite build`). Root Cargo.toml is a workspace with members=["src-tauri"], so `cargo test` compiles the Tauri crate in debug but never the frontend TypeScript, never release profile, never tauri-build's bundle validation, and never npm-installs new frontend deps.

## Steps
- [x] 1. **Confirm loop vs start.bat verification gap** — Read a few recent plans in .coding/plans/ + reviews in .coding/reviews/ to confirm the closing sequence verifies only `cargo test` (never `npm run build`/tsc, never release build). Search for 'cargo test' and 'npm run build' mentions in the plan closing steps.
- [x] 2. **Enumerate start.bat-only failure surfaces** — Diff what `tauri build --no-bundle` does beyond `cargo test`: (1) beforeBuildCommand `tsc && vite build` compiles frontend TS against the Rust IPC surface (frontend/package.json build script), (2) npm install syncs newly-added deps (start.bat lines 5-16 comment documents this failure mode), (3) release profile + custom-protocol feature, (4) tauri_build::build() validating tauri.conf.json/icons/frontendDist. List each with the file/line evidence.
- [x] 3. **Write up the missing step + fix** — Conclude with the exact verification step to add to the plan closing sequence (run `npm run build` for the frontend and `npx tauri build --no-bundle` or at minimum `cargo build --release`), and note the most faithful simulation of start.bat is running tauri.cmd build --no-bundle itself. No source changes — report findings in the final summary.
