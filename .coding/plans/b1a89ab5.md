# Plan: Re-verify LlmTraceView version-skip done-orphan (second re-dispatch)

## Goal
The item was re-dispatched immediately after research plan 6b834b3d closed it as a done-orphan last turn (work shipped at ccb568d, plan 0a54dcd5, round-2 PASS, both suites green at that verification). This plan re-confirms cheaply that the tree is unchanged since that verification and that the landed work, tests, and records are still present, amends the SPEC record with the second re-dispatch, and closes the item again with zero source changes. If any check surprises (tree moved with code changes, records missing), reclassify as an implementation plan for the actual gap. After the plan completes, the session flips the backlog item done manually (backlog_status) because the automatic flip has now missed this item's research-plan completion twice — documented evidence for the run-all done-orphan guard.

## Kind
research

## Context
Done-orphan loop, SECOND re-dispatch of backlog cd7bd623 (Skip LlmTraceView re-fetch when version unchanged). The work shipped long ago: plan 0a54dcd5, commit ccb568d "perf(trace): skip LlmTraceView detail re-fetch when version unchanged (L5)" (10 files, +308/−3), round-1 review 1-LOW-fixed + round-2 PASS (.coding/reviews/2026-09-10-llmtrace-version-skip-review.md, -round2-review.md), SPEC .coding/knowledge/spec/2027-01-07-trace-record-mutation-version-llmtraceview-detai.md. Last turn's full verification (research plan 6b834b3d, Complete): version field + both bump points in src/provider/trace.rs (with_record :889, enforce_raw_budget :1123), shouldSkipRefetch (frontend/src/components/views/LlmTraceView.tsx:141, wired :816), 9-test contract suite registered in frontend/vitest.config.ts:40, cargo test version_bumps 2/2 green, cd frontend; npm test 78 files/1087 tests green. Backlog now shows cd7bd623 still pending with a fresh dispatch pointer while the finish-closed item a09199b1 flipped to done — hypothesis: the automatic run-all done-flip misses research-plan completions (they reach Complete without a finish call). This plan re-verifies cheaply and records the second re-dispatch; after Complete the session flips the item done via backlog_status. Verification commands: git log/status via git_read; cargo test version_bumps; cd frontend; npm test (re-run only if the tree changed since e786556).

## Steps
- [x] 1. **Confirm the tree is unchanged since the last verification** — git_read log (HEAD; expect e786556 or later bookkeeping-only commits) + git_read diff --stat (expect only the known bookkeeping dirt: .coding/backlog.jsonl, .coding/knowledge/spec/2027-01-07-trace-record-mutation-version-llmtraceview-detai.md, .coding/plans/6b834b3d.md, plus this plan's new file); spot-check the landed symbols: graph_search shouldSkipRefetch (frontend/src/components/views/LlmTraceView.tsx) and search version_bumps in src/provider/trace.rs. If HEAD moved with CODE changes since e786556, re-run `cargo test version_bumps` and `cd frontend; npm test` in step 2; otherwise last turn's green runs stand as the acceptance evidence (same tree).
- [x] 2. **Confirm the prior re-verification records** — read .coding/knowledge/spec/2027-01-07-trace-record-mutation-version-llmtraceview-detai.md (must carry the 2027-01-12 amendment from plan 6b834b3d) and .coding/plans/6b834b3d.md (research kind, 4/4 steps complete). If step 1 ordered a suite re-run, execute it here and require green before continuing.
- [x] 3. **Record the second re-dispatch** — memory_amend the SPEC record (id be5a62f5-dd31-5d15-b9d9-44e6ac25494e, file .coding/knowledge/spec/2027-01-07-trace-record-mutation-version-llmtraceview-detai.md) with a dated paragraph: re-dispatched again on 2027-01-12 immediately after plan 6b834b3d closed it; re-verified unchanged (HEAD + dirty-file + symbol spot-checks); closed again as a done-orphan with zero code changes; records the flip-miss hypothesis (research plans complete without finish, so the run-all done-flip never fires) as evidence for the run-all done-orphan guard. No source files change (no-code research).
