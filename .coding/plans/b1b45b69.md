# Plan: Slim the merge_to_main skill (prompt trim + skill_start echo de-dup)

## Goal
Cut the merge_to_main skill's injected prompt roughly in half without losing any operative rule, and stop skill_start from double-injecting the full prompt in its tool result.

## Kind
implementation

## Context
Exploration done: the skill prompt (~1.9KB) is injected into the system prompt every turn while the skill runs (src/agent/prompt.rs workflow_section, Skill state, lines 605-617) AND echoed in full once in the skill_start tool result (src/tool/workflow/skill.rs line 147-150, "Drive toward the goal: {prompt}"). ~Half the prompt is lore/rationale duplicating agent.md + the TOML header comment (which is NOT injected — TOML comments aren't parsed). Test pin: src/skill/mod.rs test shipped_skill_files_parse_and_merge_to_main_cleans_up_memories asserts prompt contains "MERGED into main" — keep that exact phrase. Seeding is write-if-missing (seed_skills), so existing projects keep their old copy; acceptable, note in review. User chose: trim prose + de-dup echo.

## Steps
- [x] 1. **Rewrite the skill prompt as a terse checklist** — Edit .coding/skills/merge_to_main.toml. Rewrite `prompt` as a compact numbered checklist keeping EVERY operative rule: (1) no stash — commit uncommitted work incl. .coding/ on the current branch; (2) checkout main + git merge --no-ff <branch>; (3) resolve conflicts via file_edit + git add/commit (backlog.jsonl has a union merge driver); (4) verify merged tree: npm run build AND cd src-tauri && cargo build, fix + repeat until green (one-line why: branch verification never compiles the bin crate); (5) git branch -d once merged + green; (6) supersede stale branch-status memories citing the merged branch (search: branch name, pre-merge tip sha, memory_list prefix SHIPPED, working-tier ACTIVE:/STEP MARKER: crash markers) with successor text containing the exact phrase "MERGED into main at <sha>" — skip when nothing matches, never supersede other branches' records; memory DB self-heals, never hand-edit; (7) skill_end; abandon_skill if unresolvable; optional git push is approval-gated. Move rationale/lore (ten-orphan-stashes story, develop-tier history) into the TOML header comment. Keep header topology lecture trimmed (agent.md already carries it). Preserve the exact phrase "MERGED into main".
- [x] 2. **De-dup the skill_start result echo** — Edit src/tool/workflow/skill.rs SkillStartTool::execute (~line 146-151): the success result must stop repeating the full prompt (the system prompt already carries it every turn). New message names the skill + target state and points to the system prompt, e.g. "started skill '{skill}' (target: {target}). Goal injected into the system prompt — follow it." First search the repo for tests/docs asserting the old "Drive toward the goal" string and update them.
- [x] 3. **Add echo-de-dup regression test** — In src/tool/workflow/skill.rs tests: extend start_skill_overrides_prompt_and_target (or add a test) asserting the start result does NOT contain the skill's prompt text (the de-dup) while the workflow still records the full prompt as the active skill goal. Verify the existing skill/mod.rs test still passes unchanged (phrase "MERGED into main" preserved by step 1).
- [x] 4. **Test + docs check** — Run cargo test (unpiped; check $LASTEXITCODE). Warning-free build is enforced by #![deny(warnings)]. Search README.md / PLAN.md / module docs for anything quoting the skill prompt or the skill_start echo text; update if stale.
