# Plan: Note commit requires message in git tool description

## Goal
Add a note to the git tool's top-level description that `commit` requires a `message` argument.

## Context
The git tool's top-level description (src/tool/agent/git.rs:140-145) lists what each subcommand does but never mentions that `commit` requires a `message` argument. The `message` field's own description says "Required for commit", but the tool description — what the LLM sees as the tool's purpose — should state it too so the model doesn't attempt a message-less commit. The runtime already enforces this (git.rs:199 returns "commit requires a 'message' field"), so this is purely a prompt clarity change.

## Steps
- [x] 1. **Add commit-needs-message note to git tool description** — in `src/tool/agent/git.rs`, in the `GitTool::schema` description string (lines 140-145), add a clause stating that `commit` requires a `message` argument (the commit message). Keep the existing content; just append the note. Then run `cargo test --lib tool::agent::git` to confirm the existing tests (which assert on the description? check) still pass and the build is warning-free.
