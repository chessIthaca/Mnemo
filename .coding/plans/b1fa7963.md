# Plan: Token-optimizer parity levers: delta reads, output compression, archive/expand, compaction survival, quality score

## Goal
Implement the six in-scope token-optimizer parity levers natively in the tool dispatch layer + agent core: delta/skeleton re-reads, semantic command-output compression, archive/expand progressive disclosure, compaction survival, S-F quality score, lean-output nudge — all [optimizer]-flagged default-off, each writing savings_events rows (schema per backlog 652ae094), with per-lever tests and the frontend ctx-popup quality display.

## Kind
implementation

## Context
Backlog item e4a50d22 (amended 2027-01-11): scope is the LEVERS only — dashboard + savings-ledger aggregates/IPC went to item 652ae094 (pending). This plan lands: (1) delta+skeleton re-reads, (2) semantic command-output compression, (3) archive/expand progressive disclosure, (4) compaction survival (checkpoints + decision extraction + digest), (6) S-F quality score surfaced in InflightBar ctx popup, (7) lean-output nudge — plus the minimal savings_events recording path (table + record_savings_event, EXACTLY 652ae094's step-1 schema: id, session_id, kind, detail, tokens_before, tokens_after, tokens_saved, measured, created_at; kinds here: delta_read, skeleton, compression, archive, archive_expand, compaction_checkpoint) so levers write before/after rows. 652ae094 keeps savings_stats aggregates + IPC + DashboardView.

CODE ANCHORS (verified this session): tool-result ingestion cap turn.rs:1671 (cap_tool_result_text, src/agent/context.rs:797, 100K-char cap); aging pass turn.rs:1838 → context.rs:950 compact_old_tool_results (10/20 hysteresis, 500-char cuts); compaction turn.rs:1929 maybe_compact → summarize_with_interrupt ~:2064, *messages = summarized :2224, store via record_stats_row turn.rs:2694 (self.memory Option<Arc<dyn MemoryStore>>, fire-and-forget spawn); manual compaction runtime/agent.rs:1103 compact_context; summary prompt build + mechanical_compaction context.rs:~1098 + build_summary_prompt (pure); volatile tail + CONTEXT_FOOTER push turn.rs:2654-2682, popped :478/:506 (cache-safe injection site); shell result shape shell.rs:589-652 (filtered output + [exit code: N], raw stdout/stderr in data JSON); shell_filter.rs = default-ON deterministic line filter precedent (is_error_line never-dropped rule, [[shell_filter.override]] user extension precedent — my compressor is separate, default-off, runs after it); read_files read_one_with_count serves full slices (DEFAULT_MAX_LINES 500, TOTAL_BYTE_CAP 512K); tool factory src/agent/factory.rs:997 (with_codegraph builder pattern), tool registry list tool_contract.rs:153; config precedent [general.laya] + [shell_filter] in src/config/general.rs (is_default + skip_serializing_if pattern); ContextUsage event channels.rs:249 + :572 + :813, wire fixture frontend/src/lib/ipc-fixtures/event-context-usage.json, src-tauri destructure sites events.rs:942 (used,max,.. unaffected) + events.rs:2101 construction site (needs quality: None added); frontend reducer agentEventReducer.ts:931 reduceContextUsage, state type agentState.ts:297, popup InflightBar.tsx:~364-422.

DESIGN INVARIANTS (hard): never mutate the already-sent conversation prefix (prompt-cache stable head + byte-stable CONTEXT_FOOTER untouched; nudges ride the volatile tail which is popped after the request); archive BEFORE substituting and fail-open on every archive/store error (fallback = today's cap_tool_result_text text, zero data loss); every lever behind an [optimizer] flag, default off → flag-off paths byte-identical, existing truncation/compaction tests must stay green UNMODIFIED; context.rs stays store-free (returns records; callers with self.memory archive); tools stay store-free (emit data.savings {kind, tokens_before, tokens_after}; the turn loop records rows); credential redaction on every model-served surface of compressed output; tools report estimated tokens (chars/4 or tiktoken if available) with measured=false.

ENVIRONMENT: Windows 11, PowerShell, root C:/AgenticCoder/AgenticCoder; run cargo test UNPIPED and read "test result:" lines (piped exit codes lie). src-tauri (mnemo-app) is UNTESTABLE on this machine (Application Control blocks fresh builds) — keep the src-tauri delta to additive construction-site fixes only (events.rs:2101 test vector + any other construction sites found by search); attempt cargo test -p mnemo-app in the verify step and document the block if it hits; CI verifies the compile. Frontend verified via npx tsc --noEmit + npx vitest run. Deps already present: similar=2 (unified diff), tiktoken-rs, rusqlite bundled (FTS5 — memories_fts trigger precedent in src/memory/schema.rs), tree-sitter grammar set — NO new dependencies. All public functions need doc comments; build must be warning-free (#![deny(warnings)]). Two uncommitted .coding/ files (backlog.jsonl amendment carrying this item's scope note + kNN spec amendment — prior session's bookkeeping) must be folded into the first commit, not lost.

VERIFICATION: cargo test (root, all existing truncation/compaction tests unregressed); npx tsc --noEmit + npx vitest run in frontend/; attempt cargo test -p mnemo-app from src-tauri/ (document Application Control limitation if blocked). Acceptance per item: unit tests per compressor family (real cargo/npm/pytest fixture outputs), delta/skeleton correctness tests, archive/expand round-trip + FTS search test, savings rows asserted with before/after counts, cargo test green with existing truncation tests unregressed.

## Steps
- [x] 1. **Ledger + [optimizer] config foundation** — Run `cargo test` (root, unpiped; read `test result:` lines) and note the green baseline count. Add OptimizerConfig to src/config/general.rs ([optimizer] TOML section, GeneralConfig field with skip_serializing_if=is_default like LayaConfig): flags delta_reads, compress_output, archive, compaction_survival, quality_score, lean_output_nudge (bool, default false); knobs archive_min_chars=20_000, compress_min_chars=2_000, lean_output_fill_pct=25, nudge_cooldown_requests=10, compress_extra_commands: Vec<String> (user-extensible allowlist). Add savings_events table to src/memory/schema.rs (id TEXT PK, session_id TEXT, kind TEXT NOT NULL, detail TEXT, tokens_before INTEGER NOT NULL, tokens_after INTEGER NOT NULL, tokens_saved INTEGER NOT NULL, measured INTEGER NOT NULL DEFAULT 0, created_at INTEGER NOT NULL) + indexes (session_id, created_at) and (kind) — exactly 652ae094's schema. SavingsEvent type + async record_savings_event on the MemoryStore trait (src/memory/mod.rs trait ~:384) + impl (~:2062 area, mirroring record_request_stats). AgentLoop helper record_savings_event(session_id, kind, detail, before, after, measured) in src/agent/turn.rs mirroring record_stats_row:2694 (fire-and-forget tokio::spawn, eprintln on failure, no-op when self.memory is None). Round-trip test in src/memory/tests.rs: insert → read back → assert before/after/saved/measured persist. `cargo test` green before step complete.
- [x] 2. **Lever 1 — delta + skeleton re-reads in read_files** — New src/tool/agent/read_cache.rs: ReadCache (Arc<RwLock<HashMap<PathBuf, FileSnapshot>>>; FileSnapshot = full served content + hash + mtime + len) attached to ReadFilesTool via with_read_cache builder, constructed fresh per factory build (src/agent/factory.rs:997) so the cache is per-agent (subagents never inherit false 'already read' state). Skeleton extractor skeleton_for(text, lang) using the existing codegraph Lang detection + per-family signature/import line regexes (Rust fn/struct/enum/trait/impl/mod/use; TS/JS function/class/interface/export/import; Python def/class/import; Go func/type/import; Java/C#/C/C++ analogues), keeping line numbers, capped ~120 lines with a truncation note. Changed-file path: unified diff via similar::TextDiff (unified format with context); if diff exceeds ~40% of file chars, serve full content instead. Wire in read_files.rs read_one_with_count: flag OFF → byte-identical today's behavior, no cache writes. Flag ON: first read of a path serves full + caches snapshot; whole-file re-read with unchanged (hash/mtime/len) → skeleton + note 'file unchanged since your last read — signature skeleton served; re-read with start_line/max_lines for exact lines' + data.savings{kind:skeleton}; changed → diff + note + data.savings{kind:delta_read}; ANY ranged read (start_line/max_lines) serves full content (natural escape hatch, no schema change). tokens_before = estimate(full), tokens_after = estimate(served) via an estimate_tokens helper (chars/4 fallback; measured=false). Turn loop: after tool result ingestion (execute_tool_call), extract data.savings if present → AgentLoop::record_savings_event. Tests (read_files.rs cfg(test)): flag-off byte-identical; unchanged re-read → skeleton (assert signature line present, body absent); changed → diff contains the changed hunk; ranged re-read → full; cache populated only when flag on.
- [x] 3. **Lever 2 — semantic command-output compression** — New src/tool/agent/output_compactor.rs: compress(command, stdout, stderr, exit_code) -> Option<CompactedOutput>, invoked only when compress_output flag on AND filtered output > compress_min_chars. Family detection by command prefix: cargo build/check/clippy/test; npm|yarn|pnpm test/build (vitest/jest/tsc/vite); pytest / python -m pytest; go test/build; PLUS user-extensible compress_extra_commands regexes from config. Per family: keep DISTINCT error/warning lines (global dedup with 'repeated N×' annotations), collapse recognized noise to counts ('[compactor] N compile-progress lines collapsed', 'N passing tests collapsed'), header line 'compressed <family> output — exit <code>: E error / W warning / P passing lines kept of T total', plus the command's own final summary lines. Credential-safe: redact_secrets fn (bearer/api-key/token/password/secret URI patterns → [REDACTED]) applied to everything model-served. Only substitute when compacted is meaningfully smaller (else keep original). Wire in src/tool/agent/shell.rs AFTER the existing shell_filter (~:604-613): shapes only the LLM-facing combined/output; raw stdout/stderr stay in data JSON; exit-code line preserved; emit data.savings{kind:compression, tokens_before, tokens_after}. Tests: fixture consts with REAL captured outputs — cargo build with compile errors + repeated 'Compiling' lines, cargo test with failures + many passing tests, npm ERR! failure block, pytest failure summary — assert: distinct error lines kept verbatim + deduped with counts, noise collapsed to counts, exit status present, an embedded api-key redacted, non-family command passes through untouched.
- [x] 4. **Lever 3 — archive + expand progressive disclosure** — Schema (src/memory/schema.rs): tool_result_archive table (id TEXT PK, session_id TEXT, tool TEXT, detail TEXT, content TEXT NOT NULL, char_count INTEGER NOT NULL, created_at INTEGER NOT NULL) + FTS5 external-content tool_result_archive_fts following the memories_fts trigger pattern, with graceful fallback when FTS5 is unavailable (search degrades to LIKE scan). Store methods on MemoryStore trait + impl: archive_tool_result(...) -> id; expand_tool_result(id, start_line?, max_chars) -> content; search_archive(query, limit) -> rows (id, detail, snippet). New tool src/tool/agent/expand_result.rs (AutoRun, read-only): args {id?, query?, start_line?, max_lines?}; id → serve archived content (capped, redacted); query without id → FTS search listing matches with snippets; register in factory.rs tool build + tool_contract.rs:153 registry (no-store contexts return a helpful error result). Ingestion substitution turn.rs:1671: flag on AND output.len() > archive_min_chars AND self.memory is Some → archive the FULL original (await the write) → serve preview = first ~1500 chars + '[full output archived: X chars ~Y tokens — expand with expand_result id=<id> or query search]' + last ~500 chars; archive write error → fall back to today's cap_tool_result_text text (fail-open, log) — archive BEFORE substituting, always. Record kind=archive savings row. Aging pass: extend context.rs:950 compact_old_tool_results to also return truncation records (message idx, tool name, original content, pointer) for results ≥ archive_min_chars — the function stays pure/store-free; the turn.rs:1838 caller archives returned records fire-and-forget; other callers (mechanical_compaction/enforce_sendable) drop them unchanged. expand_result serves record kind=archive_expand with NEGATIVE tokens_saved (net re-expansions subtract from savings). Tests: memory store round-trip (archive → expand returns identical content; FTS search finds by keyword; fallback path); turn-level big-result → preview carries id + archive row exists; store-error path → today's capped text; expand writes negative savings row.
- [x] 5. **Lever 4 — compaction survival (checkpoint + decisions + digest)** — New src/agent/optimizer.rs (pure functions, no store): extract_decisions(messages) -> Vec<String> — scan assistant/user text for decision markers (DECISION:, 'decided', 'we'll use', 'chose X over', 'must', "don't") cap ~10, dedup, in chronological order; build_digest(messages) -> String — heuristic counts: files touched (read_files/search tool calls), shell commands run, error-marked results, approximate line counts — NO LLM call; checkpoint_region(messages, keep_recent) -> String — serialized form of the approx dropped region messages[1..len-keep_recent]. Wire in turn.rs maybe_compact (flag on + self.memory Some): archive checkpoint_region as kind=compaction_checkpoint BEFORE summarize_with_interrupt (await, fail-open → compaction proceeds on error); pass decisions to the summarizer as a must-preserve block — extend context.rs build_summary_prompt with an optional must_preserve: Option<&str> param (pure; None renders byte-identical today's prompt) and include the same block in the mechanical_compaction fallback note; after *messages = summarized (:2224), push ONE system note behind the flag carrying build_digest + 'pre-compaction context archived (expand_result id=… to retrieve dropped detail)'. Mirror the checkpoint+digest wiring in runtime/agent.rs:1103 compact_context. Tests: pure-fn tests (extract_decisions caps at 10 + finds DECISION: lines + dedup; digest counts tool calls/errors; checkpoint_region excludes kept tail); store round-trip for checkpoint writes; build_summary_prompt without must_preserve renders byte-identical (pin existing compaction tests stay green unmodified — flag off = no behavior change).
- [x] 6. **Lever 6 — S-F quality score + ContextUsage surfacing** — src/agent/optimizer.rs: QualityGrade (S,A,B,C,D,F) + QualityReport {grade, fill_pct, waste_tokens, stale_read_rate, decision_density} + compute() with deterministic thresholds (e.g. S: fill <40% and waste <5% of served tokens; degrade bands through D/F at >90% fill or waste-dominated) — unit-tested at band boundaries. OptimizerState on AgentLoop/TurnState: counters (reads_total, skeleton_serves, cumulative waste/savings tokens, decisions_seen) fed by the record_savings_event helper so every lever updates ledger AND counters atomically. Event: extend AgentEvent::ContextUsage + SerializableAgentEvent::ContextUsage (channels.rs:249/:572/:813) with quality: Option<QualityReport> using skip_serializing_if Option::is_none (wire stays backward compatible — existing fixture pins intact); update EVERY construction site found by search (turn.rs:460, :2287, :3112, runtime/agent.rs:1303/:1352, channels.rs:572, agent/tests.rs sites, AND src-tauri/src/ipc/events.rs:2101 test vector gets quality: None — src-tauri delta stays additive-construction-only). Attach Some(report) at the top-of-loop emission (turn.rs:460) and post-compaction (:2287) when quality flag on; None everywhere else. Tests: grade threshold unit tests; serialization JSON carries quality only when Some (ipc fixture shape preserved).
- [x] 7. **Lever 7 — lean-output + quality-drop nudges (cache-safe)** — turn.rs:2654-2682 (volatile tail + CONTEXT_FOOTER push site): flag on AND fill > lean_output_fill_pct AND cooldown elapsed (nudge_cooldown_requests since last nudge, tracked in OptimizerState) → append ONE steering line into the volatile_tail content before the push: '[context guidance] context is N% full; output tokens cost ~5× input — keep visible output lean (no preamble, no restating context)'. Quality-drop nudge shares the same cooldown mechanics: when the computed grade drops ≥2 bands vs the session's best-seen grade → one-line note naming the drop and its top component. The tail is popped after the request (:478/:506) so the persisted prefix is never mutated — cache-safe by construction; no event emission (context-only, mirrors the correction-message precedent at turn.rs:1803). Tests: fill above threshold → tail carries exactly one guidance note; immediate second request suppressed by cooldown; flag off → tail byte-identical to today's (regression pin).
- [x] 8. **Frontend — ctx popup quality display** — frontend/src/hooks/agentState.ts:297 contextUsage type gains optional quality?: {grade: string; fill_pct: number; waste_tokens: number; stale_read_rate: number; decision_density: number}; agentEventReducer.ts:931 reduceContextUsage stores it (absent quality keeps state unchanged); InflightBar.tsx ctx popup (~:364-422) renders a color-coded S-F grade badge + component rows when quality present, nothing when absent. ipc-contract: ADD frontend/src/lib/ipc-fixtures/event-context-usage-quality.json (existing event-context-usage.json stays untouched — backward-compat pin) + test in frontend/src/lib/ipc-contract.test.ts asserting the reducer stores quality and the popup-relevant state. vitest: reducer test (quality stored/preserved), InflightBar popup render test (badge shown with quality, hidden without). Run npx tsc --noEmit + npx vitest run in frontend/ — green.
- [x] 9. **Docs + full verification + landing** — Docs: README.md feature section (optimizer levers, [optimizer] config example with all flags/knobs, expand_result tool, expand example); PLAN.md technical notes (lever design, invariants, savings_events + tool_result_archive tables, quality formula, pointer that the savings dashboard/aggregates live in backlog 652ae094); update config example files if present (search for config*.example / config docs). Verify: `cargo test` (root, unpiped — ALL existing truncation/compaction/cap tests green UNMODIFIED), `npx tsc --noEmit` + `npx vitest run` in frontend/, attempt `cargo test -p mnemo-app` from src-tauri/ and document the Application Control limitation verbatim if blocked (CI covers the compile; src-tauri delta is additive construction-site fixes only). Update backlog notes: e4a50d22 → in_flight→done on finish; note on 652ae094 that savings_events schema + record_savings_event + tool_result_archive landed here, its remaining scope = savings_stats aggregates + IPC + DashboardView. Write SPEC memory documenting the landed design + invariants. Then closing sequence: reviewer subagent, fix findings, re-run tests, commit everything (including the two pre-existing uncommitted .coding/ bookkeeping files) to the working branch with the review report.
