# Plan: Fresh-eyes trace analysis: deepseek hello-turn repetition loop

## Goal
From the clean trace capture ONLY (fresh-eyes protocol — no prior hypotheses), determine why the deepseek-v4-flash "hello" turn degenerated into a repeating "exit note" text loop (~6,194 completion tokens, ~1MB stream) until the user cancelled it: what exactly was sent, what came back, and where the loop text originated.

## Kind
research

## Context
Repro captured with a clean trace log (emptied beforehand per plan 9953fafc). Evidence so far: .coding/logs/traces.jsonl has 4 records (~2MB, ~500KB per line — read_files cannot slice within a line; read-only PowerShell JSON parsing is the extraction path). Line 1 = glm-5.3 turn confirming the protocol (request_json.messages: system, user, recalled-memories system, context-footer sentinel; response_raw = SSE stream). The deepseek-v4-flash CANCELLED "hello" request is among lines 2-4. Screenshot evidence: params stream:true, max_completion_tokens:32000, reasoning_effort:max; 8-message chain; HTTP 200; ~6,194 completion tokens, 0 reasoning tokens, ~1MB SSE payload; loop text = "[released] deepseek-exit-note: DeepSeek temporarily disabled — exit notes staged. Exit note says '...'". Also check .coding/logs/emit-diag.log (24KB, mtime matches repro window). provider-errors.jsonl is older (pre-repro) — out of scope. Fresh-eyes protocol (DECISION b4ebc339): ignore prior hypotheses; conclusions only from trace evidence. Related pending backlog item: 1f56297a (intra-response reasoning-loop defense). Verification: PowerShell parse of .coding/logs/traces.jsonl per record (id/model/ts/roles/finish), string searches for "deepseek-exit-note" in request vs response.

## Steps
- [x] 1. **Inventory the 4 trace records** — NO-CODE RESEARCH (read-only PowerShell). Parse each line of .coding/logs/traces.jsonl as JSON; print id, model, ts_ms, message count, per-message roles, and any status/finish/usage fields. Identify which record is the deepseek-v4-flash CANCELLED hello request. Also show head/tail of .coding/logs/emit-diag.log (24KB) to see what it captures.
- [x] 2. **Extract the deepseek request verbatim** — NO-CODE RESEARCH (read-only PowerShell). From the deepseek record in .coding/logs/traces.jsonl: dump request_json.messages — role + FULL content of every message except the main system prompt (first 300 chars only for that one). Catalog each injected block (prior turns, recalled memories, remaining thoughts, context footer). Answer the key question: does 'deepseek-exit-note' or 'exit notes staged' appear ANYWHERE in the request payload?
- [x] 3. **Analyze the deepseek response stream** — NO-CODE RESEARCH (read-only PowerShell). From the deepseek record's response_raw in .coding/logs/traces.jsonl: count SSE data chunks; extract the first reasoning_content delta and first content delta; find the first occurrence of 'deepseek-exit-note'; extract the repeating unit and count repetitions; determine which channel (reasoning_content vs content) carried the loop; note the final chunk state (cancelled mid-stream, no finish_reason/usage?).
- [x] 4. **Trace the loop-text origin** — NO-CODE RESEARCH. memory_search (tier working, then semantic) for 'deepseek-exit-note' / 'exit notes staged' to find whether that text pre-existed in harness state (working-memory event, exit-note feature) or is novel deepseek generation. Check .coding/logs/emit-diag.log for related diagnostics. Also check what the FOLLOWING glm-5.3 record in .coding/logs/traces.jsonl (current turn) received as context — did the loop text get re-injected as 'remaining thoughts'?
- [x] 5. **Locate the bug in the harness code** — NO-CODE RESEARCH (read-only). With trace evidence in hand, find the producing code: search src-tauri/src (Rust) for literal strings seen in the trace ('exit note', 'REMAINING THOUGHTS', 'deepseek-exit-note', 'released', 'Turn interrupted') and the request-assembly / stream-parsing paths; graph_context on the symbols found. Pin the exact defect that produces or feeds the repetition loop (where exit notes are staged, where remaining thoughts are injected, how reasoning_content/content deltas are accumulated). Old bug memories are hints only — verify everything against live code.
- [x] 6. **Write evidence-based findings** — NO-CODE RESEARCH. Synthesize root cause strictly from extracted evidence: request composition, response behavior, loop-text origin, and the code path that produces it. memory_write a BUG record (symptom → evidence → root cause → fix direction). Note relation to pending backlog 1f56297a (intra-response reasoning-loop defense) and propose concrete fix directions. No source-code changes in this plan.
