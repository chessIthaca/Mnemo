# Plan: Steering layer round 2: id-carrying nudge, read nudge, spawn rider, metrics

## Goal
Extend the steering layer: symbol-id embedded in the search nudge, whole-file read nudge, recall rider seeding spawned agents' first prompt, and steering-effectiveness metrics surfaced in the Trace tab.

## Kind
implementation

## Context
Follow-ups to the merged steering layer (plan 77768e94, commits 750fafa+7696a72 on wt/agenticcoder — tree clean; user has NOT rebuilt the app yet, nothing depends on a live app). All four items chosen by the user. Anchors from exploration: (a) symbol_nudge at src/tool/agent/search.rs:231-246 is pub(crate), SHARED with search_read, and rides on CodeGraph::symbol_exists(name)->bool at src/codegraph/mod.rs:179 — no id returned today. (b) ReadFilesTool { sandbox } at src/tool/agent/read_files.rs:105-114 has NO graph handle; ReadSpec{path, start_line: Option<usize>, max_lines: Option<usize>} (:88-96) ��� whole-file = both None; single-file shorthand lifts top-level path/start_line/max_lines into a one-element batch (:169+); output notes go ABOVE results (with_note/merged_note pattern :307-324); no symbols-by-file query exists in src/codegraph/ yet. (c) SpawnAgentTool { spawner: Arc<dyn AgentSpawner>, parent_id, model_resolver } at src/tool/agent/spawn_agent.rs:76-113 — builder-chaining pattern already established (with_parent/with_model_resolver); task text becomes the spawned agent's FIRST PROMPT (module doc :5-10); no memory store held. (d) create_plan's recall_rider (private, src/tool/workflow/plan.rs, ~:505-580 after the L1 fix; called AFTER drop(wf) per the never-hold-lock-across-store-round-trips rule) builds the RECALLED CONTEXT block from recall_peek (passive, NO access bump) — extract and reuse. (e) Metrics: tool calls dispatch via src/agent/dispatch.rs (referenced from turn.rs:9); trace/stats reach the frontend via runtime/channels.rs events + src-tauri/src/ipc/ commands (find the Trace-tab surface via frontend search "Trace"). Existing steering markers for the matcher table: search nudge contains "is an indexed symbol", shell TIP starts "TIP: for file-content search", graph miss hint contains "No symbols matched", rider starts "RECALLED CONTEXT". Constraints: recall_peek only (never recall/batch_access — self-reinforcement loop); never reorder TOOL_STRATEGY entries (pinning tests prompt.rs ~:1230); platform-neutral; #![deny(warnings)]; cargo test before completing each step; public items need doc comments.

## Steps
- [x] 1. **Graph id lookup + SYMBOL NUDGE carries the id** — src/codegraph/mod.rs: add `pub fn symbol_id(&self, name: &str) -> Option<String>` next to symbol_exists (:179) — same exact-match one-SELECT lookup but returning the symbol's id column (file::name::line), best-effort (store errors → None). Do NOT change symbol_exists (check its other callers first). src/tool/agent/search.rs symbol_nudge (:231-246): call symbol_id, embed the id — new text: `'"' is an indexed symbol — graph_context(id="<id>") gives its definition + callers in one call; prefer the graph tools for symbol lookups (search is for text)`. search_read shares the fn automatically. Update tests: bare_identifier_symbol_searches_earn_the_graph_nudge (search.rs :1088) asserts the id is embedded (graph_context(id=); non_identifier_patterns_never_nudge stays. Add a codegraph unit test for symbol_id (exact hit + miss + best-effort) mirroring symbol_exists_is_exact_and_best_effort (:397). Run cargo test.
- [x] 2. **Whole-file read_files nudge** — src/codegraph/mod.rs: add `pub fn symbol_count_in_file(&self, path: &str) -> Option<usize>` — one SELECT count of symbols whose file column matches, best-effort → None. src/tool/agent/read_files.rs: add `graph: Option<Arc<crate::codegraph::CodeGraph>>` field + `pub fn with_codegraph(...)` builder (None default keeps ~all test sites unchanged). In execute: for whole-file specs (start_line AND max_lines both None — the shorthand path lifts into the same batch) with a .rs/.ts/.tsx extension whose returned content exceeded 300 lines AND symbol_count_in_file > 0, prepend ONE leading note ABOVE the results (with_note pattern, search.rs :307): `SYMBOL NUDGE: <path> has <N> indexed symbols — graph_context(id) gives targeted edges (definition/callers/blast radius); read line slices for surrounding detail` (join multiple qualifying files with '; '). Factory: wire `.with_codegraph(...)` at the production ReadFilesTool registration gated on self.codegraph (find it in src/agent/factory.rs — the register-agent-tools fn; leave test sites untouched). Check whether the separate file_read tool (src/tool/agent/file_read.rs) is still registered in the factory; if yes, note it in the step report as a follow-up rather than expanding scope. Tests: big indexed .rs file whole-read → note present with symbol count; sliced read (start_line or max_lines set) → absent; small file / non-source extension / no graph → absent. Run cargo test.
- [x] 3. **Recall rider on spawn_agent tasks** — Extract the RECALLED CONTEXT block builder from src/tool/workflow/plan.rs recall_rider into a new shared pub(crate) module src/tool/steering.rs: `pub(crate) async fn recalled_context_block(store: Option<&Arc<dyn crate::memory::MemoryStoreTrait>>, query: &str, bug_query: Option<&str>) -> Option<String>` — same semantics (recall_peek ONLY, limit Some(3) + optional record_type Bug pass Some(2) when bug_query, dedupe by id, truncate 5, first-content-line gist ≤140 chars, same header text, silent None on no store/hits/error). plan.rs recall_rider delegates to it — the two existing rider tests (create_plan_rides_recalled_context, create_plan_without_store_has_no_rider) must pass UNCHANGED. src/tool/agent/spawn_agent.rs: add `memory: Option<Arc<dyn MemoryStoreTrait>>` + `pub fn with_memory(...)` builder (chain pattern like with_model_resolver :109); in execute, after arg validation and BEFORE delegating to the spawner, best-effort `recalled_context_block(self.memory.as_ref(), &args.task, None).await` appended to the task text passed downstream — the rider rides the spawned agent's FIRST PROMPT (task = first prompt, spawn_agent.rs:5-10). Wire with_memory at the factory's SpawnAgentTool registration (chained next to with_parent/with_model_resolver). Tests in spawn_agent.rs: a capturing mock AgentSpawner (extend the existing test fixtures) asserting the task carries RECALLED CONTEXT when a seeded store matches, unchanged task when no store, and access_count stays 0 (passivity). Run cargo test.
- [x] 4. **Steering-effectiveness metrics** — First explore-then-hook: find the single choke point every tool result passes through (src/agent/dispatch.rs, referenced from src/agent/turn.rs:9; follow execute_tool_call). Add `src/agent/steering_stats.rs`: `pub struct SteeringStats` (interior-mutable counters, Arc-shared) recording per marker kind {fired, switched} — marker matcher = stable substring table: "is an indexed symbol" → targets {graph_search, graph_context, graph_impact, graph_path}; "TIP: for file-content search" → {search, search_read, graph_search, graph_context}; "No symbols matched" → {search, search_read}; "RECALLED CONTEXT" → {} ( surfaced, not switch-targeted — count fires only ); "SYMBOL NUDGE:" (the new read nudge) → graph tools. Track per-agent last-fired marker; when the NEXT tool call from the same agent names a target, increment switched. Wire the shared Arc<SteeringStats> at the choke point (no changes inside the nudge sites themselves — detection is on result.output). Expose via a new IPC command in src-tauri/src/ipc/ (mirror an existing read-only stats command — see get_index_progress or the trace-stats surface) returning the counters; surface one line in the frontend Trace tab (find the component via frontend search 'Trace'; e.g. 'Steering: 12 nudges fired, 8 switched (67%)' + per-kind breakdown). Tests: matcher unit test per marker; a fake two-call sequence (result with marker → next call graph_context) increments fired+switched; non-target next call does not. Run cargo test AND npm run build (frontend touched).
- [x] 5. **Test suite + docs sync** — Run full cargo test (unpiped; read `test result:` lines + $LASTEXITCODE) and npm run build. Docs: README.md steering bullet (~:53) gains the id-embedding, whole-file read nudge, spawn_agent rider, and Trace-tab steering stats; PLAN.md Agent-tools bullet (~:88) + Workflow/spawn bullets extended likewise; module doc comments in codegraph.rs, read_files.rs, spawn_agent.rs, steering.rs, steering_stats.rs kept accurate (they are part of the reviewer's docs-sync check).
