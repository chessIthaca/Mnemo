# Plan: Agent|tools splitter: caret motif + region hairlines

## Goal
Make the App.tsx agent-window|tools-panel ResizeHandle fully match the other two handles' style (caret pill + cyan hover — already in source) AND add the tiny region-defining border hairlines in the app's standard border color, so the agent and tools regions read as separated at rest.

## Kind
implementation

## Context
User report: the two resize handles they like (InflightBar.tsx:127-133, FileViewer.tsx:558-567 — gray #334155 grip pill + cyan/20 hover wash) look right; "the horizontal one that splits agent window and tools" lacks the caret/color and needs a tiny #273346 region border. Analysis: that handle is App.tsx ResizeHandle (App.tsx:594-681, aria-label "Resize tools panel") — the only handle splitting the agent column from the right tools panel. Current source ALREADY has the caret pill (h-8 w-0.5 rounded-full bg-border, line 678) + hover:bg-cyan-500/20 (line 671) — restored today in unmerged commit aadd5e7 after plan 50af2cd3 had removed it; the user's running build predates the restore, which is why they don't see it. NEW work: the handle sits in a 4px transparent gap with no region-defining line (RightPanel's border-l was removed by plan e1c63669). #273346 does NOT exist anywhere in the repo — the "color we use everywhere else to define regions" is --border-color: #334155 (globals.css:14; Tailwind border-border; light theme maps to #cbd5e1, so the token must be used, not a hardcoded hex). Geometry gotcha: with border-x (1px each side) on the current w-1 (4px) div, the 2px pill would touch both hairlines and, being the same color (#334155), render as a solid band that swallows the caret — so widen to w-1.5 (6px): hairline | 2px gap | caret | 2px gap | hairline, which also matches the h-1.5 thickness of the other two handles. Drag logic (pointer capture, nextElementSibling measurement, hit area -left-1 -right-1) is unaffected. Precedent: JSX className restyles ship without new tests (no DOM harness); existing suites + build + cargo test must stay green. Branch: current feature branch (tree contains aadd5e7); never commit to main. Only pre-existing dirty file is .coding/plans/5866e160-*.md (bookkeeping checkbox — leave out of this task's commit).

## Steps
- [x] 1. **Add region hairlines to the main ResizeHandle (App.tsx)** — In frontend/src/App.tsx ResizeHandle return JSX (~:664-680): (1) on the outer div, change className `relative flex w-1 shrink-0 cursor-col-resize items-center justify-center transition-colors hover:bg-cyan-500/20` → `relative flex w-1.5 shrink-0 cursor-col-resize items-center justify-center border-x border-border transition-colors hover:bg-cyan-500/20` (widen w-1→w-1.5 so the 1px border-x hairlines don't touch the 2px pill; border-border = --border-color #334155, the everywhere-else region color). (2) Keep the grip pill div and invisible hit-area div exactly as-is. (3) Update the inline comments + the component doc comment (:580-592) to state: the handle shows the standard motif (centered grip pill + cyan hover wash, matching InflightBar/FileViewer) AND 1px border-x hairlines that define the agent-column / tools-panel regions at rest; the w-1.5 column plus the wider invisible hit area keep it easy to grab.
- [x] 2. **Verify builds and tests** — PowerShell, run each command unpiped and read test-result lines + $LASTEXITCODE directly (piped exit codes lie per constitution): (1) `cd frontend; npm test` (vitest run — all suites green). (2) `cd frontend; npm run build` (tsc + vite proves the App.tsx edit compiles). (3) From repo root `cargo test` (no Rust changed; must stay green, zero warnings under #![deny(warnings)]). Fix any failure before proceeding.
- [x] 3. **Review, fix findings, re-verify** — (1) Spawn reviewer (spawn_agent role:"reviewer") over ALL uncommitted changes with a self-contained task: goal = give App.tsx ResizeHandle (agent|tools splitter) tiny 1px border-x border-border region hairlines + widen w-1→w-1.5 (geometry: hairlines must not visually merge with the 2px bg-border pill) while keeping the restored caret/hover motif and all drag logic/aria intact; changed file = frontend/src/App.tsx only; focus = border-box geometry correctness, token use (border-border vs hardcoded hex), light-theme correctness, comment accuracy, no dead code, nothing else touched. END THE TURN after spawning; resume on the background-finished notification. (2) Read the report; fix EVERY finding; re-run step 2 checks.
- [x] 4. **Commit and finish** — (1) `git branch --show-current` + `git status --short` — commit ONLY frontend/src/App.tsx + the new review report under .coding/reviews/ to the current feature branch (never main); leave the pre-existing .coding/plans/5866e160-*.md checkbox change out of this commit. (2) Call finish with the review report path. (3) Final summary ≤3 lines: caret was already restored in source (aadd5e7, unmerged — needs an app restart/rebuild to show), hairlines added in the standard region color #334155 (= border-border; #273346 appears nowhere in the repo, token keeps light theme correct), handle widened to 6px so the caret stays distinct. Also write a short memory note (semantic) documenting the #273346→border-border interpretation.
