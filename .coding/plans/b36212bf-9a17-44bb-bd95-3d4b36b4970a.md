# Plan: Phase 3c step 5 — backlog ▶ semantics clarification (scoped UI text only)

## Goal
Clarify the backlog ▶ (Run All) button semantics in the UI: entry point for unattended processing of all pending items (with git checkpoints), stop is graceful (current finishes), progress reflects resolved count. Update labels, tooltips, confirm text, and progress display for precision without changing backend behavior or adding new features.

## Context
Step 5 of active Phase 3c subplan. Prior steps (plan UI, auto-reveal diff, statusbar persist) done. Follow scoped, preserve existing, cargo before complete.

## Steps
- [x] 1. Read BacklogView.tsx fully and related store/tauri wiring for current ▶/stop/progress code.
- [x] 2. Read backend comments in run_all.rs / commands.rs for exact documented semantics to mirror in UI labels.
- [x] 3. Make targeted, minimal text/tooltip/confirm updates in BacklogView to clarify entry point, stop semantics, and progress.
- [x] 4. Ensure no behavior change; safety note remains.
- [x] 5. Run cargo test (unpiped) before complete_step.
