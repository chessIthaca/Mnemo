# Plan: Cleanup: delete merged feat/codegraph branch + bookkeeping commit

## Goal
Delete the merged feat/codegraph branch (local + origin) and land the stack.json bookkeeping so the repo is fully clean.

## Kind
implementation

## Context
feat/codegraph (tip 3d927b2) is fully merged into main (merge commit bc210d4) and both are pushed; user asked to clean up. Cleanup = commit the dangling .coding/plans/stack.json bookkeeping change (skill-entry removal) on main, delete the local branch, delete the remote branch. Branch deletion is safe: tip is an ancestor of main. Windows 11 + PowerShell, unpiped commands reading $LASTEXITCODE.

## Steps
- [x] 1. **Commit stack.json bookkeeping on main** — Currently on main. Working tree has one modified file (.coding/plans/stack.json — the ended merge skill's stack entry removed). Commit with message 'chore: clear merge_to_main skill state from plan stack (bookkeeping)'. Then push main to origin (approval-gated).
- [x] 2. **Delete feat/codegraph local + remote** — Run `git branch -d feat/codegraph` (safe delete; must succeed since branch tip 3d927b2 is an ancestor of main bc210d4). Then `git push origin --delete feat/codegraph` (approval-gated). Verify with `git branch -a` that only main remains locally and origin/main is the sole remote branch, and `git status --short` shows a clean tree.
