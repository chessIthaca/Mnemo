# Plan: Sandbox: close the hardlink / 8.3-alias / ladder-fallback write-protection bypasses

## Goal
Close the three pre-existing ways to defeat Sandbox's path-prefix write protection in src/tool/agent/sandbox.rs — (1) a hardlink inside .coding/ shadowing a protected file, (2) a Win32 8.3-alias spelling (plus its cross-platform in-root-link twin) reaching a protected directory, (3) validate_for_write's step-5 lexical fallback returning an escaping path — each with a regression test that fails without its fix; full cargo test green, warning-free, and neutral on macOS + Windows (only the 8.3 test is Windows-gated).

## Kind
bug_fixing

## Context
CODE ANCHORS (all in src/tool/agent/sandbox.rs unless noted). `is_protected_write_target` (:217) is NAME-based: components lowercased + trailing dots/spaces trimmed, ADS + any `.git` component refused, then `.coding/{memory.db*,codegraph.db*,safety.toml,backlog.jsonl,backlog.json}` / `plans` / `reviews` / `knowledge` refused — no inode awareness, no alias awareness. `validate_for_write` (:383) is the 5-step ladder: validate → validate_for_creation → protected check → create_dir_all → revalidate; step 5 is `Err(_) => Ok(validated)` (:402-407) = bypass 3; steps 1-3 only ever see the lexical/creation spelling = bypass 2. `refuse_if_protected` (:414) is the second gate, covering file_edit (src/tool/agent/file_edit.rs:1301, :1408), convert_line_endings (src/tool/agent/convert_line_endings.rs:101), the IPC mirror (src-tauri/src/ipc/files.rs::write_sandboxed :126) and both file_write/file_edit approval previews — so a check added inside `is_protected_write_target` reaches ALL of them. Ladder callers: src/tool/agent/file_write.rs::execute (:140) and src/tool/agent/file_append.rs::execute (:157). `lexical_path_is_link_free` (:345) already exists — the research carve-out's fail-closed companion (symlink_metadata walk, stops at the first missing component) — and is reusable for the ladder's lexical branch.

FIXES. (1) Add a private `link_count(path) -> Option<u64>` (cfg unix `std::os::unix::fs::MetadataExt::nlink()`, cfg windows `std::os::windows::fs::MetadataExt::number_of_links()`, `None` elsewhere/nonexistent) and, in `is_protected_write_target`, right after the `.coding` first-component check, return true when the link count is > 1 — the fail-closed variant the backlog sanctions. Scoped to `.coding/` because that is where by-name protection lives; a hardlink OUTSIDE .coding pointing at a protected .coding file is NOT caught by this rule (needs an inode walk) — document that as the accepted residual in the doc comment. (2) In `validate_for_write`: (a) before create_dir_all, canonicalize the deepest EXISTING ancestor of the validated path and refuse via `protected_refusal(path)` when `is_protected_write_target` is true for it, so `.coding/KNOWLE~1/new/…` is refused BEFORE any mkdir; (b) re-run `is_protected_write_target` on step 5's CANONICAL result and refuse if protected (the backlog's stated direction; the canonical form resolves the alias to `knowledge`). The same two layers close the cross-platform twin `.coding/link -> .coding/knowledge` (in-root link into a protected tree), which is this family's non-skipping test. (3) Fail closed in step 5: track whether step 4 actually created the parent (`created`); on `Err(_)` return `Ok(validated)` ONLY when `created` (the just-created case is safe: link components were already refused and the parent is a fresh real dir), otherwise propagate the error; and in the step-2 creation branch require `self.lexical_path_is_link_free(&lexical)` — otherwise refuse with a dedicated link-refusal message — so an escaping link (including a dangling leaf, where `validate` returns Ok) is refused BEFORE create_dir_all.

REGRESSION-TEST DESIGN (write these FIRST; each must FAIL pre-fix). (1) `protected_hardlink_inside_coding_refused` (cross-platform): tempdir + `Sandbox::new`; make `.coding/`; write `.coding/safety.toml`; `std::fs::hard_link` it to `.coding/x.toml`; assert `validate_for_write(".coding/x.toml")` is Err mentioning "protected", `is_protected_write_target` is true, and safety.toml's content is untouched; plus a plain nlink==1 `.coding/analysis/notes.md` still validates Ok (no over-refusal). (2) `#[cfg(windows)] protected_win32_8dot3_alias_refused`: create `.coding/knowledge`; resolve the leaf's 8.3 name with `windows_sys::Win32::Storage::FileSystem::GetShortPathNameW` (windows-sys is ALREADY a target-gated dep at Cargo.toml:120-126 — add the `Win32_Storage_FileSystem` feature there; if the symbol is not re-exported, declare the FFI directly); if the resolved leaf equals the long name (or the alias does not resolve to the same dir) SKIP with a visible `eprintln!("SKIP: …")` marker, else assert `validate_for_write(".coding/<SHORT>/new/x.md")` is Err and `.coding/knowledge/new` does NOT exist. Diagnostic: `fsutil 8dot3name query C:` (read-only) decides whether this assertion exercises or skips on this volume. (2b) `validate_for_write_refuses_in_root_link_into_protected_tree` (cross-platform): plant `.coding/link -> .coding/knowledge` with the shared fixture; assert Err and no planted `.coding/knowledge/new`. (3) `validate_for_write_fails_closed_on_out_of_root_link` (cross-platform): plant `.coding/link -> <outside tempdir>`; assert Err for `.coding/link/x.md` AND `.coding/link/nested/x.md`, and that the outside dir stays empty (no created dirs either); include a dangling file-link leaf (`.coding/dangling.md` -> outside/missing.md) via the file fixture. (3b) `file_write_through_out_of_root_link_is_denied` in src/tool/agent/file_write.rs `mod tests` (mirror `make_tool` :238 and the :612/:658 pattern): execute file_write with the link path, assert an error result and that nothing exists outside. Verify with `cargo test` UNPIPED + `$LASTEXITCODE` (a piped exit code is the pipe's, per agent.md).

FIXTURE. `plant_dir_link` / `plant_file_link` exist as private copies in src/agent/dispatch.rs:2050-2113 (`New-Item -ItemType Junction` on Windows, symlink on POSIX, `false` + diagnostics when unavailable) and inline in src/agent/tests.rs:5501-5531. The new tests need them in a third place → move them ONCE into a `#[cfg(test)] pub(crate) mod link_fixture` in src/tool/agent/sandbox.rs and point the two existing sites at it (no third copy).

DOCS to sync (reviewer checks documentation): sandbox.rs doc comments (validate_for_write's step list, is_protected_write_target's protected list + the hardlink rule + residual, the module doc's Perf/syscall accounting at :11-58), PLAN.md:120-135, PLAN.md:438 and :490-505, docs/FEATURES.md:22. Multi-platform: (2) is `#[cfg(windows)]`; (1) and (3) must hold on macOS and Windows (cfg-split nlink API; the link fixtures fall back to POSIX symlinks / Windows junctions).

VERIFICATION COMMANDS: `cargo test` (full, unpiped); targeted during development `cargo test --lib sandbox` and `cargo test --lib file_write`. Build must stay warning-free (#![deny(warnings)] at src/lib.rs:5 — a green cargo test proves it; never add #[allow(...)]).

## Steps
- [x] 1. **Reproduce with failing regression test** — Write a regression test that reproduces the defect (constitution: every defect gets a regression test that fails without the fix and passes with it). Run it and confirm it FAILS.
- [x] 2. **Document root cause** — Investigate and document the root cause. memory_write a BUG: record (symptom → root cause → fix + regression test name, ≤600 chars).
- [x] 3. **Minimal fix** — Apply the minimal fix that makes the regression test pass. Do not refactor unrelated code.
- [x] 4. **Verify** — Run the regression test + the full test suite (cargo test unpiped, warning-free). Record the regression test name via update_plan (regression_test field) — finish is blocked without it. If the fix turned out FEATURE-SCALE (new pub types or cross-module surface): amend the plan context via update_plan append with a paragraph starting "Landed design —" (architecture, deviations from the sketch, measured numbers, invariants to preserve) and record landed_design=true BEFORE completing this step; at finish write a SPEC memory and amend the BUG record with the landed outcome (agent.md 'Documentation expectations'; the finish gate blocks landed_design without the context amendment).

## Detailed steps
- [x] **One canonical link fixture** — add `#[cfg(test)] pub(crate) mod link_fixture` to src/tool/agent/sandbox.rs holding `plant_dir_link(target, link) -> bool` and `plant_file_link(target, link) -> bool`, moved VERBATIM (with their doc comments and the `New-Item -ItemType Junction` / symlink / `#[cfg(not(any(unix, windows)))]` arms) from src/agent/dispatch.rs:2050-2113. Delete dispatch.rs's two private copies and call `crate::tool::agent::sandbox::link_fixture::…` from its two tests (`research_write_verdict_canonicalizes_an_existing_target`, `research_write_verdict_fails_closed_on_a_link_out_of_the_root`); replace src/agent/tests.rs:5501-5531's inline junction block with the same helper call (keep the test's `SKIP:` eprintln! semantics). Verify `cargo test --lib dispatch` + `cargo test --lib research_write` still pass before touching any fix.
- [x] **Write the five regression tests PRE-FIX and record them failing** — in src/tool/agent/sandbox.rs `mod tests` add `protected_hardlink_inside_coding_refused`, `validate_for_write_refuses_in_root_link_into_protected_tree`, `validate_for_write_fails_closed_on_out_of_root_link`, and `#[cfg(windows)] protected_win32_8dot3_alias_refused`; in src/tool/agent/file_write.rs `mod tests` add `file_write_through_out_of_root_link_is_denied` (mirror `make_tool` at :238 / the tool pattern at :612). Use the exact fixture/assertion design from the plan context (hardlink via std::fs::hard_link + safety.toml content intact + a plain nlink==1 artifact still Ok; `.coding/link -> .coding/knowledge` → Err + no planted subtree; `.coding/link -> <outside tempdir>` → Err for `.coding/link/x.md` and `.coding/link/nested/x.md` + outside stays empty + a dangling file-link leaf; 8.3 leaf via GetShortPathNameW with a visible SKIP marker when unavailable — add the `Win32_Storage_FileSystem` feature to the existing windows-sys dep at Cargo.toml:120-126 first). Run `fsutil 8dot3name query C:` (read-only) to learn whether the 8.3 assertion will exercise or SKIP on this volume. Then run `cargo test --lib sandbox` and `cargo test --lib file_write` UNPIPED and record that every new test fails for its expected bypass reason. Fix nothing yet.
- [x] **Fix (1): refuse hardlinked targets under .coding/** — in src/tool/agent/sandbox.rs add a private `fn link_count(path: &Path) -> Option<u64>` next to `normalize_path` (doc comment; `std::fs::metadata(path).ok()?` — follows symlinks, which is what we want — then cfg unix `Some(meta.nlink())`, cfg windows `Some(u64::from(meta.number_of_links()))`, cfg(not(any(unix, windows))) `let _ = meta; None` — no unused-variable warning). In `is_protected_write_target`, immediately AFTER the `.coding` first-component check (:264-266) and before the final match, `return true` when `link_count(path)` is `Some(n)` with `n > 1`. Extend the doc comment: detection is by link count, canonicalize cannot see a hardlink, the rule is fail-closed and scoped to `.coding/**`, and the residual (a hardlink outside `.coding` pointing at a protected `.coding` file is not caught — it needs an inode walk of the protected trees) is recorded there as accepted out-of-scope.
- [x] **Fix (2): protected check on the CANONICAL form** — in `validate_for_write`, (a) in the step-2 creation branch (after the fix-(3) link-free gate lands, or before it if done first), walk up from `validated` to the deepest EXISTING ancestor, canonicalize it, and `return Err(protected_refusal(path))` when `is_protected_write_target` is true for that canonical ancestor — this refuses `.coding/KNOWLE~1/new/…` BEFORE create_dir_all (skip the check when canonicalize errors; document that). (b) In step 5, on `Ok(canonical)` re-run `is_protected_write_target(&canonical)` and refuse if protected, before returning it. Both layers also close the cross-platform twin `.coding/link -> .coding/knowledge`. Add the `Win32_Storage_FileSystem` feature to the windows-sys entry in Cargo.toml if step 2 has not done it yet.
- [x] **Fix (3): fail closed in the ladder** — in `validate_for_write`: (a) in the step-2 creation branch, after `validate_for_creation`, require `self.lexical_path_is_link_free(&lexical)`; otherwise return `Err(Error::InvalidInput(...))` with a message naming the refusal (e.g. "refused: '<path>' traverses a symbolic link or junction — write to the resolved path instead") so an escaping link, including a dangling leaf, is refused BEFORE create_dir_all; (b) track `created: bool` from step 4 (true only when `create_dir_all` actually ran because the parent was missing); in step 5 return `Ok(validated)` on `Err(_)` ONLY when `created`, and otherwise propagate the error (`Err(e)`) — fail closed. Rewrite the doc comment's numbered step list to describe the link-free gate, the canonical-ancestor check, the canonical re-check, and the narrowed fallback (note the TOCTOU nuance in the comment: the just-created parent was link-free-checked and is a real dir).
- [x] **Docs sync** — update the sandbox.rs module doc's Perf/syscall paragraph (:11-58) for the ladder's new syscalls (the link-free walk on the creation path, one canonicalize for the ancestor check, one metadata per `.coding` write in the protection check), `is_protected_write_target`'s protected list (hardlink rule + residual), and PLAN.md:120-135, PLAN.md:438, PLAN.md:490-505, docs/FEATURES.md:22 so the three new rules are stated there. No marketing language — match each file's existing register; keep the reviewer's documentation-sync check in mind.
- [x] **Full verification** — run `cargo test` UNPIPED, then read `$LASTEXITCODE` (never trust a piped exit code, per agent.md): it must be green with the five new tests passing (the 8.3 one either passing or printing its visible SKIP marker) and, since src/lib.rs:5 is `#![deny(warnings)]`, warning-free without any `#[allow(...)]`. Confirm the pre-fix→post-fix evidence is recorded (step 2's failures vs now-green) and that no existing test regressed (especially the sandbox protection suite, research-write dispatch tests, and the file tool tests). Then hand off to the closing sequence (reviewer → findings → commit).

## Bug
Three pre-existing bypasses of Sandbox's path-prefix write protection (found in review rounds 2-4 of plan ee65fd4b, 2027-01-11; BUG memory d7ebe9f9, backlog 1a5bffcf). (1) A hardlink inside .coding/ to a protected file (`.coding/x.toml` hardlinked to `.coding/safety.toml`) is not protected by name — canonicalize cannot see a hardlink (no link to follow; the path IS the file) — so any plan kind can truncate the shared inode; `.coding/x.rs` hardlinked to `src/lib.rs` does the same outside-in. (2) A Win32 8.3 spelling (`.coding/KNOWLE~1/new/x.md`) reads as ordinary — aliases are not symlinks, so the per-component case/trailing-dot trim cannot see them — and validate_for_write's create_dir_all resolves the alias and plants a new subtree inside the real `.coding/knowledge/` (overwriting an EXISTING protected file stays impossible: an alias of an existing file canonicalizes to the long name). (3) validate_for_write's step-5 revalidation failure returns the LEXICAL path (`Err(_) => Ok(validated)`), so a write through a link that escapes the root lands outside it — reachable for implementation/bug_fixing plans (the research route was closed by plan ee65fd4b).

## Regression test
protected_hardlink_inside_coding_refused
