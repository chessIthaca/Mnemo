# Plan: Run-all lane memory-message clarity (worktree path + gist markers)

## Goal
Close the three run-all lane memory-message clarity gaps so dispatched (possibly lesser-reasoning) models get actionable messages: (Me4) a worktree lane agent's memory_write result explicitly says the knowledge record landed in the MAIN project tree when the agent's root differs from the store's root; (gist truncation) a recall gist cut at 300 chars ends with "..." so truncation is visible; (gist path pointer) a recall hit's file path (data.rel_path, when present) rides as a fourth field on the "- [tier] title — gist" line so every hit is actionable without raising the gist budget.

## Kind
implementation

## Context
Backlog 0bba3241, remediating LOW 4 + suggestions 2-3 of .coding/reviews/2026-09-08-full-review-memory.md (and proposal 5 of the model-pipeline review). Message-format changes only — no store or ranking behavior changes. Verified facts: (1) MemoryWriteTool (src/tool/memory/mod.rs:59-93) holds store/now/knowledge: Option<KnowledgeSupport>; the knowledge-backed success branch (:154-182) reports " — knowledge record at .coding/knowledge/{rel} (files are the truth; the memory row is a budgeted digest)" at :167-170. KnowledgeStore::dir() (src/memory/knowledge.rs:534) exposes the main-tree .coding/knowledge dir → project root = dir().parent().parent(). (2) Factory: register_memory_tools(&self, registry) (def factory.rs:1140, sole call site :846) does NOT receive the root spec, but root: Option<&AgentRootSpec> IS in scope at the call site (:842/:843/:847 pass it to siblings); AgentRootSpec (factory.rs:107-117) has project_root: PathBuf = the worktree root. Factory-wide main-tree knowledge store for all agents is by design (shared corpus). (3) format_recalled_context (src-tauri/src/ipc/run_all.rs:321-344, RECALL_BLOCK_HITS=5, RECALL_BLOCK_GIST_CHARS=300) flattens control chars 1:1 then takes 300 chars with no truncation marker; hit line is "\n- [tier] title — gist". Knowledge-backed Memory rows carry their file path in data.rel_path (src/memory/indexer/knowledge.rs:398-402: json!({"kind": "knowledge", "rel_path": record.rel_path, ...})); other sources' rows and DB-only rows have no rel_path (data Null/absent). Existing test: format_recalled_context_formats_hits_with_a_caveat (run_all.rs:977-1034). Existing knowledge-write test asserting "knowledge record at .coding/knowledge/": src/tool/memory/mod.rs:1129. (4) Two independent cargo packages: root (mnemo lib) + src-tauri (mnemo-app bin) — verify must run cargo test in BOTH.

## Steps
- [x] 1. **Me4: memory_write names the MAIN tree for lane agents** — (a) src/tool/memory/mod.rs: add field `agent_root: Option<PathBuf>` to MemoryWriteTool (None in both `new` and `with_knowledge`) plus a public builder `with_agent_root(mut self, root: Option<PathBuf>) -> Self` with a doc comment (the sandbox root of the agent this tool serves, when it differs from the knowledge store's main-tree root — run-all lane agents). (b) Same file, knowledge-backed success branch: after the " — knowledge record at .coding/knowledge/{rel} (files are the truth; the memory row is a budgeted digest)" push (:167-170), when `self.agent_root` is Some and differs from the knowledge store's project root — `support.knowledge.dir().parent().and_then(Path::parent)` — append one sentence: " — landed in the MAIN project tree, not this worktree (this lane's file tools are worktree-rooted and cannot read that path)". Import std::path::Path if not already imported. (c) src/agent/factory.rs: give register_memory_tools (def :1140) a `root: Option<&AgentRootSpec>` param; update the sole call site (:846) to pass `root` (in scope there); in the mk_write closure apply `.with_agent_root(root.map(|r| r.project_root.clone()))` to the with_knowledge construction (the plain-DB arm never reports a path, so the line cannot fire there). (d) Tests in src/tool/memory/mod.rs's tests module (near the existing knowledge-backed test at :1129): a lane write — MemoryWriteTool::with_knowledge(store, KnowledgeStore rooted at <tmp>/.coding/knowledge, plans_dir).with_agent_root(Some(<different tmp root>)) → typed write → result contains "MAIN project tree"; controls: with_agent_root(None) and with_agent_root(Some(<tmp>)) (equal to the store root) → result does NOT contain "MAIN project tree".
- [x] 2. **Gist ellipsis + path pointer in format_recalled_context** — (a) src-tauri/src/ipc/run_all.rs:321-344: in the hit loop, first flatten control chars into a `flat` String (1:1 mapping — char count unchanged), compute `truncated = flat.chars().count() > RECALL_BLOCK_GIST_CHARS`, take the budget into `gist`, and push "..." onto the gist when truncated; then read `hit.memory.data.get("rel_path").and_then(|v| v.as_str())` and emit "\n- [tier] title — gist — path" when present, else the existing three-field "\n- [tier] title — gist". (b) Update the doc comments to match: RECALL_BLOCK_GIST_CHARS (:311-313 — the gist is cut at the budget with a trailing "..."; the hit line carries the record's file path when it has one) and format_recalled_context (:316-320 — each hit as `- [tier] title — gist[...] [— path]`). (c) Extend the test format_recalled_context_formats_hits_with_a_caveat (:977-1034): add a hit whose content exceeds 300 chars → its line's gist ends with "..."; add a hit with data = json!({"kind": "knowledge", "rel_path": ".coding/knowledge/spec/x.md"}) → its line shows " — .coding/knowledge/spec/x.md"; keep the existing assertions (three-field shape for hits without rel_path, hit cap, gist cap).
- [x] 3. **Verify: both crates green** — Run `cargo test` at the repo root (mnemo lib — covers the memory tool + factory tests) AND `cargo test` in src-tauri (mnemo-app bin — covers the run_all tests); check $LASTEXITCODE unpiped each time (PowerShell). Both must pass warning-free under deny(warnings). Then the closing sequence (reviewer → fix findings → commit) runs automatically.
