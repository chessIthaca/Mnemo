# Plan: Plan-only default tools + subagent cleanup on Complete→Planning

## Goal
(1) All right-panel tools start disabled except the Plan tool; right panel starts open on Plan. (2) Transitioning from Complete into Planning (new plan) removes all subagents — backend cleanup + their tabs disappear from the top bar.

## Context
Frontend store defaults: rightPanelVisible: false, rightPanelTab: "plan", disabledTabs: [] (useAgentStore.ts ~533-545). ALL_RIGHT_PANEL_TABS = md|plan|diff|output|files|safety|stats|backlog. Requirement 1: all tools disabled EXCEPT plan at startup, and the right panel open on the plan tab. Requirement 2: when workflow transitions Complete→Planning (create_plan from Complete state clears the finished stack — workflow.rs), clean up all old subagents: the backend should remove subagent agents (AgentManager::remove / send Cancel?) so the top bar no longer shows stale subagent tabs. Need to check: how subagents are removed today (AgentManager.remove, AgentHandle, is there an 'exited'/'removed' event to the frontend?), where the Complete→Planning transition happens (workflow.rs create_plan_from_complete_clears_finished_stack), and how to hook subagent cleanup into it (probably in the runtime/commands layer, since workflow.rs is per-agent core logic without access to the AgentManager). Frontend: agents record entries for removed agents should be dropped so tabs disappear.

## Steps
- [x] 1. Explore: how workflow Complete→Planning works (workflow.rs create_plan_from_complete_clears_finished_stack), how the backend knows the active agent's workflow transition, AgentManager remove/Cancel mechanics, and what events reach the frontend for agent removal (exited?). Decide where subagent cleanup hooks in.
- [x] 2. Frontend defaults: disabledTabs = all tabs except 'plan'; rightPanelVisible = true; rightPanelTab stays 'plan'. Update any docs/comments describing the defaults.
- [ ] 3. Backend: on Complete→Planning transition, remove all agents that have a parent (subagents) from the AgentManager and stop their tasks; emit an event (or reuse 'exited') so the frontend removes their tabs.
- [ ] 4. Frontend: handle subagent removal — when an agent is removed, drop its entry from agents/agentNames/agentParents/workflowStates and if it was the active agent, fall back to the main agent.
- [ ] 5. Run cargo test + tsc --noEmit; fix anything broken.
- [ ] 6. Closing sequence: reviewer reviews ALL changes, fix ALL findings (never defer — iterate until a clean round), commit to feat/reviewer-report-instructions.
