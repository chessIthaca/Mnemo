# Plan: Shell and browser tool calls always render on their own transcript line

## Goal
Shell and browser-family tool calls never merge into a grouped transcript card — every shell command and every browser action renders on its own line (backlog daa38cbe).

## Kind
implementation

## Context
Investigation pinned the merge site: reduceToolCallStart in frontend/src/hooks/agentEventReducer.ts:516-555 — canMerge (535-540) merges consecutive same-name calls into one transcript entry (cap MAX_CALLS_PER_TOOL_CARD=50 at line 89), covering both sequential and parallel batches; Message.tsx:365 renders each entry as one ToolCard. Shell tool name is exactly "shell" (src/tool/agent/shell.rs:161). Browser family = browser_* + offscreen_browser_*; the prefix check is duplicated in frontend/src/lib/toolCardPaths.ts (browserArgLabel:403-405, browserResultInfo:504-505) — export it as shared isBrowserToolName instead of adding a third copy. Grouping tests live in frontend/src/hooks/useAgentStore.test.ts:285-340 (sequential merge + cap-split, style: handleAgentEvent + agent(ID).transcript.filter). Backlog item daa38cbe-3a69-4380-bd5f-967b9cf43b8b. Frontend-only change; Rust untouched.

## Steps
- [x] 1. **Shared browser-family predicate** — In frontend/src/lib/toolCardPaths.ts: export isBrowserToolName(toolName: string): boolean (startsWith "browser_" || startsWith "offscreen_browser_") with a doc comment covering both families; refactor browserArgLabel (lines ~403-405) and browserResultInfo (~504-505) to call it, deleting the duplicated prefix checks.
- [x] 2. **Never-merge policy in the reducer** — In frontend/src/hooks/agentEventReducer.ts reduceToolCallStart (~516-555): add `const neverGroups = event.name === "shell" || isBrowserToolName(event.name);` and extend canMerge with `!neverGroups`. Update the comment block (516-522) to document the policy: shell commands and browser actions always get their own card — the command/target is the point of the card and grouped chips bury it (backlog daa38cbe). Import isBrowserToolName from ../../lib/toolCardPaths.
- [x] 3. **Regression tests** — In frontend/src/hooks/useAgentStore.test.ts next to the existing grouping tests (~285-340): (a) shell sequential — tool_call_start(shell) → tool_result ok → tool_call_start(shell) → expect 2 tool entries, 1 call each (fails without the fix: currently 1 entry, 2 calls); (b) shell parallel batch — two tool_call_start(shell) with no intervening result → 2 entries; (c) browser_navigate → result → browser_navigate → 2 entries; (d) offscreen_browser_navigate → result → offscreen_browser_navigate → 2 entries; (e) existing search-merge tests stay green (unaffected tools still merge). If a toolCardPaths test file exists, add isBrowserToolName unit cases (browser_navigate true, offscreen_browser_navigate true, search false).
- [x] 4. **Verify** — npm test --prefix frontend — full suite green (read the Tests summary lines). cargo test — full suite green per the project rule (change is frontend-only; the suite is the gate; test result: lines are the evidence, not piped exit codes).
- [x] 5. **Docs + close** — Check README/doc comments for tool-card grouping mentions (search README.md for tool card/grouping); update if the behavior is documented anywhere. Then the closing sequence: spawn role:reviewer over all uncommitted changes → fix every finding → re-run tests → commit on wt/agenticcoder (include the review report) → finish. Close backlog item daa38cbe-3a69-4380-bd5f-967b9cf43b8b done. Document decisions in the final summary.
