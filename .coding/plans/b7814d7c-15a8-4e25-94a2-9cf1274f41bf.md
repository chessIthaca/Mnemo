# Plan: Fix crash on clear conversation mid-stream

## Goal
Stop the app from crashing when the user clears the conversation while the agent is mid-stream.

## Context
Reviewer (2026-04-04-clear-midstream-crash-review.md) found: (1) HIGH — clearConversation doesn't clear pendingApproval, the exact crash vector; (2) MEDIUM — fire-and-forget interrupt leaves a race where the rAF text buffer repopulates streamingText after clear; (3) LOW — test doesn't seed an approval / not executed (pre-existing no-runner); (4) LOW — interrupt() lacks a doc comment (constitution); (5) INFO — /load has the same class of bug. Fixing all: clear pendingApproval+streamingReasoning+activityLog in clearConversation; drain the rAF buffer via a module-level clearStreamingBuffer exposed from useAgentEvents; apply interrupt-when-running to /load; improve the test to seed+assert pendingApproval cleared; add doc comment to interrupt.

## Steps
- [x] 1. Fix the `/clear` slash command in InputBar.tsx to interrupt the backend stream before clearing the frontend store when the agent is running.
- [x] 2. Add a doc comment to clearConversation in useAgentStore.ts documenting that callers must interrupt the backend first when clearing mid-stream.
- [x] 3. Add a regression test in useAgentStore.test.ts asserting clearConversation wipes transcript/streamingText/steers.
- [x] 4. Run cargo test + tsc + vite build to confirm green.
- [x] 5. Fix the `/clear` slash command in InputBar.tsx to interrupt the backend stream before clearing the frontend store when the agent is running.
- [x] 6. Add a doc comment to clearConversation in useAgentStore.ts documenting that callers must interrupt the backend first when clearing mid-stream.
- [x] 7. Add a regression test in useAgentStore.test.ts asserting clearConversation wipes transcript/streamingText/steers.
- [x] 8. Run cargo test + tsc + vite build to confirm green.
- [x] 9. Spawn a read-only reviewer subagent to review all uncommitted changes; act on every finding; re-run tests.
- [x] 10. REVIEW FIX #1 (high): clearConversation must also clear pendingApproval (the crash vector) + streamingReasoning + activityLog.
- [x] 11. REVIEW FIX #2 (medium): drain the rAF text-delta buffer on clear — expose clearStreamingBuffer from useAgentEvents (module-level buffer) and call it in the /clear handler; justify the residual Tauri-channel race as cosmetic after the pendingApproval fix.
- [x] 12. REVIEW FIX #3 (low): improve the regression test to seed an approval_request and assert pendingApproval===null after clear.
- [x] 13. REVIEW FIX #4 (low, constitution): add a doc comment to interrupt() in lib/tauri.ts.
- [x] 14. REVIEW FIX #5 (info): apply the same interrupt-when-running pattern to /load in InputBar.tsx.
- [x] 15. Re-run cargo test + tsc + vite build after all review fixes.
- [x] 16. Commit to the current feature branch with a clear message.
