# Plan: Minimal fix: synthesize cut tool results + frontend card sweep

## Goal
Make the two backend RED tests green with minimal edits, add the frontend sweep (RED first) + regression tests, warning-free; full suites green.

## Kind
bug_fixing

## Context
ROOT CAUSE (confirmed, RED tests in place): (a) src/agent/turn.rs mid-stream stop block (~line 1253, `if stop_reason.is_some()`): returns BEFORE acc.finalize() + the tool loop, so a tool call already announced to the UI via AgentEvent::ToolCallStart (forwarded at turn.rs:1085) never gets a ToolResult — its card stays "running" forever. The tool loop's own stop paths (turn.rs:1499, 1820) synthesize ToolResult::error("interrupted: not run (turn stopped)") but this earlier path does not. (b) turn.rs bad-JSON retry branch (~1321): pushes Role::Tool error messages into history but emits NO ToolResult events. (c) Frontend: reduceFinished (frontend/src/hooks/agentEventReducer.ts:1163) sweeps no running cards; reduceError:1203 sweeps vision cards only; reduceChildFinished:1266 sweeps nothing.
FAILING TESTS (RED, pre-fix): src/agent/tests.rs `mid_stream_steer_emits_not_run_result_for_announced_tool_call` (panics "the cut tool call must get a synthetic ToolResult") and `bad_json_tool_call_emits_tool_result_event` (panics "a bad-JSON tool call must still emit a ToolResult event").
EVENT SHAPES: AgentEvent::ToolResult { tool_call_id: String, result: crate::tool::ToolResult } — construct failures with crate::tool::ToolResult::error(...) (tool result type is crate::tool::ToolResult, src/tool/mod.rs:76). acc.finalize() consumes acc → returns Vec<ToolCall> { id, name, arguments } (src/provider/stream.rs:83). fanin_tx.send((agent_id, AgentEvent::...)).await. Frontend events: { kind: "tool_call_start", index: number, id: string, name: string }, { kind: "tool_result", tool_call_id, result: { success, output } }, { kind: "finished", reason: "stop" }, { kind: "error", error, retrying }. TranscriptEntry tool: { kind: "tool", name, calls: [{ id, index, args, result: { success, output } | null }] }; memory: { kind: "memory", name, id, index, args, running, success, tier?, title?, snippet?, matched?, hits? }; vision: { kind: "vision", index, total, query, description, running, success }. MEMORY_TOOLS + parseMemoryEntry live in agentEventReducer.ts. Frontend harness: useAgentStore.getState().handleAgentEvent({ agent_id: ID, event: ... }), agent(ID) helper; vitest suites in frontend/src/hooks/useAgentStore.test.ts + agentEventReducer.memory.test.ts. IMPORTANT CONSTRAINT: the mid-stream stop block pushes the partial assistant message with tool_calls: vec![] — do NOT push Role::Tool messages into history there (a message with a tool_call_id but no assistant message carrying tool_calls would be rejected by providers on the next request); the synthetic results are UI-only events.

## Steps
- [ ] 1. **Reproduce with failing regression test** — Write a regression test that reproduces the defect (constitution: every defect gets a regression test that fails without the fix and passes with it). Run it and confirm it FAILS.
- [ ] 2. **Document root cause** — Investigate and document the root cause. memory_write a BUG: record (symptom → root cause → fix + regression test name, ≤600 chars).
- [ ] 3. **Minimal fix via sub-plan** — Push a sub-plan (create_plan — it becomes the active plan and pops back to this one when it completes) whose steps implement the fix with the LEAST amount of change possible; do not refactor unrelated code. Write it for a lesser model to execute alone: every step self-contained (explicit file paths, exact edits, the root cause + failing test name as context).
- [ ] 4. **Verify** — Run the regression test + the full test suite (cargo test unpiped, warning-free). Record the regression test name via update_plan (regression_test field) — finish is blocked without it.

## Bug
read_files tool card never leaves "running" and keeps the amber running border after a mid-stream steer lands — announced tool call gets no ToolResult
