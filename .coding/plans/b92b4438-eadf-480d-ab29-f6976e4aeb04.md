# Plan: Execute deferred review items from CONSOLIDATED.md

## Goal
Execute every actionable deferred item from CONSOLIDATED.md: StatsView refresh-split + unified error handling, StatusBar failure surfacing + dropdown a11y, setCode*Color consolidation, CodeBlock copy fix, memory stats field docs, correction trigger tightening, set_model reorder + default_model fallback + blank-line nit, and a production CSP with a dev-mode devCsp.

## Context
CONSOLIDATED.md (from the earlier review round) lists 14 deferred items. This plan executes the actionable ones; the note-only ones (ipc#2 cosmetic spawn race, ipc#7 eprintln-vs-log consistency, rust#6 deliberate fire-and-forget stats) get documented as reviewed-no-change.

Current state verified: StatsView.refresh() re-fetches project stats + full session list on EVERY tokenUsage bump and double-fetches on agent switch (fe#2/#3); error handling mixes silent-empty with a banner (fe#1). StatusBar selectModel/selectReasoningEffort only console.error on set_model failure (fe#4); model/effort dropdowns are mouse-only, no Escape/aria (fe#5). useAgentStore has 7 near-identical setCode*Color setters, each calling applyCodeColors with 6 get() calls (fe#6); ConfigDialog.tsx:203-239 uses them via onChange. Message.tsx CodeBlock copy uses String(children) → [object Object] for highlighted code (fe#7). memory/types.rs RequestStats/SessionStats/ModelBreakdown/ProjectStats/DayBreakdown/SessionSummary have sparse field docs (rust#4). correction.rs trigger "summar" (line 58) matches benign "summarize this file" (rust#5); tests require "your summary is too long" to still match. commands.rs set_model builds the provider (incl. env reads) BEFORE checking state.factory (ipc#3) and the model allowlist rejects the endpoint's current default_model after a config edit (ipc#1); get_session_list is missing a blank line before get_config (ipc#6). tauri.conf.json has csp: null (ipc#8); Tauri v2 supports app.security.csp + app.security.devCsp so prod can be strict while dev (Vite HMR on :5179, inline scripts, ws:) stays permissive. No asset-protocol usage in the frontend; pasted images are data: URLs. default_model is Option<String> in src/config/general.rs:44.

## Steps
- [x] 1. StatsView (fe#1/fe#2/fe#3): split refresh into refreshAll (project stats + session list + session stats; runs on mount + activeAgent change) and a real-time session-only effect keyed on tokenUsage prompt/completion/cached that calls only getSessionStats; unify error handling so project/session-list failures set a single error banner while getSessionStats failure remains the legitimate 'no session yet' state; drop the double-fetch on agent switch.
- [x] 2. StatusBar failure surfacing (fe#4): add a modelError state shown inline next to the model button; on setModel failure surface the error and re-sync the store from getConfig() (default provider/model/effort) so the label can't desync; auto-clear after ~4s.
- [x] 3. StatusBar dropdown a11y (fe#5): for the model + reasoning-effort dropdowns add Escape-to-close, aria-haspopup/aria-expanded on triggers, role=menu/menuitem on the lists, and ArrowUp/Down + Enter/Space keyboard navigation via a shared keydown handler.
- [x] 4. useAgentStore setCode*Color consolidation (fe#6): add a single internal setCodeColor(key, value) that builds the color tuple from get() once and calls applyCodeColors; collapse the 7 public setters into one-line delegates so ConfigDialog call sites are untouched.
- [x] 5. Message.tsx CodeBlock copy fix (fe#7): add a ref to the <code> element and copy codeRef.current?.textContent ?? String(children) instead of String(children).
- [x] 6. memory/types.rs field docs (rust#4): add concise field docs to RequestStats, SessionStats, ModelBreakdown, ProjectStats, DayBreakdown, SessionSummary (only the currently undocumented fields).
- [x] 7. correction.rs trigger tightening (rust#5): replace the bare 'summar' trigger with specific phrases ('your summary', 'the summary', 'summary is too', 'summaries should', 'summary should'); add regression tests that 'summarize this file' / 'add a summary field' return None while 'your summary is too long' still matches.
- [x] 8. set_model (ipc#3 + ipc#1 + ipc#6): move the state.factory presence check to the top of the command before any provider build/env reads; treat the endpoint's configured default_model as implicitly allowed in the model allowlist; add the missing blank line between get_session_list and get_config.
- [x] 9. CSP hardening (ipc#8): set app.security.csp (strict: default-src 'self', script-src 'self', style-src 'self' 'unsafe-inline', img-src 'self' data: blob:, connect-src ipc: http://ipc.localhost, object-src 'none', base-uri 'self') and app.security.devCsp (permissive for Vite HMR: adds http://localhost:5179, ws://localhost:5179, 'unsafe-inline' 'unsafe-eval' to script-src; same for connect-src) in tauri.conf.json.
- [x] 10. Verify + document: cargo test (expect 364+ lib tests, now with new correction tests), frontend tsc --noEmit, cargo build -p myharness-app; then update .coding/reviews/CONSOLIDATED.md marking each executed item as fixed and .coding/plans/spawn-agent-completion-feedback.md's follow-up list; note the three reviewed-no-change items (ipc#2, ipc#7, rust#6) and that the CSP needs a manual dev smoke test.
