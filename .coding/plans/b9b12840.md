# Plan: Remove the turn-level repetition guard — identical responses after successful batches must not abort

## Goal
Remove the turn-level repetition guard from src/agent/turn.rs: after the 7f72d3d7 fix its only reachable trip path is identical responses following SUCCESSFUL tool batches, which aborts legitimate re-reads (2027-01-24 12:41 incident). Error loops stay owned by MAX_RETRIES; in-stream loops by the R10 guard.

## Kind
bug_fixing

## Context
Root cause: after the 7f72d3d7 fix, error-following responses reset the ring (src/agent/turn.rs:763-765, keyed on tool_error_count > 0), so the guard's ONLY reachable domain is identical responses following successful batches — and the guard block sits after the no-tool-calls early return, so it only ever sees tool-call responses. The user decided (2027-01-24) that identical responses after successful batches must not abort; relaxing that case leaves no reachable trip path, so the guard is removed outright. Loop defense remains: MAX_RETRIES owns same-error-across-three-interactions (unchanged), the in-stream R10 guard (detect_repetition, src/provider/stream.rs) owns within-response loops (untouched). Removal surface: src/agent/turn.rs sig computation + ring push/trip block (:754-795), recent_response_sigs field (:180) + init (:220), state-struct doc comment (:174-179). Tests: src/agent/tests.rs identical_responses_trip_repetition_guard (:10035-10116) replaced by an inverted regression test; the 7f72d3d7 tests (:11469-11586) stay valid (their negative assertions still hold); harness doc comments (:11419-11436) reworded. Docs: docs/FEATURES.md:54 carries the guard contract clause. Regression-test design: reuse guard_test_harness (:11440) with three identical responses each carrying the incident's two-call successful file_read batch, then Stop — assert all 6 tool results appended, no "repetition guard" error event, finish_reason Stop. At finish: amend BUG memory 121c3316 with the landed outcome (guard removed; MAX_RETRIES interaction accounting unchanged).

## Steps
- [x] 1. **Reproduce with failing regression test** — Write a regression test that reproduces the defect (constitution: every defect gets a regression test that fails without the fix and passes with it). Run it and confirm it FAILS.
- [x] 2. **Document root cause** — Investigate and document the root cause. memory_write a BUG: record (symptom → root cause → fix + regression test name, ≤600 chars).
- [x] 3. **Minimal fix** — Apply the minimal fix that makes the regression test pass. Do not refactor unrelated code.
- [x] 4. **Verify** — Run the regression test + the full test suite (cargo test unpiped, warning-free). Record the regression test name via update_plan (regression_test field) — finish is blocked without it. If the fix turned out FEATURE-SCALE (new pub types or cross-module surface): amend the plan context via update_plan append with a paragraph starting "Landed design —" (architecture, deviations from the sketch, measured numbers, invariants to preserve) and record landed_design=true BEFORE completing this step; at finish write a SPEC memory and amend the BUG record with the landed outcome (agent.md 'Documentation expectations'; the finish gate blocks landed_design without the context amendment).

## Detailed steps
- [x] **Remove the guard from turn.rs** — delete the response_sig computation, ring push/trip block (src/agent/turn.rs:754-795), the recent_response_sigs field (:180) and its init (:220); replace the state-struct doc comment (:174-179) and the guard block's comment with a short note: cross-iteration loop defense is owned by MAX_RETRIES (error interactions) and the in-stream R10 guard.
- [x] **Replace the trip test with the inverted regression test** — in src/agent/tests.rs remove identical_responses_trip_repetition_guard (:10035-10116) and add identical_responses_after_successful_batches_do_not_abort using guard_test_harness: three identical responses, each the same two-call successful file_read batch (mirroring the 2027-01-24 incident), then a Stop finish; assert 6 tool results, no 'repetition guard' error event, finish_reason Stop. Reword the harness/file_read_call doc comments (:11419-11436) that name the guard.
- [x] **Sync docs/FEATURES.md:54** — drop the turn-level repetition-guard clause; keep the MAX_RETRIES interaction semantics (same error across three interactions, same-batch = one interaction) and the in-stream R10 guard mention.
- [x] **Verify** — run cargo test (full suite, unpiped, read $LASTEXITCODE) green + warning-free; then close per the bug_fixing sequence: reviewer subagent, fix findings, re-test, commit to the working branch, finish — amending BUG memory 121c3316 with the landed outcome.

## Bug
Turn aborted with "repetition guard: 3 identical consecutive responses — breaking the tool loop" (src/agent/turn.rs:782) at 2027-01-24 12:41 while every intervening tool batch SUCCEEDED — the model had re-emitted the same two read_files calls (extensions/ShapeTools/AsmModeller.js lines 120-319, extensions/ShapeTools/ShapeTools.js lines 5960-6019) three times; the guard killed the turn before the third batch executed (the UI's ✗ marks on calls #5/#6 are the aborted never-executed batch, not failures).
