# Plan: Commit Sidebar registry fix and merge to main

## Goal
Commit the Sidebar VIEW_BY_ID registry fix + session bookkeeping on a feature branch, review it, then merge to main via the merge_to_main skill.

## Kind
implementation

## Context
Working tree on main holds: frontend/src/components/layout/Sidebar.tsx (VIEW_BY_ID registry fix — replaces hand-copied TOOL_META map that drifted when the game tab was added; already tsc-clean), .coding/plans/stack.json + e2f9a314 plan (bookkeeping updates), and two untracked research-plan records (b0ee3a49, fec83d67). Never commit directly to main — must branch, review, commit, then merge via the merge_to_main skill after the plan closes.

## Steps
- [x] 1. **Branch + verify green** — `git checkout -b feat/sidebar-view-registry` (from main). Run `npx tsc --noEmit` in frontend/ (unpiped) and `cargo test` from the repo root (unpiped, read test result: lines + $LASTEXITCODE) — both must pass.
- [x] 2. **Commit the fix + bookkeeping** — `git add frontend/src/components/layout/Sidebar.tsx .coding/plans/` and commit on feat/sidebar-view-registry with a message like 'fix: derive sidebar tool meta from RIGHT_PANEL_VIEWS registry' (covers Sidebar.tsx + plan bookkeeping + the two research-plan records).
- [x] 3. **Review + act + close** — Spawn a read-only reviewer (role:"reviewer") over the branch diff vs main (`git diff main...HEAD`): the only source change is Sidebar.tsx replacing TOOL_META with a VIEW_BY_ID lookup from RIGHT_PANEL_VIEWS (frontend/src/hooks/rightPanelViews.tsx:60 includes game/Gamepad2); focus = the Map lookup `VIEW_BY_ID.get(tab)!` is safe for every RightPanelTab (registry test covers parity), no unused imports remain, doc comment accurate. Report to .coding/reviews/<date>-sidebar-view-registry-review.md. Do NOT sleep/poll — end the turn after spawning; resume on the notification. Read the report, fix EVERY finding, re-run tsc + cargo test, commit (report included).
