# Plan: Chat visibility for graph + memory + auto-recall cards (show_knowledge_activity, default ON)

## Goal
Add a `show_knowledge_activity` setting (default ON) that makes graph + memory + auto-recall activity cards visible in the chat transcript by default, reusing the existing card components (ToolCard for graph_*, MemoryEntryCard for memory tools + auto-recall). The existing `show_tool_activity` (default off) continues to govern all other tool/vision/skill cards. Also add prompt guidance telling the model not to narrate auto-recalled memories that aren't useful.

## Kind
implementation

## Context


## Steps
- [x] 1. **Backend: show_knowledge_activity setting (default true) — full Rust plumbing + fixture sync (follows the show_tool_activity precedent, plan 0e71e4f9, exactly). (a) src/config/general.rs: in UiConfig add `pub show_knowledge_activity: bool,` (after show_tool_activity at :329) with a doc comment: GUI-only display filter for knowledge-access activity cards (graph_* tool calls, memory tool calls, auto-recall entries) in the chat transcript — visible by default so the user sees graph + memory + auto-recall activity; the transcript store and the model's context echo are unaffected; the Output tab and console still log every tool call; defaults to true. Set `show_knowledge_activity: true,` in Default (after :349). Tests: add `show_knowledge_activity_defaults_true` asserting `cfg.ui.show_knowledge_activity` on an empty config; add `show_knowledge_activity_round_trips` parsing `[ui] show_knowledge_activity = false` and asserting it survives serialize+re-parse; in `save_round_trips_all_sections` add `show_knowledge_activity: false` to the UiConfig literal and `assert!(!reloaded.ui.show_knowledge_activity);`. (b) src/config/patch.rs: add `pub show_knowledge_activity: Option<bool>,` to SettingsPatch (:582 area) + apply block `if let Some(show) = patch.show_knowledge_activity { general.ui.show_knowledge_activity = show; }` (:472 area). (c) src/config/settings_dto.rs: add `pub show_knowledge_activity: Option<bool>,` to SettingsSaveDto (:211 area) + apply block (:514 area). (d) src-tauri/src/ipc/settings.rs: add `pub show_knowledge_activity: bool,` to GetSettingsUi (:544 area) with doc; add populate `show_knowledge_activity: config.general.ui.show_knowledge_activity,` (:737 area); in the two wire-test literals (:1155 and :1254 area) add `show_knowledge_activity: true,`; add `assert_eq!(v["ui"]["show_knowledge_activity"], true);` near :1197. (e) src-tauri/src/ipc/contract_fixtures.rs (:231 area): add `show_knowledge_activity: true,` to the fixture literal. (f) frontend/src/lib/ipc-fixtures/dto-get-settings.json (:54 area): add `"show_knowledge_activity": true,` after show_tool_activity. Run `cargo test` (root) and `cargo test --manifest-path src-tauri/Cargo.toml` unpiped — check test-result lines + $LASTEXITCODE. The contract test asserts the fixture JSON, so both sides MUST change together.**
- [x] 2. **Frontend: wire setting + isKnowledgeActivityEntry predicate + Conversation render filter + ChatSection toggle. (a) frontend/src/lib/tauri.ts: add `show_knowledge_activity: boolean;` to GetSettingsUi (:408 area) with doc; add `show_knowledge_activity?: boolean;` to SettingsSavePatch (:462 area). (b) frontend/src/hooks/useAgentStore.ts: add `showKnowledgeActivity: boolean;` to the store interface (:252 area, mirror showToolActivity) with a doc comment (persisted to config.toml [ui].show_knowledge_activity, default ON — graph + memory + auto-recall cards visible out of the box); add the initial state value `showKnowledgeActivity: true` in the store creation (find where showToolActivity: false is seeded); add `setShowKnowledgeActivity` setter (mirror setShowToolActivity). (c) frontend/src/App.tsx: add hydration `store.setShowKnowledgeActivity(settings.ui.show_knowledge_activity !== false);` near :312 (absent field reads as ON — the default). (d) frontend/src/components/settings/types.ts: add `showKnowledgeActivity: boolean;` to ChatDraft (:378 area). (e) frontend/src/components/settings/sections/ChatSection.tsx: add `showKnowledgeActivity: s.showKnowledgeActivity` to readDraftFromStore (:39 area); in save add `s.setShowKnowledgeActivity(draft.showKnowledgeActivity);` + `show_knowledge_activity: draft.showKnowledgeActivity,` in saveSettings (:80-84 area); add a new checkbox after the show_tool_activity one (~:142): label 'Show knowledge activity in chat (graph, memory, auto-recall)' bound to draft.showKnowledgeActivity. (f) frontend/src/hooks/agentState.ts: add exported `isKnowledgeActivityEntry(entry: TranscriptEntry): boolean` — returns true for `entry.kind === "memory"` (covers memory tool calls + auto-recall entries) OR `entry.kind === "tool" && entry.name.startsWith("graph_")`. Doc comment: the knowledge-access cards gated by show_knowledge_activity (graph_* tool calls, memory tool calls, auto-recall) — a subset of isActivityEntry visible by default so the user sees graph + memory + auto-recall activity without enabling the full show_tool_activity toggle. (g) frontend/src/components/chat/Conversation.tsx: import isKnowledgeActivityEntry; add `const showKnowledgeActivity = useAgentStore((s) => s.showKnowledgeActivity);`; update the render filter at :108 to `showToolActivity || !isActivityEntry(entry) || (showKnowledgeActivity && isKnowledgeActivityEntry(entry)) ? (<Message key={i} entry={entry} />) : null`. Logic: show_tool_activity ON shows everything; OFF + knowledge toggle ON shows knowledge cards (graph/memory/auto-recall) + all conversation entries; both OFF hides all activity cards. Run `cd frontend; npx tsc --noEmit; npm test; npm run build` unpiped.**
- [x] 3. **Prompt: add 'don't comment on useless recalled memories' guidance to TOOL_STRATEGY. In src/agent/prompt.rs TOOL_STRATEGY const (after the memory_search bullet ~:226, before memory_consolidate ~:227), add a new bullet: '- Auto-recalled memories are injected each turn — use them silently; if they are not useful to the current task, do not narrate or apologize for them.' Verify the new text contains NO steering-marker substrings ('is an indexed symbol', 'TIP:', 'SYMBOL NUDGE', 'RECALLED CONTEXT', 'No symbols matched', 'known memory hit', 'consider memory_consolidate') so it never self-fires steering_stats. Add a prompt.rs test pin asserting the stable head contains the new phrase (e.g. 'do not narrate or apologize'). NEVER reorder TOOL_STRATEGY entries (byte-stable head constraint — wording addition only, same position after memory_search). Run `cargo test` unpiped.**
- [x] 4. **Tests + full verification + closing sequence. (a) frontend/src/components/chat/Conversation.test.ts: extend with show_knowledge_activity filter tests — a graph_ tool entry renders when showKnowledgeActivity=true + showToolActivity=false; a memory entry renders; an auto-recall memory entry renders; showKnowledgeActivity=false + showToolActivity=false hides all three; showToolActivity=true renders everything regardless of showKnowledgeActivity; isKnowledgeActivityEntry pure tests (graph_ tool → true, memory entry → true, shell tool → false, vision → false). (b) frontend/src/hooks/useAgentStore.test.ts: add `showKnowledgeActivity: true` to resetStore seed (:43 area). (c) frontend/src/lib/ipc-contract.test.ts: add `showKnowledgeActivity: true` to resetStore seed (:111 area); add `expect(typeof dtoGetSettings.ui.show_knowledge_activity).toBe("boolean");` to the dto-get-settings shape test. (d) frontend/src/components/settings/sections/ChatSection.test.ts: add source pin `expect(chatSource).toContain("show_knowledge_activity: draft.showKnowledgeActivity");`. (e) Run full matrix unpiped: `cargo test` (root), `cargo test --manifest-path src-tauri/Cargo.toml`, `cd frontend; npm test; npm run build` — all green with zero warnings. Then closing sequence: spawn_agent role:'reviewer' (omit model param) over ALL uncommitted changes with a self-contained task (changed files + plan goal; check: show_knowledge_activity default true end-to-end, isKnowledgeActivityEntry covers graph_*/memory/auto-recall, render filter logic correct, prompt guidance added without marker substrings, doc comments, no #[allow], tests cover all three card kinds + toggle off + default). END turn after spawning. When report lands: fix EVERY finding, re-run tests, commit to the current wt/* branch, finish with the report path. Mark backlog 68c4c9a5 done via backlog_status.**
