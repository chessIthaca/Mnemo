# Plan: File-tool reliability: drift/gate fixes, multi-edit + append, memory_amend, constitution, observability

## Goal
Make shell-based file mutation unnecessary: (1) fix the two file_edit reliability bugs — false drift errors (old_string-not-found on verified-identical text, EOL mismatch class) and the stale-read gate freeze (observe_call marker consumption); (2) extend file_edit with atomic multi-edit batches and append mode; (3) add the sanctioned knowledge-amendment tool memory_amend for .coding/knowledge/**; (4) add file_write chunking guidance + append-creates-missing; (5) encode the file-tools-first rule in the compiled SYSTEM constitution (src/agent/prompt.rs stable head) plus agent.md and the reviewer expectations; (6) add per-session shell-vs-file-tool mutation observability surfaced in the Trace panel.

## Kind
implementation

## Context
Exploration findings (2027-01-09 session, branch wt/agenticcoding @ 772b79a):
- Stale-read gate: src/agent/steering_stats.rs should_gate_file_edit (:584) arms when fired>=1 && switched==0. observe_call (:520) consumes last_fired on ANY call and only counts a switch when the immediately-following call names a read tool — the gate's own interception or any interleaved call eats the switch, so a later read never registers and every file_edit is intercepted for the rest of the session (the session-long freeze). Existing test file_edit_redirect_intercepts_after_threshold_and_lifts_after_a_read (src/agent/dispatch.rs:1024) only covers read-immediately-after-fail. Interception site: src/agent/dispatch.rs file_edit_redirect (:892).
- False drift errors: src/tool/agent/file_edit.rs matching path — line-ending helpers (normalize_line_endings/denormalize_literal_newlines/detect_line_ending from line_endings.rs) + fuzzy_whitespace mode left multi-line old_strings failing on verified-identical text when EOLs mismatched (session workaround was EOL-agnostic single-line anchors). Emission-artifact validator (validate_emission_artifacts :178) is separate and stays.
- Truncation: src/agent/turn.rs:2779-2843 — FinishReason::Length mid-arguments already retried with "split into smaller chunks" advice; no app-side argument cap found; fix is tool-side guidance + append-creates-missing.
- Constitution: compiled system prompt stable head = src/agent/prompt.rs build_stable_head (:476), CORE PRINCIPLES file_edit guidance ~:40-43; project constitution = <root>/agent.md loaded by src/project/agent_md.rs (mtime-guarded, takes effect next turn, no restart).
- Knowledge protection: .coding/knowledge/** refuses file_edit/file_write by design; sanctioned writer = KnowledgeStore (src/memory/knowledge.rs); memory_update contract is full-body replacement (digest guard, plan e8565822) — amendment needs its own append-only tool.
- Observability hook: dispatch funnel (src/agent/dispatch.rs execute_tool_call) sees every call; SteeringStats snapshot already IPC-exposed to the Trace panel.
- Project rules: #![deny(warnings)] (zero-warning build), regression test per defect (fails without fix), cargo unpiped reading test result: lines, all public fns documented.



RESUME HARDENING (added 2027-01-09 for post-restart execution):
- Standing user directive: shell-based file mutation is NOT preferred — retry file_edit after a genuine fresh read before any Python/shell fallback; script surgery only for protected paths (.coding/knowledge/** refuses file_edit/file_write by design) or a verified interceptor freeze, with the justification stated. This plan ships the fixes that retire the fallback.
- memory_amend date helpers: format_epoch_date (src/tool/memory/mod.rs:369-382), knowledge_fmt_date (src/memory/indexer/knowledge.rs:671-684).
- Pre-plan test baselines: root crate 2166 unit + 16 integration; src-tauri 293 + 4 + 2; vitest 77 files / 1070 tests; tsc clean; #![deny(warnings)] at both crate roots (src/lib.rs, src-tauri/src/main.rs) — zero-warning build mandatory.
- Restart survival: the plan file (.coding/plans/) and the active-plan pointer (.coding/plans/stack.json, local) survive an app restart; workflow state resumes as Executing at step 1. On resume: re-read this context, write the RED tests FIRST (step 1) before any fix, run cargo unpiped reading test result: lines.

## Steps
- [x] 1. **Reproduce both bugs as RED tests** — Two RED test suites that fail on current code. (a) False drift, in src/tool/agent/file_edit.rs tests: multi-line old_string with CRLF file + LF old_string; LF file + CRLF old_string; fuzzy_whitespace=true with EOL mismatch; mixed-EOL file — assert the edit applies (current code fails with old_string-not-found). (b) Gate freeze, in src/agent/steering_stats.rs + src/agent/dispatch.rs tests: sequence drift-fail → one interleaved non-read tool call → read_files → file_edit — assert NOT intercepted (current code intercepts: observe_call consumed the pending marker on the interleaved call so the read never counted as a switch). Also assert a successful file_edit does not clear fired (current behavior) to pin the reset semantics change.
- [x] 2. **Fix matching: EOL-agnostic in all string modes** — In src/tool/agent/file_edit.rs: normalize line endings on BOTH sides (file content and old_string) before matching in literal mode AND fuzzy_whitespace mode (regex/line-range modes unaffected); splice new_string into the original content using the file's detected ending (existing detect_line_ending + denormalize_literal_newlines from line_endings.rs); keep the boundary-marker restoration path intact. All step-1(a) tests GREEN; existing file_edit tests stay green.
- [x] 3. **Fix the gate: per-file drift state, read clears, success resets** — In src/agent/steering_stats.rs + src/agent/dispatch.rs: stop relying on the last_fired chain for the EditStaleRead gate — record reads directly (the dispatch funnel already knows the tool name; a read of path P clears P's drift state). Make drift state per-(agent, path): HashMap keyed by (AgentId, PathBuf) instead of the per-agent array slot; should_gate_file_edit takes the path; a successful file_edit on P resets P's fired count; the interception error names P (file_edit_redirect already formats the path). Keep the global marker telemetry (counts/snapshot) unchanged. Step-1(b) tests GREEN; existing gate tests updated to the new per-file semantics.
- [x] 4. **file_edit multi-edit mode (atomic batch)** — Extend FileEditArgs (src/tool/agent/file_edit.rs) with edits: Option<Vec<EditItem>> where EditItem = {old_string, new_string, count?, fuzzy_whitespace?}. Validate ALL anchors first: each old_string must match exactly once in the current content unless count/replace_all is given — any failure aborts with NO write, error names the anchor index and the first line of its old_string. Apply sequentially on the in-memory content, run validate_emission_artifacts once on the final content, write once, return one combined diff. Mutually exclusive with single-edit fields (old_string/new_string at top level) and with line-range/append modes. Tests: happy path (3 anchors), anchor-2-fails leaves file byte-identical, ambiguous anchor rejected, combined diff renders.
- [x] 5. **file_edit append mode** — Add append: bool to FileEditArgs (src/tool/agent/file_edit.rs): new_string is appended at EOF prefixed by the file's detected line ending; no old_string needed; error when the file is missing (steer to file_write). Mutually exclusive with edits/line-range/old_string. Tests: append happy path (EOL correct), missing-file error, diff renders as pure addition.
- [x] 6. **file_write chunk guidance + append-creates-missing** — (a) file_write schema description (src/tool/agent/file_write.rs): state the chunking strategy up front — content above ~4KB should be written as a first mode:"overwrite" call followed by mode:"append" calls per chunk (matches the turn.rs retry advice). (b) Verify mode:"append" creates the file when missing; if not, add it. Tests: append-creates-missing; schema text contains the chunking guidance.
- [x] 7. **memory_amend: sanctioned knowledge amendment** — New tool memory_amend (src/tool/memory/, registered in src/agent/factory.rs + tool filter + prompt capabilities list): args {id, paragraph}. Appends a dated amendment paragraph to the knowledge file via KnowledgeStore (src/memory/knowledge.rs — the sanctioned writer, bypassing the .coding/knowledge/** file-tool protection by design), using the existing date-formatting helper; then reindexes and updates the derived row. Refuses unknown ids with the file list hint. Append-only by construction so the digest guard cannot fire. Tests: amend appends + row digest updates; unknown id errors; file_edit/file_write on .coding/knowledge/** still refuse (unchanged protection).
- [x] 8. **Constitution: system prompt + agent.md + reviewer check** — (a) src/agent/prompt.rs stable head, CORE PRINCIPLES beside the existing file_edit guidance (~:40-43): add the rule — mutate files only through file_edit/file_write (memory_amend for .coding/knowledge/**); shell-based file mutation is a last resort requiring a stated justification; retry file_edit after a fresh read before falling back. (b) agent.md (project root): new "File mutation policy" section with the same rule + a Review-expectations bullet (reviewer flags unjustified shell file-mutation). (c) prompt.rs head test asserting the line is present in build_stable_head output. Note: stable-head change invalidates the prompt cache at next session start — intended, ship it.
- [x] 9. **Observability: shell-vs-file-tool mutation counter** — In the dispatch funnel (src/agent/dispatch.rs execute_tool_call): classify each tool call — file-tool mutation (file_edit, file_write, memory_amend) vs shell-mutation (shell tool whose command matches file-mutation patterns: Set-Content, Out-File, Add-Content, '>>', sed -i, tee, python with a script file or -c writing files) vs other. Per-session counters exposed via the SteeringStats snapshot (new fields, IPC-exposed) so the Trace panel can render file-tool vs shell mutation counts. Tests: classification unit tests (mutating vs non-mutating shell commands, e.g. Get-Content/cargo test are NOT mutations).
- [x] 10. **Docs + full verification** — Sync docs: README tool-mode lists if they exist, module doc comments for the new modes/tools, agent.md final read-through. Full verification: cargo test (root) and cargo test (src-tauri) unpiped — redirect to files, read test result: lines, $LASTEXITCODE check; vitest frontend suite; tsc --noEmit clean. All green before spawning the reviewer.
