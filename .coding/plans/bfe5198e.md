# Plan: Boot-window reconcile progress (486955d5 completion)

## Goal
Close backlog 486955d5's remaining acceptance gap: the startup memory-index reconciliation (the one open/create phase that can exceed ~1s with no live feedback) renders inside the BootSplash — "Building memory index N/M" with the SplashProgress bar fed by the existing memory://reconcile events — while the already-shipped layering (static splash, BootSplash, phase-aware buttons, IndexingOverlay) keeps covering the rest of the flow.

## Kind
implementation

## Context
CONTINUATION of backlog 486955d5 (the item re-dispatched with its full text after plan 969510fc's missed auto-flip). Plan 969510fc already landed the layering (commits 18de87a + 0bb658a, round-3 PASS): the static pre-React splash kills the restart white-flash, the BootSplash early return covers the pre-index boot phases, and the phase-aware picker buttons replaced the bare "Working…". PHASE MAP against the full item's acceptance ("every open/create phase >1s shows a progress bar with phase text"): (a) create_project's own work — MET (the scaffold/register are <1s; the phase buttons give the text; the seed index has the overlay with counts); (b) the restart window — MET (the static splash + BootSplash); (c) pre-index phases — the ONE remaining gap: the startup derived-index reconciliation (src-tauri/src/main.rs:1246-1400, inside build_brain) can run for SECONDS on a drifted memory DB (git merge landed, records edited, memory.db deleted) and the BootSplash shows only a generic spinner — the events ALREADY EXIST (memory://reconcile: ReconcileEvent started/progress{done,total}/done{summary}/failed{error}, memory_maintenance.rs:48-56) and App ALREADY tracks them (App.tsx:107-112 the `reconcile` state: {phase:"running",done,total} | {phase:"done",summary} | {phase:"failed",error} | null, fed by the App.tsx:126 onReconcileEvent listener) — but the BootSplash early return suppresses the reconcile dialog during the boot window, so nothing renders; (d) the walk phase before total — ADEQUATE (IndexingOverlay:198-202 already renders label "Indexing files…" + an indeterminate "…" count; a pre-walk event is not worth the surface — SKIPPED, documented here). FIX (render-only, no new backend surface, no new events — so the item's "new event emission covered by tests" clause is N/A): BootSplash gains an optional `reconcile` prop (a BootReconcile type mirroring App's state shape exactly); when running it renders "Building memory index" + SplashProgress (from ./SplashCard — the same component IndexingOverlay uses; API: label, right, pct — pct null = indeterminate) with right = total > 0 ? `${done}/${total}` : "…" and pct = total > 0 ? Math.round((done/total)*100) : null; done → "Memory index ready"; failed → "Memory index check failed" (the error detail stays in the post-boot reconcile dialog — App's done/failed states leave that dialog visible until dismissed); absent → the current spinner + "Starting…". App's early return becomes `<BootSplash reconcile={reconcile} />`. TIMING NOTE (why live listening suffices, no snapshot needed): the reconcile runs INSIDE build_brain which blocks the setup hook until IpcState is managed, so the frontend's boot checks cannot resolve before the reconcile finishes — the BootSplash window always overlaps a slow reconcile, and React mounts while it streams (the webview loads in parallel from main.rs:253); a fast reconcile needs no bar (<1s acceptance). VERIFICATION: cd frontend; npm test; npx tsc --noEmit. Tests: BootSplash.test.tsx gains the reconcile renders (running with counts, running indeterminate total=0, done, failed, the null fallback) via renderToStaticMarkup, and the existing App source-contract block gains the assertion that the early return passes the prop (`return <BootSplash reconcile={reconcile} />;`).

## Steps
- [x] 1. **BootSplash renders the reconcile phase** — frontend/src/components/common/BootSplash.tsx: add the exported `BootReconcile` type ({ phase: "running"; done: number; total: number } | { phase: "done"; summary: string } | { phase: "failed"; error: string }) + an optional `reconcile?: BootReconcile | null` prop. Rendering: when reconcile?.phase === "running" — the wordmark, then "Building memory index" + <SplashProgress label="Reconciling memories…" right={total > 0 ? `${done}/${total}` : "…"} pct={total > 0 ? Math.round((done/total)*100) : null} /> (import SplashProgress from "./SplashCard"; the spinner swaps out — the bar is the activity indicator); when done — "Memory index ready" under the wordmark (keep the spinner); when failed — "Memory index check failed"; when absent/null — the current spinner + "Starting…" unchanged. Update the doc comment (the boot window overlaps a slow reconcile because build_brain blocks the setup hook; the post-boot reconcile dialog still carries the done summary / failed error). In frontend/src/App.tsx, change the early return to `if (!checkedStartup) { return <BootSplash reconcile={reconcile} />; }`.
- [x] 2. **Tests — the reconcile renders + the App wiring** — frontend/src/components/common/BootSplash.test.tsx: add cases via renderToStaticMarkup — (1) reconcile running with counts: the markup contains "Building memory index" and "3/10"; (2) running with total=0: indeterminate ("Reconciling memories…" present, no "0/0"); (3) done: "Memory index ready"; (4) failed: "Memory index check failed"; (5) the null fallback: "Starting…" (the existing assertions). Extend the App source-contract describe block: assert appSource contains `return <BootSplash reconcile={reconcile} />;` (the early return passes the tracked state — a dropped prop silently reverts the boot window to the generic spinner).
- [x] 3. **Verify — suites, tsc, reviewer** — Run `cd frontend; npm test` (all suites green including the extended BootSplash tests) and `npx tsc --noEmit` (clean). Then spawn the read-only reviewer over the uncommitted changes (changed files: frontend/src/components/common/BootSplash.tsx, frontend/src/components/common/BootSplash.test.tsx, frontend/src/App.tsx). The reviewer should check: the prop type mirrors App's state exactly (assignability), the SplashProgress usage matches IndexingOverlay's pattern (pct null = indeterminate), the done/failed texts don't duplicate the post-boot dialog's role, the source-contract assertion, and docs sync (the BootSplash doc comment + the README open/create UX sentence — extend it with the reconcile-in-boot mention if stale).
