# Plan: Tool-description contract hardening: required-field-first ordering, zero-arg contrast, minItems

## Goal
Cut the empty-call failure class at the description-contract level: (1) re-order the seven required-field tools' advertised descriptions so the contract — required-field rule + exact call shape + no-zero-argument rule + recovery rule — is the FIRST sentence and the value proposition follows; (2) add the content-first anti-pattern clause ("if you catch yourself emitting <tool> with no args, stop — write the content first, then the call"); (3) add an explicit "Takes NO arguments — call it with {}" note to the four genuinely zero-arg tools (current_plan, backlog_list, list_models, skill_reload) so they contrast sharply with required-field tools; (4) add "minItems": 1 to the read_files files array property in the advertised schema.

## Kind
implementation

## Context
Failure evidence (user report 2027-01-17, deepseek sessions): empty argument-object calls cluster on required-field tools, and the live read_files description still leads with the value proposition ("Read files — one or many, in one call instead of N round-trips. Always pass `files` …") before the required-field rule — the exact ordering that invites the empty call. Prior art: read_files collapse (plan 9e0b266a, backlog 26cdbaf8) and the evidence-based empty-call sweep (plan e02134b2, backlog d9ad618e, commit a44d1c8) put rule + example + recovery hint on seven tools.

VERIFIED ANCHORS:
- read_files: src/tool/agent/read_files.rs — description ~:161-191, invalid-args hint :236-239, empty-array runtime check :242-244, description/error tests :526-531.
- shell: src/tool/agent/shell.rs — description :392-418, hint :465, tests :1136-1162.
- graph_search/graph_context/graph_path: src/tool/agent/codegraph.rs — descriptions :271-281, :417-426, :591-597; hints :309, :619, :647; tests :724.
- git_read: src/tool/agent/git_read_tool.rs — description :81 area, hint :118, tests :243.
- memory_write: src/tool/memory/mod.rs — description :127-136, hint :158, tests :1404.
- Zero-arg tools: list_models src/tool/agent/list_models.rs:59 ("No parameters."), current_plan src/tool/workflow/plan.rs:2213, backlog_list src/tool/workflow/backlog.rs:480, skill_reload src/tool/workflow/skill.rs (grep its description site — "Re-read .coding/skills"; takes no arguments by design, plan 8a95a233).

CONSTRAINTS: description tests assert .contains substrings (reorder-safe), but new assertions must pin the LEADING position of the contract sentence, the zero-arg notes, and minItems. Descriptions ride in every request — keep additions tight and watch agent::factory::tests::tools_array_stays_within_context_budget (the prior sweep raised ceilings per the documented measured+headroom pattern; reordering is net-free, new clauses are small additions). minItems is ADVERTISEMENT-level (honest pre-call signaling); the runtime empty-array check at read_files.rs:242 stays. No behavior change beyond description text, schema JSON, and tests.

VERIFICATION: cargo test unpiped at repo root (read $LASTEXITCODE), cargo test in src-tauri/, both warning-free under #![deny(warnings)].

## Steps
- [x] 1. **Reorder the seven required-field descriptions — contract first** — Per tool, move the existing contract text (required-field rule + e.g. example + no-zero-argument rule + rewrite-don't-resend recovery) to the FRONT of the description and demote the value proposition after it, keeping every existing clause verbatim where possible (net char delta ~0). Append one compact anti-pattern clause per tool: 'If you catch yourself emitting <tool> with no args, stop — write the <content> first, then the call.' Files: src/tool/agent/read_files.rs (~:161-191), src/tool/agent/shell.rs (~:392-418), src/tool/agent/codegraph.rs (three tools: ~:271-281, ~:417-426, ~:591-597), src/tool/agent/git_read_tool.rs (~:81), src/tool/memory/mod.rs (~:127-136). Keep descriptions tight — the context budget test must stay green.
- [x] 2. **Zero-arg tools: explicit 'takes NO arguments' note** — Add 'Takes NO arguments — call it with {}.' as the leading/early sentence of the description for current_plan (src/tool/workflow/plan.rs:2213 area), backlog_list (src/tool/workflow/backlog.rs:480 area), list_models (src/tool/agent/list_models.rs:59 area — replace/extend the bare 'No parameters.'), and skill_reload (src/tool/workflow/skill.rs — grep its description site first). Keep each note to one sentence.
- [x] 3. **Schema hardening: minItems on the read_files files array** — In src/tool/agent/read_files.rs schema() (~:161-191): add "minItems": 1 to the files array property (advertisement-level; the runtime empty-array check at :242-244 stays). Survey the other six tools' array params (grep for '"type": "array"' in the seven files) and add minItems only where an empty array is already a runtime error — do not invent new validation.
- [x] 4. **Tests: pin the new contract positions + notes + minItems** — Extend the per-tool description tests (read_files.rs:526-531, shell.rs:1136-1162, codegraph.rs:724, git_read_tool.rs:243, memory/mod.rs:1404): assert the contract sentence LEADS the description (e.g. description starts with 'Always pass' / 'REQUIRES'), the anti-pattern clause, and — for the four zero-arg tools — the 'Takes NO arguments' note (new tests where none exist). Assert read_files' schema carries minItems == 1 on files. All tests must fail without the change and pass with it.
- [ ] 5. **Closing sequence: tests, review, commit, finish + SPEC amend** — Run cargo test unpiped at repo root ($LASTEXITCODE) + cargo test in src-tauri/ — green and warning-free; watch tools_array_stays_within_context_budget (raise the ceiling only per the documented measured+headroom pattern if needed). Then reviewer subagent → fix findings → re-run tests → commit to the working branch → finish. At finish: amend SPEC 50242afc (empty-call sweep) via memory_amend with the contract-first convention. Files under test: src/tool/agent/read_files.rs, src/tool/agent/shell.rs, src/tool/agent/codegraph.rs, src/tool/agent/git_read_tool.rs, src/tool/memory/mod.rs, src/tool/agent/list_models.rs, src/tool/workflow/plan.rs, src/tool/workflow/backlog.rs, src/tool/workflow/skill.rs.
