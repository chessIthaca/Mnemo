# Plan: Freeze fixes — F1 git-branch spawn_blocking, F3 transcript caps, F1b slower poll, F2 trace-file cap, F6 forwarder prune

## Goal
Fix all five freeze/slowdown findings from the 2026-04-19 diagnosis: the blocking git-branch subprocess (freeze), the unbounded frontend transcript/activityLog (progressive slowdown), the 5s branch poll, the unbounded traces.jsonl, and the forwarder prev_workflow_state leak. Every fix gets a regression test per the project constitution.

## Kind
implementation

## Context
Implements the fix plan in .coding/reviews/2026-04-19-freeze-diagnosis.md (memory id 51b8e87f has the summary). User restarted the app before this plan executed — the resumed session starts here. PARENT CONTEXT: the CodeGraph plan (5/9, paused at 'graph_* agent tools') has UNCOMMITTED work in the tree (modified Cargo.toml/lock, src-tauri/src/main.rs, src/agent/factory.rs, src/config/general.rs, src/lib.rs, src/project/mod.rs, src/tool/agent/mod.rs + untracked src/codegraph/, src/tool/agent/codegraph.rs, plan files, the diagnosis report). The git tool's commit stages ALL changes (git add -A), so this plan's closing commit will include that CodeGraph WIP — say so in the commit message ('includes in-flight CodeGraph WIP from the parent plan'). Constraints: Windows 11 + PowerShell; never pipe commands to check exit codes (run unpiped, read $LASTEXITCODE); builds are warning-free under #![deny(warnings)] — no #[allow(...)]; no wait-timer patterns. After the last step the standard closing sequence runs: cargo test → reviewer subagent → fix findings → commit to the current feature branch (never main) → finish.

## Steps
- [x] 1. **F1 — get_git_branch off the async runtime** (the freeze fix). In src-tauri/src/ipc/files.rs:287-311: extract a sync helper `fn read_git_branch(root: &std::path::Path) -> String` that builds the `git rev-parse --abbrev-ref HEAD` command (keep the existing Windows CREATE_NO_WINDOW block) and returns trimmed stdout or "no-branch" on any failure. Rewrite the `get_git_branch` command to: `let root = state.project.root.lock().await.root.clone();` then `tokio::task::spawn_blocking(move || read_git_branch(&root)).await.map_err(|e| format!("git branch task failed: {e}"))` — the project lock is dropped BEFORE the blocking subprocess runs (mirrors src-tauri/src/ipc/run_all.rs:213-216). Regression test (#[cfg(test)] mod in files.rs): create a tempdir, `git init` + `git checkout -b fix-test` via std::process::Command, assert read_git_branch returns "fix-test"; assert a non-repo tempdir returns "no-branch". Run `cargo test` unpiped.
- [x] 2. **F3 — cap frontend transcript + activityLog** (the slowdown fix). In frontend/src/hooks/agentState.ts add exported consts `MAX_TRANSCRIPT_ENTRIES = 1000` and `MAX_ACTIVITY_ENTRIES = 300` plus cap helpers (`entries.slice(-N)` pattern, mirroring the existing toolOutputLog `.slice(-50)` at agentEventReducer.ts:856). Apply at EVERY mutation of the two arrays — grep `transcript` and `activityLog` in frontend/src/hooks/{agentState.ts,agentEventReducer.ts,useAgentStore.ts} to find them all: pushTranscriptEntry (agentState.ts:337-340), reduceReasoningDelta (agentEventReducer.ts:148-167), the error spread (:699), the child-finished spread (:737), and the useAgentStore batch path (:835-888). Tradeoff to note in a comment: saved-conversation exports keep only the last 1000 entries. Add vitest tests (existing suites live under frontend/src/hooks/ — follow their pattern): pushing past each cap keeps exactly the last N entries, oldest-first order preserved. Run `npm test` in frontend/.
- [x] 3. **F1b — slow the branch poll + refresh on turn end**. In frontend/src/App.tsx:307-325: change the interval from 5000 to 60_000, keep the focus listener, and add a turn-end refresh: subscribe to the main agent's `running` flag via the useAgentStore selector and call the same refresh() when it transitions true→false (turn resolved — covers agent-run git commits/checkouts). Verify nothing else depends on 5s freshness (grep getGitBranch — only frontend/src/lib/tauri.ts:890 defines it). If a frontend test covers this hook-up exists, extend it; otherwise verify by `npm test` staying green.
- [x] 4. **F2 — cap traces.jsonl**. In src/provider/trace.rs add `const MAX_TRACE_FILE_BYTES: u64 = 8 * 1024 * 1024;` near MAX_RECORDS (:44). In write_records_to_file (:700-759): before the merge, check `std::fs::metadata(path).map(|m| m.len()).unwrap_or(0)` — if it exceeds the cap, skip reading existing lines entirely (start fresh; the 32-record ring is rewritten on every batch anyway, so nothing live is lost). Regression test next to the existing trace tests (e.g. `raw_response_cap_truncates_and_flags` at :893): write a >8MB junk file, call write_records_to_file with one record, assert the result is < cap and contains only that record's id. Run `cargo test` unpiped.
- [x] 5. **F6 — prune prev_workflow_state on agent Exited**. In src-tauri/src/ipc/events.rs: in the `SerializableAgentEvent::Exited` arm of the forwarder (the arm that removes the agent from manager + agent_loops + notified_children, ~:322-325) add `prev_workflow_state.remove(&agent_id);` alongside the other removals. If an existing forwarder/latch test seam can observe this map, extend it; if not (the forwarder needs a Tauri AppHandle), state that justification explicitly in the commit message per the constitution's regression-test rule.
- [x] 6. **Verify the whole fix set**. Run `cargo test` unpiped (confirm `test result: ok` and zero warnings — #![deny(warnings)] means any warning fails) and `npm test` in frontend/. Fix any failure before completing this step; do NOT mark steps complete with failing tests.
