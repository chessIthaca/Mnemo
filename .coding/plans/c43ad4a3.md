# Plan: Cache-hit round 6: diagnose poor hits from the enabled trace log (traces + request_stats)

## Goal
Diagnose why cache hits are poor in the current trace data: build a round-6 extractor over the newly-enabled `.coding/logs/traces.jsonl` (13 records, full request bodies) plus the always-on `request_stats` and `provider-errors.jsonl`, localize byte-exactly where each request pair's prefix cache breaks (system head / tools array / a mutated earlier message / pure append / over-cliff / model switch / cold start), attribute each miss bucket a measured share, and deliver `.coding/analysis/cache-hit-6-report.md` with a ranked, executable fix list — then hand the source fixes off as a separate follow-up implementation plan.

## Kind
implementation

## Context
WHY: the user enabled the opt-in trace log and reports "so many poor cache hits" — round 6 of the cache-hit line. The deliverable is PERSISTED ANALYSIS ARTIFACTS (`.coding/analysis/cache-hit-6-extract.py`, `cache-hit-6-aggregates.txt`, `cache-hit-6-report.md`), which is why this plan is kind=implementation and not research: a research plan runs `ToolFilter::ExecutingResearch` (src/tool/mod.rs:204-218) = Executing MINUS file_edit/file_write/file_append/convert_line_endings, so it cannot write its own deliverables by construction. NO `src/` SOURCE CHANGES happen in this plan — the ranked fixes it produces get a SEPARATE follow-up implementation plan.

INSTRUMENTS (verified):
- `.coding/logs/traces.jsonl` — now READ: 13 records, 1 session, models ['deepseek-v4.1-flash', 'glm-5.3-flash'], providers from the record set; top-level keys = ['backoff_ms','base_url','cancelled','compact_ms','connect_ms','error','finish_reason','generation_ms','guard_cut','http_status','id','is_complete','model','prep_ms','provider','raw_tool_calls','reasoning_ms','request_json','request_truncated','response_evicted','response_raw','response_truncated','stall_ms','tools_ms','ts_ms','ttft_ms','usage','version'] — timestamp field is `ts_ms` (NOT ts), usage is nested under `usage`, and `request_json` carries the FULL body (messages + tools), which is what makes byte-exact prefix-break localization possible. 13 records is a small sample: treat them as ground truth for REAL provider cached_tokens and for the break mechanism, and `request_stats` as the statistical backbone.
- `.coding/memory.db` `request_stats` — always-on; round 5 read 29,759 rows / 677 sessions / 18 models, window 2026-08-10..09-10 (epoch clock, shifted vs displayed dates). Columns: model, session_id, prompt_tokens, completion_tokens, reasoning_tokens, cached_tokens, ttft_ms, generation_ms, created_at, outcome, purpose. Read-only: `sqlite3.connect("file:.coding/memory.db?mode=ro", uri=True)` + `PRAGMA busy_timeout=3000`.
- `.coding/logs/provider-errors.jsonl` (1,303 B) — 429/fallback evidence.
- Precedent extractor to seed from: `.coding/analysis/cache-hit-5-extract.py` (341 lines; it writes its own aggregates file — the accepted pattern for GENERATED analysis artifacts) and `.coding/analysis/cache-hit-5-aggregates.txt` (347 lines).

BASELINE (round 5): reporting-tier all-time hit 88.6% (n=27,544, resets<50% = 12.5%); post-R12 79.8% (n=14,751, resets 18.1%); NON-reporting models 0.0% BY CONSTRUCTION (2,215 rows: gemini-vlm-gcp, glm-5.3-flash*) and must never be averaged into the headline; per-model spread is the signal (glm-5.2 93.6% … deepseek-v4-flash 18.6%, glm-5.2-maas-gcp 17.5%); avgP 120K-300K, max 541K. NOTE both models in the current trace file (glm-5.3-flash, deepseek-v4.1-flash) sit in or near the non-reporting tier — the extractor must therefore state per-model whether `usage` actually carries a cache field, instead of assuming the heuristic filled it.

ALREADY-FIXED CAUSE (do NOT re-report as new): R14 per-batch tool-result truncation mutating already-sent messages (capped hits ~46-50% at ANY context size) — bug record `.coding/knowledge/bug/2026-12-31-cache-hit-terrible-at-any-context-size-r14-per-b.md`, fixed by hysteresis compaction `compact_old_tool_results(keep=10, keep_high=20, summary_chars=500)` in `src/agent/context.rs` + `src/agent/turn.rs:2105` (token_accounting.reset only when truncated>0). Round 4 also fixed the min(prev,curr) heuristic that masked misses (`src/agent/turn.rs:1163-1173`).

CANDIDATE CAUSES TO RANK BY MEASURED SHARE: cold start per session; model/endpoint switch mid-session (cache is per-model); over-cliff prompts above the proxy's ~340K limit (round-4 observation 99% at 341K → 4.4% at 370K); any REMAINING prefix mutation (found via first-differing-message index); non-reporting models (0% by construction); 429-fallback re-dispatch / cancelled-then-retried turns; prompt-management injections that rewrite the head or the tools array (state switch, memory/steering block, plan rider).

FALSIFIABLE PREDICTION (reuse): prefix identical up to message k ⇒ provider cached_tokens ≈ tokens(messages[0..k]) + tools. Large gap ⇒ OUR mutation; no gap with low cached ⇒ provider/proxy limit.

TOOL NOTE FOR EVERY STEP: file writes go through file_write/file_edit (now available); the ONLY shell-written file is the extractor's own generated aggregates artifact (the round-5 precedent). Shell commands must run UNPIPED so exit codes are trustworthy (agent.md).

ANALYSIS DONE (report `.coding/analysis/cache-hit-6-report.md`, extractor + 250-line aggregates in `.coding/analysis/`). Measured verdict: hits are poor for TWO of our own reasons, both proven from byte-exact trace evidence — (1) the hysteresis gate in `compact_old_tool_results` can never close (60-63 permanently-unmarkable short results keep it > keep_high=20, so the pass fires on EVERY request and rewrites one already-sent tool result ~20 messages from the end; cached == calibrated pre-break prefix, 36-41K tokens re-billed per request; steady state 70-78% where 99.0-99.7% is demonstrably reachable), and (2) the advertised schema differs per plan KIND (`Workflow::schema_filter`), so each plan transition rewrites system+tools and collapses the cache to ~6K (7/24 window requests at ~5% hit, TTFT 15.7-17.9 s). SCOPE AMENDMENT: the plan said "hand the fixes off as a separate plan"; the user asked to fix it, so R6-1 (the contained, largest measured win) is implemented HERE, and R6-2 stays a decision item (it reverses a documented intent pinned by its own test) with R6-3 as opportunistic hygiene.

## Steps
- [x] 1. **Build the round-6 extractor over traces + request_stats** — Create `.coding/analysis/cache-hit-6-extract.py` (file_write) by adapting `.coding/analysis/cache-hit-5-extract.py`: keep its read-only `sqlite3.connect("file:.coding/memory.db?mode=ro", uri=True)` + `PRAGMA busy_timeout=3000` open, its R19 reporting/non-reporting tier split and its R21 error/summarize/cancelled segmentation; ADD section A over `.coding/logs/traces.jsonl` — load it line-by-line (`json.loads` per line), print the record count, sorted top-level keys of record 0, the `ts_ms` window, the model/provider sets, and per record: index, id, `ts_ms` formatted UTC, model, provider, len(request_json['messages']), len(request_json['tools']), `usage['prompt_tokens']`, every nested usage key containing 'cach' (recursive walk — the field name differs per provider), system-message length, last-message length, `finish_reason`, `error`, `cancelled`, `guard_cut`. Write everything to `.coding/analysis/cache-hit-6-aggregates.txt` and print the section headers to stdout. Run it unpiped: `python .coding/analysis/cache-hit-6-extract.py`. Report explicitly that 13 trace records is a small sample and that `request_stats` carries the statistics.
- [x] 2. **Localize the prefix break per request pair** — Extend `.coding/analysis/cache-hit-6-extract.py` with section B (prefix-break localizer) over the trace records, in file order and only between consecutive records with the SAME model: diff the two `request_json['messages']` arrays element-wise using a fingerprint of (role, sha1 of canonical-JSON content, content length) to find the first differing index k; classify k==0 as HEAD, k>=min(len) as TOOLS when the `tools` arrays differ else TAIL, otherwise MID (an already-sent message mutated in place — the R14 failure shape); print k, the class, the record's provider cached value, and `predicted = sum(len_content(messages[0..k]))//4` with the delta, marking predicted as an ESTIMATE. Emit a classification count line. For every MID/HEAD/TOOLS finding, also dump the two messages involved (role + first 400 chars of canonical content, both sides) into the aggregates file so the mutation is identifiable without re-running. Re-run unpiped.
- [x] 3. **Attribute every miss to a cause bucket** — Add section C to `.coding/analysis/cache-hit-6-extract.py` over `request_stats`: headline hit% for main-loop rows (outcome=None AND purpose=None) split by the R19 reporting tier (a model is reporting iff ANY of its rows has cached_tokens>0), plus window, row/session counts, and resets<50%; a per-model table (n, hit%, resets, avg/p90/max prompt) for reporting models; a per-session table for the trace session; a prompt-size histogram in 40K bins with hit% per bin (the cliff's MEASURED location, not the assumed 340K); and the miss buckets — exactly one per main-loop row, precedence SWITCH (session's previous row had a different model) > COLD (first row of a session) > NONREPORTING > CLIFF (prompt > 340000) > STEADY — each with n, share_n, miss_tokens, share_of_all_miss, and a reconciliation line totalling the buckets. Re-run unpiped and read its own summary lines.
- [x] 4. **Cross-check the mutation paths in the agent context code** — Cross-check the measured mutations against the code that can rewrite an already-sent message or the stable head — READ ONLY, no edits: `src/agent/context.rs` (`compact_old_tool_results` + its hysteresis window), `src/agent/turn.rs` (~1163 heuristic fallback, ~2105 token_accounting.reset gating, request-assembly site), `src/agent/prompt.rs` (state-switch prompt swap), the steering/memory injection site, the compaction/summarize path, the plan-rider injection, and the model/endpoint + 429-fallback selection. For each measured bucket decide INTENDED (cold start, model switch — quantify the floor), AVOIDABLE (dynamic content that could move LATER in message order to keep the prefix byte-stable), or BUG (in-place mutation of already-sent messages / an unnecessary head or tools rewrite), naming the exact function + line for every AVOIDABLE/BUG. Record the verdicts as notes in `.coding/analysis/cache-hit-6-aggregates.txt` section D so the report and a restarting session both see them.
- [x] 5. **Write the round-6 findings report** — Write `.coding/analysis/cache-hit-6-report.md` (file_write): (1) headline table round-5 → round-6 (reporting-tier hit%, resets, avg/max prompt, request counts) with the non-reporting tier stated separately and the sample sizes labelled; (2) cause ranking — every bucket with measured share + the evidence that produced it (session ids, record indices, section-B class counts) and the explicit outcome of the falsifiable prediction (confirmed/refuted); (3) the cliff's measured location; (4) the ranked fix list — per fix: file + function + line, what changes, expected hit-% win derived from the bucket share, risk, and a proposed regression-test name; each item executable by a weaker model from the text alone; (5) 'what is NOT the cause' (the R14 shape and the heuristic inflation are already fixed) so the next round does not re-litigate them. Label every number measured vs estimated.
- [x] 6. **Hand off R6-2/R6-3 and record the findings** — R6-1 is implemented inside this plan: the gate now measures the TRUNCATABLE population (`tool_result_is_truncatable` helper + the gate in `src/agent/context.rs::compact_old_tool_results`), guarded by `agent::context::tests::short_results_do_not_hold_the_gate_open` (verified to fail with the fix neutered: left 2 / right 0). Write the BUG memory (permanently-open gate → a sent tool result rewritten every request; evidence `.coding/analysis/cache-hit-6-report.md`). Then hand off the two remaining items to the user in the final reply — R6-2 schema stability across plan kinds (`src/workflow/mod.rs::schema_filter` + the test `schema_filter_research_plan_uses_executing_research`; a deliberate-intent reversal needing their call) and R6-3 trace-writer partial last line — and OFFER backlog entries without queueing them unasked. No further `src/` edits in this step.
