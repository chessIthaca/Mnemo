# Plan: Decompose run_turn into phase methods (quality review HIGH 1 + LOW 7)

## Goal
Decompose run_turn (src/agent/turn.rs) into named phase methods on AgentLoop, leaving a ~100-line orchestrator, and name the swap magic numbers (SWAP_SUMMARIZE_FILL, KEEP_RECENT_ON_SWAP) — pure code motion, behavior pinned by the 303KB test suite.

## Kind
implementation

## Context
Quality review HIGH 1 + LOW 7 (.coding/reviews/2027-01-06-full-review-quality-coverage.md:29-35, :105): run_turn is a single 2,120-line pub async fn (92% of turn.rs, ~10-level nesting — the plan-completion memory write sits at 40-space indent) mixing ≥5 phases; magic numbers 0.8 (:79 swap-summarize fill) and 6 (:92 keep-recent) duplicate ContextManager fill-rate concepts unexplained. Structural map established by reading the whole file: pending-swap prologue :62-181 (interrupt early-return inside); per-iteration model resolution :303-356 (ModelChanged emission); tool schemas + token accounting :358-399; auto-compaction :401-598 (interrupt early-return, recall-cache invalidation, accounting reset); auto-recall :615-702 (per-turn cache); prompt build/install :704-872 (returns is_local for later pops); provider request select! :873-1034 (sets stop_reason, buffered steers); streaming loop :1036-1286 (Usage stats + re-anchor); post-stream hard-stop recording :1288-1414; stream-error paths :1416-1458; bad-JSON handling :1487-1644 (continue/return outcomes); tool batch :1699-2091 (safe-point drain, execute_tool_call, stop_signal merge, error counting, durable recording, workflow emission :1939-2067 incl. plan-completion memory, hard-stop not-run synthesis); post-batch :2093-2161. complete_with_retry (dispatch.rs:753) returns BoxStream<'a, LlmEvent> (Unpin — no generic pinning needed for consume_stream). Loop-carried state: tool_error_count, bad_json_count, deny_all_latched, stop_reason, compact_announced, token_accounting, last_recalled_user_query/results; stop_signal is always None between batches (taken in the merge) → becomes an execute_tool_batch local. CONSTRAINT: the source-contract test workflow_state_changed_emission_requires_success (turn.rs:2287) greps turn.rs for the marker comment "If this was a workflow tool that succeeded" → "AgentEvent::WorkflowStateChanged" and asserts result.success inside — the comment and gate must move intact with emit_workflow_state (method stays in turn.rs). Editing technique: shell + PowerShell here-string .Replace with Contains guards (file_edit unavailable this session); keep payloads small by splitting each extraction into insert-method + replace-call-site operations. DISCIPLINE: pure code motion — no logic changes, no reordering, comments move with their code; cargo test (2004 tests) green after every step.

## Steps
- [x] 1. **Magic numbers + TurnState + handle_pending_swap** — In src/agent/turn.rs: (a) add module consts near the top — SWAP_SUMMARIZE_FILL: f64 = 0.8 (doc: summarize before a provider swap when the conversation exceeds 80% of the NEW window — the same fill-rate policy as ContextManager's effective_summarize_at, but tighter because the swap is a one-shot window shrink and the regular trigger only re-fires next turn) and KEEP_RECENT_ON_SWAP: usize = 6 (doc: same keep-recent count as the regular auto-compaction path's normal tier; the preflight hard-ceiling tier uses 3); replace the inline 0.8 and 6 in the pending-swap block. (b) Add a private TurnState struct (doc: loop-carried turn state, one instance per run_turn) with fields tool_error_count, bad_json_count, deny_all_latched, stop_reason, compact_announced, token_accounting, last_recalled_user_query, last_recall_results; replace the individual locals in run_turn (stop_signal stays a local — it moves into execute_tool_batch in step 3). (c) Extract the pending-swap prologue (:62-181) into `async fn handle_pending_swap(&self, messages: &mut Vec<Message>, fanin_tx, agent_id, cmd_rx) -> Option<TurnOutcome>` (Some = the interrupt-during-summarization early return; the method completes the swap + emits the switch note); run_turn calls it and returns the outcome if Some. Read the exact current text of :54-181 first; move comments with the code. cargo test green.
- [x] 2. **synthesize_not_run_results + emit_workflow_state** — In src/agent/turn.rs: (a) Extract the not-run synthesis (push Message::tool_result "interrupted: not run (turn stopped)" + emit ToolResult error per call) into a free async fn synthesize_not_run_results(messages, fanin_tx, agent_id, calls: &[ToolCall]) — used at 3 sites: the post-stream hard-stop block (:1376-1399, over `orphaned`), the between-call safe point (:1759-1771, over &tool_calls[i..]), and the post-tool hard_stop (:2076-2088, over &tool_calls[i+1..]). (b) Extract the workflow-state emission + plan-completion memory block (:1939-2067) into `async fn emit_workflow_state(&self, fanin_tx, agent_id, tc: &ToolCall, result: &ToolResult, session_id)` — includes WorkflowStateChanged, StepCompleted, and the Complete-state episodic memory write with dedup. CRITICAL: move the marker comment "If this was a workflow tool that succeeded" and the result.success gate INTACT (the source-contract test workflow_state_changed_emission_requires_success greps for them in turn.rs). cargo test green (the contract test must pass).
- [x] 3. **execute_tool_batch** — In src/agent/turn.rs extract the tool-batch execution (:1699-2091) into `async fn execute_tool_batch(&self, tool_calls: &[ToolCall], messages: &mut Vec<Message>, fanin_tx, agent_id, cmd_rx, session_id, provider: &Arc<dyn LlmClient>, state: &mut TurnState)` — covers: the assistant-message push, tools_start + Phase::RunningTools, the per-tool-call loop (safe-point try_recv drain + fold, hard-stop check → synthesize_not_run_results + break, execute_tool_call, stop_signal merge + hard_stop determination, buffered-command re-injection, tool_error_count tracking with user-denial exclusion, write_review_report path capture, durable tool-event recording, result feedback message + event, emit_workflow_state call, hard_stop → synthesize_not_run_results + break), record_tools_phase_ms, compact_old_tool_results + token_accounting reset. stop_signal becomes a method-local (verified always None between batches). The post-batch stop_reason and MAX_RETRIES checks stay in run_turn. cargo test green.
- [x] 4. **Prep-phase methods: resolve_iteration_provider, maybe_compact, auto_recall** — In src/agent/turn.rs extract three prep-phase methods: (a) `async fn resolve_iteration_provider(&self, default_provider: &Arc<dyn LlmClient>, default_context_manager: &context::ContextManager, fanin_tx, agent_id) -> (Arc<dyn LlmClient>, context::ContextManager)` — the per-iteration model resolution + ModelChanged emission (:303-356). (b) `async fn maybe_compact(&self, state: &mut TurnState, messages, provider: &Arc<dyn LlmClient>, context_manager: &context::ContextManager, fanin_tx, agent_id, cmd_rx, token_count, breakdown) -> Option<TurnOutcome>` — the auto-compaction block (:401-598): trigger check, Phase::Compacting + CompactStarted (once per turn via state.compact_announced), preflight hard-ceiling keep_recent 3-vs-6, summarize_with_interrupt, record_compact_ms, failure note, buffered-command re-injection, accounting reset + re-update + ContextUsage, Compacted event, recall-cache invalidation, interrupt early-return as Some(TurnOutcome). (c) `async fn auto_recall(&self, state: &mut TurnState, messages, fanin_tx, agent_id) -> Option<String>` — the per-turn-cached recall (:615-702) returning the memory context. The else-branch ContextUsage emission stays in run_turn. cargo test green.
- [x] 5. **install_system_messages + request_stream** — In src/agent/turn.rs extract: (a) `async fn install_system_messages(&self, messages, provider: &Arc<dyn LlmClient>, memory_context: Option<&str>, tool_filter) -> bool` — the prompt build (:704-872): stable head + volatile tail + plan nudge + hidden tool groups + session primer (three-state cache) + is_local fold + messages[0] install + tail/footer pushes; returns is_local (run_turn uses it for the post-request pops). (b) `async fn request_stream(&self, messages, provider: &Arc<dyn LlmClient>, tool_schemas, fanin_tx, agent_id, cmd_rx, state: &mut TurnState) -> Result<futures::stream::BoxStream<'_, LlmEvent>>` — record_prep_ms, Phase::Waiting, strip_cross_vendor_reasoning, and the select! loop around complete_with_retry handling Interrupt/Cancel/Compact/Clear/Suggestion/Prompt/CancelSuggestion (buffered_steers local; sets state.stop_reason). The post-return pops, hard-stop early return, and buffered-steer application stay in run_turn. cargo test green.
- [x] 6. **consume_stream + post-stream + bad-JSON methods** — In src/agent/turn.rs extract: (a) `async fn consume_stream(&self, stream: futures::stream::BoxStream<'_, LlmEvent>, provider, context_manager, state, fanin_tx, agent_id, cmd_rx, session_id, breakdown) -> StreamOutcome` — the streaming select! loop (:1036-1286) accumulating acc/text/reasoning_text/finish_reason/had_error/stream_error/stream_phase, Usage stats recording + ContextUsage re-anchor, cmd_rx folds into state.stop_reason; define a private StreamOutcome struct { acc: DeltaAccumulator, text: String, reasoning_text: String, finish_reason: FinishReason, had_error: bool, stream_error: Option<String> }. (b) `async fn record_interrupted_output(...) -> Option<TurnOutcome>` — the post-stream hard-stop block (:1288-1414): orphaned-call sanitization, assistant record + not-run results (via synthesize_not_run_results), Finished, early return. (c) `async fn handle_bad_json(...) -> Option<TurnOutcome>` — the bad-JSON block (:1487-1644): counter, MAX_BAD_JSON_RETRIES abort, sanitize + re-inject + per-call error results + UI cards, steer-pending return; None = continue the loop. The stream-error paths (:1416-1458) and null-turn/no-tool-calls returns stay in run_turn. cargo test green.
- [x] 7. **Final orchestrator + module doc + full verification** — In src/agent/turn.rs: run_turn should now read as a ~100-line orchestrator (Phase::Sending, prep anchor, resolve_iteration_provider, schemas + accounting, maybe_compact/ContextUsage, auto_recall, install_system_messages, request_stream + pops + hard-stop check, consume_stream, record_interrupted_output, error paths, handle_bad_json, null-turn return, assistant push, execute_tool_batch, stop_reason + MAX_RETRIES checks, loop). Update the module doc comment (:5-14) to describe the phase-method structure. Run the FULL verification: cargo test (all 2004+ tests, 0 warnings under #![deny(warnings)]) — the source-contract test workflow_state_changed_emission_requires_success must pass. Fix any compile/borrow issues (compiler-guided; keep bodies textually identical apart from state.* field access).
- [x] 8. **Review, fix findings, commit, finish** — Closing sequence: spawn the read-only reviewer (role:"reviewer", no model param) over all uncommitted changes with a self-contained task (the structural map, the pure-code-motion discipline, the TurnState design, the source-contract constraint, test status). Read the report; fix every finding (re-run cargo test after); commit to the current working branch including the review report; finish(report path).
