# Plan: H3: Memory store read/write connection split

## Goal
Remove the single-connection lock contention in MemoryStore (src/memory/mod.rs) by enabling WAL and splitting reads onto a separate connection from the serialized writer, keeping MemoryStoreTrait and all existing tests intact. Touch only src/memory/mod.rs, src/memory/schema.rs, and possibly Cargo.toml. No commit.

## Context
Finding H3: MemoryStore wraps one rusqlite::Connection in a tokio::Mutex, serializing all memory ops (recall/write/access/stats/sessions/consolidation) across agents. Fix: WAL mode + read/write connection split. In-memory DBs are per-connection, so tests using open_in_memory need a shared-cache URI or single-conn fallback. Another agent is concurrently refactoring src/agent/ — stay strictly within src/memory/.

## Steps
- [x] 1. Read src/memory/mod.rs (all 1211 lines incl. trait + tests) and src/memory/schema.rs; classify each public method as read vs write.
- [x] 2. Implement WAL pragma + read/write connection split (handle in-memory stores via shared-cache URI or fallback), routing each method to the correct connection. Keep MemoryStoreTrait unchanged.
- [x] 3. cargo build + cargo test (unpiped, check $LASTEXITCODE); all ~25 memory tests must pass.
- [x] 4. Report approach, method routing, trait-unchanged confirmation, and test result. No commit.
