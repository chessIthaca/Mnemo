# Plan: Re-tier system prompts: universal rules into compiled prompt, project-specific into agent.md

## Goal
Dedupe and re-tier the system prompts: move universal app-mechanics rules (closing sequence, review process, never-sleep/poll, core-ops-gated, etc.) into the compiled stable head in prompt.rs so every project inherits them; genericize project-specific references (cargo test → the project's tests); trim redundant tool tutorials and merge the overlapping preamble+lifecycle blocks; DRY the summarization prompt; restructure project agent.md to keep only project-specific content.

## Kind
implementation

## Context


## Steps
- [x] 1. **Add universal app-mechanics rules to the compiled stable head (prompt.rs)** — Add a new `APP_RULES` const (byte-stable) wired into `build_stable_head` after WORKFLOW_LIFECYCLE, containing the universal rules currently only in project agent.md: (a) closing sequence for implementation plans — run the project's tests → spawn reviewer (role:"reviewer", read-only, writes report to .coding/reviews/) → fix EVERY finding (never defer) → commit to feature branch (never main) → finish with report path; (b) never sleep/poll for a spawned subagent — end the turn, resume on the finish notification; (c) reviewer reviews ALL uncommitted changes (git status/diff HEAD), not just its own task; (d) core operations (git merge/push) are always approval-gated regardless of safety mode; (e) bookkeeping tools (memory_*, plan-lifecycle) are AutoRun — never prompt; (f) never resend a failed tool call with identical args; (g) final summaries ≤3 short lines; (h) preserve line-ending style of existing files (prefer file_edit targeted over file_write full rewrite). Genericize: say "the project's tests" not "cargo test". Keep it concise (~1500 chars).
- [x] 2. **Merge CODING_SYSTEM_PREAMBLE + WORKFLOW_LIFECYCLE, trim redundancy (E1+E2)** — Consolidate the two consts into one `CODING_SYSTEM_PREAMBLE` block. Drop from the preamble the bullets that duplicate the lifecycle map (research-planning encouragement, sub-plan-at-any-point, tool-gating explanation) — keep those in the lifecycle. Drop the parameter-level tool tutorials the schemas already carry (file_edit use_regex/count/start_line/end_line/fuzzy_whitespace semantics ~600 chars; read_files {path,start_line,max_lines} shape ~150 chars). Keep only strategy: read-before-write, be-precise (exact string matches), prefer file_edit targeted over file_write, use read_files to cut round-trips, chunk large writes with file_append, explain-briefly-then-do-it. Net stable-head reduction ~1000-1700 chars even after adding APP_RULES.
- [x] 3. **Move file_append ~5000-char guidance into the file_append tool schema** — In src/tool/agent/file_append.rs schema(), add to the `content` parameter description: "Keep each call under ~5000 chars; for large files write the first section with file_write then append subsequent sections." so the chunking guidance survives the preamble trim and travels with the tool.
- [x] 4. **Trim workflow_section Executing arm + genericize Reviewing arm (E4)** — In prompt.rs workflow_section Executing arm (lines ~329-347): drop the ~400-char sub-plan/update_plan/abandon_plan lecture (already in the lifecycle + tool schemas); keep CURRENT STEP + PROGRESS + "← resume here" + one line: "Complete the current step before moving on; call complete_step when done." In the Reviewing arm (line ~361): change "re-run cargo test" → "re-run the project's tests".
- [x] 5. **DRY the summarization prompt (E3)** — In src/agent/context.rs build_summary_prompt (lines 329-392): extract the shared 5-section format (Current Task / Key Decisions / Files & Identifiers / Errors & Blockers / Open Items) + the compression instructions (~700 chars duplicated across both branches) into a `const SUMMARY_FORMAT: &str` or a helper fn. Branch only on the intro (running-update vs fresh) + payload placement (previous-summary block vs conversation-turns block).
- [x] 6. **Restructure project agent.md** — Remove the universal rules now in the compiled prompt: the entire "Standard plan closing sequence" section (lines 66-120), the never-sleep/poll bullet, the never-defer-findings rule, the reviewer-read-only/report-path instructions, the core-operations-always-gated bullet, the bookkeeping-tools-never-prompt bullet, the never-resend-failed-tool-call bullet, the final-summaries-≤3-lines rule, the preserve-line-endings bullet, and the never-commit-to-main-except-merge_to_main-skill carve-out. KEEP project-specific content: Environment section (Windows 11, PowerShell syntax, project root C:\AgenticCoder\AgenticCoder, don't-trust-piped-exit-code), Code style (follow existing style, all public fns need doc comments, run `cargo test` before marking a step complete, `#![deny(warnings)]` + never add #[allow] to silence). Add a one-line header note: "Project-specific rules only — universal workflow/review/closing-sequence rules are built into the app's system prompt."
- [x] 7. **Update prompt.rs tests + verify** — Update the prompt.rs test assertions: `preamble_emphasizes_workflow_tool_gating_research_and_subplans` currently asserts the PREAMBLE const has "gates your tools"/"Plan even for research"/"create sub-plans AT ANY POINT" — retarget these to `build_stable_head(&constitution)` (the merged block) so they still pass. `lifecycle_encourages_research_planning_and_subplans` stays as-is (checks the head). Add a test `stable_head_contains_universal_closing_rules` asserting the head contains "reviewer", ".coding/reviews/", "never defer" (or equivalent), and "feature branch". Ensure `stable_head_byte_stable_across_workflow_changes` + `stable_head_excludes_volatile_sections` still pass. Run `cargo test` unpiped, check test-result lines + $LASTEXITCODE, zero warnings.
- [x] 8. **Review + commit** — Spawn a read-only reviewer (role:"reviewer") over ALL uncommitted changes (git diff HEAD): list changed files (src/agent/prompt.rs, src/agent/context.rs, src/tool/agent/file_append.rs, agent.md) + the plan goal (re-tier universal rules into compiled prompt, dedupe, genericize cargo-test, restructure agent.md to project-specific only). Focus: universal rules are now in the compiled stable head (every project inherits them); no universal rule was LOST in the agent.md trim; "cargo test" genericized in compiled prompt but preserved in project agent.md; build warning-free under deny(warnings); no #[allow] added; doc comments on new public fns/consts; cache-stability preserved (stable head byte-stable, CONTEXT_FOOTER intact). Have it write to .coding/reviews/. Read report, fix EVERY finding, re-run cargo test, commit to a new feature branch (never main).
