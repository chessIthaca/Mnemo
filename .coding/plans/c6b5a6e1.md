# Plan: Re-read pointers on truncated tool results (D2)

## Goal
When compact_old_tool_results truncates an old tool result, append a one-line re-read/re-run pointer derived from the originating tool call's arguments (file + line range for read_files; pattern + glob for search/search_read) so the agent can re-read cheaply instead of re-searching; extend the compaction tests to assert the pointer appears and idempotency holds; cargo test --workspace green.

## Kind
implementation

## Context
Backlog a6b3fa57 (D2 from .coding/reviews/2026-09-09-perf-comm-review.md, LOW/optional): compact_old_tool_results (src/agent/context.rs:519-572, called from src/agent/turn.rs:1460 with keep=10, keep_high=20, summary_chars=500) truncates old tool results to the first 500 chars + COMPACTED_MARKER — bounded and idempotent, but the lost content is unrecoverable from context, forcing re-search instead of cheap re-read. DESIGN DECISION: the ToolResult envelope's `data` field (src/tool/mod.rs:56-92) is DROPPED at message construction (src/agent/turn.rs:1400-1406 keeps only result.output), so the pointer derives from the tool-call ARGUMENTS carried in the preceding assistant message's tool_calls (matched by tool_call_id — assistant messages are never truncated by this path, and compaction receives the full &mut [Message] slice). No wire-format/schema changes; function signature unchanged; unmatched id or non-pointer tool → plain marker as today (graceful). Pointer formats (always one line, newline-sanitized): read_files → `[re-read: read_files <path>:<start>-<end>, ...]` from path/start_line/max_lines + files[] (reuse the read_files_paths parsing pattern from src/agent/dispatch.rs:1055-1068; cap 3 specs + " (+N more)"; bare path when no range); search/search_read → `[re-run: <name> pattern="..." glob="..."]` (omit absent glob). Idempotency preserved: the contains(COMPACTED_MARKER) check governs; the pointer rides after the marker and never contains it. Prefix-cache safe: the pointer lands within the same single-pass mutation compaction already performs. The provider ToolCall's arguments field is a JSON string (see turn.rs:1365 usage). Verification: cargo test --workspace run UNPIPED with $LASTEXITCODE read directly (never trust a piped exit code); the extended tests assert the pointer line appears, names file + line range (the acceptance), and a second pass is a byte-identical no-op.

## Steps
- [x] 1. **Add the pointer builder + wire it into compaction** — In src/agent/context.rs: add `fn rerun_pointer(name: &str, arguments: &str) -> Option<String>` — parse the arguments JSON; "read_files" → collect specs from path/start_line/max_lines plus files[] (reuse the read_files_paths parsing pattern from src/agent/dispatch.rs:1055-1068), emit `[re-read: read_files <path>:<start>-<end>, ...]` capped at 3 specs + " (+N more)", bare path when no range; "search"/"search_read" → `[re-run: <name> pattern="..." glob="..."]` (omit absent glob; sanitize newlines/control chars, cap pattern length); other tools → None. In compact_old_tool_results's truncation loop: clone tool_call_id + name before the mutable borrow, scan the slice for the assistant tool_call with matching id, append the pointer line after COMPACTED_MARKER when Some. Update the function doc comment (pointer semantics + the arguments-not-envelope rationale). Signature unchanged.
- [x] 2. **Extend the compaction tests** — In src/agent/context.rs test module (after compact_fires_at_high_water_mark, ~line 1143): (a) compact_appends_reread_pointer_for_read_files — assistant message with tool_calls [{id "1", name "read_files", arguments r#"{"path":"src/foo.rs","start_line":10,"max_lines":50}"#}] + 3 fat tool results (ids 1-3), compact(1, 1, 100) → truncated content contains COMPACTED_MARKER AND `[re-read: read_files src/foo.rs:10-59]` (acceptance: file + line range named); (b) compact_appends_rerun_pointer_for_search — search tool_call with pattern+glob → `[re-run: search pattern="..." glob="..."]`; (c) compact_pointer_keeps_idempotency — second pass returns 0 and content is byte-identical; (d) compact_no_pointer_without_matching_tool_call — unmatched id → plain marker, no pointer line. Existing tests stay green (their ids have no matching assistant tool_calls → no pointer → unchanged expectations).
- [x] 3. **Verify the workspace** — Verify src/agent/context.rs (the changed file) compiles and its tests pass: run `cargo test --workspace` UNPIPED and read $LASTEXITCODE directly plus the `test result:` lines (never trust a piped exit code); all green including the new compaction tests. Fix any failures in src/agent/context.rs before continuing.
- [x] 4. **Docs, memory, commit** — Check README.md and PLAN.md for any compaction/truncation documentation that should mention the pointer (the function doc comment in src/agent/context.rs is the primary doc — update only if a doc actually describes the truncation marker). Write a SPEC memory: truncated tool results now carry re-read pointers derived from the originating tool-call arguments (the ToolResult envelope's data field is dropped at message construction, turn.rs:1400-1406). Commit src/agent/context.rs to the working branch.
