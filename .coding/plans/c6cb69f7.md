# Plan: Fix DeepSeek reasoning_content 400, third recurrence — tail-owner escape (live 2026-09-12)

## Goal
Close the guard hole that let a THIRD DeepSeek thinking-mode 'reasoning_content must be passed back' 400 escape — reproduced first as a failing regression test on the exact failing shape (foreign raw assistant turns + tail-owner issuing trailing tool results), then a minimal serializer fix, a green warning-free matrix, and a BUG memory — or, if a fresh binary provably covers every extracted shape variant, ship a passing coverage test with an explicit stale-binary/session-shape adjudication instead of a fake fix. Live acceptance: a thinking-mode deepseek-v4-flash turn whose tail-owner issued tool calls completes with no 400 and no new provider-errors row.

## Kind
bug_fixing

## Context
BACKLOG ITEM 969bc37d (the user queued it 2026-09-12 from this live failure; this session's task). LIVE EVIDENCE: this agent session died TWICE (~17:35-17:45 EDT 2026-09-12) with the non-retryable DeepSeek 400 — the app surfaced 'SERIALIZATION BUG (not retried)'. Session model: deepseek-v4-flash @ api.deepseek.com, switched mid-session from another model (.coding/logs/emit-diag.log shows the ModelChanged event early in the session). The always-on mirror rows should be the NEWEST entries in .coding/logs/provider-errors.jsonl — extract them in step 1.

PRIOR OCCURRENCES (both fixes are IN the current tree, which is the point: the guard exists, yet the 400 recurred): (1) b23463c / plan c9b5cbe4 — first fix: age-0 raw-echo injection + synthetic-path unconditional key. (2) id 1042, 2026-09-04, plan .coding/plans/1e64d71d.md — hypothesis 'tail-owner turn still escapes the age-0 guarantee'; step 1 [x], steps 2-4 UNCHECKED; forensics doc .coding/analysis/deepseek-1042-shape.txt. Memory 429a3d7b ADJUDICATED id-1042 as a STALE PRE-FIX BINARY — re-test that adjudication, do NOT inherit it: the hole hypothesis was never closed either way, and today's recurrence happened while the fixed shape was already in-tree.

WORKING HYPOTHESIS (evidence-backed, to be confirmed in step 3 not assumed): the tail-owner contract is violated when the conversation carries FOREIGN raw assistant turns — recorded while another provider/model was active, so their raw carries no reasoning_content — and the tail-owner is an assistant turn followed by tool results (not the last message). Candidate holes: age-0 gating catching only 'last message'; sanitize stripping the key after injection (src/provider/mod.rs:551, :790 — obj.remove('reasoning_content')); compaction or the turn builder dropping it (src/agent/context.rs:1040-1060, src/agent/turn.rs:671, :1291, :2973, :3130); policy consult resolving a non-DeepSeek policy at serialization time (src/provider/policy.rs:311 '_ => reasoning_content' default, :413-480 per-model reasoning_field/reasoning_required, :688-691 pre-0.20 conservative default).

IN-TREE FIX SHAPE (present at HEAD — cite these when tracing the hole): src/provider/openai/request.rs:247-250 (policy.reasoning_field == Some('reasoning_content') consult), :478 (m.reasoning_content pass-through), :577 (echo write), :1061-1095 (raw-echo inject branch; unwrap_or_default so '' counts — request.rs:1095). Tests: src/provider/openai/tests.rs:1681-1686 and :1800-1805 ('system messages follow the tool results (provider-errors id 1042 incident shape)'); existing regression raw_echo_injects_reasoning_content_when_required_but_absent at ~1523-1574 — the new test sits BESIDE it in the same module.

REGRESSION-TEST DESIGN (required): assert on the SERIALIZED wire JSON (not the message struct) for the array [... assistant(tool_calls, raw echo, reasoning_content key ABSENT) , tool result(s)] under a DeepSeek thinking policy (reasoning_required, reasoning_field='reasoning_content') with reasoning_effort present; assert the tail-owner message OBJECT carries a reasoning_content key (value may be ''). It must FAIL before the fix; if every plausible variant passes on a FRESH binary, do not fake a fix — ship the passing coverage test, record the adjudication, and conclude stale-binary/session-shape instead.

VERIFICATION COMMANDS (project rules: unpiped, read $LASTEXITCODE; #![deny(warnings)] means a green suite == zero warnings): targeted `cargo test --lib provider::openai`; full `cargo test` and `cargo test -p mnemo-app` from the repo root; live acceptance from backlog 969bc37d — rebuild release (`cd src-tauri; npx tauri build --no-bundle`), relaunch, run a thinking-mode turn whose tail-owner issued tool calls: no 400, no new provider-errors row (relaunching the running app is the USER's call — if I cannot relaunch, state it as the residual).

EXTRACTION DISCIPLINE (hard rule): .coding/logs/provider-errors.jsonl is capped at 8MiB and single rows can be hundreds of KB — PowerShell tail + truncating field extraction ONLY, never dump a row into context (memory 9cc7e643); traces.jsonl is opt-in and per-record capped 256KiB. DATE NOTE: knowledge-file paths carry 2027-01-11 while the machine clock and git say 2026-09-12 (push 7c90111 at 17:29 EDT) — treat 2026-09-12 as today. BRANCH: create_plan forks/reuses the per-directory wt/* working branch; the two dirty .coding/ files (backlog.jsonl with this item, amended SPEC knowledge file) ride along and belong in the eventual commit.

ROOT CAUSE ESTABLISHED (2026-09-12, mid-fix session — steps 1-3 materially done, fix NOT yet written).

FRESHNESS RULED OUT: running release exe built 16:17:48, started 16:17:49 — newer than the fix sources (16:13:15). Failures at 17:31:17 / 17:32:57 (provider-errors ids 104, 112; .coding/logs/traces.jsonl holds exactly these two bodies, request_truncated=false).

FAILING SHAPE (both, verified by parsing request_json): last message = tool result 'skill ended'; tail-owner assistant turn correctly paired (call_4ixlk0w3 / call_rp6pf5ij) AND carries reasoning_content (1295 / 742 chars). The documented 'tail-owner carries the key' contract is SATISFIED — yet DeepSeek returned the 400. THEREFORE the December contract ('only the tail-owner matters; historical turns tolerate missing keys') is INCOMPLETE. That is recurrence #3.

DISCRIMINATOR (unique to both failures): they are the FIRST DeepSeek request after a mid-turn CROSS-VENDOR re-entry. Per ~/.mnemo/config.toml, [models.skill.merge_to_main] = Ollama Cloud / glm-5.3-flash; the session pin is deepseek-v4-flash @ deepseek. During the skill, strip_cross_vendor_reasoning(messages, provider.kind(), provider.model()) (Rule 5, turn.rs:2455, one-way/irreversible) stripped reasoning from every cross-vendor assistant turn; skill_end then re-resolves the provider back to DeepSeek (loop_impl resolve_turn_provider) — so the re-entry request carries a GLM-era tool-call chain whose turns lost reasoning_content. Note skill_end is NOT in the post-state tools array while skill_start IS (skill_end exists only while a skill is active).

CENSUS (id=104: 78 assistants, 72 keyless; id=112: 85/79): EVERY tool-call turn is keyless except the tail-owner; the only other keyed turns are 5 TEXT-ONLY DeepSeek summary turns (reasoning_content native in raw). DeepSeek tool-call raws are natively keyless (same pattern as GLM's); text-only raws carry rc.

CONCLUSION: the builder keys ONLY age 0 (src/provider/openai/request.rs:1080-1083: needs_readd = assistant_age == 0 && reasoning_required && field==Some(reasoning_content) && raw lacks the key). Two candidate API-side readings, BOTH cured by one strict-superset fix: (a) the validator needs the key on every tool-call turn, not just the tail-owner; (b) the undeclared skill_end tool call makes the validator fall back to the previous (keyless) tool-call turn. Fix = key EVERY assistant turn carrying tool_calls under a requires-rc policy. Value: structured reasoning at age 0 (unchanged), "" for age >= 1 (keeps the strip's token savings; December probes established any value incl. "" is accepted). No live API probe was run — the fix is a superset of every candidate reading and the user's live turn is the plan's acceptance step.

FIX TARGET: echo_assistant_raw (request.rs:1070-1098) — extend needs_readd to (assistant_age == 0 || has_tool_calls); thread has_tool_calls from the builder's message loop (~:449-478). REGRESSION TEST beside deepseek_tail_owner_keyed_when_system_follows_tool_results (tests.rs:1722): a DeepSeek-policy body whose chain has TWO tool-call turns whose raws lack reasoning_content (the stripped shape) must serialize BOTH with the key — it FAILS today (the age-1 turn gets none) and passes with the fix. Expect existing retention/tail tests pinning the age-0-only behavior to need updating.

## Steps
- [x] 1. **Reproduce with failing regression test** — Write a regression test that reproduces the defect (constitution: every defect gets a regression test that fails without the fix and passes with it). Run it and confirm it FAILS.
- [x] 2. **Document root cause** — Investigate and document the root cause. memory_write a BUG: record (symptom → root cause → fix + regression test name, ≤600 chars).
- [x] 3. **Minimal fix** — Apply the minimal fix that makes the regression test pass. Do not refactor unrelated code.
- [x] 4. **Verify** — Run the regression test + the full test suite (cargo test unpiped, warning-free). Record the regression test name via update_plan (regression_test field) — finish is blocked without it. If the fix turned out FEATURE-SCALE (new pub types or cross-module surface): amend the plan context via update_plan append with a paragraph starting "Landed design —" (architecture, deviations from the sketch, measured numbers, invariants to preserve) and record landed_design=true BEFORE completing this step; at finish write a SPEC memory and amend the BUG record with the landed outcome (agent.md 'Documentation expectations'; the finish gate blocks landed_design without the context amendment).

## Detailed steps
- [x] **Adjudicate binary freshness + extract the live failure shape (read-only)** — PowerShell from C:/Mnemo, unpiped, truncating only. (a) Freshness: `Get-Item target/release/mnemo-app.exe | Select LastWriteTime` vs the mtimes of src/provider/openai/request.rs, src/provider/policy.rs, src/provider/mod.rs — a release exe OLDER than those sources means the running app predates the in-tree fix shape; a NEWER one means a real in-tree hole. (b) `Get-Content .coding/logs/provider-errors.jsonl -Tail 5` emitting ONLY id, human ts, model, http_status and a 160-char error/message slice — never dump a row (8MiB cap, rows can be hundreds of KB). (c) If .coding/logs/traces.jsonl exists: extract the newest DeepSeek record's TAIL messages as {i, role, has_rc_key, has_tool_calls, tool_results_after} only. (d) Read .coding/analysis/deepseek-1042-shape.txt (id-1042 forensics + adjudication) and the tail of .coding/logs/emit-diag.log for this session's ModelChanged history (which models, in what order). (e) Record ALL findings via update_plan append=true — the plan file is the crash-resumption document and this session has already been killed twice by the bug; a fresh session must not re-derive the extraction.
- [x] **Reproduce with a failing regression test** — In src/provider/openai/tests.rs beside raw_echo_injects_reasoning_content_when_required_but_absent (~1523-1574) add a test constructing the extracted failing shape: message array ending [... assistant(tool_calls, raw echo, reasoning_content key ABSENT), tool result(s)] under a DeepSeek thinking policy (reasoning_required, reasoning_field='reasoning_content') with reasoning_effort present. Assert on the SERIALIZED wire JSON that the tail-owner message object carries a reasoning_content key (value may be ''). Run `cargo test --lib provider::openai::tests::<name>` unpiped and confirm it FAILS. If every plausible shape variant PASSES on a fresh binary: keep a passing coverage test pinning the contract, record the stale-binary/session-shape adjudication in the plan context and a BUG memory, and treat that as the outcome (no fake fix) — then skip to step 5.
- [x] **Document root cause** — With a failing test in hand, trace which guard lets the tail escape, in this order: src/provider/openai/request.rs 201-250 / 478 / 577 / 1061-1095 (age-0 gating + raw-echo injection — check whether 'tail-owner' is resolved as last message only, missing the issuer-of-trailing-tool-results case); src/provider/mod.rs 551 / 790 (sanitize paths that remove reasoning_content — check ordering vs injection); src/agent/context.rs 1040-1060 and src/agent/turn.rs 671 / 1291 / 2973 / 3130 (turn builder / compaction dropping the key after it was set); src/provider/policy.rs 311 / 413-480 / 688-691 (policy consult resolving a non-DeepSeek policy at serialization time). memory_write a BUG: record (symptom -> root cause -> fix + regression-test name). Also record whether .coding/plans/1e64d71d.md's open hypothesis (tail-owner escapes the age-0 guarantee) is CONFIRMED or REFUTED — its steps 2-4 are still unchecked.
- [x] **Minimal fix** — Apply the smallest change that makes the regression test pass — most likely: resolve the tail-owner as the last assistant message OR the issuer of trailing tool results (not merely the last message) and inject the reasoning_content key whenever policy.reasoning_required && policy.reasoning_field == Some('reasoning_content') && key absent; alternative shape: stop a sanitize/compaction path from stripping the key after injection (src/provider/mod.rs:551/:790). No unrelated refactors, no #[allow(...)] (#![deny(warnings)] at both crate roots), existing tests stay green. If the fix grows feature-scale (new pub types, cross-module surface), follow the agent.md 'Landed design' protocol: update_plan append a paragraph starting 'Landed design —' and set landed_design.
- [x] **Verify — full matrix + live acceptance** — Run the targeted test again, then from the repo root `cargo test` and `cargo test -p mnemo-app` UNPIPED reading $LASTEXITCODE and the `test result:` lines (green suite == zero warnings under deny(warnings); a piped exit code is not evidence). Then the live path from backlog 969bc37d: rebuild the release binary (`cd src-tauri; npx tauri build --no-bundle`) so a relaunch picks up the fix, and record the acceptance criterion — a thinking-mode deepseek-v4-flash turn whose tail-owner issued tool calls completes with no 400 and no new provider-errors.jsonl row. Relaunching the app is the user's call: if I cannot relaunch, state it as the residual verification (the user relaunching and retrying is the final step).
- [ ] **Close: review -> commit -> finish** — Spawn role:reviewer (name 'deepseek-400-fix-review', model param OMITTED) with a self-contained task: audit the diff, the regression test (does it exercise the changed path and fail without the fix?), the root-cause documentation, the BUG memory, and the deny(warnings) constraint. Fix every finding (skip only factually wrong ones, justified in writing), re-run the matrix, then commit to the current wt/* branch including the reviewer report, and finish. At closure check backlog item 969bc37d's status and stamp it done if the resolution shipped (memory d899e4f7); leave it in_flight if anything remains open.

## Bug
DeepSeek thinking-mode requests die non-retryably with HTTP 400 'The `reasoning_content` in the thinking mode must be passed back to the API' — THIRD recurrence. Live evidence 2026-09-12 ~17:35-17:45 EDT: the running app killed THIS agent session twice with it (deepseek-v4-flash @ api.deepseek.com), surfaced in the UI as 'SERIALIZATION BUG (not retried): ... The request builder dropped or mutated a reasoning field the provider requires.' Prior history: first fix b23463c (plan c9b5cbe4) — age-0 raw-echo injection + synthetic-path unconditional key; second occurrence id 1042 (2026-09-04, plan 1e64d71d, steps 2-4 still open) — hypothesis 'tail-owner turn still escapes the age-0 guarantee'. Contract to enforce: on DeepSeek thinking requests (reasoning_effort present) the tail-owner assistant turn — last message OR issuer of trailing tool results — must carry the reasoning_content KEY, any value including empty string.

## Regression test
deepseek_keys_every_tool_call_turn_after_cross_vendor_reentry
