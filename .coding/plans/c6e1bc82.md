# Plan: Tool-filter doc fix: stale browser-blocked-in-Reviewing comments

## Goal
Correct the two stale doc comments in src/tool/mod.rs (hidden_groups_for doc ~:744-746 and the load_tools skip comment ~:841-844) that claim the browser family is category-blocked during Reviewing — the actual Reviewing filter arm (:414-421) allows it for the closing sequence. Both comments must match the code; the filter-awareness rationale keeps its still-valid examples (no vision model, CDP off).

## Kind
implementation

## Context
Backlog 117844c5, flagged as an observation by .coding/reviews/2026-09-08-spawn-cancel-event-delivery-landing-review-round2.md (pre-existing, outside that item's delta). Verified: the Reviewing filter arm (src/tool/mod.rs:414-421) ALLOWS ToolCategory::Browser — "Browser tools are available during the closing sequence too (~800 tokens/turn, mitigated by progressive disclosure): verifying a frontend fix live (navigate → screenshot the visible tab) is review-adjacent work, the tools never mutate project files, and mutating ones still prompt via their NeedsApproval gate." Two doc comments still describe an older design where the browser family was category-blocked during Reviewing: (1) the hidden_groups_for doc (:744-746 — "the browser family is blocked by category during Reviewing, so offering it there would cost index tokens (and a load_tools schema) to sell something that cannot be delivered"); (2) the load_tools skip comment (:841-844 — "During Reviewing the browser family is category-blocked anyway, so the loader would be advertising a door that opens onto nothing"). The skip LOGIC at both sites is correct (hidden_groups_for is filter-aware; the load_tools skip fires when no group is loadable) — only the cited example is wrong. The still-valid unavailability examples (no vision model, CDP off) stay. Doc-only change; no behavior changes. src/tool/mod.rs is in the mnemo lib crate (root cargo test).

## Steps
- [x] 1. **Fix the two stale browser/Reviewing comments in src/tool/mod.rs**
- [x] 2. **Fix the two stale browser/Reviewing comments in src/tool/mod.rs** — (a) hidden_groups_for doc (~:740-748): replace the false example "the browser family is blocked by category during Reviewing, so offering it there would cost index tokens (and a `load_tools` schema) to sell something that cannot be delivered" with the general principle (a category the filter disallows in the current state would cost index tokens to sell something undeliverable) plus a corrective note that the browser family IS loadable in Reviewing (the closing sequence allows it for live frontend verification), so it is advertised there like anywhere else. Keep the still-valid examples (no vision model, CDP off). (b) load_tools skip comment (~:841-844): replace "During Reviewing the browser family is category-blocked anyway, so the loader would be advertising a door that opens onto nothing" with the accurate rationale — hidden_groups_for is filter-aware, so when every hidden group's tools are unavailable under the current filter (category-blocked, no vision model, CDP off), the loader would advertise a door that opens onto nothing. No logic changes.
- [x] 3. **Verify: cargo test (lib) green**
- [x] 4. **Verify: cargo test (lib) green** — Run `cargo test` at the repo root (mnemo lib — src/tool/mod.rs compiles warning-free under deny(warnings)); check the `test result:` lines. Then the closing sequence (reviewer → fix findings → commit) runs automatically.
