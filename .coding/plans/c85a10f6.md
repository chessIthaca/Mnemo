# Plan: Verify + close bb54bdcc: Laya foundation already shipped via 9a8b39c

## Goal
Close stale backlog bb54bdcc as already-shipped: cite the fresh green evidence (full suite green earlier this session on this tree), take one cheap row-specific re-run (cargo test classifier + laya) as the acceptance citation, resolve the row done with an audit note, and finish without re-doing any shipped work.

## Kind
research

## Context
Backlog bb54bdcc re-dispatched work that already shipped on this branch (wt/mnemo): commit 9a8b39c "Laya foundation: opt-in Classifier trait + Laya HTTP backend + status + Settings section (backlog bb54bdcc)" — the exact item — plus the follow-up managed-runtime plan d6fc659a (9de7e08..82dd8b6, five review rounds, round-5 PASS, tree clean). The SPEC record's "NOT yet merged to main" was re-verified as still ACCURATE (merge 4cbc3b0 predates the Laya files; the Laya commits sit on wt/mnemo's tip) — landing is the merge_to_main skill's job, orthogonal to item completion; no knowledge amendment needed. Fresh evidence from this session on this exact tree (no source changes since): full root cargo test 2,602 passed / 0 failed / 6 ignored (includes memory::classifier::tests in src/memory/classifier.rs — disabled-path, wire shapes, stub round-trip; config laya_defaults_to_disabled; client_factory build_classifier_*; settings_dto laya_patch_*), frontend tsc --noEmit exit 0, vitest 91 files / 1,273 tests passed (incl. frontend/src/components/settings/sections/ClassifierSection.test.ts, 20 tests). Acceptance criteria all pinned: disabled-by-default unchanged behavior, enabled stub round-trip (laya_classifier_round_trips_a_choice_question), Settings enabled/disabled + endpoint (ClassifierSection). Foundation review's 4 LOW doc-accuracy findings are superseded by the round-5 PASS over the final accumulated surface. User instruction: just close out and move to the next item — no re-doing, no further review archaeology.

## Steps
- [x] 1. **Record the shipped-surface confirmation** — No-code research: evidence already gathered. Commits 9a8b39c (this item) + 9de7e08..82dd8b6 (follow-up d6fc659a, round-5 PASS) reachable from HEAD on wt/mnemo; tree clean except .coding bookkeeping; full suite + tsc + vitest green earlier this session on this identical tree. Record via complete_step.
- [x] 2. **Row-specific green signal** — No-code research: one quick pass — `cargo test classifier; cargo test laya` unpiped with "exit=$LASTEXITCODE" (compiled, seconds) — confirming in src/memory/classifier.rs the acceptance pins: laya_defaults_to_disabled (disabled ⇒ unchanged), laya_classifier_round_trips_a_choice_question (enabled stub round-trip), build_classifier_* (null/Laya backend). Frontend ClassifierSection.test.ts cited from this session's green vitest run (tree unchanged since).
- [x] 3. **Close the row and finish** — No-code research, .coding bookkeeping only: backlog_status bb54bdcc-b692-47f2-9a72-7f0692f9f7c2 → done with an audit note (shipped via 9a8b39c + follow-up d6fc659a round-5 PASS; tests green; unmerged-to-main unchanged — landing via merge_to_main is separate). No knowledge amendment — the SPEC record's not-yet-merged marker is accurate. Then finish the plan so the run moves to the next item.
