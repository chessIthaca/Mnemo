# Plan: Remove the Output right-panel tool tab

## Goal
Remove the right-panel "Output" tool tab and its now-dead toolOutputLog plumbing (state, effects, type, tests, docs).

## Kind
implementation

## Context
Precedent: Safety-tab removal (plan 0afb2240) + Game-tab removal (plan 4b00920f) — remove the union member + registry entry and everything downstream dies. The Output tab is extra: its only data source toolOutputLog exists solely for the removed view, so the whole chain goes. Sites (verified by reading): frontend/src/hooks/agentState.ts:198-230 (union, ALL_RIGHT_PANEL_TABS, ToolOutputEntry), rightPanelViews.tsx:24,53 (import + registry entry; Terminal icon), ToolOutput.tsx (delete file), agentEventReducer.ts:23,48,87,400-412,785-799,933-935 (import, AppStateLike field, Effects field, two entry constructions, dispatcher apply), useAgentStore.ts:86,124,253,542 (import, re-export, AppState field, initial value). Tests touching toolOutputLog: useAgentStore.test.ts:26,368,867; useAgentStore.preview.test.ts:13; ipc-contract.test.ts:94. PLAN.md:472,537 mention ToolOutput in docs. Tab state is session-only (not persisted to localStorage — confirmed in prior reviews), so no migration needed. rightPanelViews.test.ts parity is generic (length-based), stays green with 9 entries. Windows/PowerShell; run cargo test + src-tauri cargo test? No Rust changes — root cargo test suffices per constitution. Frontend: cd frontend; npm test + npm run build (tsc strict will catch unused imports/helpers).

## Steps
- [x] 1. **Branch + prep** — Create/switch to a feature branch (e.g. feat/remove-output-tab) so nothing lands on main. `git status` first to confirm the working tree is clean.
- [x] 2. **Remove the tab id from agentState.ts** — In frontend/src/hooks/agentState.ts: delete the `| "output"` line from the `RightPanelTab` union (~line 201) and the `"output",` entry from `ALL_RIGHT_PANEL_TABS` (~line 214). Delete the `ToolOutputEntry` interface (~lines 224-230).
- [x] 3. **Remove registry entry + delete view** — In frontend/src/hooks/rightPanelViews.tsx: delete the `import { ToolOutput }` line (24), the `{ id: "output", label: "Output", icon: Terminal, component: ToolOutput },` entry (53), and drop `Terminal` from the lucide-react import (18). Delete frontend/src/components/views/ToolOutput.tsx entirely.
- [x] 4. **Strip toolOutputLog from the reducer** — In frontend/src/hooks/agentEventReducer.ts: remove `ToolOutputEntry` from the type import (23); remove `toolOutputLog: ToolOutputEntry[];` from `AppStateLike` (48); remove the `toolOutputEntry?: ToolOutputEntry;` field from `Effects` (86-87); in reduceToolResult delete the toolOutputEntry construction + its explanatory comment (400-412, keep the resolvedPreview/pendingApproval logic above it); in reduceError delete the entry construction (785-790) and simplify the effects to `planVersionBump: !event.retrying ? true : undefined` shape (795-800); remove the dispatcher apply spread (933-935). Then grep `truncateOutput` and `truncateError` — if either helper in agentState.ts now has zero remaining callers, delete it from agentState.ts and any imports.
- [x] 5. **Strip toolOutputLog from the store facade** — In frontend/src/hooks/useAgentStore.ts: remove `ToolOutputEntry` from the type import (86) and the re-export (124); remove the `toolOutputLog: ToolOutputEntry[];` field from AppState (252-253) and `toolOutputLog: [],` from the initial state (542). Then grep `toolOutputLog|ToolOutputEntry` across frontend/src — expect zero non-test hits.
- [x] 6. **Update tests + docs** — frontend/src/hooks/useAgentStore.test.ts (lines ~26, 368, 867): remove `toolOutputLog: []` fixtures and the assertions that read the log; if a whole test exists only to assert log appends, delete it. frontend/src/hooks/useAgentStore.preview.test.ts:13 and frontend/src/lib/ipc-contract.test.ts:94: delete the `toolOutputLog: []` fixture line. Update PLAN.md lines ~472 and ~537 to drop the ToolOutput mentions from the views list.
- [x] 7. **Run tests + build** — cd frontend; npm test (vitest — rightPanelViews.test.ts parity must pass with 9 entries) and npm run build (tsc + vite, catches unused imports). From the project root run cargo test unpiped and check $LASTEXITCODE (no Rust changed; confirms green per constitution).
- [x] 8. **Reviewer subagent** — Spawn a read-only reviewer (spawn_agent role:"reviewer") over ALL uncommitted changes (git diff HEAD): goal = remove the Output right-panel tab + dead toolOutputLog chain; changed files list; focus = no dangling "output"/toolOutputLog/ToolOutputEntry refs, reducer error path still bumps planVersionBump on final errors, parity test holds, no dead helpers left (truncateOutput/truncateError removals justified), docs updated. It writes to .coding/reviews/. End turn after spawning; on notification, read the report, fix EVERY finding, re-run npm test + npm run build + cargo test.
- [x] 9. **Commit** — Commit all changes (including the review report) to the feature branch with a clear message like 'remove Output right-panel tab + dead toolOutputLog plumbing', then call finish with the review report path.
