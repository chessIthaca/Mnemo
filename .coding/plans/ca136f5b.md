# Plan: Add tcp_nodelay(true) to reqwest client — disable Nagle's algorithm

## Goal
Add `.tcp_nodelay(true)` to the reqwest client builder in src/provider/openai.rs to disable Nagle's algorithm — eliminates up to ~200ms of packet-buffering delay per LLM request (the "send" phase in the Trace tab). One-line change, no behavior change beyond faster packet delivery.

## Kind
implementation

## Context


## Steps
- [x] 1. **Add tcp_nodelay to the reqwest client builder** — In src/provider/openai.rs, in the `new_with_trace` method's reqwest::Client::builder() chain (line ~150), add `.tcp_nodelay(true)` after the `.connect_timeout(Self::CONNECT_TIMEOUT)` call. Add a brief comment explaining why: Nagle's algorithm buffers the tail of the request body for up to ~200ms before sending; disabling it (Nagle off) sends packets immediately, reducing the send phase (record creation → response headers) by up to 200ms per request. This is the standard setting for low-latency HTTP clients (curl, browsers, and most HTTP libraries set it by default).
- [x] 2. **Verify build + tests + review + commit + finish** — (1) Run `cargo build --lib` — confirm warning-free under #![deny(warnings)]. (2) Run `cargo test --lib` — confirm zero failures (the change is a builder flag, no logic change; existing tests should pass unchanged). (3) `git status --short` + confirm branch is wt/* (not main). (4) Spawn a read-only reviewer (spawn_agent role:reviewer) over the single-line change: goal = add tcp_nodelay(true) to disable Nagle's algorithm for faster LLM request delivery. Ask for: correctness (tcp_nodelay is the right reqwest builder method; it's set on the shared client so all requests benefit; no interaction with the connect_timeout or gzip/brotli/deflate settings), the TEST-SUCCESS GATE (cargo test --lib has ZERO failures), constitution (warning-free; multi-platform neutral — tcp_nodelay is a cross-platform socket option). END TURN after spawning. (5) Read report, fix EVERY finding, re-run cargo test --lib. (6) Commit to wt/* with a clear message. (7) finish with the report path. Final summary ≤3 lines.
