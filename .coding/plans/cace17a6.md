# Plan: Single-dispatch steer keeps the item in flight (plan⇔backlog sync)

## Goal
Extend the b83e891f intervention contract to single-dispatched items: a steer/interrupt keeps the item InFlight (annotated) and restores the single_in_flight pointer (check-and-set) so the turn that completes the plan resolves it — status stays linked to the plan lifecycle, with Pending untouched (pre-dispatch) and the requeue arm narrowed to the genuinely dead case (run drained before resolution). Sync the contract docs and pin with regression tests.

## Kind
bug_fixing

## Context
Backlog item: none queued — direct user request 2027-01-09 (the user steered item 0296d448 and hit the out-of-sync). Canonical contract: .coding/knowledge/spec/2027-01-07-backlog-item-status-plan-lifecycle-failed-means.md (line 15 lists the single-dispatch intervention requeue as an escape hatch — this plan removes it). Sibling fix: b83e891f / plan 28bc06a2 (run-all arm keeps InFlight, decision .coding/knowledge/decision/2027-01-07-run-all-intervention-pauses-the-item-kept-inflig.md — its "Single-dispatch items still requeue" sentence needs amending).

Exploration at HEAD 50969db (wt/agenticcoding, clean tree):
- handle_user_intervention (src-tauri/src/ipc/run_all.rs:6540-6669): source tracking = latch item_id (run-all halt only) → run current_item → single_in_flight.take() (:6563-6570 — the CONSUME). from_run_all = latch/current_item origin; the single_in_flight fallback leaves from_run_all=false. interrupted_run_active = the run's current_item still matches (identity-guarded stop-flag set; :6590-6613). Disposition match (:6624-6664): InFlight + from_run_all + run active → KEEP InFlight, annotate "{reason}, kept in flight — the plan stays active, resume to continue" (:6625-6642); InFlight otherwise → REQUEUE Pending, note "{note} | {reason}, returned to queue" (:6643-6654); Pending → annotate only (:6655-6659); terminal → leave alone (:6660-6663).
- The completing-turn resolution path is READY for the fix: resolve_single_dispatch_turn (run_all.rs, post-cb43f994 decomposition) takes single_in_flight, resolves via single_dispatch_disposition — done via the plan-loop gate + plan_linkage_allows_done (InFlight + plan_id match — the 5bb1e4cd/e33a07fd guard), non-closure → annotate + CHECK-AND-SET pointer restore (the 96e2862a amplifier fix; the pattern: `let mut slot = ...lock().expect(...); if slot.is_none() { *slot = Some(id); }`, run_all.rs ~:2393-2406). stamp_backlog_in_flight on Executing entry only lifts still-Pending items (idempotent — a kept-InFlight item is untouched).
- single_in_flight is Arc<std::sync::Mutex<Option<String>>> (state.rs:190) — leaf acquisition, never across an await; store (tokio) → std-mutex nesting is the established file-wide discipline.
- Escape hatches that REMAIN after the fix: deliberate deferral (backlog_status in_flight→pending), terminal→pending requeue, main-agent-exit drain (done-orphan guard), run-start adoption (adopt_orphaned_in_flight excludes the live single-dispatch pointer — a kept-InFlight item with a restored pointer is NOT adopted).
- Docs to sync: the spec line 15 (escape-hatch sentence), README.md:79 ("a single-dispatch item returns to the queue"), PLAN.md backlog section, run_all.rs module doc (the intervention/status sections), the b83e891f decision record (append amendment).
- Tests: source-contract pattern (fn_body) for run_all.rs; the b83e891f tests (intervention_keeps_a_run_all_item_in_flight_with_its_run, steer_halt_keeps_the_run_state_for_the_in_flight_item, intervention_handler_is_never_terminal_and_never_continues) pin the run-all arm — find the single-dispatch requeue pin (search "returned to queue" in the run_all tests) and flip it.
- Bookkeeping: item 0296d448 is Pending with the stale steer note while its work shipped (f4a2e6d..50969db) — mark done with a commit-pointing note (the show-tool-activity review class).
- Windows/PowerShell: cargo test UNPIPED, read $LASTEXITCODE; #![deny(warnings)] — no #[allow]; file_edit may freeze (use the established Python-surgery fallback with abort-on-mismatch probes).

## Steps
- [x] 1. **Reproduce with failing regression test** — Write a regression test that reproduces the defect (constitution: every defect gets a regression test that fails without the fix and passes with it). Run it and confirm it FAILS.
- [x] 2. **Document root cause** — Investigate and document the root cause. memory_write a BUG: record (symptom → root cause → fix + regression test name, ≤600 chars).
- [x] 3. **Minimal fix** — Apply the minimal fix that makes the regression test pass. Do not refactor unrelated code.
- [x] 4. **Verify** — Run the regression test + the full test suite (cargo test unpiped, warning-free). Record the regression test name via update_plan (regression_test field) — finish is blocked without it.

## Detailed steps
- **Reproduce with a failing regression test** — In src-tauri/src/ipc/run_all.rs's test module, find the test pinning the single-dispatch intervention requeue (search "returned to queue"; likely named intervention_requeues_single_dispatch or inside intervention_handler_is_never_terminal_and_never_continues). Write/adjust the regression test intervention_keeps_a_single_dispatch_item_in_flight: single-dispatch InFlight item (single_in_flight = Some(id), no run) + intervention → status STAYS InFlight, note annotated "kept in flight — the plan stays active, resume to continue", single_in_flight RESTORED to Some(id). Run it — it must FAIL against the current code (which requeues + consumes the pointer). Also keep/add a drained-run test: run-all InFlight item whose run was drained (no current_item) + intervention → still requeues (the round-1 LOW B recovery).
- **Root cause** — Document in the plan file (this step): handle_user_intervention :6546-6550 (the source-decides-disposition comment), :6563-6570 (the single_in_flight.take() consume), :6643-6654 (the requeue arm) — the b83e891f fix deliberately kept the single-dispatch requeue because the consumed pointer left the queue as the only recovery; with the pointer RESTORED instead, the completing turn's resolution resolves the item through the normal plan-tied rules (done via plan_loop_allows_done + plan_linkage_allows_done; failed only on abandonment; non-closure → annotate + re-restore), so the recovery rationale is void and the requeue is the out-of-sync defect.
- **Fix: keep single-dispatch items InFlight** — In handle_user_intervention: (1) narrow the requeue arm to `BacklogStatus::InFlight if from_run_all && !interrupted_run_active` (drained run — the queue is genuinely the only recovery; keep the "returned to queue" suffix + checkpoint sha); (2) add the single-dispatch keep arm `BacklogStatus::InFlight if !from_run_all` BEFORE it: annotate "{reason}, kept in flight — the plan stays active, resume to continue" (same wording as the run-all arm) and restore the pointer check-and-set under one lock hold (`let mut slot = state.backlog.single_in_flight.lock().expect("single_in_flight lock poisoned"); if slot.is_none() { *slot = Some(id.to_string()); }` — never clobber a concurrent ▶ dispatch, the 96e2862a round-2 pattern); (3) update the source-decides-disposition comment (:6546-6550) and the interrupted_run_active comment tail (:6585-6589) to the new contract. Pending + terminal arms unchanged.
- **Contract doc sync** — Sync the contract docs: (a) .coding/knowledge/spec/2027-01-07-backlog-item-status-plan-lifecycle-failed-means.md line 15 — the escape-hatch sentence drops the single-dispatch intervention requeue (steer/interrupt keeps ANY dispatched item InFlight; the escape hatches are the deliberate backlog_status deferral, terminal→pending requeue, exit drain, run-start adoption) + an amendment line dated 2027-01-09; (b) README.md:79 — "a single-dispatch item returns to the queue" → the item stays in_flight and the completing turn resolves it; (c) PLAN.md backlog section — same delta; (d) run_all.rs module doc — the intervention/status sections; (e) .coding/knowledge/decision/2027-01-07-run-all-intervention-pauses-the-item-kept-inflig.md — append the amendment (single-dispatch now keeps InFlight too, user request 2027-01-09, this plan's id).
- **Verify + closing sequence** — Run the full suites unpiped (root + src-tauri, $LASTEXITCODE); fix failures. Bookkeeping: mark backlog item 0296d448 done with a note pointing at f4a2e6d..50969db (its work shipped; the stale steer note misdescribes it). Then the closing sequence: reviewer subagent → fix findings → re-run tests → commit → finish.

## Bug
Steering or interrupting a SINGLE-DISPATCHED backlog item requeues it to Pending while its plan stays active — the item falls out of sync with the plan lifecycle. Live 2027-01-09: item 0296d448 (the .git sandbox-escape fix) was steered mid-plan; handle_user_intervention requeued it to Pending ("steered by the user, returned to queue") AND consumed the single_in_flight pointer, so the continuation turn that completed plan eda38891 resolved nothing — the item sat Pending with finished work. User model (2027-01-09): as long as the plan is active, the backlog item is active — status linked to the plan lifecycle, pending being only the pre-dispatch exception. The run-all strand of this exact damage class was already fixed (backlog b83e891f, plan 28bc06a2: steer keeps a run-all item InFlight with its stopped run); the single-dispatch strand kept the requeue deliberately ("their pointer is consumed — the queue + auto-feed is their only recovery", decision 2027-01-07-run-all-intervention-pauses-the-item-kept-inflig) — that asymmetry is what this plan removes.

## Regression test
intervention_keeps_a_single_dispatch_item_in_flight
