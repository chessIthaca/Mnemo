# Plan: Recall: semantic fallback on zero keyword hits + embedder-aware sim weight

## Goal
Make memory recall fall through to a capped semantic scan when FTS returns zero keyword matches (instead of returning empty), and re-tune the cosine weight embedder-aware (0.2 for the hash fallback embedder, 0.35 for real embedding models like the default MiniLM) so semantic similarity is no longer understated.

## Kind
implementation

## Context
Backlog 82b831dc (user-approved option 1 + weight re-tune). Defect confirmed in code: src/memory/mod.rs:1322-1327 — FtsResult::NoMatches returns Ok(Vec::new()), so zero keyword hits → empty result and the already-embedded query vector is never used; the Unavailable branch (1328-1332) already has the reusable capped path Self::load_recent_locked(&conn, &filter, FULL_SCAN_FALLBACK_CAP) (cap 200, mod.rs:1299). The deliberate-empty comment at 1294-1298/1323-1325 must be rewritten (rationale rejected: zero-keyword recall is precisely the case embeddings exist for). Compounding factor (out of scope, note only): FTS uses unicode61 without a stemmer (schema.rs:125) so worktree/worktrees are disjoint tokens — the semantic fallback covers such misses too. Scoring: mod.rs:1398 `sim * 0.2 + decayed_strength * 0.3 + keyword_boost(0.3 flat) + tier_weight + class_weight`, with a HashEmbedder-era rationale comment at 1344-1351 — but MiniLM (BundledEmbedder) is now the default and the Embedder trait exposes model_id() ("hash" default, e.g. "all-MiniLM-L6-v2" for bundled; embedder handle is already in scope at mod.rs:1285, model_id() is sync/cheap). Test injection pattern: TinyEmbedder (maintenance.rs:355) with custom dim + model_id. Gotcha (memory 2db4c7f1): recall test fixtures MUST have disjoint vocabularies — any shared token defeats zero-keyword negatives. There may be an existing test asserting the old empty-on-no-match behavior — find and update it.

## Steps
- [x] 1. **NoMatches → capped semantic scan** — in src/memory/mod.rs recall_peek (~1322): change the FtsResult::NoMatches arm from Ok(Vec::new()) to Self::load_recent_locked(&conn, &filter, FULL_SCAN_FALLBACK_CAP) (same call as the Unavailable arm). Rewrite the two stale comments (the cap rationale at ~1294-1298 and the arm comment at ~1323-1325): zero-keyword recall is the case embeddings exist for; the scan is bounded by the existing 200-recent cap; the FTS candidate pool stays the fast path. Do NOT touch the tokenizer/stemmer (schema.rs) — known limitation, covered by this fallback.
- [x] 2. **Embedder-aware sim weight** — in recall_peek's scoring (~1355-1398): compute `let sim_weight = if embedder.model_id() == "hash" { 0.2 } else { 0.35 };` once (embedder is already bound at ~1285; call model_id() before the scoring loop, outside the closure). Use it in the score formula (mod.rs:1398). Rewrite the HashEmbedder-era rationale comment (~1344-1351): hash projection = token-overlap noise, keeps the historic 0.2; real models (bundled MiniLM default, remote) get 0.35 — a strong semantic match (sim ≥ ~0.85) now outweighs a bare keyword hit (0.3), keyword keeps its edge on ties. Keep everything else (keyword 0.3, strength 0.3, tier, class) unchanged.
- [x] 3. **Regression tests** — in the memory test module: (a) find any existing test asserting empty-on-no-keyword-match (search for NoMatches / recall tests with no matches) and update it to the new behavior; (b) add `recall_semantic_fallback_on_zero_keyword_hits`: a deterministic test embedder (TinyEmbedder pattern, custom model_id like "anchor-test") whose embed maps anchor words to orthogonal unit vectors; store 2 memories whose title+content share ZERO tokens with the query (disjoint vocabularies — gotcha 2db4c7f1); assert recall returns the anchor-matching record (fails on current code: empty). (c) add `sim_weight_follows_embedder_model`: same fixture under a "hash" model_id embedder vs the anchor embedder — assert the ordering/relative scores shift as expected (keyword-hit record keeps rank under hash; strong-sim no-keyword record overtakes under the real-model weight). Run: cargo test --lib -- recall_semantic sim_weight (adjust names as implemented).
- [x] 4. **Docs sync** — README: update the memory-search description (semantic search re-ranks keyword candidates AND runs a capped semantic pass when keywords return nothing); check src/memory/mod.rs module docs + PLAN.md memory-retrieval section for the old empty-on-no-match / 0.2-weight wording and update both.
- [x] 5. **Verify** — full `cargo test` (must stay warning-free under #![deny(warnings)]) + frontend untouched (no tsc needed). Confirm the new tests fail without the step-1 change (mentally or via git stash-free inspection — they exercise the NoMatches arm directly).
