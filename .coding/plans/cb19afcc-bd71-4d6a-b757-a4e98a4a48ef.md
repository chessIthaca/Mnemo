# Plan: Rename to Mnemo + README, license, copyright headers

## Goal
Rename the project from myharness to Mnemo across crates, app config, frontend strings, env vars, and the global config dir (with migration); add a non-commercial LICENSE (PolyForm Noncommercial 1.0.0); prepend copyright headers to all source files; write a GitHub README covering key features, key ideas, the enforced plan-first workflow (with an SVG diagram), and Windows + macOS build instructions.

## Kind
implementation

## Context
Rename surface confirmed by search: root crate `myharness` (Cargo.toml package+lib name, license="MIT", description "A coding-first agentic harness in Rust"); app crate `myharness-app` (src-tauri/Cargo.toml, dep `myharness = { path = ".." }`, license="MIT"); 100+ `use myharness::` imports across src-tauri/src (24 files) + tests/; tauri.conf.json productName "myharness", identifier "com.myharness.app"; npm names `myharness` (root package.json) + `myharness-frontend` (frontend/package.json); frontend strings: AboutDialog.tsx:42,85,105 (incl. an "MIT-licensed" claim + APP_LICENSE_URL that must change to the new license), Sidebar.tsx:37-38, AdvancedSection.tsx:191 (`myharness-settings-<date>.json`); CustomEvent `myharness:open-file` (openFile.ts producer + FileViewer.tsx consumer + tests); env vars MYHARNESS_DISABLE_WIN_OCCLUSION (src-tauri/src/main.rs:144,155; built in src/webview_args.rs), MYHARNESS_BASE_URL/API_KEY/MODEL (tests/provider_integration.rs); global config dir `~/.myharness` (src/config/mod.rs:358-374 global_config_dir, doc refs src/project/agent_md.rs:14); web_fetch user-agent "myharness-agent/0.1" (src/tool/agent/web_fetch.rs); window title "myharness — {folder}" in src-tauri/src/main.rs. Historical .coding/* plans/reviews/logs keep the old name (history — do not rewrite). The repo dir C:\AgenticCoder\AgenticCoder stays as-is (renaming it would break the running session; agent.md paths unchanged).

## Steps
- [x] 1. **Branch + LICENSE (PolyForm Noncommercial 1.0.0) + license metadata** — Branch feat/mnemo-rename is created by create_plan. web_fetch https://polyformproject.org/licenses/noncommercial/1.0.0/ to get the canonical PolyForm Noncommercial License 1.0.0 text and write it verbatim to LICENSE at the repo root (plain text, LF endings). In root Cargo.toml: replace `license = "MIT"` with `license-file = "LICENSE"`, update description to "Mnemo — a coding-first agentic harness in Rust with persistent memory". In src-tauri/Cargo.toml: same license swap, description "The Mnemo Tauri app shell". License rationale: PolyForm Noncommercial is a standard, software-specific non-commercial license (CC BY-NC is not recommended for software by Creative Commons itself). Write a step-boundary crash-marker memory (PROTOCOL v2).
- [x] 2. **Rename Rust crates: myharness→mnemo, myharness-app→mnemo-app** — Root Cargo.toml: `[package] name = "mnemo"`, `[lib] name = "mnemo"`. src-tauri/Cargo.toml: `name = "mnemo-app"`, dependency `mnemo = { path = ".." }`. Then sweep all .rs files under src/, src-tauri/src/, tests/: replace `myharness::` → `mnemo::` (imports, qualified paths, doc comments) and `myharness-app` → `mnemo-app` (comments/plan refs in code only — skip .coding/). Use file-by-file file_edit or a careful PowerShell -replace pass on .rs files only. Run `cargo test` unpiped at the root + `"exit=$LASTEXITCODE"` — this regenerates Cargo.lock with the new names and proves the rename compiles warning-free under #![deny(warnings)]. Crash-marker memory.
- [x] 3. **Rename user-facing strings: Tauri config, window title, frontend, env vars, user-agent** — src-tauri/tauri.conf.json: productName "Mnemo", identifier "com.mnemo.app". src-tauri/src/main.rs: window-title format "myharness — {}" → "Mnemo — {}" (find the set_title call in setup; also the NeedsProject-path title if separate). frontend: AboutDialog.tsx (title "About Mnemo", "Mnemo is" body, doc comment) — ALSO replace the "MIT-licensed" claim + APP_LICENSE_URL target with a PolyForm Noncommercial line + https://polyformproject.org/licenses/noncommercial/1.0.0/ (find the constant, likely frontend/src/components/about/*.ts); Sidebar.tsx:37-38 title/aria-label; AdvancedSection.tsx:191 download name `mnemo-settings-<date>.json`; CustomEvent literal `myharness:open-file` → `mnemo:open-file` in frontend/src/lib/openFile.ts + the FileViewer.tsx listener + any test referencing it (search literal `myharness:open-file` across frontend/). package.json name "mnemo", frontend/package.json name "mnemo-frontend". frontend/index.html <title> if it says myharness. Env vars: MYHARNESS_DISABLE_WIN_OCCLUSION → MNEMO_DISABLE_WIN_OCCLUSION in src-tauri/src/main.rs + src/webview_args.rs (code + comments); MYHARNESS_BASE_URL/API_KEY/MODEL → MNEMO_* in tests/provider_integration.rs. web_fetch.rs user-agent "mnemo-agent/0.1". Sweep root-level markdown (PLAN.md if present) for remaining 'myharness' outside .coding/. Run cargo test (both crates) + `cd frontend; npx tsc --noEmit; npx vitest run`. Crash-marker memory.
- [x] 4. **Global config dir ~/.myharness → ~/.mnemo with automatic migration** — src/config/mod.rs global_config_dir() (:358-374): return `~/.mnemo` on all branches (directories::BaseDirs::home_dir / HOME / USERPROFILE / fallback). Add `pub fn migrate_legacy_config_dir()` with doc comment: if `~/.mnemo` does not exist and `~/.myharness` does, `std::fs::rename` old→new (atomic same-volume move; on error log + leave the old dir in place and continue with the new empty dir). Implement as a testable pure helper `fn migrate_legacy_dir(old: &Path, new: &Path)` + unit tests with tempfile (old exists/new absent → moved with file contents intact; new already exists → untouched; neither exists → no-op). Call `migrate_legacy_config_dir()` once at startup in src-tauri/src/main.rs before the first config load (build_brain / browser_inspection_enabled read config.toml — migration must run first, top of main()). Update doc comments in src/config/mod.rs:3,31 + src/project/agent_md.rs:14 (`~/.myharness` → `~/.mnemo`). cargo test unpiped. Crash-marker memory.
- [x] 5. **Copyright headers on all source files (idempotent PowerShell script)** — Create scripts/add-copyright-headers.ps1 (checked in, documented). Get the holder name from `git config user.name` and substitute it below. Header (3 lines, `//` style): `// Copyright (c) 2026 <holder>` / `// SPDX-License-Identifier: LicenseRef-PolyForm-Noncommercial-1.0.0` / `// See LICENSE in the repository root (non-commercial use only).` — prepended at the very top of every file matching: src/**/*.rs, src-tauri/src/**/*.rs, src-tauri/build.rs, tests/**/*.rs, frontend/src/**/*.ts, frontend/src/**/*.tsx, frontend/*.ts (vite.config.ts etc.). Skip .coding/, target/, node_modules/, dist/, lockfiles, JSON/MD/TOML. Requirements: (a) idempotent — skip files whose first ~5 lines already contain 'Copyright (c) 2026'; (b) BOM-safe — read/write via [System.IO.File]::ReadAllBytes/WriteAllBytes, detect + preserve a UTF-8 BOM; (c) preserve each file's line-ending style — use \r\n in the header when the file content contains \r\n, else \n; (d) legal Rust — `//` comments before `#![deny(warnings)]`/`//!` are fine, no reordering needed since we prepend plain line comments. Add a `#` variant of the header to the .ps1 itself. Run the script, then verify: `cargo test` unpiped at root, `cargo test` in src-tauri, `cd frontend; npx tsc --noEmit; npx vitest run` — all must stay green (proves headers didn't break parsing). Crash-marker memory.
- [x] 6. **GitHub README.md + docs/workflow.svg diagram** — Create README.md at the repo root (the GitHub front page) + docs/workflow.svg (hand-authored, referenced relatively so GitHub renders it — GitHub strips inline SVG, so it MUST be a separate file). README structure: (1) `# Mnemo` + one-line tagline (agentic coding harness that remembers — plan-first workflow, four-tier persistent memory, code knowledge graph); (2) Key features: enforced plan-first state machine (Planning→Executing→Reviewing→Complete, skills, sub-plan stack, crash-safe resume), reviewer subagent gate before every completion, four-tier memory (working/episodic/semantic/procedural, SQLite + FTS5 + bundled ONNX embeddings, auto-recall + consolidation), tree-sitter code knowledge graph (graph_search/context/impact/path tools + interactive graph tab), multi-agent runtime (spawn_agent, channel-decoupled), backlog with an agent-facing backlog_add tool, path sandbox + diff-based approval gate + safety modes + core git ops always gated, headless Chromium browser tools + live WebView2 game_* tools (Windows), per-workflow-state model routing, React/Tauri desktop UI. (3) Key ideas: memory as first-class; plans enforced by the tool surface (state machine changes which tools exist); review unskippable by construction; everything resumable after a crash. (4) The workflow section explaining Planning/Executing/Reviewing/Complete + how skills and sub-plans nest, with `<img src="docs/workflow.svg" alt="Mnemo workflow">`. SVG: 4 state boxes left→right with labeled transition arrows (create_plan →, steps done →, review passed →, back-edges: findings → Executing, abandon → Planning), a small 'reviewer subagent' callout under Reviewing, dark-theme-friendly colors (#0f172a bg optional, high-contrast text), system font stack, viewBox ~960x320, title/desc for a11y. (5) Building: prerequisites + commands for Windows (VS Build Tools C++ workload, WebView2 Runtime [preinstalled on Win11], Rust stable, Node LTS; `npm install`; dev: `npx tauri dev` from src-tauri/ — beforeDevCommand runs the Vite dev server; release: `npx tauri build`) and macOS 11+ (Xcode Command Line Tools, Rust, Node; same npm/tauri commands; note the Windows-only live-webview Browser tab is disabled on macOS, headless browser tools still work). (6) Configuration: `~/.mnemo/` (config.toml, endpoints.toml, keys.toml) + per-project `.coding/`. (7) License section: PolyForm Noncommercial 1.0.0 — free for non-commercial use, link to LICENSE, copyright line with the git user.name holder. Keep tone tight, no marketing fluff.
- [x] 7. **Final sweep + full test matrix** — Sweep: `search` for `myharness` (case-insensitive) across the repo excluding .coding/ and target/ — every remaining hit in shipped code/config/docs gets renamed or justified (e.g. none should remain). Then the full matrix from C:\AgenticCoder\AgenticCoder, all unpiped with test-result lines + $LASTEXITCODE checked: (1) cargo test (root, workspace incl. src-tauri); (2) cd frontend: npx tsc --noEmit, npx vitest run, npm run build. Fix failures at the root cause — no #[allow]. Then the standard closing sequence runs: reviewer subagent over ALL uncommitted changes, fix every finding, commit to feat/mnemo-rename including the review report, finish. Final crash-marker memory recording the completed rename.
