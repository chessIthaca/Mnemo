# Plan: Decompose run_all.rs's two largest resolution functions

## Goal
Behavior-preserving decomposition of run_all_dispatch_next (~290 lines) and on_main_turn_resolved (~757 lines) in src-tauri/src/ipc/run_all.rs into per-outcome helpers (the run_turn decomposition pattern), leaving no function over ~200 lines, with all 17 fn_body source-contract pins retargeted/split to the helpers that now own the pinned code (assertion strings preserved), behavioral regression tests untouched, and cargo test green.

## Kind
implementation

## Context
Exploration findings (2027-01-09 session): run_all.rs is now 6505 lines. Current boundaries: run_all_dispatch_next at ~3663-3952 (~290 lines: DISPATCH_LOCK → exclude-set selection → nothing-eligible arm → busy guard #1 → git checkpoint + failure arm → record in-flight → recall enrichment → manager-lock dispatch section), on_main_turn_resolved at 4962-5719 (~757 lines: intervention latch 4972-5082 → run-all branch 5084-5523 (success match with 4 arms / failure paths / bump+clear / stopped check / auto-compact tail) → single-dispatch branch 5525-5719). CRITICAL CONSTRAINT: 17 fn_body(include_str!("run_all.rs"), ...) source-contract pins live in the test module (lines ~1129-3209) and assert strings/orderings INSIDE these two bodies. The run_turn decomposition template (plan c4eb5525, commit 0cf3af9 — run_turn → ~470-line orchestrator over 11 phase methods) had no such pins; here the pins must be retargeted/split to follow the moved code. Behavioral regression tests (real store/workflow tests, e.g. run_all_dispatch_next_selects_via_next_pending_eligible at :618) are untouched. fn_body (run_all.rs:1570) is a text extractor — retargeting to any top-level fn works. Lock discipline: DISPATCH_LOCK/LANDING_LOCK scoping must stay exactly as-is (locks dropped before awaits) — moving a lock-holding block verbatim into a helper preserves this; never split a lock hold across a helper boundary.

## Steps
- [x] 1. **Extract run_all_dispatch_next's dispatch stage into three helpers** — in src-tauri/src/ipc/run_all.rs, move verbatim (comments included): (a) the git-checkpoint block + its failure arm (~3764-3799) into `async fn checkpoint_run_all_item(app: &tauri::AppHandle, state: &IpcState, item: &BacklogItem) -> Result<String, String>` (the failure arm keeps its annotate + end-run-if-no-lanes + Err(e) return); (b) the record-in-flight block (~3801-3817) into `async fn record_in_flight_item(state: &IpcState, item_id: &str, checkpoint: &str)`; (c) the manager-lock dispatch section (~3834-3951) into `async fn dispatch_run_all_to_main(app: &tauri::AppHandle, state: &IpcState, item: BacklogItem, checkpoint: &str, context_block: Option<String>) -> Result<(), String>` — the whole section moves as one unit so the manager-lock hold/drop scope is byte-identical (re-check → defer arm → enter_planning_if_complete → images → send → emits → fill_spawned_window). The orchestrator keeps: DISPATCH_LOCK, the exclude-set selection (pin :1408), the nothing-eligible arm, busy guard #1 (pins :1881/:1917), and the recall call (pin :1129) — target ~110 lines. Place the three helpers immediately after run_all_dispatch_next with doc comments (per-outcome: checkpoint/record/dispatch).
- [x] 2. **Retarget the three run_all_dispatch_next pins that follow the moved code** — in the test module: (1) the test at ~1125-1138 keeps its `fn_body(..., "run_all_dispatch_next")` fetch for the recalled_context_block assert (stays in the orchestrator) and ADDS a second fetch `fn_body(..., "dispatch_run_all_to_main")` for the run_all_prompt assert (moved into the helper) — assertion strings unchanged; (2) the test at ~2632 retargets to `"checkpoint_run_all_item"` (the .annotate(git checkpoint failed) assert + the no-BacklogStatus::Failed negative both hold in the helper); (3) the test at ~2657 retargets to `"dispatch_run_all_to_main"` (AgentCommand::Prompt + the banned-tool list all live in the helper). Then run `cargo test` in src-tauri — all dispatch pins + behavioral tests green.
- [x] 3. **Extract on_main_turn_resolved's run-all branch into per-outcome helpers** — add `enum RunAllItemDisposition { Waited, Proceed { terminal_resolution: bool } }` (doc: Waited = the run waits, annotate+emit already done; Proceed = fall through to the done-counter/pointer block, stopped check, and next dispatch). Move verbatim from the run-all branch (5084-5523): (a) the success match (~5123-5379) into `async fn run_all_success_disposition(app, state, item: &BacklogItem, root: std::path::PathBuf, loop_evidence: bool, plan_abandoned: bool) -> RunAllItemDisposition` — it computes closed_earlier (orphan_work_landed, pin :2201) and the scrutinee `loop_evidence || closed_earlier` (pin :2201), then delegates per arm; (b) the Done arm (~5153-5207) into `async fn flip_done_if_linked(app, state, item, root, closed_earlier: bool) -> bool` (may_flip via plan_linkage_allows_done → commit_success + transition Done + true; else the blocked annotate + false); (c) the abandoned arm (~5218-5254) AND the failure-path abandoned arm (~5380-5417) — near-identical — into ONE shared `async fn flip_failed_if_abandonment_linked(state, item, abandoned_plan_id: Option<&str>) -> bool` (may_flip via plan_linkage_allows_failed → transition Failed("plan abandoned (abandon_plan)") + true; else the eprintln + false) — a genuine dedup, used by both the success match and the failure path; (d) the (Complete,false)+Pending arm (~5281-5332) into `async fn resolve_slid_pending_item(state, item) -> bool` (orphan_work_landed + still_pending re-verify → transition Done + true; else stale-pointer eprintln + false); (e) the waiting arm (~5350-5378) into `async fn annotate_run_all_waiting(app, state, item, main_state: Option<WorkflowState>)` — KEEP its already_waiting guard inline (pins :1989 assert already_waiting + n.contains(note.as_str()) inside the arm); (f) the failure paths (~5380-5453) into `async fn run_all_failure_disposition(app, state, item, plan_abandoned, abandoned_plan_id, error: Option<String>) -> RunAllItemDisposition` (abandoned arm → shared helper; error arm keeps its inline already_waiting guard, pins :2040). `resolve_run_all_turn(app, state, success, error, loop_evidence, plan_abandoned, abandoned_plan_id)` (~110 lines) holds: the (item_id, stopped) read + item read, the if-let item { root; disposition = success ? success_disposition : failure_disposition; match disposition { Waited => return, Proceed { terminal_resolution } => the bump/clear block verbatim } }, the stopped check, emit_backlog_changed, and the auto-compact/dispatch tail (pins :2843 — all its strings stay here). The ORCHESTRATOR on_main_turn_resolved keeps the ENTIRE intervention latch block (4972-5082) inline — pins :1672 and :3184 stay 100% intact — then calls resolve_run_all_turn and returns; the single-dispatch branch becomes a call (step 4). Target: orchestrator ~145 lines, resolve_run_all_turn ~110, every helper < 100.
- [x] 4. **Extract the single-dispatch branch** — move verbatim (5525-5719): the (status, note) determination match (~5552-5647, including the may_flip_done/may_flip_failed computations) into `async fn single_dispatch_disposition(state, in_flight_id: &str, success, error, loop_evidence, plan_abandoned, abandoned_plan_id) -> (Option<BacklogStatus>, Option<String>)`; the rest (the single_in_flight take, the transition-or-restore apply with its inline already_waiting guard, resolved_terminally, emit, auto-feed) stays in `async fn resolve_single_dispatch_turn(...)` (~75 lines) — pins :2085 (take before resolved_terminally = false, = Some(in_flight_id) between) and :2179 ("if resolved_terminally && state.backlog.auto_feed") hold in resolve_single_dispatch_turn. The orchestrator ends with the resolve_single_dispatch_turn call.
- [x] 5. **Retarget/split the on_main_turn_resolved pins** — in the test module, following the catalog: RETARGET with assertions byte-identical: :2040 → "run_all_failure_disposition"; :2085 → "resolve_single_dispatch_turn"; :2843 → "resolve_run_all_turn" (all its ordering asserts — clear before gate, gate before dispatch, gate before auto_feed — hold in resolve_run_all_turn). RE-EXPRESS two ordering pins against the new structure (same intent): :1989's tail ("if stopped {" after the arms) becomes — in resolve_run_all_turn, the `RunAllTurnDisposition::Waited => return` arm precedes `if stopped {`; :2249's (Complete,false)-before-waiting becomes — in run_all_success_disposition, the `(Some(WorkflowState::Complete), false)` arm precedes the catch-all `(main_state, _)` arm that delegates to annotate_run_all_waiting (region asserts item.status == BacklogStatus::Pending / orphan_work_landed( / landed && still_pending / no return; move with the arm into resolve_slid_pending_item — retarget those to it). SPLIT four cross-helper pins per owning helper, assertion strings unchanged: :2153 (plan_loop_allows_done(main_state, true) + "_ if plan_abandoned =>" → run_all_success_disposition; commit_success(root.clone(), item.clone()) + BacklogStatus::Done → flip_done_if_linked; plan_open_note(main_state) + .annotate(&item.id, → annotate_run_all_waiting; "plan abandoned (abandon_plan)".to_string() → flip_failed_if_abandonment_linked; resolved_terminally + the auto_feed gate → resolve_single_dispatch_turn; the no-CantResolve/no-rollback negatives per helper); :2201 (orphan_work_landed(...) + loop_evidence || closed_earlier → run_all_success_disposition; plan_open_note(main_state) → annotate_run_all_waiting; terminal_resolution → resolve_run_all_turn); :2374 (plan_linkage_allows_done: flip_done_if_linked AND single_dispatch_disposition each contain it — the both-sites intent; guard-before-commit_success ordering holds inside flip_done_if_linked; the blocked-note count >= 2 becomes both-helpers-contain "completing plan is not this item's plan — item kept, no transition"); :2427 (plan_linkage_allows_failed: flip_failed_if_abandonment_linked AND single_dispatch_disposition each contain it — note the shared helper DEDUPS the two run-all sites so the old >= 3 count becomes the both-helpers form; "plan_abandoned && may_flip_failed" >= 2 holds in single_dispatch_disposition; the for-loop at ~2449 keeps on_spawned_turn_resolved as-is and checks main's blocked eprintln via flip_failed_if_abandonment_linked).
- [x] 6. **Verify the decomposition** — (1) `cargo test` in src-tauri: ALL tests green, zero test bodies changed except the pin retargets/splits from steps 2/5 (behavioral regression tests byte-identical — verify with git diff on the test module: only fn_body target strings and regrouped asserts may differ); (2) line-count check: no function in run_all.rs over ~200 lines (spot-check with a quick search of fn boundaries); (3) `git diff` review: production changes are moves + helper signatures + doc comments, no logic edits — lock scopes byte-identical; (4) module doc: if the run_all.rs module doc describes the two functions' internals, add one line noting the per-outcome helpers; (5) run the full `cargo test` at the repo root too (the lib crate is untouched but confirm no cross-crate drift).
