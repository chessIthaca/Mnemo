# Plan: Memory docs batch: three stale-doc fixes (Me1-Me3)

## Goal
Fix the three stale-doc findings from the 2026-09-08 memory review so the docs match the code: (Me1) memory_debug_recall's doc comment describes the read-only recall_peek path instead of claiming a bump side effect; (Me2) the main.rs startup re-embed comment drops the false "memory DB committed to git" rationale; (Me3) README.md and PLAN.md list the current six-tool memory surface (memory_write, memory_search, memory_consolidate, memory_update, memory_supersede, memory_delete) with zero retired tool names remaining.

## Kind
implementation

## Context
Findings LOW 1-3 of .coding/reviews/2026-09-08-full-review-memory.md (memory-axis review, 0 high / 4 low; LOW 4 is a separate item). Doc-only — no behavior changes. Verified facts: (1) memory_debug_recall calls store.recall_peek (memory_debug.rs:255-258, inline "Peek, not bump" comment) and production auto-recall also uses recall_peek (turn.rs:1800-1803) — the doc comment's bump side-effect and "no read-only recall path today" claims are both stale. (2) .gitignore:46 ignores .coding/memory.db; README.md:44 documents DBs-as-gitignored-caches; the re-embed check itself is still valid for model swap + legacy/empty fingerprints. (3) The registered memory tools are memory_write/memory_search/memory_consolidate/memory_update/memory_supersede/memory_delete (retrieval.rs:5-12 "One tool replaces six"; factory.rs:1138-1198); reviewer allow-list names memory_search (spawn.rs:432-466, memory_search at :463). Drift sites found by grep memory_recall|memory_list|plans_search|reviews_search|past_fixes|context_pack over {README,PLAN}.md: PLAN.md:234,236,237,388 + README.md:42,53 — PLAN.md:388 (reviewer query list) is an extra site beyond the review's LOW-3 list but must be fixed for the acceptance grep to return zero.

## Steps
- [x] 1. **Me1: rewrite memory_debug_recall doc comment** — In src-tauri/src/ipc/memory_debug.rs, replace the doc-comment paragraph at lines 231-238 (the "**Side effect:** `MemoryStore::recall` bumps `access_count` … there is no read-only recall path today …" block) with a read-only description: the command goes through `MemoryStore::recall_peek` — the same non-bumping path production auto-recall takes — so a debug recall test never freshens the memories it returns (no `access_count`/`last_accessed_at` update, no decayed-strength boost) and previewing recall ranking cannot change future production rankings. Keep the first doc paragraph (lines 225-229: scored results, working-tier inclusion, `Err` when no store wired) unchanged.
- [x] 2. **Me2: fix the startup re-embed comment rationale** — In src-tauri/src/main.rs, rewrite the comment opening at lines 1366-1367 ("Cross-machine re-embed check: the memory DB lives in .coding/ (committed to git), so it can travel to a machine running a different bundled model.") to the current rationale: the memory DB is a gitignored per-machine cache (.gitignore), so git never carries it — but stored vectors can still mismatch the configured embedder via a Settings model swap (a config save changes the model) or a legacy/empty fingerprint set (rows embedded before fingerprint tracking); only a wholesale project-directory copy moves the DB between machines. Keep the rest of the comment (fingerprint-absent check, fire-and-forget, Ready-only gating, hash-path skips) unchanged; fold the now-redundant "This also covers the model-switch case" sentence into the lead rationale.
- [x] 3. **Me3: update README.md + PLAN.md to the six-tool surface** — Four edits; after them, neither file may contain any of the six retired names (memory_recall, memory_list, plans_search, reviews_search, past_fixes, context_pack):
(a) PLAN.md:234-238 Memory-tools bullet: list `memory_write`, `memory_search`, `memory_consolidate` plus the hygiene tools (`memory_update`, `memory_supersede`, `memory_delete`) and the read-only git bridge (`git_log`, `git_show`); describe `memory_search` as the single read path — one tool replaced the six former per-purpose read tools (they differed only by a record-type constant, a tier, or the presence of a query), its query-less browse mode (newest-first, narrowed by record_type/tier/prefix) replacing the former list-only tool. Keep the trailing sentences (four tiers, typed prefixes, supersede-not-delete, tool/memory/, always available) unchanged.
(b) PLAN.md:388: replace "QUERIES (recall/list/search/past_fixes/context_pack, `backlog_list`)" with "QUERIES (`memory_search`, `backlog_list`)" — matches spawn.rs REVIEWER_BASE_TOOLS.
(c) README.md:42 tail: replace "hygiene tools `memory_update`/`memory_supersede`/`memory_delete`/`memory_list` plus retrieval tools `plans_search`/`reviews_search`/`past_fixes`/`context_pack` and read-only `git_log`/`git_show`" with the current surface: hygiene tools `memory_update`/`memory_supersede`/`memory_delete`, `memory_consolidate` for distillation, and the single retrieval tool `memory_search` (ranked query, or a query-less browse narrowed by record_type/tier/prefix that replaces the former list-only tool) plus read-only `git_log`/`git_show`.
(d) README.md:53: in the mandatory-triggers parenthetical, change "`memory_recall` before non-trivial work on existing code" to "`memory_search` before non-trivial work on existing code".
- [x] 4. **Verify: grep + cargo test** — Run the acceptance grep over README.md and PLAN.md for memory_recall|memory_list|plans_search|reviews_search|past_fixes|context_pack — must return zero matches. Run `cargo test` (PowerShell, check $LASTEXITCODE unpiped) — doc comments must compile warning-free under deny(warnings). Then the closing sequence (reviewer, fix findings, commit) runs automatically.
