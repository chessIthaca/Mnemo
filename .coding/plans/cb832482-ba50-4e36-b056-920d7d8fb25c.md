# Plan: Memory access log (last 100) on the Graph tab + graph-first tool strategy + descriptive graph tool cards

## Goal
A rolling 100-entry memory access log (reads + writes) surfaced in the Graph tab's bottom section, plus system-prompt changes that make the agent reach for graph tools before text search.

## Kind
implementation

## Context
User ask (3 parts): (A) the right-panel Graph tab (frontend/src/components/views/GraphView.tsx, bottom strip = legend at line 528) gets a section showing the last 100 memory accesses, reads AND writes, one line per entry, capped at 100. (B) The system prompt (TOOL_STRATEGY, src/agent/prompt.rs:146-180) must make the agent use graph tools BEFORE search. (C) Added 2026-08-19: make the graph_* tool cards in the chat transcript more descriptive — show WHAT was searched (e.g. the graph_search query). FINDING for C: the ToolCard header chips come from argPaths/argLabel; argLabel (frontend/src/components/chat/Message.tsx:240-314) handles shell purpose / spawn_agent name / skill_start skill / git subcommand / read_files paths / search pattern — but has NO case for graph_search ({query}), graph_context/graph_impact ({id?, name?}), graph_path ({from, to}), so those cards render bare. The React-free pure-logic home is frontend/src/lib/toolCardPaths.ts (already imported by Message.tsx for argPaths/basename; its colocated toolCardPaths.test.ts is ALREADY in the vitest.config.ts include list — no config change needed). displayName (Message.tsx:320) can get friendly names ("Graph Search" etc.) mirroring spawn_agent.

Key findings:
- ALL memory reads/writes funnel through MemoryStoreTrait methods on the concrete src/memory/mod.rs MemoryStore: `write` (:708), `recall` (:758), `strongest` (session primer, called from src/agent/turn.rs:470 — SQLite impl overrides the trait default at :976). memory_write/memory_recall tools, auto-recall, consolidation writes, and the Memory-tab recall test box all flow through these. Do NOT log list_by_tier/count_by_tier — the Memory tab polls its overview every 2s and would flood the log.
- MemoryStore has two constructors to init any new field: `open` (:287) and `open_shared_in_memory` (:374). Existing style: `Arc<Mutex<Connection>>` + `.expect("... lock poisoned")`; `now: Arc<dyn Fn() -> i64>` clock for timestamps. In `write`, the `memory` value is MOVED into the spawn_blocking closure — hoist `let tier = memory.tier;` (MemoryTier is Copy) + `let title = memory.title.clone();` before it (near `let id = memory.id.clone();` :730) so the log line after success can use them.
- IPC: src-tauri/src/ipc/memory_debug.rs is the natural home (memory-domain commands; update its module doc to mention the Graph tab). IpcState.runtime.memory_store is `Option<Arc<MemoryStore>>` (CONCRETE handle, state.rs:100) — the ring buffer is readable without touching the trait (mocks unaffected). Register the command in src-tauri/src/main.rs invoke_handler next to the memory_debug entries (~:510). The log read is an in-memory VecDeque clone behind a short std Mutex — NOT SQLite, so no spawn_blocking needed (doc-comment why; contrast memory_debug's SQLite reads).
- Frontend: wrapper pattern in frontend/src/lib/tauri.ts near memoryDebug* (~:546-620); GraphView has a pure-helper test pattern (GraphView.test.ts tests exported pure fns only, no DOM). Add a light poll (5s) for the access log while mounted — reading the log does NOT push entries (only write/recall/strongest do), so no feedback loop.
- Wire shape (define in src/memory/types.rs, add to mod.rs pub use list at :23): MemoryAccessEntry { at: i64 (unix secs, via self.now), op: String ("read"/"write"), tier: Option<String>, detail: String (query for reads / title for writes, char-capped ~120), hits: Option<usize> (read result count) }. Derive Serialize+Deserialize + Debug + Clone + doc comments on every field. Cap consts: MAX_ACCESS_LOG_ENTRIES=100, MAX_ACCESS_DETAIL_CHARS=120 (char-boundary-safe truncation like MAX_WORKING_CONTENT_CHARS at mod.rs:85).
- TOOL_STRATEGY (prompt.rs:146-180): currently `search / search_read` line (:161) comes BEFORE the graph line (:163). Reorder: graph line first with strengthened "try these FIRST for code lookups" wording; demote search to "fallback when graph tools come up empty + non-symbol text". Check prompt.rs tests for TOOL_STRATEGY assertions; add an order-pinning test (graph before search).
- Constraints: Windows 11 + PowerShell, unpiped commands reading $LASTEXITCODE (piped codes lie); #![deny(warnings)] both crates — no #[allow], doc comments on all pub items; root cargo test does NOT compile src-tauri — run both; preserve line endings; commit to feature branch never main; commit includes the review report. Branch feat/memory-access-log already created off main (step 1 in progress).

## Steps
- [x] 1. **Branch + memory access log ring buffer** — Branch feat/memory-access-log off main (DONE — already created + checked out). In src/memory/types.rs add `pub struct MemoryAccessEntry` (Serialize + Deserialize + Debug + Clone) with doc-commented fields: `at: i64` (unix secs), `op: String` ("read"/"write"), `tier: Option<String>`, `detail: String` (query or title), `hits: Option<usize>`; add to the pub use list in src/memory/mod.rs. In src/memory/mod.rs add consts `MAX_ACCESS_LOG_ENTRIES: usize = 100` + `MAX_ACCESS_DETAIL_CHARS: usize = 120` (doc comments, mirroring MAX_WORKING_CONTENT_CHARS), `use std::collections::VecDeque;`, an `access_log: Mutex<VecDeque<MemoryAccessEntry>>` field on MemoryStore (init empty in BOTH constructors, open :287 + open_shared_in_memory :374), a private `fn log_access(&self, op: &'static str, tier: Option<String>, detail: impl AsRef<str>, hits: Option<usize>)` that truncates detail char-boundary-safe then push_back + pop_front over cap, and `pub fn access_log(&self) -> Vec<MemoryAccessEntry>` returning a newest-first snapshot under the short lock (doc comment: in-memory ring, no SQLite, safe inline). Push sites (success path only): `write` after the spawn_blocking await (hoist `let tier = memory.tier;` + `let title = memory.title.clone();` BEFORE the closure move — memory is moved into it), `recall` right before `Ok(scored)` (read, filter.tier, query, Some(scored.len())), and the concrete `strongest` override after truncate (read, None, "session primer", Some(memories.len())). Unit tests in the mod.rs tests module: cap evicts oldest at 100 (105 writes → len==100, newest-first snapshot ends at write #5 / starts at #104), write+recall produce correct op/detail/tier/hits, strongest logs a "session primer" read, detail truncation is char-boundary safe (multibyte title). Run root `cargo test` UNPIPED (test-result lines + $LASTEXITCODE).
- [x] 2. **IPC command memory_access_log** — In src-tauri/src/ipc/memory_debug.rs add `#[tauri::command] pub async fn memory_access_log(state: State<'_, IpcState>) -> Result<Vec<MemoryAccessEntry>, IpcError>`: when `state.runtime.memory_store` is None return Ok(Vec::new()) (degrade to an empty log, not an error — Graph tab shows an empty section); else `Ok(store.access_log())` — inline short lock, NO spawn_blocking, with a doc comment stating why (in-memory VecDeque snapshot, not SQLite; contrast the SQLite reads below). Update the module doc comment to mention the Graph tab's access-log section. Import MemoryAccessEntry from myharness::memory. Register `ipc::memory_debug::memory_access_log,` in src-tauri/src/main.rs invoke_handler next to the other memory_debug entries (~:510). Run `cargo test` UNPIPED in src-tauri/ — fix anything red before continuing.
- [x] 3. **Graph tab bottom section** — In frontend/src/lib/tauri.ts add the `MemoryAccessEntry` interface (doc comment) + `export async function memoryAccessLog(): Promise<MemoryAccessEntry[]>` wrapping invoke("memory_access_log"), placed near the memoryDebug* wrappers. In frontend/src/components/views/GraphView.tsx: (1) add exported pure helper `export function formatAccessLine(e: MemoryAccessEntry): string` — one line, no timestamp: read → `R [tier] "detail" → N hits` (omit `→ N hits` when hits null), write → `W [tier] "detail"` (tier omitted when null); (2) state `accessLog: MemoryAccessEntry[]`, loaded on mount + polled every 5s while the component is mounted (window.setInterval in a useEffect with cleanup; reading the log pushes no entries, so no feedback loop — doc comment); (3) below the existing legend strip (line ~528) add a `border-t border-slate-800` section: header `Memory access — last {accessLog.length} (capped 100)` + a `max-h-32 overflow-y-auto font-mono text-[10px]` list, one `<div class="truncate">` per entry prefixing `new Date(e.at * 1000).toLocaleTimeString()` (text-sky-400 for reads / text-emerald-400 for writes). Extend frontend/src/components/views/GraphView.test.ts with formatAccessLine table tests: read with hits, read null hits, write with tier, null tier. Run `npm test` + `npx tsc --noEmit` in frontend/.
- [x] 4. **Descriptive graph tool cards (show what was searched)** — In frontend/src/lib/toolCardPaths.ts add `export function graphCallLabel(toolName: string, argsJson: string): string | null` (doc comment: one-line label for the graph_* ToolCard header — WHAT was searched; React-free so the node-env suite exercises it). Parse args with a guarded JSON.parse (null on failure); graph_search → quoted `query`; graph_context / graph_impact → `name` when present, else `id` with the `file::` prefix + `::line` suffix stripped (id format is `file::name::line`); graph_path → `from → to` with the same id shortening; any other tool name → null. In frontend/src/components/chat/Message.tsx wire it into `argLabel` (a branch for the four graph_* names calling graphCallLabel, before the path fallback) and extend `displayName` with "Graph Search" / "Graph Context" / "Graph Impact" / "Graph Path" mirroring the spawn_agent case. Add table tests to the EXISTING frontend/src/lib/toolCardPaths.test.ts (already in vitest.config.ts include — no config change): query quoting, name preferred over id, id prefix/suffix stripping, from→to, unknown tool → null, malformed JSON → null. Run `npm test` + `npx tsc --noEmit` in frontend/.
- [x] 5. **System prompt: graph tools before search** — In src/agent/prompt.rs TOOL_STRATEGY (:146-180): move the graph_* line ABOVE the search line and reword — graph line: `graph_search / graph_context / graph_impact / graph_path: TRY THESE FIRST for code lookups — graph_search to find where a symbol is DEFINED (prefer over grep/search), graph_context for its callers/callees/imports, graph_impact for blast radius before editing shared code, graph_path for how one symbol reaches another.`; search line becomes the fallback: `search / search_read: text search across files — the FALLBACK when the graph tools come up empty, and for non-symbol text (strings, config values, comments); cheaper than blind file reads.` Keep the backslash line-continuation style. Check the prompt tests module for existing TOOL_STRATEGY assertions and keep them green; add a test asserting the graph_* line appears BEFORE the search line in the built prompt (order-pinning). Run `cargo test --lib agent::prompt` then full root `cargo test` UNPIPED.
- [x] 6. **Verify all suites** — Run each UNPIPED reading $LASTEXITCODE: root `cargo test`, `cargo test` in src-tauri/, `cargo build` in src-tauri/ (bin crate not covered by root), `npm test` + `npx tsc --noEmit` + `npm run build` in frontend/. Zero warnings (#![deny(warnings)] enforces). Fix anything red before continuing.
- [x] 7. **Review, fix, commit** — Spawn reviewer (spawn_agent role:"reviewer") over ALL uncommitted changes with a self-contained task: goal = last-100 memory access log (MemoryStore ring buffer → memory_access_log IPC → Graph tab bottom section) + descriptive graph tool-card labels + TOOL_STRATEGY reordering graph-before-search; list changed files; ask for correctness (cap eviction, newest-first order, no log-feedback loop, no lock held across await, mock-store trait compatibility), bugs, security (no injection via query/title into the one-line render), constitution compliance (doc comments on pub items, no #[allow], regression tests). END THE TURN after spawning; resume on the notification. Read the report, fix EVERY finding, re-run the step-6 matrix, then commit everything including the review report to feat/memory-access-log (never main). Final summary ≤3 lines.
