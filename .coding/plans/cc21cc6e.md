# Plan: Split indexer.rs per-source indexers into submodules

## Goal
Split src/memory/indexer.rs (2571 lines) into a facade + per-source submodules under src/memory/indexer/ — behavior-preserving, public surface (crate::memory::indexer::*) unchanged via re-exports, every indexer-family file under ~1000 lines, cargo test green. Backlog item 150f6e77; review pointer .coding/reviews/2026-09-08-full-review-quality.md suggestion 4.

## Kind
implementation

## Context
Refactor, no behavior change. indexer.rs sections (verified by reading the whole file): header+orchestration (index_derived, rebuild_derived, reindex_knowledge_files, write_derived, SourceRecord, IndexReport), Scanning (scan_sources, scan_plans, scan_reviews, backlog structs+scan_backlog+parse_backlog_lines), Knowledge corpus (KnowledgeMeta, scan_knowledge, knowledge_source, build_knowledge_metas, knowledge_id, ResolvedLink, resolve_link, row_link, backlinks_for, corpus_is_stale, MigrateReport, migrate_authored_typed_rows, MIGRATION_MARKER, strip_knowledge_prefix, knowledge_fmt_date), digest builders (plan_record, review_record), helpers (first_content_line, budgeted_digest, truncate_title, first_commit_hash, content_hash, utc_now_secs), tests (1275 lines: core section 1299-1780, knowledge section 1782-2571).

External consumers (must keep compiling with ZERO changes): src-tauri/src/ipc/memory_debug.rs (ResolvedLink, resolve_link, backlinks_for), src-tauri/src/ipc/memory_maintenance.rs (rebuild_derived), src-tauri/src/main.rs (index_derived, migrate_authored_typed_rows, corpus_is_stale), src/tool/memory/mod.rs (index_derived, reindex_knowledge_files, knowledge_id), src/memory/finish_capture.rs (budgeted_digest, first_commit_hash, knowledge_id — budgeted_digest/first_commit_hash are pub(crate) and STAY in the facade).

TRAPS: (1) src/memory/consolidation.rs:1266 (inside a #[cfg(test)] fn) does include_str!("indexer.rs") and asserts the strings "mnemo: failed to delete migrated row" and "idempotency keeps re-runs safe" — both live in migrate_authored_typed_rows which moves to indexer/knowledge.rs, so that include_str! path must be updated to "indexer/knowledge.rs" (assertions unchanged). (2) In the facade, `mod knowledge;` shadows the `knowledge::` path — the facade's two `knowledge::KNOWLEDGE_DIR_NAME` uses (in index_derived and corpus_is_stale) must become bare KNOWLEDGE_DIR_NAME via `use crate::memory::knowledge::KNOWLEDGE_DIR_NAME;`. (3) In indexer/knowledge.rs, `use crate::memory::knowledge::{self, KnowledgeRecord, KnowledgeStatus};` is legal (a module's own name is not in its own namespace) and keeps `knowledge::parse_file`-style call sites verbatim; if the compiler rejects it, import the five items directly (parse_file, dir_for_record_type, slug_for, SUPERSEDED_SENTINEL, KNOWLEDGE_DIR_NAME) and drop `self`. (4) In tests/mod.rs the local `mod knowledge;` (test file) shadows the glob-imported production submodule name — harmless; the test-local `fn knowledge_id` helper shadows the glob-imported pub fn exactly as today. (5) Chained glob imports (tests/knowledge.rs does `use super::*;` where tests/mod.rs itself does `use super::*;`) should propagate the facade's items; if not, add `use crate::memory::indexer::*;` to tests/knowledge.rs.

Deliberate non-verbatim-section mapping: corpus_is_stale STAYS in the facade (cross-family staleness check over all four source families via scan_sources) even though it currently sits inside the Knowledge corpus section. reindex_knowledge_files + predecessor_rel + MAX_PREDECESSOR_CONTEXT MOVE to knowledge.rs (knowledge-family logic).

Visibility changes (only these): scan_plans, scan_reviews, scan_backlog, scan_knowledge, build_knowledge_metas become pub(super); KnowledgeMeta and its two fields become pub(super). All currently-pub items stay pub in their submodule and are re-exported; private helpers stay private within their submodule.

Line endings: run `git ls-files --eol src/memory/indexer.rs` first and write all new files in the same style (expected LF). All new files carry the standard 3-line copyright header (every src/memory/*.rs has it).

## Steps
- [x] 1. **Create plans.rs, reviews.rs, backlog.rs submodules** — First run `git ls-files --eol src/memory/indexer.rs` (PowerShell) to confirm line-ending style and match it in all new files. Create src/memory/indexer/plans.rs: copyright header + module doc (//! Plan-file scanning + digest building for the derived-record indexer), then move VERBATIM from src/memory/indexer.rs: fn scan_plans (lines 493-518, becomes pub(super)) and fn plan_record (lines 1142-1177, stays private). Imports: use std::path::Path; use crate::memory::{MemoryRecordType, MemorySearchConfig}; use crate::workflow::PlanFile; use super::{budgeted_digest, first_commit_hash, first_content_line, truncate_title, SourceRecord};. Create src/memory/indexer/reviews.rs: scan_reviews (lines 520-544, pub(super)) + review_record (lines 1179-1217, private); imports: std::path::Path, crate::memory::{MemoryRecordType, MemorySearchConfig}, super::{budgeted_digest, first_content_line, truncate_title, SourceRecord}. Create src/memory/indexer/backlog.rs: BacklogEntry struct + de_backlog_id + BacklogFile + scan_backlog (lines 546-634, scan_backlog becomes pub(super), rest private) + parse_backlog_lines (lines 636-656, private); imports: std::path::Path, serde::Deserialize, crate::memory::{MemoryRecordType, MemorySearchConfig}, super::{budgeted_digest, truncate_title, SourceRecord}. Keep every doc comment attached to its item.
- [x] 2. **Create knowledge.rs submodule** — Create src/memory/indexer/knowledge.rs: copyright header + module doc (//! Knowledge-corpus indexing: scan, supersede metadata resolution, targeted reindex, link resolution, and the authored-row migration). Move VERBATIM from src/memory/indexer.rs: reindex_knowledge_files (lines 211-409, pub), MAX_PREDECESSOR_CONTEXT (411-413), predecessor_rel (415-438, private), KnowledgeMeta (663-666, becomes pub(super) struct with pub(super) fields), scan_knowledge (668-715, pub(super)), knowledge_source (717-731, private), build_knowledge_metas (733-814, pub(super)), knowledge_id (816-827, pub), ResolvedLink (829-847, pub), resolve_link (849-905, pub), row_link (907-931, private), backlinks_for (933-958, pub), MigrateReport (1012-1019, pub), migrate_authored_typed_rows (1021-1108, pub), MIGRATION_MARKER (1110-1112), strip_knowledge_prefix (1114-1120), knowledge_fmt_date (1122-1138). Do NOT move corpus_is_stale (960-1010 — stays in the facade). Imports: use std::collections::hash_map::Entry; use std::collections::{HashMap, HashSet}; use std::path::Path; use serde::Serialize; use uuid::Uuid; use crate::error::{Error, Result}; use crate::memory::knowledge::{self, KnowledgeRecord, KnowledgeStatus}; use crate::memory::{Memory, MemoryClass, MemoryFilter, MemoryRecordType, MemorySearchConfig, MemoryStoreTrait, MemoryTier}; use super::{content_hash, write_derived, IndexReport, SourceRecord};. If the `knowledge::{self,...}` import errors (name collision), fall back to importing parse_file, dir_for_record_type, slug_for, SUPERSEDED_SENTINEL, KNOWLEDGE_DIR_NAME directly and rewriting the `knowledge::` prefixes.
- [x] 3. **Rewrite indexer.rs as the facade** — Edit src/memory/indexer.rs down to the facade via targeted file_edit operations (do not touch moved code's text): (1) after the import block add: mod backlog; mod knowledge; mod plans; mod reviews; #[cfg(test)] mod tests; and pub use knowledge::{backlinks_for, knowledge_id, migrate_authored_typed_rows, reindex_knowledge_files, resolve_link, MigrateReport, ResolvedLink}; (2) trim the import block to what remains in the facade — keep: std::collections::HashSet, std::path::Path, uuid::Uuid, crate::error::{Error, Result}, crate::memory::knowledge::KNOWLEDGE_DIR_NAME (NEW — replaces the `knowledge::` prefix), crate::memory::{Memory, MemoryClass, MemorySearchConfig, MemoryStoreTrait, MemoryTier}; drop: Entry/HashMap, serde::{Deserialize, Serialize}, knowledge::{self, KnowledgeRecord, KnowledgeStatus}, MemoryFilter, MemoryRecordType, PlanFile; (3) in index_derived and corpus_is_stale replace `knowledge::KNOWLEDGE_DIR_NAME` with bare `KNOWLEDGE_DIR_NAME` (2 sites: ~line 89, ~line 982); (4) delete the moved blocks: reindex_knowledge_files + MAX_PREDECESSOR_CONTEXT + predecessor_rel (211-438), scan_plans + scan_reviews (493-544), backlog items (546-656), knowledge corpus section minus corpus_is_stale (658-959 and 1012-1138), plan_record + review_record (1142-1217); (5) keep: module doc (add a short Layout paragraph naming the four submodules + tests), IndexReport, SourceRecord, index_derived, rebuild_derived, write_derived, scan_sources (its calls become scan_plans/scan_reviews/scan_backlog/scan_knowledge resolved via the submodule mods), corpus_is_stale, and the Helpers section (first_content_line, budgeted_digest, truncate_title, first_commit_hash, content_hash, utc_now_secs) unchanged; (6) replace the whole inline `#[cfg(test)] mod tests { ... }` block (lines 1299-2571) with nothing (the mod decl from step 1 wires the file-based tests).
- [x] 4. **Split the tests into tests/mod.rs + tests/knowledge.rs** — Create src/memory/indexer/tests/mod.rs with the test-module header VERBATIM from indexer.rs lines 1300-1346 — use super::*; use crate::memory::{Embedder, HashEmbedder, MemoryFilter, MemoryRecordType, MemoryStore}; (ADD MemoryRecordType to this list — the facade drops it and the tests use it) use std::sync::Arc; use std::sync::Mutex; use tempfile::tempdir; — plus the helpers make_store, plans_dir, backlog_path, write_plan, write_review, write_backlog, noop_progress and consts PLAN_A, REVIEW_A, BACKLOG, then declare `mod knowledge;` and move these tests VERBATIM (bodies untouched): indexes_plans_reviews_and_pending_backlog_items, backlog_lines_with_deferred_field_still_index, soft_deleted_backlog_item_is_not_indexed, second_run_skips_unchanged_sources, changed_plan_reindexes_in_place, deleted_source_removes_record_and_state, legacy_plan_without_structured_sections_still_indexes, digests_always_fit_their_typed_budget, progress_ticks_to_total, rebuild_derived_replaces_derived_and_preserves_authored. Create src/memory/indexer/tests/knowledge.rs starting with `use super::*;` then move VERBATIM: the write_knowledge + knowledge_id helper fns and KNOWLEDGE_DECISION const (lines 1784-1794), the test lowered_config_budget_truncates_derived_digest (lines 1666-1710 — it uses write_knowledge + the knowledge_id helper, so it belongs here), and all knowledge-corpus tests: knowledge_files_index_as_derived_typed_records, knowledge_supersede_chain_marks_predecessor, superseded_without_successor_gets_the_sentinel, successor_added_later_supersedes_unchanged_predecessor, removed_knowledge_file_drops_its_row, reindex_knowledge_files_updates_just_the_given_files, reindex_knowledge_files_resolves_supersedes_against_the_corpus, resolve_link_targets_records_and_files, corpus_is_stale_detects_changes_and_removals, corpus_is_stale_when_state_table_is_empty, deleted_memory_db_rebuilds_everything_from_files, backlinks_for_finds_referencing_memories, migrate_authored_typed_rows_writes_files_and_drops_rows. If chained glob imports fail to resolve facade items in tests/knowledge.rs, add `use crate::memory::indexer::*;` there.
- [x] 5. **Update the consolidation.rs self-check path** — In src/memory/consolidation.rs around line 1264-1274 (inside the cfg(test) self-check fn): change `let indexer = include_str!("indexer.rs");` to `let indexer = include_str!("indexer/knowledge.rs");` and update the preceding comment from `// indexer.rs: the migrated-row delete logs on Err; the marker stays` to `// indexer/knowledge.rs: the migrated-row delete logs on Err; the marker stays`. The two assertions (contains "mnemo: failed to delete migrated row", contains "idempotency keeps re-runs safe") stay byte-identical — the strings moved with migrate_authored_typed_rows into indexer/knowledge.rs.
- [x] 6. **Verify: cargo test + line counts + consumer check** — Run `cargo test` UNPIPED then `"exit=$LASTEXITCODE"` (PowerShell — never trust a piped exit code); fix any compile errors (expected categories: missing/unused imports under #![deny(warnings)], visibility, glob-import resolution — see the fallbacks in the plan context). Then verify: (a) every indexer-family file is under ~1000 lines (indexer.rs ~510, knowledge.rs ~690, tests/mod.rs ~470, tests/knowledge.rs ~840, plans/reviews/backlog ~85-130) via a line count; (b) the public surface is unchanged — search for `indexer::` across src/ and src-tauri/ and confirm NO consumer file needed edits (src-tauri/src/ipc/memory_debug.rs, memory_maintenance.rs, main.rs, src/tool/memory/mod.rs, src/memory/finish_capture.rs); (c) `git status` shows only: new files under src/memory/indexer/, modified src/memory/indexer.rs and src/memory/consolidation.rs. Pre-existing over-1000-line files in src/memory/ (mod.rs, tests.rs, knowledge.rs, consolidation.rs) are out of scope for this refactor.
