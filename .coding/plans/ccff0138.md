# Plan: Full four-axis review: memory, quality, performance, model pipeline

## Goal
Run a fresh full-codebase review round at HEAD 2bdedd7 across four axes — (1) the memory system, (2) code quality & maintainability, (3) performance & resource use, (4) the model-interaction pipeline including run-all/parallel orchestration — via four parallel read-only reviewer subagents; then synthesize a master report in .coding/knowledge/ and queue actionable findings as backlog items. No source-code changes; deliverables are review reports, a synthesis report, and backlog items.

## Kind
research

## Context
Baseline gathered this turn: HEAD 2bdedd7 (merge of the run-all/parallel batch), clean tree (only untracked .coding/plans/stack.json.bak-merge-skill). Backlog is EMPTY — the 2027-01-06 round's 18 findings are all resolved. Prior full-round reports for dedupe: .coding/reviews/2027-01-06-full-review-memory-perf.md (3H/3L), 2027-01-06-full-review-quality-coverage.md (3H/8L), 2027-01-06-full-review-mac-readiness.md (0H/3L). High-value fresh code since 9a0d5b9: parallel run-all (48a35ad), run-all stall fixes (dfd2dfa, e5c6492, 0c0307d), finish-gate re-index (3d13884), boundary-token escaping (47c36ca), bug-fixing model slot (405be6d), backlog position param (534aef8), backlog soft-delete (afd527d), dispatch detail bar (08a9295), fill-rate compact gate (d92b117). Reviewers run on the configured reviewing model (no model override for role:"reviewer").

## Steps
- [x] 1. **Baseline & dedupe context** — Confirm review target and gather dedupe pointers (done in planning): HEAD 2bdedd7 on wt/agenticcoding, clean tree; prior full round 2027-01-06 @ 9a0d5b9 with reports .coding/reviews/2027-01-06-full-review-memory-perf.md, -quality-coverage.md, -mac-readiness.md (18 findings, all resolved, backlog empty); list of major commits since 9a0d5b9 to hand reviewers as fresh-code focus areas.
- [x] 2. **Spawn four parallel read-only reviewers** — spawn_agent role:"reviewer" (NO model param — configured reviewing model is authoritative), four agents in one block: memory-reviewer (memory system end-to-end: store, embeddings, consolidation, recall/auto-recall, supersede/update, budget, knowledge-file index reconciliation, Memory/Debug tabs), quality-reviewer (both Rust crates + frontend: boundaries, dead code, duplication, swallowed errors, doc comments, test gaps, oversized files), performance-reviewer (startup, indexing costs, display-update pipeline, streaming concat, allocation churn, lock contention, IPC payloads, leaks), model-pipeline-reviewer (request assembly, SSE parse + reasoning split, tool-call parsing + boundary tokens, retries/reroutes, compaction + fill-rate dial, context caps/BPE/prompt-cache, model routing, run-all/parallel orchestration). Each task: self-contained scope, dedupe pointers (2027-01-06 reports + fixed targeted reviews), constitution checks (docs sync: README/PLAN/module docs; macOS+Windows neutrality), improvement suggestions explicitly requested, verdict format '## Verdict: PASS' or '## Verdict: FINDINGS (n high, n low)', write via write_review_report to 2027-01-08-full-review-<axis>.md. End turn after spawning; finish notifications resume.
- [x] 3. **Re-spawn the three cancelled reviewers (one per turn)** — The spawn-cancellation race (top backlog item queued 2026-09-08; BUG memory written) killed the memory/quality/performance reviewers — only model-pipeline survived (report: .coding/reviews/2026-09-08-full-review-model-pipeline.md, FINDINGS 0 high 1 low). Re-spawn each reviewer with its original task text, ONE spawn per assistant message — the LLM round-trip between spawns lets each agent's first Started event fire before the next spawn's cleanup runs, dodging the race. Report filenames: 2026-09-08-full-review-memory.md, -quality.md, -performance.md (the app dates reports itself; the stale 2027-01 dates in memory records are unreliable).
- [x] 4. **Re-spawn memory + quality reviewers (one per turn)** — Both died without reports: killed by the app restarts during the UI-event-blackout diagnosis (last traced LLM activity 14:46; performance landed 14:55; no 2026-09-08-full-review-memory.md / -quality.md exists). Re-spawn each reviewer with its original axis task text from step 2's scope, ONE spawn per assistant message (race-dodging protocol — the has_ever_started fix is live in the running app but uncommitted). End turn after each spawn; the finish notification resumes.
- [x] 5. **Synthesize master report** — Read all four reviewer reports (.coding/reviews/2026-09-08-full-review-model-pipeline.md, -performance.md, -memory.md, -quality.md); cross-check and dedupe overlapping findings; verify surprising claims against the code (graph tools / read_files). Write .coding/knowledge/2026-09-08-full-review-synthesis.md: per-axis verdict summary, consolidated findings table (severity, location, symptom, fix direction), improvement roadmap (quick wins vs structural), and notes on what was verified as already-fixed.
- [x] 6. **Queue backlog items for findings** — For each actionable finding from the four reports: backlog_add (default position end) with a short headline + self-contained body: symptom with repro, exact file paths and symbol names, fix direction, acceptance criteria, pointers to the review reports. Batch trivial doc-only nits into one item; skip findings that are factually wrong (with written justification in the synthesis report). Include the model-pipeline report's LOW-1 (429-fallback viability, src/agent/loop_impl.rs:1701-1710) and its prioritized improvement proposals.
- [x] 7. **Memory records & wrap-up** — Write REVIEW: memory records (round digest + one per axis report, pointer-first to the report paths), then give the user a concise final summary: verdicts per axis, findings count by severity, where the synthesis report lives, how many backlog items were queued.
