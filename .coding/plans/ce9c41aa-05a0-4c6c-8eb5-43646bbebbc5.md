# Plan: Fill quality/arch gap in whole-app review

## Goal
Re-run the quality/architecture review with a time-boxed, report-early sub-agent; replace the main-agent fallback section in the consolidated report with the sub-agent's independent findings.

## Kind
research

## Context
The consolidated review .coding/reviews/2026-08-13-full-app-review.md has a known gap: the quality/architecture section (Q1-Q10) was written by the main agent because the quality/arch sub-agent's LLM stream aborted mid-run ("stream aborted — consumer dropped" in .coding/logs/provider-errors.jsonl) before it could call write_review_report. The security and perf sections are full independent sub-agent reports. User chose "Fill the gap first": re-spawn a time-boxed quality/arch reviewer, then merge its findings into the consolidated report. Lesson from failures: sub-agents that run long lose output — the task must be narrow (small file set) and must call write_review_report EARLY, before ending the turn. NOTE: spawning is only possible from an active plan (spawn_agent is gated); this is a research plan (produces only .coding/reviews/ docs, no source changes).

## Steps
- [x] 1. **Spawn time-boxed quality/arch reviewer** — call spawn_agent (role:"reviewer", name "reviewer-quality-arch-3"). Task must be self-contained + hard time-box: review ONLY these files (read them, nothing else): src/tool/mod.rs, src/workflow/mod.rs, src/error.rs, src-tauri/src/ipc/state.rs, src-tauri/src/ipc/settings.rs (first 120 lines), src-tauri/src/ipc/files.rs (first 60 lines), frontend/src/hooks/useAgentStore.ts (first 120 lines), frontend/src/lib/tauri.ts (first 120 lines), frontend/src/components/settings/types.ts. Assess: Tool trait/registry/SafetyLevel design, workflow-state gating, plan lifecycle, IPC layering (typed DTOs, fixture contract), error handling, frontend store shape, module boundaries, duplication, doc-comment coverage. CRITICAL: write the report via write_review_report(path: "2026-08-13-app-review-quality-arch.md") as the FIRST substantive action after reading (form findings from memory, don't keep reading) — prior reviewers lost all output by writing at the END and getting stream-aborted. Short report fine (~60-120 lines): verdict + severity-grouped findings (Critical/High/Medium/Low) with file:line refs + one-line fix each + clean areas. End turn immediately after the write.
- [x] 2. **Read the sub-agent's report** — after the finish notification, file_read .coding/reviews/2026-08-13-app-review-quality-arch.md. If the file exists and is substantive (findings + refs), keep it as the authoritative quality/arch section. If it is missing/empty again (second stream abort), do NOT re-spawn a third time; fall back to the main-agent-written section already in the consolidated report and note the gap honestly.
- [x] 3. **Merge into the consolidated report** — update .coding/reviews/2026-08-13-full-app-review.md: (1) replace the quality/arch findings (Q1-Q10) and the verdict-table Quality row with the sub-agent's actual findings, deduped against the main-agent fallback (keep the stronger of any overlapping findings, preserving file:line refs); (2) update the Method note (quality/arch now sub-agent-reviewed; remove the 'written directly by main agent' caveat for that section); (3) renumber/adjust severity counts in the verdict table and the recommended-action list. Preserve the security (H1/H2/M1-M4/L1-L3) and perf/build/memory (B/R/M) sections unchanged. No source-code changes.
- [x] 4. **Report to user + next step** — summarize what the independent quality/arch review found vs the fallback (new findings, dropped findings, severity shifts) and present the updated verdict table. Then ask the user how to proceed (fix Highs / fix Highs+cheap Mediums / keep discussing) via ask_user.
