# Plan: Phase 3c completion: tests + full verification + constitution close

## Goal
Complete Phase 3c steps 6-8: add targeted vitest coverage for new store behaviors (autoRevealDiff, applyBacklogChanged, safety resync path), run full cargo + npm test (unpiped), then execute standard close sequence (reviewer on ALL uncommitted changes, fix findings, commit on feature branch with report included). Keep strictly scoped to Phase 3c changes.

## Context
Resuming active Phase 3c plan at step 6 (tests). Prior steps 1-5 (PlanProgress depth/skill, auto-reveal Diff on file approvals, StatusBar safety persist+resync, backlog Run-All semantics) are implemented and cargo-green from previous runs. Follow constitution strictly: cargo before complete_step, reviewer on ALL uncommitted at close, fix all findings, include report in commit. FE has vitest; store actions are testable. No drive-bys.

## Steps
- [x] 1. Read current FE test files (useAgentStore.preview.test.ts + .test.ts) and the exact new functions added in steps 3-5 (autoRevealDiff, safety resync in StatusBar, backlog state apply).
- [x] 2. Add minimal, focused vitest tests for autoRevealDiff/autoRevealPlan (panel hidden + tab enabled case, no-op when visible or disabled) and applyBacklogChanged (sets the three fields). No new component tests unless logic is pure.
- [x] 3. Run unpiped cargo test (full) and cd frontend; npm test; confirm exit=0 and no breakage of existing tests.
- [x] 4. If tests pass, prepare for close: git status + git diff HEAD to capture ALL uncommitted (Phase 3c + any prior), spawn read-only reviewer agent with task listing files changed + plan goal + ask for correctness/bugs/security/constitution findings. Write report to .coding/reviews/....md.
- [x] 5. Read the review report, fix EVERY finding (no defer), re-run cargo test after fixes.
- [x] 6. Commit the changes (including the review report file) on the current feature branch with clear message. Do not touch main.
