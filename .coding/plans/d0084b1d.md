# Plan: Harden file_edit: read-before-edit guidance + newline-corruption investigation

## Goal
Investigate the recurring \n corruption in file_edit regex-mode payloads across the last two sessions, determine root cause (transport regression vs regex-replacement semantics), add "read file before editing" guidance to the file_edit tool description, and add a regex-mode newline warning. Rebuild so the hardened description takes effect.

## Kind
implementation

## Context
BUG memory 9777844a (active) documents "code-bearing tool-payload emissions degrade under long sessions" across FOUR mechanisms, confirmed via wire capture, context-depth-dependent. Superseded BUG 166dac2a documents the SAME corruption during this exact plan (5c94b497 step 1): "EVERY file_edit payload containing indented multi-line Rust source was emitted corrupted." This session: file_edit use_regex=true with \n in new_string produced literal backslash-n in 156 sites (fixed via PowerShell). Literal-mode edits failed due to guessed whitespace. PowerShell scripts malformed under frustration. file_edit tool description lives at src/tool/agent/file_edit.rs:529 (FileEditTool::schema); regex path is prepare_edit_regex (same file). The use_regex field description is at file_edit.rs ~line 36-40.

## Steps
- [x] 1. **Investigate root cause of \n corruption in file_edit regex mode**
- [x] 2. **Add read-before-edit guidance to file_edit tool description**
- [x] 3. **Add regex-mode newline warning to use_regex field description**
- [x] 4. **Rebuild + test**
