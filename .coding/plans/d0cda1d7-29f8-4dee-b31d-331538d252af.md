# Plan: Phase 3 — retrieval tools + prompt enforcement

## Goal
Phase 3 of the semantic memory system: retrieval tools (plans_search, reviews_search, past_fixes, context_pack) over the indexed store with per-class caps and recency decay; read-only git_log/git_show tools bridging memory records to shipped code; MANDATORY prompt habits (context_pack before planning non-trivial work, past_fixes before diagnosing any bug, memory_supersede on contradiction) shipped in the same phase. Tests throughout; cargo test + src-tauri build + frontend tests green.

## Kind
implementation

## Context
Parent: orchestrator 1c9ba6c8 — ALL work stays on feat/semantic-memory-system (NO branch; create_plan without a branch param). Phases 1+2 landed: typed records (SPEC:/DECISION:/BUG:/PLAN:/HOW:/REVIEW: prefixes, budgets, record_class authored/derived, superseded_by), hygiene tools (memory_update/supersede/delete/list), MemoryFilter with record_type()/include_superseded()/limit(), recall with per-class caps + configurable decay (MemorySearchConfig), derived indexer (plans/reviews/backlog digests with path+commit pointers). GROUNDING (verified 2026-08-22): (1) Tool pattern — src/tool/memory/mod.rs: MemoryWriteTool etc. (Tool trait: name/category/schema/safety/execute; ToolCategory::Memory; SafetyLevel::AutoRun for store-only tools); factory registration at src/agent/factory.rs:820-839 (register_memory_tools, `if let Some(store) = &self.memory` block) and register_agent_tools at :640 (GitTool::with_core_operations(&self.project_root, ...) at :659); tool-list test at factory.rs:1355-1376 (expected tool names array). (2) Recall API — store.recall(query, &MemoryFilter) -> Vec<ScoredMemory> (ScoredMemory{memory, score}); MemoryFilter::new().record_type(MemoryRecordType::Plan).limit(n); per-class derived cap (default 5) + per-query cap (default 50) already applied inside recall. (3) Git bridge — GitTool at src/tool/agent/git.rs:220 (run_git_impl spawns `git` with argv, current_dir project_root, kill_on_drop, CREATE_NO_WINDOW on Windows, cap_tool_output); test helper init_repo at git.rs:1081 (tempdir + git init -b main + commits on main/feat, returns false if git unavailable) + current_branch at :1111. (4) Prompt — TOOL_STRATEGY const at src/agent/prompt.rs:159 (MANDATORY-habit phrasing, stable head, prefix-cached — keep LEAN); MEMORY_RECORDS block at :216 already carries the memory_supersede habit (Phase 1) — Phase 3 adds context_pack + past_fixes lines to TOOL_STRATEGY. (5) Constitution: cargo test unpiped, warning-free (#![deny(warnings)]), no #[allow], doc comments on public fns, regression tests for defects; root cargo test does NOT compile src-tauri — verify with cd src-tauri; cargo build. DESIGN DECISIONS: retrieval tools live in a NEW module src/tool/memory/retrieval.rs (mod.rs is 1600+ lines) registered in the factory's memory block; git_log/git_show are NEW tools in src/tool/agent/git_read.rs (NOT extensions of GitTool — separate tools make the read-only contract explicit and allow SafetyLevel::AutoRun, which the NeedsApproval git tool can't); commit-ish args validated against option injection (reject leading '-', allow [0-9a-f]{7,40}/branch/tag/HEAD~N); context_pack output capped ~2KB (2048 chars) with a truncation note; no frontend changes in this phase (design says nothing; MEMORY_TOOLS set stays as-is).

## Steps
- [x] 1. **Retrieval tools module: plans_search / reviews_search / past_fixes / context_pack** — New file src/tool/memory/retrieval.rs (add `pub mod retrieval;` to src/tool/memory/mod.rs). Four tools, all ToolCategory::Memory, SafetyLevel::AutoRun, taking `store: Arc<dyn MemoryStoreTrait>`:
(1) PlansSearchTool `plans_search(query)` — recall with MemoryFilter::new().record_type(MemoryRecordType::Plan).limit(10); output `N plans matched:` + per hit `[tier] title (id: <id>, score: x.xx)
  <content ≤200 chars>` (same line shape as memory_recall so the frontend parser's regexes keep working).
(2) ReviewsSearchTool `reviews_search(query)` — same with record_type(Review), limit 10.
(3) PastFixesTool `past_fixes(error_text)` — same with record_type(Bug), limit 8; the error text IS the query (recall already does semantic+keyword matching).
(4) ContextPackTool `context_pack(topic)` — recall with NO record_type filter, limit 12 (per-class derived cap 5 already applies inside recall); format a pointer-first bundle: header `# context pack: <topic>` + per hit `- [<record_type>] <title> (id: <id>, score: x.xx) — <content>`; cap the OUTPUT at 2048 chars (truncate with `… [truncated]` note) — digests are pointer-first by construction (path/commit pointers), NEVER file contents. Empty results → `no memories match` success.
Register all four in src/agent/factory.rs register_memory_tools (the `if let Some(store)` block ~:820, after MemoryListTool). Update the tool-list test at factory.rs:1355-1376 (add the 4 names).
Tests in retrieval.rs tests mod (make_store pattern from mod.rs tests): plans_search returns only Plan records and ranks by score; reviews_search only Review; past_fixes returns Bug records for an error-text query; context_pack output ≤ 2048 chars with many hits, contains ids + record types, and empty-store returns the no-match success; a superseded record is excluded by default. Run cargo test unpiped.
- [x] 2. **Read-only git bridge tools: git_log / git_show** — New file src/tool/agent/git_read.rs. Two tools, ToolCategory::Agent, SafetyLevel::AutoRun (read-only by construction — the whole point vs the NeedsApproval git tool), taking `project_root: PathBuf`:
(1) GitLogTool `git_log` — args: optional `path` (repo-relative, validated: no leading '-', no '..', no absolute) and optional `limit` (1..=100, default 20). Runs `git log --oneline -<limit> [-- <path>]` via the same spawn pattern as git.rs run_git_impl (Command::new("git"), current_dir, kill_on_drop, CREATE_NO_WINDOW on Windows, cap_tool_output). Output `$ git log ...` + combined stdout/stderr + `[exit code: N]`.
(2) GitShowTool `git_show` — args: required `commit` (validated: reject leading '-', allow [0-9a-f]{7,40} / branch / tag / HEAD~N — regex [A-Za-z0-9._~^/]+ with no leading '-'), optional `stat: bool` (default true → `git show --stat <commit>`, else `git show <commit>`). Same spawn pattern.
Both return ToolResult with data {exit_code, stdout, stderr} like GitTool. Register both in register_agent_tools (src/agent/factory.rs ~:659, next to GitTool, using &self.project_root). Update the tool-list test.
Tests in git_read.rs tests mod (reuse the init_repo pattern from git.rs:1081 — tempdir + git init -b main + commits; skip gracefully when git unavailable): git_log lists the commits newest-first with the expected subjects; git_log with a path filter narrows; git_show --stat shows the commit; option injection rejected (`--output=x` commit arg → error, never executed); path validation rejects `..` and absolute paths; limit bounds the log. Run cargo test unpiped.
- [x] 3. **Prompt enforcement: MANDATORY habits in the stable head** — src/agent/prompt.rs: extend TOOL_STRATEGY (const at :159) with two MANDATORY lines (the memory_supersede habit already lives in MEMORY_RECORDS at :216 — do NOT duplicate it):
- `- context_pack: MANDATORY before planning non-trivial work — pull the pointer-first bundle for the topic (past plans, reviews, bugs, decisions) so the plan builds on recorded knowledge, not fresh guesses.`
- `- past_fixes: MANDATORY before diagnosing any bug — search recorded BUG: records with the error text first; a past root cause + fix + regression test may already exist.`
Keep each line one sentence (the head is prefix-cached — LEAN).
Tests in prompt.rs tests mod: stable_head_carries_phase3_habits — build_stable_head contains 'context_pack' and 'past_fixes' with MANDATORY phrasing, and the lines appear after the TOOL STRATEGY header; the existing MEMORY_RECORDS block still carries memory_supersede (no duplicate 'context_pack' mention outside TOOL_STRATEGY). Run cargo test unpiped.
- [x] 4. **Full verification + step marker (no commit)** — cargo test unpiped — green + warning-free (#![deny(warnings)]; fix root causes, never #[allow]). cd src-tauri; cargo build (bin crate compiles separately). cd frontend; npm test (unchanged this phase — must stay green). memory_write a STEP MARKER (working tier): plan 1c9ba6c8 Phase 3 done, files changed, next action = Phase 4 sub-plan (bug_fixing plan kind + finish-step auto-capture). Do NOT commit — the root orchestrator reviews + commits the whole feature at the end.
