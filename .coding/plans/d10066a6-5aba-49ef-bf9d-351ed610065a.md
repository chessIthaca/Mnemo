# Plan: Make memory_write + plan-write tools always AutoRun

## Goal
Make memory_write, complete_step, and abandon_plan SafetyLevel::AutoRun so they never prompt for approval, regardless of safety mode. Update module docs and the agent.md constitution note to match. Remove the now-redundant memory_write rule from .coding/safety.toml.

## Context
These tools are pure bookkeeping: memory_write writes to the project's own memory store (sandboxed), complete_step/abandon_plan only mutate .coding/ plan state (sandboxed, non-destructive to user code). They should never block on approval. create_plan/update_plan are already AutoRun. The backlog IPC (backlog_add etc.) is UI→Tauri with no approval gate, so nothing to change there. Core git merge/push never_auto_for stays untouched. Also clean up the now-redundant memory_write regex rule in .coding/safety.toml.

## Steps
- [x] 1. In src/tool/memory/mod.rs: change MemoryWriteTool::safety() to SafetyLevel::AutoRun and update the module doc comment (line 4) to say memory_write is auto-run.
- [x] 2. In src/tool/workflow/plan.rs: change CompleteStepTool::safety() and AbandonPlanTool::safety() to SafetyLevel::AutoRun, and update the module doc comment (lines 7-9) to reflect that all four plan tools are AutoRun.
- [x] 3. Update src/tool/mod.rs doc comment (line ~6) and any test-fixture expectations that asserted complete_step/memory_write need approval — check src/agent/tests.rs and src/tool/mod.rs tests.
- [x] 4. Remove the redundant [[rule]] memory_write block from .coding/safety.toml (lines 68-70).
- [x] 5. Update agent.md constitution: note that memory_write + plan-lifecycle tools are always AutoRun (bookkeeping), keeping the core git merge/push approval gate intact.
- [x] 6. Run cargo test and confirm it passes.
- [x] 7. Commit the changes to the current feature branch.
