# Plan: Memory UX polish: tab staleness + lane-written knowledge surfacing + auto-recall store version

## Goal
Close three memory-UX gaps: (1) the Memory tab's record list re-fetches when a poll observes a tier-count change (mid-session writes appear within one poll cycle); (2) the run-all completion note "lane wrote N knowledge record(s) — uncommitted in the main tree" surfaces after a run whose lanes wrote knowledge files, persisting until the next run; (3) a store version counter (bumped on every mutating store op) invalidates the per-turn auto-recall cache so a same-turn memory_write is visible to the next recall. No ranking or store-behavior changes beyond the counter.

## Kind
implementation

## Context
Backlog 3ed2d537, implementing suggestions 1/3/4 of .coding/reviews/2026-09-08-full-review-memory.md. Verified code: (1) MemoryDebugView.tsx — the overview polls every POLL_MS via setInterval (:394-414, tick → memoryDebugOverview → setOverview); the record list fetches ONLY on [tierFilter] change (:416-434, local `load` fn) and via the manual refreshList (:456-465); imports have useEffect/useMemo/useState only. (2) Auto-recall cache — TurnState.last_recalled_user_query + last_recall_results (src/agent/turn.rs:126/:128, doc at :120-125 says "Invalidated ONLY on summarize"), init :147-148, cleared on summarize :1771-1772, cache-hit check :1855 `state.last_recalled_user_query.as_deref() == Some(q)`, fresh-recall stores :1862-1863 and :1873-1874 (two branches). MemoryStoreTrait (src/memory/mod.rs:135, async fns) — impl MemoryStoreTrait for MemoryStore at :1237 (write :1238, update_memory :1535, supersede_memory :1595, delete_memory :1649); struct MemoryStore at :392. (3) Run-all — NO completion summary exists today: end_run (src-tauri/src/ipc/run_all.rs:242-245) just clears state.backlog.run_all + emits backlog-changed; RunAllState (state.rs:236-254: stop/current_item/done/total/concurrency/spawned — no timestamps) is created at THREE sites (backlog_cmds.rs:555, :633, run_all.rs:1179); RunAllProgress (backlog_cmds.rs:88-110: active/done/total/compacting/concurrency/spawned) is the wire shape the UI polls; the UI renders the progress line only while runAll.active (BacklogView.tsx:1069-1080) so a note must live OUTSIDE the run state to persist after completion. The knowledge-file write success path in src/tool/memory/mod.rs (the same branch as the Me4 lane note) is where a process-global write counter belongs — the store is factory-shared, so lane + main writes both count, and a run-window diff isolates the run's writes.

## Steps
- [x] 1. **Memory tab: piggyback list re-fetch on count change**
- [x] 2. **Memory tab: piggyback list re-fetch on count change** — frontend/src/components/views/MemoryDebugView.tsx: (a) import useCallback + useRef alongside the existing react imports. (b) Extract the list fetch from the [tierFilter] effect (:416-434) into `const loadList = useCallback(async () => { ...memoryDebugList(tierFilter === "all" ? null : tierFilter, 200)... }, [tierFilter])` and call it from that effect (keeping the cancelled-flag cleanup). (c) Add `const lastCountsSig = useRef<string | null>(null);`; in the overview tick (:394-414), after setOverview(ov), compute `const sig = JSON.stringify(ov.counts)`; when `lastCountsSig.current !== null && lastCountsSig.current !== sig`, call `void loadList()` (a comment: records written mid-session — by the agent or a consolidation — otherwise do not appear until a manual refresh); then set lastCountsSig.current = sig. The first poll only sets the baseline (the tierFilter effect already fetched on mount). (d) Add the tick effect's dep: [loadList]. (e) Vitest: add a source-assertion test (the ModelsSection.test.tsx pattern — read the source, assert the contract) in a MemoryDebugView test file (create frontend/src/components/views/MemoryDebugView.test.ts if none exists): assert the source contains the counts-signature piggyback ("lastCountsSig" + "JSON.stringify(ov.counts)") and that loadList is reused ("void loadList()").
- [x] 3. **Auto-recall store version counter**
- [x] 4. **Auto-recall store version counter** — (a) src/memory/mod.rs MemoryStoreTrait (~:135): add `fn version(&self) -> u64 { 0 }` with a doc comment (the store's mutation counter — bumped on every recall-result-changing write; the default 0 keeps test mocks inert). (b) struct MemoryStore (:392): add `store_version: std::sync::atomic::AtomicU64` (init 0 at every construction site — search `MemoryStore {` / `MemoryStore::new`); override `fn version(&self) -> u64 { self.store_version.load(Ordering::Relaxed) }` in the impl (:1237); bump `self.store_version.fetch_add(1, Ordering::Relaxed);` at the top of write (:1238), update_memory (:1535), supersede_memory (:1595), and delete_memory (:1649) — with a one-line comment (invalidates per-turn auto-recall caches). (c) src/agent/turn.rs TurnState: add `last_recall_store_version: u64` next to the cache fields (:126-128) with a doc comment; init 0 (:147-148); reset to 0 in the summarize clear (:1771-1772). (d) The cache-hit condition (:1855) becomes `state.last_recalled_user_query.as_deref() == Some(q) && state.last_recall_store_version == store.version()`; BOTH fresh-recall branches (:1860-1864, :1870-1876) also store `state.last_recall_store_version = store.version();`. (e) Update the cache doc comment (:120-125): invalidated on summarize OR a store write (the version counter — a same-turn memory_write is visible to the next recall). (f) Test: follow the existing auto-recall cache test pattern (search last_recalled_user_query in src/agent/tests.rs) — prime the cache with query Q, write a memory via the store, re-run auto_recall with the same Q → a FRESH recall fires (the new record appears / the MemoryRecalled event re-fires), asserting the version check invalidated the cache.
- [x] 5. **Run-all completion note: lane-written knowledge**
- [x] 6. **Run-all completion note: lane-written knowledge** — (a) src/tool/memory/mod.rs: add `pub static KNOWLEDGE_WRITES: std::sync::atomic::AtomicU64 = std::sync::atomic::AtomicU64::new(0);` with a doc comment (total knowledge-file writes since process start, all agents — the store is factory-shared; the run-all completion note diffs this across the run window); bump it (`KNOWLEDGE_WRITES.fetch_add(1, Ordering::Relaxed);`) in the knowledge-backed write success path (where the file lands + the Me4 lane note is appended). (b) src-tauri/src/ipc/state.rs RunAllState (:236): add `pub knowledge_writes_at_start: std::sync::atomic::AtomicU64` (doc: the counter snapshot at run start — end_run diffs it to count the run's knowledge writes); initialize it at ALL THREE creation sites (backlog_cmds.rs:555, :633, run_all.rs:1179) with `knowledge_writes_at_start: std::sync::atomic::AtomicU64::new(mnemo::tool::memory::KNOWLEDGE_WRITES.load(Ordering::Relaxed))`. (c) IpcState (state.rs, next to run_all :198): add `pub run_completion_note: Arc<std::sync::Mutex<Option<String>>>` (init None at the IpcState construction site — search where the other Arc fields are initialized). (d) end_run (run_all.rs:242): BEFORE clearing the run state, snapshot it, diff `KNOWLEDGE_WRITES.load(...) - knowledge_writes_at_start.load(...)`; when > 0, set run_completion_note to format!("lane wrote {n} knowledge record(s) — uncommitted in the main tree"); then clear the run state + emit as today. (e) The run-start sites (the three creation sites) also clear run_completion_note (a new run resets the note). (f) RunAllProgress (backlog_cmds.rs:88): add `pub note: Option<String>` (doc: the last run's completion note — lane-written knowledge surfacing; persists until the next run starts); populate it in the RunAllProgress builder (search `RunAllProgress {` in backlog_cmds.rs) from the slot. (g) frontend BacklogView.tsx (~:1069-1080): when `runAll.note` is present, render it as a small muted line near the run controls (e.g. below the progress line / next to the Run All button) — it persists after the run ends because the slot outlives the run state. (h) tauri.ts RunAllProgress interface: add `note: string | null;`. (i) Vitest: extend the BacklogView source-assertion test (BacklogView.test.ts:239 pattern) — assert the note render ("runAll.note") exists.
- [x] 7. **Verify: cargo ×2 + vitest green**
- [x] 8. **Verify: cargo ×2 + vitest green** — Run `cargo test` at the repo root (mnemo lib — parts 2+3's lib changes) AND `cargo test` in src-tauri (bin — the run-all/state changes); check the `test result:` lines. Run `npm test --workspace frontend` (vitest — the MemoryDebugView + BacklogView tests). All green + warning-free. Then the closing sequence (reviewer → fix findings → commit) runs automatically.
