# Plan: Backlog aec6cc8a: bare memory_search cards — parse browse-mode results (bug)

## Goal
Fix bare memory_search activity cards: browse-mode results (prefix/tier-filtered) and their zero-hits show tier, first-row title, and the count ("N matched" / "no matches") like search cards; prefix-only searches get a query label.

## Kind
bug_fixing

## Context
Backlog aec6cc8a dispatch (run-all): "sometimes memory search is empty... I guess you didn't find anything. Maybe we add that info then." ROOT CAUSE (verified): memory_search BROWSE mode (no query, retrieval.rs:176-204) emits a different wire shape than search mode — header "N memories (newest first):" (:183), rows "{id}  {tier}  {record_type}  {YYYY-MM-DD}  {title}[ [superseded]]" (:190-198, double-space separated, no brackets/score), zero-hit "no memories match the filter" (:181). parseMemoryEntry (agentEventReducer.ts:390-433) only parses "matched:" headers (:431) and "[tier] title (id:…, score:…)" lines (:401/:415) → browse cards get tier/title/matched/hits all undefined → bare rows; browse zero-hits also miss the "no matches" chip. Zero-hit SEARCH already chips "no matches" ("no memories matched the query" parses) — gap is browse-only. memorySearchLabel (toolCardPaths.ts:299, tests :150-179) covers query/tier but prefix-only args may yield null (verify; explains the missing label in the screenshot). DECISION: FRONTEND-ONLY fix — extend parseMemoryEntry with the browse shapes (header regex incl. zero-hit phrasing; browse-row regex for tier/title/hits, score left undefined so the card omits the score chip) + extend memorySearchLabel for prefix/record_type-only if null. Rationale: the browse wire format is the stable agent-facing contract (backend reshaping risks the model's parsing + Rust browse tests for a UI-labeling ask); the parser already owns format tolerance (legacy score lines, [superseded] suffixes). The 41f39672 batch made the card render/expand hits — this round makes browse results populate them.

## Steps
- [x] 1. **Reproduce with failing regression test** — Write a regression test that reproduces the defect (constitution: every defect gets a regression test that fails without the fix and passes with it). Run it and confirm it FAILS.
- [x] 2. **Document root cause** — Investigate and document the root cause. memory_write a BUG: record (symptom → root cause → fix + regression test name, ≤600 chars).
- [x] 3. **Minimal fix via sub-plan** — Push a sub-plan (create_plan — it becomes the active plan and pops back to this one when it completes) whose steps implement the fix with the LEAST amount of change possible; do not refactor unrelated code. Write it for a lesser model to execute alone: every step self-contained (explicit file paths, exact edits, the root cause + failing test name as context).
- [x] 4. **Verify** — Run the regression test + the full test suite (cargo test unpiped, warning-free). Record the regression test name via update_plan (regression_test field) — finish is blocked without it.

## Bug
memory_search activity cards render completely empty (no title, no count, no hit list) for browse-mode searches — prefix/tier/record_type-filtered calls without a query — leaving "found 10" indistinguishable from "found nothing" (user screenshot; reproduced in-session by the SHIPPED/ACTIVE:/STEP MARKER: prefix sweeps).

## Regression test
frontend/src/hooks/agentEventReducer.memory.test.ts
