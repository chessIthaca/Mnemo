# Plan: Status bar keeps the streaming status through mid-stream stalls (match the trace graph)

## Goal
The status bar's live phase must match the trace graph's attribution: a mid-stream stall is byte-silence INSIDE the generate window (stall_ms ⊂ generation_ms, decision f2b62d85), so the status bar keeps showing "reasoning…"/"answering…" through a stall instead of flipping to "waiting…". Stall stays visible only post-hoc as the red hatch overlay in the trace graphs. This removes the STALL_PHASE_AFTER=10s phase-flip timer from the turn loop (plan 6e734ccb step 7); the recorded stall_ms metric (StallTracker, 2s, provider layer) is untouched.

## Kind
implementation

## Context
User request (2026-12-31, arrived mid merge_to_main — merge paused via abandon_skill; this fix lands on the same wt/* branch first, then the merge is re-run). The mismatch: src/agent/turn.rs stream select has a resettable STALL_PHASE_AFTER=10s timer; on fire while streaming it emits PhaseKind::Waiting and takes stream_phase so the next delta re-emits Streaming. The trace graphs (f2b62d85) count that same silence as generate. Status bar = InflightBar phaseLabel (frontend/src/components/chat/InflightBar.tsx:31-54) — frontend needs NO change (it renders Phase events; Waiting still exists for the pre-first-byte window). Regression test to rewrite: mid_stream_stall_flips_phase_to_waiting (src/agent/tests.rs:1020, StallingProvider :991, paused clock). Docs to amend: channels.rs PhaseKind::Waiting doc, README.md inflight bullet (~:60), .coding/knowledge/spec/2026-12-30-trace-phase-attribution-connect-backoff-stall-sp.md ("Inflight bar" sentence), .coding/knowledge/decision/2026-12-30-trace-graphs-stall-counts-as-generate-with-a-dit.md (point 15 — now superseded).

## Steps
- [x] 1. **turn.rs: remove the stall→Waiting phase flip** — In src/agent/turn.rs's stream select loop: (a) delete the STALL_PHASE_AFTER const, the stall_timer binding, and its tokio::pin! (lines ~1059-1071); replace that comment block with the new rationale: a mid-stream stall deliberately keeps the current streaming phase — the trace graphs count stall_ms as part of the generate window (decision f2b62d85), so the live status bar matches; stall stays visible post-hoc as the trace overlay (StallTracker 2s threshold, provider/mod.rs). (b) Delete the timer re-arm at the top of the event arm (lines ~1079-1083, "Any event proves the stream is alive…"). (c) Delete the entire `_ = &mut stall_timer =>` select arm (lines ~1294-1316). Keep stream_phase + the emit-on-change logic and everything else untouched. Build must stay warning-free (#![deny(warnings)] — no dead remnants).
- [x] 2. **Rewrite the regression test** — src/agent/tests.rs: rename mid_stream_stall_flips_phase_to_waiting (~:1020) → mid_stream_stall_keeps_streaming_phase; keep StallingProvider/StallingStream (one delta, 11s silence, second delta, Finish; paused clock auto-advances); assert the emitted phases are exactly [Sending, Waiting, Streaming] — the 11s silence emits NOTHING (no Waiting flip, no re-emitted Streaming). Update the doc comment: the status bar matches the trace graph (stall ⊂ generate), so the phase stays Streaming through byte-silence. This fails on the pre-fix code ([S, W, Streaming, Waiting, Streaming]) and passes after.
- [x] 3. **Docs sync** — (a) src/runtime/channels.rs PhaseKind::Waiting doc (~:372-377): append that mid-stream stalls deliberately do NOT flip to Waiting — they keep Reasoning/Streaming, matching the trace graphs' stall⊂generate attribution. (b) README.md inflight-bar bullet (~:60): note a mid-stream stall keeps showing reasoning…/answering… (consistent with the trace graphs, where stall is part of generate and renders as the red hatch overlay); waiting is pre-first-byte only. (c) Check PLAN.md for any stall-flip mention; update if stale. (d) Amend .coding/knowledge/spec/2026-12-30-trace-phase-attribution-connect-backoff-stall-sp.md (the "Inflight bar:" sentence) and .coding/knowledge/decision/2026-12-30-trace-graphs-stall-counts-as-generate-with-a-dit.md (point 15, "inflight timeline INTENTIONALLY unchanged") to record the supersession: 2026-12-31 user request — the status bar matches the trace graph, ignoring stall.
- [x] 4. **Test, review, commit, finish** — (1) cargo test unpiped from the repo root; read $LASTEXITCODE (0, zero warnings). No frontend change → npm not required (verify git status --short shows only src/agent/turn.rs, src/agent/tests.rs, src/runtime/channels.rs, README.md, the two knowledge files + .coding/ bookkeeping). (2) Spawn a read-only reviewer (role:"reviewer") over ALL uncommitted changes with a self-contained task: the goal, changed files, and focus points — timer removal complete (deny(warnings) proves no dead code), stream_phase emit-on-change untouched, the rewritten test exercises the changed path (11s silence, paused clock) and would fail on the old code, StallTracker/stall_ms recording untouched, docs synced, multi-platform neutral. END TURN after spawning; on resume read the report, fix EVERY finding, re-run cargo test. (3) Commit everything (incl. the review report) on the current wt/* branch (never main). (4) Update the stored DECISION ("trace graphs — stall counts as generate…") and SPEC ("trace phase attribution…") memory records to drop the "inflight intentionally unchanged" point (superseded), and write a semantic record of the shipped change. (5) finish with the report path; final summary ≤3 lines — then merge_to_main can be re-run for the combined branch.
