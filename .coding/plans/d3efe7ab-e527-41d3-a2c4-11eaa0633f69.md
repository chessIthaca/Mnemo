# Plan: Phase 5b — Split useAgentStore + pure reducers + vitest (Maint H3/M9, Quality M7, UI M9)

## Goal
Phase 5b: Split the 1636-line useAgentStore.ts god object into focused pure modules (per-event reducers + appearance helpers + agent-state types), keep useAgentStore.ts as a thin facade over the same Zustand store (all 80 consumers + exports unchanged), and add a table-driven vitest suite covering every SerializableAgentEvent kind (Maint H3/M9, Quality M7, UI M9). npm test must stay green.

## Context
Parent: Architecture review remediation, step 22 (Phase 5b). Findings: Maint H3 (useAgentStore.ts god object ~1636 LOC: appearance helpers 1-200, AppState 308-537, per-event reducers 705-1239, store impl 1241-1710, backlog/run-all, settings dialog state, right-panel chrome), Maint M9 (thin FE test coverage — only useAgentStore.test.ts [264 lines, legacy exported-assertion style] + types.test.ts + DiffView.test.ts + useAgentStore.preview.test.ts; recommend extracting pure reducers + table-test every SerializableAgentEvent kind), Quality M7 / UI M9 (right-panel extensibility multi-touch; event reducer correctness). Vitest IS configured (npm test = vitest run; currently 24 tests pass). 80 files import useAgentStore. Store structure verified: state slices = agents/agentNames/agentParents/activeAgent/workflowStates (agent runtime), model/provider/reasoningEffort/pricing/gitBranch/configVersion (runtime prefs), rightPanel*/disabledTabs/planVersion/toolOutputLog/lastDiff (chrome+output), safetyMode, appearance (font/theme/colors ~20 fields), backlog/autoFeed/runAll, settingsOpen/settingsSection. Per-event reducers already pure (reduceStarted, reduceTextDelta, reduceReasoningDelta, reduceToolCallStart, reduceToolCallArgDelta, reduceToolResult, reduceUsage, reduceContextUsage, reduceApprovalRequest, reduceWorkflowStateChanged, reduceStepCompleted, reduceSuggestionInjected, reducePromptDispatched, reduceSkillStarted, reduceFinished, reduceExited, reduceError, reduceChildFinished) + applyAgentEvent dispatcher + Effects interface. Approach: extract pure modules (agentEventReducer.ts with reducers+dispatcher+Effects+emptyAgentState; appearance.ts with theme/font/color helpers+defaults+ls keys; types for AgentState/etc.), keep useAgentStore as thin facade composing Zustand slices (cross-store actions may reference each other via get()), add table-driven vitest for every event kind. Risk: 80 consumers — facade must keep ALL exports (types, emptyAgentState, selectMainAgentId, applyFontVars/applyTheme/applyColors/applyCodeColors/resolveTheme/Theme, PendingApproval/TokenUsage/AgentState/RightPanelTab/ALL_RIGHT_PANEL_TABS/ToolOutputEntry/LastDiff/ActivityEntry/SteerEntry). Keep facade's create() in useAgentStore.ts re-exporting everything.

## Steps
- [x] 1. Create frontend/src/hooks/agentState.ts: move AgentState interface + emptyAgentState() + getOrCreate + selectMainAgentId + PendingApproval/TokenUsage/RequestTiming/SteerEntry/ActivityEntry/ToolOutputEntry/LastDiff types + flushStreamingText/pushTranscriptEntry/truncateOutput/truncateError helpers from useAgentStore.ts (pure, no store deps).
- [x] 2. Create frontend/src/hooks/appearance.ts: move Theme type + DEFAULT_* constants + LS_* keys + applyFontVars/resolveTheme/applyTheme/applyColors/applyCodeColors + readLs/readLsNumber/readLsNumberOrNull/writeLs + readTheme/readShowTokenUsage + setCodeColor + CodeColorKey from useAgentStore.ts (pure DOM/localStorage helpers).
- [x] 3. Create frontend/src/hooks/agentEventReducer.ts: move ALL per-event reducers (reduceStarted...reduceChildFinished) + Ev type + Reducer/ReducerResult types + Effects interface + applyAgentEvent dispatcher from useAgentStore.ts, importing types from agentState.ts. Make each reducer + applyAgentEvent exported for table-driven tests.
- [x] 4. Rewrite useAgentStore.ts as a thin facade: import { create } from zustand; import types + emptyAgentState + selectMainAgentId from agentState.ts; import appearance helpers from appearance.ts; import all reducers + applyAgentEvent + Effects from agentEventReducer.ts; keep the SAME AppState interface + all actions + the same create() store impl (initial state from appearance defaults; actions unchanged); re-export everything the 80 consumers import (all types, constants, helpers, useAgentStore, emptyAgentState, selectMainAgentId, Theme, applyFontVars, etc.) so no consumer changes.
- [x] 5. Rewrite frontend/src/hooks/useAgentStore.test.ts as a table-driven vitest suite: for EVERY SerializableAgentEvent kind, construct a fresh store state, dispatch via handleAgentEvent (or applyAgentEvent directly for purity), assert the resulting state slice. Table rows: started, text_delta, reasoning_delta, tool_call_start (new+merge), tool_call_arg_delta, tool_result (incl. diff snapshot + output log + approval clear), usage, context_usage, approval_request, workflow_state_changed, step_completed, suggestion_injected (landed steer), prompt_dispatched, skill_started, finished, exited (cross-map removal + active fallback), error (retrying vs final), child_finished. Use vitest describe/it/expect.
- [x] 6. Add a reducer-purity test module frontend/src/hooks/agentEventReducer.test.ts (optional if covered above) OR extend the main suite — ensure applyAgentEvent's Effects (toolOutputEntry, lastDiff, planVersionBump, workflowStates, agentNames, removeAgent, scheduleSteerRemoval) are each exercised by at least one table row.
- [x] 7. Run npm test (vitest) + npx tsc --noEmit; fix all failures; confirm the facade compiles and all 80 consumers still typecheck.
- [x] 8. Run cargo test (workspace) to confirm no Rust side effects; verify nothing else imports removed symbols.
- [x] 9. Spawn a read-only reviewer subagent over all uncommitted changes; it must write its report to .coding/reviews/ (file_write bug is fixed); read report, fix every finding, re-run tests.
- [x] 10. Commit Phase 5b to the current feature branch.
