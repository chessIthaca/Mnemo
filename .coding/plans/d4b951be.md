# Plan: Delete never-sent template_kwargs from policy.rs (BUG 10)

## Goal
Remove the dead template_kwargs registry from src/provider/policy.rs — the struct field, the GLM/Qwen/Kimi entries, the must-send doc claims, and the kwargs tests — replacing the tests with vendor-detection coverage plus a source-scan regression guard, and syncing PLAN.md and the reasoning-state-continuity spec record. The wire path is rejected: the vendor-policy config direction was rolled back by user decision (2027-01-09, main reset to e0ac5dd) and the param names (clear_thinking, preserve_thinking) appear in no public vendor docs, so wiring unverifiable params would risk 400s and cannot be live-verified unattended.

## Kind
bug_fixing

## Context
Symptom: policy.rs registers template_kwargs whose doc comments claim vendor must-send behavior no code performs; the params never reach any request body. Root cause: the registry predates the 2027-01-09 rollback to e0ac5dd (think_tags/vendor-policy layer removed); the wiring never existed at HEAD. DECISION — delete, don't wire: (a) the wire path is gated on the vendor-policy config direction, which the user rolled back 2027-01-09 (main reset to e0ac5dd) and is TBD; (b) neither clear_thinking nor preserve_thinking appears in public vendor docs, so wiring unverifiable params risks 400s and cannot be live-verified in unattended mode. include_params (the sibling field) is NOT dead — the Responses builder hardcodes its include value — so only template_kwargs goes. Edit sites in src/provider/policy.rs: field+doc :200-202; empty entries :336 (claude), :367 (gemini), :394 (openai_responses), :421 (deepseek), :502 (vllm_default); populated entries qwen :436-437/:444, kimi :455-456/:463, glm :475-476/:483; must-send claims :434, :453, :471-473; module-doc table extras :34-35 (→ "—"); test assert :523; kwargs tests :583-605 (rewrite as vendor-detection tests to keep Alibaba/Moonshot/Zhipu coverage). Docs: PLAN.md:330 (drop "/template_kwargs" from the follow-up mention); amend knowledge record .coding/knowledge/spec/2026-12-21-reasoning-state-continuity-raw-payload-source-of.md (RULE 2 names the field — find its memory id via memory_search, then memory_amend). REGRESSION TEST DESIGN: policy_source_carries_no_never_sent_template_kwargs in policy.rs's tests module — reads concat!(env!("CARGO_MANIFEST_DIR"), "/src/provider/policy.rs"), split_once("mod tests"), asserts the non-test source contains none of "template_kwargs"/"clear_thinking"/"preserve_thinking"; red before the fix (all present), green after; guards reintroduction. Verification: cargo test policy_source_carries (red→green), cargo test --workspace green + warning-free under #![deny(warnings)], search src/ for the identifiers (only the regression test's needles may remain).

## Steps
- [x] 1. **Reproduce with failing regression test** — Write a regression test that reproduces the defect (constitution: every defect gets a regression test that fails without the fix and passes with it). Run it and confirm it FAILS.
- [x] 2. **Document root cause** — Investigate and document the root cause. memory_write a BUG: record (symptom → root cause → fix + regression test name, ≤600 chars).
- [x] 3. **Minimal fix** — Apply the minimal fix that makes the regression test pass. Do not refactor unrelated code.
- [x] 4. **Verify** — Run the regression test + the full test suite (cargo test unpiped, warning-free). Record the regression test name via update_plan (regression_test field) — finish is blocked without it.

## Detailed steps
- **Reproduce — red source-scan regression test** — Add test policy_source_carries_no_never_sent_template_kwargs to src/provider/policy.rs's tests module: read concat!(env!("CARGO_MANIFEST_DIR"), "/src/provider/policy.rs"), split_once("mod tests") to isolate the non-test source, assert it contains none of "template_kwargs", "clear_thinking", "preserve_thinking" (doc comment cites BUG 10 + the rolled-back vendor-policy gate). Run `cargo test policy_source_carries` and confirm it FAILS (red) — the three identifiers are present in the registry.
- **Fix — delete the dead registry + stale claims** — In src/provider/policy.rs: delete the template_kwargs field + its doc comment (:200-202); delete every `template_kwargs:` line from the seven registry fns (claude :336, gemini :367, openai_responses :394, deepseek :421, vllm_default :502, and the populated qwen :436-437/:444, kimi :455-456/:463, glm :475-476/:483 — drop the HashMap construction entirely); delete the must-send doc claims (:434, :453, :471-473) and set the module-doc table extras for Qwen/Kimi and GLM rows to "—" (:34-35); remove `assert!(p.template_kwargs.is_empty())` (:523); rewrite the three kwargs tests (:583-605) as vendor-detection tests (qwen→Vendor::Alibaba + reasoning_field, kimi→Vendor::Moonshot, glm→Vendor::Zhipu + fold_volatile_tail=false) so vendor coverage is preserved. Write the BUG knowledge record (.coding/knowledge/bug/2027-01-11-*.md): symptom → root cause (registry predates the e0ac5dd rollback, wiring never at HEAD) → fix + regression test name.
- **Docs + knowledge sync** — PLAN.md:330 — drop `/template_kwargs` from the "full builder consultation of include_params/template_kwargs/reasoning_field is a follow-up" sentence (leave include_params/reasoning_field). Find the memory id for .coding/knowledge/spec/2026-12-21-reasoning-state-continuity-raw-payload-source-of.md via memory_search (query e.g. "reasoning state continuity raw payload") and memory_amend it with a dated paragraph: template_kwargs deleted 2027-01-11 (BUG 10) — never sent, names absent from public vendor docs, wire path blocked by the rolled-back vendor-policy direction; RULE 2's struct listing no longer includes the field.
- **Verify — green + full suite + sweep** — Run `cargo test policy_source_carries` → green. Run full `cargo test --workspace` → all green, 0 failed, warning-free under #![deny(warnings)] (read the `test result:` lines, not the pipe exit code). Sweep: search src/ for template_kwargs/clear_thinking/preserve_thinking — the only remaining hits must be the regression test's own needles inside mod tests. Then spawn the read-only reviewer over the uncommitted changes.

## Bug
src/provider/policy.rs template_kwargs (GLM clear_thinking: false, Qwen/Kimi preserve_thinking: true) doc comments assert vendor must-send requirements, but no request builder reads the field — the params never leave the process, and neither name appears in current public vendor docs (Zhipu documents do_sample/temperature/top_p/max_tokens/stream/thinking/reasoning_effort). Stale-claim doc/code drift + dead registry entries (2027-01-09 provider-comms review finding 4, LOW / executive summary BUG 10).

## Regression test
policy_source_carries_no_never_sent_template_kwargs
