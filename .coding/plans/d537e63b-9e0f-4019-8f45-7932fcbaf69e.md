# Plan: Remove dead parse_model_ids + enforce deny(warnings)

## Goal
Eliminate the `parse_model_ids` dead-code warning by removing the dead wrapper and migrating its tests, then make all compiler warnings fail the build via `#![deny(warnings)]` and bake the "no warnings" check into the standard plan-closing sequence and the reviewer checklist.

## Context


## Steps
- [x] 1. **Remove dead `parse_model_ids` wrapper; migrate its 6 tests onto `parse_models_with_vision`** — In `src/provider/openai.rs`: delete the `parse_model_ids` function (lines ~239-257, including its doc comment) since `fetch_models_with_vision` calls `parse_models_with_vision` directly and `parse_model_ids` is now called only by tests. Then rewrite the 6 `parse_model_ids_*` tests (lines ~1679-1736) to call `parse_models_with_vision` instead and assert on the returned `ModelWithVision` items' `.id` (and `.vision_capable` where the test already implies a value): `parse_model_ids_openai_shape` → `parse_models_with_vision_openai_shape` asserting ids `["gpt-4o","gpt-3.5-turbo","  spaced  "]` all with `vision_capable==false`; `parse_model_ids_ollama_shape` → `parse_models_with_vision_ollama_shape` asserting ids `["llama3:latest","qwen2:7b"]` both `vision_capable==false`; `parse_model_ids_openai_data_takes_precedence_when_present` → `parse_models_with_vision_openai_data_takes_precedence` asserting single id `"a"`; `parse_model_ids_empty_data_is_valid_empty_list` → `parse_models_with_vision_empty_data_is_valid_empty_list` asserting `Some(vec![])`; `parse_model_ids_empty_models_array_is_valid_empty_list` → `parse_models_with_vision_empty_models_array_is_valid_empty_list` asserting `Some(vec![])`; `parse_model_ids_unknown_shape_returns_none` → `parse_models_with_vision_unknown_shape_returns_none` asserting `None` for both bodies. Keep the existing `parse_models_with_vision_*` and `model_supports_vision_*` tests (lines ~1757+) untouched. Do NOT touch the `parse_models_with_vision` doc comment's `Mirrors [parse_model_ids]` reference — instead update that doc line to drop the cross-reference to the now-deleted function (reword to describe the shape handling directly).
- [x] 2. **Fix `ContextManager.max_tokens` dead field (context.rs:18)** — `src/agent/context.rs`: the `max_tokens` field carries `#[allow(dead_code)]` because it's stored but never read. Under `deny(warnings)` this fails. Remove the `#[allow(dead_code)]` attribute and make the field genuinely used: add a `pub fn max_tokens(&self) -> usize { self.max_tokens }` accessor with a doc comment ("The model's context-window size, in tokens."), OR if simpler, remove the field entirely and stop storing it in `new` (it's only used to compute `summarize_at`). Prefer the accessor only if a caller could plausibly need it; otherwise remove the field + its assignment in `new`. Verify with `cargo build` that no warning remains for this struct.
- [x] 3. **Fix `unused_assignments` in turn.rs:346 and openai.rs:423** — `src/agent/turn.rs:346` has `#[allow(unused_assignments)] let mut interrupted = false;` and `src/provider/openai.rs:423` has `#[allow(unused_assignments)] let mut last_chunk: Option<std::time::Instant> = None;`. Under `deny(warnings)` these fail. For each: remove the `#[allow(unused_assignments)]` attribute and restructure so the variable is either (a) genuinely read on every code path that matters, or (b) not declared `mut` / not assigned when the value is never observed. For `interrupted`: confirm whether it's ever read after assignment; if it's only set and never read, remove the variable and its assignments entirely (keep the side effects that set it, if any). For `last_chunk`: it's written every chunk and read only when Usage arrives — keep it but ensure the read path exists and compiles without the suppression; if the read genuinely only happens conditionally, restructure to avoid the `mut`+assign-without-read pattern (e.g. compute on demand or scope the binding). Verify both compile clean with `cargo build`.
- [x] 4. **Add `#![deny(warnings)]` to both crate roots** — In `src/lib.rs` (the `myharness` lib crate root) add `#![deny(warnings)]` as the first line (before the module doc comment is fine, or immediately after). In `src-tauri/src/main.rs` add `#![deny(warnings)]` after the existing `#![cfg_attr(not(debug_assertions), windows_subsystem = "windows")]` line. This makes ANY warning a hard error for both crates. Do NOT add it to test modules or individual files — crate-root level is the single source of truth. Run `cargo build` and fix any additional warnings that surface (there should be none if steps 2-3 are complete, but surface and fix any others).
- [x] 5. **Raise Vite chunk-size warning limit** — In `frontend/vite.config.ts`, inside the `build: { ... }` block (currently lines 12-20), add `chunkSizeWarningLimit: 1500,` (kB) so the existing large-bundle warning no longer fires for the current bundle size. Place it as a sibling of `target`/`outDir`/`emptyOutDir`. This addresses the `build.chunkSizeWarningLimit` warning from the user's paste. (If the bundle is dramatically over 1500kB, note it in the review but still raise the limit to clear the warning for now.)
- [x] 6. **Update agent.md closing sequence to enforce no-warnings** — In `agent.md`, under the `## Standard plan closing sequence` section, update step 1 (**Test**) to require a warning-free build: change the instruction so the agent runs `cargo build` (or `cargo test`, which compiles) and confirms there are ZERO compiler warnings before marking the step complete — any warning must be fixed, not suppressed with `#[allow]`, before proceeding. Add a sentence: "Under `#![deny(warnings)]` any warning fails the build; never add `#[allow(...)]` to silence a warning — fix the root cause." Also update step 2 (**Review**) reviewer checklist to explicitly include "no compiler warnings (the build must be warning-free under `#![deny(warnings)]`)" as a constitution-compliance check the reviewer must verify.
- [x] 7. **Test, review, commit** — Run `cargo build` (confirm zero warnings — under deny(warnings) this is enforced) and `cargo test` (confirm all migrated `parse_models_with_vision_*` tests pass + the untouched vision tests pass + nothing else regressed). Run `cd frontend && npm run build` to confirm the Vite config change is valid and the chunk-size warning is gone. Then spawn a read-only reviewer subagent over ALL uncommitted changes (`git diff HEAD`): list the changed files (src/provider/openai.rs, src/agent/context.rs, src/agent/turn.rs, src/lib.rs, src-tauri/src/main.rs, frontend/vite.config.ts, agent.md) and the plan's goal (remove dead parse_model_ids + enforce deny(warnings) + bake into closing sequence); ask for correctness/bugs/security/constitution-compliance findings — specifically that the migrated tests preserve the original assertions' intent, that the `#[allow]` removals didn't change runtime behavior, that `deny(warnings)` is applied at both crate roots, and that agent.md's closing sequence now mandates a warning-free build. Have it write the report to `.coding/reviews/`. Read the report, fix every finding, re-run `cargo test` + `npm run build`, then commit to the current feature branch with a clear message including the review report.
