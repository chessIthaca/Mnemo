# Plan: Click-to-edit a pending steer (draft mode) — verify the X half, add the edit affordance

## Goal
Click-to-edit a pending (not-yet-landed) steer (user request 2026-12-30, backlog item): click a pending steer bubble in the InputBar backlog and edit its text before it lands; once the steer lands (suggestion_injected), editing is no longer possible. Draft-mode design: clicking the pending steer's text loads it into the main input (the edit surface) and cancels the pending steer FULL-STACK immediately (the exact X flow: removeSteer + cancelSuggestion) — sending then re-queues the edited text as a fresh steer via the existing steer branch. First verify the already-shipped X-to-delete half still renders and works (the user asked for it as if missing), then add click-to-edit, pin both with source-contract tests, and run the full verification (frontend vitest + tsc typecheck + both Rust suites).

## Kind
implementation

## Context
Verified current state: (1) The X-to-delete half SHIPPED and renders — InputBar.tsx:697-713 wires removeSteer + cancelSuggestion (lib/tauri.ts:63) → AgentCommand::CancelSuggestion (backend arm + 5 unit tests + integration test per SPEC .coding/knowledge/spec/2026-08-30-cancel-delete-a-scheduled-steer-via-the-x-on-eac.md, commit 5d9e253). (2) The send flow: steerMode (running) → pushPrompt + addSteer + sendSuggestion (InputBar.tsx:307-316) — the re-queue path a draft-mode edit reuses. (3) SteerEntry type at frontend/src/hooks/agentState.ts:138 (id, text, status). (4) The store needs NO changes (addSteer/removeSteer exist; suggestion_injected lands + auto-removes steers, useAgentStore.ts:1101-1126). (5) The frontend test pattern: source-contract tests via vitest + Vite ?raw imports, node env, no React DOM infra (App.shellRender.test.ts, Conversation.layout.test.ts) — the established way to pin component wiring. (6) InputBar.tsx imports from lucide-react (Compass, X, MessageCircleQuestion, ImageIcon, Play, Square, Send) — add Pencil. DESIGN DECISIONS (to document): (a) Draft mode over inline editing — the main input is the natural editing surface (full keyboard, history); reuses 100% of the existing machinery (zero backend changes, zero new store actions); the cramped one-line truncated bubble is a poor inline-editing surface. (b) Cancel-at-click (not swap-on-send) — eliminates the mid-edit-landing hazard BY CONSTRUCTION (the backend queue no longer holds the steer, nothing can land mid-edit) and leaves no edit state to go stale after injection; the trade-off (an accidental click cancels the steer) is mitigated because the text sits in the input, one Enter away from re-queuing. (c) Click overwrites any input draft — consistent with the existing Up-arrow history-navigation precedent (handleKeyDown ArrowUp → setText(shown), InputBar.tsx:627-639). (d) The text-based queue limitation (two pending steers with identical text are both cancelled by one cancel) is pre-existing and documented in the SPEC — re-documented in the edit handler's comment. (e) The pending steer's TEXT becomes the edit button (type="button", sibling of the X — valid HTML, no nested buttons); landed steers keep the plain span + "landed" label (no edit affordance — editing impossible once landed, by construction, and they auto-remove after fading).

## Steps
- [x] 1. **Verify the X-to-delete half still renders and works** — Code-level verification (already read this session, re-confirm): InputBar.tsx:697-713 — the pending-steer branch renders the X button (title="Cancel this steer") whose onClick calls removeSteer(activeAgent, steer.id) + cancelSuggestion(activeAgent, steer.text); lib/tauri.ts:63 wraps the backend cancel_suggestion command; the backend AgentCommand::CancelSuggestion arm + its unit/integration tests exist per the SPEC (commit 5d9e253). Document the verification result in the plan file / final summary (the user asked for the X as if missing — the answer is: it shipped and works; the new source-contract test in step 3 pins it durably).
- [x] 2. **Add click-to-edit (draft mode) in InputBar.tsx** — In frontend/src/components/layout/InputBar.tsx: (1) import Pencil from lucide-react and the SteerEntry type from ../../hooks/agentState; (2) extract a local cancelPendingSteer(steer: SteerEntry) helper (removeSteer + cancelSuggestion with the existing error handling) used by BOTH the X button and the new edit click; (3) make the pending steer's text span a <button type="button"> — className "min-w-0 flex-1 cursor-pointer truncate text-left transition-colors hover:text-amber-300", title "Edit this steer — loads it into the input; send re-queues it", containing the InlineMarkdown + a faint Pencil affordance (h-2.5 w-2.5, opacity-40 group-hover:opacity-100; add "group" to the pending bubble's class string) — whose onClick: setText(steer.text) (overwrites any draft — the Up-arrow history precedent), resetNavigation(), cancelPendingSteer(steer), textareaRef.current?.focus(); the comment documents the draft-mode design (cancel-at-click eliminates the mid-edit-landing hazard by construction; accidental click is recoverable — the text sits in the input; the text-based queue cancels ALL identical-text steers — pre-existing limitation); (4) the landed branch stays a plain span + "landed" label (no edit affordance — editing impossible once landed); (5) the X button keeps its own onClick (now via the helper) — sibling of the text button, no propagation concern.
- [x] 3. **Source-contract tests (InputBar.steerEdit.test.ts)** — Create frontend/src/components/layout/InputBar.steerEdit.test.ts mirroring the established ?raw pattern (App.shellRender.test.ts / Conversation.layout.test.ts — vitest node env, no React DOM): import inputBarSource from "./InputBar.tsx?raw". Assertions: (a) the pending steer's text is an edit button — the source contains the edit title ("Edit this steer") and its handler loads the text (setText(steer.text)) and cancels full-stack (cancelPendingSteer / removeSteer + cancelSuggestion); (b) the X cancel still renders and wires — title="Cancel this steer" + the cancelSuggestion call (the verification pin for the shipped half); (c) landed steers are NOT editable — the landed branch renders the "landed" span with no button/onClick (editing impossible once landed); (d) the draft-mode contract — the edit handler focuses the textarea (textareaRef.current?.focus()) and resets navigation (resetNavigation()).
- [x] 4. **Verify — frontend tests + typecheck + both Rust suites** — Run, reading $LASTEXITCODE after each (PowerShell, unpiped): (1) cd frontend; npm test (vitest run — the new source-contract tests + all existing frontend tests must pass); (2) npx tsc --noEmit (the build script's typecheck half — the ?raw imports are typed by vite/client); (3) back at the root: cargo test and cargo test --manifest-path src-tauri/Cargo.toml (frontend-only change, but the constitution requires the Rust suites before completing steps — both must stay green: root 2031/0/4, src-tauri 233/0).
