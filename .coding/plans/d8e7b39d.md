# Plan: Reviewer spawns run on the configured reviewing model — ad-hoc per-reviewer model picks denied (backlog c8e48f81)

## Goal
Reviewer subagents must run on the configured reviewing model — no ad-hoc per-reviewer model picks. Fix = guidance (spawn_agent schema + doc comment + the prompt's closing-sequence/Reviewing reviewer-spawn text all say OMIT the model; the configured reviewing model is authoritative) + structural enforcement (a reviewer spawn carrying an explicit model is denied at dispatch unless it is the user-sanctioned failed-reviewer retry: reviewer failed without a report → ask_user → retry on the user-picked model — that path keeps working via a reviewer_retry_sanctioned flag opened by ask_user only when a failure was pending, consumed by any reviewer spawn). Regression tests: (a) pin existing no-model → configured-reviewing-model behavior (tests exist, keep green); (b) ad-hoc reviewer+model denied; (c) the sanctioned retry allowed + sanction consumed.

## Kind
bug_fixing

## Context
Investigation (2026-01-03): ROOT CAUSE — the spawn_agent schema (src/tool/agent/spawn_agent.rs:166-181) says a reviewer runs on the configured reviewing model "unless `model` is set" and the doc comment (:69-82) says "An explicit `model` arg always wins"; nothing discourages ad-hoc per-reviewer picks (the 2026-12-30 plan 72329f2c agent invented "model diversity": glm-5.2/glm-5.3-gcp/deepseek-v4-flash; the deepseek reviewer failed with no report). FIX DESIGN — latch-sanctioned gate: (1) src/agent/loop_impl.rs: add reviewer_retry_sanctioned: Mutex<bool> + getter/setter next to reviewer_failure_pending (:1264-1282, same pattern); (2) src/agent/dispatch.rs ask_user interception (:223-239): when reviewer_failure_pending() is true, set_reviewer_retry_sanctioned(true) BEFORE clearing the latch (that ask_user IS the failed-reviewer protocol's user consultation — the retry may carry the user-picked model); (3) dispatch.rs reviewer-spawn block (:241-265): keep the pending-latch denial first; then for ANY reviewer spawn read + consume the sanction (set false — any reviewer spawn closes the retry window so a lingering sanction, e.g. after the user chose "abandon", can't authorize a later ad-hoc pick); if the call carries a non-empty explicit `model` arg (trim, matching the tool's semantics at :227-242) AND the sanction wasn't set → deny with guidance (omit the model; the configured reviewing model [reviewing → executing → subagent → default] is authoritative; an explicit model is sanctioned only via the failed-reviewer protocol: reviewer failed without a report → ask_user → retry on the model the user picks). REJECTED ALTERNATIVES: "ignore the explicit model" breaks the ask_user retry path (needs explicit models); "surface for approval" — spawn_agent is already NeedsApproval and auto-approve modes would still silently bypass. GUIDANCE: spawn_agent.rs schema model+role descriptions + doc comment; prompt.rs :115 (closing-sequence step 2) + :297-298 (Reviewing block) add "Omit the model param — the configured reviewing model is authoritative"; pin tests at prompt.rs :866-867/:1405-1406 may need updating. EXISTING PINS (regression (a), keep green): spawn_agent.rs reviewer_without_model_arg_pins_reviewing_model (:842), reviewer_falls_back_to_executing_model (:873), reviewer_without_any_configured_model_gets_default (:958). UI spawn path (main.rs:386) bypasses dispatch — user-initiated, sanctioned by definition, unaffected. Dispatch test module: src/agent/dispatch.rs:924+.

## Steps
- [x] 1. **Reproduce with failing regression test** — Write a regression test that reproduces the defect (constitution: every defect gets a regression test that fails without the fix and passes with it). Run it and confirm it FAILS.
- [x] 2. **Document root cause** — Investigate and document the root cause. memory_write a BUG: record (symptom → root cause → fix + regression test name, ≤600 chars).
- [x] 3. **Minimal fix** — Apply the minimal fix that makes the regression test pass. Do not refactor unrelated code.
- [x] 4. **Verify** — Run the regression test + the full test suite (cargo test unpiped, warning-free). Record the regression test name via update_plan (regression_test field) — finish is blocked without it.

## Bug
Main agent spawned three parallel role:reviewer subagents each on a DIFFERENT explicit model (glm-5.2 arch-perf, glm-5.3-gcp security, deepseek-v4-flash quality — plan 72329f2c, 2026-12-30); the deepseek-v4-flash reviewer failed outright without writing a report (DeepSeek endpoint fragility; recovery needed ask_user + respawn). Per-reviewer model assignment is configured nowhere — the agent invented it as "model diversity". Expected: all reviewers run on the configured reviewing model (GLM-5.3 here; chain: reviewing → executing → subagent → default), which exists to prevent exactly this failure.

## Regression test
reviewer_spawn_gate_denies_ad_hoc_model_picks
