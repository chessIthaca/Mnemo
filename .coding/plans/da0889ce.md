# Plan: Fix GLM-5.3 stop boundaries: inject explicit stop sequences + stop_token_ids

## Goal
GLM-5.3/5.3-Flash requests carry the model's new tokenizer stop boundaries explicitly — "stop": ["", "", "", "\n\n\n"] and "stop_token_ids": [151329, 151330, 151336] — so backend servers that don't enforce the boundaries stop the stream at the first boundary the model emits instead of running past a closing code block or reasoning tag into thousands of empty lines.

## Kind
bug_fixing

## Context
The request-body builder is src/provider/openai.rs::build_request_json (:1324, body assembled at :1606-1612 as `let mut body = serde_json::json!({...})`, conditional fields inserted after via `body["field"] = serde_json::json!(...)` — the pattern: tools :1613-1614, tool_choice :1616-1627, reasoning_effort :1629-1634, then `Ok(body)` :1636). The injection goes right before Ok(body), conditioned on `self.config.model.to_ascii_lowercase().starts_with("glm-5.3")` (covers glm-5.3 + glm-5.3-flash on z.ai AND Ollama Cloud — both are OpenAI-kind and go through this builder; anthropic.rs is untouched — no GLM there). The exact payloads per the backlog ask (reconstructed): `stop` = ["", "", "", "\n\n\n"] (the three role-tag boundaries + the empty-line cascade), `stop_token_ids` = [151329, 151330, 151336] (the same boundaries' token ids — belt+suspenders: servers that ignore stop_token_ids get the string list, and vice versa). Tests mirror build_request_json_includes/omits_reasoning_effort_when_set (:3536-3585) — the client-construction + build_request_json + body-asserts pattern. Docs: PLAN.md's numbered provider-quirks list (:214-220, items 1-7) gains the GLM-5.3 stop-boundary quirk; the build_request_json doc comment notes the injection. README has no user-facing provider-config change (the injection is transparent; endpoints.toml needs no new keys) — document that decision for the reviewer. Suites baseline: cargo 1883+16 / 0 failed; vitest 727 / 51 files (backend-only change — vitest re-run anyway at close). The stop strings contain <...> tag shapes — plain Rust string literals, no escaping hazards.

## Steps
- [x] 1. **Reproduce with failing regression test** — Write a regression test that reproduces the defect (constitution: every defect gets a regression test that fails without the fix and passes with it). Run it and confirm it FAILS.
- [x] 2. **Document root cause** — Investigate and document the root cause. memory_write a BUG: record (symptom → root cause → fix + regression test name, ≤600 chars).
- [x] 3. **Minimal fix** — Apply the minimal fix that makes the regression test pass. Do not refactor unrelated code.
- [x] 4. **Verify** — Run the regression test + the full test suite (cargo test unpiped, warning-free). Record the regression test name via update_plan (regression_test field) — finish is blocked without it.

## Bug
GLM-5.3-Flash outputs a closing code block or reasoning tag followed by thousands of empty lines: the backend server ignores the model's new tokenizer boundaries because the request never carries the explicit GLM-5.3 EOS stops (stop sequences + stop_token_ids 151329/151330/151336). Backlog 82a9480c-7f02-42ba-bcb2-5b25039d5716. Note: the backlog row's text is emission-mangled (three <...>-shaped tags stripped to empty strings) — the reconstruction is grounded in the token ids: 151329/151330/151336 are GLM's user/assistant/observation boundary tokens, so the intended stop list is ["", "", "", "\n\n\n"].

## Regression test
build_request_json_injects_glm_stop_boundaries_for_glm_53
