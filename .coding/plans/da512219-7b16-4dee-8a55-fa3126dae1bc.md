# Plan: Address all actionable findings from the 2026 four-perspective review

## Goal
Address every actionable finding from the 2026 four-perspective review (.coding/reviews/2026-consolidated-review.md) in priority order. Each fix is scoped to be independently shippable with tests passing. Findings span: type-safety (H2), concurrency correctness (H4), test coverage (H7), UX (H1, H6, M3, M4), dead code (M9), duplication (M2), frontend store refactor (M5), fan-in throughput (M6), spec/docs (M7, M8, L1-L8 doc-only), and the two large refactors (shadcn/Radix adoption H1, AgentLoop god-object split M1, memory connection pool H3). The two large refactors + memory pool are scoped as their own steps and may be deferred by the user.

## Context
Source of truth: .coding/reviews/2026-consolidated-review.md (the deduplicated, prioritized findings from the 2026 four-perspective review round). Build/test baseline: cargo test green (400 lib + 9 ipc + 5 workflow integration; 3 provider ignored), tsc --noEmit clean, vite build clean. No P0/Critical findings — everything is P1-P3. The highest-leverage fix is adopting shadcn/ui (Radix) which closes the largest UI finding (accessibility absent codebase-wide) and a PRD deviation simultaneously.

Verified against source this session: WorkflowStateInfo Rust has 4 fields (commands.rs:93-103: state/plan/depth/parents) but TS has 2 (types.ts:140-143); `as never` casts at App.tsx:98 + StatusBar.tsx:87. PendingApprovals keyed by tool_call_id only (approval.rs:39-53,65-77). Memory store single tokio::Mutex<Connection> (memory/mod.rs:103). convert_message/convert_tool_choice test-only (openai.rs:694,772); build_request_json IS tested (openai.rs:1536+). PLAN.md:54 specifies shadcn/ui; package.json has no @radix-ui.

Ordering principle: quick high-confidence wins first (each independently shippable + tested), then medium-effort, then the two large refactors (shadcn adoption, AgentLoop split) and the deferred memory-pool as their own scoped steps. PLAN.md spec reconciliation is documentation-only and last. Every code step ends with cargo test / tsc + vite build green.

## Steps
- [x] 1. [H2] Fix WorkflowStateInfo TS type drift + remove `as never` casts: in Rust, serialize state via the WorkflowState enum's serde (lowercase) form instead of Display in get_workflow_state; add depth + parents to the TS WorkflowStateInfo type (types.ts:140-143) and type state as WorkflowState; replace `wf.state as never` at App.tsx:98 and StatusBar.tsx:87 with a direct typed assignment. Verify casing matches at runtime (Rust serde lowercase == TS union lowercase). Run cargo test + tsc --noEmit + vite build.
- [x] 2. [H4] Key PendingApprovals by (agent_id, tool_call_id): change the map key in src-tauri/src/ipc/approval.rs from String to (AgentId, String); insert with the agent_id from the event forwarder (events.rs:177-191); resolve() and cleanup_for_agent() take the agent_id so only that agent's entries are dropped on exit. Update the existing approval.rs unit tests + tests/ipc_bridge.rs. Run cargo test.
- [x] 3. [M9 + L1 + L2] Dead-code & import cleanup: delete the test-only convert_message/convert_tool_choice layer + its tests in src/provider/openai.rs (keep build_request_json + its tests); delete src/provider/_dbg_test.rs; remove the top-level `#[allow(unused_imports)] use futures::StreamExt` at openai.rs:20-21 (tests re-import locally). Confirm async-openai is no longer referenced and remove it from Cargo.toml if safe. Run cargo test.
- [x] 4. [M2] Extract the duplicated provider key-resolution + client-build block into one shared fn (e.g. build_openai_client(config, endpoint, model, multimodal, reasoning_effort) -> Arc<dyn LlmClient>) used by main.rs (main + vision paths) and commands.rs::set_model. Drop the 'Mirror the api-key fallback chain' comment. Run cargo test.
- [x] 5. [H7] Add tests for the unattended Run-All loop: integration test covering checkpoint -> dispatch -> on_main_turn_resolved (success: commit) and (failure: rollback) in src-tauri/src/ipc/run_all.rs or a new tests file; a set_model provider-swap test; and a direct safety_rules::is_safe(tool, key_arg) signature-matching test. Run cargo test.
- [x] 6. [M3] Fix light-theme code highlighting: define the --code-* CSS vars inside the html.light block in frontend/src/styles/globals.css (or switch to a light hljs theme) so code blocks don't render dark-tuned token colors on a light background. Verify both themes. Run tsc + vite build (visual only).
- [x] 7. [M4] Slash command discoverability: add a `/`-triggered command menu in InputBar.tsx listing /clear /model /provider /help /save /load /panel with descriptions; surface 'restart required' inline for /model + /provider; add a file picker (or path completion) for /save + /load. Run tsc + vite build.
- [x] 8. [M5] Refactor handleAgentEvent in useAgentStore.ts (~340-line switch) into per-case pure reducer functions (reduce(agent, event) => AgentState) that are independently unit-testable; add a few unit tests for the key cases (tool_result, finished, exited). Run tsc + vite build.
- [x] 9. [M6] Reduce fan-in lock contention: batch the forwarder's running-state updates so the manager lock in src-tauri/src/ipc/events.rs is not acquired on every single event; prefer the existing atomic AgentHandle::running flag for state reads and only lock for structural changes (register/remove). Run cargo test.
- [x] 10. [H6] First-run empty state + plan discoverability: add a centered welcome panel (example prompts + one-line plan-first explainer) shown when the active agent's transcript is empty (Conversation.tsx); auto-open the right panel on the Plan tab when a plan is created or a step completes (mirror the DiffViewer approval auto-show), via the store + useAgentEvents. Run tsc + vite build.
- [x] 11. [H1 — large, may be deferred] Adopt shadcn/ui (Radix + Tailwind) per PLAN.md:54 to provide accessible Dialog, Tabs, ScrollArea, and a focus trap. Migrate ConfigDialog + SafetyToggleDialog to Radix Dialog (role=dialog, aria-modal, focus trap + restore), the MainPanel + RightPanel tab bars to Radix Tabs (role=tab/tablist, keyboard nav), and scrollable views to ScrollArea. Add prefers-reduced-motion guards on the thinking-dots/agent-pulse/count-up animations and keyboard shortcuts (A=Approve, D=Deny) on ApprovalPrompt. This closes the largest UI finding + the PRD shadcn deviation. Run tsc + vite build; add @radix-ui deps to package.json.
- [x] 12. [M1 — large, may be deferred] Split the AgentLoop god-object (src/agent/mod.rs, 2376 lines): extract PromptBuilder (constitution + workflow + recall -> system prompt), ToolDispatcher (approval gate + exec + result feedback), and move session/cache heuristics out; keep DeltaAccumulator as-is. Target ~3-4 files of 400-700 lines, preserving all existing tests (move them with the code). Run cargo test.
- [x] 13. [H3 — deferred until multi-agent is a real workload] Memory connection pool / WAL read concurrency: allow read connections per agent while keeping a single write connection over the tokio::Mutex<Connection> in src/memory/mod.rs; enable SQLite WAL. Goal: stop serializing every recall (O(N) scan) across agents on one lock. Run cargo test (incl. memory tests).
- [x] 14. [M7 + M8 + spec reconciliation — documentation only] Update PLAN.md: qualify 'multi-agent-ready from day one' (line 6/11) to note shared memory-connection + global safety/config as concurrency limits (M7); document the factory-vs-live-loop provider-swap two-step contract (M8); amend the locked-in-decisions table to match reality (Tailwind v3, highlight.js not Shiki, hand-rolled LCS not react-diff-view) OR note shadcn/Shiki/react-diff-view as 'to adopt' (depends on step 11); fix the directory diagram so agent.md is at <root>/agent.md not .coding/agent.md (project/mod.rs:47); document the beyond-spec features (vision fallback, spawn_agent, backlog/Run-All, safety-rules, stats, sub-plan stack).
- [x] 15. [P3 low batch — L3-L8 + L13 + L14] Small fixes: add a doc comment to get_config (commands.rs:587, L3); rename MemoryTier::from_str or impl FromStr (memory/types.rs:36, L4); apply mechanical clippy fixes is_some_and/contains/derivable-Default (L5); disambiguate the two MAX_RETRIES constants (L6); add a one-line note where let _ = wf.load_latest() is intentional (factory.rs:203, L8); add explanatory comments to the eslint-disable sites lacking them (L13); nudge inline-code contrast (Message.tsx:107, L14). Run cargo test + tsc + vite build.
- [x] 16. Standard closing sequence: run cargo test + tsc + vite build and confirm green; spawn a read-only reviewer subagent to review all uncommitted changes (git status / git diff HEAD) for correctness/bugs/security/constitution-compliance, writing its report to .coding/reviews/; act on every finding (re-run tests); commit to the current feature branch (never main) with a clear message.
