# Plan: Reasoning bar stats tick live during reasoning (fix 🧠 render gate)

## Goal
Make the reasoning bar's 🧠 token counter tick continuously during an active reasoning phase (each reasoning delta updates it), eliminating the update-at-end-only behavior, by fixing the render gate in InflightBar.tsx and pinning it with a source-contract test.

## Kind
bug_fixing

## Context
Root cause found by investigation: the "reasoning bar" is the InflightBar (frontend/src/components/chat/InflightBar.tsx). The 🧠 reasoning counter's target already rides the reasoning_delta path — line 143 `const reasoningDisplay = useCountUp(tokenUsage.reasoning + liveReasoningTokens);` with liveReasoningTokens climbing chars/4 per reasoning_delta (agentEventReducer.ts:313-314, prior art 4173dda). But the RENDER GATE at InflightBar.tsx:286 `{tokenUsage.reasoning > 0 && (` hides the counter until the first Usage event lands (Usage fires only after a response completes; `started` resets tokenUsage to zeros every turn — agentEventReducer.ts:193 — and usage accumulates only across requests within a turn). So during the first reasoning phase of EVERY turn the 🧠 counter is hidden and the stats row (↑0 ↓0, no 🧠, no tok/sec, static ctx) sits frozen while the reasoning panel streams — then everything refreshes at once when Usage lands. The reducer comment even states the intent ("the 🧠 counter climbs during the (often longest) reasoning phase instead of sitting at 0 until the usage event lands") — the gate defeats it. Fix: admit the live estimate in the gate. The reducer path is already pinned by useAgentStore.test.ts "liveTokens: deltas accumulate chars/4 estimates per bucket and usage snaps them to zero" (lines 111-139); the missing pin is the render gate. [Round-1 review LOW 1: the original context wrongly claimed "tokenUsage accumulates across turns, reset only by clearConversation" — corrected per the reviewer; the fix repairs every turn's first reasoning phase, not just the session's first.]

## Steps
- [x] 1. **Reproduce with failing regression test** — Write a regression test that reproduces the defect (constitution: every defect gets a regression test that fails without the fix and passes with it). Run it and confirm it FAILS.
- [x] 2. **Document root cause** — Investigate and document the root cause. memory_write a BUG: record (symptom → root cause → fix + regression test name, ≤600 chars).
- [x] 3. **Minimal fix** — Apply the minimal fix that makes the regression test pass. Do not refactor unrelated code.
- [x] 4. **Verify** — Run the regression test + the full test suite (cargo test unpiped, warning-free). Record the regression test name via update_plan (regression_test field) — finish is blocked without it.

## Bug
Reasoning bar stats only refresh when reasoning completes: during a long reasoning phase the InflightBar's stats row sits frozen while the reasoning panel streams new reasoning text (user report 2027-01-04).

## Regression test
frontend/src/components/chat/InflightBar.test.ts
