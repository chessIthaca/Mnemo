# Plan: Exempt search/search_read tool calls from transcript card merging

## Goal
Every `search` and `search_read` tool call renders on its own transcript line: extend the existing never-merge set (shell + browser, backlog daa38cbe) in reduceToolCallStart to include both search tools, with regression tests mirroring the shell/browser pattern.

## Kind
implementation

## Context
Backlog item 24ddb845 (dispatched this session). The transcript grouping lives in frontend/src/hooks/agentEventReducer.ts reduceToolCallStart (lines 485-559): consecutive same-name tool calls merge into one card (canMerge, lines 545-551) unless neverGroups (lines 543-544: `event.name === "shell" || isBrowserToolName(event.name)`). The never-merge directive for shell + browser was backlog daa38cbe (plan b6aee084, commit f80f2ff, SPEC memory 2a107921) — that work is in main. search/search_read are NOT in the set, so consecutive search calls currently merge into one grouped card, burying each distinct query. Different tool names never merge by construction (the canMerge condition requires last.name === event.name), so only same-name runs need the exemption. Existing tests to mirror: useAgentStore.test.ts:343-358 (shell never merges within one parallel batch) and 360-420 (browser_navigate / offscreen_browser_navigate never merge). Frontend verification: `npm test` (vitest run) and `npm run build` (tsc && vite build) in the frontend workspace (root package.json delegates build via --workspace frontend). The SPEC memory (2a107921, id 2a107921-d667-561a-bcd3-e3c2c5bc6ccd) documents the never-merge set and must be refined to include search/search_read after the change.

## Steps
- [x] 1. **Extend the never-merge set to search/search_read** — In frontend/src/hooks/agentEventReducer.ts, extend the neverGroups expression (lines 543-544) to also match `event.name === "search"` and `event.name === "search_read"`. Update the inline comment (lines 539-542) and the reducer's doc comment (lines 478-484) to state that shell, browser, and search/search_read calls never merge — each search query is distinct and grouped chips bury it (backlog 24ddb845, extending the daa38cbe directive). No other logic changes: canMerge, MAX_CALLS_PER_TOOL_CARD, and the failed-call chain-break stay as-is.
- [x] 2. **Add regression tests for the search exemption** — In frontend/src/hooks/useAgentStore.test.ts, mirror the existing shell/browser never-merge tests (lines 343-420): (a) "search calls never merge even within one parallel batch" — two tool_call_start(search) with no intervening result must produce two transcript tool entries with one call each; (b) "search_read calls never merge" — same shape for search_read; (c) a sequential search→result→search run also stays two cards. Reference backlog 24ddb845 in the comments, matching the daa38cbe citation style.
- [x] 3. **Verify: frontend tests + build green, SPEC memory updated** — Run `npm test` (vitest run) in the frontend workspace — the new tests pass and the existing merge tests (same-tool runs merge, MAX_CALLS_PER_TOOL_CARD split, failed-call chain-break) still pass, proving other tools keep the compact behavior. Run `npm run build` (tsc && vite build) — green. Then refine the still-current SPEC memory (id 2a107921-d667-561a-bcd3-e3c2c5bc6ccd) via memory_update to record the extended never-merge set: shell, browser_*/offscreen_browser_*, search, search_read (backlog 24ddb845).
