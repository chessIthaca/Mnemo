# Plan: Review git tool parameter schema and documentation

## Goal
Review the git tool's parameter schema and documentation to ensure every subcommand's required and optional parameters are accurately documented. The current schema description is ambiguous — e.g., `branch` requires an `action` field (list/delete/create) but this isn't clearly stated, and `checkout` requires a `branch` field that's easy to omit. Produce a clear, per-subcommand parameter reference.

## Kind
research

## Context


## Steps
- [x] 1. **Read the git tool schema definition and dispatch logic (1/2) — locate the git tool's schema (JSON schema for the tool) and its execute/dispatch function in src/tool/agent/git.rs. Read the full file to understand every subcommand, which parameters each requires, and how they're validated. Note any discrepancies between the schema description and the actual validation logic.**
- [x] 2. **Document findings and discrepancies (2/2) — write a clear per-subcommand parameter reference based on the code. For each subcommand (status, diff, log, commit, merge, checkout, stash, branch, push, list, delete, create, pop, drop), list: required params, optional params, and what each does. Flag any documentation gaps or schema description improvements. Record findings as a HOW memory.**
