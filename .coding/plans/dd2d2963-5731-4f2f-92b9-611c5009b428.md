# Plan: Agents to top bar + per-tool left sidebar toggles

## Goal
Move the agent switcher (main + subagents) into the top tab bar with a subtle animated running indicator; strip agents from the left sidebar; add per-tool on/off toggle buttons in the left sidebar (plus keep the show/hide-all), wiring each tool to enable/disable its right-panel tab.

## Context
Agents currently render in Sidebar.tsx (left) with ○/●; the top tab bar in MainPanel.tsx shows only a static 'main'. Right-panel tools are 8 tabs (RightPanel.tsx TABS) shown/hidden together via rightPanelVisible + toggleRightPanel. AgentInfo{id,name,running} exists. Store has rightPanelTab/rightPanelVisible/toggleRightPanel/setRightPanelTab in useAgentStore.ts (RightPanelTab = md|plan|diff|output|files|safety|stats|backlog). globals.css already defines a thinking-pulse keyframe for a subtle dot animation.

## Steps
- [x] 1. Store: add `disabledTabs: RightPanelTab[]` state + `toggleTab(tab)` and `isTabEnabled` logic to useAgentStore.ts; ensure the active tab auto-falls-back to the first enabled tab when its tab is disabled; keep rightPanelVisible/toggleRightPanel as the show/hide-all.
- [x] 2. Top bar: rework MainPanel tab bar to list every agent (from store `agents` + `activeAgent`) as a clickable tab showing the agent name and a subtle animated running dot (reuse a pulse CSS animation); clicking sets activeAgent.
- [x] 3. Sidebar: remove the agent switcher buttons and the spawn `+` button (agents no longer live here); keep the logo/settings/panel-toggle.
- [x] 4. Sidebar: add a vertical list of per-tool toggle buttons (one per RightPanelTab, using each tool's icon) that call toggleTab; visually indicate enabled/disabled (dim + strikethrough or reduced opacity) and add a tooltip naming the tool.
- [x] 5. RightPanel: filter the rendered TABS to only enabled ones; guard so a disabled active tab can't render (fall back to first enabled).
- [x] 6. CSS: add a subtle `agent-pulse` keyframe animation for the running indicator dot in globals.css.
- [x] 7. Run cargo test to confirm no regressions.
