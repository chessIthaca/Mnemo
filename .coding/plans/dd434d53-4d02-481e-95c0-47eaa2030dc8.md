# Plan: Fix all deferred review findings + never-defer rule

## Goal
Fix ALL remaining review findings (M2, M3, M4, N1, N3) and add a constitution rule that review findings are never deferred — always fixed.

## Context
The reviewer report (.coding/reviews/reviewer-instructions-review.md) had 4 Minor + 3 Nit findings; I fixed only N2 and deferred M2/M3/M4/N3, and N1 was dismissed as harmless. The user now rules: never defer review findings — always fix all of them. Findings to fix: M2 setRightPanelTab has no disabled-tab guard (useAgentStore.ts:583, RightPanel.tsx:47); M3 setAgentName is dead code — runtime-spawned agents show as agent-<id> in the top bar (need to wire names from backend events or a listAgents refresh); M4 BacklogView reads workflowStates[activeAgent] but the backlog dispatches to the main agent — track main agent id explicitly; N1 agent.md report-filename placeholder has no concrete example; N3 Sidebar.tsx handleToggleTool is a pointless one-line wrapper. Also encode the new rule in agent.md step 3 so it persists. Branch: feat/reviewer-report-instructions (commit 05fa162 already holds the previous round).

## Steps
- [x] 1. agent.md: amend step 3 (Act on findings) — never defer findings; fix ALL of them (skip only with explicit justification that a finding is factually wrong). Also fix N1: give a concrete report filename example (e.g. .coding/reviews/2026-04-04-review.md).
- [x] 2. M2: guard setRightPanelTab in useAgentStore.ts so selecting a disabled tab is a no-op (keeps rightPanelTab consistent with the content fallback).
- [x] 3. M3: make runtime-spawned agents show their real name in the top bar — investigate the event flow (child_finished carries a name; check for a spawn/started event) and wire setAgentName accordingly, or refresh names via listAgents when an unknown agent id appears.
- [x] 4. M4: BacklogView.tsx — derive the main agent id explicitly (smallest registered agent id) and read workflowStates[mainAgentId] for the in-flight phase badge instead of activeAgent.
- [x] 5. N3: Sidebar.tsx — remove the one-line handleToggleTool wrapper; call toggleTabAndReveal directly in onClick.
- [x] 6. Run cargo test and frontend tsc --noEmit to verify.
- [x] 7. Closing sequence: spawn reviewer to review all uncommitted changes, act on findings (fix ALL), commit to feat/reviewer-report-instructions.
