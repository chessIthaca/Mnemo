# Plan: Review all system prompts for efficiency, accuracy & context optimization

## Goal
Review every system prompt the model sees for (a) token efficiency / context optimization, (b) accuracy of tool & process descriptions, and (c) redundancy. Produce a findings report with concrete, ranked recommendations. No source-code changes — analysis only.

## Kind
research

## Context


## Steps
- [x] 1. **Read the remaining prompt-assembly code** — read src/agent/turn.rs around the system-prompt assembly site (build_stable_head + build_volatile_tail + CONTEXT_FOOTER push/pop, ~lines 280-450) to confirm exactly what the model sees each turn and in what order. Also read src/project/agent_md.rs to see how the global + project constitutions are loaded/trimmed. Note byte sizes of each block.
- [x] 2. **Inventory every tool schema description** — read the schema() fn of each tool in src/tool/agent/ (file_read, file_write, file_edit, file_append, shell, git, git_diff, search, search_read, read_files, spawn_agent, list_models, describe_image) + src/tool/workflow/ (create_plan, update_plan, complete_step, abandon_plan, ask_user, skill_start, skill_end, abandon_skill, finish, current_plan, memory_write, memory_recall, memory_consolidate, backlog_*). For each: note the description length and whether it duplicates guidance already in the system prompt preamble.
- [x] 3. **Cross-check redundancy: preamble vs lifecycle vs constitution vs workflow_section** — build a redundancy matrix: which concepts (research planning, sub-plans, review/closing sequence, tool-gating, file_edit modes) appear in more than one prompt block. Quantify approximate wasted tokens from each duplication. The stable head (preamble + lifecycle + constitution) is cached but still paid on cache miss and counts against context budget.
- [x] 4. **Cross-check accuracy: preamble tool guidance vs actual tool behavior** — verify each tool-usage claim in CODING_SYSTEM_PREAMBLE (file_edit use_regex/count/start_line/end_line/fuzzy_whitespace semantics, read_files {path,start_line,max_lines}, file_append chunking ~5000 chars) against the tool implementations + schemas. Flag any drift between what the prompt teaches and what the tool actually does.
- [x] 5. **Analyze the summarization + consolidation prompts** — assess build_summary_prompt (context.rs): the two branches are ~90% identical (DRY violation); note exact duplicated spans. Assess the three consolidation prompts (consolidation.rs): are they clear, do they specify output format unambiguously, are they minimal? These are one-shot (not per-turn) so lower priority but still worth tightening.
- [x] 6. **Write the findings report** — create .coding/analysis/system-prompt-review.md with: (1) an inventory of all prompt sources + approximate token costs, (2) redundancy findings ranked by wasted tokens, (3) accuracy findings (prompt-vs-tool drift), (4) efficiency recommendations ranked by impact (stable-head trimming > per-turn tool-schema trimming > one-shot prompt trimming), (5) a note on cache-stability constraints (which changes are safe for the byte-stable head, which would invalidate the prefix cache). Present a concise summary to the user.
