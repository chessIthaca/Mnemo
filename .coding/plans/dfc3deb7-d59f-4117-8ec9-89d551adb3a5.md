# Plan: Humanize memory_write's confirmation message

## Goal
Make memory_write's confirmation human-meaningful: a tier-specific sentence naming WHAT was saved (the title) and why it matters, with the UUID moved to structured data; update the frontend parser (new format + legacy fallback) and its pin tests.

## Kind
implementation

## Context
Root cause: src/tool/memory/mod.rs:108 `memory_write` returns `ToolResult::success(format!("wrote {tier} memory (id: {id})"))` — machine-flavored, UUID-centric, no title. This string flows to (a) the Output tab / tool result verbatim and (b) frontend/src/hooks/agentEventReducer.ts parseMemoryEntry (:253-261) which parses tier via /wrote (\w+) memory/i and uses the whole output as the `memory` transcript entry's snippet — rendered by Message.tsx:106-115 as name + tier badge + truncated snippet. That's exactly the "memory_write / semantic / wrote semantic memory (id: …)" the user quoted. The title (the human-meaningful part) is known Rust-side at write time but dropped from the message. The UUID is never used later by the agent (recall is query-based) — it belongs in structured `data`, not the human text. Pinned consumers: frontend/src/hooks/agentEventReducer.memory.test.ts:26-33 pins the old format. No Rust test pins the write message (write_and_recall only asserts success + recall contains title). memory_consolidate messages keep their session-relative wording (session_id is agent-supplied/meaningful) — out of scope. ToolResult has `.with_data(Value)` (src/tool/mod.rs:79-86) for machine-readable fields.

## Steps
- [x] 1. ****Humanize memory_write output (Rust)**** — src/tool/memory/mod.rs: (1) Add a private helper `tier_summary(tier: MemoryTier) -> (&'static str phrase, &'static str suffix)` with a doc comment — working: phrase "a session event", suffix "— consolidation will distill it into summaries and facts later"; episodic: "a session summary", suffix ""; semantic: "a durable fact", suffix "— it will be recalled in future sessions when relevant"; procedural: "a reusable workflow", suffix "— it will be recalled when a matching task comes up". (2) In MemoryWriteTool::execute (:107-110), keep `args.title` before Memory::new consumes it; on success return `ToolResult::success(format!("Saved {phrase} \"{title}\" ({tier} memory){suffix}", ...))` where {tier} is the lowercase tier word, applying the suffix with a leading space only when non-empty — then `.with_data(json!({"id": id, "tier": tier-as-lowercase-str, "title": title}))` so the UUID survives machine-readably. (3) Update/extend tests in the same file's tests mod: new test `write_message_is_human_readable` (write tier=semantic title="auth fact" → success; output contains `Saved a durable fact "auth fact"` and `(semantic memory)`; output does NOT contain `(id:`; result.data["id"] is a string); add a loop test covering all four tiers' phrases (working/episodic/semantic/procedural each yield their phrase + `({tier} memory)` and no `(id:`).
- [x] 2. ****Frontend parser + pin tests**** — frontend/src/hooks/agentEventReducer.ts parseMemoryEntry memory_write branch (:253-261): first try the new format — tier via /\((working|episodic|semantic|procedural) memory\)/ and title via the first double-quoted run /"([^"]+)"/, snippet = the whole output sliced to 160 as today; if the new-format tier regex misses, fall back to the legacy /wrote (\w+) memory/i (old logs/robustness) with title undefined. Update the doc comment (:254) to describe both formats. Update frontend/src/hooks/agentEventReducer.memory.test.ts:26-33 — new-format case (exact sample `Saved a durable fact "auth fact" (semantic memory) — it will be recalled in future sessions when relevant.` → tier "semantic", title "auth fact", snippet contains "Saved a durable fact") + keep a legacy-format case asserting tier parses via the fallback.
- [x] 3. ****Run all checks**** — PowerShell, unpiped, read test-result lines + $LASTEXITCODE: (1) cargo test (root — new Rust tests + all suites green, zero warnings under deny(warnings)); (2) cd frontend; npm test (reducer pin tests + all suites); (3) cd frontend; npm run build (tsc proves the reducer edit compiles). Fix failures before proceeding.
- [x] 4. ****Review, fix, commit, finish**** — (1) git status --short + git branch --show-current — expected changed files: src/tool/memory/mod.rs, frontend/src/hooks/agentEventReducer.ts, frontend/src/hooks/agentEventReducer.memory.test.ts (+ review report + .coding bookkeeping). (2) spawn_agent role:"reviewer" with a self-contained task (goal: humanize memory_write's confirmation — old message leaked a UUID and omitted the title; list changed files; note the UUID moved to ToolResult.data, frontend parses new format + legacy fallback, consolidate messages intentionally unchanged; ask for correctness/regression/test/security/constitution findings). END TURN after spawning. (3) Read the report, fix EVERY finding, re-run step 3's checks. (4) Commit all files incl. the review report to fix/memory-write-message (never main). (5) finish with the report path; final summary ≤3 lines + Decisions section.
