# Plan: Address all 31 findings from the 2026-08-13 full-app review

## Goal
Resolve all 31 findings from the 2026-08-13 consolidated whole-app review: remove dead build deps, close two security exfiltration/permission gaps, add perf caches + spawn_blocking, promote the error enum to structured variants + typed IPC errors, do quality cleanups, and split the two oversized modules (settings.rs, useAgentStore.ts).

## Kind
implementation

## Context
Consolidated whole-app review at .coding/reviews/2026-08-13-full-app-review.md lists 31 findings (6 High, 12 Medium, 13 Low). Verified anchors: Cargo.toml:20 (syntect), :48 (markdown), :51 (reqwest gzip/brotli/deflate); src/error.rs:11-36 (9 string-bucket (String) variants); src-tauri/src/ipc/settings.rs (1750 lines, 7 concerns); src/tool/agent/describe_image.rs:178-181 (AutoRun) + :52-70 (extension-only MIME check); src/provider/trace.rs:345-385 + :448-487 (unrestricted log files). Several findings are accept/note-only: Q-L2 (schemas sort — keep), L3 (browse_markdown_file — by design), R3 (navigate lock — acceptable), R4 (screenshot — fine). Q-H2 (useAgentStore 958 lines) is "acceptable as-is; extract if it grows" — included as preventive. Parallelization: the frontend pass (step 10) is disjoint from all Rust work (different files) and can be spawned as a parallel background agent early in execution while Rust steps 2-9 run; the final review (step 11) covers ALL uncommitted changes including the agent's. Sub-plans will be created during execution for steps 6, 7, 9, 10.

## Steps
- [x] 1. **Remove dead build deps (B1, B2, B3)** — In `Cargo.toml`: delete `syntect = "5"` (line 20) and `markdown = "1"` (line 48); first `search` for `syntect::` and `markdown::` / `mod markdown` to confirm zero Rust references. Audit `reqwest` features (line 51): drop `gzip`/`brotli`/`deflate` unless a fixture/test proves the LLM/embeddings endpoint sends compressed bodies (keep `json`/`rustls-tls`/`stream`). Run `cargo build` then `cargo test`; confirm warning-free.
- [x] 2. **Harden describe_image exfiltration channel (H1)** — In `src/tool/agent/describe_image.rs`: (a) change `safety()` (line 178-181) from `SafetyLevel::AutoRun` to `SafetyLevel::NeedsApproval`. (b) In `load_image_data_url` (line 52-70), after `std::fs::read`, verify magic bytes match the claimed MIME before building the data URL — PNG `\x89PNG`, JPEG `\xff\xd8\xff`, GIF `GIF8`, WebP `RIFF...WEBP`, BMP `BM`; reject mismatches with `Error::InvalidInput`. Update the module doc (line 14-15) and the test at line 264 that asserts `AutoRun`. Run tests.
- [x] 3. **Restrict provider log permissions + redact secrets (H2)** — In `src/provider/trace.rs`: after creating the error-log file (`maybe_log_error`, line 374-385) and the traces file (`write_record_to_file`, line 448-487), apply the same permission-restriction helper used for `keys.toml` (find it via `search restrict_permissions` — likely `src-tauri/src/main.rs:500-512` or a keys module; it sets user-only DACL on Windows / 0600 on Unix). Call it on both log paths at creation. Add secret redaction: before serializing request/response bodies to either log, redact `Authorization`-shaped strings and `api_key` fields to `[REDACTED]`. Run tests (the trace tests at trace.rs:643-807 must still pass).
- [x] 4. **Security hardening pass (M1-sec, M2-sec, M3-sec, M4-sec, L1, L2)** — Six fixes: (a) `src/tool/agent/sandbox.rs:170-180` `is_protected_write_target`: lowercase the relative path (`to_ascii_lowercase`) before `==` comparison (NTFS is case-insensitive — `.coding/SAFETY.TOML` currently bypasses). (b) M2-sec TOCTOU: document the accepted low risk with a code comment at `sandbox.rs:78-109` (no behavior change — review accepted it). (c) `src/browser/mod.rs:85` ALLOWED_SCHEMES + `src/tool/browser/mod.rs:171`: ensure `browser_eval` is in `never_auto_for` so no broad safety rule auto-approves it; restrict `data:` to `#[cfg(test)]` or remove from the allow-list. (d) M4-sec `get_api_keys` (`src-tauri/src/ipc/settings.rs:155-167`): return masked key values (e.g. `****1234`) instead of full keys; update `frontend/src/components/settings/ProvidersSection.tsx:34,93` to display masked values (keep the clear-on-deactivate behavior). (e) L1 `src/provider/openai.rs:99-119`: set explicit `.redirect(reqwest::redirect::Policy::none())` on the client builder. (f) L2 `src/tool/agent/shell.rs:186-197`: cap `data.stdout`/`data.stderr` byte length before placing in `data` (reuse the display cap or a dedicated constant). Run tests.
- [x] 5. **Performance pass (R1, R2, M1-perf)** — Three fixes: (a) R1 `src/agent/context.rs:246`: cache the `cl100k_base()` BPE tokenizer in a `std::sync::OnceLock<CoreBPE>` (or `once_cell::sync::Lazy`) so it initializes once, not per `count_tokens` call. (b) R2 `src/memory/mod.rs`: wrap the rusqlite calls in `recall` (:522), `write` (:502), and `stats` (:743-861) with `tokio::task::spawn_blocking` (mirror `describe_image.rs:107`). (c) M1-perf `src/browser/mod.rs:201-220`: move `sweep_stale_profiles` (blocking `read_dir` + `remove_dir_all`) out of the state lock and into `spawn_blocking`; call it outside the lock in `ensure_browser` (:168). Run tests.
- [ ] 6. **Promote error-enum string buckets to structured variants (Q-H3)** — [SUB-PLAN] In `src/error.rs:11-36`: add dedicated variants for the most-branched-on Workflow failures — e.g. `WorkflowNoPlan` and `WorkflowWrongState { current: String, expected: String }` — so `match` can discriminate 'no plan exists' from 'update outside Executing' (both currently `Workflow(String)`). `search` for `Error::Workflow(` call sites and update the no-plan / wrong-state constructors to the structured variants; keep `Workflow(String)` as the free-form fallback. Ensure `NotFound`/`InvalidInput` are used consistently. Run tests. (Unlocks step 7.)
- [ ] 7. **Typed IPC error DTO (Q-M1)** — [SUB-PLAN] Add `struct IpcError { kind: String, message: String }` (serde `Serialize`, `#[serde(into)]` or a `From<myharness::Error>` impl mapping kind=variant name, message=Display). Change IPC commands from `Result<T, String>` to `Result<T, IpcError>` in `src-tauri/src/ipc/settings.rs:27` and `files.rs:24` (and any other `Result<_, String>` commands found via `search`). Verify the frontend's error handling (`useAgentStore.ts` / IPC call sites) still reads the message — adjust if it expects a bare string (the DTO serializes to `{kind, message}`; the frontend may need `.message`). Run tests.
- [ ] 8. **Quality cleanups (Q-M2, Q-M3, Q-M4, Q-M5, Q-L1, Q-L3)** — Six fixes: (a) Q-M2 `src/tool/mod.rs:126-128`: remove the dead `never_auto()` method (re-add when a real blanket-prompt tool appears). (b) Q-M3 `src/tool/mod.rs:197`: make `ToolFilter::from_state` return `Option<ToolFilter>` (or panic with a clear message) for `Skill` instead of silently mapping to `Planning`; update callers. (c) Q-M4 `src-tauri/src/ipc/settings.rs:116-118` + `frontend/src/components/settings/types.ts:131-134`: emit only the serde form (`"openai"`) from `get_config`; delete the Debug-form (`"OpenAI"`) lenient parsing in both places. (d) Q-M5 `src/workflow/mod.rs:699`: replace `serde_json::from_str(&text).unwrap_or_default()` with a path that logs a warning when the sidecar exists but fails both parses. (e) Q-L1 `src/tool/mod.rs:385`: accept the per-call clone (add a one-line note) or pass by value if trivially cheap. (f) Q-L3 `src/workflow/mod.rs:762-778`: replace `entry.ok()?` chains with `filter_map` so one bad dir entry doesn't abort the scan. Run tests.
- [ ] 9. **Split settings.rs god-module (Q-H1)** — [SUB-PLAN] Split `src-tauri/src/ipc/settings.rs` (1750 lines) along its header seams into new files under `src-tauri/src/ipc/`: `endpoints.rs` (endpoint DTOs + `save_endpoints`), `keys.rs` (`get_api_keys` + key handling), `pricing.rs` (pricing DTOs + commands), `rewire.rs` (runtime vision/embedder rewire after config change); keep `settings.rs` for `get_config`/`get_settings`/`save_settings` + shared wire structs. Register the new modules in `src-tauri/src/ipc/mod.rs`. Pure file move — no behavior change. Run tests; verify the golden fixture test still passes (it pins the `get_config` payload shape).
- [ ] 10. **Frontend pass (Q-H2, Q-L4, Q-L5, B4, M2-perf)** — [SUB-PLAN, can spawn as parallel agent — disjoint from Rust] (a) Q-H2 `frontend/src/hooks/useAgentStore.ts` (958 lines): extract the steer/backlog action block into its own hook module (e.g. `useSteerBacklog.ts`). (b) Q-L5 `frontend/src/components/settings/types.ts:224-225`: remove the double blank line. (c) Q-L4 `types.ts:105-110`: add a comment that `makeUid` is for React keys only, never security. (d) B4 `frontend/vite.config.ts`: lazy-load the markdown/highlight stack (react-markdown + rehype-highlight) via dynamic import; remove the `chunkSizeWarningLimit: 1500` suppression. (e) M2-perf: in the `streamingText` concat path (find via `search streamingText`), join-once per flush instead of O(n²) string concat. Run `npm run build` + `npm run lint` in `frontend/`.
- [ ] 11. **Final: verify accept-only findings + cargo test + review + commit** — Verify the accept/note-only findings need no code change: Q-L2 (schemas sort must stay — confirm the sort is present), L3 (browse_markdown_file native picker — by design), R3 (navigate lock — acceptable), R4 (screenshot base64 — fine). Run `cargo test` (must pass, warning-free under `#![deny(warnings)]` — no `#[allow(...)]` added). Spawn a reviewer subagent (role:reviewer) to review ALL uncommitted changes against the 31 review findings + constitution compliance; give it the changed file list and the review doc path. Read its report from `.coding/reviews/`, fix every finding, re-run `cargo test`. Commit to the feature branch with a clear message; include the review report file.
