# Plan: Graph tool guidance: semantic-first for Rust AND frontend TS

## Goal
Tool descriptions + TOOL_STRATEGY steer the agent to graph-first semantic symbol lookup for both Rust and frontend TS/TSX, with the language coverage stated explicitly in the graph_search/graph_context descriptions.

## Kind
implementation

## Context
User request (dispatched backlog item, evidence memory "HOW frontend TS IS codegraph-indexed"): make the agent reach for semantic symbol lookup FIRST (graph_search → graph_context/graph_impact) for Rust AND frontend TS instead of grep + whole-file reads; state frontend TS coverage explicitly in the graph tool descriptions. GROUNDING (verified this session): (1) src/agent/prompt.rs TOOL_STRATEGY const :145-173 — graph line :159-163 says "MANDATORY first step for SYMBOL questions … never grep chains or whole-file reads" but never states language coverage; the head is prefix-cached so keep additions LEAN. (2) Pinning test tool_strategy_lists_graph_tools_before_search :1227-1267 asserts: graph before search order, contains "MANDATORY first step", "for SYMBOL questions", "String literals", "FALLBACK when the graph", "switch to graph_context" — keep those phrases or update the test deliberately. (3) src/tool/agent/codegraph.rs descriptions: graph_search :108-113 (schema text), graph_context ~:193-195, graph_impact ~:274, graph_path ~:353; module doc :5-22; tests ~:431-457. (4) TRUTH for wording: src/codegraph/walk.rs Lang::from_extension — rs→Rust, ts→Ts, tsx→Tsx ONLY (js/md → None), so the graph indexes Rust + TypeScript/TSX and the new text must claim exactly that (README.md:50 already says "Tree-sitter knowledge graph (Rust + TypeScript/TSX)" — likely no README change needed, verify). (5) 2026-08-27 miss: TraceStats legend located via search + two whole-file reads; graph_impact skipped before editing shared traceStats exports. (6) Windows/PowerShell: run cargo test UNPIPED and read test-result lines / $LASTEXITCODE (piped exit codes lie); #![deny(warnings)] so green = warning-free; root cargo test does NOT compile src-tauri — build it separately; frontend untouched. Also: a working-memory note holds the backlog text for this task — mark it covered once the plan exists.

## Steps
- [x] 1. **TOOL_STRATEGY graph line: explicit dual-language coverage** — src/agent/prompt.rs, TOOL_STRATEGY graph line (:159-163): reword to state coverage explicitly, e.g. "- graph_search / graph_context / graph_impact / graph_path: MANDATORY first step for SYMBOL questions (where defined, callers, blast radius, reachability) — in BOTH Rust and frontend TS/TSX (the graph indexes .rs/.ts/.tsx; a symbol lookup never starts with grep or a whole-file read, whatever the language). graph_impact before editing a shared/public symbol — frontend lib exports count too. String literals (tool names, config keys, log text) are NOT indexed — use search for those." Keep the pinned phrases "MANDATORY first step", "for SYMBOL questions", "String literals", "FALLBACK when the graph", "switch to graph_context" and the graph-before-search order. Update tool_strategy_lists_graph_tools_before_search (:1227): keep existing asserts, add one asserting the dual-language clause (e.g. contains "frontend TS/TSX"). Run: cargo test --lib agent::prompt (UNPIPED, from project root).
- [x] 2. **graph_search/graph_context descriptions state TS coverage** — src/tool/agent/codegraph.rs: (1) graph_search schema description (:108-113): insert the coverage clause — "in the project's code knowledge graph (Rust and TypeScript/TSX — .rs/.ts/.tsx sources)" — keeping "MANDATORY first step" and the string-literal carve-out. (2) graph_context description (~:193): add a short coverage clause (e.g. "works the same for Rust and frontend TS/TSX symbols"). (3) Module doc (:5-22): one line stating the indexed languages. (4) Leave graph_impact/graph_path lean (downstream tools inherit the framing) — document that choice in the commit. (5) New co-located test: the graph_search + graph_context schema descriptions contain "TypeScript" (guards the coverage clause against drift). Run: cargo test --lib tool::agent::codegraph (UNPIPED).
- [x] 3. **Full verify + doc-accuracy check** — From project root: cargo test (UNPIPED — read the test-result lines, do not trust piped exit codes). Then: cd src-tauri; cargo build (root test does not compile src-tauri). Confirm no stale doc statements: README.md:50 and any docs/*.md mentioning the graph's language coverage already say Rust + TypeScript/TSX — edit only if something contradicts the new wording. All green + warning-free before proceeding.
- [x] 4. **Review, fix, commit** — Spawn reviewer (role:"reviewer") over ALL uncommitted changes: goal = dual-language graph-first steering (TOOL_STRATEGY + graph tool descriptions + tests); changed files = src/agent/prompt.rs, src/tool/agent/codegraph.rs (+ possibly a docs touch); ask for correctness, wording truthfulness vs walk.rs Lang coverage, constitution (doc comments, no #[allow], warning-free), doc sync, multi-platform neutrality. END THE TURN after spawning; resume on the notification. Fix EVERY finding, re-run the step-3 matrix, commit to the current wt/* branch (never main) including the review report. Final summary ≤3 lines.
