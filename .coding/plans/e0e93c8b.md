# Plan: Add resume-cadence clause to reviewer-spawn bullet (③ continuation gap)

## Goal
Add a cadence clause to the reviewer-spawn bullet in APP_RULES so that when the finish notification resumes the agent, it immediately continues the closing sequence instead of stalling. Fixes the third continuation gap (resume-from-notification) that ① and ④ don't cover — observed live this session when the agent produced (no output) after the reviewer finished.

## Kind
implementation

## Context


## Steps
- [x] 1. **Add resume-cadence clause to reviewer-spawn bullet — prompt.rs:121-122**
- [x] 2. **Add test assertion for the resume-cadence clause**
- [x] 3. **Run cargo test, fix any failures**
