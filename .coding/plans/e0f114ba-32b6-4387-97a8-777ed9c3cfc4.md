# Plan: Deep review — security, architecture, maintainability, refactoring backlog

## Goal
Produce a consolidated, prioritized deep-review report (security, architecture, maintainability, refactoring backlog with code-size reduction estimates) covering the whole codebase, written to .coding/reviews/, feeding future implementation plans — without changing any source code.

## Kind
research

## Context
User asked for a deep review of the whole codebase: security, maintainability, refactoring opportunities (explicitly: reduce overall code size, make future agent changes easier), and architecture. Prior work exists: .coding/reviews/2026-04-08-architecture-quality.md produced the Phase 6b items (M5 protected-write targets, M6 keys.toml ACL, M3-strict) — must read it first to avoid re-reporting known findings and to check what was already fixed. Codebase layout: lib crate src/ (agent, browser, config, memory, model_resolver, project, provider, runtime, safety_rules, skill, tool, workflow — ~833 tests), bin crate src-tauri/ (ipc + main), frontend/ (React+TS+Zustand, ~136 tests). Recent features merged: model-cap discovery (fetch_models_with_vision caps), startup ctx-bar seeding (registration ContextUsage + context_caps IPC + seedContextCaps). This is a RESEARCH plan: no source-code changes; output is a consolidated prioritized report under .coding/reviews/. Reviewer subagents (role:reviewer) can write reports to .coding/reviews/ via write_review_report. Windows/PowerShell environment; use shell for line-count metrics once executing (search tools can't aggregate sizes). Known hot spots from prior sessions worth personal attention: sandbox protected-path logic (tool/agent/sandbox.rs), keys.toml perms (config/keys.rs), trace redaction (provider/trace.rs), IPC surface (src-tauri/src/ipc/*, 40+ commands), spawn.rs lock/fan-in dance, main.rs (874 lines, factory+setup), agent/turn.rs (1396 lines), InflightBar/StatusBar/store size on frontend, duplication between ipc command modules.

## Steps
- [x] 1. **Read prior reviews + gather size metrics** — Read .coding/reviews/2026-04-08-architecture-quality.md and every other report in .coding/reviews/ to build the already-known/deferred list (M5/M6/M3-strict status). Then via shell (PowerShell): per-file line counts for src/**/*.rs, src-tauri/**/*.rs, frontend/src/**/*.{ts,tsx} sorted descending; per-directory sums; test-vs-code ratio for the biggest modules. Output: a metrics table identifying the top ~15 refactoring candidates by size.
- [x] 2. **Spawn three parallel deep reviewers** — spawn_agent role:reviewer ×3, each with a self-contained task naming its scope, the metrics table, and the prior-findings list to skip: (a) SECURITY — threat-model the Tauri IPC surface (input validation, error/secret leakage), sandbox + protected paths, keys.toml handling, trace/error redaction, approval-gate bypasses, races (locks, fan-in), unsafe, injection (regex/shell/git args); (b) MAINTAINABILITY/REFACTORING — duplication across ipc modules + provider + frontend, dead code, oversized files/functions, over-abstraction vs missing abstraction, concrete refactors ranked by LOC saved and future-change payoff; (c) ARCHITECTURE — layering (lib vs bin vs frontend), module boundaries/coupling, error-handling consistency, event-flow design (fan-in/forwarder/store reducers), config/state management, test architecture. Each writes .coding/reviews/2026-04-18-deep-review-<area>.md. End turn after spawning; resume on notifications.
- [x] 3. **Own deep pass on highest-risk areas** — while waiting / after reports land: read tool/agent/sandbox.rs (protected-path matrix, case sensitivity, symlink), config/keys.rs + project.rs (perms, .bak recovery), provider/trace.rs redaction paths, src-tauri ipc command surface (spot-check 8-10 commands for validation + leakage), agent/turn.rs + runtime/agent.rs event ordering, spawn.rs/manager lock protocol. Cross-check reviewer claims against actual code before accepting them.
- [x] 4. **Consolidate into the final prioritized report** — Merge all findings into .coding/reviews/2026-04-18-deep-review.md: (1) Security findings by severity with file:line; (2) Architecture assessment (what's sound, what's drifting); (3) Ranked refactoring backlog — each item with est. LOC delta, risk, and why it makes future agent changes easier; (4) explicit non-findings (checked, clean). Reject reviewer claims that are factually wrong (verify first). No source changes.
- [x] 5. **Persist durable knowledge** — memory_write semantic entries for the key architectural facts/gotchas confirmed by this review (lock-ordering invariants, event-flow contract, config-write protocol) so future sessions recall them; one episodic entry summarizing the review's top recommendations.
