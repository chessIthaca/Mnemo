# Plan: Delete obsolete merged develop branch (git cleanup)

## Goal
Delete the obsolete, fully-merged local `develop` branch and confirm the repo is left with only `main` (and no stashes).

## Kind
research

## Context
Git inventory (2026-09-08) found one lingering artifact: local `develop` branch @ 4c2fb01, fully merged into main, obsolete since the develop tier was removed (DECISION c8fd9d60, topology now wt/* → main). No stashes, no unmerged branches, no extra worktrees. Currently on `main`. `git branch --merged main` already confirmed develop is safe to delete with `git branch -d` (refuses if unmerged). No source-code changes involved — pure ref cleanup.

## Steps
- [x] 1. **Re-verify merged status + check for any remote develop, then delete the local branch. Commands (PowerShell, project root):
- `git branch --merged main` — confirm `develop` still listed as merged (idempotency guard)
- `git branch -r` — confirm there is NO `origin/develop` remote branch to also delete/prune
- `git branch -d develop` — safe delete (fails if unmerged; develop IS merged so this succeeds)
Run unpiped; read $LASTEXITCODE. If `git branch -d` reports 'not fully merged' for any reason, STOP and report — do not force-delete (`-D`).

Note: currently on `main`, so deleting `develop` is allowed (cannot delete the checked-out branch).**
- [x] 2. **Confirm clean state. Re-run:
- `git branch -vv` — should show only `main`
- `git stash list` — should be empty
- `git worktree list` — should show only the main worktree
Report the final clean state to the user. Update the git-hygiene memory record (id 2fdc739c) to note develop was deleted and the repo is now fully clean.**
