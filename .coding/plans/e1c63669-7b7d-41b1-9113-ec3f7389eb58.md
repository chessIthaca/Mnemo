# Plan: Drop tools-panel left border line

## Goal
Remove the full-height left border line from the tools (right) panel in frontend/src/components/layout/RightPanel.tsx, keeping the resize handle's grip pill and hover behavior in App.tsx unchanged.

## Kind
implementation

## Context
Clarified with user: the line the red arrow points at is the full-height vertical border on the tools panel's left edge, NOT the small centered grip pill (the pill should be KEPT). That border comes from frontend/src/components/layout/RightPanel.tsx line 34: `className="flex min-w-[300px] flex-col border-l border-border bg-bg-secondary"`. The `border-l border-border` is the white/gray line. The resize handle (App.tsx ResizeHandle) already provides a w-1 spacer + hover tint + grip pill, so the panel's left border is redundant. I restored App.tsx to keep the grip pill (reverted my earlier removal).

## Steps
- [x] 1. **Remove left border from RightPanel** — In frontend/src/components/layout/RightPanel.tsx line 34, change `className="flex min-w-[300px] flex-col border-l border-border bg-bg-secondary"` to drop `border-l border-border`, leaving `className="flex min-w-[300px] flex-col bg-bg-secondary"`. Do NOT touch the tab bar's own `border-b border-border` on line 39 (that's the horizontal tab underline, unrelated). App.tsx ResizeHandle (grip pill) stays as-is.
- [x] 2. **Verify frontend build** — Run the frontend type/build check (e.g. `cd frontend; npx tsc --noEmit`) to confirm it compiles. Fix any fallout by removing dead code, not silencing.
- [x] 3. **Run tests** — Run `cargo test` per project rules to confirm nothing else broke (change is frontend-only, but the constitution requires the suite green before closing).
