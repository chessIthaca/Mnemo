# Plan: One wt/* branch per agent directory (auto-fork, no auto-accumulation)

## Goal
Enforce "one wt/* branch per agent directory": create_plan auto-forks a stable per-directory wt/* branch from main when on main (no branch arg) and reuses the current branch otherwise; the `branch` arg becomes explicit-user-only; run_all stops auto-passing per-item branch names. Closes G1 (opt-in gap), G3 (auto-accumulation).

## Kind
implementation

## Context
User decision 2026-09-08 (DECISION memory + .coding/knowledge/decision/2026-08-26-one-wt-branch-per-agent-directory-auto-fork-no-a.md): one wt/* branch per agent directory. create_plan must AUTO-FORK a stable per-directory wt/* branch from main when on main (no branch arg), and REUSE the current branch when already on one. The `branch` arg is reserved for EXPLICIT user requests — agents never auto-pass it. run_all's RUN_ALL_STEER currently tells agents to pass branch: "fix/<slug>" (one unmerged branch per backlog item → accumulation); that steering must go. merge_to_main already deletes the merged branch (skill step 6), so no accumulation across sessions once merged.

Code map (verified):
- src/project/git_ops.rs: prepare_branch_sync (line 290) + prepare_branch (473) + PrepareBranchOutcome enum (161) + valid_branch_name (153). The carry/restore machinery + AlreadyOnBranch/SwitchedExisting/Created outcomes already exist.
- src/tool/workflow/plan.rs: CreatePlanTool.execute branch fork at lines 327-364. The (None, _) => None arm (line 363) is the opt-in gap — it does NOTHING, so plans on main stay on main. The branch schema description is at line 267.
- src-tauri/src/ipc/run_all.rs: RUN_ALL_STEER const at line 94-105 (rule 1 tells agents to pass branch: "fix/<short-slug>"); run_all_prompt at 119; test run_all_prompt_tells_the_agent_to_pass_branch_to_create_plan at 218-232 asserts the slug hint + "never commit work to main".
- .coding/skills/merge_to_main.toml line 11 comment says "each plan works on its own wt/* branch".
- agent.md lines 51-52 (branch policy), README.md line 45, PLAN.md line 141 (topology descriptions).
- Existing tests: git_project() fixture (plan.rs:1510) starts on feat/prev; create_plan_works (1235) uses a plain tempdir (no git). Auto-fork must be a no-op (no note) when not a git repo and when already on a non-main branch's reuse produces no mutation.

Design: add work_branch_name(root) -> "wt/<sanitized-basename>" (unique per worktree dir — two worktrees of the same repo cannot share a branch name) + ensure_work_branch(root) -> Result<Option<PrepareBranchOutcome>,String> (None = not a repo / nothing to do; Some(Created/SwitchedExisting) = forked/resumed from main; Some(AlreadyOnBranch) = reuse). Wire into create_plan's (None, _) arm.

## Steps
- [x] 1. **git_ops: stable work-branch name + ensure_work_branch helper + tests**
- [x] 2. **create_plan: auto-fork when branch arg is None**
- [x] 3. **run_all: stop steering agents to pass per-item branch names**
- [x] 4. **Docs + compiled prompt text sync**
- [x] 5. **Full test run (root + src-tauri)**
- [ ] 6. **Review, fix findings, commit, finish**
