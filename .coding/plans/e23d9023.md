# Plan: Roll back main to e0ac5dd — undo think_tags/vendor-policy merge

## Goal
Move main and the wt/* working branch back to e0ac5dd ("Merge wt/agenticcoding: 429-fallback live-token-count fix + run-all heartbeat stall recovery"), removing the per-model think_tags + config-driven vendor policy work (commits 5361db7, 38d939d, merge b05e72d) per the user's 2027-01-09 decision that the vendor-specific direction was wrong — DeepSeek degraded and GLM leaked literal think tags to the main chat, both models served via the the LiteLLM proxy. The reasoning-leak bug is OPEN on main again after this rollback.

## Kind
implementation

## Context
main history (newest first): b05e72d (merge, plan 77278715) > 38d939d (docs) > 5361db7 (fix) > e0ac5dd (TARGET). The GLM stop-boundaries work (e20b1df) and 429-fallback work (e0ac5dd) predate the target and stay. Working tree is on main (post merge_to_main branch deletion) with uncommitted changes ONLY in .coding/knowledge/spec/ (2 modified + 2 untracked files — supersede bookkeeping for the vendor records; the files themselves are removed by the rollback, so the edits are moot and safe to discard). Removed commits stay recoverable via git reflog (~90 days; pre-rollback main tip = b05e72d). After the rollback: the reasoning-leak bug (memory baad2dc1) is OPEN on main again; supersede the b05e72d-merged vendor memories (0d49d31c, 8b13e90a, 85c86c30, baad2dc1 — re-verify live IDs via memory_search before superseding), write a DECISION record, and mark backlog 960e4ffb failed (vendor fields no longer exist on main). Closing sequence after the last step: reviewer verifies main + wt/* at e0ac5dd and tests green; then commit the knowledge-record changes on the wt/* branch only (never main).

## Steps
- [x] 1. **Verify pre-rollback state** — Run via shell (PowerShell): `git branch --show-current` (expect main, or wt/* if create_plan already forked), `git log --oneline -3` (HEAD = b05e72d), `git rev-parse main` (must resolve to b05e72d's full hash), `git remote -v` (empty output = local-only repo, no force-push needed; if a remote exists, STOP and ask_user whether to force-push main before proceeding), and `git status --short` (must show only the four .coding/knowledge/spec entries — 2 modified, 2 untracked). If anything differs from this picture, stop and re-assess before mutating anything.
- [x] 2. **Roll back both branches to e0ac5dd** — On the current wt/* working branch (create_plan forks it from main if we were on main): `git reset --hard e0ac5dd` (moves the branch to the target, cleans the tree, discards the moot knowledge-file edits), then `git clean -fd .coding/knowledge/spec` (removes the two stale untracked records), then `git branch -f main e0ac5dd` (moves main back — safe because we are not on it). Verify: `git log --oneline -2` shows e0ac5dd at HEAD, and `git rev-parse main` equals `git rev-parse HEAD`. These are destructive, approval-gated operations — the user has explicitly requested this rollback.
- [x] 3. **Verify the rolled-back build** — Run `cargo test` unpiped, then `"exit=$LASTEXITCODE"` to read the real exit code (never trust a piped exit code). Must be green — e0ac5dd was green before the vendor merge, and #![deny(warnings)] means a green run also proves zero warnings. If anything fails, STOP: the rollback landed wrong or the target was misidentified — do not 'fix' forward.
- [x] 4. **Record the rollback in memory and backlog** — First re-verify the live memory IDs via memory_search (query: think_tags vendor policy merged b05e72d). Then memory_supersede each of the b05e72d-merged records (PLAN 0d49d31c, SPEC 8b13e90a, SPEC 85c86c30, BUG baad2dc1) with rolled-back status: 'ROLLED BACK from main 2027-01-09 — main reset to e0ac5dd; vendor policy + think_tags removed; the DeepSeek/GLM reasoning leak is OPEN on main again; recovery via reflog b05e72d'. Then memory_write a DECISION record (tier semantic): rollback rationale (vendor-specific complexity grew while DeepSeek degraded and GLM leaked literal think tags via the the LiteLLM proxy), what stays (e20b1df GLM stop-boundaries, e0ac5dd 429-fallback + heartbeat), and that the next leak-fix direction is TBD. Then backlog_status id=960e4ffb status=failed with note: 'Obsolete — vendor/think_tags fields removed from main by the 2027-01-09 rollback to e0ac5dd.'
