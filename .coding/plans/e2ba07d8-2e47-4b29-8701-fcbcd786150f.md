# Plan: Fix the freeze: trace.rs lock inversion + main-thread trace commands

## Goal
Fix the recurring UI freeze by eliminating (a) the LlmRequestLog lock-order inversion in the trace writer (log_path guard held across records.lock() + up-to-8MiB write_records_to_file) and (b) the sync trace commands (list_llm_requests, clear_llm_requests, get_trace_logging) that run on the Tauri main thread and block on those same locks. Add a regression test that fails on the pre-fix code.

## Kind
bug_fixing

## Context
Evidence: 12 watchdog reports in .coding/logs/hang-*.txt (8/21 02:42 → 8/22 13:49), all showing main-thread stall with 0.0% CPU (blocked, not busy). 10 of 12 are ~10s stalls that recover. Documented bug (memory fbd81a71): src/provider/trace.rs writer_main (~line 951) keeps the log_path MutexGuard alive across records.lock() (962) and write_records_to_file (969) — edition-2021 if-let temporary; with_record (588) holds records while maybe_log_error locks log_path → lock-order inversion. src-tauri/src/ipc/trace.rs has 3 SYNC #[tauri::command]s (list_llm_requests, clear_llm_requests, get_trace_logging) — Tauri v2 runs sync commands on the main thread; they lock the same records/log_path mutexes the writer thread holds during its 8MiB clone+write. get_llm_request and set_trace_logging already use the async + spawn_blocking pattern (reviews N1/N6). Parent plan: 308b845 (research, audit UI freeze candidates) — resumes at step 3 (blocking file/DB I/O) when this sub-plan pops.

## Steps
- [x] 1. **Reproduce with failing regression test** — Write a regression test that reproduces the defect (constitution: every defect gets a regression test that fails without the fix and passes with it). Run it and confirm it FAILS.
- [x] 2. **Document root cause** — Investigate and document the root cause. memory_write a BUG: record (symptom → root cause → fix + regression test name, ≤600 chars).
- [x] 3. **Minimal fix** — Apply the minimal fix that makes the regression test pass. Do not refactor unrelated code.
- [x] 4. **Verify** — Run the regression test + the full test suite (cargo test unpiped, warning-free). Record the regression test name via update_plan (regression_test field) — finish is blocked without it.

## Bug
App UI freezes repeatedly (AppHangB1; hang watchdog shows 12 episodes, main thread 0% CPU → blocked ~10s each). Suspected: trace.rs LlmRequestLog lock-order inversion (log_path guard held across records lock + 8MiB file rewrite) plus sync list/clear/get_trace_logging commands blocking on those locks from the main thread.

## Regression test
provider::trace::tests::writer_never_holds_log_path_while_waiting_for_records
