# Plan: Fix all deep-review findings — 6 sequential batches (A security → B structure → C hardening → D size → E moves → F extractions)

## Goal
Fix all 15 consolidated deep-review findings in six sequential, independently-shipped batches: A security+stall, B structural correctness, C quick hardening, D mechanical size reduction, E moves+snapshot+frontend, F medium-risk extractions — net ≈ −1,000 LOC, zero regressions, each batch reviewed/committed/merged before the next.

## Kind
implementation

## Context
Implements the 2026-04-18 deep-review backlog (.coding/reviews/2026-04-18-deep-review.md + area reports -security/-maintainability/-architecture.md — all High/Medium findings pre-verified in-session with file:line refs). USER DECISIONS (ask_user, this session): (1) A2 plans_dir fix = PER-AGENT plan dirs (.coding/plans/agents/<id>/ for non-main spawns); (2) execution = SEQUENTIAL BATCHED plans — each batch its own feature branch + review + commit + merge to main before the next batch starts; (3) INCLUDE the two medium-risk extractions (R2 EventCoordinator, R4 settings-policy-to-lib) as the final batch. Batch order preserves severity: security/stall → structural correctness (incl. live resolved_model drift bug) → hardening → mechanical size reduction → moves/snapshot → extractions. Env: Windows/PowerShell; cargo test UNPIPED (read test-result lines, piped exit codes lie); deny(warnings) — no #[allow], no dead code; every batch runs root cargo test + src-tauri cargo test AND cargo build (root test does NOT compile the bin crate) + frontend npx vitest run + npm run build. Reviewer per batch reviews ALL uncommitted changes; fix every finding, never defer; commit includes the review report; merge via git checkout main + merge --no-ff (approval-gated) + post-merge npm build + src-tauri cargo build + branch delete. Key file refs (verified at HEAD): search.rs:44,150-176; search_read.rs:118-144; cmd_class.rs:246,249,319-331; tool/agent/mod.rs:40-46; read_files.rs truncate_to_boundary; run_all.rs:472-475 vs :568, :103-184, :632; events.rs:169,268-307; openai.rs:205-222,487-496; vision.rs:154-159; factory.rs:326-330; spawn.rs:121-131,143-148,264-274; agent.rs:447-519 (set_resolved_model at :514); settings.rs:310-321,330-381,401-416,59-67/767-775,963-1260,1216-1248; file_write.rs:86-148,175-186; file_append.rs:89-106; file_edit.rs:534+,586; main.rs:250-368,741-771; rewire.rs:58-92; useAgentStore.ts:700-833; StatusBar.tsx:213-258; App.tsx:166-233; agent/tests.rs (28 fixture blocks); agent.rs:194-278,704-770 (send/stats bodies).

## Steps
- [x] 1. **Batch A — security + stall fixes (5 items, branch feat/deep-review-a-security)** — Create a SUB-PLAN (create_plan, it auto-pushes) with these steps, then execute/review/commit/merge it: (A1, HIGH) search-glob sandbox escape: in src/tool/agent/search.rs + search_read.rs — validate the glob arg before use (reject absolute paths, drive prefixes like `C:`, UNC `\\`, and any `..` component on / and \; return a clear tool error); defense-in-depth skip any glob entry whose path does not start_with the canonical root before is_file/read_to_string; make should_search fail CLOSED (strip_prefix(root) Err → return false, search.rs:44); regression test: file in tempdir PARENT, glob `../**/*.txt` → no match, no read. (A2, HIGH) cmd_class auto-approve bypass (src/safety_rules/cmd_class.rs): classify → None if any statement contains `$(`; is_benign_expression: only single-quoted literals + bare `$name` vars (no `(`), reject statements starting with `"`; with_subcommand/npm_class/npx_class: tokens[1] starting with `-` → None (leading flags now unclassifiable); reject any classified token containing `(`/`)`/backtick; table-driven tests for the four vectors (`cargo test; $(Remove-Item …)`, `cargo test; "exit $(evil)"`, `cargo test $(evil)`, `git -C ..\other checkout -- .` → None/no-rule-match) + update existing lenient tests (e.g. git -c … status now → None). (A3, MED) cap_tool_output panic: reuse char-boundary-safe truncation (make truncate_to_boundary pub(crate), tool/agent/mod.rs:40-46) + test with 200 KB of `é`. (A4, HIGH) forwarder git stall: wrap run_all git ops (checkpoint/commit_success/rollback, run_all.rs:103-184) in tauri::async_runtime::spawn_blocking (own the arg Strings; map errors); clone project root BEFORE checkpoint in run_all_dispatch_next mirroring run_all.rs:568 (currently :472-475 holds the lock); keep on_main_turn_resolved ordering. (A5, MED) 401/403 body suppression: extract shared helper from openai.rs:205-222; apply to main chat path openai.rs:487-496 and vision.rs:154-159; tests assert no body text in error strings for 401/403 on all three paths. Full verification matrix; reviewer over ALL uncommitted changes (.coding/reviews/2026-04-18-batch-a-review.md); fix all findings; commit; merge to main (approval-gated) + post-merge builds; delete branch.
- [x] 2. **Batch B — structural correctness (3 items, branch feat/deep-review-b-structure)** — Sub-plan then execute/review/commit/merge: (B1) per-agent plan dirs (user decision): non-main spawns get their own plans dir — factory build path gains a per-agent dir variant (plans_dir.join(format!("agents/{id}"))); apply to UI-spawned parentless agents in spawn_agent_shared (subagents already can't mutate; main keeps .coding/plans/); ensure stack.json sidecar + plan markdown land per-dir, get_plan/get_workflow_state resolve plan ids across dirs, and load_latest for a fresh side agent starts empty (not resuming main's plan); tests: two parentless agents create plans → stacks don't clobber; document main_agent_id (= min parentless id) backlog-target semantics in a comment at src/runtime/mod.rs:127-133. (B2) config-write unification: new src-tauri/src/ipc/config_io.rs — persist_and_reload(state, config), swap_live_provider(state, provider, ctx_mgr) which ALWAYS clears resolved_model (port set_model's behavior agent.rs:514 — fixes the live drift where save_endpoints settings.rs:371-373 doesn't), set_runtime_safety; rewire save_endpoints (settings.rs:310-321,330-381), save_settings (:1216-1248), set_model (agent.rs:447-519); contract fixtures must stay byte-identical (ipc/contract_fixtures.rs + ipc-contract.test.ts). (B3) Sandbox::validate_for_write ladder: new method on Sandbox (validate → creation-fallback → is_protected_write_target → mkdir parents → revalidate) with ONE shared refusal message; rewire file_write.rs:86-148 + its prepare_for_approval:175-186, file_append.rs:89-106 (now creates parent dirs — consistency decision), file_edit.rs:534+/586; move/adopt the M5 creation-gap tests to sandbox.rs. Full matrix; reviewer (.coding/reviews/2026-04-18-batch-b-review.md); fix all; commit; merge; delete branch.
- [x] 3. **Batch C — quick hardening (2 items, branch feat/deep-review-c-hardening)** — Sub-plan then execute/review/commit/merge: (C1) allowlist fail-closed: compute_subagent_allowlist (src-tauri/src/ipc/spawn.rs) — a KNOWN parent_id whose loop is missing returns Some(vec![]) (deny all) instead of None; None only for genuinely parentless spawns; update missing_parent_loop_returns_none test and audit the spawn_agent-tool fallback paths (:264-274) for the same semantic. (C2) NTFS ADS: in is_protected_write_target (src/tool/agent/sandbox.rs) reject any final path component containing `:` after the drive prefix (compare the component before the first `:`); test `.coding/memory.db:evil` → refused. Full matrix; reviewer (.coding/reviews/2026-04-18-batch-c-review.md); fix all; commit; merge; delete branch.
- [x] 4. **Batch D — mechanical size reduction (branch feat/deep-review-d-mechanical, target ≈ −570 LOC)** — Sub-plan then execute/review/commit/merge: (D1) agent/tests.rs fixture builder (−400): cfg(test) test_support module (shared via src/agent/mod.rs) — test_agent(dir, provider) + TestAgentBuilder (.context/.agent_id/.safety); consolidate the 6 mock LlmClient impls from runtime/agent.rs tests; migrate all 28 construction sites (agent/tests.rs:102-123,154-174,382-405,542-565,1304-1308,2336-2342,2681-2688,2918-2925,…). (D2) IpcState accessors (−115): factory()/memory_store()/safety_rules()/set_safety(mode)/embedder_status() + send_cmd(state,id,cmd,label) helper; rewrite the 6 send-command bodies (agent.rs:194-278), 3 stats bodies (:704-770), and the repeated ok_or_else messages across agent.rs/embeddings.rs/memory_debug.rs/settings.rs/spawn.rs. (D3) misc in-file dedup (−55/−30/−28): fallback_ipc_state() for main.rs:250-368 degraded arms; AgentLoopMap alias in state.rs (8 spellings); run_all end_run helper (5 copies of clear+emit exit sequence :466-468,486-488,507-509,626-628,705-706) + drop self-qualified crate::ipc::run_all:: paths (7); fix settings.rs:401-416 triplicated/orphaned comment block; dedup pricing map (settings.rs:59-67 ≡ 767-775); openai.rs doc-line dup (:263-264) + parse_models_with_vision arm merge (:356-405) + u64 usage extractor (:1066-1085); StatusBar useClickAway hook (4 identical effects :213-258). Full matrix; reviewer (.coding/reviews/2026-04-18-batch-d-review.md); fix all; commit; merge; delete branch.
- [x] 5. **Batch E — moves + startup snapshot + frontend table (branch feat/deep-review-e-moves, ≈ −170 LOC net)** — Sub-plan then execute/review/commit/merge: (E1, R1) pure moves: BacklogStore → myharness::backlog (new lib module, tests travel), run-all git ops (git/checkpoint/commit_success/rollback, run_all.rs:103-184) → myharness::project::git_ops; IPC modules import from lib; fix A9 while there (run_all.rs:632 swallowed dispatch error → log + mark item). (E2, R3) dedup maybe_reembed (main.rs:741-771 ≡ rewire.rs:58-92) into one helper in rewire.rs or src/memory/. (E3, R7) startup_snapshot() IPC command returning {agents, context_caps, ALL workflow states, backlog, embedder_status}; App.tsx:166-233 replaces its 5 sequential pulls with one invoke (closes the stale-non-active-workflowStates gap); typed wrapper in tauri.ts. (E4) frontend COLOR_PREFS table (appearance.ts): one const of {stateKey, ls, def} × 14 + applyColorPrefs/writeColorPrefs; collapse the 11 hand-written actions + resetColors/resetAppearance (useAgentStore.ts:700-833); facade re-exports unchanged. (E5) delete get_config command (settings.rs) — frontend migrates to get_settings (StatusBar.tsx, tauri.ts; general/endpoints/pricing are a strict subset — verify field-by-field before deleting). Full matrix; reviewer (.coding/reviews/2026-04-18-batch-e-review.md); fix all; commit; merge; delete branch.
- [x] 6. **Batch F — medium-risk extractions, two sub-plans (branch feat/deep-review-f-extractions)** — Two sequential sub-plans on ONE branch, each with its own reviewer pass before the next starts: (F1, R2) EventCoordinator: extract the forwarder's orchestration (TurnResolveLatch wiring, running-flag flips, notify_parent_on_completion, cleanup_inactive_subagents, per-agent workflow-state tracking — events.rs:59-196,243-330,419-557) into a lib-side coordinator fed by the channel and emitting through an EventSink trait; src-tauri events.rs keeps only rx.recv() + app.emit glue; add unit tests for the previously untestable arms (Started/Finished/Exited flag flips, notify-parent lock dance, cleanup FIFO); preserve ordering semantics exactly (forwarder still single-consumer; run_all calls stay behind the same await points). Reviewer pass 1 (.coding/reviews/2026-04-18-batch-f1-review.md) → fix all → re-test. (F2, R4) settings policy → lib: move validation + patch-application (endpoint/model validation settings.rs:206-279, [models] double-Option patch semantics :818-881,:1153-1182, summarize-range/theme/sentinel rules :1129-1140) into myharness::config::patch; command bodies become validate-via-lib + persist + helpers; wire structs STAY in the adapter and fixtures stay byte-identical; new lib unit tests for the moved policy. Reviewer pass 2 (.coding/reviews/2026-04-18-batch-f2-review.md) → fix all. Full matrix; single commit for the branch (or one per sub-plan if findings warrant); merge; delete branch. Final wrap-up: confirm all 15 backlog items closed against .coding/reviews/2026-04-18-deep-review.md; ≤3-line summary.
