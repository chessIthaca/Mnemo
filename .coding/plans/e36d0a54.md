# Plan: Branch prep: sync wt/mnemo with main, keep the local-only Laya knowledge commits

## Goal
Bring the divergent local branch `wt/mnemo` up to the latest `main` (614c42f7, PR #6 merged) and make it the working branch, WITHOUT losing the local-only work: commit 11a16560's seven `status = "superseded"` front-matter lines and the seven untracked superseder records `.coding/knowledge/spec/2027-01-11-*-2.md`. The deleted remote branch (stale tracking ref origin/wt/mnemo = ec4605d) is NOT restored — no push in this plan.

## Kind
research

## Context
Topology verified this session by reading .git/refs, .git/packed-refs, .git/logs/HEAD and git show. HEAD = refs/heads/main = 614c42f70206dc1225d028789fb04b89e218bf40 (= origin/main, PR #6 merge). Local branch wt/mnemo = 11a16560bd5cf7f96bb8ee22862d85117de3076e ("laya files", parent ec4605d) — its ONE local-only change is `status = "superseded"` added to the front matter of 7 files: .coding/knowledge/spec/2027-01-11-knn-overlay-for-failure-triage-landed-design-pla.md, -laya-classifier-foundation-opt-in-classifier-tra.md, -laya-failure-triage-startup-fine-tune-landed-des.md, -laya-managed-runtime-sidecar-lifecycle-setup-inv.md, -laya-memory-auto-typing-confidence-gated-prefix.md, -multi-edit-tool-compact-line-op-grammar-for-file.md, -token-dashboard-savings-aggregates-get-savings-s.md. Reading ...-opt-in-classifier-tra.md on main confirms it has NO status line there. The matching successor records are UNTRACKED in the working tree (git status --short shows 7 "??" entries): the same 7 names with a -2.md suffix, 765-935 B each, front matter `supersedes = "<slug>"` — committing only one half of a supersede pair corrupts the knowledge store, so both halves must land. .git/refs/remotes/origin/wt/mnemo = ec4605dfa9e9fec1940f94a0c95711f4d89dcc99 is a STALE tracking ref (the human merged PR #6 and the remote branch was deleted). Merge geometry: ec4605d is an ancestor of both sides (main = merge(02d051f, ec4605d); 11a16560 = ec4605d + 1 commit), so merge base = ec4605d and main's tree should equal ec4605d's tree — the merge is content-clean and NO source file changes anywhere in this plan (checkout main->wt/mnemo differs only in the 7 .coding files). Verification commands: `git status --short`; `git log --oneline -3`; `git merge-base --is-ancestor main HEAD`; `git diff main --stat` (expect ONLY .coding/** paths); `cargo test` run unpiped with `$LASTEXITCODE` read directly. Environment caveat (recorded HOW memory): src-tauri/mnemo-app cannot be built on this Windows machine (Application Control policy), so scope the test run to the crate that builds if the workspace build trips on it. Recovery anchor if the branch ref is moved: 11a16560 (also in .git/logs/HEAD).

## Steps
- [x] 1. **Snapshot the topology before any mutation** — Read-only (no-code research marker: inspects git state only, writes nothing). Record the pre-state so nothing can be lost if a later step goes wrong: `git log --oneline -3` (expect main = 614c42f7), the local branch tip .git/refs/heads/wt/mnemo = 11a16560bd5cf7f96bb8ee22862d85117de3076e, the stale .git/refs/remotes/origin/wt/mnemo = ec4605dfa9e9fec1940f94a0c95711f4d89dcc99, and `git status --short` (expect exactly 7 untracked .coding/knowledge/spec/2027-01-11-*-2.md files). Commit 11a16560 = 7 insertions of `status = "superseded"`.
- [x] 2. **Switch to wt/mnemo as the working branch** — create_plan was called with branch="wt/mnemo", so the EXISTING branch is reused (checked out) rather than re-forked from main — the checkout touches only .coding/knowledge/spec/2027-01-11-*.md (main and wt/mnemo are identical elsewhere). Verify afterwards: .git/HEAD = `ref: refs/heads/wt/mnemo` AND .git/refs/heads/wt/mnemo still = 11a16560. If the tip was moved/reset to main, restore it before continuing: `git branch -f wt/mnemo 11a16560bd5cf7f96bb8ee22862d85117de3076e` (while not checked out on it) or `git reset --hard 11a16560` (while on it) — the commit is also recoverable from .git/logs/HEAD.
- [x] 3. **Confirm the local-only work survived the checkout** — Run `git status --short`: the 7 untracked files .coding/knowledge/spec/2027-01-11-*-2.md must still be listed (untracked files follow the checkout; if any is missing, stop and restore it before going on). Then read .coding/knowledge/spec/2027-01-11-laya-classifier-foundation-opt-in-classifier-tra.md and confirm its front matter now carries `status = "superseded"` — it does NOT on main, so this proves we are on the local-only tip.
- [x] 4. **Merge main into wt/mnemo (approval-gated)** — Core operation — run it through the approval-gated git tool (`git merge main`, no rebase, no force). No-code research marker: this git-only step changes no source file; the only content-bearing paths in play are the 7 .coding/knowledge/spec/2027-01-11-*.md originals (whose `status = "superseded"` lines must survive) plus .coding/backlog.jsonl (union merge driver). Merge base is ec4605d and the only branch-unique change is those 7 front-matter lines, so expect a content-clean merge commit. On conflict, resolve by hand keeping BOTH main's body content and the added `status = "superseded"` lines — never `-X ours`/`-X theirs`. Verify: `git log --oneline -3` shows the merge commit on wt/mnemo and `git merge-base --is-ancestor main HEAD` exits 0.
- [x] 5. **Commit the untracked knowledge records** — The 7 superseder records plus any other untracked .coding/knowledge/** file produced this session: `git add .coding/knowledge/spec/2027-01-11-*-2.md` then commit on wt/mnemo, e.g. `git commit -m "coding: commit knowledge superseder records (Laya suite, token dashboard, multi_edit)"`. These are the new files the user wants carried into the branch — .coding/knowledge travels with git. Do NOT push.
- [x] 6. **Verify the final branch state** — No-code research marker: read-only verification. .git/HEAD = refs/heads/wt/mnemo; `git status --short` clean; `git diff main --stat` lists ONLY .coding/** paths (proves zero source divergence from main); the 7 originals under .coding/knowledge/spec/ carry `status = "superseded"`; the 7 -2.md records are tracked (`git ls-files .coding/knowledge/spec`).
- [x] 7. **Run the project's tests** — From the project root C:/AgenticCoder/AgenticCoder (workspace manifest Cargo.toml) run `cargo test` UNPIPED and then read $LASTEXITCODE directly (a piped exit code is the pipe's, not cargo's). No-code research marker: no source file changed in this plan — the merged tree's src/** is byte-identical to main's — so a green run is expected; report the `test result:` lines. If the workspace build trips on src-tauri/mnemo-app (unbuildable on this Windows machine per the recorded HOW memory), scope the run to the crate that builds (`cargo test -p mnemo`) and say so in the report.
