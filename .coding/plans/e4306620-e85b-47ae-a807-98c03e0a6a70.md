# Plan: Freeze-fix review findings — M1/M2 + L1–L6 remediation + closing commit

## Goal
Fix every finding from the 2026-04-19 freeze-fix review (M1, M2, L1–L6 + F2 doc accuracy), re-run the full test matrix, and land the closing commit on feat/codegraph containing the freeze fixes, review fixes, review report, and the in-flight CodeGraph WIP.

## Kind
implementation

## Context
Implements the findings from .coding/reviews/2026-04-19-freeze-fixes-review.md (the reviewer pass over the completed freeze-fix work + in-flight CodeGraph WIP, all uncommitted on branch feat/codegraph). Reviewer verdict: freeze-fix core correct, no High bugs; required fixes = M1, M2, L3, plus fix-or-waive on L1/L2/L4/L5/L6 (fixing all — no waivers). Findings: M1 = src-tauri/src/main.rs:940-957 sets codegraph indexing flag then moves the Arc into spawn_blocking so it is never cleared → stuck "indexing" forever; panic-path comment also wrong. M2 = frontend agentEventReducer.ts:263-281 — same-name tool calls merge into ONE transcript entry whose calls array (args+outputs) grows unboundedly; MAX_TRANSCRIPT_ENTRIES never fires. L1 = planDiffs unbounded (one entry per distinct path, full diff bodies) until topPlanId changes. L2 = App.tsx poll dropped the `cancelled` unmount guard. L3 = F1b has no regression test — extract pure didMainTurnEnd helper + test all transitions. L4 = src/codegraph/mod.rs store locks use .lock().expect → poisoned mutex panics all callers. L5 = read_git_branch inherits GIT_DIR/GIT_WORK_TREE/GIT_INDEX_FILE env → not hermetic. L6 = codegraph.db lacks restrict_permissions and is not on the sandbox protected-write-target list. F2 doc accuracy = write_records_to_file doc should state residual behavior (below-cap rewrites; over-cap drops non-batch rows; oscillation possible). Constraints: Windows 11 + PowerShell; run commands unpiped and read $LASTEXITCODE; warning-free under #![deny(warnings)]; every defect fix gets a regression test; preserve line endings. After the last step the closing commit lands on feat/codegraph including ALL staged changes (freeze fixes + review fixes + CodeGraph WIP); the commit message must say it includes in-flight CodeGraph WIP and state the F6 no-change justification. Test matrix: cargo test (root), cargo test in src-tauri/, npm test + npx tsc --noEmit in frontend/.

## Steps
- [x] 1. **M2 — bound merged tool-card calls** — In frontend/src/hooks/agentEventReducer.ts add `export const MAX_CALLS_PER_TOOL_CARD = 50;` (doc comment: review M2 — same-name merges made one entry's calls array grow unboundedly; past the cap a NEW card is pushed instead). In reduceToolCallStart's canMerge (~:260) add `&& last.calls.length < MAX_CALLS_PER_TOOL_CARD`. reduceToolResult already finds the matching call by id across entries, so split cards still resolve results — verify while testing. Regression test in frontend/src/hooks/useAgentStore.test.ts: dispatch 60 same-name tool_call_start events → expect exactly 2 tool entries (50 + 10 calls); then a tool_result for a call id in the SECOND card updates that card's call in place. Run `npm test` in frontend/.
- [x] 2. **L1 — cap planDiffs** — In frontend/src/hooks/agentEventReducer.ts add `export const MAX_PLAN_DIFFS = 100;` (doc comment: one entry per distinct edited path holding full diff bodies; uncapped it grew for the life of the plan). In upsertPlanDiff (~:108-110) return `list.slice(0, MAX_PLAN_DIFFS)` after the prepend+dedupe (array is newest-first, so slice(0,N) keeps the most recent paths and drops the oldest). Regression test in useAgentStore.test.ts next to the existing 'planDiffs dedupes per path, newest first' test: upsert 101 distinct paths → length 100, newest first, oldest dropped. Re-export the const through the useAgentStore.ts facade only if tests import it from there. Run `npm test`.
- [x] 3. **L3 + L2 — F1b edge helper + unmount parity** — In frontend/src/hooks/agentState.ts add `export function didMainTurnEnd(prev, next): boolean` taking two `{ agentParents, agents }` snapshots: resolve the main agent id in each via selectMainAgentId, read `agents[id]?.running ?? false`, return true ONLY on a true→false edge (doc comment: F1b turn-end refresh, drives the git-branch refresh in App.tsx; an id change mid-edge or the main agent being removed reads as running false → fires, which is harmless/correct). In App.tsx: import didMainTurnEnd (re-export from the useAgentStore.ts facade alongside selectMainAgentId) and use it inside the useAgentStore.subscribe listener; restore the unmount guard in the poll effect — `let disposed = false;` + a local refresh wrapper that skips when disposed, used by the interval + focus listener, flipped in cleanup. Tests in useAgentStore.test.ts: false→false, false→true, true→true return false; true→false returns true; main agent removed between snapshots (was running) returns true; main id shifts to a different non-running agent returns true.
- [x] 4. **M1 — indexing flag + panic path** — Read src/codegraph/mod.rs (index(), set_indexing/is_indexing, the indexing_flag_flips_around_pass test) and the startup block at src-tauri/src/main.rs ~:930-960 plus the factory wiring exposing the handle. Move flag management INSIDE CodeGraph::index: set_indexing(true) at entry, clear on EVERY exit path (success, Err return — explicit set_indexing(false) before each return or a small Drop guard). main.rs then drops its manual g.set_indexing(true) and just awaits spawn_blocking(index). Reconcile the panic-path comment: if the wiring lets main.rs drop/None the handle on the Err (panic) arm, do that (tools then omitted as documented); otherwise rewrite the comment to state the actual degrade-to-error-at-call-time behavior. Keep indexing_flag_flips_around_pass green and extend it to assert the flag is false after an index() Err too. Run `cargo test codegraph` then full root `cargo test`.
- [x] 5. **L4 + L6 — codegraph robustness + protection** — (1) L4: in src/codegraph/mod.rs replace every `.lock().expect(...)` on the store mutex with a private helper (e.g. `fn store(&self) -> std::sync::MutexGuard<'_, Store>`) that recovers from poisoning via `Err(p) => p.into_inner()` (doc comment: the db is a derived cache — a panic mid-write is recovered by rebuild, so poisoning must not cascade into every caller panicking). Use it at all five sites (~:118, 126, 170, 191, 239). (2) L6a: in src/codegraph/store.rs apply `crate::config::keys::restrict_permissions(path)` best-effort after opening/creating the SQLite connection (swallow errors with a warning eprintln, mirroring restrict_log_file in src/provider/trace.rs:765-775). (3) L6b: find the protected-write-target check (search `is_protected_write_target` / the memory.db protection in src/tool/agent/sandbox.rs) and add the codegraph db + its `-wal`/`-shm` sidecars exactly mirroring the memory.db pattern; add a sandbox test mirroring `protected_memory_db_and_sidecars`. Run root `cargo test`.
- [x] 6. **L5 + F2 docs — git env + trace doc truth** — (1) In src-tauri/src/ipc/files.rs read_git_branch: add `.env_remove("GIT_DIR").env_remove("GIT_WORK_TREE").env_remove("GIT_INDEX_FILE")` to the Command with a doc comment (env overrides would silently point the status-bar branch read at a different repo; also makes the regression test hermetic). Existing test covers behavior unchanged. (2) In src/provider/trace.rs write_records_to_file's doc block: replace the capped-file bullet with explicit residual behavior — below the cap each writer wakeup still re-reads + rewrites up to ~MAX_TRACE_FILE_BYTES; when the file exceeds the cap the merge is skipped and rows not in the current batch are dropped, so the file is effectively recent-history-past-8MiB; with a hot ring (32 × 2 MiB bodies) the file can oscillate grow→fresh-start. Run root `cargo test` and `cargo test` in src-tauri/.
- [x] 7. **Verify all + closing commit** — Run, each unpiped reading $LASTEXITCODE: root `cargo test` (expect ~990+ green, zero warnings), `cargo test` in src-tauri/, `npm test` + `npx tsc --noEmit` in frontend/. Fix anything red before continuing. Then commit on feat/codegraph (never main) — the git tool stages all changes (git add -A) — with a message covering: freeze fixes F1/F3/F1b/F2 implemented; F6 verified already-at-HEAD (no change — diagnosis flagged it from a stale read; prev_workflow_state.remove exists at the events.rs Exited arm); review fixes M1/M2/L1-L6 per .coding/reviews/2026-04-19-freeze-fixes-review.md; INCLUDES in-flight CodeGraph WIP from the paused parent plan. Confirm the commit includes the review report file.
