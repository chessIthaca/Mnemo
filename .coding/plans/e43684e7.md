# Plan: Enforcement ladder C1–C4: alternation absorption, literal-metachar hint, nudge escalation, redirection warning

## Goal
Ship the enforcement ladder C1–C4: alternation-regex symbol absorption (embed graph ids inline), literal+metachars zero-hit hint, counter-driven escalation for repeat-ignored actionable nudges, and the shell-redirection warning — advisory escalation, never gates, per the 2026-09-17 tally analysis.

## Kind
implementation

## Context
User-directed follow-up (2026-09-17, "so nothing to improve this time?") to the merged findings round (plan 44387fcd, main @ ad8be7f). Evidence: the 2026-09-17 session tally (~26 text searches, ~9 avoidable) in HOW 2026-08-29-2026-09-17-session-tally-nudge-ignored-param-mis.md. VERIFIED SITES: C1 — symbol_nudge at src/tool/agent/search.rs:474+ (detection: strip_definition_prefix:420 handles pub/async/unsafe/extern + one definition keyword + space → bare-identifier remainder, OR is_bare_identifier:402; then exact graph lookup; note embeds "is an indexed symbol" + graph_context(id=...) — SEARCH_NUDGE_MARK wording contract, the bare-identifier sentence is byte-pinned per the 2026-09-15 steering spec, the alternation variant must ADD not change it). Observed waste shapes: "fn .*watcher|struct.*Watcher", "struct Memory(Supersede|Update)Tool" — top-level | split with per-branch reduction (def-prefix strip + leading .* strip → bare identifier) covers the first class; inline groups stay out of scope (conservative). C2 — pattern.rs compile_with_fallback (CompiledPattern{regex, fallback_note}); fire-site = search.rs/search_read.rs EMPTY-result branches (index "no matches" + walk empty), gated on args.literal && pattern has regex metachars (conservative set \^$.|?*+()[]{}). C4 — shell.rs:260-262 prepend point (is_grep_family → GREP_NUDGE); add parallel redirection check (" >$null" / ">/dev/null" / ">nul" substrings) + fired-only MarkerKind::ShellRedirect (9th kind: MARKERS len 8→9, Inner::counts, index 8, label "shell-redirect", detect = tool shell + first line; targets []). C3 — steering_stats Inner {counts, last_fired} gains per-agent per-kind fired/switched maps + escalated set; observe_result increments agent_fired; escalation queued when an ACTIONABLE kind fires for the 3rd+ time for the same agent with zero per-agent switches (one-shot per agent+kind); new take_escalations(agent_id) -> Vec<String> called in dispatch_with_interrupt AFTER observe_result at BOTH return arms, appended to successful results (the escalation phrasing must NOT contain any existing marker substring). Constraints: F6 schema budgets (NO schema description changes); #![deny(warnings)] zero-warning; cargo test before each step completes; README/PLAN carry the detail. Deliberately NOT enforceable (per the HOW record): no gating of grep behind graph; F9/F11 stay fired-only.

## Steps
- [x] 1. **C1: alternation symbol nudge (absorption)** — src/tool/agent/search.rs: extend symbol_nudge (474+) with alternation-pattern absorption. New helper alternation_identifier_branches(pattern) -> Option<Vec<&str>>: split on top-level '|' (a branch containing '(', ')', '[', ']', '{', '}', '\' — anything beyond a leading '.*' — rejects the whole pattern, conservative); per branch strip_definition_prefix, then strip a leading '.*' or trailing '.*', then require is_bare_identifier; cap branches at 3. In symbol_nudge, when the existing single-name paths miss, try the alternation path: look each branch name up via the same exact-name graph lookup the function already uses; when ≥1 branch resolves, return a note variant listing each resolved branch as "<name> is an indexed symbol — graph_context(id=\"<id>\")" joined with '; ' — the variant must CONTAIN the SEARCH_NUDGE_MARK substring but leave the byte-pinned bare-identifier sentence untouched. search_read shares symbol_nudge automatically. Tests (search.rs, mirror bare_identifier_symbol_searches_earn_the_graph_nudge): (a) indexed fixture with watcher/update symbols + pattern 'fn .*watcher|struct.*Watcher' → note names both with embedded ids; (b) one mixed branch ('watcher|hello world') or non-matching alternation → no nudge; (c) bare-identifier note text unchanged (existing byte-pinned asserts stay green). Run cargo test before completing.
- [x] 2. **C2: literal+metachars zero-hit hint** — src/tool/agent/search.rs + search_read.rs: helper has_regex_metachars(pattern) -> bool (chars from the conservative set \ ^ $ . | ? * + ( ) [ ] { }). In search.rs execute, on the EMPTY-result branches — the index path's 'no matches found (engine: index…)' body AND the walk path's results.is_empty() body — when args.literal && has_regex_metachars(&args.pattern), append '; note: the pattern has regex metacharacters but ran as literal text — if you meant a regex, retry with literal:false' (advisory only; non-empty results never carry it, regex-mode misses never carry it). Same in search_read.rs's matched_files.is_empty() branch. Tests: literal '(a|b)' pattern with no content matches → hint present; same pattern WITH a matching file → no hint; regex-mode zero hits → no hint; literal plain word zero hits → no hint. Run cargo test before completing.
- [x] 3. **C4: shell redirection warning** — src/tool/agent/shell.rs: const REDIRECT_NOTE ("TIP: output redirection detected — failure details may be hidden; the tool caps output itself, prefer running the command unredirected") + helper has_blinding_redirection(cmd) -> bool (substring: '>$null' or '>/dev/null' or '>nul' — covers 2>$null / 1>$null / 2>&1>$null forms). At the GREP_NUDGE prepend point (:260-262): if has_blinding_redirection(&args.command) { combined.insert_str(0, REDIRECT_NOTE); } (both notes may co-occur; each on its own leading line). Add MarkerKind::ShellRedirect to src/agent/steering_stats.rs — 9th kind: variant after ConsolidationDue, marker() = "TIP: output redirection detected", targets() = &[] (fired-only), label() = "shell-redirect", index() = 8, detect() = tool_name == "shell" && first_line().contains(marker), MARKERS len 8→9 + Inner::counts len 9 + SteeringStats::new [(0,0);9] + doc-comment rule line. Tests: shell.rs — a command containing '2>$null' prepends the note, clean commands do not (mirror the existing grep-nudge test harness); steering_stats — the note fires shell-only, first-line-scoped (mirror each_kind_fires_only_from_its_own_tool rows: add a matrix row for ShellRedirect). Run cargo test before completing.
- [x] 4. **C3: counter-driven escalation** — src/agent/steering_stats.rs: Inner gains agent_fired: HashMap<AgentId, [u64; 9]>, agent_switched: HashMap<AgentId, [u64; 9]>, escalated: HashSet<(AgentId, usize)>, pending_escalations: HashMap<AgentId, Vec<String>>. observe_result: per detected kind increment agent_fired; when an ACTIONABLE kind (non-empty targets) reaches fired >= 3 for the agent AND the agent's agent_switched[kind] == 0 AND !escalated.contains → insert escalated + push a note "NOTE: the <label> nudge has fired N times this session without a switch — consider following it (the marker's target tools)" (phrasing must NOT contain any existing marker substring). observe_call: after the existing switch increment, also increment agent_switched for the consumed kind. New pub(crate) fn take_escalations(&self, agent_id) -> Vec<String> — drains and returns the agent's pending notes. src/agent/dispatch.rs: in dispatch_with_interrupt, at BOTH result arms, after steering.observe_result(...) call the escalation attach: on success results only, extend r.output with each pending note (one per line, appended like the consolidation note); keep it best-effort and never failing the result. Tests (steering_stats + dispatch-level): fire a search-nudge output 3× for one agent with no observe_call switch → the 3rd queues exactly one escalation (take_escalations returns it once, then empty); an intervening observe_call('graph_context') switch (2 fires → switch → 3rd fire) → no escalation; below 3 → none. Run cargo test before completing.
- [x] 5. **Docs, knowledge, close-out** — README.md steering bullet (the code-intelligence section): append the four escalations (alternation patterns absorb the graph lookup inline with embedded ids; literal+metachar zero-hit retry hint; repeat-ignored actionable nudges escalate once per agent+kind; shell redirection warning + shell-redirect counter). PLAN.md 'Beyond the original spec': one enforcement-ladder bullet (C1–C4, files). Update HOW 2026-08-29-2026-09-17-session-tally-nudge-ignored-param-mis.md via memory_update: mark C1–C4 SHIPPED (branch/commit pointer). Write the round SPEC memory (paths + the deliberate no-gates rationale). Commit everything to the working branch. Then the closing sequence: reviewer → fix findings → re-run tests → commit → finish.
