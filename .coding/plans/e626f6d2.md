# Plan: Ship the c8531c34 audit deliverables (knowledge file, SPEC memory, backlog close-out, commit)

## Goal
Ship the missing deliverables of the c8531c34 tool-call echo audit (plan 34817533): create .coding/knowledge/tool-call-echo-map.md with the verified echo map, record the SPEC memory, run cargo test (constitution compliance; no source changes), mark backlog item c8531c34 done in .coding/backlog.jsonl, supersede the stale backlog-item memory, and commit everything on wt/agenticcoding. Research plan: no source-code changes — .coding/ files only.

## Kind
research

## Context
Plan 34817533 (research, backlog c8531c34 tool-call echo audit) reached Complete, but its steps 5-6 were checked off without the deliverables being written: .coding/knowledge/tool-call-echo-map.md does not exist, no SPEC memory was recorded, cargo test was not re-run, backlog item c8531c34 is still status=pending in .coding/backlog.jsonl, and nothing was committed. All audit evidence is verified (in-conversation): turn.rs:1104-1121 forwards every ToolCallStart unconditionally; six ToolResult sites (1329/1432/1537/1664/1816/1975) close every announced call; channels.rs:570-625 serialization drops nothing; agentEventReducer.ts:486 cards every call (MEMORY_TOOLS at 329 gated by showMemoryActivity, default true per general.rs:322-328; neverGroups at 545 = merge rules only); console.rs:241 render_event prints '→ {name}' for every tool (line 252), Silent set = {Started, ToolCallArgDelta, ContextUsage, PromptDispatched, Phase}; auto-delegation: codegraph.rs:106 symbol_delegation_block + search.rs:1057 memory_delegation_block run lookups inside search with no ToolCallStart; sub-agent calls echo into child transcripts only (events.rs:445-475).

## Steps
- [x] 1. **Write the echo-map knowledge file — create .coding/knowledge/tool-call-echo-map.md with the verified audit: (a) the direct answer — NO LLM tool call is suppressed from the echo; every call echoes into both the GUI chat (ToolCard) and the -console REPL ('→ name'); (b) the six-link echo chain with file:line refs — turn.rs:1104-1121 unconditional ToolCallStart forwarding; six ToolResult sites (real result 1816, synthetic not-run closers 1329/1432/1537/1664/1975, backlog 63cbc20f); channels.rs:570-625 into_serializable drops nothing; agentEventReducer.ts:486 reduceToolCallStart cards every call (consecutive same-name merge 520-527, neverGroups 545 = shell/search/search_read/browser never merge, capTranscript = display cap); console.rs:241 render_event '→ {name}' line 252, Silent set exactly {Started, ToolCallArgDelta, ContextUsage, PromptDispatched, Phase}; reduceToolResult logs every result to the Output tab; (c) the by-design exceptions — MEMORY_TOOLS {memory_write, memory_search, memory_consolidate, memory_update, memory_supersede, memory_delete} hidden from GUI chat when [ui] show_memory_activity=false (default true, general.rs:322-328; console ignores it), auto-delegated graph/memory lookups inside search/search_read (codegraph.rs:106, search.rs:1057 — no ToolCallStart, answer rides inline 'AUTO-DELEGATED…'), sub-agent tool calls echo into the child transcript only (parent sees spawn_agent + ChildFinished), internal harness ops echo via dedicated events (MemoryRecalled, CompactStarted/Compacted, VisionDescribe/VisionDescribed); (d) why graph/search cards are rare — read_files is the workhorse and merges into few cards, auto-delegation absorbs symbol lookups (worked example: this audit session issued many auto-delegated searches, zero direct graph_* calls), direct graph_* chains are rarer; (e) optional follow-ups (not implemented): synthetic transcript marker on auto-delegation or the AUTO-DELEGATED prefix on the search card chip.**
- [x] 2. **Record the SPEC memory + verify tests — memory_write (semantic, title 'SPEC: tool-call echo map — every LLM tool call echoes, no suppression') with the compact gist (no suppression anywhere in the chain; the three by-design exceptions; why graph/search cards are rare) + pointer to .coding/knowledge/tool-call-echo-map.md; run cargo test -- --quiet (constitution compliance; no source changes since the last green run — expect 1917 + 16 passed, 0 failed).**
- [x] 3. **Close the backlog item + commit — mark c8531c34 done in .coding/backlog.jsonl via the approval-gated shell residual: replace '"status":"pending","created_at":1788465695,"note":"125838a3d95ac335794d41ea4eba109b396c3305"' with '"status":"done","created_at":1788465695,"note":"Done - research (plan 34817533): no tool call is suppressed from the echo; exceptions = memory tools behind show_memory_activity=false (GUI only, default true), auto-delegated graph/memory lookups inside search/search_read, sub-agent transcripts; see .coding/knowledge/tool-call-echo-map.md"' (use [System.IO.File]::ReadAllText/WriteAllText to preserve encoding); supersede the stale memory 'PLAN: backlog c8531c34-b267-448a-8e73-c01e61523f9b' (id a24c0e21-6d37-578c-bacc-0d5db488dfc9) with the done status; commit the knowledge file + backlog edit on wt/agenticcoding with a message referencing backlog c8531c34.**
