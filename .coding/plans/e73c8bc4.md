# Plan: Cleanup: single-source the merge_to_main procedure (short prompt, no UI override)

## Goal
Make `.coding/skills/merge_to_main.toml` the ONE source of truth for the merge procedure. (a) Its injected prompt shrinks from ~2.5 KB to a ~6-line imperative checklist (rationale stays in the non-injected header comment). (b) The Git tab stops replacing that prompt: `enter_skill`'s `prompt` arg becomes the dispatch message only (overlay is always the registry prompt), and GitView's ~1.6 KB step-by-step duplicate is deleted in favor of a one-line target. (c) agent.md / PLAN.md / docs / comments / the 2027-01-11 SPEC knowledge file are updated so policy, procedure and UI are single-sourced.

## Kind
implementation

## Context
The merge procedure is duplicated three times and the copies have already diverged. (1) `.coding/skills/merge_to_main.toml:57-64` — the `prompt`, ~2.5 KB / ~640 tokens, injected into the system prompt EVERY turn while the skill runs (`src/agent/prompt.rs` workflow_section, Skill state). (2) `frontend/src/components/views/GitView.tsx:76-86` — `mergePrompt(branch)`, ~1.6 KB, the same 8 steps hand-maintained. (3) `frontend/src/components/layout/MergeToMainDialog.tsx:73-81` — user-facing prose.

The override is structural: `src-tauri/src/ipc/agent.rs:850` `let goal = prompt.unwrap_or_else(|| spec.prompt.clone());` feeds BOTH `wf.start_skill(&skill, &goal, ...)` (:853 — the overlay injected every turn) AND `AgentCommand::Prompt { text: goal }` (:875). One arg, two roles: any caller passing a prompt wipes the file-defined skill prompt, including its standing policy lines (which tool may run the merge, never-stash, `.coding/` must land). `GitView.tsx:159` is the ONLY caller that passes one (`StatusBar.tsx:198` passes none), so a view is currently above the source of truth.

Drift already shipped in the GitView copy: `:81` claims "the git tool forces the core-operation approval prompt, shell does not" — false since `ShellTool::never_auto_for` (2027-01-11) gates shell-invoked git merge/push, and agent.md says so; `:78` permits a stash (the skill forbids it — ten orphan stashes, 2026-08-24); it omits that `.coding/` must land in the merge; it says "Do NOT push" against the skill's optional gated push; and `GitView.test.ts:8-11` documents the two-copy drift in its own header.

Test pin: `src/skill/mod.rs:339-381` (`shipped_skill_files_parse_and_merge_to_main_cleans_up_memories`) requires the prompt to contain "MERGED into main", "git fetch origin", "git pull --no-rebase", "--no-ff <branch>" and pins their ORDER (fetch < pull < branch merge). The trimmed prompt must keep those exact substrings. `src/skill/mod.rs:164-167` embeds the file via `include_str!` into `SHIPPED_SKILLS` (recompile to pick up; `seed_skills` is write-if-missing, so other projects keep their copy).

Non-git history: plan b1b45b69 already trimmed this prompt once (~1.9 KB → smaller) and de-duped the `skill_start` tool-result echo (`src/tool/workflow/skill.rs:163-171`); it has since grown back via the sync-first change. `.coding/knowledge/spec/2027-01-11-merge-to-main-syncs-main-with-origin-first-shell.md:8` still lists "all copies" including `GitView.tsx::mergePrompt` — it must be amended once that copy is gone.

Verification (all unpiped; read `$LASTEXITCODE`): `cargo test`; `cd src-tauri; cargo test`; `cd frontend; npm run build`; `cd frontend; npm test`. Warning-free is enforced by `#![deny(warnings)]`.

## Steps
- [x] 1. **Trim the skill prompt + header comment; trim agent.md's merge bullet to policy** — Files: `.coding/skills/merge_to_main.toml`, `agent.md`.
TOML `prompt` (lines 57-64) → a ~6-line imperative checklist, target ≤900 chars (measured now: ~2540, i.e. −65%), keeping EVERY operative rule and these exact substrings IN ORDER: `git fetch origin` then `git pull --no-rebase` then `--no-ff <branch>`, plus `MERGED into main` (pin: `src/skill/mod.rs:339-381`). Draft:

Merge the branch named in the request (default: the current one) into main and clean up. Steps:
1. Commit ALL uncommitted work on that branch with a clear message, `.coding/` included (plans, knowledge, reviews, backlog.jsonl must land in the merge). NEVER stash.
2. `git checkout <branch>` if needed, then `git checkout main`. Sync main FIRST via shell (the git tool has no fetch/pull): `git fetch origin`, then `git pull --no-rebase`; no remote / nothing new → continue, conflicts → file_edit taking the UNION, then `git add` + `git commit`.
3. Merge with the **git** tool: `git merge --no-ff <branch>`. Conflicts: file_edit taking the UNION of both sides, then `git add` + `git commit`; never drop a side, never `-X ours`/`-X theirs`.
4. Verify the MERGED tree builds — fix and re-run until green: `npm run build` (in frontend/) AND `cd src-tauri && cargo build` (the root build never compiles the bin crate).
5. Green → `git branch -d <branch>` (never -D). Optional `git push` is approval-gated.
6. Supersede this branch's stale branch-status memories with a successor containing "MERGED into main at <sha>" (search branch name + pre-merge tip; skip when nothing matches; never touch another branch's records).
7. `skill_end` (→ Planning). Unresolvable → `abandon_skill`.

Header comment (lines 1-40) → compress to ~20 lines: what the skill is, how it is entered (button + skill_start), why the prompt is terse and the rationale lives HERE (comments are never injected — keep that instruction), the incident one-liners (never-stash; both builds; memories superseded at merge; sync-before-merge; both tools gated — git tool preferred), and the agent.md topology pointer. Do NOT delete the incident rationale, only compress wording.
`agent.md:22` (the Environment merge bullet) → policy-only, ~half its length: sync-first runs on shell (the git tool has no fetch/pull subcommand), merge/push MUST go through the approval-gated git tool, and both paths are gated in code (`GitTool::never_auto_for`; since 2027-01-11 `ShellTool::never_auto_for` recognises a git executable in command position), so a shell-invoked merge/push raises the same always-on prompt. Drop the mechanism narration, keep the rule.
- [x] 2. **Re-scope enter_skill's prompt arg to the dispatch message (kill the overlay override)** — File: `src-tauri/src/ipc/agent.rs` (command `enter_skill`, lines ~800-890).
Today: `let goal = prompt.unwrap_or_else(|| spec.prompt.clone());` (:850) then `wf.start_skill(&skill, &goal, ...)` (:853) + `AgentCommand::Prompt { text: goal }` (:875) + `SkillStarted { prompt: goal }` (:864).
Change to TWO values: the OVERLAY is always `spec.prompt` (the file owns the procedure — `wf.start_skill(&skill, &spec.prompt, spec.target_state, spec.tools.clone())`), and the DISPATCH message is `prompt.unwrap_or_else(|| spec.prompt.clone())` — sent as `AgentCommand::Prompt` and echoed in `SkillStarted.prompt`, so an override can only ever ADD the target, never replace the procedure.
Extract a small pure helper (e.g. `fn skill_prompts(spec_prompt: &str, dispatch: Option<String>) -> (String, String)`) with a doc comment and `#[cfg(test)] mod tests` at the bottom of the file pinning: (a) overlay == spec_prompt when a dispatch override IS passed, (b) overlay == dispatch when it is not, (c) the dispatch is never empty. Update the `enter_skill` doc comment (lines ~800-814) to state the new contract.
- [x] 3. **GitView: delete mergePrompt, send a one-line dispatch** — File: `frontend/src/components/views/GitView.tsx`.
Replace `mergePrompt(branch)` (lines 70-86, ~1.6 KB of steps) with `mergeDispatch(branch: string): string` returning ONE line naming the target, e.g. `Merge branch '<${branch}>' into main and clean up.` — doc comment: the skill file owns the procedure; this only names the target so the user can merge ANY branch (pure, exported for tests: no store, no IPC).
Update the call site line 159 (`enterSkill(activeAgent, "merge_to_main", mergeDispatch(mergeTarget))`) and the module comment lines 28-33 so it no longer claims the tab mirrors the skill's steps.
Grep the file for any other `mergePrompt` reference (the tooltip at :582 is prose — check it still reads true).
- [x] 4. **Frontend tests + tauri.ts contract doc** — Files: `frontend/src/components/views/GitView.test.ts`, `frontend/src/lib/tauri.ts`.
Rewrite `GitView.test.ts` (its current header documents the two-copy drift — delete that framing): assert `mergeDispatch("wt/x")` is a single line that names the branch and contains NO procedure text (no `git fetch`, `--no-ff`, `cargo build`, `npm run build`, `file_edit`, `skill_end`), plus a source-contract assertion (read `GitView.tsx` as text via `?raw`, the pattern used in `ModelsSection` tests) that the call site passes `mergeDispatch(mergeTarget)` to `enterSkill` — i.e. the tab cannot re-author the steps. Keep the existing test file's other tests passing.
`tauri.ts:1535-1542` — update the `enterSkill` doc: `prompt` is the DISPATCH message naming the target; the skill's own prompt from the registry is always the overlay injected every turn (it can no longer be replaced from the UI).
Check `frontend/src/hooks/useAgentStore.test.ts:1247-1264` (skill_started payload) still describes reality — adjust only if a comment there claims the payload is the skill's own prompt.
- [x] 5. **Dialog prose → 3-line summary + docs/knowledge sweep** — Files: `frontend/src/components/layout/MergeToMainDialog.tsx`, `PLAN.md`, `docs/FEATURES.md`, `.coding/knowledge/spec/2027-01-11-merge-to-main-syncs-main-with-origin-first-shell.md`.
Dialog (lines 73-81, currently a 7-item internal step list) → 3 summary lines the user can act on: the agent commits the branch (never stashes), syncs `main` with origin and merges it, resolves conflicts, verifies both builds, deletes the branch. Keep the existing approval sentence (lines 82-86).
`PLAN.md:320` (Branch topology row) — verify it does not name a Git-tab prompt copy; it may keep the sync-first wording (docs/FEATURES.md:34 same).
Amend the SPEC knowledge file via `memory_amend` (id from `memory_search` for "merge_to_main syncs main with origin first"): record that `GitView.tsx::mergePrompt` no longer exists — the skill file is the single source, `enter_skill`'s prompt is a dispatch-only target line, and the overlay is always the registry prompt. Also amend the corresponding SPEC memory content so its "all copies agree" list is no longer stale.
Search the repo for `mergePrompt` and for "overrides it with" to confirm zero stale references remain (expect hits only in the new GitView test/doc).
- [x] 6. **Run the full test suite unpiped + docs check** — Run each command UNPIPED and read `$LASTEXITCODE` (a piped exit code is the pipe's, per agent.md): `cargo test` (root lib), `cd src-tauri; cargo test`, `cd frontend; npm run build`, `cd frontend; npm test`. `#![deny(warnings)]` at both crate roots means a green run also proves zero warnings.
Specifically confirm: `src/skill/mod.rs::shipped_skill_files_parse_and_merge_to_main_cleans_up_memories` passes against the TRIMMED prompt (the four pinned substrings + order), and the new `enter_skill` overlay test fails without the fix (revert the `start_skill` arg locally to the old `&goal` to prove it, then restore).
Docs check: re-read the final `.coding/skills/merge_to_main.toml` end to end — the injected prompt must be ≤ ~900 chars and carry every operative rule, and the comment block must still point at agent.md for topology.
