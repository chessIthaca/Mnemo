# Plan: Investigate: model picker not updating

## Goal
Determine why the model picker no longer updates, with file/line evidence. No code changes.

## Kind
research

## Context
User reports "the model picker is no longer updating". Candidate pickers: StatusBar.tsx (toolbar picker), EndpointCard.tsx, VisionSection.tsx, ModelsSection.tsx (Settings). Need to find which picker and why its updates stall — likely causes: cache guard (fetchServerModels cache-key skip), stale store state, or backend set_model not propagating. Working tree has unrelated changes (build.bat, Sidebar.tsx Game icon). No source changes will be made.

## Steps
- [x] 1. **Read the StatusBar model picker** — read frontend/src/components/layout/StatusBar.tsx fully (esp. ~250-500: fetch logic, cache guards, click handlers) plus the model/provider state in frontend/src/hooks/useAgentStore.ts and setModel/listModels wrappers in frontend/src/lib/tauri.ts.
- [x] 2. **Read backend model IPC + provider swap** — read src-tauri/src/ipc/models.rs and src-tauri/src/ipc/commands.rs set_model, and src/provider/openai.rs fetch_models, to see what could make the list or applied model stale.
- [x] 3. **Pinpoint the failure + report** — identify the concrete mechanism (e.g. cache key not invalidated on endpoint change, fetch only on open, store not updated), cite file:line, and state the fix direction.
