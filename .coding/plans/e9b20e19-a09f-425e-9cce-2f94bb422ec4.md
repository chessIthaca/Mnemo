# Plan: Phase 2b — Tool output caps + search early exit + recall/token taxes (Perf M3/H2/H1/M5)

## Goal
Implement Phase 2b exactly per the architecture perf review quick wins and remediation plan: cap unbounded shell/git tool outputs (M3); search early-exit once MAX_MATCHES collected (stop full-file reads and line scans for further matches); cache auto-recall results when the triggering user message text is unchanged within the turn (H2/M5); hoist query.to_lowercase() out of the per-memory loop in recall (M5); reuse prior token count across iterations when only a small tool-result suffix was appended and no summarize occurred (H2 partial). No drive-by refactors. Preserve existing behavior, doc comments, tests (update assertions only where output caps affect golden strings), line endings. Run cargo test (unpiped) before completing the step.

## Context


## Steps
- [x] 1. Inspect current cap behavior and output paths in shell.rs, git.rs (run_git), and compare to file_read/file_write caps. Decide on byte/line caps (reuse ~100KB/2000 lines or similar). Add truncation + note in execute paths. Add/update unit tests for cap behavior.
- [x] 2. Implement search early-exit: once results.len() >= MAX_MATCHES, break out of file/line loops without reading further file contents. Adjust summary note to reflect early stop (e.g. 'and more'). Update the caps_results test expectations if totals change. Keep MAX_MATCHES const.
- [x] 3. Add per-turn auto-recall cache in AgentLoop / turn: before recall, check if the latest user message text equals the last-recalled query for this turn; if yes and results cached, reuse without embed/scan. Invalidate on summarize or large append. Store last_user_query + last_recall_results (Vec<ScoredMemory>) scoped to the turn loop.
- [x] 4. In memory/mod.rs recall: hoist let q = query.to_lowercase(); once before the map/iterator. Use &q for contains checks on lowered title/content. (Still per-m to_lowercase for boost is acceptable for this pass; avoids re-lowering query N times.)
- [x] 5. Token reuse: in turn loop, keep last_token_count. After tool-result append (no summarize), for next iteration use a cheap incremental: last + char_estimate(added_tool_msg)/4 (or full count only if > threshold delta). Ensure summarize path still uses exact count. Document the heuristic reuse. Keep the per-iteration count_tokens call site but short-circuit when safe.
- [x] 6. Update any call sites/docs that mention unbounded outputs (PLAN.md if in scope, but keep minimal). Ensure ToolResult for shell/git still includes data for exit_code etc when truncated.
- [x] 7. Run `cargo test` (unpiped, read $LASTEXITCODE and result lines). Fix any breakage from caps (test expectations on full logs). Re-run.
- [x] 8. If all pass, mark the remediation plan step 12 complete via complete_step. (No reviewer sub-step until full Phase 2 close.)
