# Plan: Update Rust toolchain to 1.91+ + reinstall latest cargo-nextest + measure

## Goal
Update the Rust toolchain to 1.98 (done — clean build, tests pass) + mock git in tests so the suite doesn't depend on git being installed and nextest becomes viable on pure-logic tests. Git cannot be a build dependency.

## Kind
implementation

## Context


## Steps
- [x] 1. **Update the Rust toolchain to latest stable + verify the build. (1) Run `rustup update stable` to update the stable toolchain to the latest (should be 1.91+). Confirm with `rustc --version`. (2) Run `cargo build --lib` — if it fails with new warnings/errors under #![deny(warnings)], FIX each one (remove dead code, drop unused imports, etc. — never add #[allow]). The new rustc may flag things 1.89 didn't. (3) Run `cargo build` in src-tauri too (the app crate has its own #![deny(warnings)]). Fix any new warnings there. Record what (if anything) broke + the fixes. If the update is clean (no warnings), note that too.**
- [x] 2. **Verify the full test suite passes under the new toolchain. Run `cargo test --lib` — confirm 1653 passed, 0 failed, 12 ignored (the baseline from round 2). If any test fails due to the toolchain change (e.g. a behavior change in std/tokio), FIX it before proceeding. The test-success gate applies: zero failures. Record the test time under the new toolchain (compare to 13.55s — a new rustc may be slightly faster or slower).**
- [x] 3. **Reinstall the latest cargo-nextest (no version pin) + measure the full run. (1) Uninstall the old pinned version: `cargo uninstall cargo-nextest`. (2) Install the latest: `cargo install cargo-nextest` (no --version flag — gets the latest, which needs rustc 1.91+, now satisfied). Confirm with `cargo nextest --version`. (3) Run `cargo nextest run --lib` and capture the Summary line + wall time. Compare to cargo test --lib's 13.55s. If nextest now runs in parallel (the latest version should), it may be faster. If it's still serial/slow, document why and recommend sticking with cargo test. (4) If nextest is faster AND green, note it as the recommended test command going forward (but don't change the constitution's test command yet — just record the recommendation).**
- [x] 4. **Define GitRunner trait + RealGit + MockGit in git_ops.rs. Add a `pub trait GitRunner: Send + Sync { fn run(&self, root: &Path, args: &[&str]) -> Result<String, String>; }` trait. RealGit spawns Command::new("git") (current git_raw behavior). MockGit is an in-memory mini-git using Mutex<MockState> (Send+Sync for spawn_blocking) tracking: head_sha, current_branch, dirty, porcelain_status (pre-programmed for prepare_branch), branches, commits, calls. It responds to: status --porcelain, add -A, commit -m, rev-parse HEAD, reset --hard, rev-parse --abbrev-ref HEAD, checkout, checkout -b, rev-parse --verify --quiet, restore, stash, diff --cached --quiet. Add helper fns git_run(git, root, args) [trimmed] and git_run_raw(git, root, args) [raw] mirroring current git()/git_raw().**
- [x] 5. **Extract inner functions that take &dyn GitRunner. Refactor checkpoint/commit_success/rollback/prepare_branch_sync into _impl functions: checkpoint_impl(git: &dyn GitRunner, root: &Path, item: &BacklogItem), commit_success_impl(...), rollback_impl(git, root, sha), prepare_branch_impl(git, root, branch, base). The public async functions call the _impl functions with &RealGit inside spawn_blocking (API unchanged). The _impl functions use git_run/git_run_raw instead of git/git_raw. prepare_branch_impl keeps the file I/O (carry mechanism) real — tests create temp dirs with real files, the mock just simulates git commands.**
- [x] 6. **Convert git_ops tests to use MockGit. Replace the TestRepo helper (which spawns 3+ git subprocesses per test) with MockGit setup. Each test creates a temp dir with real files (for the carry mechanism) + a MockGit with the right initial state. Tests call the _impl functions directly (no spawn_blocking, no real git). The 24 tests cover: checkpoint (clean/dirty tree), commit_success (clean/dirty/noop), rollback, prepare_branch (fork/switch/already-on/detached/carry/dirty-outside-coding/staged-rejected). Verify: cargo test --lib project::git_ops — 24 passed, 0 failed, ZERO git subprocesses.**
- [x] 7. **Inject GitRunner into CreatePlanTool + convert plan.rs tests. CreatePlanTool holds Arc<dyn GitRunner + Send + Sync> (default RealGit). ensure_work_branch takes the runner as a parameter (or CreatePlanTool passes it through). The factory constructs CreatePlanTool with RealGit (production unchanged). Tests construct CreatePlanTool with MockGit. Replace the git_project() helper (which spawns ~10 git subprocesses) with MockGit setup + real temp dir files. Convert the ~50 plan.rs tests. Verify: cargo test --lib tool::workflow — all pass, ZERO git subprocesses.**
- [x] 8. **Full verification + review + commit + finish. (1) Run cargo test --lib — confirm ZERO failures, ZERO git subprocesses (the entire suite no longer depends on git being installed). Record test count + time. (2) Run cargo nextest run --lib on a subset (e.g. config::) to confirm nextest no longer hangs (no subprocess spawns in pure-logic tests). (3) git status + confirm branch wt/*. (4) Spawn reviewer over ALL changes: git_ops.rs (trait + mock + inner functions + test conversion), plan.rs (CreatePlanTool injection + test conversion). Goal = mock git in tests so the suite doesn't depend on git being installed + nextest becomes viable. Ask for: correctness (MockGit faithfully simulates git's behavior for every command our code uses; inner functions are logic-identical to the originals; no test coverage lost — every assertion from the old tests is preserved; CreatePlanTool injection doesn't change production behavior), the TEST-SUCCESS GATE (cargo test --lib has ZERO failures), constitution (warning-free; multi-platform neutral — no git subprocess in tests means it works on any OS without git installed), docs sync. END TURN after spawning. (5) Fix EVERY finding, re-run tests. (6) Commit to wt/*. (7) finish with report path.**
