# Plan: Ship the trace lock-inversion freeze fix (verify, commit, rebuild)

## Goal
Close out the recurring UI freeze: verify the in-tree trace.rs lock-order-inversion fix + sync-command conversion (from sub-plan e2ba07d8) is complete and regression-tested, commit it on a feature branch, and get the fixed binary into the running app.

## Kind
bug_fixing

## Context
Evidence trail: .coding/logs/hang-*.txt (all 0.0% CPU, ~10s, during agent turns); root cause documented in memory fbd81a71; fix already applied uncommitted in src/provider/trace.rs (writer_main scoped path clones) + src-tauri/src/ipc/trace.rs (list_llm_requests, clear_llm_requests, get_trace_logging → async + spawn_blocking); regression test writer_never_holds_log_path_while_waiting_for_records added in src/provider/trace.rs. Working tree is dirty on main; exe at target/debug/mnemo-app.exe (13:36, pre-fix) is locked while the app runs — cargo build will fail until the user restarts the app. Current plan: audit plan 308b8490 (research, parent). Memory protocol: write a marker memory after EVERY step (user requirement 2026-08-22 crash-research protocol).

## Steps
- [x] 1. **Reproduce with failing regression test** — Write a regression test that reproduces the defect (constitution: every defect gets a regression test that fails without the fix and passes with it). Run it and confirm it FAILS.
- [x] 2. **Document root cause** — Investigate and document the root cause. memory_write a BUG: record (symptom → root cause → fix + regression test name, ≤600 chars).
- [x] 3. **Minimal fix** — Apply the minimal fix that makes the regression test pass. Do not refactor unrelated code.
- [x] 4. **Verify** — Run the regression test + the full test suite (cargo test unpiped, warning-free). Record the regression test name via update_plan (regression_test field) — finish is blocked without it.

## Bug
App UI freezes repeatedly: hang watchdog shows 12 episodes (8/21–8/22), main thread 0.0% CPU over ~10s — blocked on a lock, not busy. Root cause: trace.rs writer_main holds the log_path MutexGuard across records.lock() + an up-to-8MiB file rewrite (edition-2021 if-let temporary) — an AB-BA inversion against with_record — plus sync trace commands (list/clear/get_trace_logging) blocking on those same locks on the Tauri main thread. The fix is in the working tree but the running binary (built 13:36) predates it (source fix 14:10–14:17) — the 14:31 hang was the pre-fix build.

## Regression test
provider::trace::tests::writer_never_holds_log_path_while_waiting_for_records
