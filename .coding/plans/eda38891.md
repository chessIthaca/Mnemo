# Plan: Block agent file writes to .git (sandbox escape)

## Goal
Close the .git sandbox escape: extend is_protected_write_target so any path containing a .git component (the control-plane dir, its contents, nested repos' .git, and the linked-worktree .git plain file) is refused by all file write tools + the IPC write mirror, with regression tests; plus the optional --no-verify defense-in-depth on the git tool's commit/merge as a separate commit.

## Kind
bug_fixing

## Context
Backlog item 0296d448 (dispatched by the user 2027-01-09). Source: .coding/reviews/2026-09-09-security-review.md (HIGH-1), executive summary BUG 1 (.coding/analysis/2027-01-09-review-round-executive-summary.md), memory "REVIEW: 2027-01-09 four-axis round".

Exploration findings (HEAD c18b5d0, wt/agenticcoding, clean tree):
- is_protected_write_target (src/tool/agent/sandbox.rs:189-234) strips the root, lowercases, forward-slashes rel_str, then exact-matches (.coding/memory.db + sidecars, codegraph.db + sidecars, safety.toml, backlog.jsonl/.json) and prefix-matches (.coding/plans/, .coding/reviews/, .coding/knowledge/). An ADS guard rejects any final component containing ':'. Case-variants are handled by the lowercase; the creation gap is handled because the predicate works on both canonical (validate) and lexically normalized (validate_for_creation) paths.
- Exactly two internal callers: validate_for_write (:257-282, the creation ladder used by file_write + file_append — refuses BEFORE mkdir) and refuse_if_protected (:288-293, used by file_edit at :897/:951 and convert_line_endings at :129). The IPC mirror write_sandboxed (src-tauri/src/ipc/files.rs:126-130) calls sandbox.refuse_if_protected — so ONE predicate change covers all five write paths, no bypass found.
- protected_refusal (:300-306) is the shared error; all tests assert .contains("protected").
- Test patterns in sandbox.rs tests (:331-720): tempdir + sandbox.validate(Path::new(rel)) + is_protected_write_target assertions; creation-gap via validate_for_creation; ladder test validate_for_write_refuses_protected_nonexistent asserts the dir was NOT created; IPC pattern write_sandboxed_refuses_protected_plan_file (files.rs:656-660).
- git.rs: commit at :690 runs ["commit", "-m", &message] (auto-stage logic :674-689); merge at :709-714 runs ["merge", "--no-ff", &branch, "-m", &m] or without -m. checkout/restore run no hooks. The optional --no-verify defense-in-depth goes on commit + merge only.
- Negative cases to keep writable: .gitignore, .gitattributes, .gitmodules (component == ".git" must NOT match them), and all normal files.
- Windows/PowerShell: run cargo tests unpiped, read $LASTEXITCODE. #![deny(warnings)] — no new warnings.

## Steps
- [x] 1. **Reproduce with failing regression test** — Write a regression test that reproduces the defect (constitution: every defect gets a regression test that fails without the fix and passes with it). Run it and confirm it FAILS.
- [x] 2. **Document root cause** — Investigate and document the root cause. memory_write a BUG: record (symptom → root cause → fix + regression test name, ≤600 chars).
- [x] 3. **Minimal fix** — Apply the minimal fix that makes the regression test pass. Do not refactor unrelated code.
- [x] 4. **Verify** — Run the regression test + the full test suite (cargo test unpiped, warning-free). Record the regression test name via update_plan (regression_test field) — finish is blocked without it.

## Detailed steps
- **Protect .git in the write predicate** — In src/tool/agent/sandbox.rs is_protected_write_target (:189-234): after the existing exact/prefix matches, add a .git control-plane check — rel_str == ".git" (the dir or the linked-worktree plain file) OR any '/'-separated component of rel_str equals ".git" (covers .git/config, .git/hooks/pre-commit, nested subdir/.git/…; case-variants already handled by the lowercase; .gitignore/.gitattributes must NOT match — component equality only). Update the doc comment's Protected list with the .git entry and the rationale (planted hooks / core.fsmonitor execute unsandboxed on the next commit or an auto-approved git status). Refresh protected_refusal's wording to cover the .git control plane while keeping the word "protected" (tests assert .contains("protected")).
- **Sandbox regression tests** — In the sandbox.rs test module, following the existing patterns: (a) protected_git_control_plane — .git/config, .git/hooks/pre-commit via validate (create the dirs/files first) AND via validate_for_creation (non-existent, the creation gap); (b) the .git plain file itself (write a file named .git, assert protected — the linked-worktree case); (c) a nested repo dir subdir/.git/config protected; (d) case variant .GIT/config protected; (e) validate_for_write refuses .git/hooks/pre-commit BEFORE creating dirs (assert !.git/hooks exists, mirroring validate_for_write_refuses_protected_nonexistent); (f) refuse_if_protected blocks .git/config with the protected message; (g) negative tests: .gitignore, .gitattributes, .gitmodules and a normal src file are NOT protected.
- **IPC mirror regression test** — In src-tauri/src/ipc/files.rs test module, mirroring write_sandboxed_refuses_protected_plan_file (:656-660): write_sandboxed(&sandbox, ".git/config", ...) is refused with an error containing "protected"; also assert a .gitignore write still succeeds (the mirror must not over-block).
- **--no-verify on git commit/merge** — In src/tool/agent/git.rs: commit (:690) becomes ["commit", "--no-verify", "-m", &message]; merge (:709-714) gains --no-verify in both arms. Comment: planted hooks must never execute via the agent's git path (defense-in-depth on top of the .git write protection; the user's own terminal git is unaffected). Search for and update any test pinning the exact commit/merge argv. This lands as a SEPARATE commit from the sandbox fix.
- **Verify + closing sequence** — Run the full suites unpiped, reading $LASTEXITCODE: cargo test (root crate) and cargo test in src-tauri (or cargo test --workspace if that is the established command). #![deny(warnings)] means a green build also proves zero warnings. Fix any failure before proceeding. Then the closing sequence: reviewer subagent → fix findings → re-run tests → two commits (1: sandbox protection + tests; 2: --no-verify) → finish.

## Bug
Agent file tools can write the .git/ control plane (sandbox escape — 2027-01-09 security review HIGH-1). file_write/file_edit/file_append/convert_line_endings (and the IPC write_sandboxed mirror in src-tauri/src/ipc/files.rs) accept targets like .git/hooks/pre-commit or .git/config (core.fsmonitor = <command>) — both inside the project root, so the path sandbox passes them and AutoApproveProject auto-approves. The next routine commit — or an auto-approved read-only `git status` (fsmonitor chain, zero approvals) — executes the payload with full user privileges: API-key exfiltration from ~/.mnemo/keys.toml, permanent safety-rule tampering. Root cause: is_protected_write_target (src/tool/agent/sandbox.rs:189-234) protects .coding state (DBs, safety.toml, backlog.jsonl, plans/, reviews/, knowledge/) but has no .git entry; the git tool never passes --no-verify, so planted hooks run.

## Regression test
protected_git_control_plane
