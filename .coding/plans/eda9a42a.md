# Plan: Conversation recomputes O(transcript) grouping per streaming frame — memoize the filter+group+chunk pass (mem-perf HIGH 2)

## Goal
Stop Conversation from recomputing the O(transcript) filter+group+chunk pass on every streaming frame: wrap the computation in a single useMemo keyed on [state.transcript, showToolActivity, showKnowledgeActivity] (the transcript array keeps its identity across streaming-text-only frames, so the common case skips the entire pass), with chunkRuns precomputed inside the same memo. Pinned by a source-contract regression test.

## Kind
implementation

## Context
mem-perf review HIGH 2 (.coding/reviews/2027-01-06-full-review-memory-perf.md). Conversation.tsx:140-145 filters and :152-159 groups the transcript in the render body with no useMemo; :207 re-runs chunkRuns(turn) per turn per render. Conversation re-renders every streaming frame (the state prop's identity changes per rAF flush — appendStreamingText spreads the agent per flush, useAgentStore.ts:1020-1036), so each frame re-filters/re-groups up to 1000 entries — O(n) work at up to 60 fps; per-frame cost scales with transcript length, not delta size. Premise verified in the store: appendStreamingText carries agent.transcript by reference (only streamingText + activityLog change), so a useMemo keyed on state.transcript hits across streaming-text-only frames (applyToolCallArgDeltas clones the transcript per batch, so tool-arg streaming still recomputes — acceptable, the common case is text streaming). Design: one useMemo computes filter → group-into-turns → chunkRuns-per-turn, yielding turns: TranscriptEntry[][][] (turn → chunks → entries); the render maps over the pre-chunked turns (chunkRuns no longer called in the render body). The memo keys are exactly [state.transcript, showToolActivity, showKnowledgeActivity] — chatThreadLine/chatTurnTint/chatHoverTimestamps affect only rendering, not the computation. The existing Conversation.test.ts assertion on "const visible = state.transcript.filter(" survives (the filter moves inside the memo, string unchanged). Complements the transcript-windowing item (b2cb83b6) and the stable-keys item from the same report — those stay queued.

## Steps
- [x] 1. **Memoize the filter+group+chunk computation in Conversation** — In frontend/src/components/chat/Conversation.tsx: (1) add useMemo to the react import (line 5: `import { useEffect, useRef } from "react";` → `import { useEffect, useMemo, useRef } from "react";`). (2) Replace the render-body computation (lines 134-159: the `const visible = state.transcript.filter(...)` + the `const turns: TranscriptEntry[][] = []` grouping loop) with a single `const turns = useMemo(() => { ... }, [state.transcript, showToolActivity, showKnowledgeActivity])` whose callback: filters into `const visible = state.transcript.filter(...)` (same predicate, string unchanged so the existing test assertion survives), groups into `const grouped: TranscriptEntry[][] = []` (same prompt-starts-a-turn loop), and returns `grouped.map((turn) => chunkRuns(turn))` — so turns is TranscriptEntry[][][] (turn → chunks → entries). Keep/merge the existing comment blocks (the db489070 GUI-only-filter note + the turn-grouping explanation) and add the mem-perf HIGH 2 rationale: Conversation re-renders per streaming frame but the transcript ARRAY keeps its identity while text streams into state.streamingText, so the common case skips the O(transcript) pass; the toggles are keys too. (3) In the render body (line 207), change `{chunkRuns(turn).map((run, ri) =>` to `{turn.map((run, ri) =>` — the render consumes the pre-chunked turns. Run `npm test --workspace frontend` + `npx tsc --noEmit` (cwd frontend) — green.
- [x] 2. **Regression test: source-contract on the memoized grouping** — In frontend/src/components/chat/Conversation.test.ts, add a new describe block "Conversation transcript grouping memoization (mem-perf review HIGH 2)" following the file's established source-contract style (it already imports conversationSource via ?raw): (a) `expect(conversationSource).toContain("const turns = useMemo(")` and `toContain("[state.transcript, showToolActivity, showKnowledgeActivity]")` — the pass is memoized on the transcript's identity (stable across streaming-text-only frames); (b) `toContain("turn.map((run, ri)")` — the render consumes the pre-chunked turns; (c) `not.toContain("chunkRuns(turn).map(")` — chunkRuns is precomputed inside the memo, not re-run per turn per render. Header comment documents the per-frame O(transcript) recompute being pinned. Run `npm test --workspace frontend` + `npx tsc --noEmit` (cwd frontend) — green.
- [x] 3. **Final verification + closing sequence** — Run the full suites: `cargo test` (root), `cargo test` (cwd src-tauri), `npm test --workspace frontend`, `npx tsc --noEmit` (cwd frontend) — all green (Rust untouched by this change, but run per constitution). Then the closing sequence: reviewer subagent over all uncommitted changes → fix every finding → re-run tests → commit to the working branch → finish.
