# Plan: Verify + close 2e27f896: multi_edit already shipped via 88577b4

## Goal
Close stale backlog item 2e27f896 as already-shipped: verify the multi_edit tool + compact line-op grammar (commit 88577b4, plan e3d0758b, merged into main via 4cbc3b0) is intact and green in this worktree — root cargo test + frontend tsc/vitest — reconcile the stale "UNMERGED" spec-knowledge note and the pending backlog row, and finish without re-doing any shipped work.

## Kind
research

## Context
Backlog item 2e27f896 re-dispatched work that already shipped: plan e3d0758b completed on wt/mnemo (commit 88577b4, 3 review rounds, round-3 PASS, measured cost delta recorded in .coding/plans/e3d0758b.md) and landed in main via the 2027-01-11 wt/mnemo merge (4cbc3b0; branch deleted). Verified present in this worktree (forked from main): src/tool/agent/edit_ops.rs (1623 lines), multi_edit.rs (623 lines), file_edit ops field, frontend family (ApprovalPrompt.tsx, DiffViewer.tsx, toolCardPaths.ts, useAgentEvents.ts auto-reveal), docs (PLAN.md:233/938, agent.md:84, docs/FEATURES.md:43/71). Working tree clean except .coding/backlog.jsonl. Two stale artifacts to reconcile: (1) .coding/knowledge/spec/2027-01-11-multi-edit-tool-compact-line-op-grammar-for-file.md header still says "UNMERGED — on branch wt/mnemo" (now false), (2) the backlog row itself. Verification commands: cargo test unpiped at root (read `test result:` lines + $LASTEXITCODE — green proves warning-free under #![deny(warnings)]); frontend `npx tsc --noEmit` + vitest run. Known environment block: `cargo test -p mnemo-app` dies on the Application Control policy (os error 4551) blocking freshly built build scripts — recorded in plan e3d0758b's FINAL VERIFY; skip it with that justification.

## Steps
- [x] 1. **Record the shipped-surface confirmation** — Evidence gathered during planning is the deliverable: edit_ops.rs/multi_edit.rs line counts, frontend file list, docs lines, commit 88577b4 reachable from HEAD (main-forked branch). Write a working note into the plan via complete_step; no code to write.
- [x] 2. **Root cargo test green** — Fast signal first: cargo test tool::agent::multi_edit and cargo test tool::agent::edit_ops, reading `test result:` lines. Then full root `cargo test` UNPIPED (PowerShell, Windows) followed by "exit=$LASTEXITCODE" — green under #![deny(warnings)] proves warning-free. Do NOT trust piped exit codes per agent.md.
- [x] 3. **Frontend typecheck + vitest** — In frontend/: `npx tsc --noEmit` (exit 0) and the vitest run (check package.json scripts for the exact test command). Expect the multi_edit chip/preview/auto-reveal tests (toolCardPaths.test.ts, DiffViewer.test.ts, useAgentEvents.dispatch.test.ts) to pass. src-tauri `cargo test -p mnemo-app` is skipped: blocked by the Application Control policy (os error 4551) — known environment fact, recorded in plan e3d0758b FINAL VERIFY.
- [x] 4. **Reconcile records + close the row** — memory_search for the SPEC record backing .coding/knowledge/spec/2027-01-11-multi-edit-tool-compact-line-op-grammar-for-file.md; memory_amend it: merged into main via 4cbc3b0 on 2027-01-11, wt/mnemo deleted — the UNMERGED header no longer applies. Write a short semantic memory: backlog 2e27f896 was a stale re-dispatch of already-shipped work (88577b4), closed after verification. Then backlog_status the row (2e27f896-578f-406b-b7d5-d126e2a0f947) to done with a note naming 88577b4 if the plan lifecycle has not already flipped it, and finish the plan.
