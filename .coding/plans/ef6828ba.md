# Plan: Splash-branded startup/reindex dialogs (IconCard + network motif)

## Goal
Startup/project-change progress dialogs show a professional Mnemo splash header (brand + network graphic in the icon-source.svg palette) with the progress bar + counter, and the verbose "on-disk knowledge changed" body text is removed.

## Kind
implementation

## Context
Two "reindexing" full-screen dialogs exist:
(1) App.tsx:595-649 reconcile dialog (memory://reconcile) — contains the body paragraph the user wants GONE ("The on-disk knowledge changed (a merge, an edit, or a deleted index) — ...", lines 606-610). It also lacks useBrowserOverlay (pre-existing gap, flagged in .coding/reviews/2026-indexing-progress-overlay.md).
(2) frontend/src/components/projects/IndexingOverlay.tsx (codegraph://index-progress, self-subscribing) — has a similar body paragraph (lines 104-107: "First-time indexing of this project's sources — ...").
Branding assets: frontend/src/components/common/MnemoLogo.tsx (inline SVG badge, namespaced ids via useId, tests in MnemoLogo.test.tsx). Brand palette lives in src-tauri/icons/icon-source.svg + the user's promo image: deep navy background, blue network nodes/edges (#587CFF/#6D8BFF glow), white bold "mnemo" wordmark, blue "SMART CODING HARNESS", white "MEMORY. SPEC. IMPROVEMENT." No raster assets are imported in the frontend today — an inline-SVG network motif keeps that pattern (no binary assets, no public/ dir).
Frontend tests MUST be registered in frontend/vitest.config.ts include[] — it replaces default discovery (silent test loss otherwise, past review HIGH finding). MnemoLogo.test.tsx uses renderToStaticMarkup under environment node — the same approach works for SplashCard.
App.tsx note: useBrowserOverlay(reconcile !== null) must be called with the other hooks near the top of App(), BEFORE the early returns at lines 561-589 (rules of hooks).

## Steps
- [x] 1. **Create SplashCard component (frontend/src/components/common/SplashCard.tsx)** — Create the component:
- Props: `{ children: React.ReactNode; className?: string }` — children = the dialog content slot rendered BELOW the splash band; className passed through to the card root so callers keep their width (default `w-96`).
- Card root: `overflow-hidden rounded-xl border border-border bg-bg-tertiary shadow-2xl` (matches the existing dialog cards).
- Splash band (h-36 ~144px tall, `relative overflow-hidden rounded-t-xl`): background is an inline-style diagonal gradient from `#050A16` to `#0D1B33` (deep navy from the promo image). Inside it an absolute, `pointer-events-none` full-bleed `<svg viewBox="0 0 384 144" preserveAspectRatio="xMidYMid slice">` network motif: ~8 edges connecting ~9 nodes, edges stroke `#2E4A8F` (a couple `strokeDasharray="4 4"` like the promo), nodes as small circles r=2..3.5 fill `#4C7DFF`, one faint radial-glow circle (gradient def, per-instance id via useId exactly like MnemoLogo — duplicate ids across instances break refs). Motif sits behind content (`absolute inset-0` + content `relative`).
- Centered brand stack (flex col items-center justify-center gap, relative z): `<MnemoLogo className="h-12 w-12" />`, wordmark `mnemo` (text-xl font-bold tracking-tight text-slate-100), tagline `SMART CODING HARNESS` (text-[9px] font-semibold tracking-[0.3em] text-[#6D8BFF]), values `MEMORY. SPEC. IMPROVEMENT.` (text-[8px] tracking-[0.25em] text-slate-400). All text in JSX — NOT baked into the SVG, so it stays crisp and testable.
- Below the band: a `p-5` div wrapping `{children}`; hairline separation via `border-b border-cyan-500/20` on the band.
- Also export `SplashProgress({ label, right, pct }: { label: string; right: string; pct: number | null })` — the progress block both dialogs share: a row (`label` left, mono `right` right, text-[0.7em] text-slate-500) above a 1.5px rounded-full `bg-bg-secondary` track with a cyan-500 fill (`pct` as a % width; `pct === null` renders the indeterminate 30% width, exactly like today's fallback). Doc comments on both exported items (repo rule).
- [x] 2. **SplashCard tests + vitest registration** — Create frontend/src/components/common/SplashCard.test.tsx modeled on MnemoLogo.test.tsx (renderToStaticMarkup, environment node — no DOM needed). Assert: (a) rendered markup contains the wordmark `mnemo`, tagline `SMART CODING HARNESS`, and values `MEMORY. SPEC. IMPROVEMENT.`; (b) brand blue `#587CFF`/`#6D8BFF` and network-edge stroke `#2E4A8F` appear; (c) two simultaneous instances never produce duplicate def ids (same urlRefs technique as MnemoLogo.test.tsx); (d) children render below the brand (e.g. pass a marker div, assert it appears after the wordmark in the markup); (e) SplashProgress renders the label/counter text, pct=40 → `width: 40%`-ish fill, pct=null → 30% indeterminate fill. THEN add `"src/components/common/SplashCard.test.tsx"` to frontend/vitest.config.ts include[] (replaces default discovery — unregistered tests silently never run).
- [x] 3. **IndexingOverlay: adopt SplashCard, drop body paragraph** — Keep the subscription/reducer logic (applyIndexProgress, onIndexProgress effect, useBrowserOverlay) untouched. Replace the card markup: outer stays `fixed inset-0 z-50 flex items-center justify-center bg-black/50 backdrop-blur-sm`; the inner card becomes `<SplashCard>`; inside the content slot render: the title row (spinning RefreshCw + "Indexing project", unchanged), then for kind==="progress" the `<SplashProgress label="Indexing files…" right={total>0 ? `${done}/${total} files indexed` : "…"} pct={total>0 ? Math.round(done/total*100) : null} />` — DELETING the current body paragraph (lines 104-107, "First-time indexing of this project's sources — ..."); the failed branch keeps its red text + Dismiss button. Update the component doc comment where it describes the visible content.
- [x] 4. **App.tsx reconcile dialog: drop the text, adopt SplashCard, add useBrowserOverlay** — (a) Delete the body paragraph (lines 606-610 — the exact "The on-disk knowledge changed..." text the user flagged).
(b) Rebuild the card on `<SplashCard className="w-96">`: content slot keeps the existing title row (RefreshCw + "Rebuilding the semantic memory index"), the running branch becomes `<SplashProgress label="Reconciling sources…" right={total>0 ? `${done}/${total}` : "…"} pct={...} />`, the done summary (green) / failed error (red) blocks and the Dismiss button stay unchanged inside the content slot. Splash band thus stays visible in every phase including done/failed (consistent card).
(c) Close the pre-existing review finding: add `useBrowserOverlay(reconcile !== null)` in App() with the other hooks near the top of the file (BEFORE the `if (checkedStartup && needsProject)` early return at line 561) and import it from ./hooks/useBrowserOverlay — matches IndexingOverlay and every other full-viewport modal (the native Browser-tab WebView2 would otherwise punch through the dialog).
- [x] 5. **Verify: frontend tests + build, cargo test, docs/spec sync** — From C:\AgenticCoder\AgenticCoder\frontend: `npx vitest run` (all suites, incl. the new SplashCard suite + existing indexingOverlay/MnemoLogo suites) and `npm run build` (tsc) must be green. Then from the repo root run `cargo test` unpiped and check the `test result:` lines (Windows machine — PowerShell syntax, never trust a piped exit code). Check README.md for its indexing-overlay bullet and update it for the splash look if it describes the dialog; then write/update the SPEC memory for the indexing/reconcile overlay (it currently describes the dropped body paragraph — pointer-first, path .coding/knowledge/spec/2026-08-27-indexing-progress-overlay-on-project-open-bar-co.md).
