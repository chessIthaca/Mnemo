# Plan: Top-impact review fixes (B1/B2, H1, H2, R1, M1-sec)

## Goal
Implement the five highest-impact review findings from the 2026-08-13 whole-app review: remove two dead build deps, close the describe_image exfiltration channel, restrict + redact provider log files, cache the tiktoken BPE tokenizer, and make the protected-write guard case-insensitive on Windows.

## Kind
implementation

## Context
Consolidated whole-app review at .coding/reviews/2026-08-13-full-app-review.md. User chose the "Top impact fixes" batch (review's action items 1-5). Verified anchors: Cargo.toml:20 (syntect), :48 (markdown) — zero Rust refs (search confirmed); src/tool/agent/describe_image.rs:178-181 (AutoRun) + :52-70 (extension-only MIME check, no magic-byte sniff); src/provider/trace.rs:345-385 (maybe_log_error, always-on error log) + :448-487 (write_record_to_file, traces.jsonl) — both write plaintext to .coding/logs/ with no permission restriction and no secret redaction; src/agent/context.rs:244-258 (try_tiktoken_count calls cl100k_base() per count); src/tool/agent/sandbox.rs:170-180 (is_protected_write_target, case-sensitive == on Windows NTFS). The restrict_permissions helper already exists at src/config/keys.rs:136 (private fn; Unix chmod 0600, Windows user-only DACL via windows-sys) — reuse it by making it pub(crate). regex is already a dep (Cargo.toml:39). The five fixes touch disjoint files (Cargo.toml, describe_image.rs, trace.rs+keys.rs, context.rs, sandbox.rs) so they are independent.

## Steps
- [x] 1. **Remove dead deps syntect + markdown (B1, B2)** — In `Cargo.toml`: delete `syntect = "5"` (line 20) and the `markdown = "1"` line + its comment (lines 47-48). Keep `similar = "2"` (still used for diffs); adjust the line-19 comment from `# Syntax highlighting + diff` to `# Diff`. Run `cargo build` then `cargo test` (unpiped, check `$LASTEXITCODE` + `test result:` lines) to confirm syntect/markdown are truly unused and the build is warning-free under `#![deny(warnings)]`.
- [x] 2. **Gate describe_image behind approval + content-sniff MIME (H1)** — In `src/tool/agent/describe_image.rs`: (a) change `DescribeImageTool::safety()` (line 178-181) from `SafetyLevel::AutoRun` to `SafetyLevel::NeedsApproval`. (b) In `load_image_data_url` (line 52-70), after `let bytes = std::fs::read(&validated)?;` and before building the data URL, verify the bytes' magic bytes match the claimed `mime`: PNG `\x89PNG`, JPEG `\xff\xd8\xff`, GIF `GIF8`, WebP `RIFF` at 0 + `WEBP` at offset 8, BMP `BM`; on mismatch return `Error::InvalidInput(format!("file content does not match .{ext} (expected {mime})"))`. Add a private `fn magic_matches(mime: &str, bytes: &[u8]) -> bool` helper. (c) Update the module doc (line 14-15) from 'read-only (AutoRun)' to note it is `NeedsApproval` (reads a sandbox file + sends it to the vision endpoint) and content-sniffs magic bytes. (d) Update the test at line 264 asserting `SafetyLevel::AutoRun` → `NeedsApproval`. (e) Add a test `load_image_data_url_rejects_content_mime_mismatch` writing a `.png` file with non-PNG bytes (e.g. `b"not a png"`) and asserting an `Error::InvalidInput`. The existing tests that write `b"\x89PNG\r\n\x1a\n"` still pass (valid magic). Run `cargo test`.
- [x] 3. **Restrict log file permissions + redact secrets (H2)** — In `src/config/keys.rs`: change `fn restrict_permissions` (line 136) to `pub(crate) fn restrict_permissions` (keep the Windows helper private). In `src/provider/trace.rs`: (a) add `use crate::config::keys::restrict_permissions;` and a private best-effort helper `fn restrict_log_file(path: &Path)` that calls `restrict_permissions(path)` and on `Err` `eprintln!`s a warning (never blocks the write). Call it after the file is opened/written in `maybe_log_error` (after the `writeln!`, line 374-385) and in `write_record_to_file` (after `std::fs::write`, line 486). (b) Add secret redaction: a recursive `fn redact_json(v: serde_json::Value) -> serde_json::Value` that replaces the value of any key named `api_key` or `authorization` (case-insensitive) with `"[REDACTED]"` (recurse into objects/arrays); and `fn redact_text(s: &str) -> String` using two `OnceLock<regex::Regex>` patterns — one for `Bearer <token>` → `Bearer [REDACTED]`, one for JSON `"api_key":"..."` → `"api_key":"[REDACTED]"`. In `write_record_to_file`, clone the record, apply `redact_json` to `request_json` and `redact_text` to `response_raw` before `serde_json::to_string`. In `maybe_log_error`, apply `redact_text` to the `error` field of the serialized summary. (c) Add tests: a record whose `request_json` contains `{"api_key":"sk-secret"}` is written to traces.jsonl with `[REDACTED]` (assert the file does NOT contain `sk-secret`); a `response_raw` containing `Authorization: Bearer abc123` is redacted; and a Unix test asserting the error-log file mode is 0o600 after a failed request is logged (mirror keys.rs `save_restricts_permissions_to_user_only`). Run `cargo test`.
- [x] 4. **Cache tiktoken BPE tokenizer in OnceLock (R1)** — In `src/agent/context.rs` `try_tiktoken_count` (line 244-258): replace `let bpe = cl100k_base().ok()?;` with a module-level `static BPE: std::sync::OnceLock<Option<tiktoken_rs::CoreBPE>> = std::sync::OnceLock::new();` initialized once via `BPE.get_or_init(|| cl100k_base().ok()).as_ref()?` (yields `&CoreBPE`). Keep the rest of the function (the `encode_with_special_tokens` loop) unchanged — it already takes `&self`. Update the function doc comment to note the BPE is initialized once and cached. Run `cargo test` (the `count_tokens_nonempty` + `should_summarize_*` tests exercise it).
- [x] 5. **Case-insensitive protected-write guard on Windows (M1-sec)** — In `src/tool/agent/sandbox.rs` `is_protected_write_target` (line 170-180): change `let rel_str = rel.to_string_lossy().replace('\\', "/");` to also `.to_ascii_lowercase()` (i.e. `let rel_str = rel.to_string_lossy().replace('\\', "/").to_ascii_lowercase();`). The comparison literals (`.coding/memory.db`, `.coding/safety.toml`, `.coding/plans/`, etc.) are already lowercase ASCII, so they match case-variants like `.coding/SAFETY.TOML` / `.coding/MEMORY.DB` on NTFS. Add a test `protected_case_variant_caught` asserting `is_protected_write_target` returns true for `.coding/SAFETY.TOML`, `.coding/MEMORY.DB`, and `.coding/PLANS/stack.json` (via `validate_for_creation` so no FS access needed). Run `cargo test`.
