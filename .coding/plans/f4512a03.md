# Plan: Disable macOS CI job temporarily — publish v0.1.1 Windows-only

## Goal
Temporarily disable the macOS job in .github/workflows/build.yml (macOS support everywhere else unchanged) and make the release job Windows-only, so the stuck v0.1.1 release finally publishes with Windows installers.

## Kind
implementation

## Context
Diagnosis: v0.1.1 tag run 35531680467 (at main tip 21bf39d, WITH the five macOS test fixes) — windows job SUCCESS, macos job FAILED at "Rust tests (workspace, macOS compile validation)", release job (needs both) skipped. User decision 2026-09-20: disable the macOS CI job for now; keep macOS support everywhere else (codebase, local builds, README platform claims). Change: `if: false` on the macos job (mirrors the release job's if-first style) + dated comment with the re-enable checklist; release job → needs: [windows], drop the macos-bundles-aarch64 download step + dist/macos globs; STATUS note in the file header. Tests: tests/integration/ci_workflow.rs parses build.yml — no `if:` line may reference `secrets.` (the new `if: false` is clean), CI env stays clap-bool "true", no node20 pins — all stay green. Docs: .coding/skills/new_release.toml prompt claims .msi/.exe/.dmg/mnemo.app.zip assets → add temporary Windows-only note; README.md unchanged (its macOS mentions are local-build claims, still true). Memories: write the disable DECISION + amend d898bb80 (CI owns release creation) with the temporary Windows-only state. Post-plan follow-through (skills, after finish): merge_to_main → force-move the v0.1.1 tag from 21bf39d to the new main tip (delete + re-push, approval-gated) → verify the run started via the actions API → report run + release URLs. No release exists for the tag (release job never ran), so nothing is immutable. Note: file_edit froze on build.yml in an earlier session (plan fbada5bc) — if it recurs, fall back to shell surgery with the stated justification.

## Steps
- [x] 1. **Disable the macos job** — In .github/workflows/build.yml, add a dated comment block above the `macos:` job (line 80): TEMPORARILY DISABLED (2026-09-20, user decision) — the macOS leg failed `cargo test --workspace` again on run 35531680467 (tag v0.1.1) even after the five-test fixes, blocking every release; disabled HERE ONLY (codebase macOS support + local builds unchanged); re-enable checklist: (1) delete the `if: false` line, (2) restore the release job's macos references (add `macos` back to `needs:`, re-add the macos-bundles-aarch64 artifact download, re-add the dist/macos/**/*.dmg + *.zip globs). Then add `if: false` as the first key of the macos job (before `runs-on`), mirroring the release job's if-first style.
- [x] 2. **Make the release job Windows-only** — Same file: (a) `needs: [windows, macos]` (line 195) → `needs: [windows]` with a one-line comment pointing at the macos job's disable note; (b) delete the macos-bundles-aarch64 download step (lines 206-209); (c) drop `dist/macos/**/*.dmg` and `dist/macos/**/*.zip` from the gh-release files list (lines 216-217); (d) add a STATUS note to the file header after line 1: macOS job temporarily disabled (`if: false`) — releases publish Windows-only installers until re-enabled; see the macos job comment; codebase macOS support unchanged.
- [x] 3. **Docs + memory sync** — (a) .coding/skills/new_release.toml — in the prompt (line 38), note the release assets are Windows-only (.msi/.exe) while the macOS job is temporarily disabled, to be re-added on re-enable. (b) memory_write (semantic, DECISION:): macOS CI job temporarily disabled 2026-09-20 by user decision — releases Windows-only; re-enable checklist lives in build.yml's macos job comment. (c) memory_amend d898bb80 (DECISION: CI owns GitHub release creation) with the temporary Windows-only state. README.md needs no change — its macOS mentions (platform badge, local dev prerequisites, bundle command) are local-build/platform claims that stay true.
- [x] 4. **Verify** — PowerShell, unpiped: `cargo test --workspace` then `"exit=$LASTEXITCODE"` — all workspace tests green (the three ci_workflow.rs regression tests parse build.yml: no `if:` line references `secrets.`, CI env is clap-bool "true", no node20 action pins). Plus a YAML parse sanity check: `python -c "import yaml,io; yaml.safe_load(io.open('.github/workflows/build.yml',encoding='utf-8'))"` (the proven guard from earlier CI fixes).
