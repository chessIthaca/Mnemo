# Plan: Fix the macOS-incompatible setting in src-tauri/tauri.conf.json (named by the release-build warning)

## Goal
Run the release build to capture the warning that names a macOS-incompatible app setting in src-tauri/tauri.conf.json, then fix that setting so the config is macOS-clean — while the Windows bare-window + ordered add_child CDP topology and the capability scoping stay intact.

## Kind
bug_fixing

## Context
USER REPORT (2027-01-16): "The tauri conf file has a Mac incompatible app setting. Fix that pls" + "the build gives a warning about app" + "run the build for release and you see it in the output". So the warning is REPRODUCIBLE ON THIS WINDOWS BOX by the release build; step 1 captures its exact text, which names the field.

FILE: src-tauri/tauri.conf.json (33 lines, read this session, LF). Shape: build{frontendDist "../target/frontend-dist", devUrl "http://localhost:5179", beforeDevCommand "npm run dev", beforeBuildCommand "npm run build"}; app{windows: [], security{csp, devCsp}}; bundle{active true, targets "all", icon[32x32.png, 128x128.png, 128x128@2x.png, icon.icns, icon.ico], macOS{minimumSystemVersion "11.0"}}. No platform-override conf file exists (src-tauri/ holds only tauri.conf.json).

AUDIT ALREADY DONE (do NOT re-litigate; these are cleared): all five icon files exist (src-tauri/icons/); bundle.macOS.minimumSystemVersion is the schema-correct key (mac-port research .coding/analysis/2026-08-20-macos-port-research.md:63-72); targets "all" means app+dmg on macOS and the CI macOS job builds exactly that on macos-latest (.github/workflows/build.yml:156-172); the CSP's `connect-src ipc: http://ipc.localhost` carries the `ipc:` scheme-source, which matches macOS's ipc://localhost; the vendored wry implements the child-webview path on macOS (vendor/wry/src/wkwebview/mod.rs:178), so add_child itself is not macOS-incompatible. LIVE CANDIDATES (step 1 decides): (a) app.windows: [] — the deliberate plan-48402867 change; (b) build.devUrl localhost (macOS resolves ::1 first; vite binds 127.0.0.1); (c) the CSP connect-src spelling; (d) bundle.targets "all".

WIN CONSTRAINTS (must survive): the bare window "main" + ordered add_child webviews in src-tauri/src/main.rs:232-266 exist so the FIRST child grabs CDP 9222 (WEBVIEW2_ADDITIONAL_BROWSER_ARGUMENTS is cfg(windows), main.rs:48/196; src-tauri/tests/webview_udf.rs pins the builder sites); capabilities/default.json:5 scopes on "windows": ["main"]. A conf-level window declaration therefore belongs in a PLATFORM OVERRIDE file (tauri.macos.conf.json / tauri.windows.conf.json are the Tauri v2 mechanism), not in the base conf.

VERIFY COMMANDS: `cd src-tauri; npx tauri build --no-bundle` (release; the `npm start build` path — capture with Select-String on warn/Warning/warning); `cd src-tauri; cargo build` (tauri_build::build() in src-tauri/build.rs parses tauri.conf.json via generate_context! at compile time, so a bad/renamed key fails HERE); root `cargo test` unpiped (read the `test result:` lines — never a piped exit code, per agent.md); `cd frontend; npm run build`. Regression-test precedent: src-tauri/tests/webview_udf.rs (source-contract tests over main.rs/conf shapes, fail without the fix).

STEP 1 DONE — root cause pinned (release build, log .coding/analysis/mac-conf-warning.txt): `Warn  The bundle identifier "com.mnemo.app" set in "tauri.conf.json" identifier ends with `.app`. This is not recommended because it conflicts with the application bundle extension on macOS.` So the macOS-incompatible setting is `"identifier": "com.mnemo.app"` (src-tauri/tauri.conf.json:5) — app.windows: [], the localhost devUrl, the CSP and bundle.targets are all CLEARED. The build then failed with `error: failed to remove file C:\Mnemo\target\release\mnemo-app.exe — Access is denied. (os error 5)` because the user's running instance locks the exe (build-environment artifact, unrelated to the conf); the warning itself is emitted in the CLI's config-validation phase, before compiling, so verifying it is gone needs only that phase (the app does not have to be closed). CONSEQUENCES TO HANDLE: the identifier drives the Windows WebView2 default user-data folder (`%LOCALAPPDATA%\com.mnemo.app\EBWebView`, PLAN.md's multi-instance row; src-tauri/src/webview_udf.rs keys its named mutex on the resolved default UDF path, and src-tauri/tests/webview_udf.rs pins the builder sites) and the macOS CFBundleIdentifier — a rename moves the Windows profile (one-time cookie/login reset for existing installs) and changes the bundle id, so every reference must be swept together.

## Steps
- [ ] 1. **Reproduce with failing regression test** — Write a regression test that reproduces the defect (constitution: every defect gets a regression test that fails without the fix and passes with it). Run it and confirm it FAILS.
- [ ] 2. **Document root cause** — Investigate and document the root cause. memory_write a BUG: record (symptom → root cause → fix + regression test name, ≤600 chars).
- [ ] 3. **Minimal fix** — Apply the minimal fix that makes the regression test pass. Do not refactor unrelated code.
- [ ] 4. **Verify** — Run the regression test + the full test suite (cargo test unpiped, warning-free). Record the regression test name via update_plan (regression_test field) — finish is blocked without it. If the fix turned out FEATURE-SCALE (new pub types or cross-module surface): amend the plan context via update_plan append with a paragraph starting "Landed design —" (architecture, deviations from the sketch, measured numbers, invariants to preserve) and record landed_design=true BEFORE completing this step; at finish write a SPEC memory and amend the BUG record with the landed outcome (agent.md 'Documentation expectations'; the finish gate blocks landed_design without the context amendment).

## Detailed steps
- [x] **Reproduce the release-build warning and capture the exact field** — In src-tauri run the release build the user means: `npx tauri build --no-bundle` (the same path as `npm start build`), capturing output to a file under .coding/analysis/ (e.g. `.coding/analysis/mac-conf-warning.txt`) and grepping it for warn/Warning/warning + the config field names (app.windows, devUrl, targets, csp). Record the EXACT warning line, the field it names, and whether it is platform-conditional — append it to this plan's context via update_plan (append=true). Note per agent.md: a piped exit code is meaningless; read the command's own lines. If (contrary to the user's report) no warning appears, run the same command with `--verbose` and then STOP and ask_user for the exact line.
- [ ] **Pin the root cause and choose the fix shape** — With the field named by the warning, confirm the platform rule from authority: web_fetch the Tauri v2 config schema (https://schema.tauri.app/config/2) or docs.rs tauri-utils config structs for that key's description. Then choose the fix: (1) value correction in src-tauri/tauri.conf.json; or (2) a platform override file (src-tauri/tauri.macos.conf.json or tauri.windows.conf.json) when the field must differ per platform — required shape if the field is app.windows, whose empty array is load-bearing on Windows (src-tauri/src/main.rs:232-266 + src-tauri/tests/webview_udf.rs) and whose macOS fix must let main.rs REUSE the already-created "main" window (tauri::Manager::get_window) instead of double-creating it; or (3) relocation of the setting into Rust code. Write the decision + rationale into the plan context (update_plan append) with the exact file paths to touch.
- [ ] **Apply the fix** — file_edit src-tauri/tauri.conf.json (and/or add the platform override file if that is the chosen shape); keep the JSON valid and the file's LF endings; touch src-tauri/src/main.rs ONLY if the shape requires reusing a conf-declared window (guard it so the Windows CDP ordering and the add_child agent-chat webview are byte-unchanged on Windows). Sync the docs that describe this config/topology: PLAN.md (the macOS/webview rows) and docs/FEATURES.md if they state the window or bundle shape.
- [ ] **Regression pin** — Add or extend a source-contract test in src-tauri/tests/ following src-tauri/tests/webview_udf.rs's pattern, asserting the fixed config shape so the setting cannot silently come back (e.g. the base src-tauri/tauri.conf.json no longer carries the flagged key/value, and the platform override file exists with the intended content). Run `cargo test -p mnemo-app` from src-tauri (unpiped; read `test result:`): the new test must pass, and it must FAIL with the pre-fix value (verify by temporarily reverting the value in a scratch copy or by asserting the exact new value).
- [ ] **Verify** — Re-run the release build `cd src-tauri; npx tauri build --no-bundle` and confirm the warning is GONE (quote the relevant output lines in the step result); `cd src-tauri; cargo build` green (the conf is schema-validated at compile time by tauri_build::build()); `cd frontend; npm run build` green; root `cargo test` unpiped, reading the `test result:` lines (no piped exit-code trust), zero warnings under #![deny(warnings)].
- [ ] **Closing sequence: review, fix, commit, finish** — update_plan append the landed design + evidence (exact warning before/after, files changed, test names). Then spawn a read-only reviewer (spawn_agent role:"reviewer", OMIT the model param) over ALL uncommitted changes with a self-contained brief: changed files + the goal (make src-tauri/tauri.conf.json macOS-clean per the release-build warning), asking for correctness/bugs/security, docs sync (PLAN.md, docs/FEATURES.md), multi-platform neutrality (macOS AND Windows — the Windows CDP/add_child ordering and capability scoping must be provably unchanged), and test genuineness. Fix EVERY finding, re-run the tests, commit to the current wt/* branch (never main; include the review report), then finish with the report path.

## Bug
The release build prints a warning naming an app setting in src-tauri/tauri.conf.json that is incompatible with macOS (user report 2027-01-16: "The tauri conf file has a Mac incompatible app setting... the build gives a warning about app... run the build for release and you see it in the output"). Reproduced by step 1 via `cd src-tauri; npx tauri build --no-bundle`; the audit already cleared the icon set, bundle.macOS.minimumSystemVersion, targets "all" and the CSP `ipc:` entry, leaving app.windows: [], the localhost devUrl, the CSP connect-src spelling and bundle.targets as the live candidates. Regression-test design: a source-contract test in src-tauri/tests/ (webview_udf.rs pattern) asserting the fixed config shape, failing with the pre-fix value.
