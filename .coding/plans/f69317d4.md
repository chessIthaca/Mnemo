# Plan: Fix all remaining findings: R6-2 schema stability, R6-3 trace tail, shell core-op guard, landing sync, stale comments

## Goal
Fix every remaining item this conversation surfaced: R6-2 make the advertised tool schema byte-stable across plan kinds (kills the measured 6,016-token cache collapse and 15.7-17.9 s TTFT after plan transitions); R6-3 stop the trace log emitting a partial last line (diagnose the cause first); A add a command-aware core-operation approval guard to ShellTool for `git merge`/`git push`; B sync main with origin before the app-managed `--no-ff` landing merge in src/project/worktrees.rs; C fix the stale StatusBar comments that still promise a stash in the merge flow. Verify with cargo test + frontend tests/build, sync the affected docs, and record the post-restart effect check for R6-1/R6-2.

## Kind
implementation

## Context
WHY: round-6 (plan c43ad4a3) is COMPLETE and landed — R6-1 (the compaction gate measuring the truncatable population) is committed with its regression test, and the analysis report carries two remaining items plus three follow-ups parked during the merge_to_main work (plan d826b9ad). The user asked for one plan covering ALL of them. Nothing is in flight: HEAD is 21fefe7 on `wt/mnemo`, tree clean.

ROUND-6 EVIDENCE (all committed): report `.coding/analysis/cache-hit-6-report.md`, extractor `.coding/analysis/cache-hit-6-extract.py`, generated `.coding/analysis/cache-hit-6-aggregates.txt` (sections A/B/B2/B3/C/E/F), reviews `2026-09-14-cache-hit-round6-review{,-round2,-round3}.md`. R6-1 landed in f1a5809: `tool_result_is_truncatable` + the gate in `src/agent/context.rs::compact_old_tool_results` (gate now at ~line 992), guarded by `agent::context::tests::short_results_do_not_hold_the_gate_open`.

R6-2 EVIDENCE (measured, aggregates B/B2/E): a plan-KIND change rewrites the head — pair 5→6: system 26,993 → 25,831 chars AND tools array 30 → 26 entries in one request; provider `cached` collapses to 6,016 (the common system prefix). `request_stats` window rows 02:59:40/50, 03:00:53, 03:01:22, 03:02:07/25/29 = 7 of the window's rows at ~5% hit with TTFT 15,772 / 17,874 ms, recovering to 99.7% once one request with the new head completes. Mechanism: `Workflow::schema_filter` (`src/workflow/mod.rs:458-474`) picks `ToolFilter::ExecutingResearch` for kind=research; `ToolFilter::PlanFrozen` (`src/tool/mod.rs:219-239`) documents that the frozen surface is ADVERTISEMENT, NOT ENFORCEMENT (dispatch denies at call time via `ToolFilter::allows`, `src/tool/mod.rs:302`). Intent pinned by the test `schema_filter_research_plan_uses_executing_research` (`src/workflow/mod.rs:1836`) — this fix deliberately reverses that intent (user-authorized), so update the test and the docs that state it.

R6-3 ANCHORS: trace producer `src/provider/trace.rs` (module doc :27, `log_file_path` :436, mirror sites ~:1092/:1406, cap rationale :73-95). Observed: `.coding/logs/traces.jsonl` is appended while requests stream; a read raced it and hit a 444,566+ char line failing `json.loads` (unterminated string); the round-5 extractor `.coding/analysis/cache-hit-5-extract.py` (blind parse) crashed on it, the round-6 one tolerates and reports it. Cause NOT confirmed (mid-write race vs crash-truncated record) — diagnose before fixing.

FOLLOW-UP A ANCHORS: `src/tool/agent/shell.rs` defines no `Tool::never_auto_for` override; `GitTool::never_auto_for` (`src/tool/agent/git.rs:516`) is subcommand-aware over the runtime `core_operations` list (default merge/push) and is consumed at `src/agent/dispatch.rs:339` to force the core-operation approval prompt even in Autonomous mode. So `shell git merge|push` currently bypasses a gate that agent.md, the APP RULES and MergeToMainDialog all promise. agent.md documents this as the standing residual and asks for a command-aware guard.

FOLLOW-UP B ANCHORS: `src/project/worktrees.rs::land` (~157-209): `worktree add … main` → `reset --hard main` → `git merge --no-ff` via `git_raw`, with NO fetch/pull — the same stale-main bug plan d826b9ad fixed in the merge_to_main skill still applies to the app-managed parallel-run-all landing. Real-repo tests live at 240-425.

FOLLOW-UP C ANCHORS: `frontend/src/components/layout/StatusBar.tsx:191` and `:781` still describe the merge flow as "(stash, commit, merge, …)" — contradicts the skill's NEVER-stash rule fixed in d826b9ad (the same stale text was already fixed in `MergeToMainDialog.tsx`). Comment-only.

VERIFICATION: `cargo test` at the root UNPIPED (a piped exit code is the pipe's, not the test's — agent.md), `cd frontend; npm test`, `cd frontend; npm run build`. Crate roots are `#![deny(warnings)]` — never add `#[allow(...)]`; fix root causes. Every defect fix here needs a test that fails without it (prove it empirically like R6-1's `|| true` neutralization).

## Steps
- [x] 1. **R6-2: stabilize the advertised tool schema across plan kinds** — Edit `src/workflow/mod.rs::schema_filter` (458-474) to return ONE frozen advertised surface for both plan kinds (the implementation `Executing`-based filter), so the tools array is byte-identical across a research↔implementation transition. Enforcement must NOT move: verify in `src/tool/mod.rs::allows` (302) that the `ExecutingResearch` arm still DENIES file_edit/file_write/file_append/convert_line_endings/complete_step/create_plan/update_plan/abandon_plan at dispatch, and add a test proving a research plan still cannot write even though the schema advertises the tool. Replace the now-wrong test `schema_filter_research_plan_uses_executing_research` (`src/workflow/mod.rs:1836`) with `schema_filter_is_stable_across_plan_kinds` asserting research and implementation filters are equal for the advertised surface. Update the doc comments on `ToolFilter::ExecutingResearch` (`src/tool/mod.rs:204-218`) and `PlanFrozen` (219-239) to state the surface is advertised in full and enforced at dispatch, citing the measured 6,016-token collapse (`.coding/analysis/cache-hit-6-report.md` cause 2).
- [x] 2. **R6-3: diagnose then fix the trace log's partial last line** — In `src/provider/trace.rs` (module doc :27, `log_file_path` :436, mirror sites ~:1092/:1406) FIRST diagnose the partial last line observed in `.coding/logs/traces.jsonl` (a 444,566+ char line failing `json.loads` while the file is live): decide from the write path whether a record is appended in several writes (a concurrent reader sees a half-written line) or a record can be crash-truncated mid-write. Record the finding in the commit message. Then fix along the confirmed cause: assemble the record (JSON + trailing newline) in memory and append it with ONE write so a reader sees either nothing or a complete line; for the crash-truncation arm, additionally drop an unparsable trailing line at open and document it. Add a regression test in this file's test module (existing traces tests near :2617-3228): a reader holding the file open must never observe a partial line — or, for the truncation arm, the open path must drop the trailing fragment.
- [x] 3. **Follow-up A: command-aware core-operation guard for ShellTool** — Add a command-aware `Tool::never_auto_for` override to `src/tool/agent/shell.rs`, mirroring `GitTool::never_auto_for` (`src/tool/agent/git.rs:516`, consumed at `src/agent/dispatch.rs:339`): detect a `git` invocation whose subcommand is in the runtime `core_operations` list (default merge/push) and require approval, so `shell git merge|push` can no longer bypass the core-operation gate in Autonomous mode. Keep false positives low — match the git executable at a command position (start of line or after `;`, `&&`, `||`, `|`) — and document the residual evasion (a quoted string, an alias, or a wrapper script). Tests in `src/tool/agent/shell.rs`: `git push` and `git merge` are never auto-run; `git status`, `git log`, `cargo test` and a command merely mentioning the words (e.g. `echo git-push-note`) stay auto-runnable.
- [x] 4. **Follow-up B: sync main before the app-managed landing merge** — `src/project/worktrees.rs::land` (~157-209): after `worktree add` + `reset --hard main` and BEFORE `git merge --no-ff`, sync the landing worktree with the remote — `git_raw(landing, ["fetch", "origin"])` then `git_raw(landing, ["pull", "--no-rebase"])` — treating 'no remote'/'no upstream'/'already up to date' as success and never aborting the landing on them. This closes the same stale-main gap that plan d826b9ad fixed in the merge_to_main skill. Add a test beside the existing real-repo tests (240-425): a repo whose `main` is behind its upstream must be synced before the item branch merges, and a repo with NO remote must still land successfully.
- [x] 5. **Follow-up C: fix the stale StatusBar merge-flow comments** — Fix the two stale doc comments in `frontend/src/components/layout/StatusBar.tsx` (around :191 and :781) that still describe the merge flow as "(stash, commit, merge, resolve conflicts, delete branch)" — the stash part contradicts the skill's NEVER-stash rule. State the current flow: commit on the branch, checkout main, sync main with origin (fetch + pull --no-rebase), merge --no-ff, verify both builds, delete the branch — matching `.coding/skills/merge_to_main.toml` and `frontend/src/components/views/GitView.tsx::mergePrompt`. Comment-only, no behavior change.
- [ ] 6. **Verify everything, prove each new guard, sync docs, and record the effect check** — Run `cargo test` at the root UNPIPED (read its own `test result:` lines), then `cd frontend; npm test` and `cd frontend; npm run build` — all green before the review step. For each new regression test, prove it is a real guard by neutralizing its fix, observing the failure, and restoring (the R6-1 method: `|| true` → `left: 2, right: 0` → restore). Then sync the docs the changes contradict: `docs/FEATURES.md` and `PLAN.md` where they describe the research-plan tool surface or the cache-hit items, and `agent.md`'s Environment note on shell-run git core operations (it currently documents the residual as un-gated). Finally append a short 'Post-restart effect check' section to `.coding/analysis/cache-hit-6-report.md`: the fix only takes effect after the app is rebuilt/restarted, so re-run `python .coding/analysis/cache-hit-6-extract.py` on fresh traffic and confirm section B3 reads `closed` for new records and the MID share drops (pre-fix baseline: MID 5/5 pairs, ~70-78% steady state); if it does not, reopen this work.
