# Plan: file_edit file-name chip opens the Diff tab (other tools unchanged)

## Goal
Make a file_edit tool card's file-name chip open the right-panel Diff tab at that file; all other tools keep opening the file in the Files tab.

## Kind
implementation

## Context
Backlog item: "file_edit clicking on the file_name should open the diff, all other stay the way they are". Exploration: the ToolCard header chips (frontend/src/components/chat/Message.tsx :425-527) render file paths from argPaths (frontend/src/lib/toolCardPaths.ts — file_edit returns [path]) as clickable buttons calling openFileInViewer (frontend/src/lib/openFile.ts → useAgentStore requestFileOpen → reveals+selects the Files tab). The right panel has a Diff tab (frontend/src/hooks/rightPanelViews.tsx:56) rendered by DiffViewer (frontend/src/components/views/DiffViewer.tsx), which consumes `selectedDiffPath` from the store (:197-251 — finds the per-file entry by path) and the store already exposes `revealRightPanelTab("diff")` (useAgentStore.ts:694-701 — enables+selects+reveals) and `selectDiffPath(path)` (:654). So the fix: a file_edit chip click opens the Diff tab at that file instead of the Files tab; every other tool keeps openFileInViewer unchanged.

## Steps
- [x] 1. **Deep-link file_edit chips to the Diff tab** — In frontend/src/lib/openFile.ts: add `openDiffInViewer(path)` mirroring openFileInViewer — `useAgentStore.getState().revealRightPanelTab("diff")` then `selectDiffPath(path)` (the store's revealRightPanelTab enables+selects+reveals the panel; selectDiffPath sets the per-file diff entry, and DiffViewer already consumes it). Doc comment: called from file_edit tool-card file-name links; every other tool keeps openFileInViewer. In frontend/src/components/chat/Message.tsx: import openDiffInViewer; in the chip onClick (~:511-514), branch on `name === "file_edit"` → `openDiffInViewer(chip.path)`, else `openFileInViewer(chip.path)`; the chip title for file_edit becomes `Open the diff of ${chip.path} in the Diff tab` (others keep "Open ${path} in the Files tab"). Regression test: the helper is store-level — add a test to frontend/src/hooks/useAgentStore.test.ts (existing suite) or a new openFile test asserting that calling openDiffInViewer("src/x.rs") leaves the store with rightPanelTab === "diff", rightPanelVisible === true, selectedDiffPath === "src/x.rs" (and if a new file, register it in frontend/vitest.config.ts include list). Run `cd frontend; npx tsc --noEmit` and `npm test`.
- [x] 2. **Docs + full verification matrix** — README.md: no dedicated bullet exists for tool-card file links — if the Agents & interface list has one about clickable file links, adjust it to note that file_edit chips open the diff; otherwise skip (the behavior is self-describing). Full matrix: `cd frontend; npx tsc --noEmit` + `npm test` and root `cargo test` (unpiped, read test-result + $LASTEXITCODE) — all green, zero warnings. Fix everything found before review.
