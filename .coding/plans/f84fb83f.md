# Plan: Roll back merge 5cdd822 — reset main to 2769fca

## Goal
Undo merge 5cdd822 on main per the user's decision ("the merge should not have happened"): reset main and every branch holding it back to the merge's first parent 2769fca, sync origin, verify the test suite, and re-bookkeep backlog + memory so the repo and knowledge base reflect the clean pre-merge state.

## Kind
implementation

## Context
User decision (this session): merge 5cdd822 ("Merge wt/agenticcoding: neutralize CONTEXT_FOOTER tag form", plan 7bb9a608) must be undone — main goes back exactly one submission, to the merge's first parent 2769fca ("Anthropic conversation-history caching (3rd breakpoint)"). Why: (1) the merge improperly carried the ABANDONED plan 833fa2fc reasoning_effort allow-list work (0012714, 67328a3 — abandoned at user request during review but left on the shared wt branch; the src/config/endpoints.rs + EndpointCard/ModelsSection/frontend changes) which the user reports breaks GLM; (2) the sentinel fix itself (942bc44) failed live verification — the user's DeepSeek session still mirrors the context footer (bracket form visible in their paste) and reasoning still leaks into the main window (open BUG 5, backlog 0d4f54e3 — NOT addressed by this rollback). Tree is clean (git status empty, verified). Rollback = git reset --hard 2769fca on main + the working wt branch; commits 0012714/67328a3/942bc44/09f3ed9/5cdd822 become reflog-only (~90 days; recovery: git reflog → 5cdd822). Precedent: the 2027-01-09 vendor-policy rollback (main reset to e0ac5dd; decision file .coding/knowledge/decision/2027-01-07-vendor-policy-direction-rolled-back-main-reset-t.md — that file is ≤2769fca and survives). Verification: cargo test unpiped + $LASTEXITCODE (the tree becomes byte-identical to pre-merge main, previously green). Bookkeeping: the reset reverts .coding/backlog.jsonl to its 2769fca content (the merge had flipped item 86fec233 to failed + one more line) — re-apply via backlog_status; supersede/update memory records (PLAN b45a4a48, BUG 5cf5469c, BUG 80c859a1); write a DECISION record + knowledge file.

## Steps
- [x] 1. **Pre-flight: map everything holding 5cdd822** — Read-only git inspection, no file mutation (no-code research step). PowerShell, each command unpiped: `git branch --show-current`; `git status --short` (MUST be empty — if not, stop and report to the user); `git branch -a --contains 5cdd822`; `git worktree list`; `git remote -v`; if a remote exists, `git log origin/main -1 --oneline`; `git diff 2769fca 5cdd822 -- .coding/backlog.jsonl` (the exact backlog lines the reset will revert — expect item 86fec233 in_flight→failed plus one more). Record the outputs; they drive steps 2, 3, and 5.
- [x] 2. **Reset main + working branch to 2769fca** — This reverts the working tree to 2769fca — the merge's file set returns to pre-merge state: src/agent/prompt.rs, src/provider/openai/tests.rs, src/config/endpoints.rs, frontend/src/components/settings/sections/EndpointCard.tsx, frontend/src/components/settings/sections/ModelsSection.tsx, frontend/src/lib/endpoints.ts, README.md, .coding/backlog.jsonl, and the merge-added files (.coding/knowledge/bug/7bb9a608.md, .coding/knowledge/bug/2027-01-07-reasoning-effort-off-shipped-as-the-highest-cura.md, .coding/knowledge/decision/2027-01-07-neutralize-context-footer-tag-form-first-fallbac.md, .coding/knowledge/spec/2027-01-07-context-footer-sentinel-context-footer-cache-law.md, .coding/plans/7bb9a608.md, .coding/plans/833fa2fc.md, .coding/reviews/2026-09-10-*.md). Commands: on the checked-out wt/* branch: `git reset --hard 2769fca`; then `git branch -f main 2769fca` (if another worktree holds main, `git -C <that path> reset --hard 2769fca` instead). For every OTHER branch from step 1's `--contains 5cdd822` list: tip == 5cdd822 (no unique work) → `git branch -f <name> 2769fca`; unique commits on top → LEAVE it and flag it in the final report (merging it later would re-introduce the rollback). Verify: `git log --oneline -3 main` shows 2769fca at the tip; `git branch -a --contains 5cdd822` returns only flagged leftovers (ideally none).
- [x] 3. **Sync origin** — No working-tree files touched — updates only the git refs .git/refs/heads/main and .git/refs/remotes/origin/main (no-code research step for the tree). From step 1: no remote → skip. origin/main at 5cdd822 or a descendant → `git push --force-with-lease origin main` (approval-gated; the rollback must rewrite the remote tip; --force-with-lease guards against racing pushes). origin/main at/below 2769fca → `git push origin main` (fast-forward; still approval-gated as a core op). Confirm afterwards with `git log origin/main -1 --oneline`.
- [x] 4. **Verify the rolled-back tree** — No-code research step (verification only — runs the test suite, mutates no files). `cargo test` unpiped, then `"exit=$LASTEXITCODE"`; every `test result:` line must show 0 failed. The tree is byte-identical to pre-merge main (previously green); under #![deny(warnings)] a green run also proves zero warnings. Never pipe through cmdlets (exit-code trap).
- [x] 5. **Re-apply the reverted backlog state** — The reset reverted .coding/backlog.jsonl to its 2769fca content. Using backlog_status only (never hand-edit .coding/backlog.jsonl): item 86fec233 (reasoning_effort allow-list) → status failed, note: 'plan 833fa2fc abandoned during review; its commits (0012714/67328a3) reached main only via merge 5cdd822, rolled back at user request (main reset to 2769fca) — the allow-list work is OFF main; requeue if needed'. Re-apply the second reverted line from step 1's diff the same way if still accurate.
- [x] 6. **Memory + knowledge bookkeeping** — (a) memory_supersede id b45a4a48 → 'PLAN: Neutralize the CONTEXT_FOOTER tag form — ROLLED BACK (merge 5cdd822 undone)': merged at 5cdd822, rolled back at user request (main reset to first parent 2769fca) — the fix failed live verification (DeepSeek still mirrors the bracket-form footer) and the merge improperly carried the abandoned 833fa2fc allow-list work; commits 942bc44/09f3ed9/5cdd822 reflog-only (~90 days). (b) memory_supersede id 5cf5469c → 'BUG: DeepSeek sentinel-mirror (CONTEXT_FOOTER echo) — OPEN, fix reverted': the bracket-form fix landed 5cdd822 but the mirror persisted (user live check; reasoning also leaks to the main window = open BUG 5) and the merge was rolled back to 2769fca; fallback ladder: (1) relocate volatile tail + sentinel into the final USER message for DeepSeek, (2) BUG 5 stream-level ThinkTagFilter heuristic (backlog 0d4f54e3), (3) freeze-and-audit DeepSeek serving-stack assumptions. (c) memory_update id 80c859a1 → the 833fa2fc allow-list work is off main since the 5cdd822 rollback (bug moot on main; knowledge file .coding/knowledge/bug/2027-01-07-reasoning-effort-off-shipped-as-the-highest-cura.md removed by the reset, reflog-only; resurfaces only if the allow-list work is requeued). (d) memory_search 'context footer sentinel' — supersede any live SPEC record still claiming the fix is current (mechanism facts — cache law, template hoisting — restated in the successor; spec file reflog-only). (e) memory_write tier semantic, 'DECISION: merge 5cdd822 rolled back — main reset to 2769fca': user decision 'the merge should not have happened' — it carried abandoned 833fa2fc work (broke GLM, user-reported) alongside the ineffective sentinel fix; stays: everything ≤2769fca (GLM stop-boundaries e20b1df, 429-fallback e0ac5dd, Anthropic caching 2769fca); re-opens: sentinel-mirror bug, reasoning-leak BUG 5, allow-list misalignment; recovery git reflog → 5cdd822. (f) Get the date (`Get-Date -Format yyyy-MM-dd`), then file_write .coding/knowledge/decision/<date>-merge-5cdd822-rolled-back-main-reset.md with the full decision body (untracked — rides the next merge, per the vendor-policy rollback precedent).
- [x] 7. **Closing sequence: review, fix, commit** — spawn_agent role:'reviewer' (omit model) with a self-contained task: verify the rollback — main at 2769fca with 5cdd822 absent from all branches, origin synced, cargo test green, backlog 86fec233 failed-with-note, memory records consistent, no unintended source changes (git diff HEAD empty; untracked = .coding/knowledge/decision/<date>-merge-5cdd822-rolled-back-main-reset.md + .coding/plans/<id>.md only). Fix every finding, re-run cargo test, commit the bookkeeping (.coding/knowledge/decision/<date>-merge-5cdd822-rolled-back-main-reset.md + .coding/plans/<id>.md + the review report in .coding/reviews/) to the wt branch, then finish.
