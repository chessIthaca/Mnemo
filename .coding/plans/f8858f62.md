# Plan: R19: split cache-hit aggregates by reporting tier

## Goal
Make the cache-hit round-5 extraction distinguish "provider never reports cached_tokens" from "cache missed": every cached-token aggregate in .coding/analysis/cache-hit-5-extract.py emitted split by reporting vs non-reporting model tier, re-run to produce the split aggregates (reporting-tier hit ~81% / resets ~17% vs non-reporting 0%/100% ballpark), with the daily-trend interpretation updated in the round's executive summary.

## Kind
implementation

## Context
Backlog fd8f67c0 (R19, from .coding/reviews/2026-09-09-cache-hit-5-review.md LOW-3): the headline "30% of requests are resets" overstates real waste — 2,495 of the 4,711 post-R12 resets are rows from model groups that never report cached tokens (all glm-5.3-flash* tiers, gemini-vlm-gcp — Gemini's OpenAI-compat layer doesn't populate prompt_tokens_details.cached_tokens). "Field absent" and "nothing cached" are indistinguishable per-row; the 0-50K bucket's 50.6% hit is likewise contaminated (gemini-vlm-gcp avgP 52.9K, ~half its 761 rows in that bucket at 0%). NOTE: the review's static non-reporter list is stale for glm-5.2-maas-gcp (17.5% hit) and deepseek-v4-flash (18.6% hit) — they DO report sometimes, so the heuristic "ever cached_tokens>0 in the window" is primary and the static list is a cross-check only. cached_tokens is INTEGER NOT NULL (src/memory/schema.rs:106) so the >0 test is NULL-safe. The optional schema flag (item step 2) is SKIPPED: the window heuristic self-adapts when a provider fixes reporting (a persisted static flag goes stale and needs backfills), and the write site cannot distinguish field-absent from field-zero without SSE-layer changes that queued item bd9df146 (R21) reworks anyway. Verification: run `python .coding/analysis/cache-hit-5-extract.py` from the project root and read $LASTEXITCODE directly (never trust a piped exit code); the acceptance ballpark (reporting ~81% hit / ~17% resets vs non-reporting 0%/100%) was measured on the round's window (2026-08-10..2026-09-09) — the db has likely grown since, so verify the split mechanics + reconciliation lines (rep+non = combined totals; non-reporting hit exactly 0.0%/100.0% by construction) and expect numeric drift, investigating only if the split itself looks wrong. The script must stay read-only over memory.db (file:.coding/memory.db?mode=ro unchanged; its only write is the aggregates .txt). No Rust files are touched; cargo test at the end proves the workspace stays green per project rules.

## Steps
- [x] 1. **Add tier classification + A0 headline split** — In .coding/analysis/cache-hit-5-extract.py, after the request_stats header block (after the `ttft_ms=0 rows` line, ~line 53): define REPORTING = {models with any row cached_tokens>0 in the window}, NON_REPORTING = the rest, and STATIC_NON_REPORTING = ('gemini-vlm-gcp', 'glm-5.3-flash', 'glm-5.2-maas-gcp', 'deepseek-v4-flash') as cross-check only. Emit "--- A0. Cache-reporting tier split (R19) ---": both model lists, the cross-check disagreements (heuristic vs static list), all-time + post-R12 headline per tier (n / hit% / resets% — the acceptance numbers land here), and reconciliation lines (rep_n + non_n = total rows; rep_resets + non_resets = total resets; non-reporting hit prints 0.0% / resets 100.0%). Do not touch line 1 (the file carries a UTF-8 BOM).
- [x] 2. **Split A1–A5 aggregates by tier** — In .coding/analysis/cache-hit-5-extract.py (same file as step 1): A1 per-model table gains a tier column (R/N) right after the model name (header + row format). A2 daily trend gains repN / repHit% / repRes% / nonN columns plus a one-line legend (rep* = reporting-tier rows only — the prompt-shape baseline; nonN = non-reporting rows, cached always 0; read day dips as nonN share + fallback-induced cold caches + over-cliff requests, not prompt-shape regression). A3 bucket table emitted three times via a shared helper (all rows / reporting tier / non-reporting tier, each keeping the all-time | post-R12 column pair). A4 post-R12 subset emitted per tier via a shared helper (three labeled tables; the reporting one is the prompt-shape baseline). A5 gains a reporting-tier-only worst-sessions table (>=20 requests) after the existing all-rows table. Sections B (traces) and C (provider-errors) unchanged — no cached-token aggregates there.
- [x] 3. **Re-run the extraction and verify the split** — From the project root run `python .coding/analysis/cache-hit-5-extract.py` and check $LASTEXITCODE directly (unpiped). Read the regenerated .coding/analysis/cache-hit-5-aggregates.txt and verify: A0 shows the tier split with non-reporting hit exactly 0.0% / resets 100.0% and reporting materially above the combined headline (~81%/~17% on the round's window; numeric drift expected if the db window grew); reconciliation lines add up; A2's daily rows carry the rep/non columns; A3/A4/A5 tier tables present; the sqlite connection in .coding/analysis/cache-hit-5-extract.py is still file:.coding/memory.db?mode=ro (read-only preserved — the script's only write is the aggregates .txt). If the split itself looks wrong (e.g. reporting tier at 0%), fix before proceeding.
- [x] 4. **Update the daily-trend interpretation** — file_edit .coding/analysis/2027-01-09-review-round-executive-summary.md, section 3 bullet "Reset metric conflates 'not reported' with 'missed'": append the shipped outcome — the tier split is live in the extraction (A0 headline + per-aggregate splits in .coding/analysis/cache-hit-5-aggregates.txt), reporting-tier post-R12 hit/resets is the prompt-shape baseline, non-reporting is 0%/100% by construction, and 09-09's 37.6% combined dip reads as flash-tier traffic + fallback-induced cold caches + over-cliff requests, not a prompt-shape regression. The review report itself (.coding/reviews/) is reviewer-protected — not edited.
- [x] 5. **Test, memory, commit** — Run `cargo test` (no Rust files touched — proves the workspace stays green per project rules; read $LASTEXITCODE unpiped). Write a DECISION memory: R19 shipped as the script-side tier split in .coding/analysis/cache-hit-5-extract.py (heuristic primary, static list cross-check); the optional reports_cached_tokens schema flag skipped (self-adapting heuristic vs stale persisted flag; field-absent vs field-zero indistinguishable at the write site — R21/bd9df146 reworks that path). Commit .coding/analysis/cache-hit-5-extract.py, the regenerated .coding/analysis/cache-hit-5-aggregates.txt, and the .coding/analysis/2027-01-09-review-round-executive-summary.md amendment to the working branch.
