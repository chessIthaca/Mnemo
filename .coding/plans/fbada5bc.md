# Plan: Bump checkout/setup-node to v5 (Node 24) in build.yml

## Goal
Eliminate the Node.js 20 deprecation warning emitted on every build run by bumping the two node20-targeting actions (actions/checkout@v4 x3, actions/setup-node@v4 x2) to their Node-24-native v5 majors in .github/workflows/build.yml, with a regression guard pinning the fix.

## Kind
implementation

## Context
Every build run annotates both jobs with "Node.js 20 is deprecated. The following actions target Node.js 20 but are being forced to run on Node.js 24: actions/checkout@v4, actions/setup-node@v4" (seen on runs 35514715816 and 35521221353; changelog 2025-09-19: deprecation of Node 20 on GitHub Actions runners). Future runner images may drop Node 20 entirely, turning the warning into a hard failure. Current pins in .github/workflows/build.yml: actions/checkout@v4 x3 (L46, L95, L201 — windows, macos, release jobs) and actions/setup-node@v4 x2 (L47, L96 — windows, macos). The other actions (dtolnay/rust-toolchain@stable, Swatinem/rust-cache@v2, actions/upload-artifact@v4, actions/download-artifact@v4, softprops/action-gh-release@v2) are NOT flagged — leave them untouched. v5 compatibility verified via the release notes: checkout@v5.0.0 (node 24; minimum runner v2.327.1 — GitHub-hosted windows-latest/macos-latest are far past it; drop-in) and setup-node@v5.0.0 (node 24; breaking change = automatic caching keyed on a packageManager field in package.json — our root package.json has none, and the explicit `cache: npm` + `node-version: 22` inputs at L48-50/L97-99 keep working). Verification commands: python -c PyYAML parse of build.yml; cargo test --test integration ci_workflow; cargo test --workspace (filtered to test-result summaries). The in-progress release run 35521221353 uses the workflow from its own commit (aa40d42) — landing this on main does not affect it; the annotation-free confirmation rides the next workflow_dispatch or v* tag run. NOTE: the file_edit transport froze repeatedly on build.yml earlier this session (phantom `lines:[]` field steering emissions) — if it recurs, fall back to shell surgery with single-quoted PowerShell strings + [IO.File]::ReadAllText/WriteAllText (the proven pattern from the CI-env fix), with the freeze stated as justification.

## Steps
- [x] 1. **Bump the five action pins to v5** — In .github/workflows/build.yml: `actions/checkout@v4` → `actions/checkout@v5` (L46, L95, L201) and `actions/setup-node@v4` → `actions/setup-node@v5` (L47, L96). Leave the `with:` blocks (node-version: 22, cache: npm) and all other actions (rust-toolchain, rust-cache, upload/download-artifact, action-gh-release) untouched. If file_edit rejects the edit (transport freeze seen earlier this session), fall back to shell surgery with single-quoted PowerShell strings + [IO.File]::ReadAllText/WriteAllText, stating the freeze as justification.
- [x] 2. **Add the regression guard** — Extend tests/integration/ci_workflow.rs with `build_workflow_avoids_node20_actions`: read build.yml via CARGO_MANIFEST_DIR, assert no `uses:` line pins the known node20-targeting majors (actions/checkout@v4, actions/setup-node@v4). Prove it fails pre-fix (5 offending lines) and passes post-fix — write the test first, run it against the pre-bump file, then apply the bump from step 1 and re-run.
- [x] 3. **Verify + land** — PyYAML parses build.yml (python -c "import yaml; yaml.safe_load(open('.github/workflows/build.yml', encoding='utf-8'))"); cargo test --test integration ci_workflow green (3 guards); full cargo test --workspace green (filtered summaries). Then the closing sequence: reviewer round, fix findings, commit, land on main via the merge procedure (shell fetch/pull, git merge --no-ff, push, delete branch). The annotation-free confirmation rides the next workflow_dispatch or v* tag run — note it for the user.
