# Plan: CodeGraph review fixes — F1 refresh flow + F2-F5 low findings + closing commit

## Goal
Fix all actionable findings from the 2026-04-19 CodeGraph review (F1 Medium refresh flow, F2-F5 Low), re-run the full test matrix, and land the CodeGraph closing commit on feat/codegraph.

## Kind
implementation

## Context
Implements the findings from .coding/reviews/2026-04-19-codegraph-review.md (reviewer pass over CodeGraph steps 7-8: IPC commands, slice API, Graph tab — all uncommitted on feat/codegraph). Verdict: slice engine, spawn_blocking lifetimes, IPC security, fixtures all verified clean. Findings to fix (notes 6+7 need no action except 6 folds into the MEDIUM fix):
- F1 MEDIUM Refresh flow (GraphView.tsx onRefresh :287-298 + poll effect :169-192; codegraph_cmds.rs :130-138): codegraph_refresh spawns the pass then reads stats — the spawned index() usually hasn't set indexing=true yet, so the returned status says indexing:false, the poll effect (keyed on status?.indexing) never starts, the "Indexing…" indicator never shows for user refreshes, and nothing reloads the graph payload when the pass completes (loadGraph runs immediately against the pre-pass store). Fix: (a) backend — in codegraph_refresh, early-out with current stats when graph.is_index() is already true (idempotent; also resolves note 6's overlapping passes), else set_indexing(true) SYNCHRONOUSLY before spawning so the returned status deterministically shows indexing:true (index() re-sets it on entry; its drop guard clears it on every exit path — the flag being true while the pass is merely scheduled is semantically correct); (b) frontend — add a prevIndexing ref + effect keyed on [status?.indexing, loadGraph] that calls loadGraph() on the true→false falling edge (an indexing pass just completed → reload the visualization).
- F2 LOW slice edge sort lacks kind tiebreak (query.rs :445): a symbol pair can carry two edge kinds; pre-sort order comes from iterating a HashSet (RandomState), so stable sort preserves randomized relative order run-to-run — contradicts the deterministic-order doc + the store's ORDER BY from_id,to_id,kind. Fix: append .then(a.kind.as_str().cmp(b.kind.as_str())). Regression test: from_parts with duplicate-id edges of two kinds → slice output ordered calls<contains<imports (assert exact order).
- F3 LOW transient render glitch (GraphView.tsx :425-452): render maps OLD nodePos.current against NEW model.radii/degrees between setState and the simulation effect; when the payload shrinks, model.radii[i] is undefined → <circle r={undefined}> invisible for a frame. Fix: guard inside the node map — skip render when model.radii[i] === undefined (mirrors the existing links guard).
- F4 LOW dead derives + false doc claims (store.rs GraphStats :29-31 Serialize; query.rs GraphSlice :117 Serialize): neither type is ever serialized (the IPC consumes GraphStats field-by-field into CodegraphStatus; returns CodegraphGraph, not GraphSlice; EdgeRow's Serialize IS used — keep it). Drop the two Serialize derives + fix the doc comments to claim nothing false.
- F5 LOW accidental line-join (query.rs :635): the test comment in view_load_roundtrips_store collapsed onto the fn signature line — restore it to its own line.
Constraints: Windows 11 + PowerShell, unpiped commands reading $LASTEXITCODE; warning-free under #![deny(warnings)]; regression test per defect; preserve line endings. After fixes: re-run full matrix (root cargo test, src-tauri cargo test, frontend npm test + tsc + npm run build), then the CodeGraph plan's closing commit lands on feat/codegraph including the review report (.coding/reviews/2026-04-19-codegraph-review.md) — then finish with that report path.

## Steps
- [x] 1. **F1 — refresh determinism + falling-edge reload** — (a) Backend src-tauri/src/ipc/codegraph_cmds.rs codegraph_refresh: before spawning, `if graph.is_indexing() { return Ok(stats_via_blocking(graph).await?); }` (idempotent early-out), else `graph.set_indexing(true);` synchronously then spawn — update the command doc comment to state the flag is set synchronously so the returned status deterministically reports the scheduled pass (index() re-sets it on entry; its drop guard clears it on every exit). (b) Frontend frontend/src/components/views/GraphView.tsx: add `const prevIndexing = useRef(false);` and an effect keyed `[status?.indexing, loadGraph]` that records the transition and calls `void loadGraph()` when prev was true and current is false (doc comment: reload the visualization when an indexing pass completes). Run `cargo test codegraph` in src-tauri/ and `npm test` + `npx tsc --noEmit` in frontend/.
- [x] 2. **F2 — slice edge kind tiebreak + regression test** — In src/codegraph/query.rs slice()'s final sort (~:445) append `.then(a.kind.as_str().cmp(b.kind.as_str()))` (mirrors the store's ORDER BY). Add a regression test in query.rs tests: build GraphView::from_parts with TWO edges sharing from/to ids but different kinds (calls + imports), slice unfiltered, assert the output edges are in kind order (calls before imports) with exact expected Vec.
- [x] 3. **F3 — node render guard** — In frontend/src/components/views/GraphView.tsx's node render map (~:425-452): inside the map, `const r = model.radii[i]; if (r === undefined) return null;` and use `r` for the circle radius (mirrors the existing links guard — old nodePos vs new model between setState and the simulation effect). Run `npm test` + `npx tsc --noEmit` in frontend/.
- [x] 4. **F4 + F5 — drop dead derives, fix docs, restore comment** — (1) src/codegraph/store.rs GraphStats: remove `Serialize` from the derive list and drop the 'Serializable so the IPC status command returns it directly' doc sentence (the command consumes it field-by-field). (2) src/codegraph/query.rs GraphSlice: remove `Serialize` from the derive list (the IPC returns CodegraphGraph; EdgeRow's Serialize stays — it IS serialized). (3) query.rs ~:635: restore the collapsed comment in view_load_roundtrips_store onto its own line above the fn (accidental line-join). Verify no other Serialize consumers break via `cargo test` at root (warning-free under deny(warnings)).
- [x] 5. **Verify all + closing commit** — Run each unpiped reading $LASTEXITCODE: root `cargo test`, `cargo test` in src-tauri/, `npm test` + `npx tsc --noEmit` + `npm run build` in frontend/. Fix anything red. Then commit on feat/codegraph (never main; git stages all) covering: CodeGraph steps 7-8 (codegraph_status/refresh/graph IPC + GraphSlice API + Graph tab + fixtures) and review fixes F1-F5 per .coding/reviews/2026-04-19-codegraph-review.md; confirm the commit includes the review report file. Then finish the CodeGraph plan with the report path .coding/reviews/2026-04-19-codegraph-review.md.
