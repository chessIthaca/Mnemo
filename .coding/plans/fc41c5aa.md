# Plan: Post-merge closeout: sync local main to the merged wt/mnemo (PR #7) and retire the branch

## Goal
Close out the merged wt/mnemo branch: fast-forward local main to origin/main (PR #7 merged, remote branch deleted), delete the merged local branch, and supersede this branch's status memories with the "MERGED into main at <sha>" successor. No source-code changes — git state, .coding knowledge records and memory rows only.

## Kind
research

## Context
The human merged PR #7 and deleted the remote branch wt/mnemo. PR #7 head = 2203439 (pre-merge tip), branch base origin/main = 614c42f, 14 commits carried. main is ruleset-protected (ruleset 23755694: pull_request rule, required_approving_review_count 1), so local main can only advance by fast-forwarding to origin/main — never a direct commit to main and never a direct push. Verified before the PR: `npm run build` in frontend/ -> exit 0, `cargo build` in src-tauri/ -> exit 0, root `cargo test` (unpiped) -> exit 0, warning-free under `#![deny(warnings)]`; the two commits after that verification (2021765, 2203439) are knowledge-text only, so the merged tree is the verified one. Facts to pin in step 1: PR #7's merge commit sha and the pre-merge tip 2203439 (both go into the memory successor text). Memory state: a merge_to_main run in this session already superseded the eight earlier wt/mnemo SPEC rows, which now read "MERGED via PR #6 (main 614c42f); PR #7 OPENED (tip 2021765) awaiting review" — step 4 must re-supersede exactly those (plus the f4636852 landed-design SPEC at .coding/knowledge/spec/2027-01-11-reviewer-rounds-2-are-delta-scoped-via-plan-fram.md, whose row still says "Landed: commit a85de74 on wt/mnemo", and the auto-captured PLAN digest for f4636852) with the MERGED-at-<merge sha> successor. Do NOT touch records for other branches. The .coding/knowledge/** successor files written by step 4 cannot be pushed to protected main; they stay in the worktree (step 5) and ride the next plan's branch + PR.

## Steps
- [x] 1. **Verify PR #7 landed and pin the merge sha** — no-code research (read-only gh/git verification; touches no repo file). Confirm the human's merge actually landed and pin the shas the memory successor needs. Run on the shell tool: `gh pr view 7 --json number,state,mergedAt,mergeCommit,headRefOid` (expect state MERGED; record mergeCommit.oid and headRefOid). Then `git fetch origin --prune`, `git log origin/main --oneline -4`, and `git log origin/main..wt/mnemo --oneline` — this MUST be empty (the branch is fully contained in origin/main); if it is not, STOP and report the unmerged commits instead of continuing. Also record the pre-merge tip for the successor text: it is 2203439 unless the log above says otherwise. Confirm `git rev-parse --abbrev-ref HEAD` is wt/mnemo and `git status --short` is clean (uncommitted work at this point would block the branch switch in step 2).
- [x] 2. **Fast-forward local main to origin/main** — no-code research (git state only; touches no repo file). On the approval-gated `git` tool: subcommand `checkout` to switch to local main (`git checkout main`). Then sync it with the remote on the same git tool: subcommand `merge` with args ["--ff-only","origin/main"]. Expect a fast-forward from 614c42f to origin/main's tip; the approval prompt is expected (core operation). If --ff-only refuses, do NOT force or reset: stop and report the divergence (local main carrying its own commits) and ask the user how to proceed.
- [x] 3. **Delete the merged local branch** — no-code research (git state only; touches no repo file). Delete the merged local working branch with the safe form only: on the `git` tool, branch delete of `wt/mnemo` (`git branch -d wt/mnemo` — NEVER -D). It succeeds because main now contains the branch's commits. If -d refuses ("not fully merged"), do NOT force-delete: report it — that would mean the human squash-merged and the original commits are not ancestors of main; in that case leave the branch in place and say so. Confirm the result with `git branch --list` (wt/mnemo gone, main present, main is HEAD).
- [x] 4. **Supersede the branch's post-merge status memories** — Writes .coding/knowledge/spec/*.md successor files (the file tools refuse .coding/knowledge/**; memory_supersede is the sanctioned writer). Search memory for the records whose status line is now stale: query "PR #7 OPENED (tip 2021765) awaiting review" — expect the eight wt/mnemo SPEC rows superseded earlier in this session (titles end '... — MERGED via PR #6 (main 614c42f); PR #7 OPENED (tip 2021765) awaiting review'; ids 8c41c2cd, 47997944, 3d4f962e, 20eac134, 313f514b, c3908848, c653cb5e, 52061e7e). Also cover: the f4636852 landed-design SPEC record (query "reviewer rounds 2+ are delta-scoped"; knowledge file .coding/knowledge/spec/2027-01-11-reviewer-rounds-2-are-delta-scoped-via-plan-fram.md) and the auto-captured PLAN digest for plan f4636852. For each, memory_supersede with tier semantic, a title keeping the same typed prefix and topic but ending '— MERGED into main (<merge sha from step 1>)', and a compact content body in the established house style: 'MERGED into main at <merge sha> (<full sha>) on <date> via PR #7 (merge_to_main skill, PR path); branch wt/mnemo deleted both remotely (by the human) and locally (step 3); pre-merge tip 2203439'. Never supersede another branch's records; skip any query that matches nothing.
- [x] 5. **Leave the superseder record files for the next PR** — no-code research (bookkeeping over .coding/knowledge/spec/*.md; no commit, no push). main is ruleset-protected, so the new .coding/knowledge/spec/*.md successor files written by step 4 can NOT be pushed to main directly — do not commit them to local main and do not push. Verify with `git status --short` that the only dirty paths are under .coding/knowledge/spec/ (the successor files, plus the superseded markers on their predecessors) and leave them in the worktree. They ride the next plan's wt/* branch and its PR. State this explicitly in the final report so the next session does not mistake the dirty tree for lost work.
- [x] 6. **Verify main and report** — no-code research (verification + report; touches no repo file). Confirm and report: `git log main --oneline -3` (top commit = the PR #7 merge, sha matching step 1), `git merge-base --is-ancestor 2203439 main; "exit=$LASTEXITCODE"` (expect 0 — the pre-merge tip is contained in main), `git branch --list` (no wt/mnemo), `git rev-parse --abbrev-ref HEAD` (main), and `git status --short` (only .coding/knowledge/spec/**). Report: the merge sha, that local main == origin/main, the deleted branch, the memory supersede count, and the pending uncommitted .coding records awaiting the next PR.
