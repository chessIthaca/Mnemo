# Plan: Verify status-bar tok/sec rolling window already landed (done-orphan re-dispatch)

## Goal
The dispatched backlog item (Status bar tok/sec: rolling last-3-requests window) describes work already shipped: plan 70328e41, commit 41f3cae, round-2 review PASS, SPEC record written; the item was orphaned in flight and requeued by the run-all machinery. This plan verifies every acceptance criterion in the CURRENT tree (ring buffer + reducer guard + total-over-total display + interpolated tooltip + both test suites + the scope guard), runs the named acceptance suites (npm test, npx tsc --noEmit), amends the SPEC record with the re-verification, and closes the item as a done-orphan with zero source changes. If any verification check fails, the plan is abandoned and reclassified as an implementation plan for the actual gap.

## Kind
research

## Context
Done-orphan re-dispatch (backlog a21993a4, "orphaned in flight — requeued by run-all start"): the work shipped at plan 70328e41, commit 41f3cae on wt/agenticcoding, round-2 review PASS; SPEC record .coding/knowledge/spec/2027-01-07-status-bar-tok-sec-rolling-last-3-requests-windo.md. Verified present in the current tree during planning: SessionTiming.recentTiming ring buffer (frontend/src/hooks/agentState.ts:108, init :389 + useAgentStore.ts:986), reducer append gated on generation_ms > 0 (frontend/src/hooks/agentEventReducer.ts:794-808), recentOutputTokPerSec total-over-total via aggregateTokPerSec (frontend/src/hooks/agentState.ts:149-161 — NOT average-of-rates, the 463ddd4 lesson), InflightBar wiring + interpolated tooltip "Output tok/sec over the last ${RECENT_TIMING_WINDOW} requests" (frontend/src/components/chat/InflightBar.tsx:8/:148/:330), tests useAgentStore.test.ts:988 ("recentTiming: rolling last-3 window feeds the status-bar tok/sec") + agentState.test.ts:254 (recentOutputTokPerSec describe). Scope guard verified: recentOutputTokPerSec's only UI caller is InflightBarPanel (graph callers: InflightBar.tsx:76 + the two test files); sessionTiming totals still accumulate over ALL requests and feed StatsView. Acceptance suites to run: cd frontend; npm test; npx tsc --noEmit. If any check fails, abandon and reclassify as an implementation plan for the gap. After Complete, the session flips the item done manually via backlog_status (the automatic flip misses research-plan completions — recorded in .coding/knowledge/bug/2027-01-11-run-all-done-flip-misses-research-plan-completio.md).

## Steps
- [x] 1. **Confirm the landed work + scope guard in the current tree** — re-confirm via the graph/search evidence: SessionTiming.recentTiming (frontend/src/hooks/agentState.ts:108), the generation_ms > 0 gated append (frontend/src/hooks/agentEventReducer.ts:794-808), recentOutputTokPerSec calling aggregateTokPerSec total-over-total (frontend/src/hooks/agentState.ts:149-161), the InflightBar display + interpolated tooltip (frontend/src/components/chat/InflightBar.tsx:8/:148/:330), and the two test suites (frontend/src/hooks/useAgentStore.test.ts:988, frontend/src/hooks/agentState.test.ts:254); scope guard: recentOutputTokPerSec's only UI caller is InflightBarPanel and StatsView/lastRequestTiming/traceStats are untouched by the window. If any piece is missing, stop and reclassify as an implementation plan.
- [x] 2. **Run the acceptance suites** — `cd frontend; npm test` (the window tests + the full suite green) and `npx tsc --noEmit` from frontend/ (the item's acceptance names it) — both must be clean; no-code research otherwise.
- [x] 3. **Confirm the records + amend the SPEC** — read .coding/plans/70328e41-e5e7-4be0-823e-81f69557c519.md (6/6 steps complete) and the window plan's round-2 review report in .coding/reviews/; then memory_amend the SPEC record (id found via memory_search for the file-name fragment 'status-bar-tok-sec-rolling', file .coding/knowledge/spec/2027-01-07-status-bar-tok-sec-rolling-last-3-requests-windo.md) with a dated paragraph: the 2027-01-12 run-all re-dispatch (item orphaned in flight, requeued) verified the work landed (commit 41f3cae in history, all pieces present, npm test + tsc --noEmit clean) — done-orphan closed with no code changes.
