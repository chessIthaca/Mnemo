# Plan: Centralize Message/ToolCall construction via typed constructors

## Goal
Introduce typed constructors (Message::user_text, Message::system, Message::assistant_text, Message::assistant, Message::tool_result, ToolCall::new) in src/provider/mod.rs, then migrate all ~90 Message and ~20 provider::ToolCall struct-literal sites across the codebase to use them, so future field additions to these core structs touch only the constructors, not every call site.

## Kind
implementation

## Context


## Steps
- [x] 1. **Add constructors to provider/mod.rs + unit tests', body:**
- [x] 2. **Bulk-migrate plain patterns via PowerShell script', body:**
- [x] 3. **Fix remaining complex sites in tests.rs + context.rs', body:**
- [x] 4. **Fix remaining complex sites in turn.rs + other files', body:**
- [x] 5. **Remove redundant local helpers', body:**
- [x] 6. **Build + test + review', body:**
