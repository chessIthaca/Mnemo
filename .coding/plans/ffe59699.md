# Plan: Vendor-specific reasoning retention + proxy-cliff compaction ceiling

## Goal
Implement the two remaining prompt-caching items from backlog 8814f81c: (4) policy-driven vendor-specific reasoning retention — Gemini 3 thinking text is droppable from historical assistant turns (thought_signatures always survive); DeepSeek historical reasoning_content is dropped only when context approaches the ~340K proxy ceiling, with the most recent assistant turn always kept verbatim (Rule 1 echo unchanged for every other provider) — and (5) a proxy-cache-cliff compaction ceiling so summarization triggers before context crosses ~340K tokens, where LiteLLM-class proxies drop whole-conversation prefix caching (hit rate collapse 99% -> 4%). No changes to tool calling, workflow states, or provider wire contracts; stored Message.raw is never mutated — stripping applies to the outgoing wire payload only.

## Kind
implementation

## Context


## Steps
- [x] 1. **Add ReasoningRetention to ProviderPolicy (policy.rs)** — define `pub struct ReasoningRetention { pub keep_recent: usize, pub historical_droppable: bool, pub signatures_required: bool }` with `Clone+Copy+PartialEq+Eq+Debug`, a `NEVER_STRIP` const (keep_recent = usize::MAX), and a doc-commented method `pub fn strips_reasoning(&self, assistant_age: usize, under_pressure: bool) -> bool` (age counted from the END of history: 0 = most recent assistant turn; returns false when age < keep_recent, else `historical_droppable || under_pressure`; NEVER_STRIP returns false always). Add field `pub retention: ReasoningRetention` to ProviderPolicy and set registry values: gemini() = {keep_recent: 1, historical_droppable: true, signatures_required: true} (thinking text droppable, thought_signature kept), deepseek() = {keep_recent: 1, historical_droppable: false, signatures_required: false} (drop only under pressure), every other constructor (claude, openai_responses, qwen, kimi, glm, vllm_default) = NEVER_STRIP. Update the module doc table with a retention column. Unit tests in policy.rs tests module: retention values per provider + strips_reasoning matrix (gemini age>=1 strips even without pressure, age 0 never strips; deepseek strips only when under_pressure; qwen/claude never strip even under pressure).
- [x] 2. **Apply retention at the wire (mod.rs helper + openai.rs builder)** — in src/provider/mod.rs add `pub const PROXY_CACHE_CEILING_TOKENS: usize = 340_000;` (LiteLLM-class proxies drop whole-conversation prefix caching above ~340K input tokens) and `pub const PROXY_CACHE_PRESSURE_MARGIN_TOKENS: usize = 32_768;`, plus `pub fn strip_reasoning_text_fields(obj: &mut serde_json::Value, keep_signatures: bool)` which removes top-level "reasoning_content" and "reasoning" keys and, only when keep_signatures == false, also "thought_signature" at message level AND inside each entry of tool_calls[] — never touching content/tool_calls structure (unit tests in mod.rs). In src/provider/openai.rs build_request_json (~line 1321): resolve `ProviderPolicy::for_kind_and_model(config.kind, &config.model)` once before the message map; compute `under_pressure = estimate_prompt_tokens(messages, tools) + PROXY_CACHE_PRESSURE_MARGIN_TOKENS > PROXY_CACHE_CEILING_TOKENS`; count assistant messages; inside the map, for an assistant message at age >= policy.retention.keep_recent where policy.retention.strips_reasoning(age, under_pressure), apply strip_reasoning_text_fields to the outgoing serialized object on BOTH paths (the raw-echo clone at the early return ~line 1344 and the synthetic field-built path) — run the strip AFTER the synthetic DeepSeek reasoning_content re-add (~line 1414). Never mutate the stored Message (clone/raw-copy only). Comment explaining the sanctioned Rule-1 deviation is policy-driven. Regression tests in openai.rs tests: gemini multi-turn — historical assistant turns lose reasoning keys but per-tool-call thought_signature SURVIVES and the most-recent assistant turn stays byte-verbatim; deepseek under pressure — historical reasoning_content dropped, most-recent kept; deepseek below pressure — nothing dropped; qwen under pressure — nothing dropped; stored Message.raw unchanged after build_request_json.
- [x] 3. **Proxy-cliff compaction ceiling (context.rs + config + wiring)** — in src/agent/context.rs add field `proxy_cache_ceiling: Option<usize>` (default None = current behavior) + builder `pub fn with_proxy_cache_ceiling(mut self, ceiling: Option<usize>) -> Self`; make the effective summarize trigger `min(summarize_at, ceiling.saturating_sub(PROXY_CACHE_PRESSURE_MARGIN_TOKENS))` in summarize_at()/should_summarize() and document that this keeps requests below the proxy cliff. In src/config/general.rs add `pub proxy_cache_ceiling_tokens: Option<usize>` to ContextConfig with default Some(PROXY_CACHE_CEILING_TOKENS); add validation (Some(v) requires v >= 65_536) in src/config/patch.rs and src/config/settings_dto.rs following the exact summarize_at_fill_rate pattern at those sites (patch.rs ~259/~397/~516, settings_dto.rs ~200/~248/~456). Wire config -> AgentFactory (src/agent/factory.rs, fill_rate threading at ~109/~226/~457/~525) -> ContextManager::new(...).with_proxy_cache_ceiling(...) and through the runtime/agent.rs fill_rate-threading functions (~1314/~1756/~2003/~2188/~2361) following the same pattern. Tests: ceiling caps summarize_at (1M window, fill 0.5 -> 500K uncapped, capped to ceiling - margin with ceiling set), default config value + TOML round-trip, validation rejects Some(1024), existing summarize tests stay green.
- [x] 4. ****Add Advanced-settings UI control for proxy_cache_ceiling_tokens** — AdvancedSection.tsx, mirroring the summarize_at_fill_rate control (read at line ~74, patch at ~128, undo-snapshot at ~237): number input with thousands display, empty/null = guard disabled, save via patch.proxy_cache_ceiling_tokens; respects the existing validation (0/empty disables; ≥65_536 enforced Rust-side). Run frontend tests (vitest) + backend workspace tests.** — Mirror the existing context-knob control pattern in frontend/src/components/settings/sections/AdvancedSection.tsx: read state from s.context.proxy_cache_ceiling_tokens (number | null — null/empty means the cliff guard is disabled), render a labeled number input next to the summarize_at_fill_rate control with a one-line helper text ("Summarize before the ~340K proxy cache cliff where prefix caching dies; empty = disabled"), write to patch.proxy_cache_ceiling_tokens on change (null when cleared). Rust-side validation (Some(v) requires v >= 65_536, Some(0) clears) already exists — the UI just sends the raw value or null. Then run the frontend test suite (vitest per frontend/package.json) and re-run cargo test --workspace to confirm no IPC contract drift.
- [x] 5. ****Docs + knowledge sync** — README config example for proxy_cache_ceiling_tokens, PLAN.md provider-strategy note (vendor reasoning retention + cliff ceiling), knowledge decision file, DECISION memory record.** — README.md: add proxy_cache_ceiling_tokens to the config example table near the context settings with a one-line rationale. PLAN.md: add a provider-strategy note describing the retention policy (Gemini signatures-only history, DeepSeek pressure-triggered historical drop, all other providers verbatim Rule-1 echo) and the cliff ceiling. Create .coding/knowledge/decision/2026-12-22-vendor-reasoning-retention-and-proxy-cache-cliff.md documenting the design (policy field, strip sites, ceiling math, why stored raw is never mutated). memory_write a DECISION record pointing at the file + plan ffe59699.
- [x] 6. ****Verify + closing sequence** — full workspace + frontend tests, reviewer subagent, fix findings, commit, finish.** — Run cargo test --workspace and the frontend suite (must be green and warning-free). Spawn reviewer (role:"reviewer") covering all uncommitted changes against the plan goal plus project-specific checks (doc sync, multi-platform neutrality). Fix every finding, re-run tests, commit to the current working branch including the review report, then finish. After finish: re-close backlog 8814f81c as done (the user interrupt flipped it to pending; per decision aeac2be5 an interrupt should not fail a completed item).

## Regression test
retention_deepseek_synthetic_path_strips_under_pressure
