## Verdict: FINDINGS (0 high, 2 low)

Reviewed ALL uncommitted changes on `wt/agenticcoder` (git diff HEAD: `src/skill/mod.rs` +33/-0, `src/project/mod.rs` +60/-0; untracked `.coding/plans/b33d21c9.md` is the plan file, not source). The feature is correct, well-tested, and platform-neutral; the only findings are two doc-staleness items (one method doc comment, one README paragraph). Details below.

### What was verified as correct

- **include_str! path (focus a).** `src/skill/mod.rs` resolves `../../.coding/skills/merge_to_main.toml` to the repo root, where the file exists and is **tracked in git** (`.gitignore` excludes only `memory.db*`, `codegraph.db*`, `logs/`, `plans/stack.json`; `git status` shows no untracked file under `.coding/skills`), so a fresh clone compiles. `include_str!` is resolved relative to the source file at compile time — forward-slash relative paths work identically on Windows and macOS, and nothing is read from disk at runtime, so installed binaries work without the source tree. No Windows-only assumptions.
- **Write-if-missing semantics (focus b).** `seed_skills` does `create_dir_all` + per-skill `if path.exists() { continue; }` before `fs::write` — user-modified files are never overwritten, re-seeding a populated dir is a no-op, and deleting a file/dir self-heals. The exists/write race is not worth flagging for a local desktop app, as noted in the task.
- **Init self-heal consistency (focus c).** `Project::init` is called ONLY at project-creation events: the `create_project` IPC (src-tauri/src/ipc/projects.rs:99) and the CLI `--project` pointing at a non-project dir (src-tauri/src/main.rs:1014). Existing projects opened at app start use `Project::from_root` — init is NOT invoked on every app open. Seeding on these creation paths is acceptable and mirrors the pre-existing agent.md re-scaffold precedent in the same function; even repeated calls cost only `create_dir_all` + N exists-checks. Errors are mapped to `Error::Project` with the skills dir path in context, consistent with the surrounding code.
- **End-to-end wiring.** The seeded files are actually consumed: startup builds the registry via `SkillRegistry::load_dir(&project.skills_dir)` (src-tauri/src/main.rs:1675-1676).
- **Multi-platform + warnings (focus f).** Pure `std::fs` + `Path::join`; no `cfg(windows)`, no path/API assumptions; no `#[allow(...)]` added. Both new public items are used (no dead-code warnings under `#![deny(warnings)]`); the green `cargo test` run (1569 passed / 0 failed / 1 ignored) confirms a warning-free build.
- **Test quality (focus g).** All four tests genuinely pin the behavior and a revert of any part fails at least one:
  - `init_scaffolds_coding_dir` (extended) asserts the skills dir + every shipped skill file after init — fails if init seeding is reverted.
  - `init_seeded_skills_never_overwrite_user_edits` overwrites a seeded skill with custom content, re-inits, asserts content unchanged — fails if seeding ever overwrites.
  - `init_self_heals_missing_skill_files` removes the whole skills dir, re-inits, asserts every shipped skill returns — fails without the self-heal branch.
  - `shipped_skills_embedded_entries_parse_and_carry_their_name` parses every embedded TOML, asserts the pair name equals the spec's `name` field, and pins `merge_to_main` presence — fails on malformed/renamed/emptied entries.

### Findings

**1. (low) `Project::init` doc comment is now stale — "no-op" claim.** `src/project/mod.rs:126-130` documents: "If `.coding/` already exists, this is a no-op (returns the existing project), though a missing `agent.md` is still scaffolded." The already-initialized path now ALSO seeds `.coding/skills/` (self-heal), so the "no-op" claim is inaccurate — the doc should mention skill seeding alongside the agent.md re-scaffold. Related: the inline comment at `src-tauri/src/ipc/projects.rs:96-97` ("a directory that is already a project is left untouched (a missing agent.md is still written)") is similarly incomplete — it now also re-seeds missing skills. Fix: extend both comments (one sentence each). No code change required.

**2. (low) README.md "Per-project state" paragraph omits the auto-seeded skills dir.** `README.md:120` enumerates what lives in `<project>/.coding/` (knowledge, plans, reviews, backlog, DBs) but not `skills/` — which the app now populates automatically at project creation with the embedded `merge_to_main` skill. Since the README already documents skills as a feature (lines 31, 45-46) and this is user-visible behavior (new projects get the skill; user edits are preserved; deletion self-heals on next init), a brief clause in the paragraph at line 120 (e.g. "and the shipped skills (`.coding/skills/`, seeded at init — write-if-missing, so project-specific edits are preserved)") keeps the docs in sync. PLAN.md has no scaffolding/skills section that this change contradicts — no finding there.
