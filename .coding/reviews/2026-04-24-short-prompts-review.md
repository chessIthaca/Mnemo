## Verdict: FINDINGS (1 high, 2 low)

Scope reviewed: `git diff HEAD` on branch `feat/short-prompts` — `src/agent/prompt.rs` (prompt compression, the plan's work), `src/tool/mod.rs` (pre-existing: reviewer-only authorship filter + `abandon_plan` in Reviewing), `src/tool/agent/sandbox.rs`, `src/tool/agent/file_write.rs`, `src/agent/dispatch.rs`, `src/agent/loop_impl.rs`, `src-tauri/src/ipc/events.rs`, `agent.md`, plus the unchanged-but-relevant docs (`README.md`, `PLAN.md`) for constitution documentation-sync. `.coding/backlog.json` + `.coding/plans/stack.json` are app bookkeeping (ignored per scope). Build: `cargo test` reported green (1430 passed, 0 failed; `#![deny(warnings)]` at both crate roots) — I verified no new `#[allow(...)]`, unused imports, or dead code were introduced.

### Summary of the substantive review (no findings here)

**Prompt compression (`src/agent/prompt.rs`) — rule-by-rule semantics check passed.** I compared every hard rule in the old text against the compressed text: never-commit-to-main + `merge_to_main` exception ✓; core-ops approval gate (merge/push, no mode/skill bypass) ✓; bookkeeping never prompts ✓; never-resend-identical-failed-call (parse → fix named param → re-issue) ✓; line-ending preservation + file_edit-over-file_write ✓; ≤3-line summaries ✓; full closing sequence test→review→fix-every-finding (only skip = factually-wrong finding with written justification)→commit-includes-report→finish ✓; reviewer tool surface (reads + git_diff/log/show + web_fetch + graph + memory/backlog queries + `write_review_report` to `.coding/reviews/`; cannot ask/mutate/plan/finish) ✓; verdict contract + fail-closed ✓; bug-plan checklist (regression test exercises changed path, root cause documented, BUG: memory written) ✓; never sleep/poll after spawn + end-turn-and-resume ✓; failed-reviewer ask_user protocol ✓ (with the new no-self-review wording); reviewer-only authorship + `abandon_plan` escape ✓; mandatory memory_write/memory_recall/context_pack/past_fixes habits ✓; graph-first for symbols + string-literal carve-out ✓; search-as-fallback + chain-to-graph_context ✓; record budgets/prefixes ✓; pointer-first ✓; supersede/update/delete hygiene ✓; branch-status default ✓; plan kinds (implementation/research/bug_fixing + never-research-a-code-change + locked bug skeleton) ✓; sub-plan push/resume ✓; `CURRENT PLAN/GOAL/PROGRESS/CURRENT STEP` data lines and CAPABILITIES untouched ✓. The two new preamble principles (memory-first; ask-when-unsure) are present and asserted. `stable_head_byte_stable_across_workflow_changes` and `context_footer_is_stable_literal` still hold (head/footer unchanged in structure; tests updated to the new marker strings).

**Tool-layer enforcement (`src/tool/mod.rs`, `sandbox.rs`, `file_write.rs`) — correct and consistent.** The `write_review_report` hard gate (`!matches!(self, ToolFilter::Reviewer(_))` → deny) is checked before every filter arm, so base states and the Skill allow-list can never grant it; the reviewer spawn path (`role: "reviewer"` → `set_reviewer_allowlist` → `ToolFilter::Reviewer`) still grants it (gate passes, Reviewer arm matches the name). No built-in skill lists the tool, and `write_review_report_visible_only_under_reviewer_filter` covers schema-level (all 4 base states + a skill that lists it) and dispatch-level (allows()) denial plus the grant path and the "only when the list names it" case. The sandbox protection is complete for the file-tool surface: all four file tools (`file_write`/`file_append` via `validate_for_write`, `file_edit`/`convert_line_endings` via `refuse_if_protected`) refuse `.coding/reviews/`; case-insensitive (NTFS) and creation-gap (lexical path before the file exists) are both tested. `abandon_plan` in Reviewing is coherent: dispatch state filter grants it, the descendant gate only blocks while a reviewer is still running (the escape scenario is a *failed* reviewer — finished, no descendants), `Workflow::abandon_plan` pops to Planning (root) — matching the documented escape. The failed-reviewer protocol text in `dispatch.rs`/`events.rs`/`loop_impl.rs`/`agent.md`/prompt is uniformly de-self-reviewed, and the events test asserts the option is gone. README.md:54 verified current in the working tree (contains the reviewer-only authorship clause + "never self-review"); the earlier search hit showing the old text was a stale content index, not the file.

**Multi-platform neutrality:** no Windows-only APIs/paths/syntax in any changed code; the PowerShell/sh branching in `shell.rs` is pre-existing and untouched; the lowercase + ADS-path checks are cross-platform-safe. Constitution-compliant on that axis.

### Findings

1. **HIGH — documentation sync: `PLAN.md` still documents the deleted self-review fallback.** `PLAN.md:232` ("the parent must `ask_user` (retry on another model / abandon the review / self-review)") and `PLAN.md:698` ("instructing `ask_user` (retry on another model / abandon the review / self-review)") both still offer the self-review option that this change structurally removes. The project's technical-decision document now contradicts the shipped protocol (code, prompt, `agent.md`, `README.md` all say the main agent can never author a review). This is the exact stale-doc class the plan's own docs-sync step targets, and per the constitution "a feature that ships with its docs not updated is an incomplete change." **Fix:** update both lines to the two-option protocol (retry on another model / abandon the review — the main agent can never self-review; `abandon_plan` is the escape). Also consider adding to `src/tool/agent/write_review_report.rs`'s module doc one sentence that the tool is visible only under `ToolFilter::Reviewer` (constructor-granted), since the module doc still describes it as merely "the reviewer's single output channel."

2. **LOW — stale doc comment: "This is the **only** path out of `Reviewing`" (`src/workflow/mod.rs:649`, on `Workflow::finish`).** With `abandon_plan` now granted in the Reviewing state (pops the finished plan to Planning), `finish` is no longer the only exit; it is the only exit *to Complete*. The comment predates this change and is now false. **Fix:** reword to "the only path from Reviewing to Complete" (or note that `abandon_plan` is the escape back to Planning).

3. **LOW — the "airtight"/"never" authorship claims overstate against the shell channel.** `shell` runs arbitrary commands with cwd confined to the sandbox root and is *not* checked against `is_protected_write_target`, so a main agent could still write under `.coding/reviews/` via an approved shell command (e.g. `Set-Content`). The claims ".coding/reviews/ is protected from the file tools, so the main agent can never write a review itself" (agent.md), "the protection is airtight" (sandbox.rs:172), and README's "the main agent can never review its own code" are overbroad: the residual channel is real, though approval-gated (NeedsApproval, user sees every call). This matches the established residual for `.coding/plans/`/`safety.toml`, so the design is consistent — but the doc language should be scoped. **Fix (minimal):** qualify the claims with "approval-gated shell remains, like `.coding/plans/`" in the sandbox module doc (and optionally in README's parenthetical); no code change is required, and extending the shell filter is not worth the complexity for an approval-gated path.

### Conclusion

The prompt compression is faithful — no rule was lost — and the reviewer-only authorship enforcement is structurally correct at the filter, sandbox, and protocol layers, with solid regression tests. The three findings are documentation-scope (one constitution-mandated docs sync, two stale-comment wordings); no code-correctness or security bug was found in the runtime behavior.
