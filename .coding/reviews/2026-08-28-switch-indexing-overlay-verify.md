## Verdict: FINDINGS (0 high, 2 low)

Round-2 verification of plan eedeaeab at commit `4dc4a59` (HEAD, wt/agenticcoder; working tree has only the post-commit step-5 checkbox tick in `.coding/plans/eedeaeab.md` — bookkeeping, not source). All five code findings from round-1 are **verified fixed in the committed code** — H1, H2, H3, L5 exactly as claimed. Two residual LOWs: the L6 rename left a **duplicate test** behind, and the L4 fix's **claimed test pin (the 600/500 clamp case) does not exist** (the clamp itself is in place). Neither is behavioral; both are one-touch fixes. No new correctness, race, wedge, security, platform, or constitution issues were introduced by the fixes.

## Per-finding verification (against the committed code, not claims)

- **H1 — VERIFIED FIXED** (src-tauri/src/main.rs:1548–1600). `clear_startup_index()` runs first (1551), then the terminal `match &pass`: the `Ok(Ok(stats))` arm emits `Done` **unconditionally** (1563–1574); `Ok(Err(e))` (1576–1587) and `Err(join)` (1588–1599) emit `Failed` only inside `if forwarded.load(Relaxed)`. The restructured comment (1552–1561) now states exactly what the code does. Supporting structure intact: `forwarded` initialized to `switched_open` (1502–1505), `Started` emitted immediately on switch (1506–1511), the callback stores `forwarded=true` on every forwarded tick (1541–1542).
- **H2 — VERIFIED FIXED** (codegraph_cmds.rs:356–419 + main.rs:1496–1533). `mark_startup_index_active(switched)` stores `switched` into `STARTUP_INDEX_ACTIVE` (373–377) → fetchable at mark time ONLY on a switched launch; cold passes publish via `ensure_startup_index_visible()` (384–386) called on the first **forwarded** tick inside the gate-accepted branch (1528–1533); `record_startup_index_progress` still records every tick **before** the gate (1515–1521). The regression test `startup_snapshot_records_and_clears` (644–676) pins the exact claimed sequence: `mark(false)` + records → `None` (651–658, "no-flash contract"), `ensure_startup_index_visible()` → `Some((150,500))` (659–661), `mark(true)` → `Some((0,0))` immediately (663–666), `clear` → `None` (670–672), record-while-inactive does not resurrect (673–675). This test fails against the pre-fix design (mark was unconditional) — a true regression pin.
- **H3 — VERIFIED FIXED** (frontend/src/components/projects/IndexingOverlay.tsx:154–175). `const myToken = token;` is captured at fetch dispatch (163), inside the subscribe-`.then` (so it already reflects any events handled before dispatch); the fetch `.then` bails on `disposed || snap === null || token !== myToken` (166). Guard semantics verified: `handleEvent` bumps `token` on EVERY event (120), so a terminal `done` consumed mid-flight (before OR after response resolution) always invalidates the seed. Ordering soundness: `myToken` is read after any pre-dispatch events, so only fetch-window events cause a mismatch — the only window that matters. The `.catch` (172–174) keeps catch-up best-effort.
- **L4 — clamp VERIFIED, claimed test pin ABSENT** (see finding 2 below). `startup_index_snapshot` returns `Some((done.min(total), total))` with an accurate tearing comment (codegraph_cmds.rs:406–419). The claim that this is "pinned by the clamp case … records (600,500) → Some((500,500))" is **false against the commit** — no done>total case exists anywhere in the test module (read in full, 521–686).
- **L5 — VERIFIED RESOLVED.** Both knowledge files (`.coding/knowledge/bug/bb8a0ca9.md`, `.coding/knowledge/decision/2026-08-27-browser-tools-visible-in-every-workflow-state-ap.md`) are in 4dc4a59, and the commit message explicitly notes the deliberate inclusion ("L5: the two untracked knowledge files from the earlier browser-tools plan (bb8a0ca9) are deliberately included here — mergeable truth files its closing commit missed.") — the sanctioned alternative from round-1. Content of both files is coherent truth-file front matter pointing at the already-merged bb8a0ca9 work.
- **L6 — misleading title GONE, duplicate left behind** (see finding 1 below). "a zero-window call degenerates to immediate hide" no longer exists; the corrected title + comment is present — but so is a near-identical sibling asserting the same thing.

## End-to-end re-verification (fixes broke nothing)

- **Cold-start no-flash (already-indexed project):** `switched_open=false` → `mark(false)` → snapshot inactive for the whole gated sub-second pass (no forwarded tick ever calls `ensure_startup_index_visible`) → `get_index_progress` → `None` → the seed bails on `snap === null` (IndexingOverlay.tsx:166). The lone terminal `Done` (H1) either lands pre-subscribe (lost, harmless) or post-subscribe and folds: `applyIndexProgress(null, done)` → `null`, pinned by "done is idempotent from the hidden state" (indexingOverlay.test.ts:72–74). No flash, no wedge. A **slow** (>1s) cold pass still publishes exactly when its stream becomes visible (first forwarded tick) — catch-up tracks the pre-existing 1s-gate semantics, no regression.
- **Switch path:** marker consumed → `switched_open=true` (main.rs:958–975) → `mark(true)` publishes `Some((0,0))` immediately + `Started` emitted up front + `forwarded=true` → overlay shows immediately (event or seed, whichever lands first; the seed sets `shownAt` at 170 so the hold window is anchored). Done always emitted at pass end → ≥800ms hold from `shownAt` via `hideDelayMs` → hide. A **failed** switch pass emits `Failed` (forwarded started true) → failure card + Dismiss (IndexingOverlay.tsx:210–224). An overlay that mounts after everything gets `None` (cleared before the terminal emit, main.rs:1548–1551) → no stale seed.
- **Token-guard × hold-timer interaction:** any event cancels a pending hide (`clearTimeout` at 121–124) and re-checks staleness inside the timer callback (131). A seed can never land while a hide is pending: a pending hide implies a `done` was handled, which bumped `token` past every earlier-captured `myToken`. A `progress`/`failed` arriving mid-hold correctly cancels the hide and re-shows.
- **Hooks/cleanup:** `useState` (98) → `useBrowserOverlay(state !== null)` unconditional (104) → `useEffect` (106); the early return sits after all hooks (183). Cleanup disposes, unlistens, and clears the timer (176–180); both promises have `disposed` guards (149–152, 166). No hook-order or stale-closure issue.
- **Test-matrix claims (1564 root / src-tauri build clean / 643 frontend + tsc clean):** not re-run (read-only). Nothing in the code contradicts them: the 4-arg `startup_should_forward` signature is consistent at every call site including the gate table (610–632); all new helpers/statics are used (no dead code that `#![deny(warnings)]` would reject); TS exports/imports all consumed. (Note: the project's FTS content index predates this commit, so `search` cannot see the new code — the reviewed `git show` diff is the authoritative change set, and it is complete.)
- **Constitution:** doc comments present on every new/changed public item — `mark_startup_index_active(switched)` (356–372), `ensure_startup_index_visible` (379–383), `record`/`clear`/`snapshot` (388–405), `IndexProgressSnapshot` + fields (421–438), `get_index_progress` (440–446), `startup_should_forward` switched semantics (320–335), the terminal-block comment (1548–1561), and TS `IndexProgressSnapshot`/`getIndexProgress`/`MIN_VISIBLE_MS`/`hideDelayMs`/component doc. No `#[allow]` anywhere. README:52 now says "a sub-second pass emits nothing visible" — wire-accurate under always-Done, exactly the rewording round-1's H1 note requested. The SPEC successor (`.coding/knowledge/spec/2026-08-28-indexing-progress-overlay-on-project-open-bar-co.md`) matches the code on all points (mark(switched) publish, first-forwarded-tick publish, Done ALWAYS / Failed forwarded-gated, token guard, clamp, MIN_VISIBLE_MS=800) and the -3 file carries `status = "superseded"`. Multi-platform neutral (`std::sync::atomic`, `std::time`, `window.setTimeout/clearTimeout`). Security unchanged: `get_index_progress` is a read-only counter getter.

## Findings

### LOW 1 — Duplicate `hideDelayMs` test left behind by the L6 rename (indexingOverlay.test.ts:93–95 vs 107–111)

The suite contains BOTH `it("just shown now → hold the full window")` (93–95) and `it("shown exactly now → hold the full window", …)` (107–111, the commented one the fix claim describes). Identical assertion (`hideDelayMs(1000, 1000) === MIN`), titles differing by one word. The claimed "rename" instead added the corrected test alongside an already-retitled sibling. Cosmetic — no behavioral impact — but it is precisely the redundancy a rename was supposed to remove, and the near-identical titles invite future confusion. **Fix:** delete the uncommented `it("just shown now → hold the full window")` block (keep 107���111). Trivial accompanying nit: the file ends without a trailing newline (no prettier/lint gate exists in `frontend/package.json`, so nothing enforces it — restore the newline while touching the file).

### LOW 2 — L4's clamp is implemented but unpinned; the claimed regression case does not exist (codegraph_cmds.rs:415 vs test 644–676)

The round-2 claim states the clamp is "pinned by the clamp case in startup_snapshot_records_and_clears (records (600,500) → Some((500,500)))". No such case exists — the test records only (12,500), (150,500), (200,500), never done>total. The clamp itself (`Some((done.min(total), total))`) and its comment are in place, which is what round-1's L4 required ("clamping … or documenting" — both were done), so the original finding is resolved in substance. But the clamp is now regression-unpinned: a future edit removing `done.min(total)` keeps the entire matrix green, and the round-2 claim overstates the shipped coverage. **Fix:** add two lines inside `startup_snapshot_records_and_clears` (e.g. after line 668): `record_startup_index_progress(600, 500); assert_eq!(startup_index_snapshot(), Some((500, 500)));` — under a torn/erroneous read this pins the clamp exactly as claimed.

## Observations (no action required)

- Working tree: `.coding/plans/eedeaeab.md` step-5 checkbox tick — expected post-commit bookkeeping, not part of the reviewed change.
- The SPEC successor filename uses the `2026-08-28-…` date prefix rather than the literal "-4" suffix sketched in plan step 4; the `supersedes` front-matter chain (-3 → successor) is intact, which is what the knowledge system keys on.
- `startup_snapshot_records_and_clears` mutates module statics — safe today because it is the only test touching them; worth remembering if a parallel test ever joins that module.

## Verdict recap

H1, H2, H3, L5: verified fixed exactly as claimed. L4: fix present, claimed test pin missing (LOW 2). L6: misleading title removed, duplicate test left behind (LOW 1). No new behavioral, race, wedge, security, platform-neutrality, or constitution issues introduced. Fix the two LOWs (a few lines in `indexingOverlay.test.ts` + `codegraph_cmds.rs`), re-run the matrix, and this lands.
