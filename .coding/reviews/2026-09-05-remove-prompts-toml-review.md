## Verdict: FINDINGS (0 high, 1 low)

# Review: plan 55135f00 — Remove prompts.toml override machinery (branch wt/remove-prompts-toml)

Scope reviewed: `git diff HEAD` on branch `wt/remove-prompts-toml` — all 10 modified files (src/agent/prompt.rs, src/agent/loop_impl.rs, src/agent/factory.rs, src/agent/turn.rs, src-tauri/src/main.rs, src-tauri/src/console.rs, src-tauri/src/ipc/config_io.rs, tests/workflow_integration.rs, README.md, PLAN.md) plus the untracked `.coding/tmp-user-prompts.toml`. Verified: no dangling references to any deleted symbol (searched `PromptBlocks|PromptSection|WorkflowStateBlocks|PromptSource|PromptHolder|apply_tool_descriptors|default_prompts_toml|ensure_prompts_file|tool_descriptor_map|with_prompt_source` across all `*.rs` — zero matches), all factory/ctor and prompt-builder call sites updated, cache-stability invariants intact, docs synced, multi-platform neutral, warning-free per the reported green builds (trusted per review instructions).

## Findings

### L1 — `.coding/tmp-user-prompts.toml` still present; plan summary claims it was deleted

- **File:** `.coding/tmp-user-prompts.toml` (untracked, 89 lines)
- The plan's stated scope says "Also deleted the stray untracked .coding/tmp-user-prompts.toml (pre-existing test artifact, unrelated)". `git status --short` shows it is **still on disk and untracked**. It is not gitignored (it appears as `??`), so a `git add -A` closing commit would sweep this stale artifact into the mergeable `.coding/` side-car — it carries obsolete v1-era prompt text (e.g. `memory_recall` / `context_pack` tool names, "final answers short lines bullet points" summary rule) that contradicts the current compiled constants and would confuse any instance that later reads it.
- **Recommendation:** delete the file (`Remove-Item .coding/tmp-user-prompts.toml`) before the closing commit — or, if kept deliberately, gitignore it and adjust the plan summary. It must not be committed.
- No runtime impact (nothing reads it), hence low severity — hygiene/consistency only.

## Checks performed (all clean)

1. **Correctness / dangling references** — Zero matches for every deleted symbol across the whole `*.rs` tree (128 files searched). All 5 `AgentLoopFactory::new` call sites (src/agent/factory.rs:1059, src-tauri/src/main.rs:1483, src-tauri/src/console.rs:2239, src-tauri/src/ipc/config_io.rs:502, tests/workflow_integration.rs:401) dropped the `PromptSource` arg consistently. `turn.rs:508-521` builds head/tail with the new signatures and the `apply_tool_descriptors` call + `prompt_blocks` fetch are gone (schema build at turn.rs:614-628 retains only the workflow-filter retain). `loop_impl.rs` `prompts` field / `PromptHolder` / `with_prompt_source` removed from both constructors; `ConstitutionHolder` untouched.
2. **Cache-stability invariants** — `CONTEXT_FOOTER` literal unchanged (prompt.rs:271). The `STATE_*` consts' string bodies are NOT in the diff (only the doc comments above them changed), and `workflow_section` feeds them through the same `push_state_text` normalizer as before, so volatile-tail output is byte-identical to the former `WorkflowStateBlocks::default()` path. `stable_head_byte_stable_across_workflow_changes`, `context_footer_is_stable_literal`, and the head/tail containment tests all remain and now call the simplified signatures.
3. **Docs sync (project constitution)** — README.md Configuration list no longer names `prompts.toml`; the added sentence ("compiled constants — not configurable per install") matches the new behavior. PLAN.md section replaced with "System prompts (compiled constants)" that accurately describes the compiled-only design. prompt.rs module doc rewritten (lines 5-12) and the per-const doc comments no longer claim overridability. Remaining `prompts.toml` mentions live only in `.coding/plans/` and `.coding/reviews/` historical records — correct to leave untouched. No doc-comment links to removed items (e.g. `[WorkflowStateBlocks]` references) remain.
4. **Security** — pure deletion; no new input surface, no new fs/toml parsing, no credential-adjacent changes. The removed `toml::` usage lives on in `config/{endpoints,general,keys}.rs` (its legitimate users), so no dead-dependency issue.
5. **Multi-platform neutrality** — nothing OS-specific added; the removed code was itself portable; `use std::path::Path;` correctly dropped from prompt.rs. The deletion is neutral on macOS/Windows.
6. **Tests** — 15 prompts.toml tests + the `write_until_mtime_advances` helper deleted from prompt.rs (matches "16 deleted"), `tool_descriptor_map_contains_core_tools` deleted from factory.rs, ~25 call sites in the surviving prompt.rs tests updated to the simplified API, and the workflow-state guidance behavior is still covered implicitly by the surviving default-state tests. Green at both crate roots per the task statement (deny(warnings) ⇒ zero warnings, so no unused-import risk remains).
