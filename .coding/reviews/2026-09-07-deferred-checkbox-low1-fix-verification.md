## Verdict: PASS

Verification re-review (second pass) for the plan `Deferred checkbox — exclude backlog items from Run-All` (backlog f2d2809b), branch `wt/agenticcoding`. Scope: the LOW 1 fix from the prior review (`.coding/reviews/2026-09-07-deferred-checkbox-run-all-review.md`) — a doc-only rewording of the `run_all.rs` module doc — as committed in **c009a46** (HEAD; `git diff HEAD` and `git status --short` are both empty, so the reviewed state IS the commit). The fix is correct and complete, nothing else in the commit changed or broke, and no new findings emerged.

### 1. LOW 1 fix — verified correct and complete

The reworded module doc (src-tauri/src/ipc/run_all.rs:12-22) drops both overstated claims from the prior finding — the false `adopted` leg of `NEVER selected, adopted, or counted` and the `a deferred item stays Pending` simplification — and every sentence of the new paragraph matches the shipped behavior, checked claim-by-claim against the code:

- **`never selected ... by a run`** — `run_all_dispatch_next` selects via `next_pending_eligible()` (run_all.rs:1620), which filters `Pending && is_live && !deferred` (src/backlog.rs:655-660).
- **`the run's total counts only non-deferred pending items`** — `backlog_run_all` counts with `i.status == BacklogStatus::Pending && !i.deferred` (backlog_cmds.rs:458-461).
- **`a deferred item stays visible in the Backlog tab (reserved for manual in-app handling or later re-inclusion)`** — `items()` filters only soft-deleted items (src/backlog.rs:721-723); the UI renders the checkbox + violet DEFERRED badge (prior review's frontend verification).
- **`When only deferred items remain, the run ends cleanly via end_run — no spin, no error`** — an all-deferred backlog yields total 0 but pending items exist, so the start error — reserved for a backlog with no pending items at all (backlog_cmds.rs:462-463) — does not fire; the run state is created and the first `run_all_dispatch_next` gets `None` from `next_pending_eligible()` and takes the `end_run` arm (run_all.rs:1621-1625). No spin: `run_all_dispatch_next` runs exactly once from `backlog_run_all`; later invocations are event-driven from resolved turns, and an all-deferred run never starts a turn.
- **`Deferral is orthogonal to status (the flag never changes it)`** — `set_deferred` mutates only `item.deferred` (src/backlog.rs:668-676).
- **`auto-feed and the per-item ▶ button still dispatch deferred items`** — `dispatch_next_impl` (auto-feed + dispatch-next button) selects via the unfiltered `next_pending()` (backlog_cmds.rs:343); `dispatch_item_impl` (the ▶ button) via the unfiltered `pending_item(id)` (backlog_cmds.rs:358).
- **`orphan adoption still requeues a deferred orphan to Pending — the flag survives the requeue and then excludes the item from selection (skipping adoption would strand it InFlight with no recovery)`** — `adopt_orphaned_in_flight` (run_all.rs:2370-2406) filters only InFlight items that are neither the single-dispatch pointer nor the main agent's active root plan — no deferred filter — and requeues via `store.transition(&id, Pending, ...)`; `transition` mutates only `status` + `note` (src/backlog.rs:494-495), so the flag survives and `next_pending_eligible` then excludes the item from selection. The stranding rationale matches adoption's own doc (run_all.rs:2352-2358: dispatch only considers `Pending`, the guarded requeue refuses `InFlight`).

The applied wording matches the prior review's prescribed fix exactly (`never selected or counted` + the adoption note). Docs are in sync overall: the README Backlog paragraph (README.md:78) already carried the accurate phrasing (`never selects or counts it ... the ▶ button still dispatches it ... when only deferred items remain, the run ends cleanly`), and the `run_all_dispatch_next` doc comment (run_all.rs:1598-1612) was already accurate per the prior review.

### 2. Nothing else broke — doc-only fix confirmed

- **Commit state**: c009a46 is HEAD and the working tree is clean — the state reviewed here is exactly the committed state.
- **Doc-only corroboration (line-shift arithmetic)**: every run_all.rs landmark the prior review cited sits exactly +3 lines below its prior-review position — `run_all_dispatch_next` 1610→1613, the eligible-selection call 1617→1620, the `end_run` arm 1618-1622→1621-1625, `drain_run_all_on_main_exit` 2302→2305, `adopt_orphaned_in_flight` 2367→2370 — precisely the net +3-line growth of the reworded module-header paragraph (now lines 12-22). Landmarks in the untouched files are stable (backlog_cmds.rs: the `next_pending()` call 343, `backlog_run_all` 437, the adopt call 450; src/backlog.rs: `next_pending` 640, `next_pending_eligible` 655, `pending_item` 683). No code moved.
- **Source-contract tests are immune to the doc change**: `run_all_dispatch_next_selects_via_next_pending_eligible` (run_all.rs:341-363) slices the source from `rfind("pub(crate) async fn run_all_dispatch_next")` to the next `\\n}\\n` — the module doc (lines 12-22) lies far outside the sliced body, and the `!body.contains("next_pending()")` assertion still holds (the in-body comment at run_all.rs:1617-1619 names `next_pending`/`pending_item` without parens). The backlog_cmds.rs contracts — `backlog_run_all_counts_only_eligible_items` (backlog_cmds.rs:709-721, pins the `Pending && !i.deferred` total filter) and `backlog_set_deferred_routes_through_the_store_setter` (backlog_cmds.rs:683-695) — live in a file the fix did not touch.
- **Store regression tests present**: `next_pending_eligible_skips_deferred_items` (src/backlog.rs:1921), `deferral_survives_status_transitions_and_requeue` (1971), `deferred_flag_round_trips_and_is_omitted_when_false` (1883), `old_lines_without_deferred_parse_as_false` (1909), `set_deferred_unknown_or_deleted_id_is_noop` (1961).
- **Test status**: reported green after the fix (src-tauri 243+4+2; lib 2079+16; vitest 1021). As a read-only reviewer I cannot execute the suite; the structural analysis above is consistent with that result — a `//!` paragraph cannot change compilation or behavior, and the contracts are content-anchored, not line-anchored.

### Non-finding (observation, no action required)

The new paragraph's doc link `crate::backlog::BacklogStore::next_pending_eligible` (run_all.rs:15) uses a `crate::` path that does not resolve in the src-tauri crate — the store lives in the `mnemo` lib, and src-tauri's root declares no `backlog` module or `use mnemo::backlog` (main.rs:32-35). Not a finding: (a) the identical pattern `crate::backlog::BacklogItem::deferred` at run_all.rs:1604 shipped in the same commit's feature portion and was examined — and called accurate — by the prior review without comment, so the fix followed the file's established style; (b) it affects no project gate — rustc never resolves intra-doc links (cargo test / `deny(warnings)` unaffected), and the private `ipc` module means even a default `cargo doc` never documents it; the link text remains human-readable. If a future docs pass wants it, the resolvable form is `mnemo::backlog::...` (both occurrences). Similarly, the plan file's goal line (.coding/plans/803d2be0.md) retains the original `selects, adopts, or counts` shorthand — plan files are point-in-time records, not shipped docs, and the plan body's adoption rationale was always correct.

### Conclusion

LOW 1 is fixed correctly and completely; the fix is doc-only and broke nothing else in c009a46. The change set is ready.
