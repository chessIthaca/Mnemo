## Verdict: PASS

Round-2 verification of commit 1fd143f (HEAD on wt/agenticcoding; working tree clean — `git diff HEAD` and `git status` empty, so every file read reflects the commit) against the three LOW findings in `2026-09-07-graph-search-stale-index-review.md` (plan 55163f1e, backlog 95f21af0). All three findings are genuinely resolved; the fixes introduced no new issues.

### Finding verification

**LOW 1 — the re-resolve view failure no longer errors the lookup — RESOLVED** (src/tool/agent/codegraph.rs:328-339)

The `Some(n)` arm now reads `if let Ok(fresh) = graph.view() { matches = fresh.resolve(&args.query); }` — round-1's own recommended option (a), verbatim: a fresh-view failure keeps the (empty) matches, skips the re-resolve, and still sets `reindexed = Some(n)` so the side effect is disclosed (:366-369). The invariant holds end-to-end across the whole closure (:309-386): the ONLY error propagation is the initial `graph.view().map_err(|e| e.to_string())?` at :310 — identical to `run_query`'s pre-existing semantics (:63). Every freshness-repair error is swallowed: sweep `Err` by `if let Ok` (:316), reindex `Err` and `Ok(0)` by `.ok().filter(|n| *n > 0)` (:323-326), re-resolve view `Err` by `if let Ok` (:335). After the initial view the closure unconditionally reaches `Ok(out)` (:375) — no other `?`, no `return Err`, no panicking unwrap (`to_string_pretty` has an `unwrap_or_else` fallback at :380-381). The outer match's `Ok(Err(msg)) → ToolResult::error` arm (:384) is therefore reachable only from the initial view failure; `Err(e)` (:385) is the join/panic arm, byte-identical shaping to `run_query` (:74). The :305-307 header comment ("a lookup is never failed by its own repair") and the new :330-334 comment now agree with the code.

**LOW 2 — the unrepaired-staleness note test — RESOLVED** (`stale_above_the_reindex_cap_notes_instead_of_reindexing`, src/tool/agent/codegraph.rs:879-925)

The test genuinely pins the above-cap branch. Setup is exact: `indexed_graph()` creates only src/lib.rs + src/main.rs in an empty tempdir (in-memory DB — no stray files), the test adds extra0-6, `graph.index(None)` indexes all 9 source files, then bumps all 9 mtimes to now+5s via write-handle `set_modified` (the same cross-platform std technique as the regression test and the search tool's tests; 5s clears any filesystem timestamp granularity, and the stored mtimes were captured at index time, so the mismatch is deterministic — exactly 9 stale files, no more, no fewer). Failure modes checked:

- **Reindex-anyway** (cap check removed, inverted, or `STALE_REINDEX_CAP` raised to ≥9): the note becomes "reindexed 9 stale file(s) — serving fresh graph results" → the `contains("symbol index may be stale — 9 file(s) on disk are newer than the index")` assert (:919-922) AND the `!note.contains("reindexed")` assert (:923) both fail.
- **Note dropped**: `out["note"].as_str().expect(...)` (:918) fails.
- **Note string drift**: :919-922 fails — the expected string is exact (em-dash included) and matches the format string at :372.
- **Sweep mis-count** (8 or 10 stale): the "9 file(s)" assert fails.
- **Hint dropped on the unrepaired path**: :924 fails.

Together with the regression test (1 stale file → reindexed), the cap is bracketed and the above-cap behavior + note string are pinned. The busy-pass route (`Ok(0)`) into the same note-emission line (:370-373) remains untested directly — round-1 explicitly deemed the above-cap test sufficient ("One test (the above-cap one) would suffice"), both routes converge on the now-pinned emission line, and the `Ok(0)` semantics themselves remain pinned by `reindex_stale_files_busy_returns_zero_and_leaves_the_flag` (mod.rs:830). No gap.

**LOW 3 — the "unix seconds" doc drift — RESOLVED** (src/codegraph/mod.rs:180, src/codegraph/store.rs:619)

Both `stored_mtimes` doc comments now say "(unix millis)", and the values really are millis, verified across the whole chain: `mtime_of` (mod.rs:563-576) returns `as_millis() as i64` (with the explicit millisecond-resolution comment); `reindex_one` (:531 → :549) and both content-only arms (`index()` :327-334, `reindex_stale_files` :462-468) feed `mtime_of` into `upsert_file`, which inserts it as-is into `cg_files.mtime` (store.rs:227-231); `stored_mtimes` SELECTs it back unchanged (store.rs:623-632); `stale_source_files` compares `mtime_of` vs stored — millis-to-millis end to end, as does the M2 fast path (:278-287). No other living doc repeats the wrong unit: the repo-wide "unix seconds" hits are genuinely-seconds values (git author dates in tauri.ts:1477 / files.rs:1060 / gitLanes.ts:375, `now_secs()`/`indexed_at` bookkeeping in store.rs:50/:635 and its IPC/frontend mirrors, the steering age-gate in steering.rs:32/:165) or immutable historical records under .coding/ (reviews/plans/knowledge specs describing F10 as it shipped, when the value WAS seconds — the millis resolution arrived later with M2, per the 2026-12-16 remediation sweep that fixed the `mtime_of` doc). A scoped "seconds" sweep over src/codegraph/*.rs finds only correct usages.

### Whole-diff sanity check (git show 1fd143f) — no issues

- **`stale_source_files`** (mod.rs:500-516): the source filter lowercases the extension before `Lang::from_extension` — matching Lang's documented lowercase contract (pinned by walk.rs `lang_from_extension`: "TSX" → None, "caller lowercases first") and the identical call shapes in `index()` (:317) and `reindex_stale_files` (:453); the mtime comparison is like-for-like; vanished/unreadable → `mtime_of` 0 ≠ stored → flagged, exactly as the doc comment states. Stats only (one SELECT + one stat per indexed source file). Exactly one production caller (graph_search execute :316) — no blast radius beyond the intended tool.
- **`STALE_REINDEX_CAP`** (codegraph.rs:228-233): const 8, doc-commented, mirrors the search tool's cap; `<=` at :318 means 8 stale files repair, 9 note — as documented.
- **execute rewrite** (:291-387): spawn_blocking; outcome shaping mirrors `run_query` (:62-75) exactly; `reindexed`/`unrepaired_stale` mutually exclusive by construction (match arms :327-344), note emission checks `reindexed` first (:366-374); the no-staleness payload is byte-identical to the pre-change shape ({query, count, symbols} + hint on miss) — asserted by the unchanged pre-existing tests (`graph_search_unknown_name_returns_empty_not_error`, `graph_search_hit_carries_no_hint`), green in the reported matrix.
- **Module-doc paragraph** (:27-35): accurate — cap 8, single re-resolve, disclosure note, unrepaired-staleness note, brand-new-files residual.
- **Four tests** (:803-925): all present as committed; the regression test's RED-before-fix/GREEN-after is recorded in the plan file (step 1) with `stale_symbol_miss_reindexes_and_serves_fresh_results` in the plan's Regression test field; `fresh_miss_stays_fast` and `vanished_stale_file_degrades_gracefully` pin the no-false-positive and prune paths.
- **Bookkeeping hunks**: backlog.jsonl flips item 95f21af0 to in_flight with plan linkage + checkpoint sha (run-all bookkeeping, expected mid-plan); the new plan file records the regression test name. Both fine.

### New-issue scan — none

The LOW-1 fix only removes an early-return path (no new locking, no new panics); LOW-2 is test-only; LOW-3 is doc-only. One accepted nano-nit, not a finding: in the practically-unreachable window where the fresh view fails right after a successful reindex, the note still reads "serving fresh graph results" while the plain miss is served — this is precisely round-1's recommended fix shape, the side effect IS disclosed, and the window requires an SQLite read failure immediately after a successful write on the same process-local connection. Constitution: doc comments on all new items, no `#[allow]`, pure cross-platform std (`set_modified` is std), no `cfg(windows)`, README/PLAN.md correctly untouched (the note is documented in the module doc, same as the F10 sibling, which shipped no README change either). Incidental live confirmation during this review: the search tool's own F10 note fired mid-verification ("reindexed 8 stale file(s) — serving fresh index results") — the sibling pattern doing exactly what this change ports to the symbol index.

### Summary for the parent

All three round-1 findings are genuinely resolved in 1fd143f — LOW 1 via the recommended hardening (invariant now holds end-to-end: only the initial `view()` can error, as before the fix), LOW 2 via a test that truly pins the above-cap branch, exact note string, count, and hint survival, LOW 3 via the two doc corrections with the millis chain verified end to end and no remaining unit drift in living docs. No new issues; the committed diff is sound as a whole. Ready to finish.
