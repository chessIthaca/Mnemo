## Verdict: FINDINGS (0 high, 3 low)

The core fix is correct, minimal, and genuinely regression-tested: the Suggestion-arm streak reset closes the budget-starved closing-sequence park (the exact park the 2026-12-16 review predicted at `2026-12-16-auto-continuation-review.md:47`), the None-arm restructure preserves the gate and consolidation checkpoint exactly, the serde boundary round-trips, and the frontend banner's `!running` gate is sound because the interrupt path verifiably emits `Finished` first. All three findings are documentation/process hygiene, none block the commit.

**Scope reviewed:** full `git diff HEAD` on `wt/agenticcoding` (14 files, +878/−28) plus untracked `.coding/plans/494f9d12.md` — bug plan 494f9d12, backlog 5c33e945. Method: shipped-tree reads at every cited line, sender/reachability analysis for the Suggestion and Interrupt commands, serde-attribute verification, frontend reducer/selector trace, memory-store searches, and `git show 602c88f8`. Test results relied on as reported (read-only reviewer); the vitest registration of both modified test files was independently confirmed.

### Findings

**L1 (low, doc accuracy) — `AgentEvent::Parked` doc claims "NOT emitted for subagents", but the Interrupt arm emits for any agent.**
`src/runtime/channels.rs:437-438` states flatly: "NOT emitted for subagents (their routine turn-end parks would flood the ring)." The None arm honors that (emission gated on `!subagent`, agent.rs:342). The Interrupt arm (agent.rs:254-273) does not — it emits unconditionally, and it is reachable for a subagent: the `interrupt` IPC command (`src-tauri/src/ipc/agent.rs:341`) takes any `agent_id`, and the UI Stop button targets the active agent, which can be a subagent tab. A subagent's turn folds `StopReason::Interrupt` → the Interrupt arm → `Parked{Interrupted}` for the subagent. Impact is benign (one ring note per user-initiated stop — not flooding; the banner only reads the active agent, and "interrupted" is arguably the correct label for a stopped subagent), but this doc is the contract the ring/UI consumers read, and the 2026-12-31 review history shows flat claims in exactly this spot get relied on. **Fix:** qualify the comment — e.g. "NOT emitted at subagents' routine turn-end parks (they would flood the ring); a user Stop on a subagent still emits" — or gate the Interrupt-arm emission on `!is_subagent()` if the flat claim is the intended contract.

**L2 (low, knowledge hygiene) — the HOW record rewrite dropped still-current procedural content and now mismatches its title.**
`.coding/knowledge/how/2026-08-31-re-add-dropped-auto-continue-budget-test-via-sma.md`. The new body is accurate (verified against the code: both tests exist, plateau math 13/14/15, reset sites = Prompt + Suggestion arms, workflow setup via `create_plan_with_kind(Implementation)` + `complete_step(0)`), and the update itself was warranted — the old "Fresh Prompt resets auto_continue_streak=0" line went stale the moment the Suggestion-arm reset landed. But the rewrite discarded three still-load-bearing sections: (1) the small-increment `file_edit` methodology that the record's TITLE names as its subject (the new body never mentions it); (2) the deadlock-prevention drain idiom (drain `fanin_rx` inside every wait-loop iteration; `select!`-raced cleanup) — the new tests in this very diff use that cleanup verbatim; (3) the dual-handle Arc idiom (inline `Arc::clone` at the provider field position to avoid E0308) — also used verbatim by the new tests. It also dropped the None-arm gate description (`streak < MAX && workflow_expects_progress && !has_running_descendants && !is_subagent`) that the 2026-12-31 round-3/round-4 reviews specifically policed this record for. **Fix:** restore the dropped sections from git history (they remain true) alongside the updated reset-sites paragraph, or retitle the record to match its new role as the budget-tests reference.

**L3 (low, process) — the BUG memory record for this fix is absent from the memory store, but plan step 2 is checked complete.**
Plan 494f9d12 step 2 ("memory_write a BUG: record") is `[x]`, and the task description claims "(3) … documented (BUG memory + HOW record + queued backlog item)". Three memory searches (two `record_type:"bug"`, one unfiltered, including one on the exact regression-test name) surface only the prior auto-continue bug records (6f2f4c9c, e07dd82a, 3602e360, …) — none covering the Suggestion-arm streak reset / Parked event. The root cause IS documented in code comments (agent.rs:732-741) and the HOW record, and the app auto-captures a BUG memory at finish, so the gap self-heals — but a checked plan step should reflect reality. **Fix:** `memory_write` the BUG record (symptom → root cause → fix + regression test name `suggestion_resets_auto_continue_budget_after_exhaustion`) before finish, or re-verify it exists under wording the searches missed.

### Requested checks — results

**(a) None-arm restructure: PASS.** The gate is semantically identical: `streak < MAX_AUTO_CONTINUE && expects_progress && !descendants_running && !subagent`, with the three probe values hoisted above the `if` (agent.rs:274-302). The only behavioral delta is that the three probes now always execute where the old `&&` chain short-circuited (e.g. `workflow_expects_progress()` was skipped once `streak >= MAX`) — all three are pure async lock reads with no side effects, so no semantic change. The consolidation checkpoint (`streak % CONSOLIDATE_EVERY_N_TURNS == 0` → `spawn_consolidation`) is unchanged inside the auto-continue branch; `MAX_AUTO_CONTINUE = 12` and `CONSOLIDATE_EVERY_N_TURNS = 8` confirmed at agent.rs:65/70. Reason precedence is sound: running descendants → `WaitingForDescendants` (the designed wait dominates), else `!expects_progress` → `NoWorkExpected`, else `BudgetExhausted` — and the budget-exhausted-with-no-work case correctly reports `NoWorkExpected`.

**(b) Subagent exclusion + side-effect-free: PASS with L1.** The None arm gates emission on `!subagent`; the Interrupt arm does not (L1). Emission cannot change park/resume semantics: both sites are `let _ = fanin_tx.send(...).await` immediately before an unchanged `return AfterTurn::Continue` — the same send pattern every other event in this file uses, drained continuously by the forwarder. The Parked event rides the forwarder's generic structural-event path (events.rs:416-430: `into_serializable` → `watchdog_note` → `emit_payload`) with no resolution/latch interaction — it arrives after `Finished` and triggers nothing.

**(c) Serde boundary: PASS.** `#[serde(tag = "kind", rename_all = "snake_case")]` on `SerializableAgentEvent` (channels.rs:492-493) and `#[serde(rename_all = "snake_case")]` on `ParkReason` → `"interrupted"` / `"budget_exhausted"` / `"waiting_for_descendants"` / `"no_work_expected"`, matching the TS union in `types.ts`/`agentState.ts` exactly. `into_serializable` preserves all four fields; the round-trip test asserts the wire shape (`"kind":"parked"`, snake_case reasons).

**(d) Frontend reducer: PASS.** `reduceParked` sets `parked` with all four fields; cleared on `started` (`reduceStarted`) and on conversation clear (`useAgentStore`); banner gated `!running && parkedNeedsInput && parked !== null` with `parkedNeedsInput` = interrupted|budget_exhausted only. The critical ordering was verified end-to-end: the interrupt path emits `Finished` (turn.rs:378-400 hard-signal early return; the tool-batch interrupt path breaks and finishes the same way; ipc/agent.rs:336-338 documents "the interrupt's soft-stop `Finished` resolution"), so `running` flips false before `Parked` lands — the banner can actually show. `useAgentEvents.ts` dispatches every event kind to `handleAgentEvent` (no allow-list), so the new case is reachable in the live path, not just in tests.

**(e) Documentation sync: PASS with L2.** Field docs are thoroughly updated (AgentTask field doc, Prompt/Suggestion-arm comments, ParkReason variants, both event enums, ParkedInfo, AgentState.parked, `workflow_state_label`). PLAN.md's only auto-continue mention (line 518, Subagent state) remains accurate. README documents neither auto-continue nor InputBar affordances — the `failed`-state Continue button set the precedent (internal mechanism → doc comments only, per plan bbd712c9's explicit decision), so no README update is required. The HOW record is L2.

**(f) Multi-platform neutrality: PASS.** Pure Rust/TS logic — no paths, no `cfg(windows)`, no shell syntax, no platform assumptions anywhere in the diff.

**(g) Bug-plan checks: PASS with L3.** The regression test genuinely exercises the changed path: Reviewing setup asserted in-test, Prompt → plateau 13 calls (MAX+1), phase-2 asserts the park (300 ms, calls == cap), then Suggestion → expects cap+2 = 15. Pre-fix red: the Suggestion arm doesn't reset, the Suggestion turn is call 14, the None-arm gate fails on `12 < 12`, the agent parks, 15 never arrives → deadline panic. Post-fix green: reset → call 14 → auto-continue → call 15. The root cause is documented in the Suggestion-arm comment (agent.rs:732-741) and the field doc. The BUG memory is L3.

### Verified in detail (no action needed)

- **Emission tests** (`interrupted_turn_emits_parked_event`, `budget_exhaustion_emits_parked_event`, `descendant_park_emits_waiting_for_descendants`): all three assert the full field set; the interrupt test is race-safe because the post-turn drain folds the queued Interrupt before the stop-reason match, so `streak == 0` holds on both fold paths (mid-turn select or post-turn drain).
- **Suggestion-reset safety** (could an automated sender defeat MAX_AUTO_CONTINUE?): production Suggestion senders are the user steer (ipc/agent.rs:302), the child-completion notification (events.rs:887 — one per child), and the console steer (console.rs:783) — all bounded, external, one-per-event; the run-all RUN_ALL_STEER-as-Suggestion dispatch was removed by fe3c6c4c, and approval.rs:663 is a test. The budget's purpose (break genuine deadlocks) is unaffected: deadlocks send no Suggestions.
- **Watchdog/console plumbing:** the `watchdog_note` Parked arm's format string matches both new tests and the quoted format in backlog item a6a7727a; `render_event` renders it for the CLI console.
- **Vitest registration:** both modified test files are in `vitest.config.ts`'s explicit include list (InputBar.test.ts :31, useAgentStore.test.ts :42) — the new tests actually run; no new test files were added, so backlog 263313de's trap does not apply.
- **.coding accuracy:** the 5c33e945 note is machine-stamped verbatim from `run_all.rs:256` ("plan loop did not close (workflow: {ws}) — item left in_flight; work kept in tree; no further items dispatched") with the checkpoint sha prefix (602c88f8 = HEAD, verified via `git show`) — harness bookkeeping, accurate at stamp time; the current `pending` status is the later requeue. New item a6a7727a is accurate: `workflow_expects_progress` = Executing|Reviewing only (loop_impl.rs:1835-1841), the NoWorkExpected reason and ring-note format match the shipped code, and the rationale for not auto-fixing (covering Planning would self-answer text question-waits) is sound. The untracked plan file `.coding/plans/494f9d12.md` is accurate and must be committed with the change.
- **Security/perf:** the Parked payload is fixed-vocabulary evidence (enum labels, bool, u32) — no user input, no injection surface; one event + one ring note per park, negligible cost.
