## Verdict: FINDINGS (0 high, 1 low)

Round-2 verification of fcc1010 (wt/agenticcoding, clean tree): both round-1 findings are correctly and completely fixed — LOW-1 (both knowledge-spec amendments present, accurate, and consistent with the code) and LOW-2 (doc comment on the public `commits_after_checkpoint` wrapper) — and the committed change re-verifies clean on correctness, async lock discipline, the MockGit sha mapping, multi-platform neutrality, and security. One new LOW documentation-sync finding the round-1 sweep missed: the README backlog paragraph still describes the exit-drain/adoption requeue without the landed-work exception.

### Findings

**LOW-3 (new) — stale README sentence (documentation sync).** README.md:79 (the extended Backlog-tab line — the user-facing contract doc per plan 8c2edf0f, and one of the three surfaces the resolution-contract spec itself names as normative: "documented in the run_all.rs module doc … + README backlog paragraph + pinned by tests") still reads: "a main-agent exit or hard crash requeues the orphaned in-flight item to Pending (no turn resolution ever comes for it — the exit drain and the run-start adoption sweep)". fcc1010 changed exactly that behavior: both named mechanisms now first consult `orphan_work_landed` and auto-resolve a landed orphan (plan file all steps checked AND ≥1 commit after the pre-item checkpoint sha) to `Done`. Every other surface describing this behavior was updated — the run_all.rs module doc ("UNLESS the work already landed…"), both function doc comments, and both knowledge specs (the LOW-1 fix) — the README is the one missed. A user reading it would expect a crashed-after-commit session's item to return to the queue and be re-executed, when it now lands `Done`. Fix: one clause, e.g. "…requeues the orphaned in-flight item to Pending — unless the work already landed (plan complete + commits after the pre-item checkpoint), which auto-resolves done (the exit drain and the run-start adoption sweep; no turn resolution ever comes for it)…". No code change.

### Round-1 finding verification

**LOW-1 (stale lifecycle specs) — FIXED, verified.**
- `.coding/knowledge/spec/2027-01-07-backlog-item-status-plan-lifecycle-failed-means.md:22` now carries the amendment paragraph "Amended 2027-01-07 (the done-orphan guard, backlog 6c6966b9, plan 5b988455)" — matching the file's established bottom-amendment convention (the 5f6515f5 and 96e2862a amendments above it; the body text stays historical, superseded by the amendments, exactly as round-1 prescribed). Every clause cross-checks against the code: "consult landed-work evidence BEFORE requeueing" (both sites call `orphan_work_landed` before the transition arms); "plan file shows all steps checked" (`plan_steps_all_done`: `PlanFile::parse`, `!steps.is_empty() && all(.done)`); "≥1 commit exists on the branch after the pre-item checkpoint sha (the note head)" (`extract_checkpoint_sha` on the note → `commits_after_checkpoint`); "auto-resolves `Done` with an explanatory note (checkpoint sha preserved)" (transition note `"{note} | suffix"`, sha at the head); "unverifiable evidence (no plan id, missing/incomplete plan, no sha, git error) still requeues to `Pending`" (every false path falls to the Pending arm).
- `.coding/knowledge/spec/2027-01-07-backlog-resolution-contract-finished-terminal-cl.md` — both requested touches present: the resolution-path sentence now carries "— unless the work already landed (the done-orphan guard, 6c6966b9: plan complete + commits after the checkpoint → auto-resolve Done)", and the scope-boundary sentence was flipped from "is a SEPARATE task" to "was carved out as a separate task and has since LANDED (plan 5b988455): the drain/adoption sweep auto-resolves landed orphans to Done"; plus a bottom amendment paragraph (line 10) mirroring the lifecycle spec's, with a cross-reference pointer. Accurate and consistent with the code and with the lifecycle spec.

**LOW-2 (missing doc comment) — FIXED, verified.** `pub async fn commits_after_checkpoint` (src/project/git_ops.rs:250) now carries a doc comment (lines 243-249): semantics summary, a `[`commits_after_checkpoint_impl`]` link for the rationale, and the spawn-blocking / `'static + Send` note — exactly the shape round-1 suggested, mirroring the pre-existing `rollback`/`commit_success` wrapper convention. The impl's own thorough doc is unchanged.

### Re-verification of the committed change (fcc1010)

Scope: `git show fcc1010` (full diff, 8 files, +508/−39) + the current files re-read at the committed line ranges + the round-1 report. Working tree vs fcc1010: only `.coding/plans/5b988455.md`'s step-4 checkbox flip is uncommitted (the parent plan's own bookkeeping — not part of the reviewed change; all source and knowledge files match the commit exactly).

- **Code delta since round-1's review = the LOW-2 doc comment only** (a comment; zero behavioral change) plus `.coding` files — so round-1's code verification carries over. I re-confirmed the load-bearing sections at the committed state: `orphan_work_landed` (run_all.rs:2718), `plan_steps_all_done` (:2681), the drain (:2761), the adoption (:2860), `commits_after_checkpoint[_impl]` (git_ops.rs:222-257), the MockGit log arm (:982-1013), and all four new tests.
- **Correctness.** The guard's conjunction (plan gate AND commit gate) matches the died-between-commit-and-finish window; every unverifiable path returns false → the Pending arm (today's behavior, the safe default). The drain's new `still_in_flight` re-verify is a strict improvement over pre-change: the old code transitioned unconditionally, so a turn resolving concurrently with the agent exit could flip an already-Done item Done→Pending (a legal requeue) — now guarded. The adoption's Phase 3 re-verify (still InFlight + still not the single-dispatch pointer + still not the active root plan, with fresh `main_agent_top_plan_id` and pointer reads) covers the unlocked evidence window. `InFlight → Done` is a legal guarded transition.
- **Async lock discipline (review check).** No backlog store lock is held across the git subprocess anywhere: the drain snapshots `(note, plan_id)` in a block-scoped store-lock guard, drops it, awaits `orphan_work_landed`, then re-takes the lock and re-verifies still-InFlight before transitioning; the adoption is 3-phase (snapshot under the lock → evidence with no locks → re-verify + transition under the lock). Inside `orphan_work_landed` the project-root lock is held only for `root.clone()` (the guard temporary drops at the end of the `let`), and the git subprocess runs via `spawn_blocking` with zero locks. `main_agent_top_plan_id` (manager/agent_loops/workflow locks) is taken before the store lock in both phases — same ordering as pre-change, no new lock-ordering edge. Residual TOCTOU windows (a single-pointer set mid-loop; a manual resume during Phase 3) are unchanged from pre-change and benign.
- **MockGit log arm vs commit arm (review check).** Verified against the actual arms: the commit arm does `commits.push(msg); head_sha = format!("sha{}", commits.len())` and `MockGit::new()` starts with one initial commit at head `abc123` — so a commit at index k gets `sha{k+1}`, "shaN" is commits[N-1], and the log arm's `start = N → commits[N..]` is exactly the commits after shaN; the generated line shas (`sha{start+i+1}`) match the arm's assignment; `sha1` and out-of-range shas → "unknown revision" (matching real git); slice bounds are guaranteed by the `2 ≤ N ≤ len` filter. The unit test's matrix (abc123 → false on a clean tree; commit → head `sha2`; `abc123..HEAD` → true; `sha2..HEAD` → false; `deadbee` → Err) is consistent with the mapping. (The mapping comment's parenthetical "(head is 'sha{len}' after the push)" reads with len = post-push length — the arm's invariant `head = sha{commits.len()}`; the operative "shaN is commits[N-1]" clause is correct.)
- **Regression tests.** Both source contracts (`drain_run_all_on_main_exit_consults_landed_evidence`, `adopt_orphaned_in_flight_consults_landed_evidence`, run_all.rs:450-560) are committed, rfind-anchored on the full signatures, asserting `orphan_work_landed(` + `BacklogStatus::Done` + `BacklogStatus::Pending` in both bodies — the pre-fix bodies contained none of the first two, so both fail pre-fix by construction. Unit tests `plan_steps_all_done_requires_every_step_checked` and `commits_after_checkpoint_impl_detects_landed_work` cover the pure logic and the git matrix. The pre-existing contract tests round-1 checked still anchor (code unchanged since).
- **Multi-platform neutrality.** Clean: `Path::join` for the plan path, discrete argv for git (no shell), no Windows-only APIs; the only `cfg(windows)` code (`CREATE_NO_WINDOW` in `git_raw`) is pre-existing; the test's `Path::new("/tmp/mock")` is the pre-existing mock-key idiom, never touching the filesystem.
- **Security.** No new attack surface: the sha is hex-gated (≥7 chars) and argv-discrete; `plan_id` only steers a file read whose parse failure → false → requeue (a boolean gate, no data returned).
- **Test status.** As reviewer I have no shell — validated by code reading. Per the task brief: plain `cargo test` (2084+16) and `cargo test -p mnemo-app` (250+4+2) pass; `cargo test --workspace` fails only at the pre-existing `agent::factory::tests::tools_array_stays_within_context_budget` (verified failing on the clean tree before this change) — not a regression, out of scope.
