## Verdict: FINDINGS (0 high, 1 low)

Round-3 final verification of the committed state (fcc1010 + d8b0401 on wt/agenticcoding, clean tree): LOW-3 is correctly fixed — the README backlog paragraph now carries the landed-work exception verbatim as prescribed, matching the code and every other surface — and round-1 LOW-1/LOW-2 remain fixed with zero source delta since the round-2-verified fcc1010 (d8b0401 touched only README.md + two `.coding` bookkeeping files). Correctness, lock discipline, security, and multi-platform neutrality all re-verify clean. One new LOW documentation-sync finding the prior rounds' sweeps missed: the run_all.rs module doc's "Status ⇄ plan lifecycle" section still states the dead-owner requeue unconditionally — the module-doc twin of the lifecycle-spec sentence LOW-1 amended.

### Findings

**LOW-4 (new) — stale dead-owner sentence in the module doc's lifecycle section (documentation sync).** src-tauri/src/ipc/run_all.rs:138-145 (the `//!` "Status ⇄ plan lifecycle (backlog 45dcf577)" section) still reads: "The one dead-owner exception: a MAIN-AGENT EXIT requeues the item to `Pending` (no turn resolution ever comes for it — run-all orphans, 2027-01-07; [`drain_run_all_on_main_exit`]), and run-all start adopts any `InFlight` orphan a hard crash left behind ([`adopt_orphaned_in_flight`]) — no item is ever left `InFlight` without a live owner." Both halves are now unqualified: with the guard, a landed orphan auto-resolves `Done` at BOTH sites instead of requeueing. This is the module-doc twin of the lifecycle-spec sentence LOW-1 amended (`.coding/knowledge/spec/2027-01-07-backlog-item-status-plan-lifecycle-failed-means.md` — its body carries the same unqualified requeue, superseded by the bottom amendment) — the spec and this section now diverge on the detail. It is also near-verbatim with the README sentence LOW-3 flagged and d8b0401 fixed. The module doc is internally inconsistent: the safety-model bullet (lines 38-45) describes the same drain WITH the exception ("UNLESS the work already landed… then it auto-resolves `Done` instead of requeueing for duplicate re-dispatch"), while the lifecycle section 100 lines later states it categorically. fcc1010's module-doc hunk touched only the safety bullet (verified in the diff — the lifecycle sentence, citing the 5f6515f5-era "run-all orphans, 2027-01-07", was left untouched). Fix: one clause, e.g. "a MAIN-AGENT EXIT requeues the item to `Pending` — unless the work already landed (the done-orphan guard, 6c6966b9: then it auto-resolves `Done`) — (no turn resolution ever comes for it…)", and optionally the same touch on the adoption half. No code change.

### Round-2 finding verification

**LOW-3 (stale README sentence) — FIXED, verified.** README.md:79 (the extended Backlog-tab line) now reads exactly as prescribed: "a main-agent exit or hard crash requeues the orphaned in-flight item to Pending — unless the work already landed (plan complete + commits after the pre-item checkpoint), which auto-resolves done (no turn resolution ever comes for it — the exit drain and the run-start adoption sweep)" — committed as d8b0401 (the only README change; the rest of the line is byte-identical to the pre-fix version). Cross-checked against the code: "plan complete + commits after the pre-item checkpoint" is exactly `orphan_work_landed` (run_all.rs:2718-2740: plan_id → `plan_steps_all_done` → `extract_checkpoint_sha` from the note head → `commits_after_checkpoint == Ok(true)`); "auto-resolves done" is the landed arm's `BacklogStatus::Done` transition at both sites (drain :2799-2808, adoption :2924-2938); "no turn resolution ever comes for it — the exit drain and the run-start adoption sweep" is accurate for both mechanisms and both arms. Consistent with every other surface — the module doc's safety-model bullet (:42-45), both function doc comments (drain :2755-2760, adoption :2844-2848), and both amended knowledge specs — with LOW-4 above the one remaining unqualified surface.

### Round-1 finding verification (no regressions)

**LOW-1 (stale lifecycle specs) — still FIXED.** The lifecycle spec's bottom amendment (line 22, "Amended 2027-01-07 (the done-orphan guard, backlog 6c6966b9, plan 5b988455)") and the resolution-contract spec's two touches (line 6: the resolution-path "— unless the work already landed…" clause + the flipped scope-boundary sentence; line 10: the mirroring amendment paragraph) are all present and accurate at the committed state.

**LOW-2 (missing doc comment) — still FIXED.** `pub async fn commits_after_checkpoint` (src/project/git_ops.rs:250) carries its doc comment (lines 243-249); the impl's thorough doc (:222-237) is unchanged.

**No source delta since fcc1010.** d8b0401's diff touches exactly three files: README.md (the LOW-3 clause), `.coding/plans/5b988455.md` (the step-4 checkbox flip — bookkeeping), and the round-2 review report itself (new file). Zero source changes — rounds 1-2's code verification carries over, and I re-confirmed the load-bearing sections at the committed state: `orphan_work_landed` (:2718), `plan_steps_all_done` (:2681), the drain (:2761), the adoption (:2860), `commits_after_checkpoint[_impl]` (git_ops.rs:222-257), the MockGit log arm, and all four regression tests (both source contracts at :460/:494 present; `plan_steps_all_done_requires_every_step_checked`; `commits_after_checkpoint_impl_detects_landed_work`).

### Re-verification of the committed change (fcc1010 + d8b0401)

- **Correctness.** The guard's conjunction (plan gate AND commit gate) matches the died-between-commit-and-finish window; every unverifiable path returns false → the Pending arm (the safe default). The drain's `still_in_flight` re-verify and the adoption's Phase 3 ownership re-verify (still InFlight + not the single-dispatch pointer + not the active root plan, with fresh reads) cover the unlocked evidence window. `InFlight → Done` is a legal guarded transition.
- **Lock discipline.** No backlog store lock across the git subprocess anywhere (block-scoped snapshot → unlocked evidence → re-take + re-verify); the project-root lock is held only for the `root.clone()`; `main_agent_top_plan_id` is taken before the store lock in both adoption phases — no new lock-ordering edge.
- **Security.** No new attack surface: the sha is hex-gated (≥7 chars) and argv-discrete; `plan_id` only steers a file read whose failure → false → requeue.
- **Multi-platform neutrality.** Clean: `Path::join` for the plan path, discrete argv for git (no shell), no Windows-only APIs; the only `cfg(windows)` code (`CREATE_NO_WINDOW` in `git_raw`) is pre-existing; the test's `Path::new("/tmp/mock")` is the pre-existing mock-key idiom.
- **d8b0401 itself.** Docs-only, exactly as the round-2 fix prescribed; the commit message accurately describes it.

### Documentation-sync sweep (round-3, repo-wide)

Beyond LOW-4, every other surface mentioning the dead-owner paths was checked and is either updated or correctly historical:
- **Module doc deferred paragraph (:20-22, "orphan adoption still requeues a deferred orphan to `Pending`") and the deferred spec's point (3)** — considered and CLEARED, not a finding: their subject is the deferred flag's semantics (survival through the requeue, non-stranding, selection exclusion), and every operative claim remains true for every case where the flag has effect — a landed deferred orphan resolving `Done` leaves all guarantees intact (terminal items are never selected by run-all regardless of the flag; the item is not stranded). The requeue mention is instrumental context, not a statement of the dead-owner resolution contract.
- **Historical records** — the 5f6515f5 bug record, the intervention-pause decision record (attributing the requeue amendment to plan 5f6515f5), the superseded 2026-12-06 spec, plan files, and prior review reports are point-in-time records and correctly describe their era; the NEW bug record (2027-01-07-run-all-re-dispatches-landed-work-done-orphan-gu.md) accurately describes the guard.
- **README** — line 79 is the only backlog-orphan mention (the other "orphan" hits are compaction-related); it now carries the exception.

### Test status

As reviewer I have no shell — validated by code reading. Per the task brief: plain `cargo test` (2084+16) and `cargo test -p mnemo-app` (250+4+2) pass after the LOW-3 fix; the only delta since fcc1010 is the README clause (docs-only — cannot affect compilation or tests). `cargo test --workspace` fails only at the pre-existing `agent::factory::tests::tools_array_stays_within_context_budget` (verified failing on the clean tree before this change) — not a regression, out of scope per the brief.
