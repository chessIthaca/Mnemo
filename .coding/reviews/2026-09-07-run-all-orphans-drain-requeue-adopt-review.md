## Verdict: FINDINGS (0 high, 2 low)

Review of ALL uncommitted changes on wt/agenticcoding: `src-tauri/src/ipc/run_all.rs`, `backlog_cmds.rs`, `events.rs` (+ tests), the untracked plan file `.coding/plans/5f6515f5.md`. Note on scope: `.coding/backlog.jsonl` carries NO uncommitted change — item 3b395d27's done-stamp is already committed (9c43cd5, "backlog: 3b395d27 … Browser tab: add Stop and Reload buttons"), i.e. ordinary post-completion bookkeeping already landed; nothing uncommitted to judge there.

The core fix is correct: the drain requeues through the guarded transition with the note snapshotted before it (required — `transition` overwrites the note, `src/backlog.rs:473`), the adoption ownership model is sound, lock discipline introduces no new deadlock cycle, and the b83e891f mid-session contract is genuinely untouched. Two low findings.

### Findings

**LOW-1 — `adopt_orphaned_in_flight` reads `single_in_flight` before taking the store lock (TOCTOU window).** `src-tauri/src/ipc/run_all.rs:2302-2309`: the pointer is read first (2302-2307), then `main_agent_top_plan_id` awaits (2308), then the store lock is acquired (2309) and the orphan snapshot taken. A concurrent single dispatch — `dispatch_item` sets the pointer at `src-tauri/src/ipc/backlog_cmds.rs:378-382` before sending, and the item is stamped `Pending→InFlight` at Executing entry by `stamp_backlog_in_flight` (`run_all.rs:2556-2587`, under the store lock) — that completes entirely inside that gap makes a just-dispatched item look orphaned, and adoption requeues it to `Pending` while its agent works. Reachability is theoretical (the gap is a few lock acquisitions; the racing dispatch needs a full LLM round trip to reach Executing) and the consequence is recoverable (work kept in tree; the single-dispatch resolution can still legally transition `Pending→Done`), but the hardening is trivial and closes the class completely: read `single_in_flight` AFTER acquiring the store lock (std Mutex — no await needed). Soundness of that fix: the pointer set strictly precedes the InFlight stamp, and the stamp requires the store lock — so under the store lock, any InFlight item in the snapshot either has its pointer already visible (excluded) or was not InFlight at snapshot time (not adopted). The analogous `active_plan` window (a manually-resumed session's plan becoming active mid-sweep) cannot be closed the same way (that read awaits) and is the same class as the documented accepted residual — fine to leave.

**LOW-2 — stale contract doc: the status⇄plan-lifecycle spec still describes the old drain.** `.coding/knowledge/spec/2026-12-06-backlog-status-plan-lifecycle.md:12` still says a main-agent exit while a run is active "drains it (`drain_run_all_on_main_exit` — the item keeps its status, the note records why the run ended)". After this change the drain REQUEUES the item to `Pending` (checkpoint sha preserved). That spec is the contract document of record for this subsystem (the b83e891f DECISION memory names it "Contract:"), and it was amended when b83e891f landed — this change alters the same dead-owner drain semantics it describes, so it needs the matching amendment. The module doc, both new fn docs, and the events.rs comment are all updated and accurate; this spec file is the one stale surface. Constitution: documentation sync.

### Verified correct (the parent's checklist)

**Correctness / lock discipline:**
- Drain (`run_all.rs:2249-2267`): the store guard is dropped at the end of the `if let` block, BEFORE `end_run`'s awaits (end_run takes the run_all lock + emits — no double-lock, no guard across an await).
- `adopt_orphaned_in_flight`: the `single_in_flight` std-Mutex guard is dropped before any await; the store lock is taken last with no awaits under it (snapshot + transitions are sync `&mut self` calls).
- `main_agent_top_plan_id` (`run_all.rs:2274-2281`) is a structural clone of the pre-existing `main_agent_workflow_state` (`run_all.rs:269-276`) — same manager → agent_loops → workflow sequence. The agent_loops-guard-held-across-the-workflow-await pattern is the established lock order (identical in the pre-existing mirror used on every turn resolution), so no new deadlock cycle is introduced.
- Transition-table usage: `InFlight→Pending` is a legal row (`src/backlog.rs:462`); both fns use the guarded `transition`, never raw `set_status` and never `requeue` — exactly as the plan specified. The `format!("{n} | {suffix}")` note shape mirrors `handle_user_intervention`'s single-dispatch arm, keeping the checkpoint sha at the note head.
- No Done-resurrection path (checked because `Done→Pending` is also a legal row and the drain transitions unconditionally): `run_all.current_item` is cleared synchronously on terminal resolution (`run_all.rs:1959-1972`, the LOW-3 clearing) BEFORE the auto-compact `tokio::spawn` (`run_all.rs:2003`), and turn resolution + `Exited` are processed sequentially by the same forwarder task — so the drain can only ever see a `Pending` (dispatched, pre-Executing) or `InFlight` current item. `Pending→Pending` is refused by the table (same-status), so the pre-Executing-exit edge leaves the item correctly queued with its checkpoint note (only the informational exit annotation is lost — cosmetic, acceptable).
- Exited-arm clear (`events.rs:681-688`): correct — `single_in_flight` is only ever set by `dispatch_item`, which targets the main agent, so a main-agent exit owns any live pointer; `was_main` is captured before `mgr.remove` (pinned by the existing test). The misattribution rationale is real: `on_main_turn_resolved` takes the pointer (`run_all.rs:2015-2020`) on any main-agent turn end when no run-all is active.

**b83e891f contract genuinely untouched:** none of the five pinning tests appear in the diff — `intervention_keeps_a_run_all_item_in_flight_with_its_run` (`run_all.rs:757`), `stopped_runs_survive_open_plan_and_error_turn_ends` (:804), `in_flight_stamp_only_applies_to_pending_items` (:844), `on_main_turn_resolved_is_plan_tied` (:867), `requeue_refuses_pending_in_flight_and_unknown` (src/backlog.rs). `handle_user_intervention`, `halt_run_all`'s steer arm, and the still-open/error arms that keep stopped runs alive are unmodified. Only dead-owner paths changed (exit drain, run-all-start adoption) — the module doc's explicit carve-out is accurate.

**Edge cases:** plan-less items are adoptable — `!(i.plan_id.is_some() && i.plan_id == active_plan)` adopts `plan_id: None`, and the `is_some()` guard correctly avoids a false None==None match when the main agent has no active plan; drain with no note → suffix-only note; a stale `single_in_flight` after exit is cleared by the Exited arm before any adoption can run; adoption's caller guarantee holds (called after the already-active check, before the run state is constructed — `backlog_cmds.rs:424-452` — so `run_all.current_item` cannot own anything, and adopted items count toward the run total).

**Test quality:** both regression tests genuinely pin the fix — reverting the drain to `.annotate(` fails the `BacklogStatus::Pending` / `.transition(` / snapshot-before-transition assertions; removing the adoption call trips the `expect`; moving it after the pending count trips the ordering pin (`active_check < adopt_call < pending_count`). Noted limitations (no action required): the forwarder assertion `forwarder.contains(".single_in_flight")` (`run_all.rs:985-988`) scans all of events.rs rather than being scoped to the Exited arm — sound today (the dotted form occurs only in the new clear code) but it would not catch the clear being moved outside `if was_main`; the ownership-filter semantics are string-pinned only (an inverted condition would pass) — inherent to the source-contract house pattern given no `IpcState` test constructor exists (all three construction sites in main.rs build full app-startup state).

**Constitution:** documentation sync — module doc (both amended sections), both new fn docs, and the events.rs comment are updated and accurate; README/PLAN.md do not cover drain semantics at this granularity (no sync needed); the one stale surface is LOW-2. Multi-platform neutrality: pure Rust backend logic, no platform APIs, paths, or shell. Security: internal state transitions only — no new inputs, parsing, or attack surface. Style: doc comments on both new `pub(crate)` fns, house comment style throughout, warning-free per the green matrix under deny(warnings).

### Observations (no action required)

- **Single-dispatch orphan asymmetry:** the exit path clears the pointer but does not requeue the pointed-to item, so a single-dispatch orphan stays `InFlight` (the UI retry routes through the guarded requeue, which refuses `InFlight`) until the next run-all start adopts it. This matches the agreed scope (adoption at run-all start) and is strictly better than before (a stale pointer would have blocked adoption entirely). If the "no item ever InFlight without a live owner" goal is ever to hold at exit time uniformly, the Exited arm could requeue the cleared pointer's item with the same transition shape the drain uses.
- The pre-existing check-then-construct gap in `backlog_run_all` (two concurrent invocations can both pass the already-active check before either constructs the run state) is marginally widened by the adoption await between them — pre-existing class, unchanged severity.
