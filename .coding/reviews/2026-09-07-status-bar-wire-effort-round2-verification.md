## Verdict: PASS

Round-2 verification of commit 9fab630 ("fix: status-bar reasoning effort tracks the wire-reported effective effort (51dab4da)", plan 3e680de3) on wt/agenticcoding. Both round-1 LOW findings are genuinely fixed in the committed code — not just in the working tree — and the commit as a whole is sound: no new blocking issue found. HEAD is exactly 9fab630 and `git diff HEAD` / `git status --short` are empty, so every file read below is the committed state.

### Round-1 finding 1 (LOW) — `removeAgent` cleans `agentWireEfforts` — RESOLVED

Committed code at `frontend/src/hooks/agentEventReducer.ts:1508-1509`: the cross-map removal block now clones the map and deletes the agent id — `const agentWireEfforts = { ...s.agentWireEfforts }; delete agentWireEfforts[agentId];` — exactly mirroring the `agentEfforts` sibling lines directly above (:1506-1507). The returned `next` at :1516 includes it: `{ ...s, agents, agentNames, agentParents, agentModels, agentProviders, agentEfforts, agentWireEfforts, workflowStates, activeAgent }`. Both halves of the round-1 fix prescription (clone+delete AND inclusion in `next`) are present, and the fix is inside commit 9fab630 itself (visible in the `git show` diff), not a later uncommitted patch.

### Round-1 finding 2 (LOW) — `resetStore()` resets `agentWireEfforts` — RESOLVED

Committed code at `frontend/src/hooks/useAgentStore.test.ts:31`: `agentWireEfforts: {},` sits directly after `agentEfforts: {}` (:30) inside `resetStore()`'s `setState` partial, so the slice the new regression test writes is wiped before every test — the cross-test pollution class the helper exists to prevent is closed. Also in the commit diff, same verification as above.

### Whole-commit sanity check — SOUND

Reviewed the full diff of 9fab630 (29 files, +758/−42) against the round-1 verification's invariants; all hold in the committed code:

- **Display-space resolvers** (`src/config/endpoints.rs`): `display_reasoning_effort_for` mirrors the wire chain (ModelSpec override → endpoint → "max", `supports_reasoning_effort` gate → "off", allow-list clamp) with "off" kept verbatim; `display_normalize_reasoning_effort_for` early-outs `requested == "off"` BEFORE the allow-list clamp, so an "off" request can never clamp to a literal level. The DeepSeek unit (`display_reasoning_effort_deepseek_off_stays_off`) pins the vocabulary split: display "off" vs wire `effective_reasoning_effort_for` → `Some("none")` — the off-wire encoding cannot leak into the UI.
- **`resolve_display_effort`** (`src/provider/client_factory.rs`) mirrors `resolve_effort`'s Some/None shape; **`ModelResolver::display_effort_for`** (trait, default None) + the `ConfigModelResolver` impl (endpoint lookup → `resolve_display_effort`) are both present.
- **Loop mirrors** (`src/agent/loop_impl.rs`): `resolved_effort` / `default_display_effort` / `pinned_display_effort` fields + accessors, and `resolved_effort` is set alongside `resolved_model` at every `resolve_turn_provider` branch in the diff — skill override, pin 429-sticky-alt, pin serve (via `pinned_display_effort`), forced-with-resolver, no-resolver (→ None), default 429-sticky-alt, default no-override (via `default_display_effort`), state/subagent chain. No missed branch.
- **Emission** (`src/agent/turn.rs`): prev/now effort snapshots bracket the resolution; fires on model OR effort change (`now_resolved != prev_resolved || now_effort != prev_effort`) — steady state emits nothing, and an effort-only flip (same model id, different per-context override) correctly emits.
- **Wire fields**: `reasoning_effort: Option<String>` on `AgentEvent::ModelChanged`, `SerializableAgentEvent::ModelChanged` (`#[serde(default, skip_serializing_if = "Option::is_none")]`), and `AgentInfo` (skip pattern). The extended `model_changed_roundtrips_json` asserts Some round-trips, None is skipped, absent key → None (pre-wire JSON stays compatible); contract fixtures updated on both sides (`event-model-changed.json` + `tests/integration/contract_fixtures.rs:219` + the src-tauri dto fixture test asserting `"reasoning_effort":"low"` serializes and None is skipped).
- **Swap paths**: console `/model`, `set_model` both paths (per-agent event carries `Some(display_effort)`, global via `swap_live_provider`), `save_endpoints` resync (computes `ep.display_reasoning_effort_for`), `swap_provider_into_loops` (factory `set_default_display_effort` + per-loop default + clear resolved), `swap_provider_into_loop` (pin + default + clear), `swap_live_provider`'s ModelChanged — all carry the effort. `ResolvedModel` gained `display_effort: String`; all callers of the three signature-changed functions are updated in the diff (a missed one would fail compilation — the reported green build covers completeness).
- **Factory stamping + startup**: `AgentLoopFactory.default_display_effort` (RwLock) stamped in `build_inner` onto every loop; `main.rs` computes the startup display effort (endpoint branch → display chain, dummy → "max") and stamps the factory after construction.
- **Frontend**: `agentWireEfforts` in `AppStateLike`/`AppState` + init; `registerAgents` presence-wins (absent never clobbers); `reduceModelChanged` upserts/clears (empty string treated as absent — correct); the dispatcher merge reuses `mergeAgentProviders`, whose null/""→delete semantics I re-read at :1139-1155; StatusBar precedence `agentWireEfforts ?? agentEfforts ?? effortForEndpoint`.
- **Regression tests present**: the frontend "model_changed: records the wire-reported reasoning effort for the agent" (asserts both the "low" upsert and the clear-on-absent), the extended `mid_turn_skill_model_switch_emits_model_changed_events` (`("skill-model", Some("low"))` then `("mock", None)`), the three display resolver units, roundtrip + contract fixtures.
- **Multi-platform neutrality + warnings**: no Windows-only APIs/paths/shell syntax in the diff (the WebView2 text in main.rs is pre-existing context); every new pub item has callers; nothing plausibly breaks the warning-free build.

### Notes (non-findings)

- Test execution: as a read-only reviewer I did not re-run the suites; the commit message records root 2042+16, src-tauri 233+4, frontend 1004 + build all green after the two fixes, and nothing in the diff contradicts that matrix. The two fixes are pure additive map-cleanup lines with no behavioral risk to the green run.
- Backlog item 51dab4da remains `in_flight` in `.coding/backlog.jsonl` (note: "plan loop did not close — work kept in tree") — accurate bookkeeping while the plan's closing sequence (this round-2 review) is still running; the parent workflow should flip it to done at finish. Not a code finding.
