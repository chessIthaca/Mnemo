## Verdict: FINDINGS (0 high, 1 low)

The fix is correct and complete: both new arms recover the previously-forever-stalled (Complete, false) + Pending landing on the main path and the spawned lane, the 2026-08-20 never-planned hole stays plugged, the lock/fall-through/landing discipline mirrors the established patterns (may_flip arm + drain's done-orphan guard), and the two source-contract regression tests genuinely pin the arms (presence, ordering, gates, no early return). One LOW documentation-sync finding: the per-function doc comments of `on_main_turn_resolved` / `on_spawned_turn_resolved` still summarize the contract as "anything else → no transition + the run waits", now stale for the new arm's not-landed branch (no transition, but the run dispatches next instead of waiting).

### Change set reviewed

- `src-tauri/src/ipc/run_all.rs` (+330): module-doc bullet "The RE-QUEUED variant of the slide" (:101-113); new arm in `on_spawned_turn_resolved` (:4108-4195, before the waiting arm :4200); new arm in `on_main_turn_resolved` (:4862-4913, before the waiting arm :4931); two source-contract regression tests `slid_resolution_with_requeued_pending_item_dispatches_next` / `slid_resolution_spawned_lane_requeued_pending_recovers` (:1997-2125).
- `.coding/backlog.jsonl`: item 8a6bcece dispatch bookkeeping (pending → in_flight, checkpoint sha + plan_id af1058ee) — expected.
- New: `.coding/plans/af1058ee.md`, `.coding/knowledge/bug/2027-01-07-slid-resolution-with-a-re-queued-pending-item-st.md` (both accurate).

### Checked and verified (task checks 1-8)

1. **Arm ordering.** Main match (:4733): Done arm `(Some(main_state), true) if plan_loop_allows_done(...)` → `_ if plan_abandoned` → **new arm** `(Some(WorkflowState::Complete), false) if item.status == BacklogStatus::Pending` (:4862) → waiting arm `(main_state, _)` (:4931). Spawned match (:3950): same shape, new arm :4108 before the waiting arm :4200. No overlap: the Done arm requires the second tuple element `true` (the new arm requires `false`); `plan_abandoned` implies an observed transition (loop_evidence true → second element true → the Done arm), and even in a theoretical (Complete, false)+plan_abandoned landing the abandoned arm's `plan_linkage_allows_failed` guard leaves a Pending item untouched and its blocked row also falls through to the tail — no stall either way. A placement after the waiting arm would be dead code (and compiler-flagged unreachable); the tests pin the order independently.

2. **2026-08-20 hole stays plugged.** `orphan_work_landed_at` (:5498-5515) returns false on `plan_id == None` (:5504-5506), incomplete plan file, missing checkpoint sha, or git error — anything unverifiable is false. A Pending item with no plan_id → `landed=false` → the not-landed fall-through, no Done transition. Verified in both arms.

3. **Fall-through discipline.** Neither branch of either arm returns early (main :4862-4913; spawned :4108-4195). The main tail (:5042-5103) bumps the done counter only on `terminal_resolution`, clears `current_item` unconditionally (:5056), and dispatches next (auto-compact only when `item_resolved`); the spawned tail (:4285-4331) mirrors with `remove_spawned_run` + conditional worktree removal + dispatch. The waiting arms are untouched (diff context only) and still annotate-and-return for non-Pending items.

4. **Lock discipline.** Both arms: (note, plan_id) snapshot under the store lock with no await inside the critical section; the landed consult runs WITHOUT the store lock (`orphan_work_landed` clones the project root before the git subprocess, :5483-5487); `still_pending` re-verify under the lock before the Done transition. Matches the may_flip arm's established pattern.

5. **Spawned lane.** `land_spawned_branch` error handling mirrors the may_flip arm exactly: Ok → `remove_worktree = true`; Conflict/Git → suffix extended + eprintln, worktree + branch kept, work still counts Done (`terminal_resolution = true` regardless). The not-landed branch sets `remove_worktree = true` — and `remove_item_worktree` (src/project/worktrees.rs:104-131) prunes, removes the worktree, AND force-deletes the branch, so no stale `wt/runall-*` branch blocks later lane dispatch. No `commit_success` call — matches the drain's landed branch (drain_spawned_on_exit :4388-4427), the done-orphan semantics.

6. **Regression tests pin the fix.** Both tests use the established `fn_body(include_str!(...))` source-contract pattern. Verified the search strings are unambiguous: "(Some(WorkflowState::Complete), false)" occurs exactly once per function body (the new arms — file-wide only the two arms + the two test definitions); "plan_open_note(main_state)"'s first occurrence in the main body is the waiting arm (:4932, before the single-dispatch row :5209); "plan_open_note(ws)" occurs only at the spawned waiting arm (:4201). Removing an arm → `find` → `expect` panics (RED); reordering after the waiting arm → `arm_ix < waiting_ix` fails (RED); dropping the landed gate / the still-Pending re-verify / adding an early return → the respective `contains` / `!contains` asserts fail (RED).

7. **No regressions in surrounding logic.** The diff is purely additive (two arms + doc bullet + tests + bookkeeping). Waiting arms, plan_abandoned arms, Done arms, single-dispatch rows, and tails are untouched. The only behavior change is the previously-forever-stalled (Complete, false) + Pending landing, which now recovers — and at (Complete, false) no auto-continue fires (the e33a07fd-established premise, pinned by the test comment at :1960), so the pointer clear cannot race a resumed agent; the re-dispatch re-establishes pointer + checkpoint cleanly. A freshly-dispatched, never-planned item whose first turn ends at (Complete, false) — previously also a forever-wait — now re-dispatches, matching the documented intent (module doc :110-112).

8. **Project checks.** Multi-platform neutrality: platform-neutral Rust only (std::path, eprintln; no Windows APIs, no shell syntax). Root cause documented: BUG knowledge record (accurate symptom → root cause → fix + test names) + BUG memory exist. Tests exercise the changed path (source-contract on the exact arms — the file's established pattern). cargo test reported green (src-tauri 283+4+2, root 2138/0/4-ignored, frontend 1060/75); this reviewer is read-only and did not re-run — plan step 4 (Verify) remains open for the parent to close with the recorded run.

### Findings

**LOW-1 — stale function doc comments (documentation sync).** `on_main_turn_resolved`'s doc (:4527-4530) and `on_spawned_turn_resolved`'s doc (:3852-3855) summarize the resolution contract as "gate → `Done`; abandoned → `Failed`; anything else → no transition + the run waits — the next turn resolution re-checks". After this change that summary is wrong for the new landing: a Pending item at (Complete, false) with no landed evidence gets no transition but the run does NOT wait — the pointer is cleared and the next item dispatched. The module doc (the authoritative contract both function docs reference) was updated; the per-function summaries were not. Recommendation: add one line to each function doc, e.g. "a re-queued (Pending) item at resting `Complete` recovers — landed evidence → `Done`, else pointer clear + dispatch next (backlog 8a6bcece, see the module doc)".
