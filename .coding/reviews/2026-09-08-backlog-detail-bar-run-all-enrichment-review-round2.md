## Verdict: PASS

Round-2 verification of the uncommitted changes on wt/agenticcoding for plan 4354296c ("Backlog item detail bar + run-all dispatch enrichment") — 9 files, +621/−61, plus two untracked .coding/ files (the plan file and the round-1 report). All three round-1 findings are correctly fixed, and the additional card-toolbar work (deferred-checkbox move + send-to-bottom) is sound, well-tested, and documented. No new issues. The round-1 design-decision analysis stands; this report covers only the verification.

### Round-1 fix verification

**HIGH 1 — manager lock held across the recalled-context await: FIXED.** `run_all_dispatch_next` now computes `let context_block = recalled_context_block(state, &item.text).await;` at run_all.rs:2323, ABOVE the `let manager = state.runtime.manager.lock().await;` at :2327 — the embedder await (remote round-trip or local ONNX inference plus a sqlite read) no longer runs under the manager lock. The busy re-check and the single `send` remain atomic under the lock hold, and the send still threads the block: `run_all_prompt(&item.text, context_block.as_deref())` (:2418). The lock discipline is documented in the comment at :2315-2322 (same rule as the git checkpoint; accepted cost: a wasted recall in the rare defer-after-recheck path — trivial, and the defer path drops the manager lock before its own awaits). No other await was introduced under the lock: after :2327 the only awaits are on the agent_loops/workflow mutexes (pre-existing, plan 9057fa1f) and the backlog store lock (pre-existing). The source-contract test's two asserted strings still match the shipped function body.

**LOW 1 — Template button selection: FIXED.** `handleTemplate` (BacklogView.tsx:223-228) sets the text and a `templateJustInserted` flag; the `useEffect` keyed on the flag (:127-134) applies `focus()` + `setSelectionRange(0, "Short headline".length)` and clears the flag. The ordering is correct: React 18 batches `setText` + `setTemplateJustInserted(true)` into one commit, and the effect runs post-commit — the textarea's value IS the template when the selection applies, so the 14-char selection covers exactly the "Short headline" placeholder and the first keystroke replaces it. The flag-clearing re-render doesn't change the `value` prop, so React doesn't reassign the DOM value and the selection survives. Guards intact: the button stays `disabled={!!text.trim()}` and the handler early-returns on non-empty text (double guard). `useEffect` was already imported (:5); `textareaRef` is attached to the composer textarea (:115, :264). Rapid double-clicks are impossible (the button disables after the first).

**LOW 2 — slash-dates satisfying the path marker: FIXED.** `body_carries_detail_marker` (src/backlog.rs:95-100) now requires `find_iter(...).any(|m| m.as_str().chars().any(|c| c.is_alphabetic()))` — a match counts only when the matched token carries a letter. "10/10/2027" / "01/07/27" (digits + separators only) no longer count, on any alternative (the filter also covers the separator+extension alternative, so a degenerate "10/10.2027" is filtered too). No false negatives: every legitimate shape the regex targets carries letters (a path segment, an extension, a drive letter, a URL host). Regression cases added to `detail_marker_recognizes_paths_but_not_prose`: `!"seen 10/10/2027 and again 01/07/27"` alongside the still-asserted positives (src/auth/session.rs, frontend/src/lib/tauri.ts:373, C:\repo\src\main.rs, README.md, Cargo.toml, https://example.com/guide) and the "and/or" negative.

### Card-toolbar work (fresh review — it never had one)

- **`moveIdToBack` (BacklogView.tsx:352-356) is a correct mirror of `moveIdToFront`.** A middle id moves to a NEW array with the id last and the rest in relative order (`["a","b","c","d"], "b"` → `["a","c","d","b"]`); already-last and absent ids return an equal order (a stale-render click is a harmless no-op). Empty and single-element orders fall out correctly (`indexOf` −1 / `at === length−1`). Doc comment present.
- **`handleSendToBottom` (:615-625)** mirrors `handleSendToTop` exactly: map the live store order → `moveIdToBack` → `backlogReorder`, errors logged not thrown. The button (:734-741) rides after move-down, `disabled={index === total - 1}` (the same gate as move-down), `title="Send to bottom"`, ArrowDownToLine icon — styling consistent with the sibling controls. The parent passes `index={i}` / `total={backlog.length}` (:1088-1094), so the gate is correct.
- **Deferred checkbox move (:698-709).** The checkbox now lives in the card's right toolbar at the start of the queue-control group (immediately before send-to-top), unconditional across statuses, with the same title, aria-label, and `backlogSetDeferred` persistence as the old placement. The old block (beside the status chip, inside the non-editing branch) is fully removed — the headline row (:828-833) starts directly with the status chip span; no dead code or leftover comment (pinned by the source contract's `not.toContain("a checkbox beside the status chip")`). Side effect of the move: the toggle is now also visible while the inline editor is open (the old placement hid it during edit) — a small improvement, consistent with "orthogonal to status".
- **The existing deferred SSR tests still hold:** the checkbox renders unconditionally in the toolbar (`type="checkbox"`, `aria-label="Deferred (skip Run-All)"`), `checked={item.deferred ?? false}` renders `checked=""` only when deferred, and the Deferred badge still renders in the headline row (:834-838) — the `>Deferred<` / `text-violet-400` assertions are unaffected.
- **Tests + docs:** five new vitest cases (four `moveIdToBack` unit tests + a source contract pinning the button wiring, disabled gate, icon, toolbar comment, and the old placement's absence). README updated for both changes: "a checkbox in each card's toolbar (moved from beside the status chip, same-day user request…)" and "the card toolbar also carries the queue controls — move up/down, send to top, and send to bottom".

### Test + regression status

Reported green: cargo test (workspace) 2096 + 16 passed; npm test (frontend) 1056 passed. The counts are internally consistent with the diff: round-1's npm run was 1051 and this diff adds exactly the five moveIdToBack cases (1051 + 5 = 1056); the cargo count is unchanged because the fixes add asserts to existing tests (the slash-date cases) rather than new test functions, and the LOW-1 effect fix is inherently untestable in the node SSR harness (renderToStaticMarkup runs no effects — the wiring is instead pinned by the source contract). I could not run the suites myself (read-only reviewer); both regression cases the verification asked about are present in source: the slash-date asserts in `detail_marker_recognizes_paths_but_not_prose` and the `moveIdToBack` describe block.

### Scope check

Nothing changed beyond the three fixes, the card-toolbar work, and bookkeeping. The 9-file diff maps hunk-by-hunk to: round-1 scope (the detail bar in src/backlog.rs + src/tool/workflow/backlog.rs, the dispatch enrichment in run_all.rs, the Template button + tests, PLAN.md/README.md docs, the dto-get-settings.json fixture repair), the three round-1 fixes, and the card-toolbar work (BacklogView.tsx + BacklogView.test.tsx + the two README sentences). The untracked files are .coding/plans/4354296c.md and the round-1 review report — bookkeeping. The .coding/backlog.jsonl change is the item's in_flight status flip with its plan pointer.

### Constitution

Doc comments on every new function/handler (moveIdToBack, handleSendToBottom, backlogItemTemplate, the effect carries explanatory comments). Multi-platform neutral: React + a data-level regex — no platform APIs. Docs synced: README covers the checkbox move and send-to-bottom; PLAN.md carries the detail-bar/enrichment decisions (the card toolbar is a UI affordance — README is its home). No `#[allow]` or warning-silencing anywhere in the diff.
