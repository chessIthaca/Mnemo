## Verdict: FINDINGS (0 high, 1 low)

Round-4 verification of the round-3 doc-only fix (commit 4b867f6, on eeb28e7 on d92b117, wt/agenticcoding, working tree clean). **The round-3 fix is exactly what it claims and is correct**: the one-phrase change (run_all.rs:2775, "a picker pin" → "a picker pin with a 429-sticky reroute") matches the round-2 report's qualifier and round-3's suggested wording verbatim, and now matches the code precisely — within the pin branch the only resolver build sits inside the `sticky_endpoint` Some-arm (loop_impl.rs:1193-1204); the plain pin returns the stored factory-built `(p, cm)` pair (:1134-1138, :1205-1211). The commit is doc-only (run_all.rs 5+/5− rewrapped doc lines + the round-3 report file), so rounds 1-3's whole-commit verification stands. **One new LOW**: mapping all five `build_turn_provider` call sites in `resolve_turn_provider` against the exception enumeration surfaces a sub-path no round has covered — the **default-provider 429-sticky reroute** (loop_impl.rs:1277-1287) also serves the turn on a resolver-built CM reading the LIVE config's ceiling, but it is neither a "per-context-override turn" nor enumerated, so the leading claim ("matches the dial the engine actually applies even after a mid-session settings edit") still overclaims on that sub-path. One-phrase doc fix — the mirror image of round 3's finding (under-inclusion instead of over-inclusion), same bounded/restart-healed impact class.

## Scope & method

Reviewed the full 4b867f6 diff (git show + stat), the round 1-3 reports, and the live code every claim rests on: run_all.rs (gate doc :2765-2781, gate body :2782-2810, helper :2739-2763), loop_impl.rs (`resolve_turn_provider` :1070-1315 — skill slot :1086-1113, pin :1115-1217, forced model :1218-1247, no-resolver/default/state chain :1248-1315, all five `build_turn_provider` sites :1102/:1196/:1232/:1278/:1306), model_resolver.rs (`build_turn_provider` :366-417 — live-config ceiling read :373-379, applied on both CM builds :398/:411), turn.rs (`resolve_iteration_provider` :1439-1504 — the Some/None CM selection :1461-1468, the fill-rate trigger contract :1506-1508). Plus repo-wide searches for "picker pin" and "429-sticky reroute" (over-claim phrasing). Static review; the parent's post-fix test runs (src-tauri 278/0/0 + integration 4/2) taken as given — the same standard rounds 2-3 applied.

## Task 1 — Does the phrase match the round-3 evidence chain? Verified

- **Plain pin = factory-built snapshot ceiling (no divergence).** `resolve_turn_provider` clones the stored `explicit_provider` pair (loop_impl.rs:1134-1138) and, absent a reroute, returns it verbatim (:1205-1211) — the CM built at pin time by `factory.context_manager_for` (round 3 verified agent.rs:613 → factory.rs:588-592 `.with_proxy_cache_ceiling(self.proxy_cache_ceiling)`, the startup snapshot; forwarded verbatim via config_io.rs:291 into `set_explicit_provider` :1608-1611). No `build_turn_provider` call anywhere else in the pin branch — confirmed by reading the whole branch.
- **429-sticky reroute = resolver-built live-config CM.** :1193-1204 — `sticky_endpoint(&pinned_ref)` → `Some(alt)` → `resolver.build_turn_provider(&alt, self.fill_rate)`; model_resolver.rs:373-379 reads `proxy_cache_ceiling_tokens` from `self.config.read()` (the live handle), applied on both the cache-hit (:398) and cache-miss (:411) CM builds.
- **The phrase is exact, not just directionally right.** Within the pin branch the resolver build exists only inside the sticky Some-arm, and if that build fails (or no resolver is attached) the code falls through to the stored pair — so "a picker pin with a 429-sticky reroute" denotes exactly the set of pin turns served by a resolver-built CM: neither over-inclusive (round 3's defect) nor under-inclusive within the branch. It is also verbatim round-2's finding text and round-3's suggested fix wording.

## LOW-1 (round 4): the exception enumeration omits the default-provider 429-sticky reroute — the leading claim still overclaims on that sub-path

**Where:** run_all.rs:2770-2779 — "the gate's threshold matches the dial the engine actually applies even after a mid-session settings edit … Exception (round-2 LOW-1): per-context-override turns (a `[models.*]` slot serving the turn, a picker pin with a 429-sticky reroute, or a forced model) run on resolver-built CMs that read the LIVE config's ceiling".

**Evidence.** `resolve_turn_provider` has five `build_turn_provider` call sites. Four are covered by the enumeration as it now stands: the skill slot (:1102) and the state/subagent chain (:1306) = "a `[models.*]` slot serving the turn"; the pin reroute (:1196) = the round-3-fixed element; the forced model (:1232). The fifth is not: when NO override resolves for the context, the default provider serves — **unless** 429 stickiness has recorded an alternate endpoint for the default model, in which case loop_impl.rs:1277-1287 returns `resolver.build_turn_provider(&alt, self.fill_rate)` — a resolver-built CM reading the LIVE config's ceiling (model_resolver.rs:379). turn.rs:1465-1468 confirms the returned CM serves the turn, and the fill-rate trigger contract (:1506-1508, `token_count >= context_manager.effective_summarize_at()`) applies to whatever CM serves. So after a mid-session `proxy_cache_ceiling_tokens` edit (before restart), the gate computes the factory snapshot while that sub-path's engine threshold uses the new ceiling — the same divergence the exception paragraph documents for the override paths, on a path the paragraph's category ("per-context-override turns") excludes by definition and its parenthetical does not list.

Round 3's own Task-2 verification listed :1278 among the resolver-CM build sites (the fill-rate half: ":1102/:1196/:1232/:1278/:1306 all pass `self.fill_rate`") — the site was on its radar, but the ceiling-provenance consequence for the enumeration went unflagged; round 2's finding text carried the same three-element enumeration, so the doc faithfully implements an under-inclusive finding text.

**Reachability for the gate's subject (the main agent):** real — no `[models.*]` slot for the current state, no pin, no forced model, and the default endpoint has 429'd (sticky entries persist until overwritten or loop death, per the 2026-09-01 stickiness fix). Quadruple-conditioned together with the mid-session edit + pre-restart — rarer than the round-2/3 scenarios, but the same bounded, restart-healed, preflight-guarded impact class.

**Why LOW:** doc-only, zero behavioral impact, divergence bounded identically to rounds 1-3's findings (restart-healed, magnitude capped by the ceiling delta, preflight backstop = window − headroom still guards, the gate is an optimization never a blocker). But this sentence family exists precisely to make the provenance claim accurate: the leading claim still tells a maintainer the default path always matches the gate after a mid-session edit — false when a default-path reroute is active. Round-2's defect direction (claims matching where there is divergence), on the one sub-path no round has enumerated.

**Fix (one phrase, no code change):** widen the exception to the true set — turns served by resolver-built CMs. E.g. "Exception (round-2 LOW-1): turns served by resolver-built CMs (a `[models.*]` slot serving the turn, a 429-sticky reroute — on a picker pin or the default provider — or a forced model) read the LIVE config's ceiling — on those paths a mid-session ceiling edit diverges until restart; …". No change to the code, the tests, or any other sentence.

## Task 2 — Internal consistency: the pin fix is consistent; the residual is LOW-1

- The round-3 phrase introduces no internal inconsistency: the exception's other elements (slot, forced model) and all bounding properties ("bounded, restart-healed", "preflight backstop (window − headroom)") remain accurate — round 3 verified them against the code and 4b867f6 changed no code.
- The fill-rate half of the leading claim is accurate on ALL paths — all five resolver build sites pass `self.fill_rate`, the loop's value (verified :1102/:1196/:1232/:1278/:1306) — so only the ceiling needed an exception; the comment's claim→exception structure is right, and the residual defect is the exception's membership (LOW-1), not its consistency.
- Repo-wide "picker pin" search: the only production-code occurrence of the gate's provenance claim is run_all.rs:2774-2779 itself; loop_impl.rs's pin comments describe pin mechanics (state-scoping, endpoint rerouting) and make no gate/ceiling claim; all other hits are historical reports/plans/knowledge files, accurate in their contexts. No other doc text carries over-broad phrasing.

## Task 3 — Nothing else unaddressed: verified

- **4b867f6 is doc-only as claimed:** exactly two files — run_all.rs (10 changed lines = the 5 rewrapped doc lines, 5+/5−) and the round-3 report (new file, 54 lines). No code change → round 2's whole-commit verification of d92b117 (correctness, tests, docs sync, multi-platform neutrality, security) and round 3's verification of eeb28e7 stand unimpaired.
- **Commit chain matches the round history:** e6aaabc → d92b117 → eeb28e7 → 4b867f6 (HEAD); working tree completely clean (`git diff HEAD` and `git status --short` both empty).
- **Test pins unaffected:** the changed lines sit in the doc comment above the fn; the gate body is untouched, so `gate_body.contains("f.proxy_cache_ceiling()")` and the other source-contract pins still match. Parent reports src-tauri green post-fix (278/0/0 + integration 4/2), taken as given.
- **Round-3 no-action notes remain no-action:** the eeb28e7 commit message's over-broad pin phrasing (immutable history — round 3 documented it; 4b867f6's own message carries the correct phrasing); the plan file's step-4 historical text (superseded design description, assessed by round 2).
- **Round-1 leftovers:** all resolved or moot per round 2's verification; nothing new surfaced.

## Notes (no action required)

- The 4b867f6 commit message accurately describes the fix, qualifier included — unlike eeb28e7's, no history correction is needed.
- LOW-1 can land as a fifth doc-only commit in this sentence family; no code or test change requested, and no round-1-3 finding regresses.
