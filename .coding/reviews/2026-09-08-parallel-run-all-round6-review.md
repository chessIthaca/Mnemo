## Verdict: PASS

**Scope**: round-6 verification of the uncommitted changes on `wt/agenticcoding` for plan ffd7a86f (parallel run-all) — the single round-5 fix (R5-L1: the ours determination hoisted under the DISPATCH_LOCK hold in `on_main_turn_resolved`'s intervention closed-loop arm) verified against the working tree and the full diff, plus a regression sweep of the touched region (the ours arm + the `dispatch_decisions_are_serialized` pin) and a line-shift consistency check proving nothing else in the change set moved since round 5.

**Summary**: R5-L1 is fixed correctly and completely. The lock acquisition (run_all.rs:3887) now precedes the `let ours = {` determination (3888) — the determination, the clear, and the wind-down decision all run under the one DISPATCH_LOCK hold, closing race (a) by the same construction that closed race (b) in round 4. The pin is sound: `fn_body` resolves to the right function (the needle's trailing `(` disambiguates `on_main_turn_resolved` from `on_main_turn_resolved_is_plan_tied`), both markers occur exactly once in the extracted body, and the find-order comparison fails loudly on any de-hoist. No regressions found — the ours arm's structure, the sequential path, the lock ordering, and the rest of the change set are all intact. Zero findings.

---

## R5-L1 — the hoist → fix complete ✓

run_all.rs:3863-3934 (`on_main_turn_resolved`, the intervention closed-loop `ours` arm), verified against the working tree and the diff hunk (`@@ -2714,6 +3869,22 @@`):

- **The hoist is real**: `let _dispatch_guard = DISPATCH_LOCK.lock().await;` sits at 3887, immediately before the `let ours = {` block at 3888. The diff shows it as an added line directly above the determination block, which is byte-identical to HEAD (context lines) — the fix changed the determination's placement relative to the lock, not its logic.
- **The whole arm is now one critical section**: finish_captured_item_done (3864) → comment (3865-3886) → **lock (3887)** → determination (3888-3900) → clear + spawned-read (3911-3924) → wind-down decision (3925-3932) → `return;` (3934). The determination, the clear, and the wind-down all run under the single hold — exactly the construction the round-5 fix menu prescribed.
- **Race (a) is closed by construction**: every CROSS-TASK writer of `current_item` and `spawned` runs under DISPATCH_LOCK — the pointer writes live in `run_all_dispatch_next` (2851 `Some`, 2925 `None`), acquired at 2728; every `SpawnedRun` record is pushed by `dispatch_spawned_item`, whose only caller is `fill_spawned_window`, whose only caller is `run_all_dispatch_next`'s three exits (2804, 2934, 2996 — verified by graph enumeration, all under the lock). The remaining `current_item` writers (the normal-path clear at 4192, the R2 drain clear at 4713) run in the same single forwarder task as the ours arm and cannot interleave. So a dispatch's pointer write either completes before the arm takes the lock (the determination sees the NEW pointer → `ours=false` → no clear → the new item is preserved) or waits until the arm returns (the clear already ran on the OLD pointer → the new write lands after → preserved). The check→clear seam is gone.
- **Lock ordering is unchanged (DISPATCH_LOCK → run-state)**: the ours arm acquires DISPATCH_LOCK (3887) before every run-state acquisition (3889, 3912, 3926); `run_all_dispatch_next` does the same (2728 → 2737/2849/2923). DISPATCH_LOCK is a module-private static (3228) with exactly two acquisition sites in the entire codebase (2728, 3887 — project-wide search confirms no other source file references it), so no other lock taker can invert the order, and no path holds the run-state lock across a DISPATCH_LOCK acquisition.
- **No re-entrancy, no deadlock**: nothing in the arm calls `run_all_dispatch_next` — the arm's only call under the hold is `end_run` (229-232: `*run_all = None` + `emit_backlog_changed`, neither of which touches DISPATCH_LOCK or dispatches). `finish_captured_item_done` (5063-5091, with its multi-second git commit) runs BEFORE the lock acquisition at 3864 — the hoist did not pull the commit under the dispatch hold. No caller of `on_main_turn_resolved` (the forwarder's Error/Finished arms, `try_flush_deferred_main_resolution` — all in events.rs) can hold DISPATCH_LOCK. The `_dispatch_guard` binding is scoped to the `if let Some(id)` block and drops at the arm's `return;` (3934).
- **The sequential path is unchanged**: `ours=false` → skip the wind-down entirely and return (now merely serialized — the determination is one brief run-state read; a concurrent dispatch_next blocked on DISPATCH_LOCK during it holds nothing the arm needs, so no deadlock and no meaningful delay); `ours=true` with no spawned lanes → clear + `end_run` (the round-3/4 shape); `ours=true` with spawned lanes → clear + stop flag. The inner std-Mutex order in the clear block (current_item at 3918 BEFORE spawned at 3920) matches the documented R8 invariant.
- **The comment block is updated as described** (3872-3886): cites reviews R3 + R3-H1 + R4-L1 + R5-L1, explains that the whole determination→clear→wind-down runs under DISPATCH_LOCK, why the DETERMINATION must sit inside the lock (race (a) needed the check and the clear under the same hold), that nothing in the arm calls dispatch_next, and that the guard drops at the return.

## The pin (`dispatch_decisions_are_serialized`, run_all.rs:1191-1215) → sound ✓

- **`fn_body` resolves to the right function**: the needle is composed at runtime as `format!("fn {name}(")` = `"fn on_main_turn_resolved("` — the trailing `(` is load-bearing: `fn on_main_turn_resolved_is_plan_tied(` (1857) does NOT contain that byte sequence (the char after `resolved` is `_`, not `(`), and Rust's `str::find` is an exact literal search, so `start` lands on the real function (3819). The runtime composition also keeps the needle out of the tests module's own source (the module precedes the code — the doc comment on `fn_body`, 1421-1427, documents exactly this trap).
- **The extraction covers the arm**: `end` is the first `\n}\n` (column-0 `}`) after the signature — inside a function body every brace is indented, so that is the function's own closing brace (4372). Both markers (3887, 3888) fall inside the extracted text.
- **One occurrence of each marker in the body** (verified by search): the file's other `let _dispatch_guard = DISPATCH_LOCK.lock().await;` (2728) is in `run_all_dispatch_next`, before the extraction start; the only source occurrence of `let ours = {` in the entire file is 3888 (the test's own occurrences at 1202-1213 precede the extraction). `find` therefore returns the arm's markers.
- **The comparison is sound and load-bearing**: `lock_at < ours_at` holds in the current source (3887 < 3888, and the extraction preserves byte order). A future de-hoist (moving the lock back below the determination) flips the order and fails the assert with the R5-L1 message; deleting either marker panics the `.expect` — the pin fails loudly in every regression direction, never silently. The pre-existing `body.contains(...)` assertion (dispatch_next holds the lock) is retained and still true.

## Regression sweep — nothing else moved ✓

- **The ours arm's surroundings are intact**: the R3-H1 marker comment ("R3-H1: the steered item was just resolved", 3915) survives inside the locked hold — the existing `body.contains(...)` pin (1133-1136) still matches; the pointer-less intervention fall-through (3936-3937) is unchanged; the run-all branch below (3940+) is unreached from the ours path (the arm returns at 3934); the stop-flag set (3926-3929) is a separate run-state acquisition under the lock, same order, no awaits inside the std mutexes.
- **Line-shift arithmetic proves the round-5 fix is the only change since round 5**: every anchor the round-5 report cites BEFORE the ours arm shifts by exactly +14 lines in the working tree (dispatch_next's lock 2714→2728; the fill exits 2790/2920/2982→2804/2934/2996) — precisely the 14 lines the R5-L1 pin added to the tests module, which precedes the code. The arm region itself reflects only the comment growth (~7 lines of R5-L1 explanation) plus the hoist (the lock moved from below the determination to above it). `end_run` (229-232, before the tests module) is unmoved. No other file in the change set (26 modified + 2 untracked, per `git status`) shows any round-5-era movement — the frontend files in the diff are the earlier rounds' parallel-UI work, untouched by this fix, consistent with the parent's "Rust-side only — run_all.rs" claim.
- **Multi-platform / security / docs**: the fix is pure Rust lock-ordering inside run_all.rs — no new git invocations, no platform-specific code, no user-facing surface. README.md and agent.md describe the parallel run-all feature at the user level (both updated in the feature diff, reviewed in earlier rounds); the wind-down serialization seam is internal and requires no doc change.

## Tests

Not executed by this reviewer — the reviewer tool surface is read-only (no shell). The claimed matrix (root cargo test 2131/0/4, src-tauri cargo test 273/0/0, frontend npm test 1060/1060) is the parent's to verify before commit; it is consistent with the change set: the round-5 fix is confined to run_all.rs (the hoist + comment + the pin test), the frontend files in the diff are untouched by it, and the pin test passes against the current source by inspection (verified above — `lock_at < ours_at` holds, both `.expect`s resolve). Under `#![deny(warnings)]` a green src-tauri run additionally proves the new pin compiles warning-free.
