## Verdict: PASS

Round-2 verification of plan 77768e94 ("Steering layer: memory+graph over grep at every tool-choice point") on `wt/agenticcoder`, reviewing commit 750fafa (HEAD; working tree clean — `git diff HEAD` empty). Round-1 finding L1 (low) and its optional observation were both addressed; nothing new surfaced.

## L1 — RESOLVED

**Restructure correct (statically verified, plan.rs:448-501).** `execute` now binds `let mut result = match wf.create_plan_with_kind(...)`; the `Ok(id)` arm builds the plain `ToolResult` (output string `"created plan '{title}' with {n} steps (id: {id}, kind: {kind})"` + `[branch_note]` + the unchanged `data` JSON `{plan_id, step_count, kind, branch_note}`) with NO rider inside; the `Err(e)` arm is byte-identical to before. Then `drop(wf)` at :488, and only gated on `result.success` is `recall_rider(...)` awaited and its `Option<String>` appended to `result.output` (:489-500). Success-path output is byte-identical to round-1's (rider lands in the same final position); the Err path returns the error `ToolResult` unchanged (success=false → rider skipped); `data` is never touched by the rider. No borrow/move breakage: the match produces an owned `ToolResult`, the guard is dropped by move, and only `self.memory` / `&args.*` / `bug_symptom.as_deref()` (all borrows, alive past the drop) are used after — no use of `wf` post-drop. Consistent with the reported post-fix suite (1636 passed, 0 failed, warning-free under `#![deny(warnings)]`; reviewer is read-only and could not re-run).

**Comment documents the exception per the suggested fix (plan.rs:480-487).** The comment states the rider "does store round-trips (recall_peek — embedding, potentially a REMOTE embedder call) and needs NOTHING under the workflow lock: the plan is already persisted", cites the rule ("the codebase rule is to never hold the workflow mutex across store round-trips (see abandon_plan's rationale for the same shape)"), and explicitly contrasts the branch-prep precedent ("Unlike the branch-prep awaits above (order-critical, held deliberately), the rider has no reason to keep the lock"). The cited rule exists verbatim at abandon_plan :1014 and finish :1239 ("the codebase rule: never hold a lock across an await"); the branch-prep rationale it contrasts against exists at :383-387 ("Deliberately run while holding the workflow lock: … guarantees no other plan write can land between the branch switch and this plan's persistence"). Both citations are accurate.

## Checklist re-verification

1. **L1 restructure** — PASS (above). Early-return paths before the drop (`plan_mutations_allowed` guard) hold the lock only across non-await code, unchanged from before.
2. **Drop comment** — PASS, cites the abandon_plan rule and the branch-prep rationale precisely.
3. **`miss_hint_branches_by_candidates` test (codegraph.rs)** — PASS, pins BOTH helper branches: `miss_hint("ghost", 0)` asserts the total-miss branch names the query and points at `` `search` ``; `miss_hint("ghst", 3)` asserts the near-miss branch yields "candidates' ids". The round-1 optional observation is adopted exactly as suggested.
4. **Scope of 750fafa** — PASS. Touches exactly the plan's files: codegraph.rs (miss_hint unification + tests), shell.rs (GREP_NUDGE + conservative detector + tests), plan.rs (rider + `with_memory` + tests + the L1 fix), factory.rs (wiring at the single production site, `register_workflow_tools` :777-781, gated on `self.memory`), prompt.rs (cost-model line + TOOL_STRATEGY doc comment + pinning assert), README.md + PLAN.md (docs), plus standard bookkeeping (.coding/plans/77768e94.md, DECISION knowledge record, round-1 review report, backlog item ad4090e3 → in_flight). All other `CreatePlanTool::new` sites (runtime/agent.rs, tests.rs, factory.rs:1885 — all inside test modules; `mod tests` at factory.rs:1043) keep the `None` default, per the Option design.
5. **Docs sync + multi-platform** — PASS, unchanged since round 1: README/PLAN statements match implementation (passive `recall_peek` over title+goal+symptom, no bump; deduped 5-gist cap; graph-miss → `search`; grep-family TIP; bug plans get the record_type BUG pass). No new platform-specific code; the only `#[cfg(windows)]` in shell.rs (CREATE_NO_WINDOW) is pre-existing, and the execute-level nudge tests are platform-neutral ("echo hi | grep hi" runs on both sh and PowerShell with exit code as data).

No findings. The suite (1636 tests) was reported green by the main agent post-fix; static verification of the drop placement, the gating, and the test content found no issues.
