## Verdict: FINDINGS (0 high, 2 low)

All five round-1 resolutions are implemented and verified, and the core fix re-verifies clean from the committed diff — nothing regressed. The two round-2 findings are residual documentation staleness of the classes round-1 flagged: one R10 accumulator comment that round-1's L1 enumeration missed (the Anthropic twin of a reworded site), and the prompt.rs placement docs that the DeepSeek fold made false. Both are doc-only; neither affects correctness, security, or the constitution.

### Scope reviewed

- Commit e54fb7b in full (`git show e54fb7b`, 17 files, +449/−78): PLAN.md, src/agent/{turn,tests}.rs, src/provider/{policy,stream,anthropic}.rs, src/provider/openai/{stream,tests}.rs, plus the side-cars riding the commit (knowledge ×3, plans ×4, the round-1 review, backlog.jsonl +4).
- Working tree clean at HEAD = e54fb7b (`git diff HEAD` and `git status --short` both empty).
- Sweeps beyond round-1's enumeration: every R10 mention in src/ (11 sites), every `200-char|repeats 3` hit, every `ProviderPolicy {` literal (all 8 in policy.rs — no external construction site), every is_local / Local-kind / volatile-tail placement doc, and the L2 mirror sites (runtime/agent.rs:1114-1139 read directly; turn.rs:197-224 per runtime/agent.rs's own mirror pointer).

### Round-1 resolutions — verified

**L1 — RESOLVED at the five enumerated sites; one residual missed site (finding R2-1).** All five sites round-1 listed are reworded in e54fb7b to any-period semantics: src/provider/openai/stream.rs:112-114 (accumulator) and :447-450 (call site), src/provider/anthropic.rs:1485-1488 (call site), src/provider/openai/tests.rs:61-66 (multibyte test — now describes the byte-wise scan, notes the removed char-boundary bail as history, and the positive case cites the p=2 period), and src/provider/stream.rs REPETITION_THRESHOLD's known-limitation ("ANY repeating unit ≤ 200 bytes … ~20+ identical consecutive short lines / data records"). REPETITION_WINDOW's and detect_repetition's doc comments were rewritten to match. The Anthropic R10 accumulator comment (anthropic.rs:1372-1374) still carries the old phrasing — see R2-1.

**L2 — RESOLVED as documented-accepted.** The AgentCommand::Suggestion push site in run_turn (turn.rs:~1022-1031) carries the residual note: on fold_volatile_tail vendors a text-only suggestion leaves a short system message as the request's last message after a tool call — same echo-trigger class, far narrower (one short line vs the large structured tail); per-vendor demotion is a separate change if it ever matters. The note is accurate against the code (text-only steers push `Message::system`). The two mirrors (the turn.rs summarization path at 197-224 and runtime/agent.rs:1114-1139 — read and confirmed pushing the same system message) carry no note, but the acceptance decision is recorded in three committed places (the primary-site comment, the round-1 report, the commit message), which satisfies the finding's "document the residual as accepted"; replicating the note at the mirrors is optional polish, not a gap.

**L3 — RESOLVED.** .coding/analysis/traces-diff.txt (11,479 B) and traces-summary.txt (52,632 B) exist in the clean working tree (= HEAD = e54fb7b); the path log shows only the adding commit 9d0f3e4 (no deletion commits); neither file appears in e54fb7b's changed-file list — they ride the commit unmodified; and the commit message names the restoration ("restored .coding/analysis extracts").

**L4 — RESOLVED; amendments present and accurate.** Both knowledge records carry "Amended 2027-01-07: Correction (review 2026-09-11 L4)" paragraphs: the exit-note record (47553472) and the hello-turn record (d492ac45) both state 74 bytes / 69 chars with the em-dash-as-mojibake U+00E2 U+20AC U+201D; the exit-note amendment's arithmetic ("3 multi-byte chars totaling 8 bytes" = 2+3+3) is correct, it names the two stale in-body figures that should read 74, cites the regression test's assertions, and restates the unchanged conclusion (74 ∤ 200). The plan file's context carries the 74 figure with the mojibake breakdown plus the explicit pointer that the Bug section and Detailed steps were written with the stale 71 figure. Independently re-derived from the test's unit literal: 66 ASCII chars + 3 mojibake chars = 69 chars; 66 + (2+3+3) = 74 bytes; 74 = 2·37 does not divide 200 = 2³·5². ✓

**L5 — RESOLVED.** The plan context carries: "IMPLEMENTATION NOTE (review 2026-09-11 L5): the shipped any-period scan is 1..=window — a safer superset of the planned 16..=window (sub-16 periods that don't divide 200 are equally degenerate loops; p=1 uniform runs were already caught by the old exact-window check)." The detailed step's 16..=window text remains but is flagged by the note; a future reader will not narrow the scan to match the step.

### Core fix — re-verified from the committed diff (no regression)

1. **Any-period scan (src/provider/stream.rs, detect_repetition)** — the committed code is exactly what round-1 verified: `needed == 0 || text.len() < needed` early-false; byte-wise tail; `(1..=window).any(|p| tail[p..].iter().zip(tail).all(|(a, b)| a == b))` with per-period early exit; p = window preserves the old exact-window case as a subset; no char-boundary bail (byte compare is UTF-8-safe). The regression test's boundary assertions are right: 10× unit = 740 bytes ≥ 600 fires (a suffix of a p-periodic string is p-periodic — the 600-byte window starts 140 bytes in, unit offset 66), 8× = 592 < 600 no-fire.
2. **Fold exactly scoped** — `fold_volatile_tail: true` only in deepseek(); false in the other seven registry entries; all eight ProviderPolicy literals live in policy.rs (sweep-verified — no external construction site missed the field). turn.rs: `fold_tail = is_local || ProviderPolicy::for_kind_and_model(kind, model()).fold_volatile_tail` — only OpenAI-kind + deepseek model strings change behavior; GLM/Anthropic/OpenAI/Local request shapes unchanged.
3. **Pops safe** — `if !tail_folded { messages.pop(); messages.pop(); }` after request_stream returns and before every early exit; the fold path pushed nothing trailing, so no exit path leaks the tail/footer into the persistent vec. Unchanged from round-1's verification.
4. **Cache tradeoff documented in both places** — the fold_volatile_tail doc comment (policy.rs:212-221) and PLAN.md caching-strategy item (8), both naming the accepted prefix-cache sacrifice and the otherwise-unusable-vendor rationale. ✓
5. **Both regression tests are in the commit** — `detect_repetition_fires_on_exit_note_loop_with_74_byte_period` (src/provider/openai/tests.rs; asserts 69 chars / 74 bytes on the exact trace unit, fires at 10×, no-fire at 8×) and `deepseek_vendor_folds_volatile_tail_no_trailing_system_messages` (src/agent/tests.rs; asserts tails[0] == "hi" — the request ends with the user message — head contains # WORKFLOW STATE, no CONTEXT_FOOTER anywhere, messages.len() == 3). Both exercise the changed paths.
6. **Anthropic fixture fix** — required by the generalization (the old "non-repeating" cycling alphabet is 26-periodic and would now fire): converted to a positive any-period case, with genuinely non-periodic numbered lines replacing it (verified: the differing line numbers break every candidate period). Present and correct.
7. **Test suite** — 2256 + 16 passed / 0 failed / zero warnings under deny(warnings), attested in the commit message; this reviewer is read-only and cannot run cargo (same caveat as round-1). Code inspection found no warning triggers: all eight policy literals updated, the new test helper (new_with_model_with_tails) is used, the model field is consumed by model(), no unused imports/vars in the diff, no #[allow] suppressions.

### Bug-plan checks

- Both regression tests exercise the changed paths ✓; pre-fix failure re-confirmed logically (74 ∤ 200 defeats the old exact-window compare on the 740-byte input; the old install path pushed tail+footer for a deepseek model, so tails[0] would have been the CONTEXT_FOOTER, not "hi").
- Root cause documented: BUG memory 47553472 + both knowledge files committed with the change, now carrying the corrected 74-byte figure ✓.
- The plan's Regression test field records detect_repetition_fires_on_exit_note_loop_with_74_byte_period ✓.
- The round-1 review report is committed with the change ✓.

### Constitution checks

- Multi-platform neutrality: no platform-specific code, paths, or shell syntax in the diff ✓.
- File-tools-first: no shell mutation; the .coding corrections went through memory_amend / update_plan as designed ✓.
- Docs sync: PLAN.md item (8) ✓; README.md needs nothing (no packaging internals — round-1's verification stands; no config surface changed, fold_volatile_tail is registry-fixed). The prompt.rs doc staleness is the docs-sync gap (finding R2-2).
- backlog.jsonl +4 pending items are incident follow-ups from the fresh-eyes analysis — in-scope side-car changes ✓.

### Findings

**R2-1 (low) — Anthropic R10 accumulator comment still describes the replaced exact-window semantics (L1 class, missed site).** src/provider/anthropic.rs:1372-1374: "R10 repetition-guard accumulator — tracks the response text for stuck-loop detection (same ~200-char window repeated 3× at the tail). Bounded by `bound_repetition_buffer` to ~2 KB." This is the Anthropic twin of the openai/stream.rs:112-114 accumulator comment that L1 reworded; round-1's L1 enumeration listed the anthropic.rs:1485 call site but missed the accumulator, and the parent fixed exactly the five enumerated sites. Same staleness L1 flagged: exact-window semantics plus "char" where the guard is byte-wise any-period. Fix: the same one-line reword ("when ANY repeating unit of ≤ 200 bytes spans the last 600 tail bytes"). Documentation-only; no behavior impact.

**R2-2 (low) — prompt.rs placement docs made false by the DeepSeek fold.** src/agent/prompt.rs:471-482 (build_stable_head doc): "On OpenAI-kind providers it is the only content placed in `messages[0]`" and "On Local-kind providers (Ollama) the volatile tail is folded into `messages[0]` too" — DeepSeek-vendor providers are OpenAI-kind and now fold the tail into `messages[0]` (the accepted cache tradeoff), so the first claim is false for them and the second's enumeration is incomplete. src/agent/prompt.rs:508-525 (build_volatile_tail doc): "Production appends it as a *trailing* message after the conversation history" — not universal: Local and DeepSeek-vendor fold it into the head (the Local half of this imprecision pre-dates the change; the DeepSeek half is new). Fix: one reword each naming the fold exception (fold_volatile_tail vendors + Local fold into the head; the trailing placement is the cache-optimized default). The CONTEXT_FOOTER doc (prompt.rs:447-459) can be touched in the same pass ("appended by `turn.rs` as the FINAL system message" — on the fold path it is omitted), though it describes the footer's role where used and is the weakest of the three. Documentation-only; no behavior impact.

### Checked and acceptable (no action)

- Test-fixture comments "A 200-char segment repeated 3×" (src/provider/openai/tests.rs:20, src/provider/anthropic.rs:2895): the fixtures are ASCII ("x"/"a" repeats), where 200 chars = 200 bytes — accurate as written.
- loop_impl.rs:104-115 and turn.rs:1741-1746 headroom comments describe the trailing-path scaffolding the conversation count can't see; on the fold path the tail rides the head and the same headroom absorbs it — the wording's imprecision for fold vendors predates this change (Local) and the accounting is unaffected.
- anthropic.rs cache-breakpoint comments (239-369, 2049-2323): Anthropic resolves claude() → fold_volatile_tail: false; the trailing placement they describe is unchanged for Anthropic.
- request.rs:1100-1122 (Local system-demotion sanitizer): describes the OpenAI path's trailing pushes accurately; DeepSeek-vendor requests simply carry none to demote.
- openai/stream.rs:403-406 (R10 accumulate comment): describes borrow mechanics, not detection semantics — fine.
