## Verdict: PASS

Round-2 verification of commit 575ba94 (HEAD on wt/agenticcoding; working tree completely clean — `git diff HEAD` and `git status` both empty) for plan 85368a1f "Code-graph indexing: parallel waves + parser reuse" (bug_fixing, backlog 5a85e36c). Both round-1 LOW findings are fixed correctly in the committed source, the fixes are comment-only (zero behavioral surface), the plan's REVIEW ROUND-1 DISPOSITION matches what shipped exactly, and nothing else changed post-round-1: the commit's source diff equals the round-1-reviewed diff plus precisely the three doc-comment edits in src/codegraph/mod.rs. No new findings.

## 1. Both fixes present and correct in the committed source

**LOW-1 — stale "per file" store-lock claims — FIXED.**
- `CodeGraph::store` field doc (src/codegraph/mod.rs:89-91) now reads: "Short critical sections only: `index` takes the lock once for the metadata snapshot and once per wave of upserts, and query snapshots load in two fast SELECTs." — the round-1 remediation wording, factually matching the code: one lock for the `stored_file_meta()` snapshot (mod.rs:386-389), one per wave for `upsert_files` (mod.rs:490-492), one for the final prune/stats block (mod.rs:528-537).
- `CodeGraph::index` doc (mod.rs:341-344): "The store lock is taken once for the metadata snapshot and once per wave of upserts, so concurrent readers (agent tools, the Graph tab) stay responsive between waves during a large index." — plus the requested progress-timing clarification (mod.rs:346-348): "`progress`, when given, is called after each file is processed — after its extract, before the wave's store write — with `(files_processed, files_total)`." Both stale claims are gone; a repo-wide search of src/codegraph finds no remaining stale "per file" lock claim (the surviving mentions are accurate: the replaced-old-behavior notes at mod.rs:385 and store.rs:98/222, the per-file fallback log at mod.rs:499, and the per-language parser-cache docs in extract.rs:186/250).

**LOW-2 — wave memory bound is file-count-based — RESOLVED as documented trade-off.** The `WAVE` const in `index_inner` (mod.rs:411-417) now carries the memory-knob note: "WAVE is a MEMORY knob, not just a latency knob: each entry held between the join and the upsert carries the file's full text, so a wave of 512 × ~1 MB files (the walk's size cap) transiently holds ~0.5 GB. Realistic repos are nowhere near that; if a pathological tree ever needs it, pack waves by a byte budget (~96 MB) instead of a file count (review LOW-2, plan 85368a1f)." — the amplification, the realistic-repo assessment, and the byte-budget future work, exactly per the round-1 report's own remediation menu.

## 2. The fixes introduced no regressions (comment-only, verified three ways)

- **Anchor reconciliation:** every round-1 line anchor maps onto the committed code shifted by exactly the doc edits' net line additions — the flag + `IndexFlagGuard` block (round-1 :369-370 → now :372-373, +3: store-field doc +1, index lock claim +1, progress clarification +1) and `extract_job` (round-1 :922-1020 → now :931-1029, +9: those +3 plus the WAVE note's +6), with the same 99-line span. No code line was added or removed between the anchors.
- **Diff-count reconciliation:** the commit's source diff is 561+/130− (698+/138− total minus the 137+/8− of .coding files, per the stat and the backlog.jsonl hunk headers). Round-1's quoted "559+/133−" reconciles exactly as the full tracked-diff summary at review time (source 545+/123− + backlog.jsonl 8+/8− + plans/9441d776.md 6+/2−); 561/130 = 545+16 / 123+7 — precisely the doc-edit deltas (sites 1-3 replace 7 pre-existing comment lines with 10 new ones; the WAVE note adds 6). Zero post-round-1 changes in store.rs / extract.rs / walk.rs.
- **Content verification:** every code-level claim round-1 passed re-confirmed in the committed source — the wave structure (one `stored_file_meta()` snapshot; `AtomicUsize` work index; mpsc pulses with workers only `tx.send(())`; per-worker `ParserCache`; the coordinator draining `rx` inside the scope closure so the non-`Send` callback stays on the calling thread; `sort_unstable_by_key` on unique job indices; `upsert_files` with per-file `upsert_file` fallback so one bad file cannot lose the wave); `extract_job`'s verbatim fast-path conditions (`trust_mtime && stored_mtime == Some(disk_mtime) && has_content && stored_hash.is_some()` at :953; `stored_hash != Some(hash) || !has_content` at :975), the lossy-UTF-8 source arm (:987), the strict-UTF-8 content-only arm (:1010), mtime-after-extract (:1021); the store refactor (`upsert_entry` via `prepare_cached` throughout, `upsert_file` signature unchanged, `stored_file_meta`'s correlated subquery matching `has_content`'s `COUNT(*) > 0` semantics); extract.rs (`ParserCache` with the contains_key+get_mut NLL shape, `extract_file` thin wrapper, `Ctx.parsers` field, `visit_html`'s script-block sub-parse reusing `ctx.parsers` at :1140-1154); walk.rs (`Hash` in `Lang`'s derive at :31); and both tests (`large_fixture_indexes_on_worker_threads` at :1708-1768 with the load-immune `workers > 1` guard skipped on 1-core, `files_scanned`/`files_reindexed` == 500, DB outside the fixture root; the `#[ignore]d repo_index_timing` at :1778).

Test evidence: `cargo test codegraph` re-run green after the fixes (108 passed, 0 failed) per the task statement and the plan's disposition; this read-only reviewer cannot re-run cargo itself, but comment-only changes cannot alter behavior, and the recorded run is consistent with that.

## 3. Round-1 disposition in the plan matches what shipped

.coding/plans/85368a1f.md, REVIEW ROUND-1 DISPOSITION (line 30): LOW-1 "FIXED: both reworded to the per-snapshot/per-wave locking, plus the progress-timing clarification (called after each file's extract, before the wave's store write)" — matches mod.rs:89-91 and 341-348 exactly. LOW-2 "RESOLVED AS DOCUMENTED TRADE-OFF per the reviewer's own remediation menu: the WAVE const now carries the memory-knob note (512 × ~1 MB files ≈ ~0.5 GB transient; realistic repos nowhere near; byte-budget packing ~96 MB is the future-work hardening)" — matches mod.rs:412-417, including the ~96 MB figure. The justification for not implementing the byte budget now (per-file stat pass on the coordinator; untestable byte path without exposing wave internals; the pathological case absent from realistic repos; bounded and freed between waves) is recorded in the same section, and the commit message's round-1 note ("both fixed (stale per-file lock doc claims; the WAVE memory-knob note)") is accurate.

## 4. Nothing else in 575ba94 regressed relative to the round-1-reviewed tree

- The working tree is completely clean — not even .coding/ bookkeeping pending.
- 575ba94 is HEAD; the only commit between round-1's base and it (6309326, the 9441d776 round-2 review landing) touched only .coding/backlog.jsonl + a review report — no source; `git log -- src/codegraph` confirms 575ba94 is the only commit touching src/codegraph since the all-language work (9180626).
- Per the reconciliations in §2, the commit's source diff = the round-1-reviewed source diff + the three doc-comment edits in src/codegraph/mod.rs (store-field doc; index-doc lock reword + progress clarification; WAVE memory-knob note), nothing more. The .coding/ additions in the commit (plan file, BUG knowledge file + amendment, round-1 report, 9441d776 bookkeeping riding along per the side-car policy) are exactly what round-1 already saw as untracked/modified.

## Bug-plan checks (per the closing-sequence requirements)
- **Regression test exercises the changed path:** yes — `large_fixture_indexes_on_worker_threads` drives 500 mixed-language files (.rs/.ts/.py/.html-with-script-blocks) through `graph.index(None)` — the actual wave path (scope, workers, `upsert_files`) — and asserts the load-immune parallelism observable (`stats.workers > 1`, skipped on 1-core) plus exact file counts; red on a sequential implementation (workers would be 0), green after.
- **Root cause documented:** yes — plan 85368a1f (context + LANDED DESIGN AMENDMENT with deviations, measured numbers, and invariants) and .coding/knowledge/bug/2027-01-11-code-graph-indexing-really-slow-sequential-loop.md with the post-verification amendment (test rename + measurements).
- **BUG: memory written:** yes — semantic memory 12527cd7 + the knowledge file above.

## Security
The round-2 delta is comment-only: no new input surfaces, no SQL, threading, or path changes since round-1 (which passed security). No findings.
