## Verdict: FINDINGS (0 high, 1 low)

Round-2 re-review of plan c6cb69f7 ("Fix DeepSeek reasoning_content 400, third recurrence — tail-owner escape") on wt/mnemo, after the round-1 report (.coding/reviews/2026-09-12-deepseek-400-third-recurrence-review.md, 0 high / 2 low) was fixed by the main agent. Scope: all uncommitted changes (git diff HEAD, incl. untracked). Read-only review — static verification only, per the round-1 methodology note; the reported matrix (root cargo test 2306+16 passed; mnemo-app 297+4+2+2 passed, 0 failed) is assumed green and no test was found that cannot pass.

**Both round-1 lows are fixed correctly and completely — verified by reading the code, the tests, and the knowledge records:**

- **LOW 1 (stale contract docs) — FIXED in every named location.** `ReasoningRetention` doc (policy.rs:78-91) and `deepseek()` doc (policy.rs:409-414) now state the widened contract (key on age-0 tail-owner AND every older tool-call turn; only the TEXT is droppable; re-added on stripped tool-call turns); `fingerprint_prefix` doc (request.rs:218-231) describes the re-add MODE (0/1/2, flip 2→1) exactly as `readd_mode` (request.rs:300-306) computes it; the synthetic branch comments (request.rs:617-649, 650-665) describe the re-add → strip → conditional bare re-add ordering; `echo_assistant_raw`'s doc (request.rs:1116-1151) carries the recurrence context, the Gemini exclusion, and the strip-interplay; the test header at tests.rs:1737-1742 is widened. The SPEC knowledge record (2026-12-23-deepseek-tail-turn-reasoning-content-guarantee-i.md) carries two dated memory_amend paragraphs — the widened contract and a follow-up typo correction ("milk-turn" → "mid-turn"); the amended text matches the code and the tests on every claim (value split age-0 structured / age≥1 bare ""; strip re-add on both paths; shared `needs_reasoning_readd`; fingerprint MODE 0/1/2; both test names). The typo itself remains in the first amendment's text but is explicitly flagged by the correction paragraph — acceptable amendment hygiene.
- **LOW 2 (synthetic path) — FIXED.** request.rs:658-665 re-adds the bare "" key AFTER the retention strip, gated on `role == Assistant && policy.reasoning_required && policy.reasoning_field == Some("reasoning_content") && !m.tool_calls.is_empty() && obj.get("reasoning_content").is_none()` — the exact shape round 1 suggested, mirroring the raw-echo path. Ordering is sound: the pre-strip unconditional re-add (631-633) always sets the key; `strip_reasoning_text_fields` (mod.rs:786-802) removes it with `map.remove("reasoning_content")`, so the post-strip `is_none()` check fires exactly when the strip ran; for text-only turns `!m.tool_calls.is_empty()` excludes them; NEVER_STRIP providers never strip so the key is present pre-strip and the block no-ops. The vendor gate is strict: policy census (policy.rs) shows DeepSeek (418/420) is the ONLY provider with `reasoning_required: true` + `reasoning_field: Some("reasoning_content")` — Qwen/Kimi/GLM/vLLM are all `reasoning_required: false` (NEVER_STRIP), Gemini is `reasoning_field: None` — so the block is DeepSeek-only by construction and cannot regress Qwen's never-strip behavior (`retention_qwen_synthetic_path_never_strips` unchanged and still consistent). The new pinning test `retention_deepseek_synthetic_tool_call_turn_keeps_bare_key_under_pressure` (tests.rs:2574-2602) genuinely triggers the strip (filler = 5× `PROXY_CACHE_CEILING_TOKENS`, same magnitude as the provably-stripping sibling test at 2569, same retention; age-1 tool-call turn, keep_recent=1 ⇒ `strips_reasoning(1, true)` = true) and would fail without the hardening (strip leaves the turn keyless ⇒ the `== ""` assertion fails) — it pins the hardened behavior, not a no-op. Age-0 text preservation asserted. ToolCall/PROXY_CACHE_CEILING_TOKENS imports pre-exist in the module — the test compiles.

**Verified-pass checklist (per spawn instructions):**

1. **Core fix unchanged and consistent (round-1 content):** shared `needs_reasoning_readd` (request.rs:261-274) drives both `echo_assistant_raw` (1159-1183) and `fingerprint_prefix` (289-308); raw-echo path re-adds on age-0 OR tool-call turns, AND when the strip is about to remove the key (age-0 `strips_reasoning` protected unconditionally, policy.rs:134-145); value split age-0 structured / age≥1 ""; synthetic path unconditional re-add + post-strip bare re-add; fingerprint hashes the MODE byte (0/1/2), so a splice can never preserve an age-0 serialization. Fingerprint↔echo equivalence re-checked: for synthetic messages (raw=None ⇒ mode 0) the emitted form is a pure function of already-hashed fields (tool_calls, reasoning_content, needs_strip byte, under_pressure) — no divergence introduced by the round-2 hardening.
2. **Regression test for the recurrence:** `deepseek_keys_every_tool_call_turn_after_cross_vendor_reentry` (tests.rs:1831-1926) asserts on the serialized wire body for the exact incident shape (stripped keyless two-turn tool-call chain + DeepSeek + reasoning_effort "max"); messages[1] (age-1 tool-call echo) must carry "" — fails under the old age-0-only gate; text-only historical boundary pinned unkeyed in the same test. Updated tests (`deepseek_tail_owner_keyed_when_system_follows_tool_results` assert "" on the age-1 tool-call echo; `prefix_cache_rebuilds_when_the_readd_ages_out` asserts mode-flip rebuild with hit_count 0) are consistent with the corrected contract.
3. **Root cause documented:** plan context (".coding/plans/c6cb69f7.md" ROOT CAUSE ESTABLISHED: freshness ruled out by binary mtimes, tail-owner provably keyed, cross-vendor Rule-5 strip discriminator, census 72/78 and 79/85 keyless, conclusion + honest inferential residual) and the BUG memory (.coding/knowledge/bug/2027-01-11-deepseek-reasoning-content-400-3rd-recurrence-cr.md: symptom → root cause → fix → regression-test name). No fake fix. Note (as round 1): the plan's `regression_test` field must still be recorded via update_plan at step 4 (step 4 is unchecked — the main agent's closing-sequence duty, not a changeset defect).
4. **deny(warnings):** no `#[allow(...)]` anywhere in the diff; both new helpers used by production paths (echo + fingerprint); new block/test use pre-existing imports. Static check only.
5. **Multi-platform / file-tools / docs-sync:** pure Rust, no platform-specific APIs; all .coding/ mutations via sanctioned tools (memory_amend for the SPEC); README/PLAN correctly judged as needing no update (internal wire-contract change, front-door convention SPEC 2027-01-11). The ride-along .coding/ amendments (backlog item 969bc37d, README-front-door SPEC) are legitimate and unrelated.
6. **Matrix:** not re-executed (read-only); reported numbers accepted per instructions; static review found no test in the diff that cannot pass.

## Findings

**LOW 1 — Three December-contract sentences remain outside the round-1-named locations (same staleness class as round-1 LOW 1).**
- tests.rs:1748-1750 — the inner annotation in the CHANGED test `deepseek_tail_owner_keyed_when_system_follows_tool_results` directly contradicts its own updated assertion: the comment says the age-1 foreign-fallback turn "tolerate[s] a missing key (probes A/D/G2) and must NOT get a fabricated one", while the body (assert_eq at ~1813) asserts that exact turn carries the bare `""` key. The header (1737-1742) was widened, this line below it was missed.
- tests.rs:1643-1644 — background comment in `raw_echo_injects_reasoning_content_when_required_but_absent`: "Historical assistant turns tolerate a missing key (probes A/D/G2) — only the most recent assistant turn (age 0) is validated." Stale: historical TOOL-CALL turns are no longer within the tolerance.
- mod.rs:192-201 — `Message::reasoning_content` field doc: "Historical assistant turns tolerate a missing key" and "the builder additionally guarantees the key on the tail turn (age 0)" — under-describes the widened guarantee (age-0 AND every tool-call turn) and over-states the historical tolerance. Documentation-only, zero functional impact; fix is a one-line amendment of each (scope to TEXT-ONLY turns / mention the tool-call re-add).
