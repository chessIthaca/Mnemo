## Verdict: FINDINGS (0 high, 2 low)

Review of all uncommitted changes for plan c6cb69f7 (bug_fixing, "Fix DeepSeek reasoning_content 400, third recurrence — tail-owner escape, live 2026-09-12") on wt/mnemo. Scope: src/provider/openai/request.rs (needs_reasoning_readd / raw_has_tool_calls / fingerprint MODE / echo value split), src/provider/openai/tests.rs (new regression test + two updated tests), ride-along .coding/backlog.jsonl item 969bc37d and amended SPEC knowledge file. Methodology note: reviewer is read-only — no shell, so `cargo test` was NOT executed here; correctness is verified statically (all vendor policies, strip/readd ordering, fingerprint↔echo consistency). The agent's step-4 matrix run is still pending (plan step 4 unchecked), which the closing sequence must complete.

## Findings

**LOW 1 — Contract docs + SPEC memory still describe the December age-0-only contract.**
- `src/provider/policy.rs:76-86` (ReasoningRetention struct doc): "DeepSeek validates only the request TAIL … while older turns tolerate a missing key entirely (live-verified 2026-12-23…, c9b5cbe4)" — now stale: historical TOOL-CALL turns no longer tolerate a missing key; only text-only historical turns do. This is the canonical in-code contract doc and now contradicts the fix.
- `src/provider/policy.rs:404-407` (deepseek() doc): "older turns tolerate dropping it" — same staleness: the KEY must survive on tool-call turns; only the text is droppable.
- `.coding/knowledge/spec/2026-12-23-deepseek-tail-turn-reasoning-content-guarantee-i.md` (memory e010881d) is still live and states "DeepSeek validates ONLY the request tail … historical assistant turns tolerate a missing key entirely". The new BUG memory (2027-01-11) documents the widened contract but does not supersede/amend the old SPEC — per memory hygiene (supersede, never leave both live) and the bug-fix doc expectations, the SPEC should be amended with a dated paragraph (or superseded) noting tool-call turns must be keyed at any age.
- Minor, same bucket: `fingerprint_prefix`'s doc (request.rs:218-229) still describes "the age-0 reasoning_content re-add" without the MODE byte (corrected inline at the readd_mode site); the header comment of `deepseek_tail_owner_keyed_when_system_follows_tool_results` (tests.rs:1738-1740) still says "historical turns must stay within the missing-key tolerance" although its own updated body (1810-1819) widens it.
- README.md / PLAN.md need NO update, judged: internal wire-contract change, no user-facing feature/config surface; the front-door convention (SPEC 2027-01-11) routes such detail into code docs + knowledge records, which is where it lives.

**LOW 2 — The widened guarantee does not cover the synthetic path under pressure.**
In `echo_assistant_raw` the readd fires after the strip, so a tool-call turn can never end up keyless (verified: strip⇒readd always co-fires for tool-call raws; age 0 is strip-protected). But on the SYNTHETIC path (no raw / unusable raw → field construction), the unconditional `reasoning_content` re-add (request.rs:628-631) is applied BEFORE the retention strip (637-645), so a synthetic assistant turn carrying `tool_calls` at age ≥ 1 under DeepSeek + pressure gets its re-added key stripped — exactly the keyless-tool-call-turn shape the new guarantee is meant to preclude. Reach is narrow: real tool-call turns always have raws (the fallthrough needs a missing/corrupted raw, H2/H3), so no production shape hits it today — and this ordering is pre-existing and documented (request.rs:632-635) — but the new doc on `raw_has_tool_calls` (request.rs:234-235) claims the synthetic path "keys assistant turns unconditionally," which is only true pre-strip. Suggested one-line hardening: when the strip applies after the synthetic re-add, re-add the bare `""` key for turns with non-empty tool_calls (mirroring the echo path), so the invariant "every wire tool-call turn carries the key under a requires-rc policy" holds on both paths. If intentionally out of scope, amend the doc claim instead.

## Verified-pass checklist (per spawn instructions)

1. **Vendor gate** — `needs_reasoning_readd` fires only for DeepSeek. Policy census (policy.rs): claude (reasoning_field=None), gemini (reasoning_field=None, excluded by field check — `raw_echo_not_injected_for_gemini_plain_turn` still pins the byte-identical echo), openai_responses (reasoning_required=false), qwen/kimi/glm/vllm_default (reasoning_required=false), deepseek (true + Some("reasoning_content")). No vendor receives a fabricated key its contract forbids; the behavior delta vs the previous bool gate is DeepSeek-only.
2. **Strip interplay** — Raw-echo path: exhaustive. (a) age≥1 tool-call + key present + no strip → borrowed, key kept; (b) key present + strip ⇒ strip clause fires readd → bare `""` after strip (token savings preserved; probe T2 accepts ""); (c) key absent ± strip → readd; (d) age 0: strip never runs (`strips_reasoning` protects age 0 unconditionally, policy.rs:130), so the tail-owner branch is byte-identical to the live-verified December behavior when the key is already present (readd=false, borrowed). Text-only historical turns keep the December tolerance (unkeyed) — matches probes A/D/G2. Only the synthetic under-pressure corner remains (LOW 2).
3. **Fingerprint MODE** — Sound. The emitted echo is a function of exactly (hash_message ⊇ raw, reasoning_content, tool_calls, content) + needs_strip byte + readd_mode byte: mode 0 = borrowed/left-as-is, 2 = age-0 structured value (also covers `unwrap_or_default` → ""; the value source m.reasoning_content is hashed), 1 = bare "". Mode 2→1 flips exactly at age 0→1 for keyless tool-call turns (the case whose emitted byte would otherwise diverge); age-0 key-present turns are mode 0 on both sides of the age flip with identical emitted bytes, so no spurious rebuild is missed; pressure already in the fold header (under_pressure byte). A stale splice can never diverge from a fresh build (conservative extra misses only — `prefix_cache_rebuilds_when_the_readd_ages_out` pins hit_count 0).
4. **Regression test** — `deepseek_keys_every_tool_call_turn_after_cross_vendor_reentry` asserts on the serialized wire body; the incident shape (stripped keyless two-turn tool-call chain, DeepSeek + reasoning_effort "max"); messages[1] (age-1 tool-call echo) must carry "" — under the OLD gate (age==0 only) that assertion fails, under the fix it passes. Boundary pinned: the text-only historical raw in the same test stays unkeyed. Both updated tests (`deepseek_tail_owner_keyed_when_system_follows_tool_results`, `prefix_cache_rebuilds_when_the_readd_ages_out`) are consistent with the corrected contract (bare key at age 1; rebuild-vs-cold equality + hit_count 0).
5. **Docs** — module/function docs for the new predicate and the echo were rewritten thoroughly and honestly (recurrence context, strip interplay, Gemini exclusion). Stale bits above (LOW 1). README/PLAN: no update needed (judgment stated).
6. **deny(warnings)** — No `#[allow(...)]` anywhere in the diff; both new helpers are used by production paths (echo + fingerprint), no dead code, no unused imports. Static check only: `cargo test` must still be run by the agent at step 4 (unpiped, read `test result:` lines — green suite == zero warnings).
7. **Multi-platform / file-tools** — Pure Rust, zero platform-specific APIs or syntax; nothing Windows-only. No shell-based file mutation in the diff (file tools only). The ride-along .coding/ edits are legitimate.
8. **bug_fixing extras** — Root cause documented twice: plan context (ROOT CAUSE ESTABLISHED / DISCRIMINATOR / CENSUS / CONCLUSION — freshness ruled out by binary mtimes, tail-owner provably keyed, cross-vendor Rule-5 strip identified) and the BUG memory file (.coding/knowledge/bug/2027-01-11-deepseek-reasoning-content-400-3rd-recurrence-cr.md, untracked, symptom→root-cause→fix→test-name). Regression test named in the BUG record. No fake fix: strict-superset behavior with the API-side mechanism honestly marked inferential and the live-turn acceptance stated as residual (user relaunch). Noted for step 4: the `regression_test` plan field must still be recorded via update_plan (finish gate).

Fix the two lows, re-run the matrix, commit.
