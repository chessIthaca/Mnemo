## Verdict: PASS

Round-2 verification of plan 9441d776 at HEAD 363fb9d (wt/agenticcoding, clean tree — the commit is the tree): all three round-1 low findings are genuinely fixed and verified in code at HEAD; every round-1 "verified correct" item still holds; the bug-plan checks pass (root cause documented + amended, red→green regression test, restart-survival acceptance); the project-specific expectations (docs sync, multi-platform, file-tools-first) are met. No new findings — two cosmetic notes, both below the finding bar.

### Round-1 fix verification

**L1 (BUG record accuracy) — FIXED and verified.** `.coding/knowledge/bug/2027-01-11-bug-fix-plans-lose-sub-step-progress-across-rest.md:8` carries the dated "AMENDED 2027-01-11 (review L1, plan 9441d776 round-1)" paragraph via the memory_amend pattern (original preserved, explicitly superseded). Every mechanism claim in the amendment was checked against HEAD and is accurate: `detailed_steps: Option<String>` is the single on-disk truth (plan_file.rs:127); the tick is line surgery in `PlanFile::complete_detailed_step` (plan_file.rs:388-470) with the raw-text rebuild (428-438); legacy plain-dash bodies normalize on the first tick (397-415); marks round-trip serialize→parse verbatim (serialize writes the raw body at 327-329, parse accumulates raw lines at 285-293 — pinned by `complete_detailed_step_ticks_the_nth_checkbox`); `Workflow::complete_detailed_step` (mod.rs:843-853) neither transitions state nor calls persist_stack; the create_plan render escapes `- `-leading continuation lines (plan.rs:592-617). A future reader grepping for `Vec<Step>` in the detailed-steps path now finds the amendment explaining the refinement.

**L2 (tick mutated text beyond the mark) — FIXED and verified.** plan_file.rs:428-438 rebuilds the ticked line from the RAW text after `] `: `trimmed.strip_prefix("- [")` → `rest.split_once(']')` → `after.strip_prefix(' ').unwrap_or(after)` — exactly ONE leading space stripped, so `format!("{indent}- [x] {raw_text}")` reproduces the original spacing. Byte-level traces: `- [ ] 2. Guard the unwrap` → `- [x] 2. Guard the unwrap` (the "2. " survives); `- [ ]   spaced text` (3 spaces) → `- [x]   spaced text` (3 spaces — only the mark char differs). Coverage of `parse_checkbox`'s accepted forms (plan_file.rs:517-547: mark ∈ {space, x, X}, requires `] ` + text or bare `]`): the mark is never `]`, so `split_once` always lands on the checkbox closer, and a text containing `]`/`[...]` is untouched (first-`]` split); the `unwrap_or_default()` arm is unreachable on a counted line (defensive only); indent is preserved via the ASCII-whitespace-safe `split_at` (the stripped prefix is ASCII whitespace, so the byte index is a char boundary). The method's doc comment (367-387) states the invariant, and the new test `complete_detailed_step_preserves_the_raw_text_beyond_the_mark` (1014-1030) asserts both corners exactly (`- [x] 2. Guard the unwrap`, `- [x]   spaced text`).

**L3 (plain-dash continuations became sub-steps) — FIXED and verified.** plan.rs:587-622: after the `- [ ] {s}` bullets are joined, an escape pass keeps lines starting `- [ ] ` and prefixes `\` at the bullet position (indent preserved) on any OTHER line whose trimmed form starts with `- `. Verified interplay: a step "Fix — two edits:\n- guard X\n- add Y" renders one checkbox + `\- guard X` / `\- add Y`; the escaped lines are invisible to the tick (`parse_checkbox` requires `- [`) and to the legacy normalizer (trimmed starts `\-`, not `- `), and round-trip verbatim through serialize→parse, so repeated ticks stay stable; `escape_section_markers` (applied after, :622) targets only `## `/`# `-trimmed lines — no double-escape, no interference; indented nested bullets are escaped too; a `- [link](url)` continuation is escaped (better than round-1's note required); a continuation quoting a literal `- [x] ` line is escaped as well — only the literal `- [ ] ` quote at column 0 remains countable, exactly the residual documented in the method's doc comment (plan_file.rs:382-385). The new test `multi_line_step_continuations_do_not_become_sub_steps` (plan.rs test module) pins it end-to-end: tick 1 → "1/2", the nested bullets appear as `\- ...` in the plan file, the second step untouched. Legacy files (pre-checkable renders) still normalize all plain `- ` lines on first tick — round-1's fix direction (b) explicitly leaves normalization to genuinely-legacy files, and the tool surfaces the true count.

### Round-1 "verified correct" items — re-verified at HEAD

1. **Raw-body line-surgery design** (plan_file.rs:388-470) — unchanged in shape: absent-section error, normalization passes checkbox/non-bullet lines through, positional nth flip, idempotent re-tick (`- [x]` → `- [x]`), out-of-range error carries the true count, and `*body = lines.join` runs only after the `ok_or_else` (445-452) so a failed tick leaves the in-memory body unmutated; preview truncation char-safe (`chars().take(57)` + `…`).
2. **No-state-transition tick** (mod.rs:835-853) — stack.last_mut → plan method → write_to_dir only; no state change, no persist_stack; the tool layer's `plan_mutations_allowed` gate (plan.rs:1136-1141) covers the detailed path too.
3. **Exactly-one validation** (plan.rs:1087-1105) — distinct actionable errors for both/neither; per-param zero rejection (1116-1134); the detailed branch returns (1165-1185) before the skeleton range pre-validation, so the unused `step_index` on that path is inert.
4. **Shared `complete_step_plan_id_guard`** (plan.rs:996-1031, used at 1150-1161) — behavior-identical extraction (same trim, ≥4-char unique-prefix rule, same error text with `what` = "step {n}" / "detailed step {n}", prefix-match note returned to both paths).
5. **Restart-survival acceptance** — `bug_fixing_detailed_step_ticks_survive_restart`: mid-fix state (skeleton 1-2 done, sub-steps 1-2 ticked) → fresh `Workflow::new` + `load_latest` → skeleton progress AND marks re-derived from disk → resume ticks 3/3 and completes skeleton 3-4 → Reviewing, no redo.
6. **Budget ceilings** (factory.rs budget test) — Executing 28_800→29_500, PlanFrozen 29_900→30_600, ExecutingResearch 23_600→24_300, each with a dated deliberate-raise comment naming plan 9441d776, the ~+584 schema growth, and the workspace-unified measurements (29_037 / 30_149 / 23_826) — matching the reported green run under workspace feature unification.

### Bug-plan checks

- **Root cause documented** — BUG record 128fa7d1 + the committed knowledge file; the root-cause half was already accurate, and the L1 amendment makes the Fix half accurate too. ✓
- **Regression test red→green** — `create_plan_bug_fixing_renders_detailed_steps_as_checkable_substeps` asserts `- [ ] ` immediately before each detail text — guaranteed red under the old plain-dash render. ✓
- **Acceptance** — `bug_fixing_detailed_step_ticks_survive_restart` (item 5 above). ✓

### Project-specific review expectations

1. **Documentation sync** — PLAN.md:420-429 and README.md:36 carry the checkable-sub-steps wording (text immutable, marks tickable via `complete_step detailed_step_index`); the 77ff8f45 spec record carries the dated amendment superseding "immutable like the skeleton"; the BUG record carries the L1 amendment. A repo-wide `Detailed steps` sweep (75 matches, 32 files) found no LIVE description still claiming the section is immutable-as-a-whole or plain-text: prompt.rs:90/600/712/1726 (untouched by this commit) never claimed it; every plan.rs/mod.rs/plan_file.rs doc comment and schema description now says checkable sub-items. Historical artifacts (the 4405d82d plan file, the 2026-09-08 review reports) correctly remain untouched. ✓
2. **Multi-platform neutrality** — the delta is pure Rust string manipulation + std::fs + tempdir in tests; no platform APIs, paths, or shell syntax. ✓
3. **File-tools-first** — no shell-based mutation anywhere in the delta; both knowledge-record changes are dated amendment paragraphs (the memory_amend pattern). ✓
4. **Round-1 notes** — the `.coding/backlog.jsonl` bookkeeping (51ee41c1 checkpoint note + user-reported 486955d5 / 5a85e36c) rides the commit and is documented in the commit message — expected under the union-merge side-car policy; the PLAN.md wrap-width cosmetic remains as-is (markdown-irrelevant; the original line was already past wrap). ✓

### Notes (non-findings)

- Cosmetic: the bare empty-text checkbox `- [ ]` (no text after `] ` — hand-edited only; the render always emits text) ticks to `- [x] ` — one trailing space added to an empty step. Parse-equivalent (round-trips as done + empty text), unreachable from the render, below the finding bar.
- The documented residual ambiguity (a step text quoting a literal `- [ ] ` checkbox line at column 0 counts as its own sub-step) is noted in `complete_detailed_step`'s doc comment — accepted per the round-1 fix framing.
- This reviewer is read-only: test claims were verified by reading the code and tests at HEAD, consistent with the reported green matrix (cargo test detailed_step 12 passed + multi_line_step 1; full workspace 2318+16+297+4+2+2+0 passed, 0 failed, 18+3 ignored, warning-free under `#![deny(warnings)]`).
- The commit lands on wt/agenticcoding (never main), includes the round-1 review report, and its message documents the backlog bookkeeping — the closing-sequence requirements are satisfied.

### Scope reviewed

`git show 363fb9d` in full (11 files, +760/−54) plus direct reads at HEAD: src/workflow/plan_file.rs (complete — parse/serialize, `complete_detailed_step`, `parse_checkbox`, `strip_leading_number`, all tests), src/workflow/mod.rs:820-879, src/tool/workflow/plan.rs:575-659 + 1080-1239 (render, exactly-one validation, shared guard, detailed tick path) and the five new tests via the commit diff, src/agent/factory.rs budget ceilings via the commit diff, PLAN.md/README.md bullets, both knowledge-record amendments, .coding/plans/9441d776.md, .coding/backlog.jsonl, and the round-1 report. Byte-level traces of the L2 rebuild and the L3 escape interplay (normalizer, `parse_checkbox`, `escape_section_markers`, round-trip).
