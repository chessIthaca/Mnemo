## Verdict: FINDINGS (0 high, 4 low)

Plan 041dd8d0 "Steer feature-scale bug fixes to implementation plans" (backlog 51ee41c1), branch wt/agenticcoding — reviewed ALL uncommitted changes: `src/agent/prompt.rs` (PLAN KIND feature-scale carve-out + new stable-head test), `src/tool/workflow/plan.rs` (`referenced_module_dirs` helper, `feature_scale` detection, advisory steering note, kind-schema amendment, 3 new tests), plus expected `.coding` bookkeeping (backlog.jsonl 51ee41c1 pending→in_flight flip with plan_id; untracked plan file `.coding/plans/041dd8d0.md`).

The implementation is correct, matches the plan's documented design (AND semantics, advisory-only note, tuple 5th element with a false else-arm), and is multi-platform neutral (pure Rust string handling). All four literals the existing `stable_head_carries_bug_fixing_habit` test asserts are preserved verbatim, and the new test's three assertions match the amended text. The 4 findings are documentation-sync gaps (PLAN.md restates the superseded always-rule; README's plan-kind bullet lacks the carve-out; the run-all steer text contradicts the new guidance) and one test-coverage gap (the AND heuristic's two legs are not independently pinned — the plan's own must-not-warn case, the 9441d776 shape, is untested).

## Verified against the review checklist

**1. Prompt amendment (prompt.rs :82-94).** The carve-out is unambiguous: "EXCEPT bug-triggered FEATURES (the fix adds capabilities, new dependencies, or spans multiple modules): file those as kind:\"implementation\" with the bug documented as motivation in goal/context; bug_fixing is for CONTAINED defect fixes." The EXCEPT clause resolves to the kind choice — the "file those as kind:\"implementation\"" instruction disambiguates it from the preceding BUG:-capture clause it follows — and the three feature-scale criteria are concrete and checkable. All four literals asserted by the existing `stable_head_carries_bug_fixing_habit` (:1388-1410) are preserved verbatim in the amended text: `kind:\"bug_fixing\"` (:84), `never \"implementation\"` (:84-85), `bug:{symptom}` (:86), and `BUG: memory auto-captured at finish` (:86 — the em-dash EXCEPT continuation keeps the substring intact). The new `stable_head_carries_feature_scale_carve_out` (:1413-1435) asserts exactly three substrings, all present in the amended text: "bug-triggered FEATURES", "bug_fixing is for CONTAINED", "documented as motivation in goal/context". No other test pins the PLAN KIND text (searched).

**2. Heuristic (plan.rs :609-622).** `feature_scale = detail_texts.len() > 4 && referenced_module_dirs(detail_texts + context + goal) >= 3` — the AND is deliberate and the false-positive profile holds: the contained cross-module fix (9441d776: 4 steps, 3 modules) fails the step leg (4 > 4 is false); the long single-module fix (85368a1f: 4 steps, 1 module) fails both legs; the c87093c1 shape (5+ steps, 3+ modules) trips both. Detection reads all the payload's text (detail_texts + args.context + args.goal, :617-621). `referenced_module_dirs` (:238-261) edge cases all verified: `src/` at end of text → `after` is empty, no `/` follows, nothing counted, no panic (idx+4 is a char boundary — "src/" is ASCII — so multibyte text is safe); bare `src/lib.rs` → no second `/` → counts none; `src-tauri/src/ipc/...` and `frontend/src/components/...` match via the inner `src/` (the "src-" in "src-tauri" is not "src/"); same-module repeats dedupe via the HashSet; the scan advances past each match (`rest = after`) so the loop always terminates. Cross-tree module-name collisions (e.g. `src/agent/` + `frontend/src/agent/`) count once — conservative (under-counts), acceptable for an advisory heuristic. The borrow of `detail_texts` inside the `&& { … }` block ends before the later `into_iter` move (:626) — no borrow conflict.

**3. Advisory design (plan.rs :803-817).** The note is a string appended to the successful create_plan output — no ask_user, no error, `success` stays true, so unattended run-alls cannot stall on it. The plan still files as bug_fixing when the note fires: the note is appended after `create_plan_with_kind_and_detail` returns Ok, and the new test asserts `wf.plan().kind == PlanKind::BugFixing` alongside the NOTE assertions. The note text is actionable: it names the alternative (kind=implementation), where the bug goes (documented as motivation in goal/context), the scope rule (bug_fixing is for contained defect fixes), and the detection signals; "Advisory: your call." makes the non-blocking intent explicit.

**4. Tuple extension (plan.rs :590, :663-671).** Both arms of the if-expression carry 5 elements; the else (non-bug_fixing) arm returns `false` for feature_scale (:671), so the note can only ever fire for bug_fixing payloads, and the destructuring binding means no path leaves it unset. The note lives only in the Ok(id) arm — a failed plan creation returns the error without it. feature_scale is computed before detail_texts is consumed and is not persisted to the plan file (creation-time advisory only) — consistent with the design.

**5. Docs sync** — findings LOW-1/LOW-2/LOW-3 below. The new helper's doc comment (:238-247) accurately describes the counting semantics (verified against the code); the amended kind-schema description (:527) matches the prompt carve-out; the CreatePlanArgs.kind doc (:77-80) and the plan.rs module doc (:5-23) do not state the always-rule, so they need no change; agent.md carries no plan-kind rules (verified — Environment/Code style/File mutation/Branch policy/Review expectations only).

**6. Multi-platform neutrality.** Pure Rust string handling (`str::find`, ASCII slicing, HashSet) — no platform APIs, paths, or shell syntax. Clean.

**7. Test coverage.** The three new tests meaningfully pin the helper (4 unit cases: same-module dedup, bare src/ → 0, inner-src/ of src-tauri + frontend → 2, repeats → 1), the fire path (5 steps + 3 modules → NOTE present, kind=implementation named, plan still bug_fixing), and one silent path (4 steps, 1 module → no NOTE). Gap: the AND's legs are not independently pinned — LOW-4.
