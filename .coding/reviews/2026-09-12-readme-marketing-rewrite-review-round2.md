## Verdict: FINDINGS (0 high, 1 low)

Round-2 verification of plan 2c19aca0 ("Rewrite README as a marketing front door + move deep detail to docs/") on `wt/mnemo`, working tree vs HEAD `3217714`. All nine round-1 findings are verifiably fixed at their exact sites — the three malformed H2 headings, the false `cargo test` scope comment plus the 2,625 proof figure, the broken docs/FEATURES.md image path, and LOWs 1-6 — and the fix edits introduced no new markdown defect and did not disturb the preserved docs/ content beyond the two sanctioned deviations. One residual LOW remains, in the same file-hygiene class round-1 LOW 6 addressed: trailing blank line(s) at EOF in both docs files — docs/CONFIGURATION.md does NOT end with the "single trailing newline" the fix description claims (it ends with the last paragraph plus one blank line). Trivial to fix; detail in the sections below.
### Findings — LOW

**LOW R2-1 — docs/CONFIGURATION.md:16 and docs/FEATURES.md:85-86: trailing blank line(s) at EOF; docs/CONFIGURATION.md's claimed "single trailing newline" end state is not what is on disk.**

- docs/CONFIGURATION.md reads as 16 lines with its last content line at L15 (the per-project-state paragraph) — the file ends with that paragraph plus one blank line. Provenance is exact: the round-1 state was 18 lines (L1 blank copy-trim artifact, H1 + intro, content, L17 blank, L18 the stray `Copyright (c) 2026 Carsten Hess.`); the fix removed exactly those two lines (18 − 2 = 16), so old L17's blank survived as the new final line. The stray copyright itself is gone — the substance of round-1 LOW 2 is fixed (a literal `Copyright` search over docs/ returns zero matches) — but the file's tail is the paragraph followed by a blank line, not the single trailing newline the fix description asserts. (Line-count calibration: README.md reads as 176 lines with L176 the last content line and the git diff confirms a single trailing newline — so the reader counts no phantom line after a final newline; a 16-line file whose content stops at L15 therefore has a real trailing blank line.)
- docs/FEATURES.md reads as 86 lines with its last content line at L84 — two trailing blank lines (L85-86). The described FEATURES edits (remove the leading blank line; change the L76 img attribute in place) account for a one-line delta only, so the trailing blanks predate or at latest accompanied the leading-line removal; either way they are uncleaned EOF residue.
- Both are invisible in rendered markdown (markdownlint MD047 class — a file should end with a single newline) and are the same hygiene class round-1 LOW 6 addressed, whose fix was applied selectively (leading lines, README newline). Not flagged by round 1 and not introduced by the round-2 fix edits themselves — flagged now because (a) the LOW 2 fix description asserts an end state that is verifiably false for docs/CONFIGURATION.md, and (b) the hygiene pass should finish before commit.
- Fix: one targeted edit per file — delete the trailing blank line(s) so each docs file ends with exactly one newline after its final paragraph. README.md needs nothing: 176 lines, L176 is the License line, single trailing newline confirmed (no missing-trailing-newline marker in the diff).
- After this two-line cleanup, every round-1 finding and this residual are closed; nothing else in the diff needs to change.
### Round-1 findings — fix verification (all nine confirmed)

**HIGH 1 — FIXED.** README.md:36 `## Highlights`, :67 `## How the repository works`, :143 `## Development & tests` — the space is restored in all three, and every heading in the file is now a well-formed ATX heading. All nine `---` rule lines (L16, 34, 50, 65, 100, 141, 155, 166, 172) are standalone and separated from the following heading by a blank line — no `---##` fusions anywhere. Headings unique: `# Mnemo` plus nine H2s (Why Mnemo exists / Highlights / The enforced workflow / How the repository works / Quickstart — clone & build / Development & tests / Where the detail lives / Proof, not promises / License).

**HIGH 2 — FIXED.** README.md:146-147 read exactly:
- `cargo test                 # the mnemo library crate`
- `cargo test -p mnemo-app    # the desktop shell app crate`

Accurate (round 1 established that plain `cargo test` at the root selects only the `mnemo` package; src-tauri is covered by the second command) and matching the standing test matrix; the frontend lines beneath are unchanged and correct. Proof line (L170) now carries "2,625 Rust + 1,100 frontend tests" — internally consistent: 2,320 (library, root run: 2,304 passed + 5 ignored on one target, 16 passed on the other) + 305 (mnemo-app, measured 297+4+2+2) = 2,625 exactly; the as-of date "September 2026" matches the 2026-09-12 measurement set; the 1,100 frontend figure is unchanged from round 1. Basis note, recorded not as a finding: the 2,625 sums the library's *passing* count with the app crate's *measured total*; the README says "tests", not "passing tests", so nothing false is asserted — this is the consciously prescribed basis.

**HIGH 3 — FIXED.** docs/FEATURES.md:76 now reads `<img src="../assets/workflow.svg" alt="Mnemo workflow: Planning → Executing → Reviewing → Complete, with back-edges for review findings and abandoning a plan">` — resolves from docs/ to the root `assets/workflow.svg`; the `../` is exactly the sanctioned 3-byte deviation and the only change inside the preserved range. README.md:55 keeps the root-relative `assets/workflow.svg` (correct from the root). The surrounding `<p align="center">` block (L75-77) is intact.

**LOW 1 — FIXED.** README.md:131: `| `npm start bundle` | produce platform installers (`.msi`/`.nsis` on Windows, `.app`/`.dmg` on macOS) under `target/release/bundle` |` — the exact recommended platform-neutral phrasing; the row is still two clean table cells with no stray pipes.

**LOW 2 — FIXED in substance.** The stray `Copyright (c) 2026 Carsten Hess.` is gone (literal `Copyright` search over docs/: zero matches); the file is 16 lines and its last content is the per-project-state paragraph. Residual trailing blank line → LOW R2-1 above.

**LOW 3 — FIXED.** README.md:153: "Two heavyweight optional cargo features exist on the library — `browser` (headless-browser tooling) and `embeddings` (local ONNX memory search) — plus a dev-only `test-support` feature for cross-crate test factories. The two heavyweight ones are off by default for light builds; the app always selects both." Accurate per round 1's Cargo.toml verification (browser/embeddings heavyweight and off by default; the app selects both; test-support dev-only via src-tauri's dev-dependency).

**LOW 4 — FIXED.** Both sites rewritten as recommended: L32 "…with every change leaving a plan, an independent review, and a test suite behind." and L170 "every change leaves a plan, an independent review, and a test suite behind." The universal quantifier over ~210,000 lines is gone; both claims are now checkable against the `.coding/` archive itself.

**LOW 5 — FIXED.** README.md:30: "The spec-loop, enforced end to end: **spec → plan → code → test → review → commit**, with every artifact persisted — not remembered." Matches the enforced closing sequence (tests run before the review, commit last) and the preserved docs/FEATURES.md:81 text ("the test matrix runs, a read-only reviewer subagent audits the full diff").

**LOW 6 — FIXED as claimed, with a residue.** README.md now ends with a trailing newline (176 lines, L176 the License line; the diff carries no missing-trailing-newline marker — round 1 saw one). Both docs files start at their H1 on line 1 (leading blank removed). Residual trailing blanks → LOW R2-1.
### No-new-defects scan, preservation re-check, and context

- **Docs-only confirmed.** `git status`: ` M README.md` plus untracked `docs/`, the plan file `.coding/plans/2c19aca0.md`, the round-1 report, and one new knowledge file (`.coding/knowledge/how/2027-01-11-repairing-fused-markdown-rule-heading-lines-keep.md` — the documented HIGH 1 repair approach: keep the rule lines, restore the space). Zero source-code changes; the session's green matrix (2,320 lib / 305 app / tsc clean / 1,100 frontend, all 2026-09-12) still describes the tree — nothing in this diff can affect it, so no test re-run is required for a docs-only change.
- **Content preservation — still intact.** docs/FEATURES.md L5-84 = old README L30-109, line-for-line (80 lines): `## Key ideas` (7 bullets + the "A chat window can suggest…" close), `## Key features` (Workflow & safety ×6, Memory ×9, Code intelligence ×4, Agents & interface ×27), `## The enforced workflow` (the `<p>`/img block, numbered items 1-4 in their LONG forms, the closing paragraph). Every line was compared against the diff's removed old-README text, including heads and tails of the multi-KB lines (steering bullet ends `…zero-hit browses say "no matches" instead of rendering bare`; backlog bullet ends `…the manual resume/rollback anchor`; the hang-watchdog, memory typed-records, FTS5, multi-agent, and parallel run-all lines all match at both ends). The only in-range deviation is the L76 img `../` (sanctioned). Line arithmetic corroborates that no other line was touched.
- docs/CONFIGURATION.md L5-15 = the old `## Configuration` heading + five paragraphs byte-exact (Global config / Models inside an endpoint / MCP servers / MCP two-scope / Per-project state; line tails `…migrated from ~/.myharness automatically on first launch.`, `…resolution is by config, never by model-name prefix.`, `…omit it to keep connections until they fail).`, `…declined with a standard JSON-RPC error so servers never hang.`, `…deleted files self-heal on the next init).` all verified). 18 → 16 lines = exactly the two described deletions (leading blank + stray copyright), nothing else.
- **Markdown well-formedness across all three files.** README: four code fences all opened and closed (L69-92 anatomy, L117-122 clone, L135-139 tauri, L145-151 tests); both tables well-formed (Highlights L38-46 — 2 columns, `|---|---|` separator, 7 rows, no stray pipes; launcher L126-131 — 4 rows, the fixed row two clean cells); the `#proof-not-promises` anchor matches the `## Proof, not promises` heading; every relative link resolves (docs/FEATURES.md, docs/CONFIGURATION.md, PLAN.md, agent.md, both vendor PATCHES.md, LICENSE, assets/workflow.svg); bold and code spans balanced at every edited site. docs/FEATURES.md: three H2s (Key ideas / Key features / The enforced workflow) well-formed and unique; docs/CONFIGURATION.md: H1 + `## Configuration` clean; all four cross-links (`../README.md`, `./CONFIGURATION.md`, `./FEATURES.md`) resolve. No fused lines, double-blank artifacts, or stray text at any edit site beyond LOW R2-1's EOF blanks.
- **Intentional deviations, still in force (not findings):** the clone URL remains the placeholder `https://github.com/<owner>/mnemo` (README.md:118) — open item for the final summary/publishing; docs/FEATURES.md:39 keeps the old "(Rust + TypeScript/TSX)" graph-language claim byte-exact while README.md:42 says 12 languages (the user's verbatim-preserve choice).

Note on method: the reviewer cannot run shell — all verification is textual (read_files over the working tree, the full uncommitted diff vs HEAD `3217714` via git_read, literal searches). Test figures cited are the parent's measured evidence, cross-checked for internal consistency only.