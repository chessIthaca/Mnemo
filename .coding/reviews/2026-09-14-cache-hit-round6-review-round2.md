## Verdict: FINDINGS (0 high, 3 low)

**Scope.** Round-2 verification of commit `f1a5809` (HEAD of `wt/mnemo`; working tree clean — `git diff HEAD` and `git status --short` both empty, `.coding/instance.json` gitignored as expected) against the four round-1 findings in `.coding/reviews/2026-09-14-cache-hit-round6-review.md`. All four fixes are real and land their primary claims; each leaves a small residual, documented below. No high findings — the source fix itself is correct, complete, and comment/move-only relative to what round 1 reviewed.

**Summary.** HIGH-1's structural fix is fully delivered (doc block contiguous with the pub fn, helper relocated below it with its own doc incl. the multipart/image rule, hysteresis paragraphs rewritten to the truncatable semantics) — but one pre-existing sentence in the relocated block (rule 1, lines 926-927) still describes the OLD intact-window gate. LOW-1's guard+comment fix is correct and measurably effective (the regenerated B2 deltas shifted by exactly the now-counted intra-message bytes) with no verdict flips. LOW-2's table/per-model/caveat/snapshot-paragraph fixes match the committed aggregates exactly, but the Instruments and Artifacts header lines still carry pre-fix numbers (972 rows / 20 sessions; "250 lines" vs 247). LOW-3's placeholder/banner/rows-line fixes landed in the script, but the docstring still omits B2/B3, one comment still names a nonexistent section "D", and the committed aggregates were generated by an intermediate script version — their rows line still prints "sections B2/C/D share the same set" where the committed script prints "B2/C/E/F", so the artifact is not byte-reproducible from the committed script.

---

## Round-1 fix verification

### HIGH-1 — doc block orphaned: FIXED, one residual sentence (→ Finding 1)

All five required checks pass in `src/agent/context.rs`:

- The doc block ending `…[`COMPACTED_MARKER`] and never contains it, so idempotency is unaffected.` (line 948) is immediately followed by `pub fn compact_old_tool_results(` (949) — contiguous again.
- The helper now sits BELOW the function (fn ends 1028); its own doc (1030-1041) is immediately followed by `fn tool_result_is_truncatable(` (1042).
- The hysteresis section (896-911) describes the TRUNCATABLE population — "a hysteresis high-water mark over the TRUNCATABLE population", "cuts all but the newest `keep` truncatable results" — with the round-6 rationale (904-911: shorts are refused unmarked, 60+ shorts held the gate open, 70-78% vs 99% measured). No "intact (un-truncated)" gate description remains in that paragraph.
- The helper's doc carries the multipart/image rule: "a multipart result without an image part is refused — while one that carries an image is always truncatable, because the payload (a base64 `data:` URL) IS the size" (1033-1035).
- **Comment/move-only confirmed.** The body is semantically identical to the state round 1 reviewed, verified line-by-line against round-1's description: `effective_high = keep_high.max(keep)` (955); the intact-collect filter identical (960-973); the truncatable filter + strict-`>` gate (984-991); `to_compact = &truncatable_indices[..len - keep]` (993); helper called with `summary_chars` (987); `truncate_tool_result_at` with `summary_chars` (995); the oversize pass identical (1005-1025); return count (1027). The helper body matches round-1's description exactly (text rule 1044-1046; parts: no-ImageUrl → false 1047-1053; combined-text marker check 1054-1061). Arithmetic consistent with round-1's line numbers: old block 885-938 (54 lines) + helper 939-971 → pub fn at 973; new block 885-948 (64 lines, +10 from the rewritten hysteresis paragraph) → pub fn at 949 — everything else in the block byte-identical to the reviewed state.

→ **Finding 1 (low):** rule 1 of the "Two rules" list (926-927) is stale — see Findings.

### LOW-1 — B2 dead refinement: FIXED, verdicts unchanged (verified effective)

- Guard is now `if k < len(pm) and k < len(cm):` (extract.py:363) — correct: `pm` is the message list; with `k ≈ 271-306` and `prev` the trace RECORD dict (~10 keys), the old guard never fired.
- The comment (356-362) is accurate: it names the dead-refinement consequence and the estimator bias correctly — the numerator `chars_before` sums only message content up to `k`, while the calibration denominator (351) adds `len(canon(ctools))`, so `pre` runs ~13-16K low on 30-tool prompts at the measured 0.64-0.70 tokens/char, and "a NO verdict can be an estimator artifact — see the report, which rests only on the YES pairs" matches the report's caveat (report lines 55-57).
- **The fix demonstrably runs and no verdict flipped.** Round-1's aggregates had NO deltas +15,699 / +14,361 on pairs 1→2 / 2→3; the committed aggregates show +15,367 / +14,038 — differences of 332 and 323 tokens, exactly `byte_off × ratio` for those pairs (517×0.641≈331, 506×0.639≈323 — the refinement's `chars_before += j`). YES still holds on 3→4 / 4→5 (aggregates 83-84), which is all the report's causal claim rests on; the 5→6 YES and the 8→9 / 9→10 NOs are unchanged in class.

### LOW-2 — report ↔ aggregates drift: table fixed exactly, header lines residual (→ Finding 2)

Verified against the committed `cache-hit-6-aggregates.txt`, exact matches:

- Table: 1,043 rows (120) / 1,037 main-loop rows (124) / 79.0% (130) / resets 17.6% (130) / avg prompt 127K (131,709,658 / 1,037 from line 130) / traced window 25 rows (222-246) 63.5% (247). ✓
- Per-model rows: all six match (deepseek-v4-flash 83.7% n=454, :0731 74.7% n=270, v4.1-flash 71.7% n=152, glm-5.3-flash 78.7% n=130, glm-5.3 79.7% n=19, kimi-k3 42.4% n=12 — aggregates 135-140). ✓
- The "Snapshot, not truth" paragraph is present and honest: attributes the table to the aggregates run (1,043 / 21), says the DB keeps accumulating "including from the very session that wrote this report", names the 972-row earlier draft, and says a re-run WILL differ. ✓
- Caveat row count updated to 1,043 / 21 (115-116). ✓

→ **Finding 2 (low):** the Instruments and Artifacts header lines still cite pre-fix numbers — see Findings.

### LOW-3 — extractor bookkeeping: mostly fixed, partially delivered (→ Finding 3)

Landed:

- The dead `(pending)` placeholder is gone: the script tail (561-641) emits the filled E block once (561-604), then F (606-636), then writes OUTPATH; no `(pending)` or "(filled by…)" string anywhere.
- The regenerated aggregates carry exactly one C header (118) and one E header (174); no `(pending)` anywhere.
- The B3 banner is its own comment above the B3 section (386-390); the C banner sits above the C section (426-430); the A/B/B2/E banners are all aligned with their sections.

Not landed (see Finding 3): the docstring still omits B2/B3; one comment still references section "D"; the committed artifact predates the bookkeeping edits.

---

## Findings

### Finding 1 (low) — one stale sentence in the relocated doc block still describes the old gate

`src/agent/context.rs:926-927`, rule 1 of the "Two rules run in this order" list:

> 1. The hysteresis pass above — cut results back to `summary_chars` once the **intact window passes the high-water mark**. Byte-identical while under it.

This asserts cut ⟸ intact > high — precisely the implication the fix removed: in the measured session intact is 68-71 > 20 on every request while the post-fix function must no-op while truncatable ≤ 20 (aggregates B3). It contradicts the rewritten hysteresis section in the same doc block (896-911) and the code (991: gate on `truncatable_indices.len() > effective_high`). This is the exact class the round-2 instructions said to hunt ("any sentence still describing the old gate"). It is pre-existing text missed by the relocation (the block's only change was the +10-line hysteresis paragraph). Doc-only, no behavior impact, no agent.md violation (the pub fn has its doc comment). Fix: "once the TRUNCATABLE population passes the high-water mark", mirroring 896-902.

Note (checked, not a finding): the inline comment at 975-977 ("no-op while the intact window is below or at the high-water mark") is one-directional and true as stated (intact ≤ high ⟹ truncatable ≤ high ⟹ no-op), is immediately corrected by the 979-983 paragraph, and round 1 explicitly reviewed it as accurate — a different statement than 926-927, which asserts the cut trigger (the false direction).

### Finding 2 (low) — LOW-2 residuals: report header lines still cite pre-fix numbers

- **Instruments (report 4-6):** still says request_stats "(972 rows, 20 sessions, 2026-09-12..14)". The artifact it cites says 1,043 rows / 21 sessions (aggregates 120/123). The "Snapshot, not truth" paragraph explains the TABLE's 972→1,043 history, but the Instruments header still presents 972/20 as the instrument's size, unqualified and inconsistent with the table in the same document (20 ≠ 21). Refresh to 1,043 / 21, or label it as the report-authoring-time snapshot.
- **Artifacts (report 7-8):** cites the aggregates as "(250 lines, sections A–F)"; the committed artifact is 247 lines. Should say 247 — or drop the line count, which drifts on every re-run by design.

Everything the fix scoped (headline table, per-model rows, snapshot paragraph, caveats) matches the artifact exactly; these are the two remaining number-vs-artifact mismatches of the same class, on lines the fix didn't touch.

### Finding 3 (low) — LOW-3 partially delivered; the committed aggregates are not the committed script's output

- **Docstring still omits B2/B3** (extract.py 13-23): "Sections:" lists A/B/C/E/F while the output emits A/B/B2/B3/C/E/F (aggregates headers at 2/31/78/90/118/174/217). The claimed fix "docstring lists sections A/B/B2/B3/C/E/F" is not delivered for B2/B3.
- **extract.py:104-105** still says "sections A/B2/C/D all need it" — "D" is a section that no longer exists (and A doesn't use `rows`; the real sharers are B2/C/E/F, as line 431 now correctly says).
- **Artifact ↔ script mismatch:** the committed `cache-hit-6-aggregates.txt:120` prints `rows: 1043 (loaded up-front — sections B2/C/D share the same set)` while the committed script (431) prints `sections B2/C/E/F share the same set`. The B2 deltas (+15,367/+14,038, exactly byte_off×ratio above) prove the artifact WAS generated with the B2 guard fix — i.e. by an intermediate script version (post-LOW-1, pre-bookkeeping) — and it was never regenerated after the bookkeeping edits. The data is sound, but the artifact still carries the stale "D" reference LOW-3 targeted, and re-running the committed script yields a different line than the committed artifact (so it is not byte-reproducible from the script, contra "its generated cache-hit-6-aggregates.txt").

---

## Round-2 duties — checked

- **Commit completeness:** all 8 claimed files in the stat — `src/agent/context.rs`, the three `.coding/analysis/cache-hit-6-*` files, the BUG knowledge record, both plan records (`31231504`, `c43ad4a3`), and the round-1 review report. ✓
- **Tree clean at HEAD:** `git diff HEAD` and `git status --short` both empty; `f1a5809` is HEAD. As the task notes, this round verifies the COMMIT, so the no-uncommitted-changes statement is expected and correct. ✓
- **`cargo test` 2329 passed / 0 failed / 5 ignored:** I cannot execute it (the reviewer toolset is read-only, no shell). Verified consistent by construction: the src delta relative to the round-1-reviewed state — which round 1 verified against that exact green run — is comment/doc movement only (checked line-by-line above), the regression test `short_results_do_not_hold_the_gate_open` (context.rs:2244-2289, calling `compact_old_tool_results`) is untouched, and doc-only edits cannot change `cargo test` outcomes. ✓
- **No `#[allow(...)]`:** zero matches in `src/agent/context.rs` (the commit's only Rust file). ✓
- **Warning-free by construction:** under `#![deny(warnings)]` the green run proves it; statically, the relocated helper is still called (987), the moved doc block is well-formed, and there are no new imports or items. ✓
- **Multi-platform neutrality:** pure Rust string/index arithmetic; the extractor is stdlib-only with relative paths and read-only sqlite URIs (`mode=ro`, lines 106/613). No `cfg(windows)`, no Windows paths, no shell syntax. ✓
- **File-tools-first:** no shell mutation in the change set; the script's only write is its own OUTPATH aggregates file (638-639) — the sanctioned GENERATED-artifact pattern (round-5 precedent, accepted in round 1). ✓
- **Documentation sync:** no README/PLAN.md update owed (internal compaction mechanics, no user-facing surface). The BUG knowledge record is the documentation and its claims check out against code and aggregates (intact 68-71 vs keep_high 20, 60-63 unmarkable shorts, 7-8 truncatable = B3 lines 101-110; B2 YES pairs 3→4/4→5; R6-2 flagged as a user decision, not silently changed). Its pre-fix line citations (`context.rs:939`, `829-851`) and the aggregates E section's line numbers are historical evidence describing the measured pre-fix layout — accurate as written. ✓
- **Report shorthand checked, not flagged:** the headline "traced window (03:01–03:02:30)" label is the trace-log window while the 25 rows span the padded request_stats window (02:59:14..03:04:30 + pad, disclosed in the F header); the numbers match the artifact exactly (25 rows / 63.5%), which was LOW-2's success criterion.

---

## Notes for the fixer

Three small fixes, all comment/doc/artifact-level — no logic, no test impact:

1. `src/agent/context.rs:926-927`: "intact window" → "TRUNCATABLE population" (mirror 896-902's wording).
2. Report: Instruments line → 1,043 rows / 21 sessions (or label it as the authoring-time snapshot); "250 lines" → 247 (or drop the count).
3. extract.py: add B2/B3 to the docstring's Sections list; fix the 104-105 comment ("A/B2/C/D" → "B2/C/E/F"); re-run the script so the committed aggregates match the committed script (the rows line will then print B2/C/E/F; expect slightly grown row counts — the report's snapshot paragraph already covers that).