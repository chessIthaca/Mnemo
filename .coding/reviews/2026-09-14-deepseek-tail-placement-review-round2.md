## Verdict: PASS

Round-2 verification of commit **bbfd822** (parent 48679bd confirmed via log, branch `wt/mnemo`, working tree clean; plan 506b85e2, bug_fixing). Change set: 13 files, +547/−230 — the DeepSeek volatile-tail placement flip (tail + CONTEXT_FOOTER as trailing USER messages via `ProviderPolicy::tail_as_user_messages`, decided once in `AgentLoop::tail_is_user_role`), the four round-1 fixes, the plan/review/knowledge records, and PLAN.md sync. **All four round-1 findings are CLOSED in the committed code; no new finding of substance introduced.** Live DeepSeek acceptance (>=95% cached on check-off rows, no echo) remains **PENDING** an app restart and is correctly framed as pending everywhere it is claimed — this verdict does not rest on it.

## Round-1 findings — closure verification

**R1 — CLOSED.** `src/provider/policy.rs:537-540` (`deepseek_policy_detected_by_model_id`): the comment above `assert!(p.tail_as_user_messages)` now reads "DeepSeek echoes trailing system blocks, so its requests carry the volatile tail + footer as trailing USER messages (2027-01-11 exit-note loop) and keep messages[0] byte-stable." — describes the user-role trailing placement; no fold claimed as current.

**R2 — CLOSED.** `src/agent/tests.rs` CapturingProvider docs, all three spots reworded: :2506-2508 (`model` field) → "vendor-policy packaging branches (e.g. the DeepSeek `tail_as_user_messages` path)"; :2524-2527 (`new_local_with_tails`) → "tail + footer as trailing USER messages, no trailing system block" (the factually false "folded into the leading system message, no footer" claim is gone); :2532-2534 (`new_with_model_with_tails`) → "DeepSeek's `tail_as_user_messages`".

**R3 — CLOSED.** `src/provider/openai/request.rs:383-395`: the trailing-run exclusion is now sentinel-keyed — `trailing_transient` = `2.min(messages.len())` when the LAST message's text equals `prompt::CONTEXT_FOOTER` (the transient pair, any role), else the old role-keyed system-run count retained as a defensive fallback for footer-less hand-built shapes; `cacheable_len = messages.len() - trailing_transient`. The comment (:372-382) states the role dependence, why the sentinel is required (a role-keyed count would leave the user-role pair inside the cached prefix and force a full rebuild every iteration), and the "real user turn never equals the sentinel" rationale. Sibling docs updated with it: `PrefixCache.len` (:68-74) now says "located by the footer sentinel, not by role: it is a SYSTEM pair on most vendors but a USER pair on `ProviderPolicy::tail_as_user_messages` vendors"; `sanitize_local_messages` (:1207-1215) enumerates the user-role variants via the flag + `AgentLoop::tail_is_user_role`, leaving the index-1 summary as the remaining Local/DeepSeek system producer. Edge cases checked: no panic on an empty list (`last()` → None → fallback count 0 → `cacheable_len` 0) or a 1-message list whose only message is the footer (`2.min(1)` = 1 → len 0); `trailing_transient` ≤ `messages.len()` on every branch, so the subtraction never underflows; a genuine trailing user turn stays cacheable (its text can only match the sentinel byte-for-byte, and in production `install_system_messages` always pushes the pair after it, so the sentinel branch fires on exactly the two transient messages); correctness is fingerprint-guarded regardless (the cached prefix is revalidated over the current `messages[0..len]`), so a mis-window can only cost a miss, never stale reuse. Note: where a persistent suggestion system message previously sat directly under the role-counted trailing run, the old code over-excluded it from the cacheable prefix; the sentinel keying now includes it — correct (it is persistent), a strict improvement.

**R4 — CLOSED.** `src/agent/turn.rs:1052-1053` (`handle_pending_swap`): the text-only suggestion routes through `Self::push_suggestion_message(messages, &pending.provider, &text)` — keyed to the SWAP TARGET, with the comment (:1046-1051) explaining that the post-swap requests are served by the new vendor and that a user-role suggestion is acceptable to every vendor even if the swap ends up interrupted. The helper (turn.rs:2251-2261) applies the single `tail_is_user_role` decision (:2234-2243). The other two sites key to the provider that will serve the next request: the summarization re-injection (~turn.rs:2007 per the commit diff, `&provider`) and runtime/agent.rs:1152-155 (`&turn_provider`).
## (a) No new findings introduced by the four fixes

- R1/R2 are comment-only; R3 changes only the harness-side single-slot PrefixCache window (the provider-side cache — the thing this fix targets — is untouched); R4 is a one-argument keying change. All four sit inside crates covered by the attested green suites.
- Compile/type consistency: `matches!(messages.last(), Some(m) if m.content.as_text() == …)` is a valid guard pattern; `2.min(messages.len())` is type-consistent usize; `push_suggestion_message` takes `&Arc<dyn LlmClient>` and all three call sites pass compatible snapshots (`&pending.provider`, `&provider`, `&turn_provider`). Root suite green (2338) under `#![deny(warnings)]` attests zero warnings.
- The role decision remains single-point: `tail_is_user_role` (turn.rs:2234-2243) is the only `(kind, model)` → role lookup; it feeds `install_system_messages` (tail/footer role) and `push_suggestion_message` (all three suggestion sites). The diff removed the last duplicated inline `is_local || …` computation; round-1's exhaustive sweep of production system-message constructions stands, and the caller now pops twice unconditionally right after `request_stream` returns (same order as before — before the hard-stop early return and the `stream_result?`).
- R4 corner re-checked: swapping AWAY from a user-role vendor with the swap interrupted (old provider keeps serving while the suggestion rode SYSTEM per the target's policy) yields a mid-conversation — never trailing, tail+footer always follow before the next request — system block on the old DeepSeek vendor: the same accepted shape class as the pre-existing index-1 compaction summary, at most one prefix re-bill under DeepSeek's system-hoisting template, bounded by the R10 guard. Narrow (swap + interruption + buffered text-only steer) and consistent with round-1's prescribed fix and its stated safety rationale. Observation, not a finding.
- The sentinel's hardcoded `2` is tied to the documented two-message transient pair (SPEC invariant 2: footer is always the FINAL message, pinned by `captured_tails` asserts in all three placement tests). If a future change ever pushed a third transient message after the footer, this window would need updating — noted as an invariant to preserve, not a defect.

## (b) `fold_volatile_tail` sweep — CLEAN

Repo-wide search: 37 matches in 14 files, ALL under `.coding/knowledge/**` (6: 3 bug + 3 decision records), `.coding/plans/**` (3), `.coding/reviews/**` (5) — historical records only, correctly left as history per the hygiene policy. **Zero hits in `src/`, `src-tauri/`, PLAN.md, README.md, or docs/*.md.** Remaining `[Ff]old` mentions in `src/` are either the unrelated command-`fold` machinery (`StopReason::fold`, loop_impl.rs/turn.rs), "scaffolding" substring matches, or accurate past-tense references in the new docs ("Folding the tail in here (the previous strategy…)" — prompt.rs:492, turn.rs:2272/2396, tests.rs 2824/2887/2923/3272) and negative assertions ("must not be folded into the leading system message"). None describes the fold as current.

## (c) Change set vs. knowledge records — CONSISTENT

- SPEC `2027-01-11-prompt-block-placement-messages-0-is-the-cache-p.md`: the architecture (messages[0] = stable head; last two = transient pair; role rule via the flag + `tail_is_user_role`) and invariant (4) — "the harness-side PrefixCache excludes the trailing pair by the FOOTER SENTINEL, not by role (`request.rs`, `trailing_transient`/`cacheable_len`)" — match the committed code at request.rs:372-395 exactly. The three named regression tests all exist in tests.rs (renamed per the diff: `deepseek_head_stays_byte_stable_across_plan_progress_bumps` ~:2921, `deepseek_vendor_tail_rides_as_user_messages_no_trailing_system_block` ~:2819, `local_provider_tail_rides_as_user_messages_no_trailing_system_block` ~:3204); the known-gap paragraph (no user-tail variant of the reasoning re-add contract test) honestly matches round-1's observation.
- DECISION `2027-01-11-volatility-at-the-end-*.md`: single decision point, the three suggestion-steer sites, the renamed flag, and the measured numbers all match the code.
- Both amended bug records (`…fold-volatile-tail.md`, `…sentinel-mirror-context-footer-echo-mit.md`): dated 2027-01-11 amendments record the superseded fold, the new placement, and PENDING LIVE CONFIRMATION with the correct rollback (revert the commit — "the flag alone now only picks user vs system role"). PLAN.md:188-200 tells the same story.
- Finish-time reminder (not a code finding, outside the committed set): the semantic memory index still carries the pre-amendment digest "BUG: DeepSeek sentinel-mirror … — MITIGATED by fold_volatile_tail" (memory.db is gitignored and derives from record titles); the parent's step-4 finish actions (SPEC memory_write + BUG-record amend) should supersede/update that digest so the pointer-first gist matches the amended file.

## Constitution checks

- **Documentation sync:** PLAN.md rewritten ✓; prompt.rs/policy.rs/turn.rs/request.rs docs updated alongside the code ✓; knowledge records created/amended ✓; README.md / docs/CONFIGURATION.md / docs/FEATURES.md need nothing (round-1 verified no stale mentions; R1-R4 touch none of them) ✓. The "Landed design —" context paragraph and `landed_design: yes` are committed in `.coding/plans/506b85e2.md` (:20, :44), satisfying the finish-gate precondition at the file level; the SPEC memory_write remains a finish-time action per the plan.
- **Multi-platform neutrality:** pure Rust message-role/cache-window logic; no platform APIs, paths, or shell syntax. ✓
- **File-tools-first:** the commit contains no shell-based file mutation; the only shell use in the plan was the sanctioned read-only regeneration of the repo's own generated analysis artifact (round-1 check stands; the file shows no diff). ✓
- **Security:** no new surface — the sentinel comparison is against a compile-time constant; a user message matching it byte-for-byte could at worst widen the transient window by one pair, costing a cache miss (fingerprint-validated, never stale reuse); suggestion content/wording unchanged, only its role. ✓

## Test status + live acceptance

- This reviewer is read-only and re-ran nothing. Parent attests: root `cargo test` 2338 passed, `src-tauri` 299+12, frontend 1107 in 82 files, `npm run build` green. The four fixes are comment-only (R1/R2), a cache-window computation (R3), and a one-argument keying change (R4) — the attested suites cover all touched code; `#![deny(warnings)]` makes green a zero-warning proof.
- **Live DeepSeek acceptance is PENDING and must stay pending after finish**: >=95% cached on check-off rows + no echo/exit-note loop requires a rebuilt app + a real session. The commit message, PLAN.md, the plan, and both amended bug records all frame it as pending with revert-the-commit as the rollback. This PASS covers the code and its pinned contracts, not the live behavior.

**Verdict rationale:** all four round-1 findings verified closed at exact file:line; the R3/R4 fixes are behaviorally sound under the edge cases examined; nothing new of substance found.