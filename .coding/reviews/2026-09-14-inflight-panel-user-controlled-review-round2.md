## Verdict: PASS

**Round-1 findings: BOTH RESOLVED.** L1 (the drag-handle persistence was not actually pinned) and L2 (`docs/FEATURES.md` called the drag handle a *toggle*) are fixed, and I verified both fixes at the level they operate on (exact source substrings): the strengthened assertions genuinely bite, the count assertion is not tripped by the `useState` destructure, and the new doc wording matches the code exactly. No new findings; three observations, no action required.

**Goal re-confirmed:** the reasoning/activity panel's open/closed state is the USER's choice alone — chevron/bar click (`frontend/src/components/chat/InflightBar.tsx:252`) or starting a drag on the collapsed handle (`:200-202`); the choice persists in `localStorage` key `mh.inflightPanelOpen`; absent/garbage → CLOSED; no code path expands it.

**Reviewed (all uncommitted, `git_read op="diff"`, never truncated):** `frontend/src/components/chat/InflightBar.tsx`, `InflightBar.test.ts`, `frontend/src/lib/inflightPanelPref.ts` (new, untracked), `inflightPanelPref.test.ts` (new, untracked), `frontend/src/lib/reasoningPanel.ts` + `.test.ts` (deleted), `frontend/vitest.config.ts`, `docs/FEATURES.md`; the round-1 report (untracked) and the rest of `.coding/**` bookkeeping excluded as instructed.

### FIX 1 — resolved, and the assertions bite for BOTH call sites

`InflightBar.test.ts:180-196` now asserts, per the round-1 suggestion, plus the original substring:

| Assertion | Test line | Matches in component source |
|---|---|---|
| `toContain("onClick={() => setPanel(!expanded)}")` | `:186` | `InflightBar.tsx:252` (only hit) |
| `toContain("setPanel(true)")` | `:187` | `InflightBar.tsx:201` (only hit) |
| `toContain("writeInflightPanelOpen(")` | `:188` | `InflightBar.tsx:10` (import) + `:106` (helper body) |
| `not.toContain("setExpanded(true)")` | `:194` | zero such strings in the source |
| `/setExpanded\(/g` count `toHaveLength(1)` | `:195` | exactly one call: `InflightBar.tsx:105` |

**Bite test (revert analysis, exact-substring matched):**
- Reverting `startDrag`'s collapsed branch (`InflightBar.tsx:201`) to `setExpanded(true)` fails **three** assertions: `:187` (`setPanel(true)` has no other occurrence — the chevron routes `setPanel(!expanded)`), `:194` (`setExpanded(true)` becomes present), and `:195` (the count becomes 2).
- Reverting the chevron handler (`:252`) to `setExpanded(!expanded)` fails `:186`, the older case at `:57` ("bar click always toggles the panel"), and `:195` (count 2).
So both round-1-identified regressions are now caught — this is the round-1 L1 gap closed.

**Count-assertion brittleness (the flagged question):** `setExpanded(` occurs **exactly once** in `InflightBar.tsx` (`:105`, inside `setPanel`). The `useState` destructure at `:93` — `const [expanded, setExpanded] = useState(readInflightPanelOpen)` — does **not** match the regex (no `(` after `setExpanded`), so it cannot trip the count; likewise passing `setExpanded` as a callback would not. A future *direct* `setExpanded(` writer would trip it, and that is the intended semantics rather than brittleness: any direct writer bypasses `writeInflightPanelOpen`, i.e. silently loses the user's choice on restart. The name coupling (`setPanel` literals) means a deliberate helper rename forces a test update — normal for this repo's `?raw` source-contract convention, and the case's own comment says as much. No change needed.

### FIX 2 — resolved; wording matches the code, and nothing else still claims auto-open or a toggling handle

`docs/FEATURES.md:49` now reads: "…reasoning and the live answer both stream into the thinking box … **whose open/closed state is the USER's choice alone (it never opens itself — only the chevron click, or starting a drag on the handle, which opens it, changes it; the choice is remembered across restarts and a fresh install starts collapsed)** …". Mapped to the code: chevron click = a real toggle (`InflightBar.tsx:252`); "starting a drag on the handle, **which opens it**" = `startDrag`'s `if (!expanded) { setPanel(true); }` (`:200-202`) — the handle never closes the panel. The old "auto-opens the moment a reasoning block starts streaming (a manual collapse is respected until the next block)" clause is gone.

Repo-wide sweep for residual claims:
- `auto-open|auto-opens|auto-expand|auto-expands|opens itself|opened itself|expands itself` → only (i) comments *narrating the removed behavior* (`InflightBar.tsx:90`, `inflightPanelPref.ts:9`, `inflightPanelPref.test.ts:9`, `InflightBar.test.ts:170,191-192`), (ii) unrelated features (ConfigDialog endpoint card auto-expand, `PlanProgress.tsx` active-step auto-expand, `docs/FEATURES.md` auto-compaction, `PLAN.md:966` diff-viewer auto-open, `browser_navigate` bug records), (iii) `.coding/` history.
- Case-insensitive `inflight…(panel|box|bar)` sweep over all Markdown: `docs/FEATURES.md:49` is the only live prose about the panel's open/closed state. `README.md` has no inflight-bar bullet at all (its single `reasoning` hit is `README.md:42`, the feature table's "reasoning budgets"); `PLAN.md:966` is the diff viewer.
- `toggl*` in `docs/**` → only `docs/FEATURES.md:51` (Trace-tab legend chips) and `:58` (image-tools setting) — both unrelated.
### Regression sweep (the diff changed since round 1)

**(a) Zero auto-open paths remain — PASS.** Repo-wide `reasoningActive|reasoningBlockStarted|prevReasoningActive|reasoningPanel` = 23 matches in 6 files: `InflightBar.test.ts:166` (suite doc comment describing the defect), `:171-173` (the deliberate `not.toContain` strings), and `.coding/` artifacts only (decision record `2027-01-11-inflightbar-reasoning-panel-is-user-controlled-o.md`, plans `0d079599` + `12284516`, reviews `2026-08-22-per-agent-model-picker-review` + the round-1 report). **Zero hits in `frontend/src` outside that test file; zero under `src-tauri/`.** The deleted module has no live importer or stale include entry.

**(b) `expanded` is still written only by `setPanel`; no effect or lifecycle event can flip it — PASS.** All `setExpanded|setPanel` occurrences in the component: `:93` (destructure), `:104` (helper declaration), `:105` (helper body), `:201` (`startDrag` collapsed branch), `:252` (chevron/bar click) — nothing else. The three effects touch only unrelated state: elapsed timer `:113-117` (deps `[running]` → `setNow`), auto-scroll `:121-124` (deps `[activityLog]` → `scrollTop`), drag listeners `:175-195` (deps `[]` → `setHeight`/body cursor+userSelect). Nothing keys on `phase` or turn lifecycle events any more; the only mount-time work is the pure `readInflightPanelOpen()` read (StrictMode double-invoke harmless).

**(c) Default stays CLOSED for every non-`"1"` value, guards never throw — PASS.** `parsePanelOpen` (`inflightPanelPref.ts:27-29`) is `raw === "1"`; the table at `inflightPanelPref.test.ts:41-45` covers `""`, `"0"`, `"true"`, `"open"`, `"yes"`, `"2"`, `"-1"`, plus `null` at `:37-39`. `readInflightPanelOpen` (`:31-39`) checks `typeof window === "undefined"` first, then try/catch; `writeInflightPanelOpen` (`:41-49`) no-ops without `window` and swallows `setItem` throws. The test proves both degradations (`:69-73` no window, `:75-88` throwing storage; `vi.unstubAllGlobals()` in `afterEach` `:32-34` restores the node env before the no-window case). `useState(readInflightPanelOpen)` (`InflightBar.tsx:93`) passes the function *reference* → React's lazy initializer, pinned by `InflightBar.test.ts:177`.

**(d) Test registration — PASS.** `frontend/vitest.config.ts:65` adds `src/lib/inflightPanelPref.test.ts`; the `src/lib/reasoningPanel.test.ts` entry is deleted (confirmed in the diff). `InflightBar.test.ts` remains registered (`:22`). The filesystem-driven guard `frontend/src/lib/vitestInclude.test.ts:79-97` (itself registered, `vitest.config.ts:90`) fails listing any `*.test.ts(x)` under `src/` absent from the allow-list — so the new file provably executes in a green suite, and a resurrected unregistered test file would be loud.

**(e) Multi-platform neutrality — PASS.** Pure TS/React + one Markdown line; no OS paths, APIs or shell syntax; `frontend/package.json` untouched (not in the diff, no new dependency); `window.localStorage` is standard in WebView2 (Windows) and WKWebView (macOS).

**(f) Project constitution (agent.md) — PASS.** Doc comments on every new public symbol (`inflightPanelPref.ts:19`, `:22-26`, `:31`, `:41`), on the exported `InflightBar` (`InflightBar.tsx:61-68`) and on the local `setPanel` helper (`:99-103`). No `#[allow]`/suppression added anywhere; no Rust file is touched by this change (the diff is frontend + `docs/FEATURES.md` + `.coding/` bookkeeping), so `#![deny(warnings)]` is not at risk here. The only `eslint-disable` occurrences in `frontend/src` are pre-existing and none is in `InflightBar.tsx` or `inflightPanelPref.ts`. `git status --short` shows exactly the intended set (7 tracked modified/deleted + `inflightPanelPref.ts` / `inflightPanelPref.test.ts` untracked), with `.coding/` bookkeeping as expected.

### Observations (no action required)

1. **One residual use of "toggle" for the drag handle** — the case *title* `InflightBar.test.ts:180` reads "persists every user toggle (chevron click and drag handle)", while the handle only ever opens. The body comment directly beneath (`:181-188`) states the semantics precisely ("startDrag's collapsed branch", "a drag opens the panel for the session but a restart silently loses the choice"), and `:162` / `InflightBar.tsx:100-102` describe both routes accurately; unlike the round-1 L2 doc text, this title is not a behavioural claim that the handle *closes* the panel, so it is a wording nit in an internal test description rather than a finding.
2. **Limit of the static pin (inherent, not a defect)** — the achievable pins are: absence of the deleted module's names (catches a verbatim re-introduction) and the `setExpanded(` count/absence checks (catches the `setExpanded(true)` bypass form). A hypothetical future auto-open authored as `setPanel(true)` inside an effect would satisfy all current assertions, because a second `setPanel(true)` (e.g. a legitimate future "open panel" affordance) is indistinguishable from the drag-handle call site in a static string contract — and `frontend/vitest.config.ts:9` runs `environment: "node"` with no React DOM testing library, so effect *behaviour* cannot be asserted in this repo. Worth knowing, not worth changing; the code-level goal (no auto-open path) is verified by reading the component in full.
3. **Unrelated bookkeeping rides along** — `.coding/knowledge/bug/506b85e2.md` gains a `status = "superseded"` frontmatter line (the DeepSeek cache-tail record, same session but a different workstream). It is mergeable side-car bookkeeping, harmless in this commit, but it belongs to that other change rather than this plan — worth an intentional mention in the commit message if it ships here.

### Verification method + residual uncertainty

- Evidence: the complete uncommitted diff (`git_read op="diff"` — stat, full hunks, untracked list), the full current `InflightBar.tsx` (all 522 lines), `InflightBar.test.ts`, `inflightPanelPref.ts` + its test, `vitest.config.ts`, `vitestInclude.test.ts`, `docs/FEATURES.md:42-57`, plus repo-wide searches for every identifier, key and doc phrase the change touches (`setExpanded|setPanel` scoped to the component; the removed-identifier alternation repo-wide; `auto-open|auto-expand|opens itself|…`; a case-insensitive `inflight…(panel|box|bar)` Markdown sweep; `mh.inflightPanelOpen`; suppression patterns in `frontend/src`).
- **A `role:"reviewer"` subagent has no shell tool, so `npm test`, `npm run build` and `cargo test` were NOT re-run by me** — I do not assert a green run. The implementer's reported results (build exit 0; frontend 82 files / 1112 tests including `InflightBar.test.ts` 17 and `inflightPanelPref.test.ts` 7; root `cargo test` 2338 passed / 0 failed / 5 ignored) are taken at face value. What I could check statically instead — a coherent include list whose guard would catch a missing registration, no dangling import of the deleted module, no unused symbols (the deleted effect left `useEffect`/`phase`/`activityLog` still used by the three remaining effects and `phaseLabel`/`hasActivity`), and every assertion literal matching the source character-for-character — is verified. The "bite" analysis is likewise exact-substring reasoning against both sides, which is precisely what these assertions evaluate; it is not an execution of the suite.
- Neither finding questioned the fix's correctness, and round 2 finds no correctness, security, or constitution issue: the auto-open path is gone, `expanded` has one writer, the pref defaults closed under every guard, the user-facing doc and the component comments match the shipped behaviour, and both round-1 findings are demonstrably closed.
