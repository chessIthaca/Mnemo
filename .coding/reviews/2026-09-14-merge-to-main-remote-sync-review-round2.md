## Verdict: PASS

Round-2 verification of commit `06fdf7a` on `wt/mnemo` for plan d826b9ad (merge_to_main syncs `main` with `origin` BEFORE the branch merge). All six round-1 findings (1 high, 5 low) are verified **fixed in code**, each checked independently against the actual sources, and the fixes introduce nothing new. Working tree is clean at HEAD (empty `git diff HEAD` + empty `git status --short`), and the commit contains everything its message claims (stat matches: skill toml, GitView.tsx + new GitView.test.ts, vitest.config.ts, MergeToMainDialog.tsx, docs/FEATURES.md, PLAN.md, agent.md, .gitignore, src/skill/mod.rs, plus the plan file, round-1 report and knowledge files). Test evidence taken as given (reviewer is read-only): `cargo test` 2328 passed / 0 failed / 5 ignored; frontend `npm test` 1106 passed (82 files); `npm run build` exit 0. Three observations are recorded at the end that are **NOT counted as findings** (pre-existing, or consistent intentional redundancy), plus round-1's out-of-scope items remain unchanged.
---

### HIGH 1 — shell vs approval-gated git tool: VERIFIED FIXED

- **The gate is real.** `GitTool::never_auto_for` (src/tool/agent/git.rs:516-537) is subcommand-aware, consulting the runtime-mutable `core_operations` list (defaults `["merge","push"]`, case-insensitive, resolves either the `subcommand` or `action` field). The trait default returns `false` (src/tool/mod.rs:133-135). `ShellTool`'s full `Tool` impl (src/tool/agent/shell.rs:158-307) implements only `name`/`category`/`schema`/`safety`/`execute` — no `never_auto_for` override; a graph search over all `never_auto_for` symbols returns only GitTool's override, the trait default, and git.rs's own tests.
- **Consumption point confirmed.** src/agent/dispatch.rs:339 `let force_prompt = tool.never_auto_for(&parsed_call.arguments);` forces the interactive approval prompt even under `SafetyMode::Autonomous`, and the safety-rules auto-approve shortcut explicitly never applies when forced (dispatch.rs:349-358: "Never applies to a never_auto tool"). So a shell-invoked `git merge`/`push` would indeed run with no prompt — exactly what the new prompt text guards against.
- **Both prompt copies re-anchored.** `.coding/skills/merge_to_main.toml` step 2 runs the sync "via shell (the git tool has no fetch/pull subcommand; `--no-rebase` never rewrites main)" (line 58); step 3 reads "`git merge --no-ff <branch>` — use the **git** tool, NEVER shell: the git tool forces the core-operation approval prompt, shell does not" (line 59); step 7 push still routes "via the git tool (approval-gated)" (line 63). `GitView.tsx::mergePrompt` mirrors the split: step 3 names shell for the sync with the same reason (line 80), step 4 re-anchors the merge ("use the git tool, never shell: the git tool forces the core-operation approval prompt, shell does not", line 81).
- **The rationale is recorded where it belongs.** The never-injected comment header carries a dedicated bullet naming ShellTool/`never_auto_for` and the Autonomous-mode consequence (toml lines 30-35).
- **No prompt text reads as "shell is fine for git write ops".** The only git operation routed to shell in either copy is the fetch/pull sync, with the reason stated; merge and push are anchored to the git tool in both copies; step 1's commit names no tool (not a core op, and the git tool's own `safety()` is `NeedsApproval` regardless).

### LOW 1 — each prompt copy had half the conflict rule: VERIFIED FIXED

- `.toml` step 2 (pull conflicts): "resolve with file_edit taking the UNION of both sides, then `git add` + `git commit` to conclude the pull merge; never drop a side's change" (line 58). Step 3 (branch-merge conflicts): UNION + add/commit + never-drop + the `backlog.jsonl` union-driver note (line 59).
- `GitView.tsx` step 3 (pull conflicts): UNION + "git add + git commit to conclude the pull merge — never drop a side's change" (line 80). Step 5: UNION + add/commit + "never use -X ours / -X theirs" (line 82).
- No contradiction between the copies; the `--no-ff ${branch}` interpolation is intact (GitView lines 81 and 84; the `.toml` `<branch>` placeholder unchanged).

### LOW 2 — dialog contradicted the injected NEVER-stash rule: VERIFIED FIXED

- `MergeToMainDialog.tsx` read in full (109 lines): **zero** occurrences of "stash". The bullet now reads "Commit uncommitted changes on the branch (the skill never stashes)" (line 74); the doc comment enumerates "(commit, checkout main, sync with origin, merge, resolve conflicts, delete branch)" (lines 27-28).
- `sourceBranch` is still destructured (line 33) and used in the body paragraph (line 70) — no unused-prop TS break (`npm run build` green per evidence).
- Repo sweep: no user-facing string or living doc (README.md, docs/FEATURES.md, PLAN.md, agent.md) still describes the merge flow as stashing. The remaining "stash" mentions are the skill's own NEVER-stash rule + its rationale header, historical `.coding/` records, and two pre-existing StatusBar.tsx doc comments (see Observations).
### LOW 3 — sibling contract statements: VERIFIED FIXED (one nuance, see Observations)

- **PLAN.md: exactly ONE statement**, the Branch-topology row at line 320 (confirmed by a scoped search — one match in the whole file): syncs `main` with `origin` first (`git fetch` + `git pull --no-rebase`, so the landed merge is immediately pushable) → merges → deletes.
- **agent.md: two statements, not an accidental double-paste.** Environment bullet line 22 (sync precondition + the shell/git-tool split + why: "`ShellTool` does not override `never_auto_for`, so a shell-invoked `git merge`/`push` would run unprompted even in Autonomous mode") and Branch-policy bullet line 84 (sync precondition + rationale "a stale `main` otherwise surfaces later as a rejected push" + tool split). The two sentences are distinct wordings, each appearing exactly once ("run unprompted" 1×, "rejected push" 1×), in section-appropriate places, and they agree. The new Environment "Never commit to `main`" line (line 21) sits alongside the pre-existing Branch-policy closer (line 88) — consistent redundancy.
- agent.md's side-car bullet now lists `plans/stack.json` + `instance.json` as gitignored per-instance local state ✓.
- **Cross-document agreement on the sequence:** skill toml (checkout main → fetch → pull `--no-rebase` → merge `--no-ff` → both builds → `branch -d`), GitView copy (same, plus tests and "Do NOT push"), agent.md, PLAN.md:320, docs/FEATURES.md:34 — all state the same order; none contradict.

### LOW 4 — Rust guard incomplete: VERIFIED FIXED

- src/skill/mod.rs:355-380, inside `shipped_skill_files_parse_and_merge_to_main_cleans_up_memories`: three `find(...).expect(...)` lookups with accurate messages ("carries the fetch half of the sync", "carries the remote-sync step", "carries the branch merge step") and two ordering asserts (`fetch < sync`, `sync < branch_merge`) with index-bearing failure messages.
- **Genuinely falsifiable against the pre-change prompt:** the old prompt (visible in the commit's minus lines) contains neither `git fetch origin` nor `git pull --no-rebase` — both `expect`s panic; it DID contain `--no-ff <branch>`, so that find resolves, proving the failing halves are the new ones rather than an artifact. **Not tautological:** the ordering asserts compare independently derived indices — a prompt reordered to merge-first with all three substrings present would fail `sync < branch_merge`.
- `cargo test` 2328 passed / 0 failed (given evidence; the guard sits in the shipped-skills parse test, so a rotted toml fails loudly).

### LOW 5 — `.coding/instance.json` committed to main: VERIFIED FIXED

- `.gitignore` adds `.coding/instance.json` with a rationale comment, anchored like the adjacent working `.coding/plans/stack.json` entry.
- **Behavioral proof the ignore works:** the file exists on disk right now (`{pid: 29284, started_at: 1789353515}`) yet `git status --short` is completely clean — an untracked, non-ignored file would have shown as `??`. Additionally `git log -- .coding/instance.json` is empty: never committed in any commit.
- **Per-instance ephemeral semantics confirmed at src/instance_marker.rs:** writes `{pid, started_at}` best-effort on every launch, last-writer-wins (lines 47-55); readers (`read_marker`/`conflict_for`, lines 58-75) use it only for same-project conflict detection with a caller-supplied pid-liveness predicate; the module doc states write failures must never block startup. Nothing functional depends on it being git-tracked — it is exactly the `plans/stack.json` class.
- Not committed in `06fdf7a` while its siblings (`.coding/plans/d826b9ad.md`, the round-1 report, the two knowledge files) were — confirmed by the commit's stat list.
---

## General round-2 checks

- **Frontend guard vs the EDITED prompt.** All `indexOf` assertions in `frontend/src/components/views/GitView.test.ts` hold against the edited text: `git fetch origin` and `git pull --no-rebase` (step 3) precede `git merge --no-ff wt/demo` (step 4); the merge line's new suffix (" — use the git tool, never shell: …") sits *after* the matched substring and is invisible to `indexOf`, so the ordering assertions are unaffected. The second test's expectations hold too: `mergePrompt("wt/other")` interpolates `wt/other` everywhere and contains no `wt/demo` substring. **Registration:** `frontend/vitest.config.ts` inserts `"src/components/views/GitView.test.ts"` between `DiffViewer.test.ts` and `GraphView.test.ts` — correct alphabetical position ("Gi" < "Gr", "D" < "G"); `src/lib/vitestInclude.test.ts` discovers `src/**/*.test.{ts,tsx}` via Vite's own glob and fails on any unregistered file, so the 1106-pass run proves the new test executed.
- **Documentation sync.** agent.md, PLAN.md, docs/FEATURES.md updated and mutually consistent; README.md carries no merge-flow description (verified — nothing stale there); module doc comments updated where they described the flow (MergeToMainDialog; GitView's `mergePrompt` doc comment "mirrors the skill file's own steps, but names the target branch" remains accurate; the GitView file header describes capabilities without enumerating steps — not stale).
- **Multi-platform neutrality.** Nothing Windows-only was added; the prompts use portable git/npm/cargo commands only (the `&&`/`;` chaining inside build steps is pre-existing and only renumbered — round 1 already scoped it out). The `instance_marker.rs` module is explicitly platform-neutral (aliveness is an injected predicate).
- **File-tools-first.** No shell-based file mutation anywhere in the diff; every change is a normal file-tool edit.
- **Security.** No secrets or new attack surface; the `.gitignore` change removes pid churn (and pull-conflict exposure) from `main`, and the prompt changes strengthen the always-on approval-gate posture.

## Observations — recorded for awareness, NOT counted as findings

1. **agent.md states the sync/tool-split contract twice** (Environment :22 + Branch policy :84). The two sentences are distinct, consistent, and section-appropriate (tool-gate policy vs branch topology); no accidental double-paste of one sentence exists. If "exactly ONE per file" was meant literally, the Environment bullet is the one to trim — judged intentional layering, no contradiction, no stale text. The "Never commit to `main`" line likewise now appears in both Environment (:21) and Branch policy (:88), consistently.
2. **Pre-existing stale doc comments:** `frontend/src/components/layout/StatusBar.tsx:191` and `:781` still describe the merge flow as "(stash, commit, merge, resolve conflicts, delete branch)" — the same stale shape the MergeToMainDialog doc comment was just fixed for. Code comments only (nothing rendered to users, nothing injected to agents); a one-line cleanup candidate for a future doc pass.
3. **Pre-existing GitView carve-out:** `mergePrompt` step 1 still reads "(stash only if it belongs to another branch)" — a conditional serving the Git tab's merge-ANY-branch capability (avoid committing foreign changes onto the wrong branch), diverging from the skill copy's blanket NEVER-stash. Note the orphan-stash rationale applies to it too (nothing in steps 2-8 pops such a stash). Pre-existing text, visible to round 1, not introduced by `06fdf7a`; backlog candidate (e.g. "checkout `${branch}` first, then pop") rather than a defect of this commit.
4. **Round-1 out-of-scope items unchanged:** `src/project/worktrees.rs` app-managed landing still performs no fetch/pull, and a command-aware `ShellTool` `never_auto_for` remains unimplemented — both were explicitly filed as backlog recommendations, correctly not smuggled into this plan.

**Conclusion:** every round-1 finding is genuinely fixed with code-level evidence; nothing new was introduced. **Verdict: PASS.**