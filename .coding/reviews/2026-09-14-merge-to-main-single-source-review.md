## Verdict: PASS

Review of ALL uncommitted changes on `wt/mnemo` (plan e73c8bc4, "Cleanup: single-source the merge_to_main procedure"). Change set: 11 modified files + 2 untracked (`.coding/plans/e73c8bc4.md`; the supersede successor `.coding/knowledge/decision/2027-01-11-one-frozen-tool-surface-for-all-plan-kinds-narro-2.md`). No high or low findings. The goal is met: the skill file is now the single source of the procedure, the UI can only name a target, and every doc claim checked out against code.

### 1. Skill prompt trim — every operative rule kept (`.coding/skills/merge_to_main.toml`)
Verified against the new 7-step checklist (~1.2 KB, −53% vs the old ~2.5 KB; the test `src/skill/mod.rs:339-381` passes with its four pinned substrings in the pinned order `git fetch origin` < `git pull --no-rebase` < `--no-ff <branch>`, plus `MERGED into main`):
- Commit-all incl. `.coding/` + NEVER stash (step 1) ✓ · checkout main + sync FIRST via shell (`git fetch origin`, `git pull --no-rebase`, nothing-new/no-remote → continue) (step 2) ✓ · `git merge --no-ff <branch>` on the **git** tool (step 3) ✓ · UNION conflict resolution via file_edit covering BOTH the pull and the merge, never drop a side (step 3) ✓ · both builds `npm run build` + `cd src-tauri && cargo build` (step 4) ✓ · `git branch -d` with "never -D" now explicit, optional approval-gated push (step 5) ✓ · memory supersede with "MERGED into main at <sha>" (step 6) ✓ · `skill_end` + `abandon_skill` (step 7) ✓.
- Improvement: the `-X ours`/`-X theirs` ban and the checkout-the-target-branch capability (merge ANY branch) moved from the deleted GitView copy into the skill prompt itself.
- Judgment requested by the plan — rules the test does NOT pin that the trim dropped: the `git status` pre-check (implied by "commit ALL uncommitted work"), the backlog.jsonl union-driver note (informational), the `--no-rebase`-never-rewrites-main rationale (the command itself stays), the abandon_skill "(roll back to the pre-skill state)" explanation (abandon_skill's own schema documents it), and the memory-search hints (tool names, "SHIPPED" prefix, "ACTIVE:"/"STEP MARKER:" markers → compressed to "search its name + pre-merge tip"). All are rationale or search-strategy detail, not operative rules; all incident rationale survives in the non-injected header comment (never-stash, both builds, sync-first, tool split, memory convergence, topology pointer). **No operative rule lost.**

### 2. UI override killed and pinned (`src-tauri/src/ipc/agent.rs`, GitView)
- `skill_prompts` (agent.rs:805) makes the overlay ALWAYS `spec.prompt`; a None/blank dispatch falls back to the registry prompt so the agent always receives a goal. `enter_skill` (agent.rs:832-903) passes `&overlay` to `wf.start_skill`, `dispatch` to `AgentCommand::Prompt` and `SkillStarted.prompt`. The regression test (agent.rs:1094-1146) pins unit behavior (override passed / None / blank) plus the command body via an `include_str!` source contract — reverting to the old `prompt.unwrap_or_else`/`&goal` wiring fails it.
- Caller inventory: `enterSkill` has exactly two frontend callers — GitView.tsx:154 (one-line `mergeDispatch`) and StatusBar.tsx:198 (no prompt → dispatch = registry prompt, unchanged behavior). `enter_skill`'s only backend reference is its Tauri registration (main.rs:783). `Workflow::start_skill`'s only non-test callers are `enter_skill` and the agent-driven `skill_start` tool.
- Substitution paths that remain — BY DESIGN, not defects: (a) the agent-facing `skill_start` tool still accepts `skill_prompt`, which becomes the overlay (src/tool/workflow/skill.rs:144,162; schema documents "Overrides the skill's default goal prompt"; pinned by `start_skill_overrides_prompt_and_target`). The plan's scope was the UI path, and tauri.ts's new doc correctly scopes the guarantee to "from the UI". (b) No subagent/console path substitutes a skill prompt: `SkillStarted` is emitted only from `enter_skill` (the agent path announces via its ToolCard; spawn paths use `PromptDispatched`); console.rs:283 renders it display-only.
- SkillStarted payload change (dispatch instead of the old override/registry goal) is cosmetic: the only consumer is `reduceSkillStarted` (agentEventReducer.ts:1148-1152), which pushes a display transcript entry. The runtime overlay is read from workflow state (persisted via `persist_stack`, survives restart — `skill_persists_across_load_latest`), never from the event — no functional regression. Bonus: the Git-tab dispatch no longer contradicts the overlay (the deleted copy permitted a stash, said "Do NOT push", and falsely claimed shell-invoked merges run unprompted).
- GitView.test.ts: single-line/no-step-text assertions hold against `mergeDispatch` output; the `?raw` source contract matches GitView.tsx:154 exactly; registration unchanged (vitest.config.ts:38).

### 3. Doc claims verified against code
- `ShellTool::never_auto_for` exists (src/tool/agent/shell.rs:267-276 + `command_is_core_git_op` at :72-111): matches a `git`/`git.exe` executable in a command position per segment, steps over git's global options, and reads the SAME runtime `[git] core_operations` handle as GitTool; tests at shell.rs:416-463 gate `git merge`/`git push`/`git.exe push`/`cd repo && git push` while keeping `git pull --no-rebase` ungated. agent.md's shortened bullet is therefore accurate; the policy (sync-first on shell, merge/push via the approval-gated git tool, both gated in code) is intact. The dropped `[git] core_operations` config-key mention and the "matches the subcommand after git's global options" detail are accepted compression, not weakened policy (the config surface stays documented in the shell.rs/git.rs doc comments).
- The spec knowledge file amendment (`.coding/knowledge/spec/2027-01-11-merge-to-main-syncs-main-with-origin-first-shell.md:17`) is factually accurate on every point, including the CORRECTION paragraph (the original WHY paragraph's stale `ShellTool` claim is superseded in place — the amend convention, history kept).

### 4. Docs / comments / knowledge sweep
- PLAN.md:320 and docs/FEATURES.md:34-35,70 describe behavior only — accurate, no edit needed (matching the amendment's claim). README.md carries no merge-flow content (nothing stale). MergeToMainDialog keeps the approval sentence and its 3-line summary is accurate ("never stashes", "Sync main with origin first", "Verify both builds"). StatusBar's merge-flow comment (:189-193) matches the checklist.
- No doc/comment still describes the removed UI override as current: "branch-specific prompt" survives only in historical `.coding/` plans/reviews (immutable history, correctly left) and in the spec file's original body, which the appended amendment explicitly corrects.
- Untracked/incidental, all benign: the decision supersede pair (predecessor frontmatter `status = "superseded"`, successor `-2.md` carrying the 4cbc3b0 merge pointer) is convention-compliant and belongs in this commit; `.coding/analysis/cache-hit-6-aggregates.txt` is a regenerated analysis artifact (fresh trace-window data, unrelated to this plan's goal, rides along); the bug-record amendment is merge-hygiene bookkeeping.

### 5. Project-constitution checks
- Documentation sync ✓ (README/PLAN/FEATURES/module comments all consistent; no stale doc found).
- Multi-platform neutrality ✓ (no `cfg(windows)` additions, no Windows-only paths or shell syntax introduced; the `cd src-tauri && cargo build` phrasing pre-existed in the prompt; `?raw` is a standard Vite feature).
- File-tools-first ✓ (no shell-based file mutation in the change set; knowledge amendments used the sanctioned append convention).

Test status as reported by the parent (consistent with the pins verified here): root `cargo test` 2337 passed; `cd src-tauri; cargo test` 299 passed; frontend vitest 1107 passed / 82 files; `npm run build` green; `#![deny(warnings)]` makes green runs warning-free.