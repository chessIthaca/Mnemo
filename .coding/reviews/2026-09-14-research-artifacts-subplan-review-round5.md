## Verdict: FINDINGS (0 high, 1 low)

Round-5 verification of plan `ee65fd4b` ("Research plans may write .coding artifacts; close the sub-plan review leak", options A + D), branch `wt/mnemo`, same working tree, **no new commits** (HEAD `0e1d466`). Scope exactly as instructed: the two round-4 LOWs. **Both fixes are correct and exactly as described** — the sandbox doc's third bullet matches the code it describes (four names, `ExecutingResearch`-only, under the workflow mutex, ≤1 `validate` + one depth-bounded `symlink_metadata` walk), the blank-line fix is clean, and **nothing else in the change moved**: the changed tracked set is the same 9 files rounds 1–4 recorded, plus one new `.coding/backlog.jsonl` line that postdates round 4 *by round 4's own recommendation*, and every line anchor round 4 cited still lands exactly where it recorded it. Round 4's substantive verifications therefore stand unchanged. One LOW remains — and it sits **inside the sentence this change just rewrote**: the enumeration is one short. There is a **fourth** sync-path `Sandbox::validate` caller, the file tools' approval-preview path, which the module doc never names.

### L1 (low) — "Three sync-path callers" is still one short: the `file_edit`/`file_write` approval-preview path blocks on the agent-loop task

Evidence, all read from the tree:

- `src/agent/dispatch.rs:512` calls `tool.approval_preview(&parsed_call.arguments)` **inline in the async `execute_tool_call`**, immediately before sending the `ApprovalRequest` (`:516-528`). It is a synchronous trait method — no `spawn_blocking` anywhere on this path.
- `src/tool/agent/file_edit.rs:1296-1299` (`approval_preview`) → `prepare_for_approval` (`:1408`) → `self.sandbox.validate(Path::new(&args.path))` at **`:1409`**, then `std::fs::read_to_string` (`:1419`) and `prepare_edit` (`:1420`).
- `src/tool/agent/file_write.rs:98-107` (`approval_preview`) → `prepare_for_approval` (`:187`) → `validate` / `validate_for_creation` at **`:189-191`**, `detect_line_ending_path` (`:194`), `read_to_string` (`:205`), `compute_diff` (`:208`).
- The site is already known to run there — `src/tool/agent/file_edit.rs:2718-2724` says so in its own comment: *"the approval path (approval_preview → prepare_for_approval → prepare_edit) runs synchronously on the agent-loop task"*.
- Only these two hooks override the trait default (`src/tool/mod.rs:144`; `file_append` and `convert_line_endings` deliberately have none — `convert_line_endings.rs:97`), so this is one logical fourth caller with two concrete call sites.

So the rewritten sentence — *"Three sync-path callers remain; the first two perform a **single** `canonicalize` …"* (`sandbox.rs:23-25`) — is not the complete enumeration it reads as, and *"they wrap their **entire** blocking work … in one `spawn_blocking` closure"* (`:20-23`) overstates for `file_edit`/`file_write`, whose second entry point blocks too. This wording is **pre-existing** (round 4's L1 asked to add the new caller, and the fix did exactly that — correctly), and the *behavior* is the same trade-off already accepted and documented at `approval.rs:114-123` (same prompt flow, one `canonicalize` + one preview read per prompted file-tool call). So: docs-only, low, **no code rework** — this must not be fixed by touching behavior.

**Fix (cheap, docs only):** make it four — add a bullet naming the approval-preview path (`Tool::approval_preview` → `file_edit`/`file_write::prepare_for_approval`, called from `dispatch.rs:512`), noting it is ≤1 `canonicalize` + one preview read per *prompted* file-tool call and inherits the `is_project_scoped` trade-off in the same flow — or, if the count should stay three, scope the sentence explicitly ("three callers outside the tools' own `approval_preview` hooks"). Either way the paragraph must stop reading as an exhaustive list.

**Why this is not pedantry:** the paragraph is the standing contract for async callers of `validate`, and *this change* is what put a count on it. A future `spawn_blocking` sweep steered by that list would miss the preview path — which is exactly the failure mode round 4's L1 diagnosed one caller earlier.

---

## 1. The two round-5 fixes

### 1(a) — the third bullet matches the code it describes: CORRECT

- **Four names.** `RESEARCH_ARTIFACT_TOOLS` (`dispatch.rs:32-37`) is exactly `file_edit` / `file_write` / `file_append` / `convert_line_endings`, and `research_write_verdict` returns before any filesystem work for any other name (`:82-84`; a call without a string `path` also returns at `:85-87`). The doc's "bounded to the four `RESEARCH_ARTIFACT_TOOLS` names" ✓.
- **Only while the filter is `ExecutingResearch`.** The verdict is computed *only* under `if filter == ToolFilter::ExecutingResearch` (`:235-239`); `ToolFilter` derives `PartialEq`, so that is exact variant equality — no other state or kind can reach it ✓ ("only fires while the filter is `ExecutingResearch`").
- **Under the workflow mutex.** `let wf = self.workflow.lock().await;` at `:230`, `research_write_verdict` called at `:235-236`, guard held until the end of the block at `:265` ✓ — and the call is synchronous, so the guard really is held across those syscalls, exactly as the bullet says.
- **Cost profile.** One `validate` attempt (`:90`: one `canonicalize` + `exists`, else the parent-canonicalize fallback), plus — only when that fails — `validate_for_creation` (`:108`), which is purely lexical (`sandbox.rs:133-146`: `normalize_path` + `starts_with`, no filesystem access), plus exactly **one** `lexical_path_is_link_free` walk (`:97` canonical branch, `:109` lexical branch) — one `symlink_metadata` per existing component, stopping at the first missing one (`sandbox.rs:332-347`). "one `validate` plus a depth-bounded `symlink_metadata` walk per file-tool-named call" ✓.
- **Attribution** (`dispatch::research_write_verdict`, 2027-01-11, plan `ee65fd4b`) ✓, and "no other plan kind or state can reach it" ✓.

### 1(c) — no other live doc/comment claims "two"

`sandbox.rs:23` carries the only live count text ("Three sync-path callers"). `sync-path`, `sync caller(s)`, `callers remain`, `remaining sync` have no other source/​doc hits (`resolve_cwd`'s own doc at `shell.rs:206-210` describes one validate + `is_dir` with no count claim; `approval.rs:114-123` documents its trade-off with no count claim). Two **historical review reports** still say "two" — `.coding/reviews/2026-09-15-performance-review.md:121` and `.coding/reviews/2026-04-phase6a-spawn-blocking-review.md:168`. Those are dated records of what was true when they were written; rewriting them would falsify the record, so leaving them untouched is correct. **Not findings.**

### 1(b) — completeness of the enumeration: one short (L1 above)

Everything *named* is correctly characterized, and I re-derived the full production call-site list rather than trusting the sentence: the three named callers (`approval.rs:133`, `shell.rs:217` — called from `execute` at `:284`, no `spawn_blocking` in that file; `dispatch.rs:90/108`) plus the unnamed fourth (the preview path, L1). Every other production call site is inside a `spawn_blocking` closure, verified site-by-site: `convert_line_endings.rs:125` ← `:116`; `browser/mod.rs:261` ← `:260`, `:886` ← `:885`; `file_edit.rs:1335` ← `:1334`; `file_read.rs:120` ← `:119`; `file_write.rs:132` (execute path); `image_tools/mod.rs:64` (inside `load_image_data_url`, reached from the closure at `:145`), `:163` ← `:162`; `read_files.rs:347` (inside `read_one_with_count`, called from the closure at `:226`; `search_read`'s closure at `:192` routes through `read_one`); `search.rs:1281`. No UFCS `Sandbox::validate(` call site exists anywhere (the two hits are doc references).

## 2. The blank-line fix — clean, and nothing else in the file was touched

`src/agent/tests.rs:5567` `}`, `:5568` one blank line, `:5569` `#[tokio::test]`, `:5570` `async fn dispatch_allows_read_tool_on_skill_allow_list()`. The file's only diff hunk is the single 258-line insertion block (`@@ -5308,6 +5308,264 @@`; added content is now new lines 5311–5568), and I checked every blank line inside that block line-by-line: all single. The removal of that one blank line is also exactly the `−1` in the file's diff stat, so no other whitespace in the added region changed.

## 3. Nothing else moved since round 4

- **Changed tracked set** = the same 9 files rounds 1–4 recorded (`src/workflow/mod.rs`, `src/agent/dispatch.rs`, `src/tool/agent/sandbox.rs`, `src/tool/mod.rs`, `src/agent/tests.rs`, `src/tool/workflow/plan.rs`, `PLAN.md`, `docs/FEATURES.md`, `.coding/plans/12284516.md`) **plus one new tracked change since round 4: `.coding/backlog.jsonl` +1** — the item recording the three pre-existing bypasses. Its text cites `…-round4.md §Q5`, so it postdates round 4, and it is precisely the action round 4 recommended ("record, don't fix here"). Side-car bookkeeping; no source impact.
- **The only source/docs deltas vs round 4 are the two fixes:** `sandbox.rs` `+11/−3` (the "Three sync-path callers" rewrite plus the 8-line third bullet) and `tests.rs` `−1` (that blank line).
- **Cross-check that nothing else shifted — every anchor round 4 cited still lands exactly:** `dispatch.rs:1953-1958` (early-return guard), `:1985-1990` (escape-fixture guard), `:2023` and `:2039` (the two inner `plant_dir_link` blocks), `:2073-2113` (`plant_dir_link`, file total 2114); `tests.rs:5532-5535` (the SKIP guard); the four `research_write_verdict` pointers at `tool/mod.rs:226`, `tool/mod.rs:432`, `plan.rs:93`, `workflow/mod.rs:441`; `is_research_artifact_write` has zero hits. Line-number anchors in three files plus the end-of-file anchor in `dispatch.rs` pin the numbering of every file this change touches.
- **Transparency note (not a finding):** round 4 recorded the aggregate as `+1,351/−77`; subtracting this round's two fixes (`+11/−4`) and the new backlog line (`+1`) from the current `+1,362/−80` leaves `+1,350/−76` — one line per column apart. I cannot resolve that from here (read-only; I have no snapshot of the round-4 tree state and no shell), and it is **not** evidence of a content change: the file set is identical, every anchor lands exactly, and every hunk in the current diff is accounted for by either a round-1–4 change or the two fixes above. Most likely it is how round 4's snapshot counted a trailing-newline hunk at that moment.
- **Untracked side-car additions** (the `.coding/analysis/**` deliverables, the `.coding/knowledge/**` BUG record, the review reports themselves) ride the commit, per the accepted decision.

**Therefore round 4's substantive verifications stand unchanged:** round 3's H1 is closed by the code, no over-refusal on the legitimate paths, the file-tool ladder untouched, the gate order unchanged, `review_required`'s lifecycle intact, cache invariant #1 intact, and the carve-out still gated by exact `ToolFilter` variant equality.

## 4. Anything else before commit?

**One thing: L1 above** (documentation only, ~3 lines, no behavior change). Nothing else. The pre-existing residuals are properly recorded rather than fixed here: BUG memory `d7ebe9f9` exists, and the new backlog item names all three bypasses **including the protected-file half of the hardlink issue** that round 4 explicitly asked for ("`.coding/x.toml` hardlinked to `.coding/safety.toml` (or memory.db / plans / reviews / knowledge) … writable by ANY plan kind"), with a per-bypass acceptance test and the multi-platform note. The reported green `cargo test` (2,359 lib, 5 ignored + 16 integration) is accepted as the build/warning proof; both fixes are documentation/whitespace only, so no behavior changed and no test re-derivation is needed on top of round 4's. `.coding/plans/ee65fd4b.md` step 8 is still unchecked — that is the in-flight closing sequence, expected, not a finding.

**Bottom line.** The two round-4 LOWs are fixed exactly as described and nothing else moved; the only remaining item is a 3-line doc completion on the enumeration the change itself rewrote (four sync-path callers, not three — the approval-preview path). Fix that, then commit.
