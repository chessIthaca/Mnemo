## Verdict: PASS

Round-8 verification of the one open item from round 7 (F1-R7) on the same uncommitted change (plan ee65fd4b — "Research plans may write .coding artifacts; close the sub-plan review leak", options A + D; branch `wt/mnemo`, HEAD `0e1d466`, no new commits, rounds 1–7 reports read-only and untouched). The fix — the replaced app-crate clause in `src/tool/agent/sandbox.rs:24-27` — is true of all seven `src-tauri/src/ipc/files.rs` validate sites, the "never with `State` held across an await" clause holds for all seven, the rest of the paragraph is unchanged from the round-7 text and its four-bullet census still has no fifth caller, and the diff is still the same 10 files with the substantive hunks unchanged. **F1-R7 is closed; nothing else is open.**

Fix under test (`sandbox.rs:23-29`): "Four sync-path callers remain in THIS crate's tool layer. The app's IPC file commands (`src-tauri/src/ipc/files.rs`) validate by design as well — inline on the async task or inside their own `spawn_blocking` closure depending on the command, never with `State` held across an await. Those four perform one `canonicalize` (the fourth adds one preview read) and are kept sync by design, with the trade-off noted:"

## V1 — the clause covers every one of the seven sites, and cannot falsify any

`src-tauri/src/ipc/files.rs` is re-confirmed as the only app-crate file with sandbox-validate call sites, exactly 7 (tree-wide `\.validate(_for_creation|_for_write)?\(` search: `:37`, `:64`, `:128`, `:698`, `:858`, `:1346`, `:1383`). Each site read in source:

| site | validate | where it actually runs | clause that covers it |
|---|---|---|---|
| `read_file` | `:37` (call `:36-38`) | inline on the async task; the spawn `:39` wraps only the `read_to_string` | "inline on the async task" ✓ |
| `list_files` | `:698` | inline on the async task; the function has **no spawn at all** (its `read_dir` `:703` is inline too) | "inline on the async task" ✓ — the sentence claims nothing about a spawn, so the no-spawn case is not falsified |
| `repo_relative_pathspec` ← `git_diff_head` | `:858` (call `:974`) | inline on the async task; spawn `:977` takes the owned `Option<PathBuf>` | "inline on the async task" ✓ |
| `save_conversation` | `:1346` | inline on the async task; spawn `:1355` | "inline on the async task" ✓ |
| `load_conversation` | `:1383` | inline on the async task; spawn `:1385` | "inline on the async task" ✓ |
| `write_sandboxed` ← `write_file` | `:128` (call `:149`) | **inside** the closure the command spawns (`:149`) | "inside their own `spawn_blocking` closure" ✓ |
| `read_image_data_url_sync` ← `read_image_data_url` | `:64` (call `:110`) | **inside** the closure the command spawns (`:110`) | "inside their own `spawn_blocking` closure" ✓ |

The 5 + 2 split is exhaustive, not a sample: `write_sandboxed`, `read_image_data_url_sync` and `repo_relative_pathspec` each have exactly **one** production call site (the commands above); every other matching line in the file is in `#[cfg(test)]` code. And the sentence is unfalsifiable by construction: it states **no count** and **no per-site ordering** ("depending on the command" is the only ordering claim, and it is empty), so the 5-inline/2-wrapped split, the no-spawn case, and any per-site variation all satisfy it. This is exactly what round 7's F1-R7 asked for — the pre-fix "before their own spawns" assertion is gone.

## V2 — "never with `State` held across an await": true for all seven

- `read_file` — `state.project.sandbox.clone()` `:35`, no guard; awaited spawn `:40`. ✓
- `read_image_data_url` — clone `:109`, then owned `Sandbox` + path moved into the closure `:110`. ✓
- `write_file` — clone `:148`, owned sandbox/path/content moved `:149`. ✓
- `list_files` — no `.await` anywhere in the body. ✓
- `git_diff_head` — root cloned out of the lock `:968` (the temporary guard ends with that statement), pathspec validated inline `:974`, spawn `:977` moves owned data. ✓
- `save_conversation` — root guard taken `:1330`, `drop(project)` `:1349` **before** the spawn/await `:1355-1362`; sandbox cloned `:1331`. ✓
- `load_conversation` — clone `:1381`, then the owned `validated` path moved `:1385`. ✓

This also agrees with each function's own doc comments (`:30-32`, `:100-103`, `:139-141`), so the paragraph and the per-function comments are mutually consistent.

## V3 — the unchanged remainder of the paragraph is still exactly true

- **Four sync-path callers in this crate's tool layer, no fifth** — re-derived: production `validate`/`validate_for_creation`/`validate_for_write` call sites outside a closure are `approval.rs:133` (bullet 1; the P3 trade-off paragraph `:114`), `shell.rs:217` + the `is_dir` `:220` (bullet 2), `dispatch.rs:90`/`:108` (bullet 3), and `file_edit.rs:1409` / `file_write.rs:189`/`:191` reached from the inline `approval_preview` call at `dispatch.rs:512` (bullet 4). Everything else in the tool layer is inside a closure: `convert_line_endings.rs:125←:116`, `file_append.rs:110←:100`, `file_edit.rs:1335←:1334`, `file_read.rs:120←:119`, `file_write.rs:140←:132`, `image_tools/mod.rs:64←:145` and `:163←:162`, `read_files.rs:347←:226`, `search_read.rs:192`, `browser/mod.rs:261←:260` and `:886←:885`. `search.rs` has no `validate` at all (its `:1281` spawn wraps the scan); `sandbox.rs:385/387/402` are the ladder's own internal calls, reachable only through the wrapped `validate_for_write` sites. The change adds none: `lexical_path_is_link_free`'s only production caller is `research_write_verdict` (`dispatch.rs:97`/`:109`) and `is_artifact_write_target`'s only production caller is `dispatch.rs:121`.
- **Six wrapped ENTRY-POINT tools** — confirmed present: `file_read.rs:119`, `file_edit.rs:1334`, `file_write.rs:132`, `file_append.rs:100`, `search.rs:1281`, `image_tools/mod.rs:145`/`:162`; none of those six files is in this diff.
- **Bullet 3 (`research_write_verdict`)** — the verdict is computed at `dispatch.rs:235-236` while the `wf` guard (locked `:230`) is live and used again at `:244`/`:259`, so "the guard is held across it" holds; it is gated on `filter == ToolFilter::ExecutingResearch` and short-circuited to the four `RESEARCH_ARTIFACT_TOOLS` names inside the function — no other plan kind or state can reach it. ✓
- **Bullet 4 (approval-preview hook)** — `dispatch.rs:512` calls `tool.approval_preview(&parsed_call.arguments)` directly on the async task (no `spawn_blocking`); the only overrides are `file_edit.rs:1296` and `file_write.rs:98`, the trait default (`src/tool/mod.rs:144`) returns `None`, and `convert_line_endings.rs:97` carries the explicit "no `approval_preview` override" note (`file_append` has none). ✓
- **"Those four perform one `canonicalize` (the fourth adds one preview read)"** — the referent is still pinned to the preceding "Four sync-path callers … in THIS crate's tool layer": the app-crate sentence names no count, and its seven sites cannot be read as four, so no ambiguity is introduced. `Sandbox::validate` performs at most one `canonicalize` (the candidate `:113`, else its parent `:121`) followed by the pure `check_inside` `:162-168`; `validate_for_creation` performs none. Bullet 3's extra walk is `symlink_metadata` — not a canonicalize — and is named in its own bullet; bullet 4's preview read is the parenthetical. ✓ (The four bullets themselves are byte-identical to the round-7 verified text.)

## V4 — Regression check: same 10 files, substantive hunks unchanged

- `git diff HEAD --stat` = **10 files, +1386/−85**, identical file set to round 7: the six source/test files (`src/agent/dispatch.rs`, `src/agent/tests.rs`, `src/tool/agent/sandbox.rs`, `src/tool/mod.rs`, `src/tool/workflow/plan.rs`, `src/workflow/mod.rs`), the two synced docs (`PLAN.md`, `docs/FEATURES.md`), plus the two other-session bookkeeping entries (`.coding/backlog.jsonl` — the `pending` sandbox-bypass item `1a5bffcf`; `.coding/plans/12284516.md` — a `## Regression test` line). No file added or removed.
- Every substantive hunk re-read in the current diff matches rounds 3–7's recorded description: the dangling-link-leaf guard in both verdict branches (`dispatch.rs:97`, `:109`), `Artifact`/`Protected` mutual exclusion by construction (`is_artifact_write_target` ends in `!is_protected_write_target`), the `ToolFilter::ExecutingResearch` gate + three-arm `denial_message`, the `review_required` lifecycle (armed on sub-plan pop and on a non-root `abandon_plan`, cleared when a fresh root replaces the stack, `#[serde(default)]` on `StackSidecar`, restored before the skill early-return) including the pre-field-sidecar test, `validate_for_write` steps 1–5 untouched (no hunk touches it), and no hunk in `schema_filter` (`workflow/mod.rs:489`) — the ONE plan-frozen tools array stands.
- **"Only the comment paragraph moved since round 7"** — the strongest evidence available without a round-7 snapshot (nothing is committed since `0e1d466`, so a literal diff-of-diffs is impossible; identity is established by hunk-content + line-anchor correspondence, the same method rounds 3–7 used): every line-number anchor round 7 recorded is reproduced **verbatim** by independent searches in the current tree — `approval.rs:133`, `shell.rs:217`, `dispatch.rs:90/108/110`, the gate `:229-241`, `dispatch.rs:512`, `file_edit.rs:1296/1409`, `file_write.rs:98/189/191`, and all seven `files.rs` sites (`:37/64/128/698/858/1346/1383`) — so no line was inserted or deleted anywhere above those anchors in those files. The module-doc hunk is the only text that differs from round 7's quote (round 7 quoted the pre-fix clause as `:23-26`; the current `:24-27` carries the replacement, with `:11-23` and `:28-58` unchanged). `PLAN.md:133-142` matches round 7's Q2 quote clause-for-clause (settled as true and complete — not re-opened).
- Process note: rounds 1–7 reports are present under their original filenames with their original verdicts (FINDINGS 1h/4l, 0h/3l, 1h/3l, 0h/2l, 0h/1l, 0h/2l, 0h/1l); none was touched, and this report is the only new file in `.coding/reviews/`.

**Conclusion.** F1-R7's fix is accurate for every site it covers and cannot mislead a reader auditing "which callers run `validate` on the async runtime?": the clause now names both shapes without a count, the four-bullet census remains exact and complete for the tool layer it describes, and no other working-tree change accompanied it. The change is ready to commit on `wt/mnemo` (never `main`), including the round-1 … round-8 reports.
