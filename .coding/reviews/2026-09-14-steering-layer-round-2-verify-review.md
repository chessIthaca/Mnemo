## Verdict: PASS

Round-2 verification of the four round-1 findings (report `.coding/reviews/2026-09-14-steering-layer-round-2-review.md`, "FINDINGS (0 high, 4 low)") for plan b30475ba. Fixes landed as commit b7150a3 on `wt/agenticcoder`; working tree CLEAN (`git diff HEAD` and `git status --short` both empty — nothing uncommitted beyond the committed state). Each fix was verified at source in the current files, not just in the diff.

### All four fixes verified

1. **dispatch.rs — channel-closed arm now observes (fix 1, RESOLVED).** `dispatch_with_interrupt` (dispatch.rs:391-436) now takes `agent_id: AgentId` (all three call sites pass it: :275, :350, :365). `observe_call` runs once before the select loop (:400). BOTH completion arms observe: the `r = &mut tool_fut` arm binds the result, calls `steering.observe_result(agent_id, &r.output)` and returns it (:405-408); the channel-closed `None` arm binds `let result = (&mut tool_fut).await;`, calls `steering.observe_result(agent_id, &result.output)`, then returns it (:429-431) — with an inline comment citing review 2026-09-14 (:425-428). The module-doc claim "every tool result is matched" (steering_stats.rs:13) now holds for every completed result (the Interrupt/Cancel arms return synthetic errors without observing — correct, no tool result is produced there, the future is dropped). `observe_call` is not re-entered on loop iterations (it sits outside the loop), so no double-observation on steer/interrupt paths.

2. **spawn_agent.rs — model-override rejection hoisted before the rider (fix 2, RESOLVED).** The parent-aware check now sits at :265-275, BEFORE the rider computation at :285-294: `if forced_model.is_some() && (self.spawner.parent_aware().is_none() || self.parent_id.is_none()) { return ToolResult::error("model override requires the parent-aware spawn path (no owning agent id)"); }` — the error string is byte-identical to the removed `_`-arm check (verified against the pre-fix code in the commit diff). A rejected spawn now pays zero store round-trips. The rider is computed once into the `task` binding (:285-294) and that binding is used at BOTH spawn call sites (`&task` at :308 in `spawn_with_parent`, `&task` at :319 in the plain `spawn`). The `_` arm (:319) no longer contains any forced_model check. `forced_model` is still consumed by `forced_model.or(reviewer_pin)` (:314) — no dead-code warning. Comment cites review 2026-09-14 (:266-268). The reviewer-pin resolver call (:253-263) predates the hoisted check but is a pure model-resolver lookup, not a store round-trip, so the finding's cost concern (recall round-trip) is fully addressed.

3. **steering_stats.rs — two doc fixes (fix 3, RESOLVED).** (a) Module doc now carries the measurement caveat (steering_stats.rs:27-33): content-bearing results (`read_files` bodies, `web_fetch` text) can contain marker substrings and fire kinds spuriously; "advisory and approximate, never exact". (b) The `MARKERS` const doc (:114-120) now describes first-match-wins on multi-marker results with the co-occurrence example (content-bearing read whose payload mentions another marker) and the deliberate-simplification rationale — the old "no marker substring is a substring of another" justification is GONE from the file (verified: no trace of it remains).

4. **read_files.rs — single-prefix nudge join + batch test (fix 4, RESOLVED).** Per-file clauses are built WITHOUT the prefix (read_files.rs:244-249: `format!("{} has {count} indexed symbols — …", spec.path)`); the prefix appears exactly once via `output = format!("SYMBOL NUDGE: {}\n\n{output}", nudge.join("; "))` (:265), with a comment citing review 2026-09-14 (:262-264). New test `batch_whole_reads_join_into_one_nudge` (:791-825) asserts all four required properties: output starts with `"SYMBOL NUDGE: a.rs has "` (:804-808), contains `"; b.rs has "` (:809-813), `output.matches("SYMBOL NUDGE:").count() == 1` (:814-819), and both sections follow (`"=== a.rs"` && `"=== b.rs"`, :820-824). The existing single-file test `big_indexed_rust_file_whole_read_nudges_with_symbol_count` (:752-768) is unchanged and format-compatible: with the single-prefix join, one qualifying file yields `"SYMBOL NUDGE: big.rs has N indexed symbols — …"`, satisfying its `starts_with("SYMBOL NUDGE: big.rs has ")`, `contains("indexed symbols — graph_context(id)")`, and `contains("=== big.rs (lines 1-")` assertions.

### Spot-check re-verification (unchanged, still correct)

- **Marker substrings ↔ nudge sites:** `"is an indexed symbol"` — search.rs:243 `symbol_nudge` (shared by search_read; embeds `graph_context(id="...")`); `"TIP: for file-content search"` — shell.rs:41 `GREP_NUDGE`; `"No symbols matched"` — codegraph.rs:87 `miss_hint`; `"RECALLED CONTEXT"` — steering.rs:90 header; `"SYMBOL NUDGE:"` — read_files.rs:265. No drift.
- **Passivity:** `recalled_context_block` (steering.rs:30-95) uses `recall_peek` only (two calls, :44 and :57); the create_plan delegation preserved query semantics — plan.rs `recall_rider` folds title+goal(+symptom) into the main query and passes `bug_symptom` as the bug query (plan.rs:522-529). Both rider tests unchanged and still pin `access_count == 0` (plan.rs:1517-1565, :1568).
- **IPC/frontend:** `get_steering_stats` is a stateless sync command (trace.rs:130-133), registered in main.rs; TS wire types match (`by_marker` snake_case, types.ts `SteeringMarkerCounts`/`SteeringStatsSnapshot`); LlmTraceView polls on POLL_MS = 1500 (:654) with the same cadence as the request list and swallows poll errors (:763-765); `fired > 0` guards the header division; the view unmounts when the tab is inactive.
- **Constitution:** no Windows-only APIs in any changed line (the `\` → `/` normalization in `symbol_count_in_file` is platform-neutral by design); no new `#[allow]` (only pre-existing loop_impl.rs clippy allows); all new public items carry doc comments (`symbol_id`, `symbol_count_in_file`, `symbol_id_named`, `symbols_in_file_count`, `with_codegraph`, `with_memory`, `SteeringStats`/`MarkerCounts`/`SteeringStatsSnapshot`, `get_steering_stats`); TOOL_STRATEGY untouched (prompt.rs not in the commit's file set); README.md + PLAN.md updated in the same commit.

### Notes (non-findings)

- The step-5 test-log evidence `.coding/logs/cargo-test-step5-fixed.log` exists but is UTF-16-encoded (PowerShell redirect artifact), so its content cannot be re-parsed by the read-only review tools; the main agent reported 1649 passed / 0 failed / 1 ignored / exit 0, and all assertions in the new/updated tests were verified consistent with the implemented code statically.
- Pre-existing doc nit (unchanged line, out of the four-fix scope): dispatch.rs:384 still says "On channel close (`None`), a synthetic error is returned", but the arm returns the completed tool result (as it did before b7150a3 — the arm's inline comment at :425-428 already documents the true behavior). Left as a note rather than a finding: the line predates this plan, was reviewed in both prior rounds without being flagged, and the fix commit did not change its semantics.

All four round-1 findings are correctly and completely addressed; no new issues found in the changed lines.
