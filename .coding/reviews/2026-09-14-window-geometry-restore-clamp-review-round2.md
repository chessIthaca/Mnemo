## Verdict: PASS

Round-2 verification of plan `9ff59133` ("Clamp startup window-geometry restore: min size + fully on-screen") on `wt/mnemo`, against the **uncommitted working tree** (`git diff HEAD`: 5 modified files) plus the two untracked new files `frontend/src/lib/windowRestore.ts` (238 lines) and `frontend/src/lib/windowRestore.test.ts` (292 lines), with `.coding/plans/9ff59133.md` read as plan context. **All 8 round-1 findings are genuinely fixed in the tree, the fixes introduced no new defect, and I re-derived every changed/new test expectation by hand — all 23 are arithmetically correct and encode the intended behaviour.** No high, medium, or low findings. Three non-blocking notes at the end.

I am read-only, so I did not re-run vitest/tsc/cargo; instead I re-derived the helper's behaviour by hand on every case (below) and cross-checked the wiring, docs and invariants against the source. The parent's green runs (`npx vitest run` 1135 tests / 23 in this file, `npm run build`, root `cargo test` 2359+16) are consistent with what the code says it does.

---

## Round-1 findings — each verified in the tree

### L1 — chrome-drift direction: FIXED, and the stated direction is the correct one
All three sites now say **GROW**, and the surrounding prose is consistent:

- `frontend/src/lib/windowRestore.ts:26-30` — "the save records outer bounds while the restore sets an inner (client) size, which tao expands to the frame — so without the conversion a window would GROW by the title-bar/border height on every restart".
- `frontend/src/App.tsx:524-527` — "Chrome = outer − inner. The save records OUTER bounds while setSize takes an inner (client) size that tao expands to the frame, so without this correction the window would GROW by the title-bar/border height on every restart."
- `docs/FEATURES.md:62` — "Saved OUTER bounds are converted to an inner size via the measured chrome, so the window no longer grows by its title-bar/border height on every restart".
- Supporting sites carry no direction claim at all (`App.tsx:42-46` PersistedGeometry doc; `frontend/src/lib/windowRestore.test.ts:5-33` header) and `.coding/knowledge/spec/2027-01-11-startup-window-geometry-clamp-…md:12` states it correctly too ("the window GROWS by the chrome on every restart").

`grep` for `shrink` in the two new files returns only correct usages: `windowRestore.test.ts:87` ("shrinks a size larger than the anchor work area to fit"), `:151` ("never shrinks a good save when no monitor is known"), and the no-shrink rationale at `windowRestore.ts:212-214`. No inverted sentence survives anywhere.

Direction is right, re-confirmed against the vendored tree: the save reads `outerSize()` (`App.tsx:560`), and tao's size API is **inner-only** (`vendor/tao/src/window.rs:766 set_inner_size`, `:790 set_min_inner_size`, `:800 set_max_inner_size` — no outer-size setter), so `inner := savedOuter` yields `outer = savedOuter + chrome`, i.e. growth per restart. The conversion (outer − chrome) is what removes it.

### L2 — dropped-position path: FIXED — centering is real and the guards hold
`frontend/src/lib/windowRestore.ts:225-237`: `const area = (positioning ?? sizing).area;` then `wantedX = positioning ? savedRect.x : area.x + (area.width - (widthPhys + chromeW)) / 2;` (same for Y), clamped into `[area.x, maxX]` with `maxX = Math.max(area.x, area.x + area.width - (widthPhys + chromeW))`. The centering centres the **outer** rect (`widthPhys + chromeW`) — the correct frame to centre, since `setPosition` moves the outer top-left.

- `sizing` and `positioning` both come from `usable[0]` (`:200-201`), so when a position anchor exists they are the same monitor — `positioning ?? sizing` can never centre in a different monitor's area than the one it keeps the position in.
- **Sub-minimum work area** (the case round 1 called the subtlest): `maxX = Math.max(area.x, area.x + 640 - 816) = area.x` → the returned x is the work-area corner, so the title bar stays grabbable. Pinned by `windowRestore.test.ts:100-111`, and I verified the arithmetic (below).
- `x`/`y` are `null` **only** when `!sizing` ⇔ `monitors.length === 0` (`:215-216`, `:223`) — documented at `:39-40`, `:117-122`, `:154-159`.
- `App.tsx:540-541` now warns "no monitors reported; leaving the window at the OS default", i.e. exactly the null case; the old "saved window position is off-screen; centering instead" message is gone from the diff. `App.tsx:505-509` says "re-centered in it when the saved rect can't clear the usability gate. Only a total monitor-enumeration failure leaves the position to the OS default." No stale promise of a "centered default" on the gate-failure path survives (grep for `centered default` returns 0 geometry hits).

### L3 — MIN_VISIBLE_PX unit parity: FIXED, exact
`frontend/src/lib/windowRestore.ts:166-168`: `const minOverlap = (inputs.minOverlapPx ?? MIN_VISIBLE_PX) * factor;`, consumed at `:191` (`overlapX >= minOverlap && overlapY >= minOverlap`) — the same comparison the removed gate made (`overlapX >= 80 * factor && overlapY >= 80 * factor`), so parity is exact at every factor. Docs brought into line: the constant is documented as **LOGICAL px** "the same margin the pre-clamp restore used, which compared `80 * factor` PHYSICAL px" (`:61-67`), the override is "in logical px (compared as × factor)" (`:112`), and `docs/FEATURES.md:62` now says "80 logical px of overlap on some monitor".

### L4 — `minOverlapPx` no longer dead surface: FIXED
`frontend/src/lib/windowRestore.test.ts:222-230` ("honours the minOverlapPx override") reuses the boundary rect (`x 1700, y 1000, 400×300` on 1920×1080) with `minOverlapPx: 200`: the Y overlap of 80 now fails the 200 gate → centering → `{800, 560, 560, 260}`. Verified by hand (below). The knob is genuinely pinned — a refactor that ignored the override would fail this case.

### L5 — factor guard: FIXED, and no stale unguarded reference
`frontend/src/lib/windowRestore.ts:163-165`: `const factor = Number.isFinite(inputs.factor) && inputs.factor > 0 ? inputs.factor : 1;`. A grep for `inputs.factor` in the module returns exactly one hit — that guard line. Every downstream use goes through the local: `:168` (`minOverlap`), `:171-174` (`savedRect`), `:210-211` (`minW/minH`), `:220-221` (`width/height`), `:235-236` (position ÷ factor). A resolved `0`/`NaN` can no longer produce `Infinity`/`NaN` at the IPC. (`App.tsx:516-519` still degrades a *rejected* `scaleFactor()` to 1 — a complementary, not redundant, guard.)

### L6 — empty monitor list no longer floors the size: FIXED
`frontend/src/lib/windowRestore.ts:212-216`:
```ts
const maxW = sizing ? Math.max(minW, sizing.area.width - chromeW) : Number.POSITIVE_INFINITY;
const maxH = sizing ? Math.max(minH, sizing.area.height - chromeH) : Number.POSITIVE_INFINITY;
```
`clampNumber(v, lo, Infinity)` = `Math.min(Math.max(v, lo), Infinity)` = `Math.max(v, lo)`, i.e. a lower bound only — exactly the intent. Pinned by `windowRestore.test.ts:151-157` ("never shrinks a good save when no monitor is known": 1600×1000 survives with `x/y = null`); hand-verified below. `App.tsx:520-522` logs "failed to enumerate monitors; **bounding the size only**" and the comment at `:512-515` says "the size is then bounded by the minimum only — never shrunk" — comment, message and code now agree.

### L7 — coverage gaps + the two pin nits: FIXED (all present, all correct; L7.7 skip is acceptable)
Six new/added cases, each verified by hand:

| Case | Test | Hand-derivation |
|---|---|---|
| factor ≠ 1 **and** chrome together | `:132-144` | savedRect = 200,200,1616,1158 → overlapX 1616 / Y 1158 ≥ 160 → usable; chromeW 16 (1616>16), chromeH 39 (1158>39) → inner 1600×1119 → minW 1600 (met), minH 1120 floors 1119 → 800×560, position kept and inside → `{800,560,100,100}` ✓ (no double subtraction) |
| min floor + non-zero chrome + sub-min work area | `:100-111` | inner 284×161 → floored to 800×560; `maxX = max(0, 640−(800+16)) = 0` → x = 0, same for y → title bar stays at the work-area corner ✓ |
| monitor at negative coordinates | `:200-208` | area −1920…0; overlapX = min(−800, 0) − max(−1800,−1920) = 1000 ≥ 80, overlapY 700 ≥ 80 → usable; `maxX = max(−1920, −1000) = −1000` → clamp(−1800, −1920, −1000) = −1800 (no clamp to 0) ✓ |
| mixed-DPI layout | `:210-220` | with the window's factor 1, mon2 (1920…5760) overlapX = 3000 − 2000 = 1000 → usable, `maxX = max(1920, 5760−1000) = 4760` → (2000,100) kept; mon2's `scaleFactor: 2` provably unused ✓ — and the deliberate approximation is now documented in the code (`windowRestore.ts:178-180`) |
| hand-edited zero-size save | `:159-165` | overlapX = min(−50, 1920) − max(−50, 0) = −50 → not usable → centred: x = (1920−800)/2 = 560, y = (1080−560)/2 = 260 ✓ |
| `minOverlapPx` override | `:222-230` | minOverlap 200 → Y overlap 80 fails → centred → 560/260 ✓ |

**Pin nits fixed as described.** `not.toContain("clampRestoredGeometry(await")` is replaced by `expect(appSource.split("clampRestoredGeometry(")).toHaveLength(2)` (`:290`): the import at `App.tsx:24` carries no `(` and the single call at `:537` does — I confirmed there is no third occurrence (the mention at `App.tsx:505` reads "clampRestoredGeometry handles both:", no paren). The `setMinSize` check is scoped (`:282-288`) by slicing between the anchors `"Restore saved geometry once on mount"` (`App.tsx:487`) and `"let saveTimer"` (`App.tsx:552`); both anchors exist and are asserted (`indexOf > -1`, `> start`), the slice contains the clamp call at `:537`, and it contains no `setMinSize` — the scoping works and the assertion now expresses what its title claims. The two literal anchors are the same `?raw` contract style the repo already uses (`delegationNotes.test.ts`), and a reworded anchor fails loudly rather than silently passing, so no new brittleness of consequence.

**L7.7 (optional) skip — acceptable.** The rationale is recorded where a reader will hit it (`windowRestore.ts:49-55`: the mirror is "deliberately PROSE-ONLY … pinning it from here would need a cross-root `?raw` import and a vite `server.fs.allow` escape") and echoed in the plan amendment and the SPEC record. The claim is technically accurate, the 800×560 pair is stated in the module docs, the test header, `docs/FEATURES.md` and `src-tauri/src/main.rs:244`, and round 1 explicitly marked the pin optional. Not worth the build-plumbing for a constant that changes only when the app's real minimum changes.

### L8 — plan bookkeeping: ADEQUATE
`.coding/plans/9ff59133.md:12` carries a "Landed design — 2027-01-14 (review round 1: 0 high / 8 low, all fixed)" paragraph with the architecture, the outer-vs-inner unit convention, the sizing/position anchor split, the degradation matrix, four invariants to preserve, all 8 fixes itemised and the L7.7 skip; `:32-33` records `## Landed design` / `yes` (the feature-scale flag). The finish gate ("a `landed_design` plan whose context carries no 'Landed design' amendment") is satisfied.

Detailed-step 4's unticked box is explained in that same amendment, and the explanation is factually correct: `complete_step` is not available to the main agent while the workflow is in Reviewing, and Reviewing can only exit via `finish` (Complete) or the destructive `abandon_plan` — so the box genuinely cannot be ticked mid-review. The top-level progress is 4/4, so nothing is gate-blocked, and the amendment is the right record. Not a finding.

---

## Hand-verification of the remaining expectations (no action needed)
Beyond the table above I re-derived the other twelve expectations directly from the function body and all match: untouched geometry (`:69-76`, x/y kept at 100/100), the user-report case (`:78-85`, 300×200 → 800×560 at 0,0), the shrink-to-fit case (`:87-90`, 2000×1200 → 1920×1080 at 0,0 with `maxX/maxY` collapsing to 0), min-floor-over-smaller-area chrome-free (`:92-98`), outer→inner round trip (`:113-120`, outer 800+16 × 560+39 → exactly 800×560), the physical/logical conversion at factor 2 (`:122-130`, inner 800×600 → floored to 1600×1120 physical → 800×560 logical, x 200→100), min-only with no monitor (`:146-149`), partial overlap pulled inside (`:169-175`, boundary overlap exactly 80 passes `>=`, then x 1700→1120, y 1000→520), below-gate centring (`:177-187`, (1920−1000)/2 = 460, (1080−700)/2 = 190), largest-overlap anchor (`:189-198`, mon1 overlap 20 < 80 vs mon2 880 → x pulled to 1920), and the work-area-vs-full-monitor pair (`:232-250`, y 120→100 with the 40 px taskbar strip, 120 retained without a work area). Count: 11 size + 7 position + 5 source-contract = **23**, matching the parent's vitest count.

## Regression / invariant re-checks
- **Startup-only intact.** The helper is called exactly once, inside the mount restore (pinned by `:287`, `:290`). The diff's hunks end at `await win.setSize(new LogicalSize(restored.width, restored.height));` (`App.tsx:543`) — the `let saveTimer`/`save`/`debouncedSave`/`onResized`/`onMoved`/`beforeunload` block (`App.tsx:552-620`) and its cleanup are byte-identical, the `restoreDone` gate (`:480`, `:547-549`) and the `maximized` skip are intact, and no Rust file appears in `git diff HEAD` or `git status` (`src-tauri/` untouched; `min_inner_size` unmodified).
- **Outer/inner consistency of the position clamp.** `setPosition` is an outer-position setter — tao exposes only `set_outer_position` (`vendor/tao/src/window.rs:738`) and no inner-position setter — which matches the `outerPosition()` the save records (`App.tsx:559`), and both the centring and the `maxX/maxY` guards size the frame as `widthPhys + chromeW` (outer). So "fully inside the work area" is stated for the frame that is actually placed.
- **No regression from the three rewired branches.** Centring: the anchor used for centring is the same monitor the sizing bound came from, and the `Math.max(area.x, …)` guards preserve the "title bar at the corner" property when the min overshoots the area. Infinity max: only reachable with an empty monitor list, where the function returns early with a null position, so no `Infinity` can reach `setSize` from this branch. Factor guard: strictly defensive (a `0`/`NaN` factor previously round-tripped an IPC failure). Reworded log/warning: the message now matches its only trigger condition (`x`/`y` null ⇔ no monitors).
- **Multi-platform neutrality.** `windowRestore.ts` imports nothing (no `@tauri-apps/api`, no node/platform module) and contains no `cfg`/`process.platform`/Windows-path assumption; `App.tsx` uses only cross-platform Tauri APIs (`getCurrentWindow`, `availableMonitors`, `LogicalPosition`, `LogicalSize`), and `workArea` is runtime feature-detected with a full-monitor fallback (`windowRestore.ts:146-152`). Nothing Windows-only was added; the same code path runs on macOS (where `workArea` excludes the menu bar/dock) unchanged.
- **Docs sync.** `docs/FEATURES.md:62` is now accurate on all three rewording points (clamped restore, "centered in that work area when the saved rect fails the usability gate", "80 logical px of overlap", "no longer grows by its title-bar/border height", startup-only scope). `README.md` and `PLAN.md` contain no window-geometry statement (grep for `geometry|remembers|min_inner_size|window position`: README matches only the "remembers." tagline; PLAN.md zero hits), so nothing there is stale. Every exported symbol in the new module has an accurate doc comment (`RESTORE_MIN_*` `:49-59`, `MIN_VISIBLE_PX` `:61-67`, `SavedGeometry` `:69-78`, `MonitorGeometry` `:88-100`, `ClampInputs` `:102-114`, `ClampedGeometry` `:116-129`, `clampRestoredGeometry` `:154-160`).
- **Test wiring.** `frontend/src/lib/windowRestore.test.ts` is registered in the explicit allow-list (`frontend/vitest.config.ts:90`) and is discovered by the include-guard (`frontend/src/lib/vitestInclude.test.ts` via `import.meta.glob`), so it cannot be silently skipped. Its insertion point between `tauri.test.ts` and `vitestInclude.test.ts` violates no convention — that list is append-ordered, not sorted.
- **Constitution.** No `#[allow]`, no shell-based file mutation in the change, nothing committed to `main`, regression test present and traceable to the user's report, warning-free Rust untouched.

## Non-blocking notes
1. **Plan-file sketches are superseded, not rewritten.** Detailed steps 1 and 3 (`.coding/plans/9ff59133.md:21`, `:23`) still carry the plan-time sketch — "x=null = caller skips setPosition, centered default" and `else keep the existing console.warn("saved window position is off-screen; centering instead", saved)`. The "Landed design —" paragraph contradicts both explicitly ("x/y are null only when NO monitor is known"), and all steps are complete, so there is no resumption risk — but a reader who reads only the sketch could re-derive the old null semantics. Historical plan text; not worth rewriting.
2. **Chrome-probe failure degrades the centring too.** If `outerSize()`/`innerSize()` ever fail (`App.tsx:533-536` → `{0,0}`), the computed frame is one chrome shorter than reality, so on the centring path the placed window can overflow the work-area edge by up to the title-bar height. Console-only signal; inherited from the pre-existing degradation note round 1 recorded, and there is no better fallback without the measurement.
3. **Hostile hand-edit of `1e999`.** `JSON.parse("1e999")` yields `Infinity`, which would reach `setSize` and be rejected at the IPC (the outer `catch` logs and the window keeps the default 1200×720). Same exposure as before the fix (the old code passed the saved value verbatim); round 1's claim that `NaN`/`Infinity` "cannot arrive through JSON" is slightly imprecise, but the degradation is safe and out of the user-reported scope.
4. **Commit contents.** The fix's commit will carry unrelated `.coding` bookkeeping (`backlog.jsonl`, `plans/ee65fd4b.md`) and four untracked knowledge records — by design, since `.coding/` is the mergeable side-car.

**Final answer for the parent: PASS — `.coding/reviews/2026-09-14-window-geometry-restore-clamp-review-round2.md`. All 8 round-1 findings verified fixed in the working tree (L1 wording now GROW and correct; L2 centring with `maxX/maxY` corner guards and null only for no-monitor; L3 exact `× factor` parity; L4 `minOverlapPx` pinned; L5 single guarded local `factor`, no stale reference; L6 `Infinity` max so an unavailable monitor list never shrinks a good save; L7 all six new cases present with correct arithmetic plus both pin nits reworked — the L7.7 skip is acceptable; L8 land-design amendment + `landed_design=true`). No regression and no new problem found — startup-only invariant, outer/inner unit pairing, multi-platform neutrality and doc sync all hold. No findings to fix; proceed to commit on `wt/mnemo` (include this report) and `finish`.**
