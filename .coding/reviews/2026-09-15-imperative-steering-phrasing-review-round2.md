## Verdict: PASS

Round-2 (FINAL) closing review of the SAME uncommitted change round 1 reviewed — plan 987fef4c, backlog 56168c38 ("Imperative phrasing for steering notes and tool-strategy guidance"), branch `wt/mnemo`, **no new commits since round 1**. Reviewed: the full `git_read(op="diff")` (17 tracked paths, +57/−50) plus the two untracked bookkeeping artifacts (`.coding/plans/987fef4c.md`, `.coding/reviews/2026-09-15-imperative-steering-phrasing-review.md`).

All three round-1 LOW findings are fixed and independently verified. Nothing else moved. The marker/label contract, the re-issue escape hatch, the frontend display filter and every moved pin still hold, and no test assertion was relaxed. **The change is ready to commit.**

Method: read the plan + the round-1 report, the full diff, both repaired knowledge records, the full frontend registry (`delegationNotes.ts`, 437 lines) and its fixtures, the marker table/label/targets wiring, every touched production site and its pinning tests; then targeted searches for each removed phrasing (`prefer the graph tools`, `prefer running the command`, `prefer smaller fragments`, `consider memory_consolidate`, `are safest`, `prefer` in `src/**/*.rs`, `Advisory` in `src/**/*.rs`, `2026-09-15 steering spec`) and the new tails. Tests were NOT re-run (read-only reviewer) — see "Test status".

## 1. Fix 1 — doubled amendment prefix repaired in both knowledge records ✓

`.coding/knowledge/how/2026-08-29-search-glob-shapes-extension-anchored-only-bare.md:8` now opens **`Amended 2027-01-11: the zero-file hint's wording changed — …`**; `.coding/knowledge/spec/2026-09-01-symbol-intent-patterns-earn-the-graph-nudge-shap.md:14` now opens **`Amended 2027-01-11 (plan 987fef4c, backlog 56168c38 — "Imperative phrasing for steering notes and tool-strategy guidance"): the nudge SENTENCE TAILS are now imperatives.`** — exactly the expected forms.

- **Exactly ONE `Amended` heading per amendment paragraph.** HOW file: line 8 only (line 6's date references are "learned 2026-09-15"/"CORRECTED 2026-09-17" prose, not headings). SPEC file: line 14 only (line 12's pre-existing `AMENDMENT (2026-12-29 …)` heading is a different word and predates this plan). The `Amended 2027-01-11: Amended 2027-01-14 …` / `… : Amended 2027-01-14 (plan …` doubles are gone.
- **Collapse is minimal and grammatical.** HOW: the `memory_amend` stamp `Amended 2027-01-11: ` was kept, the inner `Amended 2027-01-14: ` removed (one space after the colon). SPEC: `: Amended 2027-01-14` removed, leaving `Amended 2027-01-11 (plan 987fef4c, …)`. In both cases the paragraph body is preserved intact and coherent — no doubled spaces, no stray `:`/`(`, no truncated clause at the join.
- **Typography/encoding intact.** Em dashes, `**`-globs, backticks, curly quotes and `—`/`…` all render correctly in both paragraphs (git diff, `read_files`, and the `are safest` search all show clean UTF-8, no `â€”`-style mojibake); the HOW paragraph's glob/brace-alternation/bare-`**` statements and the SPEC paragraph's branch quotes + "UNCHANGED and still contract" list + pin pointer are complete.
- **Front matter untouched, no BOM, no line-ending churn, nothing else changed.** Both hunks are append-only (`2 ++` each: a blank line + the paragraph, zero deletions) and start *after* the front matter (HOW `@@ -4,3 +4,5 @@`, SPEC `@@ -10,3 +10,5 @@`), so line 1 is context — a BOM insertion or a front-matter edit would have shown there. A whole-file rewrite would have shown every line as changed; it does not. Trailing newline preserved (no `\ No newline at end of file` marker).
- **The shell fallback was the sanctioned case.** `.coding/knowledge/**` is file-tool-protected by design and `memory_amend` only appends (`"{}\n\nAmended {}: {}"`, `src/memory/knowledge.rs`), so it demonstrably could not remove the stray prefix — an explicit read/write replace was the only repair path.
- **The paragraphs' claims match production.** SPEC: the quoted bare branch equals the rendered `search.rs:678-682` string; the fuzzy branch matches `:684-688`; the alternation tail matches `:622-623`. HOW: the new hint matches `search.rs:1491-1492` and `search_read.rs:352-353` rendered output.

## 2. Fix 2 — `docs/FEATURES.md` glob quote is verbatim ✓

`docs/FEATURES.md:41` now reads **`("the glob matched no files — if that's unexpected, use extension-anchored shapes like **/*.rs")`**, character-identical to the production sentence rendered at `src/tool/agent/search.rs:1491-1492` and `src/tool/agent/search_read.rs:352-353` (the leading `; ` of the code-level concatenation is correctly not part of the quote). `are safest` now appears **nowhere** in `docs/**` (search: zero matches) and nowhere in `src/**`; its only remaining occurrences are historical quotes (the HOW record's body line 6, two old plan docs, the round-1 report) — correct, those are history.

## 3. Fix 3 — `search.rs` doc comment points at the live pin ✓

`src/tool/agent/search.rs:646-652` now reads `…the bare-identifier sentence is byte-pinned by \`bare_identifier_symbol_searches_earn_the_graph_nudge\` (wording moved to imperatives 2027-01-14, plan 987fef4c / backlog 56168c38) and must not change casually.`

- The named pin exists and is the right one: graph-confirmed `src/tool/agent/search.rs::bare_identifier_symbol_searches_earn_the_graph_nudge::2121` (spans `:2121-2207`). Its escape-repeat phase asserts the **FULL** sentence in one `contains` (`:2169-2177`); I compared it char-by-char against the rendered branch-1 production string (`:677-682`) — identical: `'hello' is an indexed symbol — graph_context(id="a.rs::hello::1") gives its definition + callers in one call; use graph_search/graph_context for symbol lookups — use search only for text`.
- The comment no longer cites the superseded 2026-09-15 steering spec, and the rewording is recorded with plan/backlog pointers while keeping the "don't change it casually" intent. (The one residual `2026-09-15 steering spec` string left in `src/**` is the provenance parenthetical in the assertion's own comment — see "Checked, deliberately not flagged" #1.)

## 4. Nothing else moved ✓

- **Path inventory unchanged**: `git status --short` shows exactly 17 modified paths (`.coding/backlog.jsonl` dispatch marking: item 56168c38 pending→`in_flight` + `plan_id`/`plan_title`; the 2 knowledge records; `docs/FEATURES.md`; `frontend/src/lib/delegationNotes.ts` + `.test.ts`; `src/agent/prompt.rs`; `src/agent/steering_stats.rs`; `src/codegraph/mod.rs`; `src/tool/agent/codegraph.rs`; `src/tool/agent/file_edit.rs`; `src/tool/agent/file_write.rs`; `src/tool/agent/search.rs`; `src/tool/agent/search_read.rs`; `src/tool/agent/shell.rs`; `src/tool/steering.rs`; `src/tool/workflow/plan.rs`) plus the 2 untracked bookkeeping artifacts (plan file, round-1 report) — both expected.
- **Every hunk is attributable** to the plan's conversion + the three round-1 fixes; per-file diffstat reconciles with a hunk recount (`search.rs` 23, `plan.rs` 18, `test.ts` 14, `prompt.rs` 12, `steering.rs` 7, `shell.rs` 3, the knowledge files 2+2, the remaining files 2–4). Arithmetic cross-check: `+57/−50` vs round 1's `+53/−48` is exactly the new `search.rs` doc-comment hunk (`2−/4+`); the knowledge and docs repairs are same-line-count replacements and correctly leave the diffstat unchanged. No unexplained hunk anywhere.
- **Untouched as required**: the two round-1 deliberate leaves are not in the diff at all — `src/tool/agent/codegraph.rs:241` "(exact match preferred)" and `src/tool/workflow/ask_user.rs:43`/`:106` "(at least 2; 2–6 recommended/ideal)" (`codegraph.rs`'s single hunk is the `:86-90` doc comment; `ask_user.rs` does not appear).
- **No live advisory phrasing survives**: `prefer the graph tools` → zero hits in `src/**`, `src-tauri/**`, `frontend/**` (only `.coding/**` history); `prefer running the command` / `prefer smaller fragments` / `consider memory_consolidate` → zero live hits (`consider memory_consolidate` only in old plan/review docs); `prefer` in `src/**/*.rs` → 7 hits, all internal `//`/`///` comments (provider `client_factory.rs`, `file_write.rs:203`, `spawn_agent.rs:312`, `codegraph/query.rs:505`, `memory/indexer/knowledge.rs:357`) — none model-facing.

## 5. Contract + pins re-confirmed ✓

- **Marker/label contract byte-intact.** `MarkerKind::marker()` / `targets()` / `label()` (`src/agent/steering_stats.rs:113-179`) are context lines in the diff; that file's only changes are the two test fixtures (`:799-803` ShellRedirect matrix row, `:1093-1096` ConsolidationDue dispatch fixture), both carrying the NEW production strings and both still exercising the same assertions. Marker substrings present at their emission sites: `is an indexed symbol` in both `symbol_nudge` branches (`search.rs:679`, `:685`) and every alternation entry (`:609`, `:612`); `TIP: output redirection detected` still opens `REDIRECT_NOTE` (`shell.rs:50`); `working-memory events accumulated this session` still in `src/tool/steering.rs:118`. The alternation prefix `from the alternation pattern '` (`search.rs:622`) is untouched.
- **Escape hatch untouched.** `const ESCAPE` (`src/tool/agent/codegraph.rs:129`) is byte-identical and the file's only hunk is the doc comment; the test still asserts the escape line (`search.rs:2144-2148`); the memory-delegation twin is untouched (no code hunk in that file).
- **Frontend display filter unaffected.** Every registry matcher (`delegationNotes.ts:118-270`) keys on stable heads (`AUTO-DELEGATED`, `from the alternation pattern`, `'…' is an indexed symbol`, `the symbol '…' (from pattern '…') is an indexed symbol`, the TIPs, `RECALLED CONTEXT`, `SYMBOL NUDGE:`, `known memory hit:`, `/^NOTE: \d+ working-memory events accumulated this session/`, `TIP: output redirection detected`, the stale-read drift sentence) — none references a removed tail. The new tails (`use graph_search/graph_context …`, `use … search only for text`) match no def prefix/pattern, so `isComponentStart` (`:301-315`) cannot mis-split the merged alternation note; `delegationNotes.test.ts:189-196` still pins that merged case with an exact `.toBe` over the new text.
- **No assertion relaxed.** `search.rs:2169-2177` still asserts the FULL sentence (same strength, new wording); the `steering_stats.rs` fixtures keep identical structure (input-string updates only); `delegationNotes.test.ts` keeps its exact `.toBe` expectations (`:142-160`, `:189-196` — fixtures/expectations updated, strength unchanged); no test in any other touched file changed. `docs/FEATURES.md:41-42` remain prose-consistent (the consolidation sentence now reads "…a once-per-session consolidation-due note directing `memory_consolidate`…") and quote no stale string.
- **Multi-platform neutrality / file-tools-first**: string, comment, markdown and bookkeeping edits only — no OS-specific code, no new non-ASCII in a file that lacked it; the only shell-mutation artifact (the `.coding/knowledge/**` repair) is the documented sanctioned fallback, and the diff hunks are line-scoped with no BOM/line-ending churn and no `#[allow(...)]`.

## Checked, deliberately not flagged (no new findings)

1. **`src/tool/agent/search.rs:2165-2168`** — the assertion's own comment still opens `// Byte-pin (2026-09-15 steering spec, review F2 of 2026-12-06-symbol-intent-nudge-review): …`, naming the now-superseded record (`.coding/knowledge/spec/2026-08-29-symbol-nudge-carries-the-symbol-id-graph-context.md`, `status = "superseded"`). **Not flagged**: those lines are diff *context* (unchanged by this plan), the parenthetical is provenance for the pin's origin (review F2's argument was made against the then-live spec), and it makes no claim about the live authority — which is exactly the difference from the `:646-650` doc comment round 1 flagged ("is byte-pinned by the 2026-09-15 steering spec and must not change") and which is now fixed. Optional cosmetic nit if the user ever wants zero references to the superseded record: `// Byte-pin (review F2 of 2026-12-06-symbol-intent-nudge-review; wording moved to imperatives 2027-01-14, plan 987fef4c): …` — no re-review needed.
2. **HOW record body (`:6`) still quotes the pre-amendment hint** ("…extension-anchored shapes like **/*.rs are safest") as the record's original text. Correct: `memory_amend` appends, and the amendment at `:8` states exactly what changed — round 1's own convention (superseded/older records legitimately hold old text) applies; the amendment is the live statement.
3. **Retained stamp date `2027-01-11`** in both amended records (round 1's fix note suggested `2027-01-14` as an "e.g."): the retained date is the `memory_amend` tool stamp, it is now consistent across both records, the doubled/contradictory pair is gone and exactly one heading per paragraph remains — the finding is resolved; the wording matches the state this round-2 prompt specifies.
4. **Model-facing strings containing the word "Advisory"** — `src/tool/agent/image_tools/tools.rs:155`/`:611` `"description": "Advisory: enable the backend's reasoning mode (no-op on our vision backend)."` and `src/agent/prompt.rs:66` `enforced, not advisory:`. These are labels/negations whose content is "this parameter is a no-op" / "this IS enforced", not preference guidance; converting them to imperatives would invert their meaning. Same category as the round-1 deliberate leaves — no conversion.
5. **Historical `.coding/**` artifacts** (the two 2026-08-29 specs, the superseded 2026-09-15 spec, old plan docs, the backlog item text, the round-1 report) still quote the old tails. Correct history; the *live* records were amended (fix 1) and no live record or doc asserts the old sentence as current.

## Out-of-scope observation (pre-existing, NOT a finding, not blocking)

`src/tool/agent/image_tools/tools.rs:33` and `:102` contain `advisory only â€” our vision backend …` — mojibake (double-encoded em dash) confirmed by direct read, not a search artifact. The file is **not** part of this diff (pre-existing), so it is outside this change's scope and outside this review's verdict; worth a separate cleanup item if the user wants it fixed. Mentioned only because the `Advisory` sweep surfaced it.

## Test status

Not re-run (read-only reviewer). Basis for accepting the reported green suites — `cargo test` at the repo root 2366 passed + 16 passed / 0 failed, exit 0; frontend `npm run test` 1175 passed / 83 files; `npm run build` clean: (a) the three round-1 fixes touch a Rust doc comment, one markdown quote, and two knowledge-record prose paragraphs — none can affect compilation or any test literal; (b) every production literal changed by the plan is either asserted by an updated pin (`search.rs:2169-2177`; `steering_stats.rs:799-803`, `:1093-1096`; `delegationNotes.test.ts:142-160`, `:189-196`) or asserted nowhere (zero live hits for every removed phrasing), so no un-updated assertion can be left failing-green; (c) `#![deny(warnings)]` at both crate roots makes the green root run prove zero warnings; (d) the stable-head byte test is state-comparison-based (`stable_head_byte_stable_across_workflow_changes`) — no frozen literal needed updating, consistent with `prompt.rs` carrying no test hunk while the suite is green; (e) the two frontend changes are fixture/comment strings, matching the reported green vitest run and build.
