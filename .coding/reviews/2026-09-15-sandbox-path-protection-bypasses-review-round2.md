## Verdict: FINDINGS (0 high, 1 low)

Summary: all six round-1 fixes are real, complete and verified against SOURCE (not the diff alone). The two substantive ones hold up under hand-tracing. LOW 2's re-check genuinely closes the concurrent-link race — the escape cannot happen without the planted link existing on a component the walk reaches at re-check time — `Ok(validated)` is now reachable only for a link-free, just-created parent, and no legitimate create-parents write is newly refused. LOW 3's dangling-directory-link fallback does reach the step-5 `symlink_metadata` LEAF check (for a dangling reparse point `validate`'s parent fallback returns `Ok`, so the step-2 gate is skipped — the assertion is not vacuous), and the skip is loud in both spellings. LOW 1, 4 and 6 are closed exactly as described. LOW 5's policy sentence now matches `dispatch.rs`, but the rewrite introduced one new, small inaccuracy: its new parenthetical misattributes the link clause to plan b4812291 when that clause shipped in plan ee65fd4b (2027-01-11). That is the single finding below; nothing in the change set is behaviourally wrong, and no regression is introduced.

### LOW — the research-denial sentence attributes the LINK clause to the wrong plan

`PLAN.md:448-450`:

> … any link component that leaves the root or cannot be resolved, and any hardlinked `.coding/**` file stay denied (**the last two since plan b4812291, 2027-01-15**) …

"The last two" = the link-component clause and the hardlink clause. The hardlink rule is indeed from b4812291, but the link-component rule was closed by plan **ee65fd4b** on **2027-01-11** and is untouched by this diff:

- `src/agent/dispatch.rs:88-113` (`research_write_verdict`, the canonical branch at :97 and the link-free gate at :109) is byte-identical in this change set — the only uncommitted `dispatch.rs` edits are the `mod tests` fixture import and the deletion of the two private fixture fns (`git diff HEAd -- src/agent/dispatch.rs`).
- `git log -- src/agent/dispatch.rs` shows exactly two commits ever: the initial commit and `557f20e "Research plans may write .coding artifacts; close the sub-plan review leak (plan ee65fd4b, options A + D)"` — so the link clause shipped in ee65fd4b.
- The same repo says so twice: `src/tool/agent/sandbox.rs:37-39` (pre-existing, outside this diff) dates the research carve-out *and* its `symlink_metadata` walk to "2027-01-11, plan ee65fd4b", and the sentence's own earlier parenthetical (`PLAN.md:447`) dates the artifact-path dispatch change to "(2027-01-11)".
- The dangling/"cannot be resolved" half is equally pre-existing: `research_write_verdict_fails_closed_on_a_link_out_of_the_root` (`dispatch.rs:1982-2014`, incl. the `.coding/dangling.md` assertion at :2004-2010) is committed code, not part of this diff.

Why it matters: PLAN.md is the technical-decision document a reader consults to find *where* a rule came from; as written, someone looking for the research-route link denial in b4812291 finds nothing, and the file contradicts itself two lines above. Fix is one clause, e.g. "(the hardlink rule since plan b4812291, 2027-01-15 — the link rule since plan ee65fd4b, 2027-01-11)". Documentation-only; no code change.

### Fix verification — each of the six, traced against source

**LOW 1 — module doc restored. CLOSED.** `src/tool/agent/sandbox.rs:666-675` carries the fixture prose ("Link fixtures shared by the escaping-link tests in this crate. / ONE canonical spelling of …") immediately above `#[cfg(test)] pub(crate) mod link_fixture` (:675-676), and `fn link_count` (:582) now has only its own doc block (:571-581, link-count semantics + the directory exemption). No leftover duplication: the `New-Item`-skip rationale stays where it belongs, inside `plant_dir_link` (:722-723), and both fixtures carry their own docs (:677-679, :695-699). `cargo doc` no longer renders a wrong description for `link_count`.

**LOW 2 — the created-parent fallback is fail-closed now. CLOSED.** `sandbox.rs:510-519`: the arm re-runs `self.lexical_path_is_link_free(&validated)` and refuses with the same "traverses a symbolic link or junction" message the step-2 gate uses; `Err(e) => Err(e)` at :520 covers everything else. `Ok(validated)` is reachable from exactly one arm, guarded by `created_parent` AND link-freedom — as the step list (:426-433) and the arm comment (:501-509) now state.

*Is the race really closed?* Walk semantics (`:383-398`): iterate the root-joined components of the path, lstat each; any existing symlink/junction (junctions report `is_symlink()` on Windows — the same fact the shipped research-route tests depend on) → `false`; the first missing component → `true`. For the round-1 trace (`.coding/link/new/x.md`, link planted between the gate and `create_dir_all`): at re-check time the link necessarily EXISTS, because the escape itself required `create_dir_all` to traverse it, and every component above it existed when the gate ran — so the walk reaches the link component and reports a reparse point → refusal. A link cannot be "missed" by the stop-at-first-missing rule in any escaping case: reaching a missing component instead means the write fails with ENOENT (`create_dir_all` succeeded, so the dirs are there unless someone removed them), never an escape. Leaf-level races are covered by other layers: a live leaf link makes step 5's `validate` fail `PathOutsideRoot` with `created_parent == false` → `Err(e)` (:520); a dangling leaf makes `validate` return `Ok` via its parent fallback and is caught by the leaf check at :492-498.

*Over-refusal?* None. The ordinary create-parents case never enters the arm: after step 4 the parent exists, so step 5's `validate` succeeds → the `Ok(canonical)` branch. Traced `.coding/analysis/deep/new.md` (analysis exists, `deep` missing): gate link-free (`deep` missing → `true`), 2b ancestor `.coding/analysis` unprotected, step 3 unprotected, mkdir, step 5 canonical `Ok` — still succeeds. `.coding/analysis/notes.md` is asserted `Ok` in the hardlink test. The only new refusals are the documented ones (mkdir under a link component; a link planted in the gate→mkdir window).

*Doc claims.* `canonical_existing_ancestor`'s doc (:650-654) states the one exception ("a parent this call itself just created, which is re-checked for link-freedom") and it is true of the code; the step list (:409-433) describes gate → 2b → 3 → 4 → 5, the canonical re-check and the narrowed fallback exactly; `PLAN.md:132-141` and `docs/FEATURES.md:22` both carry the re-checked-fallback wording and match. Nothing over-claims "fails closed" without the qualifier any more.

**LOW 3 — the dangling-leaf sub-assertion can no longer vanish. CLOSED, and it reaches the intended branch.** `sandbox.rs:1538-1575`: the file-link spelling is tried first, then a dangling DIRECTORY link (junction) at `.coding/dangling`, and the `None` arm prints `SKIP: could not create a dangling link leaf …` (:1572-1574); the Windows junction path additionally prints its own failure diagnostics (:724-730), so the skip is never silent.

*Does the dir-link spelling hit the LEAF check rather than the gate (which would make it vacuous)?* Traced for the junction: `validate` (:113-144) sees `candidate.exists() == false` (a dangling reparse point's target is missing, so `metadata` fails), then the parent fallback (:129-136) canonicalizes `.coding` and appends the raw leaf → `Ok`. Step 1 therefore SUCCEEDS, which means the step-2 gate (:442-450) and 2b never run. Step 3 is not protected (the hardlink guard's `metadata` fails on the dangling target → `link_count` `None`, :302; the leaf name `dangling` is not in the protected set). Step 4 does not mkdir (`.coding` exists, `created_parent` stays false, :471-475). Step 5 takes `Ok(canonical)` and the `symlink_metadata` LEAF check at :492 refuses. So the assertion is non-vacuous and pins the branch that closes the dangling-leaf shape. This is corroborated by shipped behaviour: `dispatch.rs:91-96` documents that a dangling leaf comes back from `validate` as its own path via the same parent fallback, and the committed dispatch test asserts the denial of `.coding/dangling.md`.

*Emptiness assertion.* Planting a junction (or a file symlink) creates no target, so both `read_dir(outside).count() == 0` assertions (:1532-1536, :1566-1570) still hold; the earlier loop refuses `.coding/link/x.md` and `.coding/link/nested/x.md` at the gate before any mkdir, so nothing is planted outside.

**LOW 4 — comment corrected, alias layer pinned. CLOSED.** The test comment (:1626-1636) now names the **step-2 link-free gate** as the layer that fires and 2b / step 5 as backstops for spellings the gate cannot see (a Win32 8.3 alias is not a symlink) — accurate: `.coding/link/new/x.md` fails step 1 (its parent does not exist) and the gate refuses at :444 before 2b is reached. New test `canonical_existing_ancestor_pins_the_alias_layer` (:1413-1448) is cross-platform, calls the helper directly (:1428, :1441) so the helper can never be deleted silently (compile error instead of green), and its assertions are meaningful: the deepest existing ancestor of `.../.coding/knowledge/deep/x.md` is `.../.coding/knowledge` (`deep` does not exist, so `ends_with("knowledge")` really pins the DEPTH, not just the leaf), `is_protected_write_target` refuses it via the by-name rule, and the `.coding/analysis` ancestor stays writable. Noted, not a finding: the fixture name is not an alias, so on Windows the assertions would also hold for a helper that skipped `.canonicalize()` — the call site remains guarded only by the 8.3 test's "no subtree planted" assertion where aliases exist, which is the coverage note round 1 already accepted.

**LOW 5 — policy wording fixed; provenance slip introduced (see finding).** `PLAN.md:445-451` now reads "any link component that leaves the root or cannot be resolved … an in-root link that resolves inside the artifact tree is still a grant", which is what `dispatch.rs:88-113` does: `.coding/link -> .coding/analysis` + `file_write .coding/link/report.md` → canonical parent resolves inside the artifact tree, `lexical_path_is_link_free` is trivially true for the canonical form → `Artifact` (grant); a link out of the root or a dangling link → `NotApplicable` (denied). The remaining defect is the attribution parenthetical, above.

**LOW 6 — plan file consistent. CLOSED.** `.coding/plans/b4812291.md:26` (skeleton step 4 "Verify") and the two detailed-step boxes `**Docs sync**` (:34) and `**Full verification**` (:35) are all `- [x]`, matching `4/4 steps done` and the visibly shipped doc edits. Treated as the deliberate approval-gated shell state change described in the brief (file tools refuse `.coding/plans/**` by design; `complete_step` is hidden in Reviewing) — not a policy violation.

### Also checked (fresh pass)

- **No new over-refusal from the LOW 2 arm** — see the trace above; the arm requires `created_parent`, which the normal path never sets when step 5 can canonicalize.
- **Warning hygiene.** Repo-wide `#[allow(`/`#![allow(` search over `src/**/*.rs` (124 files): zero matches — no silencing escape hatch was added, so the green-suite claim under `#![deny(warnings)]` (`src/lib.rs:5`) is not propped up by an allow. The fixture move leaves no unused import: `plant_file_link` is still used at `dispatch.rs:2008` and `sandbox.rs:1546`; `plant_dir_link` at `dispatch.rs:1955`/`:1987`, `agent/tests.rs:5503`, `sandbox.rs:1523`/`:1551`/`:1640`, `file_write.rs:680`.
- **Symbols are real.** `canonical_existing_ancestor_pins_the_alias_layer` (`sandbox.rs:1414`), `canonical_existing_ancestor` (:655) and `link_fixture` (:676) all exist in source. The code graph does not list the first one because its index predates the uncommitted edits (it still reports `validate_for_write` at :383 vs the file's :434, and warns that 34 files are newer than the index) — index staleness, not a missing symbol. The plan's five regression-test names all exist: `protected_hardlink_inside_coding_refused` (:1579), `protected_win32_8dot3_alias_refused` (:1456), `validate_for_write_fails_closed_on_out_of_root_link` (:1510), `validate_for_write_refuses_in_root_link_into_protected_tree` (:1625), `file_write_through_out_of_root_link_is_denied` (`file_write.rs:670`).
- **Docs internally consistent.** `sandbox.rs:55-63` (module doc's syscall accounting: the depth-bounded walk + one ancestor `canonicalize` on the creation path, one `metadata` per `.coding/**` protection check, one final `symlink_metadata` on the resolved target — all inside the tools' existing `spawn_blocking` closure), the predicate doc `:210-220` (hardlink rule, fail-closed, directory exemption, outside-`.coding` residual), the step list, `PLAN.md:132-141`/`:445-451`, `docs/FEATURES.md:22`: mutually consistent and consistent with the code.
- **Fixture/junction note (not a finding).** The dangling DIR link is the spelling I cannot execute here; if PowerShell refuses a junction to a non-existent target, the visible SKIP marker fires — the LOW-3-compliant outcome (the file-link spelling still covers POSIX). If it succeeds, the assertion lands on the leaf check as traced.
