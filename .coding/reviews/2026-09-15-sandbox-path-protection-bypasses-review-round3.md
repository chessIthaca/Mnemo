## Verdict: PASS

Summary: the round-2 finding is closed correctly — `PLAN.md:449-451` now attributes the hardlink rule to plan b4812291 (2027-01-15) and the link rule to plan ee65fd4b (2027-01-11), which is exactly what the history says, and it no longer contradicts the sentence's own earlier `(2027-01-11)` parenthetical two lines above (`PLAN.md:447`). I checked every other provenance claim in the uncommitted diff one by one (PLAN.md, docs/FEATURES.md, `sandbox.rs` doc comments and inline comments, the new test comments, Cargo.toml's comment) against the code and the record: all of them are supported. The only thing worth a sentence is a citation-granularity ambiguity in the *same* parenthetical (round 2 has no `§Q5` section — its bypass content is finding L1), recorded below as a note, not a finding: the pointer still resolves to round-4 §Q5, which is the recap that names all three bypasses. The diff itself is unchanged from round 2 (mtime + line-anchor evidence below), and the fresh pass over the whole change set — the three fixes, the five regression tests plus the helper pin, the fixture dedupe, the Cargo.toml feature, the three-way doc sync — surfaces nothing else. Read-only, static review: I have no shell in my allow-list, so the reported green suite (lib 2372 passed / 0 failed / 5 ignored + 16 integration, `#![deny(warnings)]` at `src/lib.rs:5`) is accepted as the build/warning proof, per this repo's review precedent; every claim below is re-derived from the source.

### The single round-2 finding — CLOSED, and verified against history (not the diff alone)

`PLAN.md:449-451` now reads:

> … and any hardlinked `.coding/**` file stay denied (**the hardlink rule since plan b4812291, 2027-01-15; the link rule since plan ee65fd4b, 2027-01-11**) — an in-root link that resolves inside the artifact tree is still a grant, so "no source diff" still holds.

**(a) It matches the history round 2 cited.** Re-verified independently of round 2's report:

- `git log -- src/agent/dispatch.rs` (read via `git_read`) returns exactly two commits, ever: `3217714` (initial commit) and `557f20e` "Research plans may write .coding artifacts; close the sub-plan review leak (plan ee65fd4b, options A + D)". So the research-route link clause shipped in **ee65fd4b** — the new attribution is right.
- `src/agent/dispatch.rs:88-113` (`research_write_verdict`'s canonical branch at `:90-101` and its link-free lexical branch at `:102-113`) is byte-identical in this change set. The only uncommitted `dispatch.rs` edits are the `mod tests` fixture import (`src/agent/dispatch.rs:1375`) and the deletion of the two private fixture fns (old `:2049-2114`) — the diff hunks (`@@ -1372,6 +1372,8 @@`, `@@ -2047,68 +2049,4 @@`) never touch the verdict function.
- The tree says so twice on its own: `src/tool/agent/sandbox.rs:37-39` (pre-existing, outside this diff) dates the research carve-out *and* its `symlink_metadata` walk to "2027-01-11, plan ee65fd4b", and `.coding/knowledge/decision/2027-01-11-research-plans-may-write-coding-artifacts-comple.md` records the decision to commit `557f20e`.
- The hardlink half is genuinely this plan: the guard lands in this diff (`sandbox.rs:286-304` + `link_count` `:571-588`), and `sandbox.rs:486-488`'s comment already calls the alias resolution "bypass 2's stated fix direction". Dated 2027-01-15 — the same day the bypass backlog item was created and re-queued ("interrupted by the user, returned to queue") and the day of the sibling user report that interrupts that dispatch (backlog `fcc3be87`, dated 2027-01-15).

**(b) It no longer contradicts `PLAN.md:447`.** Line `447`'s `(2027-01-11)` dates the **artifact-path dispatch carve-out** ("Its file tools are denied by NAME … but pass at dispatch on `.coding/**` ARTIFACT paths (2027-01-11)") — that is `557f20e`/ee65fd4b, the same plan and date the new parenthetical names for the link rule. The two parentheticals now agree: 2027-01-11 = the research carve-out and its link rule; 2027-01-15 = this plan's hardlink rule. No contradiction remains.

**(c) Nothing else in the diff has an unsupported attribution** — full sweep below.

**(d) Only `PLAN.md` moved since round 2, and the plan file still matches its description.**

- Every `sandbox.rs`, `file_write.rs` and `PLAN.md:132-141` anchor round 2 recorded still lands exactly where round 2 recorded it: `:510-519`/`:520` (the re-checked fallback arm), `:492-498` (the leaf check), `:444`/`:426-433` (gate, step list), `:383-398` (walk), `:1413-1448` (alias-layer pin, helper calls at `:1428`/`:1441`), `:1456`, `:1510`, `:1579`, `:1625`, `:1538-1575` (incl. the `SKIP` at `:1572-1574`), `:1626-1636`, `:650-654`, `:655`, `:666-676`, `:582`, `:571-581`, `:722-723`, `:677-679`, `:695-699`, `file_write.rs:670`, `PLAN.md:132-141`. A change in any of those files would have shifted at least one of ~25 anchors; none moved.
- mtimes corroborate it: the round-1 fix batch is `dispatch.rs` 1789476661 → `tests.rs` 1789476707 → `Cargo.toml` 1789476762 → `file_write.rs` 1789476784 → `sandbox.rs` 1789477793 → `docs/FEATURES.md` 1789477807; round 2's report necessarily postdates `sandbox.rs` (it verifies the LOW-2 fix there) and `FEATURES.md` (it verifies the "re-checked" wording at `:22`). The only working-tree file newer than that batch is `PLAN.md` (1789478355) — and it must postdate round 2's report, because it carries text round 2 quoted in its *old* form and no longer does. Nothing else in the tree was written after round 2.
- `.coding/plans/b4812291.md`: all four skeleton steps (`:23-26`) and both detailed-step boxes (`:29-35`, incl. **Docs sync** and **Full verification**) are `- [x]`, matching `4/4 steps complete`; the Bug section (`:38`) still describes the three bypasses exactly as the plan's description does, and its provenance line ("review rounds 2-4 of plan ee65fd4b (2027-01-11; BUG memory d7ebe9f9, backlog 1a5bffcf)") is accurate. Round-2 LOW 6 stays closed.
### (c) Every other provenance claim in the diff, checked against the code and the record

- **`Cargo.toml:119-121`** ("`Win32_Storage_FileSystem` carries `GetShortPathNameW` for the 8.3-alias sandbox regression test (plan b4812291)"): true. A repo-wide search finds `GetShortPathNameW` in exactly three places — the call at `src/tool/agent/sandbox.rs:1469`, its SKIP message at `:1476`, and this comment. The feature is added to the already `cfg(windows)`-gated dep, so macOS/Linux builds are untouched, and the test that uses it (`#[cfg(windows)] protected_win32_8dot3_alias_refused`) is this plan's.
- **`src/tool/agent/sandbox.rs:55-63`** ("added three bounded syscalls on 2027-01-15 (plan b4812291)"): true — the walk, the ancestor `canonicalize` and the per-check `metadata`/final `symlink_metadata` all land in this diff. The "run inside the tools' existing `spawn_blocking` closure" half is true too: `validate_for_write`'s only production callers are `file_write.rs::execute` (`:140`, inside `tokio::task::spawn_blocking` opened at `:132`) and `file_append.rs::execute` (`:110`, inside `:100`) — verified by reading both bodies and by the graph's caller list (the other incoming edges are tests).
- **`sandbox.rs:210-220`** (predicate doc: hardlink rule, fail-closed, scoped to `.coding/`, directory exemption, outside-`.coding` residual) and **`sandbox.rs:289-301`** (the inline `HARDLINK GUARD (2027-01-15, plan b4812291)` comment): both match the code at `:286-304` and `link_count`'s `is_file()` exclusion at `:584-586`.
- **`sandbox.rs:416` and `:439`** ("2027-01-15, plan b4812291" / "bypass 3, 2027-01-15"): true — the step-2 gate and the narrowed fallback are this diff's work.
- **Bypass numbers** ("bypass 2" at `:455`, `:483`; "bypass 3" at `:439`, `file_write.rs:675`): correct labels — bypass 2 is the 8.3 alias (whose stated fix direction is the canonical re-check on step 5's result, exactly what `:483` cites), bypass 3 is the step-5 lexical fallback. The numbering matches the plan's and the backlog item's (1) hardlink / (2) alias / (3) fallback.
- **"review round-4 §Q5 item 1/2"** (hardlink at `:1584`, alias at `:1453` and `:1635`): supported. Plan ee65fd4b's round-4 report does contain `## Q5 — full-diff fresh pass: what four rounds have not surfaced`, whose first paragraph is the Win32 8.3 alias (NEW, environment-gated, "recommend recording, not fixing here") and whose second is the hardlink observation ("broader than you stated … `.coding/x.toml` hardlinked to `.coding/safety.toml`"), with the ladder fallback as the third. The item numbers line up with the enumeration used in the plan/backlog.
- **"round-1 review LOW 4"** (`:1421-1422`, `:1636`) and **"round-1 review LOW 3"** (`:1543-1544`): both point at the right round-1 findings — LOW 4 was the untested 2b layer plus the wrong-layer comment; LOW 3 was the silently skippable dangling-leaf assertion.
- **"review LOW-1, round 3"** (`:1518`; also the verbatim-moved occurrences at `:673` and `:698-699`): accurate — plan ee65fd4b's round-3 report is where the dangling-link-as-leaf fail-open was named for the research route ("the fail-open survives for a dangling link as the leaf component … the guard added in round 2 is consulted only in the second branch"). The two fixture-doc occurrences are text moved verbatim out of `dispatch.rs`, not new claims.
- **`docs/FEATURES.md:22`**: carries no plan name and no date — it states the three rules and the re-checked fallback — so there is nothing there that can be misattributed.
- **`PLAN.md:132-141`** ("2027-01-15, plan b4812291; review rounds 2-4 §Q5 of plan ee65fd4b"): the three rules the sentence names (hardlink refusal, canonical-form re-run, fail-closed ladder with the re-checked fallback) are all implemented by this diff, so `b4812291` / 2027-01-15 is right.

**Note (not a finding) — "rounds 2-4 §Q5" is loose about *where* in round 2.** Round-4 §Q5 is the recap that names all three bypasses (and it explicitly cross-references "round-2's L1 residual"), and "rounds 2-4" is the correct discovery range — round 2's finding L1 is the step-5 fallback, round 3's H1 is the dangling-leaf spelling, round 4's §Q5 is the hardlink + alias pair. The loose spot is that round 2's report has no `§Q5` *section*: its bypass content is finding **L1**, and its question 5 is cache stability. So the literal reading "§Q5 of rounds 2-4" only resolves cleanly for round 4. I am not calling this a finding because (i) the same citation shape already exists in the tracked `backlog.jsonl` line for `1a5bffcf` — a line this diff does not touch (its `note` field is the only change) — so `PLAN.md` is consistent with the record it cites, and (ii) every substantive claim (which rules come from which plan, and that the three bypasses were surfaced in rounds 2-4 of ee65fd4b) is true. If the parent wants it airtight, the two-word tightening would be "…; review round 4 §Q5 of plan ee65fd4b, which recaps rounds 2-4" — no code or test depends on it.

### Fresh pass — the three fixes, re-derived from the source

**Bypass 1 (hardlink).** The guard at `:302-304` sits after the `.coding` first-component test, so it can only fire inside the protected tree; `link_count` (`:571-588`) uses following `metadata` (the count belongs to the inode the write would touch) and gates on `is_file()`. That gate is load-bearing twice — POSIX directory `nlink` is structural (it counts subdirectories), and the Win32 `CreateFileW`/`GetFileInformationByHandle` spelling only makes sense for files — and it is what keeps the guard legal when the same predicate is run against a deepest-existing *ancestor* (`:456-460`), which is a directory by construction. FFI path: access 0 (no data access) with all three share modes (the permissive direction), `INVALID_HANDLE_VALUE` checked before the information call, handle closed before the `then` closure. It is empirically pinned, not just compiled: `protected_hardlink_inside_coding_refused` (`:1578-1622`) really creates a hardlink (`std::fs::hard_link` needs no elevation on NTFS) and asserts both the `nlink > 1` refusal and that a `nlink == 1` artifact still validates `Ok` — a broken Win32 count would make `link_count` return `None` and fail both assertions. Scoping cannot over-refuse: everything the app itself writes under `.coding/` has `nlink == 1`, and `.coding/plans|reviews|knowledge/**` are name-refused one step later anyway.

**Bypass 2 (8.3 alias + its in-root-link twin).** `canonical_existing_ancestor` (`:642-664`) walks up from `path.parent()` — so a file is never treated as its own ancestor — and returns the canonicalized deepest existing ancestor; `:451-460` refuses when the protection predicate fires on it, *before* `create_dir_all`, which is what the test's "no subtree may be planted" assertion pins. The Windows test's `GetShortPathNameW` handling is correct (`written` is the length without the terminating NUL; `>= buf.len()` → visible SKIP; an alias equal to the long name → visible SKIP), and its `is_dir()` assertion proves the alias resolves to the protected directory before the write is attempted. The cross-platform twin (`.coding/link -> .coding/knowledge`) is refused by the step-2 link-free gate, with 2b and step 5 as backstops for spellings the gate cannot see (an 8.3 alias is not a symlink) — the corrected comment at `:1626-1636` now says exactly that.

**Bypass 3 (step-5 lexical fallback).** Step 5 (`:477-521`) is a three-arm `match`: `Ok(canonical)` → canonical-form protection re-check (`:484-486`) + `symlink_metadata` leaf refusal (`:492-498`); `Err(_) if created_parent` → link-freedom re-check (`:511-517`) and only then `Ok(validated)` (`:518`); `Err(e)` → propagate (`:520`). There is no other exit, so the lexical fallback survives only for a parent this call just created and that is still link-free. The round-1 LOW 2 race is closed by construction: an escaping `create_dir_all` requires the link to exist at re-check time, and the walk lstats each component as the leaf of its own prefix (so `symlink_metadata` sees the reparse point and cannot be skipped by the stop-at-first-missing rule).
### The five regression tests (+ the helper pin) — pre-fix failure hand-traced

- `protected_hardlink_inside_coding_refused` (`:1578-1622`): pre-fix, `validate(".coding/x.toml")` canonicalizes to the hardlink path itself (no link to follow), step 3 matches only the name `x.toml` and misses it, step 5 returns `Ok` — so both the `is_protected_write_target` assertion and the `is_err` assertion fail pre-fix. The "shared inode stays untouched" and the plain-`nlink == 1` artifact assertions are the no-over-refusal half.
- `validate_for_write_refuses_in_root_link_into_protected_tree` (`:1624-1654`): pre-fix, step 1 fails on the missing parent, the lexical branch is trusted, step 3 misses `link`, step 4's `create_dir_all` resolves the junction and plants the real `.coding/knowledge/new`, step 5 canonicalizes to the protected path and pre-fix *returns* it — so `is_err` and "no subtree planted" both fail pre-fix. Post-fix the step-2 gate refuses before any mkdir.
- `validate_for_write_fails_closed_on_out_of_root_link` (`:1509-1576`): pre-fix `.coding/link/x.md` comes back `Ok(lexical)` (the `is_err` assert fails), and `.coding/link/nested/x.md` additionally makes step 4 create `<outside>/nested`, so the empty-`outside` assertion fails pre-fix too. The dangling-leaf sub-assertion is non-vacuous: `validate` returns `Ok` for it via its parent+file-name fallback, so the refusal can only come from the leaf check at `:492-498`; its skip path is loud (`:1572-1574`, plus the fixture's own PowerShell diagnostics at `:724-730`), and the dangling *directory* (junction) spelling covers Windows where a file symlink needs Developer Mode.
- `file_write_through_out_of_root_link_is_denied` (`src/tool/agent/file_write.rs:669-697`): pre-fix the step-5 fallback handed the lexical path to `std::fs::write`, which followed the junction — the recorded pre-fix outcome was success plus `<outside>/x.md`; post-fix the tool returns an error and nothing lands outside. This is the implementation-plan route end to end (no dispatch-level path filter on that route).
- `#[cfg(windows)] protected_win32_8dot3_alias_refused` (`:1450-1507`): pre-fix the lexical ladder plants `.coding/knowledge/new` and returns the canonical protected path, so both assertions fail; the two SKIP arms are visible and honest (buffer too small; alias equals the long name), and they are the only conditional exits.
- `canonical_existing_ancestor_pins_the_alias_layer` (`:1413-1448`): cross-platform, calls the helper directly (`:1428`, `:1441`) so deleting 2b or the helper becomes a compile error instead of a green suite; `ends_with("knowledge")` really pins the DEPTH (the `deep` component does not exist), and the `.coding/analysis` ancestor must stay writable. This is the pin round-1 LOW 4 asked for in place of the volume-dependent alias spelling.

### Fixture dedupe, Cargo.toml, docs

- **Fixture dedupe.** The moved bodies are verbatim: junction-first with the `New-Item` rationale, the PowerShell status/stdout/stderr diagnostics, the `symlink_dir` fallback, and the `#[cfg(not(any(unix, windows)))]` arm returning `false`. The `tests.rs` inline block's semantics and its `SKIP:` eprintln survive, and both helpers are still used in three modules (`dispatch.rs` uses `plant_file_link` and `plant_dir_link`; `agent/tests.rs` and the sandbox/file-write tests use `plant_dir_link`), so no unused-import warning can break `#![deny(warnings)]`.
- **Cargo.toml.** One feature string added to the already `cfg(windows)`-gated `windows-sys` dependency (plus its explanatory comment): macOS/Linux dependency graphs are unchanged.
- **Docs.** Module doc (`:55-63` syscall accounting), predicate doc (`:210-220`), the numbered step list (`:409-433`), `PLAN.md` (`:132-141`, `:445-455`) and `docs/FEATURES.md` (`:22`) all state the same three rules and the re-checked fallback, in each file's existing register, with no over-claim left ("fails closed" now carries its one qualifier).

### Constitution and policy checks

- **Warning-free, no escape hatch.** `#![deny(warnings)]` at `src/lib.rs:5`; the diff adds no `#[allow(...)]` (a `src/**/*.rs` search surfaces only identifier/comment uses of "allow", and round 2's exhaustive 124-file check found zero attributes), so the green suite is the build/warning proof.
- **Documented public/test-visible items.** `link_count`, both `platform_link_count` arms, `canonical_existing_ancestor` and `link_fixture` + both helpers all carry doc comments — round-1 LOW 1 stays closed (the fixture prose now sits above the module at `:666-676`).
- **Multi-platform.** Everything Windows-specific is `#[cfg(windows)]` (the FFI in `platform_link_count`, the 8.3 test) or a cfg'd arm of a test fixture; the unix half uses the stable `MetadataExt::nlink()`, and `not(any(unix, windows))` returns `None` ("the guard stays quiet"). No Windows path or shell syntax in library code; the only `powershell` spawn is the pre-existing, test-only junction spelling.
- **File-tools-first.** The change set contains no shell-based file mutation; the bookkeeping edits were made with the sanctioned tools / the approval-gated shell state (round 2's LOW 6 treatment of the plan-file ticks).

### Accepted residuals (no action; all previously recorded)

- 2b skips its check when the ancestor cannot be canonicalized (`canonical_existing_ancestor` → `None`, documented at `:650-654`). Step 5 still refuses the *write* on the canonical form, and an ancestor that cannot be canonicalized is also one `create_dir_all` cannot traverse — so this is not an escape, and the "skip rather than refuse" wording is the honest one.
- In the concurrent-writer race, the `create_dir_all` side effect can still create directories outside the root before the write is refused; the refusal is what the fix guarantees, and reaching it needs the concurrent mutation (the `shell` residual round 1 recorded).
- A hardlink *outside* `.coding/` pointing at a protected file stays undetected (it needs an inode walk) — recorded in the predicate doc, the inline comment and the plan, and sanctioned by the backlog item as the fail-closed variant.
- `shell` remains unrouted through the sandbox (standing residual).
- Side-car drift in the diff, expected for `.coding/` (it travels with git by policy): `backlog.jsonl` carries a new soft-deleted item (`fcc3be87`), a cleared `deferred` flag on `488248ce`, and the re-queued note on `1a5bffcf`; plus the untracked plan file and the two prior reports. Worth one line in the commit message (the ee65fd4b precedent), not a finding.

**Bottom line:** PASS. The round-2 attribution fix is correct and complete (verified against `git log`/`git diff`, not the report alone), no other attribution in the uncommitted diff is unsupported, the rest of the change set is provably untouched since round 2, and the fresh pass over the three fixes, the five regression tests, the helper pin, the fixture dedupe, the Cargo.toml feature and the three-way doc sync surfaces nothing new. This report is the one the commit and `finish` should point at.
