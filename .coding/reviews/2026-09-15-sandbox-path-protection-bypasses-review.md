## Verdict: FINDINGS (0 high, 6 low)

Summary: the three bypasses are genuinely closed. I traced each fix through the ladder control flow rather than trusting the tests — the hardlink guard (scoped to `.coding/**`, directories exempt so the deepest-existing-ancestor call stays legal), the lexical link-free gate, the canonical-ancestor check before `create_dir_all`, the canonical re-check, the symlink-leaf refusal, and the narrowed step-5 fallback — and hand-traced each new test's pre-fix failure path. The security core is correct, the fixture dedupe is behaviour-preserving, and the platform split is clean. All six findings are documentation / test-hygiene level; LOW 2 is the only one that touches a (documented-as-closed) escape, and it needs a concurrent tree mutation.

### LOW 1 — `mod link_fixture`'s doc comment was absorbed into `link_count`'s; the module now has no doc comment

`src/tool/agent/sandbox.rs:556-576` vs `:658-662`.

The block that opens "Link fixtures shared by the escaping-link tests in this crate. / ONE canonical spelling of …" runs straight into the hardlink-count paragraph and is attached to `fn link_count` (one contiguous `///` block ending immediately above `fn link_count(path: &Path) -> Option<u64>` at :576). `#[cfg(test)] pub(crate) mod link_fixture` at :658 therefore has NO doc comment, while `link_count`'s rustdoc tells the reader about link fixtures and the round-3 LOW-1 skip rationale.

Why it matters: the dedupe's whole justification (one canonical fixture + the "a caller must SAY when it could not plant the link" rule) is exactly what a future editor needs to read, and it is now filed under the wrong symbol; `cargo doc` renders a wrong description for `link_count`. Fix: split the two paragraphs, putting the fixture prose above `#[cfg(test)] pub(crate) mod link_fixture`.

### LOW 2 — the created-parent fallback's safety argument is not race-proof, and both the comment and the docs claim it fails closed

`src/tool/agent/sandbox.rs:499-504` (arm + comment), `:644-646` (`canonical_existing_ancestor` doc: "step 5 … fails closed when it cannot"); claimed closure repeated at `PLAN.md:132-135` and `docs/FEATURES.md:22`.

The comment says the lexical fallback is safe because "every existing component passed the link-free gate above, so the parent is a fresh real directory inside the root". A link planted *between* step 2's walk and step 4's `create_dir_all` invalidates that. Trace for `.coding/link/new/x.md` with `.coding/link` absent during the walk and a junction to `<outside>` present at step 4:

1. step 2: creation form, walk sees no link (component absent), step 2b canonicalizes `.coding` → harmless; step 3 sees the lexical path → not protected;
2. step 4: `parent.exists()` follows the junction → `<outside>/new` missing → `create_dir_all` **creates the directory outside the root**;
3. step 5: `validate` canonicalizes the parent to `<outside>/new` → `PathOutsideRoot` → `Err(_) if created_parent => Ok(validated)` returns the lexical path, and the tool writes `<outside>/new/x.md`.

So the arm is fail-open precisely in the case the comment declares impossible; a re-run of `lexical_path_is_link_free(&validated)` (or a comparison of `validated.parent()`'s canonical form against its lexical form) in that arm would restore fail-closed behaviour at one stat, and would also make `canonical_existing_ancestor`'s "step 5 fails closed" sentence true. Why it matters: pre-fix this required nothing but a pre-existing link; post-fix it requires a concurrent writer, which the app itself can supply (multi-agent sessions; the agent's own approval-gated `shell` can plant a junction on a timer — and shell is the already-documented unrestricted residual). Fix at minimum the two over-broad claims; ideally add the re-check.

### LOW 3 — the dangling-link-leaf assertion in the ladder test is silently skipped when the fixture cannot plant it

`src/tool/agent/sandbox.rs:1484-1494` (contrast `src/agent/dispatch.rs:2008-2011`, which prints `SKIP: could not create a file link for {leaf}`).

`if link_fixture::plant_file_link(&outside.path().join("missing.md"), &dir.path().join(".coding/dangling.md")) { … }` has no `else` diagnostic. `symlink_file` needs Developer Mode on Windows, and the repo's own fixtures prefer junctions because `symlink_dir` is unavailable on this machine — so on Windows this sub-assertion plausibly never runs, and the test still reports green.

Why it matters: this is the only ladder-level test of the new `symlink_metadata` leaf check (the branch that closes the dangling-leaf shape for `file_write`/`file_append`), and a silent skip is the exact failure mode the fixture module's own doc and round-3 LOW-1 flag ("a fixture that silently gives up … is how the dangling-leaf hole survived three review rounds"). The sibling dispatch test shows the one-line fix; note the 8.3 test's skip path IS honest (two distinct visible markers). A dangling *directory* link (plantable privilege-free via the junction fixture) would additionally pin the same branch on Windows.

### LOW 4 — the canonical-ancestor layer (step 2b) can ship untested, and the in-root-link test credits the wrong layer

`src/tool/agent/sandbox.rs:1544-1550` (comment), `:454-458` (the 2b check), `:1400-1453` (the only test that reaches it).

For `.coding/link/new/x.md` (link → `.coding/knowledge`) the refusal is produced by the link-free gate at `:442-448` — step 2's gate runs before step 2b, and `symlink_metadata` reports Windows junctions as symlinks (this is provable from the suite itself: for the *out-of-root* junction, 2b cannot refuse — `canonical_existing_ancestor` yields a path that fails `strip_prefix(root)`, so `is_protected_write_target` is false and step 5's fallback would return `Ok`, i.e. the test would still fail — yet it passes, so the walk must see junctions). The comment's "the canonical-form checks must refuse it first" therefore describes a layer that never runs for that spelling.

Consequence: `canonical_existing_ancestor` + the pre-mkdir refusal (`:454-458`) is exercised only by `protected_win32_8dot3_alias_refused`, which SKIPs (honestly, with visible markers) on any volume where `GetShortPathNameW` returns the long name. On such a volume a regression that deleted the 2b block would keep the suite green (the 8.3 test's `result.is_err()` would then also be satisfied by step 5 for the file write, though its "no subtree planted" assertion would catch the pre-mkdir loss — that assertion is the real guard, so this is a coverage note, not a hole). Fix: correct the comment to name the layer that fires, and add a cross-platform pin on the helper (e.g. assert `canonical_existing_ancestor` of `.coding/knowledge/deep/x.md` resolves to the real `.coding/knowledge` and that `is_protected_write_target` refuses it) so the intent survives where the alias cannot be built.

### LOW 5 — PLAN.md's research-denial summary overstates "any path with a link component"

`PLAN.md:445-447` ("source, docs, protected side-car files, `..` escapes, any path with a link component and any hardlinked `.coding/**` file stay denied").

Against `src/agent/dispatch.rs:90-113`, an in-root link component that resolves *inside* `.coding/` is granted: with `.coding/link -> .coding/analysis`, a research `file_write` to `.coding/link/report.md` takes the canonical branch (`validate` resolves the parent through the link), `lexical_path_is_link_free` is then trivially true for the canonical path, and `judge_research_write` returns `Artifact`. That is the correct verdict, but it contradicts the sentence as written. Fix: "any link component that leaves the root or cannot be resolved" — which is what the code does.

### LOW 6 — the plan file's checkboxes disagree with the workflow state

`.coding/plans/b4812291.md:26` (step 4 "Verify" is `- [ ]`) and `:34-35` ("Docs sync", "Full verification" are both `- [ ]`) while the plan reports `4/4 steps complete` and the work is visibly done (sandbox.rs module doc, PLAN.md, docs/FEATURES.md all carry the b4812291 text). Why it matters: that file is the crash-resumption document — a resumed session reads the unticked boxes as outstanding work and re-runs the docs sync / full-suite verification. Tick them (`complete_step detailed_step_index`) or say why they are intentionally left open.

## Checked and correct

- **Bypass 1 (hardlink).** `link_count` (`:576-582`) uses `std::fs::metadata` (follows links — the count belongs to the file the write would touch) and gates on `is_file()`, which is load-bearing for the `:454-458` ancestor call (a POSIX directory's nlink is 2 + subdirs, so without it every legitimate `.coding/analysis/**` creation on macOS would be refused). Scoping the guard after the `.coding` first-component test is right: the ancestor is a directory (exempt), and the canonical re-check at `:482-484` covers paths whose *resolution* enters `.coding/`. Fail-closed across the whole `.coding/**` tree rather than only the name-protected set is the backlog-sanctioned design and cannot over-refuse anything legitimate (nlink is 1 for every `.coding/**` file the app writes; `.coding/plans|reviews|knowledge/**` are name-refused one step later anyway).
- **Bypass 2 (8.3 alias).** The trace holds end to end: `validate` fails on the missing parent → creation form passes the walk (the alias is not a link) → step 2b canonicalizes `.coding/KNOWLE~1` to the real `knowledge`, `is_protected_write_target` fires on `knowledge` (index 1) → refusal *before* `create_dir_all`, which the test's "no `knowledge/new`" assertion pins. Step 5's canonical re-check is a working backstop for the same spelling if 2b is ever skipped, and the sibling shapes are closed too: an alias of an *existing* protected file canonicalizes to the long name (`:482-484`), `.CODING/KNOWLEDGE` is name-folded at step 3, and trailing-dot/space variants are trimmed per component. `canonical_existing_ancestor` walking up from `path.parent()` (so the leaf itself is never treated as an ancestor) is correct.
- **Bypass 3 (fallback).** The five-layer result is right for every non-racy spelling: escaping dir link refused by the gate; dangling dir link refused by the gate (an lstat of a dangling component succeeds and reports a link, so the "stop at the first missing component" rule cannot skip it); dangling *file* leaf refused by the `symlink_metadata` check at `:490-496` (necessary — `validate` returns `Ok` for it via its parent+file-name fallback); a nonexistent parent path that merely cannot canonicalize now propagates the error instead of returning a lexical path.
- **Over-refusal.** Nothing legitimate is refused: new files directly inside an existing dir take step 1 (links resolved and judged in canonical form, so in-root symlinked dirs still work for direct children), `file_append` create-parents follow the same ladder, `.coding/analysis/**` with nlink 1 validates `Ok` (pinned in the hardlink test), and the research carve-out's artifact grants are unchanged. The genuinely new refusal is "a path needing `mkdir` under a link component" — fail-closed by design and documented in the step list.
- **Multi-platform.** No Windows-only API leaks: the FFI lives in `#[cfg(windows)] platform_link_count` (`:597-627`) and in the `#[cfg(windows)]` test; unix uses the stable `MetadataExt::nlink()`; the `not(any(unix, windows))` arm returns `None` (documented as "the guard stays quiet"); `Cargo.toml` adds a *feature* to the already-`cfg(windows)`-gated `windows-sys` dep, so macOS builds are untouched. The FFI is justified (`MetadataExt::number_of_links()` is still feature-gated) and follows the repo's existing raw-`windows-sys` idiom (`src/config/keys.rs`); creating a handle with access 0 and all three share modes is the permissive direction, and `INVALID_HANDLE_VALUE` is checked before `GetFileInformationByHandle`.
- **Fixture dedupe.** Behaviour-preserving: the moved bodies are verbatim (junction first, `Ok(out)` failure and `Err` both print and fall through to `symlink_dir`, non-unix/windows arm returns `false`), the tests.rs inline block's semantics and its `SKIP:` eprintln survive, and coverage is unchanged (dispatch still uses *both* helpers — `plant_file_link` at `:2008`, so the combined import is used and no unused-import warning can break `#![deny(warnings)]`).
- **Constitution.** No `#[allow(...)]` added; every new public/test-visible item carries a doc comment (modulo LOW 1); no shell file mutation outside test fixtures (the `New-Item -ItemType Junction` call is test-only, the pre-existing spelling); docs synced in three places; each of the five new tests has a real pre-fix failure mode (I hand-traced them; the hardlink test's `is_protected_write_target` and `validate_for_write` assertions cannot pass without the guard, the out-of-root test's `outside` must stay empty, and the file_write test's pre-fix success was recorded).

## Accepted residuals (no action)

- A hardlink **outside** `.coding/` pointing at a protected `.coding/**` file is not detected — recorded in the predicate doc, the inline comment and the plan, and explicitly sanctioned by the backlog item as the fail-closed variant.
- `shell` remains unrouted through the sandbox (standing residual), which is also the only way the LOW 2 race is reachable in-process.
- `file_edit` / `convert_line_endings` / the src-tauri IPC mirror (`src-tauri/src/ipc/files.rs:126-135`) inherit the hardlink guard via `refuse_if_protected` but do not run the ladder; their link handling is unchanged by this plan (they read/judge existing files, and `validate` resolves existing links), so no regression — the dangling-leaf spelling is reachable there only for an already-open editor buffer, and that is pre-existing and out of this plan's scope.
- On filesystems without hardlink support the count is not reported/meaningful, but no link can be created there either.
