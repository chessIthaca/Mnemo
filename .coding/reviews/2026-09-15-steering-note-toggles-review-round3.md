## Verdict: FINDINGS (0 high, 1 low)

Scope: round 3 (final) review of ALL uncommitted changes on the working branch (`git diff HEAD`: 21 paths / +1411 −180) for plan `3ae367bd` "Per-note toggles for steering notes in the Chat config", after the round-3 edits (three doc/doc-comment spots + one new test). Severity: **high** = correctness/security/contract breakage; **low** = narrow gaps, partial coverage, doc inaccuracies. Rounds 1 (`FINDINGS (0 high, 4 low)`) and 2 (`FINDINGS (0 high, 1 low)`) were read first.

Round-3 mandate result: the two user-facing spots (`docs/CONFIGURATION.md:9`, `docs/FEATURES.md:42`) now state exactly what the code does, are mutually consistent, and the new test's expectation hand-derives through `filterLine`'s whole-line pattern pass as the real behavior (non-vacuous). One residual overclaim remains in the third spot — the module doc's sentence about the **backend's** detection — hence 0 high / 1 low. Nothing else regressed; the round-1 fixes are intact.

### LOW 1 — `frontend/src/lib/delegationNotes.ts:39-41`: the backend's detection "is first-line scoped" is not true for 3 of the 10 kinds

- Text (`:37-41`, module doc MATCHING paragraph): "… can match a note copied verbatim at column 0. The backend's own detection is first-line scoped for the same reason (`steering_stats.rs` `detect()`, and its test pinning that a grep result quoting the shell TIP does not fire)."
- Ground truth — `src/agent/steering_stats.rs::detect()` (`:228-271`): `first_line().contains(…)` for SearchNudge `:233`, ShellTip `:236`, LiteralTip `:251`, KnownMemoryHit `:255`, ShellRedirect `:262`, EditStaleRead `:268`; `output.starts_with(…)` for ReadNudge `:235` — but **full-output `contains`** for GraphMiss `:246`, RecallRider `:248`, ConsolidationDue `:260`. The source states the qualification itself one screen up (`:237-241`): "… a full-output scan inside their gate is safe (but the first-line scoping the search tools get is **not available** — the miss hint is a JSON field value, the rider a trailing appendix)". The cited tests are real and pin exactly the quoted case (`search_nudge_is_first_line_scoped` `:1169`, `shell_tip_is_first_line_scoped` `:1199`, the latter with the "src/docs.md: the TIP: …" echo asserted to fire 0 times).
- Why it is still a finding: this is the same claim class round 1 LOW 4 / round 2 LOW 1 removed from the user-facing docs — an unqualified statement stronger than the implementation. Impact is narrow (an internal module comment; the operative point — no marker fires from a payload echo of quoted file content — holds, because the three full-output kinds are gated to tools that cannot carry foreign content), and it may predate this round, but it sits inside the paragraph this round rewrote and is the only remaining "first-line scoped" claim in the tree (`search "first-line scoped"` → 1 hit, this sentence).
- Concrete fix (one clause; no behavior or test change): qualify the mechanism, e.g. *"The backend's own detection is first-line scoped wherever a result could echo foreign content (`steering_stats.rs` `detect()` — the graph-miss / recall-rider / consolidation notes scan the whole output, safe inside a tool gate that cannot carry another kind), and its tests pin that a grep result quoting the shell TIP does not fire."*

---

## Round-3 items verified correct (0 findings)

**1. The three spots now state what the code does, and are mutually consistent.**

Ground truth (`frontend/src/lib/delegationNotes.ts`): `matchesDef` (`:291-294`) tests `linePatterns` against the **RAW** line and `linePrefixes` against the note-unwrapped text via `startsWith` (start-anchored); `filterLine` (`:321-340`) runs a whole-line pass first (`matchesDef("", line, def)` `:323` → patterns only, since `"".startsWith(prefix)` is false), then the single-component pass, then splits only at `"; "` offsets followed by a recognized note start (`componentStarts` `:302-310` via `isComponentStart` `:297-299`). Registry patterns are all `^`-anchored (`search_nudge` `:143-144`, `graph_miss` `:172`, `consolidation_due` `:233`) — **except** `edit_stale_read` `:259`, `/— the content has likely drifted from your last read\.\s*Re-read the file/`, deliberately unanchored and therefore firing in the whole-line pass wherever it sits, `path:line:` prefix included. That is exactly the exception all three spots now name.
- `docs/CONFIGURATION.md:9`: "Matching is anchored to the start of a line or to the note's own marker shape … with one deliberate exception: the stale-read matcher keys on the note's own drift sentence … location prefix included. Otherwise only a raw stream (`shell` output, the generic output fallback) can match a note copied verbatim at column 0." ✓ true, no residual absolute claim, and the named raw-stream sites are the real ones (`Message.tsx` shell stdout/stderr `:726-735`/`:815-830`, generic `<pre>` `:836`).
- `docs/FEATURES.md:42`: same qualification, same exception, consistently worded ("the one exception: the stale-read matcher keys on the note's own drift sentence, hidden wherever it sits"; "otherwise only an unfiltered raw stream reproducing a note verbatim at column 0 can match"). ✓
- `frontend/src/lib/delegationNotes.ts:32-41`: "Each pattern is anchored to a line start or scoped to its own marker shape … the ONE exception being `edit_stale_read`, whose unanchored drift-sentence match fires wherever the sentence sits, prefix included" + the raw-stream caveat. ✓ (the only issue is the following backend sentence — LOW 1).
- The registry comment `:257-258` ("a file's content quoting 'Re-read the file' is never hidden") stays true under the documented exception: the pattern requires the drift clause **and** the marker in order, so a line quoting only the marker survives — pinned by the test at `:266-277` (`src/steering_stats.rs: const EDIT_STALE_READ_MARK: &str = "Re-read the file";` + hidden `shell_tip`+`edit_stale_read` → byte-identical), while the full-sentence line is dropped. Both sides of the documented boundary are pinned.

**2. Residual-claim sweep.** `search "first-line scoped"` → 1 hit total (LOW 1's sentence). `search "merely quote|never hidden|never hide|is not hidden|never be hidden|first-line"` in `docs/**/*.md` → only `docs/FEATURES.md:25`, an unrelated shell-output-filtering claim ("errors are never hidden"); in `frontend/src/**/*.{ts,tsx}` → `ModelPickerDropdown.tsx:38`, `VisionSection.tsx:140` (unrelated) plus `delegationNotes.ts:124` "the delegated answer itself always stays visible" ✓ (def:/callers:/full-360° lines match no matcher → they stay) and `:258` (above) ✓. No old absolute claim survives.

**3. New test — expectation is the real behavior and non-vacuous.** `frontend/src/lib/delegationNotes.test.ts:232-241`, hidden `["edit_stale_read"]`: `stripSteeringNotes(hit, …)` → one line → `filterLine` `:322` non-empty → `:323` `matchesDef("", rawHit, def)` → `def.linePatterns.some(rx => rx.test(rawHit))` → the unanchored drift-sentence pattern matches inside `"src/agent/dispatch.rs: 1568: old_string not found in file — the content has likely drifted from your last read. Re-read the file (read_files, this exact path) and retry …"` → returns `null` → the line is dropped → `""`. `.trim()` on `""` === `""`, so the assertion can only pass if the whole line is gone (a no-op implementation would return the long hit line). The fixture text matches the real emission shape (`file_edit.rs` `with_fresh_read_nudge`, quoted in the registry comment `:253-256` and `docs/FEATURES.md:42`). The test's title says "(documented exception)" and the docs name the same exception — no contradiction.

**4. Round-1 fixes intact.** (a) alternation prefix present (`linePrefixes: ["AUTO-DELEGATED", "from the alternation pattern"]` `:141`) with its merged-component pin (`test :191-196`) and the no-over-match pin for every other kind (`:168-182`); (b) collapsed error summary filtered (`Message.tsx:456-465`, render `:656-668`); (c) legacy seeding (`App.tsx:338-352` passes both fields; `hiddenKeysFromConfig` ignores the legacy boolean whenever the table is present, absent table + `true` → `[]`) — agrees with the Rust resolver `effective_steering_note_visible` (`src/config/general.rs:~581`, `unwrap_or(self.show_delegation_notes)`); (d) docs reworded (above).

**5. Nothing regressed in the whole diff.** All four render paths route through the per-kind filter: chip `Message.tsx:520-524` (`filterSteeringNote`, remainder-preserving), error summary `:462-465`, shell stdout/stderr `:726-735` + `:815-830`, generic `<pre>` `:836`. Merged-note splitter re-join is byte-identical (`filterLine:330-339`: part slices exclude the `"; "` separators, re-join with `"; "`, and the original `line` is returned when nothing was dropped — `:338`). The alternation prefix cannot mis-split: it only *adds* a component start, and `stripSteeringNotes(text, all-but-search_nudge)` stays byte-identical (pinned `:181-182`).

**6. Contract chain agrees.** `src/config/general.rs` `UiConfig::STEERING_NOTE_KEYS`/`SteeringNotesCfg` (11 keys, per-field doc comments, `is_empty`) ↔ `src/config/settings_dto.rs:175-217` (11 optional fields, per-field docs) + `:602-625` (`Some`-only patch application) ↔ `src-tauri/src/ipc/settings.rs` `GetSettingsSteeringNotes` (`:632-655`, resolved at `:868`) ↔ `src-tauri/src/ipc/contract_fixtures.rs:255-267` ↔ `frontend/src/lib/ipc-fixtures/dto-get-settings.json:60-74` ↔ `frontend/src/lib/ipc-contract.test.ts:436-452` (sorted 11-key set + `auto_delegated === false`, `literal_tip === true`) ↔ `frontend/src/lib/tauri.ts:468-471` / `:540` ↔ `ChatSection` draft/save. Key set, values and order agree end-to-end.

**7. Display-only contract holds.** `effective_steering_note_visible` consumers = the GetSettings DTO path + tests only; no tool/agent/prompt consumer of the hidden set. Frontend `hiddenSteeringNotes` consumers = the two `Message.tsx` sites (+ ChatSection draft + App hydration) only. The tool-result text the model reads is untouched — the DECISION 950fdba5 contract.

**8. Constitution.** Documentation sync ✓ (both docs updated; the superseded SPEC record got `status = "superseded"`). Multi-platform neutrality ✓ (TS/serde/TOML only — no platform APIs, paths or shell syntax). File-tools-first ✓ (no shell-based mutation in the diff; the `.coding/` churn is app bookkeeping: backlog status flip, knowledge frontmatter, plan `9ff59133` regression-test field). Warning-free Rust ✓ (`search "#\\[allow\\("` → 0 matches in `src/**/*.rs`; `src-tauri/src` has only two pre-existing doc-comment mentions; no attributes added in the diff; no whole-file rewrites, so line endings are preserved). Doc comments on new public items ✓ (`SteeringNotesCfg` + every field, `STEERING_NOTE_KEYS`, `effective_steering_note_visible`, `is_empty`, `SteeringNotesDto` + every field, `GetSettingsSteeringNotes`).

## Observations (not findings — no action required)

- `docs/FEATURES.md:42` and `delegationNotes.ts:34-35` say "search/read results prefix every hit with `path:line:`". That is the exact shape for `search` (`src/tool/agent/search.rs` hit lines); `search_read` renders matched files through the shared `read_one` formatter (`search_read.rs:370-391`), i.e. a file header plus line-numbered content. The substantive consequence the sentence draws — a location prefix keeps quoted note text at bay for both tools — holds either way; worth noting only because this wording predates the round and was carried forward.
- Test status is as reported by the main agent (vitest 1175 / 83 files, `npm run build` clean, `cargo test` 2366 + 16 passed); I am read-only and could not re-run the suites. All frontend expectations relevant to this round were hand-re-derived above.

## Bottom line

The round-3 documentation work landed correctly: both user-facing docs match `filterLine`/`matchesDef`/`componentStarts` and the registry (exception included), they agree with each other, the new test pins the documented exception non-vacuously, no other spot in the tree carries the old absolute claim, and nothing else in the diff regressed. One narrow doc-comment overclaim about the backend's detection (LOW 1) is the only remaining item; a one-clause qualification closes it. Fix LOW 1, re-run the suites (docs-only edit — no test impact expected), commit, and finish with this report.
