## Verdict: FINDINGS (0 high, 1 low)

Round-4 (final) verification of plan `3ae367bd` "Per-note toggles for steering notes in the Chat config" on `wt/mnemo`, against the FULL uncommitted diff (`git_read(op="diff")`: **21 paths / +1414 −180**), after the single post-round-3 edit in `frontend/src/lib/delegationNotes.ts`. Rounds 1 (`0 high, 4 low`), 2 (`0 high, 1 low`) and 3 (`0 high, 1 low`) were read first; their checklist is the baseline. Severity convention as before: **high** = correctness/security/contract breakage; **low** = narrow gaps, partial coverage, doc inaccuracies. I am read-only — the suites were not re-run; every claim below is hand-derived from source.

**Round-3 LOW 1 is substantially fixed.** The revised sentence now states the scope claim plus the exact exception set the implementation has: `first_line().contains(…)` for SearchNudge/ShellTip/LiteralTip/KnownMemoryHit/ShellRedirect/EditStaleRead (`steering_stats.rs:233`, `:236`, `:251`, `:255`, `:262`, `:268`), `output.starts_with(…)` for ReadNudge (`:235`), and full-output `contains(…)` for **GraphMiss `:246`, RecallRider `:248`, ConsolidationDue `:260`** — the three kinds the clause names. It is no longer the unqualified "is first-line scoped" of round 3. The cited tests exist and pin exactly the quoted case (`search_nudge_is_first_line_scoped` `:1169`, `shell_tip_is_first_line_scoped` `:1199`, the latter asserting 0 fires for `"src/docs.md: the TIP: for file-content search string quoted from a file"`).

**One residual over-claim remains** in the clause's new parenthetical, which attributes a structural safety mechanism to the consolidation arm that the code does not have (LOW 1 below). It is a doc-only wording item.

**Nothing else moved since round 3.** Same 21 paths (4 `.coding/` bookkeeping + the 2 docs + the 15 frontend/Rust files round 3 enumerated); the stat delta is exactly the clause's growth (old sentence lines `:39-41` → new `:39-44`, +3 lines), and every line anchor round 3 recorded in the *other* files still lands on the identical line — `contract_fixtures.rs:255-267`, `settings.rs:632-655` + `resolved()` `:661`, `ipc-contract.test.ts:438-452`, `tauri.ts:468-471`, `dto-get-settings.json:60-72`, `Message.tsx:449/:462-465/:520-524/:715/:729-733/:832`, `delegationNotes.test.ts:232-241`, `general.rs:578-594`, `settings_dto.rs:602-637`, `ChatSection.tsx:42-43/:91-93`, `App.tsx:341-352`, `docs/CONFIGURATION.md:9`, `docs/FEATURES.md:42`. A shifted file would have moved every anchor inside it; none did. Inside `delegationNotes.ts` everything *after* the clause is shifted by exactly +3 (`matchesDef` `:291→:294`, `componentStarts` `:302→:305`, `filterLine` `:321→:324`) and everything *before* it is unmoved (`auto_delegated` `defaultVisible: false` `:128`, `search_nudge` prefixes/patterns `:144-147`, `graph_miss` `:175`, `edit_stale_read` pattern `:262`) — consistent with one insertion and no other edit.

## LOW 1 — `frontend/src/lib/delegationNotes.ts:39-44`: the new parenthetical gives the consolidation note a "tool gate" it does not have

**Claim under test** (module doc, MATCHING paragraph, final sentence): *"The backend's own detection is first-line scoped wherever a result could echo foreign content (`steering_stats.rs` `detect()` — the graph-miss, recall-rider and consolidation notes scan the whole output instead, safe inside a tool gate that cannot carry another kind), and its tests pin that a grep result quoting the shell TIP does not fire."*

**Why it is wrong for 1 of the 3 kinds it names.** The participial clause covers the whole three-item list, but the implementation gives the gate argument for exactly two of them:

- **GraphMiss `:242-247`** and **RecallRider `:248`** — tool-gated (`matches!(tool_name, "graph_search" | "graph_context" | "graph_impact" | "graph_path")`; `tool_name == "create_plan"`), and the comment directly above them (`:237-241`) is the one that argues exclusivity: "The graph tools and create_plan cannot carry a kind other than their own, so a full-output scan inside their gate is safe". ✓ the clause matches these two.
- **ConsolidationDue `:260`** — **no tool gate at all**: `MarkerKind::ConsolidationDue => output.contains(self.marker())` never consults `tool_name`. Its own comment (`:257-259`) states a *different* rationale: "Dispatch-owned (no tool emits it) and appended AFTER the result, so full-output detection applies; the phrasing is specific to the dispatcher."
- The carrier really is arbitrary: `with_consolidation_note` (`src/agent/dispatch.rs:781-796`) appends the note to **any** successful `ToolResult` (`r.output.push('\n'); r.output.push_str(&note)` — no tool restriction, invoked at both dispatch-funnel returns), so the result it rides can be a `search` result that already carries the literal TIP / known-memory-hit note. The gate therefore **can** carry another kind, and the consolidation note rides an echo-prone result — the exact situation the sentence's headline ("first-line scoped wherever a result could echo foreign content") says cannot happen.

**Why it is still a finding.** Same claim class rounds 1-3 removed from this paragraph: a structural guarantee stated more strongly than the implementation gives it, inside the very clause this round rewrote. The implementation's stated safety for this kind is *phrasing specificity*, not gate exclusivity (it is advisory, fired-only telemetry, so the practical impact is nil) — but "safe inside a tool gate that cannot carry another kind" is not true as written, and a reader who checks it against `detect()` finds a `tool_name`-blind arm.

**Concrete fix (one clause, docs-only — no behavior or test change).** Split the rationale, e.g.:

> *"The backend's own detection is first-line scoped wherever a result could echo foreign content (`steering_stats.rs` `detect()` — the graph-miss, recall-rider and consolidation notes scan the whole output instead: the first two sit inside a tool gate that cannot carry another kind, the third is appended by the dispatcher to any successful result and kept unambiguous by its dispatcher-specific phrasing), and its tests pin that a grep result quoting the shell TIP does not fire."*

Everything else in the sentence should stay as it is (see the verification below).

## Round-3 items re-verified (spot checks, all correct)

**1. The three spots agree with `filterLine` / `matchesDef` / `componentStarts` and with each other.** `matchesDef` (`:294-297`) tests `linePatterns` against the **RAW** line and `linePrefixes` against the note-unwrapped text via `startsWith`; `filterLine` (`:324-343`) runs the whole-line pass first (`defs.some(def => matchesDef("", line, def))` `:326` — patterns only, since `"".startsWith(prefix)` is false), then the single-component pass (`:330-332`), then splits only at `"; "` offsets followed by a recognized note start (`componentStarts` `:305-313` via `isComponentStart` `:300-302`). Registry patterns are `^`-anchored (`search_nudge` `:146-147`, `graph_miss` `:175`, `consolidation_due` `:236`) **except** `edit_stale_read` `:262` (`/— the content has likely drifted from your last read\.\s*Re-read the file/`, deliberately unanchored) — exactly the exception all three spots name, and the one LOW 1 does not affect:
- `docs/CONFIGURATION.md:9` — "with one deliberate exception: the stale-read matcher keys on the note's own drift sentence … location prefix included. Otherwise only a raw stream (`shell` output, the generic output fallback) can match a note copied verbatim at column 0." ✓ true; the named raw streams are the real ones (`Message.tsx` shell stdout/stderr `:729-733`/`:816-828`, generic `<pre>` `:831-833`).
- `docs/FEATURES.md:42` — same qualification, consistently worded, no "merely quotes" absolute left. ✓
- `delegationNotes.ts:32-38` — same qualification, naming `edit_stale_read` as "the ONE exception". ✓ (the only issue is the backend sentence → LOW 1).

**2. The exception test is real and non-vacuous.** `delegationNotes.test.ts:232-241`, hidden `["edit_stale_read"]`: the `path:line:`-prefixed hit reproduces the full drift sentence → `filterLine:326` `matchesDef("", rawLine, def)` → the unanchored pattern matches inside the line → `null` → the line is dropped → `""`; `.trim() === ""` can only pass if the whole line is gone (a no-op filter returns the long hit line). Fixture shape matches the real emission (`file_edit.rs` `with_fresh_read_nudge`). Its counterpart (marker-only quote survives) is pinned at `:243-250`. ✓

**3. All four render paths are filtered, and there are exactly two store reads.** `Message.tsx:449` / `:715` (`useAgentStore((s) => s.hiddenSteeringNotes)` ×2, count pinned by the test): chip `:520-524` (`filterSteeringNote`, remainder-preserving, `noteText !== ""` guard), error summary `:462-465` + render, shell stdout/stderr `:729-733` → rendered `:816-828`, generic `<pre>` `:832`. ✓

**4. The merged-note splitter re-joins byte-identically.** `filterLine:333-342`: part slices exclude the `"; "` separators (`to = starts[i + 1] - 2`), re-join with `"; "`, and the ORIGINAL line is returned when nothing was dropped (`:341`). ✓

**5. Legacy seeding agrees with `effective_steering_note_visible`.** `general.rs:578-594`: `auto_delegated` → `notes.auto_delegated.unwrap_or(self.show_delegation_notes)`, every other key → `unwrap_or(true)`, unknown key → `true` (`:592`, plus the accurate comment at `:1276`). Frontend `hiddenKeysFromConfig` (`:403-411`): absent/null table → `legacyShowDelegationNotes ? [] : ["auto_delegated"]`, present table → only explicit `false` entries in registry order — semantically identical for every payload the shipped DTO can carry (11 non-optional bools, no skip attribute). ✓

**6. Fixture / contract chain agrees end to end.** `GetSettingsSteeringNotes` (`settings.rs:632-655`, per-field docs) ← `resolved()` via `effective_steering_note_visible` (`:661-675`) ↔ `contract_fixtures.rs:255-267` ↔ `dto-get-settings.json:60-72` (same 11 keys, same values — `auto_delegated: false`, rest `true` — same order as the Rust struct) ↔ `ipc-contract.test.ts:438-452` (sorted 11-key set + `auto_delegated === false` + `literal_tip === true`) ↔ `tauri.ts:468-471` ↔ `ChatSection` draft/save (`steering_notes: draft.steeringNotes`) ↔ `settings_dto.rs:602-637` (`Some`-only per-field patch, legacy key still applied at `:599-601`). ✓

**7. Display-only contract holds.** `graph_context(effective_steering_note_visible)`: one incoming call — `src-tauri/src/ipc/settings.rs::resolved:661` (the GetSettings DTO path), no tool/agent/prompt consumer. Frontend `hiddenSteeringNotes` consumers are exactly `Message.tsx` (2 reads + the 4 render sites), `ChatSection.tsx:43`, the store field/seed/setter (`useAgentStore.ts:284/:662/:885`), `App.tsx` hydration, and tests. No path writes the hidden set back into tool output; the tool-result text the model reads is untouched (DECISION 950fdba5). ✓

**8. Residual-claim sweep.** `first-line scoped` → **1** hit tree-wide: the revised sentence itself (LOW 1's parenthetical is another matter, but there is no unqualified survivor). `never hidden|never hide|is not hidden|never be hidden` → only: `delegationNotes.ts:261` (the registry comment, verified true — the pattern requires the drift clause AND the marker in order, so a marker-only quote survives; both sides pinned), `general.rs:1276` (unknown key → visible, accurate), and unrelated sites (`shell_filter.rs:158/:403`, `ModelPickerDropdown.tsx:38`, `VisionSection.tsx:140`, `docs/FEATURES.md:25`). `merely quote(s)` → no hits outside the historical review reports that quote the pre-fix wording (records, not docs/source). ✓

## Constitution checks

- **Documentation sync** ✓ — both docs updated (`docs/CONFIGURATION.md:9`, `docs/FEATURES.md:42`), the superseded SPEC record carries `status = "superseded"`. (LOW 1 is an internal module comment.)
- **Multi-platform neutrality** ✓ — TypeScript plus serde/TOML only; no platform API, path or shell syntax in the diff.
- **File-tools-first** ✓ — no shell-based file mutation in the diff; the `.coding/` churn (backlog status flip, knowledge front matter/amendment, `plans/9ff59133.md` regression-test field) is app bookkeeping.
- **Warning-free Rust, no `#[allow(...)]`** ✓ — `search "#\[allow\("` over `src/**/*.rs` → 0 matches (124 files); no attributes added in the diff.
- **Doc comments on new public items** ✓ — `SteeringNotesCfg` + fields, `UiConfig::STEERING_NOTE_KEYS` (`:548-551`), `effective_steering_note_visible` (`:566-577`), `SteeringNotesDto` + patch, `GetSettingsSteeringNotes` + every field (`:625-655`).

## Observations (not findings — no action required)

- `ReadNudge` (`:235`) uses `output.starts_with(…)`, which is *stricter* than first-line scoping (the marker must be at column 0 of line 1). The clause's "first-line scoped" is therefore an under-description for that kind, not an over-claim: any result it fires on does carry the marker on its first line, so the operative guarantee holds.
- The mandate's ground-truth framing ("the implementation's own comment at `:237-241` stating the first-line scoping 'is not available' for those") covers all three full-output kinds; the comment itself names the graph tools and `create_plan` only — the consolidation arm has its own comment at `:257-259`. That gap is the root of LOW 1.
- Test status is as reported by the main agent (vitest 1175 / 83 files, `npm run build` clean, `cargo test` 2366 + 16 passed / 0 failed); I am read-only and could not re-run the suites. LOW 1's fix is a comment-only edit in `frontend/src/lib/delegationNotes.ts` — no test touches the module doc, so the suites cannot be affected.

## Bottom line

The round-3 LOW 1 is fixed in substance: the clause's scope claim, its three-kind exception list and its test citation now match `detect()` exactly, and nothing else moved — same 21 paths, the ±stat delta is precisely the clause's +3 lines, and every cross-file anchor round 3 recorded still lands on the same line. Every other round-3 verification (three-spot consistency, non-vacuous exception test, four filtered render paths, byte-identical splitter, legacy seeding parity, contract chain, display-only contract, constitution) still holds on spot-check. The one remaining item is LOW 1: the new parenthetical extends the "tool gate that cannot carry another kind" rationale to the consolidation arm, which has no tool gate (`detect()` ignores `tool_name`; the note is appended to any successful result by `dispatch.rs:781-796`) and whose own comment gives phrasing specificity as the safety reason. Fix that one clause, re-run the suites once for hygiene (comment-only edit), commit, and finish with this report — after that fix the change is ready to commit.
