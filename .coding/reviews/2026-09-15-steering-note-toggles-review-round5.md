## Verdict: PASS

Round-5 (final) verification of plan `3ae367bd` "Per-note toggles for steering notes in the Chat config" on `wt/mnemo`, against the FULL uncommitted diff (`git_read(op="diff")`: **21 paths / +1416 −180**) after the single post-round-4 edit — one clause in the `frontend/src/lib/delegationNotes.ts` module doc. Rounds 1-4 were read first; their checklist is the baseline. I am read-only — the suites were not re-run; every claim below is hand-derived from source.

**The change is ready to commit.** The revised clause is fully accurate against `detect()` and `with_consolidation_note`, including the now-correct split rationale for the three full-output kinds; nothing else moved (same 21 paths, stat delta exactly the clause's +2 lines, every cross-file anchor still on round-4's line); and every round-4 baseline spot check still holds. No findings — hence PASS with 0 findings of any severity.

The clause under test (`delegationNotes.ts:39-46`): *"The backend's own detection is first-line scoped wherever a result could echo foreign content (`steering_stats.rs` `detect()` — the graph-miss, recall-rider and consolidation notes scan the whole output instead: the first two sit inside a tool gate that cannot carry another kind, the third is appended by the dispatcher to any successful result and kept unambiguous by its dispatcher-specific phrasing), and its tests pin that a grep result quoting the shell TIP does not fire."*

## 1. The clause is accurate — every arm checked against ground truth

**Scope arm — "first-line scoped wherever a result could echo foreign content".** `MarkerKind::detect()` (`src/agent/steering_stats.rs:228-271`) scopes six kinds with `first_line().contains(…)` (SearchNudge `:233`, ShellTip `:236`, LiteralTip `:251`, KnownMemoryHit `:255`, ShellRedirect `:262`, EditStaleRead `:268`) and ReadNudge with `output.starts_with(…)` (`:235` — *stricter* than first-line scoping, the pre-existing under-description round 4 recorded as an observation, not an over-claim). The only full-output scans are the three the clause names — GraphMiss `:246`, RecallRider `:248`, ConsolidationDue `:260`. The headline claim is therefore exhaustive and exact.

**Exception arm — "the graph-miss, recall-rider and consolidation notes scan the whole output instead".** Exactly those three use `output.contains(self.marker())`; the implementation's own doc comment above `detect()` (visible as the bullet list ending `:227`) names the same three full-output kinds. ✓

**Split rationale — first two.** "the first two sit inside a tool gate that cannot carry another kind": GraphMiss's arm is gated `matches!(tool_name, "graph_search" | "graph_context" | "graph_impact" | "graph_path")` (`:242-247`); RecallRider's is `tool_name == "create_plan"` (`:248`). The implementation's comment at `:237-241` states this exclusivity verbatim — "The graph tools and create_plan cannot carry a kind other than their own, so a full-output scan inside their gate is safe" — and it covers exactly those two, which is now exactly the clause's attribution. **Round-4 LOW 1 is fixed.** ✓

**Split rationale — third.** "the third is appended by the dispatcher to any successful result and kept unambiguous by its dispatcher-specific phrasing": the ConsolidationDue arm (`:260`) never consults `tool_name`; `with_consolidation_note` (`src/agent/dispatch.rs:781-796`) returns early unless `r.success` and otherwise does `r.output.push('\n'); r.output.push_str(&note)` — no tool restriction — and is invoked from the dispatch-funnel returns at `:660`, `:712` and `:751` (three call sites, matching "any successful result"). Its own comment (`:257-259`) gives dispatch ownership + append position + dispatcher-specific phrasing as the safety reason, and the note text (`src/tool/steering.rs:116-119`, "NOTE: {n} working-memory events accumulated this session — consider memory_consolidate(session_id) to distill them") is unique to the dispatcher. ✓

**Test-citation arm — "its tests pin that a grep result quoting the shell TIP does not fire".** `shell_tip_is_first_line_scoped` (`src/agent/steering_stats.rs:1199-1222`) feeds `"grep done\nsrc/docs.md: the TIP: for file-content search string quoted from a file\n[exit code: 0]"` and asserts `snapshot().fired == 0` (`:1208`), then pins that the line-1 prepended TIP fires exactly once (`:1216-1221`). The companion `search_nudge_is_first_line_scoped` (`:1169-1196`) pins the same shape for the search nudge. ✓

**No residual over- or under-claim** beyond the ReadNudge strictness (which errs in the safe direction) and the reading note in Observations below.

## 2. Nothing else moved since round 4

- **Same 21 paths, delta = the clause alone.** `git diff HEAD --stat`: 21 files / **+1416 −180** vs round 4's +1414 −180 — exactly +2 insertions, 0 deletion change. The round-4 clause occupied 6 lines (`:39-44`); the revised one occupies 8 (`:39-46`). Same file list round 3/4 enumerated: 4 `.coding/` bookkeeping + `docs/CONFIGURATION.md`/`docs/FEATURES.md` + the 15 frontend/Rust files.
- **Cross-file anchors still land on round-4's lines** (a shifted file would have moved every anchor inside it — none did): `Message.tsx:449` (store read), `:462-465` (errorSummary), `:520-524` (chip), `:663-665` (render guard), `:715` (second read), `:729-733` (shell strips), `:816-828` (shell render), `:832` (generic `<pre>`); `delegationNotes.test.ts:232-241` (documented-exception test) + `:243-250`; `contract_fixtures.rs:255-267`; `settings.rs:632-655` + `resolved()` `:661-675`; `ipc-contract.test.ts:438-452`; `tauri.ts:468-471` + save patch `:540`; `dto-get-settings.json:60-72`; `general.rs:578-594` (`Default.steering_notes` `:535`, `STEERING_NOTE_KEYS` `:552`); `settings_dto.rs:602-637` (`SteeringNotesDto` `:175-217`); `ChatSection.tsx:42-44` / `:91-93` / save patch `:103`; `App.tsx:341-352`; `useAgentStore.ts:662`; `docs/CONFIGURATION.md:9` (the diff hunk `@@ -6,6 +6,8 @@` places the new paragraph there) and `docs/FEATURES.md:42` (single-line replacement, same line number).
- **Inside `delegationNotes.ts`** everything after the clause shifts by exactly +2 (`matchesDef :294→:296`, `isComponentStart :302`, `componentStarts :305→:307`, `filterLine :324→:326`) and the registry entries likewise (`search_nudge` prefixes `:144→:146`, `graph_miss :175→:177`, `edit_stale_read` pattern `:262→:264`); everything before `:39` is unmoved. Consistent with one insertion and no other edit — no unreviewed change found.

## 3. Round-4 baseline spot checks — all still hold

1. **Three doc spots agree with `filterLine` / `matchesDef` / `componentStarts`.** `matchesDef` (`:296-299`) tests `linePatterns` against the RAW line and `linePrefixes` against the note-unwrapped text via `startsWith`; `filterLine` (`:326-345`) runs the whole-line pass first (`:328` — patterns only, since `"".startsWith(prefix)` is false), then the single-component pass (`:330-333`), then splits only at `"; "` offsets followed by a recognized note start (`componentStarts :307-315` via `isComponentStart :302-304`). Registry patterns are `^`-anchored (`search_nudge :147-150`, `graph_miss :177`, `consolidation_due :238`) **except** `edit_stale_read :264` (`/— the content has likely drifted from your last read\.\s*Re-read the file/`, deliberately unanchored) — exactly the exception all three spots name and the revised clause does not affect. `docs/CONFIGURATION.md:9`, `docs/FEATURES.md:42` and `delegationNotes.ts:32-38` carry the same qualification with the same exception, mutually consistent.
2. **The documented-exception test is real and non-vacuous.** `delegationNotes.test.ts:232-241`: a `path:line:`-prefixed hit reproducing the full drift sentence under `["edit_stale_read"]` → whole-line pass → the unanchored pattern matches inside the line → `null` → dropped → `""`; `.trim() === ""` can only pass if the whole line is gone (a no-op filter returns the long hit line). Its counterpart (marker-only quote survives) is pinned in the same test file.
3. **All four render paths are filtered, with exactly two store reads.** Chip `Message.tsx:520-524` (`filterSteeringNote(info.note, hiddenSteeringNotes)` + `noteText !== ""` guard); error summary `:462-465` + render `:663-665`; shell stdout/stderr `:729-733` → render `:816-828`; generic `<pre>` `:832`. Store reads: `:449` and `:715` (the test pins the count at 2, and `Message.tsx` no longer contains the removed single-toggle field).
4. **The merged-note splitter re-joins byte-identically.** `filterLine:335-345`: part slices exclude the `"; "` separators (`to = starts[i + 1] - 2`), re-join with `"; "`, and the ORIGINAL line is returned when nothing was dropped (`:343`); `null` when everything was.
5. **Legacy seeding agrees with `effective_steering_note_visible`.** Backend (`general.rs:578-594`): `auto_delegated → notes.auto_delegated.unwrap_or(self.show_delegation_notes)`, the other ten → `unwrap_or(true)`, unknown key → `true` (`:592`, accurate comment `:1276`). Frontend `hiddenKeysFromConfig` (`:405-413`): absent/null table → `legacyShowDelegationNotes ? [] : ["auto_delegated"]`, present table → only explicit `false` entries in registry order; `App.tsx:347-352` passes BOTH fields. Semantically identical for every payload the shipped DTO can carry (11 non-optional bools, no skip attribute).
6. **Fixture / contract chain agrees end to end.** `GetSettingsSteeringNotes` (`settings.rs:632-655`, doc comment + per-field docs, resolved via `effective_steering_note_visible` in `resolved()` `:661-675`) ↔ `contract_fixtures.rs:255-267` ↔ `dto-get-settings.json:60-72` (same 11 keys, same values — `auto_delegated: false`, rest `true` — same order) ↔ `ipc-contract.test.ts:438-452` (sorted 11-key set + `auto_delegated === false` + `literal_tip === true`) ↔ `tauri.ts:468-471` (resolved) / `:540` (save patch) ↔ `ChatSection.tsx:42-44` (draft from store) / `:91-93` + `:103` (store write + save patch) ↔ `settings_dto.rs:602-637` (`Some`-only per-field patch; legacy key still applied at `:599-601`).
7. **Display-only contract holds.** `graph_context(effective_steering_note_visible)` → incoming calls: exactly ONE, `src-tauri/src/ipc/settings.rs::resolved:661` (the GetSettings DTO path) — no tool/agent/prompt consumer. Frontend `hiddenSteeringNotes` consumers are the two `Message.tsx` reads (four render sites), `ChatSection.tsx:43`, the store field/seed/setter (`useAgentStore.ts:662`), `App.tsx` hydration and tests. No path writes the hidden set back into tool output; the tool-result text the model reads is untouched (DECISION 950fdba5).

## 4. Residual-claim sweep on the final tree

- `first-line scoped` → **1** source hit tree-wide: the revised sentence itself (`delegationNotes.ts:39`); every other hit is a review report (record, not source/docs). No unqualified survivor.
- `never hidden|never hide|is not hidden|never be hidden|merely quote` → sources: `delegationNotes.ts:263` (registry comment — verified true: the pattern requires the drift clause AND the marker in order, so a marker-only quote survives), `general.rs:1276` (unknown key → visible, accurate), and unrelated sites (`docs/FEATURES.md:25` shell-output filtering, `ModelPickerDropdown.tsx:38`, `VisionSection.tsx:140`, `shell_filter.rs:158`/`:403`). No "merely quote(s)" outside historical review reports that quote the pre-fix wording. ✓

## 5. Constitution checks

- **Documentation sync** ✓ — `docs/CONFIGURATION.md:9` (table, 11 keys, defaults, legacy seeding, display-only contract) and `docs/FEATURES.md:42` (per-note toggles sentence) both updated and consistent with the code; the superseded SPEC record carries `status = "superseded"`.
- **Multi-platform neutrality** ✓ — TypeScript plus serde/TOML only; no platform API, path or shell syntax anywhere in the diff.
- **File-tools-first** ✓ — no shell-based file mutation in the diff; the `.coding/` churn (backlog status flip + `plan_id`, knowledge amendment, knowledge front matter, `plans/9ff59133.md` regression-test field) is app bookkeeping.
- **Warning-free Rust, no `#[allow(...)]`** ✓ — `#[allow(` over `src/**/*.rs` → 0 matches (124 files); `src-tauri/src` → only two pre-existing doc-comment prose mentions (`backlog_cmds.rs:818`, `main.rs:988`). No attributes added in the diff.
- **Doc comments on new public items** ✓ — `SteeringNotesCfg` + fields, `UiConfig::STEERING_NOTE_KEYS` (`general.rs:548-551`), `effective_steering_note_visible` (`:566-577`), `SteeringNotesDto` + all 11 fields (`settings_dto.rs:175-217`), `GetSettingsSteeringNotes` + all 11 fields (`settings.rs:625-655`).

## Observations (not findings — no action required)

- **Reading note on "a tool gate that cannot carry another kind".** The clause mirrors the implementation's own comment (`steering_stats.rs:237-241`) and now applies it to the first two kinds only; the sentence's third arm draws the tool-emitted / dispatcher-appended distinction explicitly, so the pairing is coherent: gate exclusivity covers notes a tool emits, the dispatcher's append is the third arm's own subject. (Even in the one scenario where a graph tool result also receives the dispatcher-appended consolidation note, the append cannot make the first two's scans false-fire — the appended text contains neither "No symbols matched" nor "RECALLED CONTEXT".)
- `ReadNudge` (`:235`) uses `output.starts_with(…)`, *stricter* than first-line scoping — an under-description, not an over-claim (unchanged from round 4).
- Test status is as reported by the main agent (vitest 1175 passed / 83 files, `npm run build` clean, `cargo test` 2366 + 16 passed / 0 failed); I am read-only and could not re-run the suites. The edit is comment-only and no test touches the module doc, so it cannot affect any suite.

## Bottom line

Round-4 LOW 1 is fixed exactly as specified: the parenthetical now attributes gate exclusivity to the graph-miss and recall-rider kinds only, and gives the consolidation kind its real safety reason — dispatcher append to any successful result plus dispatcher-specific phrasing (`detect()` `:260` never consults `tool_name`; `with_consolidation_note` `dispatch.rs:781-796`). Every arm of the sentence matches `detect()` and the cited tests (`steering_stats.rs:1199-1222`), with no residual over- or under-claim. Nothing else moved — same 21 paths, stat delta exactly the clause's +2 lines, every cross-file anchor on round-4's line — and all round-4 baseline verifications (three-spot consistency, non-vacuous exception test, four filtered render paths, byte-identical splitter, legacy seeding parity, contract chain, display-only contract, constitution) still hold on spot-check. **PASS: the change is ready to commit.**
