## Verdict: PASS

Round-2 verification of `wt/mnemo` (project root `C:\AgenticCoder\AgenticCoder`). I reviewed ALL uncommitted changes; the round-2 surface is `src/memory/knowledge.rs` (`leading_self_heading` :848-891, the extracted `stamp_shaped` :898-914, `strip_self_headings` :916-944, the reworded `replace_in_body` doc :801-808, tests :1493-1726) and `src/tool/memory/mod.rs` (the reworded amend NOTE :995-1006, its test :2469-2530). Everything else in the change set (factory.rs ceilings, prompt.rs, agent.md, PLAN.md, docs/FEATURES.md, tool schemas, the repair path) is byte-identical to what round 1 verified — I re-read it and found no drift.

Both round-1 findings are closed against SOURCE, and the LOW 1 fix introduces no new false positive, no panic path, and no regression for any previously-stripped shape. I could not re-run the tests (the reviewer surface is read-only — no shell), so RED/green claims are hand-traced against source, as in round 1; the parent's recorded evidence (2380 passed / 0 failed / 5 ignored, exit 0; `memory::knowledge::tests` 27; `tool::memory::tests` 34; the budget test standalone and under `--workspace`) is consistent with everything I traced, and I found no counterexample in the source.

### Round-1 LOW 1 (strip-rule gap) — CLOSED

Method: for every input below I walked the candidate chain (`line.match_indices(':').map(|(i,_)| i).filter(|i| *i >= word_len).chain(std::iter::once(line.len()))`, :870-874) through the per-candidate cap + `stamp_shaped` (:875-879) and the remainder selection (:880-887).

**(a) The two named shapes now strip, to exactly one tool-dated heading.**

- `Amended 2027-01-14 (review L1: the message was wrong): the wording changed` → candidate 1 = the colon after `L1`: head `2027-01-14 (review L1` → tail `-01-14 (review L1`, `tail.find('(') == Some(7)`, `tail[7..]` does not end `)` → **false**. Candidate 2 = the colon after `)`: head `2027-01-14 (review L1: the message was wrong)` → `tail[7..].ends_with(')')` ✓ and `tail[..7] = "-01-14 "` all `date_char` ✓ → strip at candidate 2, remainder `&paragraph[colon + 1..] = " the wording changed"` → loop's `trim_start` → `"the wording changed"`; the loop re-enters, `leading_self_heading("the wording changed")` returns `None` → `("the wording changed", 1)`.
- `Amendment (plan 987fef4c: the nudge fix): x` → candidate 1 head `(plan 987fef4c` starts `(` but does not end `)` → false; candidate 2 head `(plan 987fef4c: the nudge fix)` ✓ → `("x", 1)`.
- No-colon forms (whole-line candidate, `colon == line.len()`): remainder is `&paragraph[line.len()..]` with one `strip_prefix('\n')` → `Amended 2027-01-14\n\nBody text` → `("Body text", 1)`; `Amended 2027-01-14\nBody text` → `("Body text", 1)`; `Amendment (plan 987fef4c)\nBody text` → `("Body text", 1)`; bare `Amended 2027-01-14` → `("", 1)`.
Every expectation matches the new assertions in `strip_self_headings_matches_stamp_shapes_only` (:1634-1648) and the new end-to-end case (:1552-1562). End-to-end the count is 1, not 2, because `MemoryAmendTool::execute` computes the NOTE with the same pure function on the same input (:998) and `KnowledgeStore::amend` then re-strips that same input (:983) — `strip_self_headings` trims internally (:937) and `amend` trims before it, so both see one and the same paragraph and agree on the count.

**(b) No new false positive.** Every newly-stripping input is one where a LATER colon yields a documented stamp head (year + date-shaped tail, or a parenthesized group) — the fix's intended class. The named prose shapes:

| first line | strips? | trace |
|---|---|---|
| `Amended the docs: 2027-01-14 (see notes): x` | **no** | cand 1 head `the docs` (no `(`, no 4-digit year) false; cand 2 head `the docs: 2027-01-14 (see notes)` starts `t` → false; cand 3 (whole line) same → verbatim |
| `Amended the readme: 2027-01-14 was the year: really` | **no** | every candidate head starts `t` → false → verbatim |
| `Amended the readme and the changelog: both now mention the flag` | **no** | verbatim (unit control :1648-1656) |
| `Amended 2027 was the year the store moved: really` | **no** | heads `2027 was…` / `2027 was…: really`: tails contain `w`, not `date_char` → verbatim |
| `Landed outcome (2027-01-14): …` | **no** | word boundary fails at :855 — no candidate is ever built |
| `Amended 2027-01-14 12:30: x` | **yes** | at candidate 1 (the colon INSIDE `12:30`): head `2027-01-14 12` is date-char-only → remainder `"30: x"` |

The last row is the only loose strip, and it is **pre-existing, not new**: candidate 1 is byte-identical to round 1's `line.find(':')` and round 1's inline predicate accepted that same head, so the pre-fix code landed `"30: x"` too. It is outside the round-1 finding's named shapes and the backlog acceptance list, no body text is lost (the `x` survives; the residue is a `30: ` fragment of heading material), and — pleasingly — the residue is exactly what the new `find`/`replace_with` repair mode cleans up in-tool. Recorded as a note, not a finding (see below). Nothing here swallows content a reasonable caller would mourn.

**(c) Panic-freedom of every new slice — verified for arbitrary input.**

- `line[word_len..colon]`: `word_len ∈ {7, 9}` and `bytes[..7]`/`bytes[..9]` can only `eq_ignore_ascii_case` when those bytes are ASCII, so `word_len` is a char boundary; `colon` comes from `match_indices(':')` (ASCII) so it is a boundary too, and `.filter(|i| *i >= word_len)` guarantees the range is non-inverted. That filter is in fact a no-op — `:` cannot occur inside `amended`/`amendment` — but it makes the invariant explicit rather than assumed, which is the right direction.
- `&paragraph[line.len()..]`: `lines().next()` is always a prefix of the paragraph (split at the first `\n`, minus a trailing `\r`), so `line.len() <= paragraph.len()` and the index is a char boundary.
- `strip_prefix('\n')` returns an `Option` — no indexing. For a CRLF paragraph the remainder keeps its `\r` and `strip_self_headings`' `trim_start` (:940) removes it; `leading_self_heading` has no other caller (searched the tree: called only at :939), so the raw remainder is never surfaced to anything else.
- Edge cases traced, all safe: empty paragraph (`"".lines().next()` → `None`), a first line that is only `:` (len < 7 → `None` before any slice), `Amended :` and `Amended:` (empty head → `continue`; the other candidates' heads start with `:` and can never be stamp-shaped), multibyte (`Amended 2027-01-14 (план: тест): текст` → colon indices are ASCII boundaries, and non-ASCII bytes fail `is_ascii_digit`/`date_char` → no panic, no strip), CRLF (`"Amended 2027-01-14\r\n"` → remainder `"\r\n"` → `strip_prefix` no-op → `trim_start` empties it → `("", 1)`).
- `stamp_shaped`'s own slices are unchanged in kind and safe: `head.as_bytes()[..4]` is a byte slice guarded by `head.len() < 4`, and `head[4..]` is only reached when bytes 0..4 are ASCII digits (hence index 4 is a boundary); `tail[open..]`/`tail[..open]` use a `(` byte index, which is a boundary.

**(d) Termination — the no-colon form still strictly shrinks.** Each successful step consumes at least the word plus its terminator: the colon form returns a suffix starting at `colon + 1 >= word_len + 1 >= 8`, the whole-line form a suffix starting at `line.len() >= 7` and then one more byte when the `\n` is stripped — so ≥ 7 bytes per iteration, and `rest` is only ever replaced by a strict suffix of itself or that suffix `trim_start`ed. The loop therefore cannot re-enter with identical text. This is the form that could have looped (same first line, no colon), and it is the one the ≥-line-length argument covers.

**(e) `stamp_shaped` is semantically identical for every previously-passing shape (no regression).** The chain visits round 1's candidate FIRST — the first `:` at/after the word, which is exactly the index `line.find(':')` returned — with the same predicate body (parenthesized head ⇒ `ends_with(')')`; else 4-digit year + `date_char` tail, `find('(')`-split tail, same `- / . : , space` set), so any input the round-1 code stripped strips at the same colon and returns the byte-identical remainder; only inputs that previously returned `None` can newly strip. The two asymmetries cannot break that: (i) the cap is now `chars().count() > 120` while `stamp_shaped` remains byte-based — a previously-passing head is non-empty and passed the old cap, and `chars ≤ bytes`, so it still passes; the change can only *admit* previously-refused multibyte heads, in the direction the doc already names ("a head of at most 120 chars", :924); (ii) `continue` replaced `return None` on the empty/over-cap head — an empty head means the colon sits at `word_len`, which forces every later candidate head to start with `:`, and an over-cap head makes every later head *longer* (candidate heads are prefixes of one another), so neither abort path could have stripped elsewhere; observably identical. Method note: the round-1 interim source was never committed (the whole change is uncommitted vs `HEAD`), so equivalence rests on round-1's report — which quotes the pre-fix lines (`let colon = line.find(':')?;` :863, `head = line[word_len..colon].trim()` :864) and traces the same branches and the same `date_char` set — plus the structural argument above.

**(f) The new/extended tests are RED without the fix.** Both interior-colon expectations and all four no-colon expectations require a strip the round-1 code provably refused: `line.find(':')?` returns `None` for the no-colon forms (so `strip` returned the input, count 0), and for the interior-colon forms the head was truncated before the group's `)` (round-1 traced both named inputs to "nothing stripped"). The end-to-end case `Amended 2027-01-14 (review L1: …): the wording changed` fails pre-fix at `assert_eq!(headings(&body), 1)` — the doubled heading counts 2. Non-vacuity of that helper: `KnowledgeStore::write` suffixes slug collisions (`-2`, `-3`, … :667-684), so each `amended(...)` call gets a FRESH file whose body is exactly `"the store is sqlite"` — the count of 1 is a real single-heading assertion — and the fixed clock (`CLOCK = 2026-02-01`, :1303) plus the empty-artifact tempdir (no `.coding/reviews`, so `artifact_date_floor` yields nothing) make the `"Amended 2026-02-01: …"` expectations deterministic.

### Round-1 LOW 2 (doc overstatement) — CLOSED

`src/memory/knowledge.rs:801-808`. The clause now reads "…that guard stops a condensed row digest from REPLACING a body, while a unique-substring replacement cannot do so *silently* — the caller must spell out the exact text it removes (a paraphrase matches nothing) —". Checked line by line against the code: `replace_in_body` genuinely does not consult `looks_like_row_digest` (:809-843, :1018-1027); the only thing between a caller and a shrink is `replace_unique`'s exact-match rule (:475-497 — `body.matches(find).count() == 1`, no normalization, no fuzzy matching), so "a paraphrase matches nothing" is precisely what `count() == 0` → `"not present"` enforces; and the sentence no longer denies the reachable digest shape (caller spells out the verbatim body minus the pointer tail), which is the overstatement round 1 flagged. The retained half — a shrinking repair "would otherwise be refused whenever the body ends in a `.coding/knowledge/…md` pointer tail" — matches `looks_like_row_digest`'s two conditions exactly (strictly shorter than the current body AND a last non-empty line containing `.coding/knowledge/` and ending `.md`). No understatement either: nothing in the paragraph promises more than the code does.

### Amend NOTE (round-1's cosmetic note) — closed, and it fires exactly when a strip happened

`src/tool/memory/mod.rs:995-1006`. The reworded string is `"; NOTE: stripped {stripped} self-supplied amendment heading(s) ('Amended …' / 'Amendment (…)') from the paragraph — the tool stamps its own date"`.

- **Fires exactly when a strip happened.** It is computed from the same pure function on the same input the store strips (`strip_self_headings(&args.paragraph)` :998 vs `amend`'s `strip_self_headings(paragraph)` :983 — the store trims first at :973 and the helper trims internally at :937, so the counts agree), and it is interpolated ONLY into the success arm of the amend `match` (:1009-1015). The error arms — unknown id (:985-994), `amend` failure (:1021), reindex failure (:1019) — never carry it, so the header-only-paragraph error path (where a strip did happen but no amendment landed) reports no NOTE. A stripped count of 0 yields an empty string, so plain paragraphs get neither a NOTE nor a stray `; `.
- **The test's assertion still matches.** `amend_result_reports_a_stripped_self_heading` asserts `result.output.contains("NOTE: stripped 1 self-supplied")` (:2496-2499) against the new format string ✓, plus `text.matches("Amended ").count() == 1` and `!text.contains("Amended 2027-01-14")` ✓; the control asserts `!output.contains("NOTE: stripped")` ✓. The reword also closes round 1's note that the NOTE said only `'Amended …'` even for an `Amendment (…)` strip — it now names both forms.
- **Budget neutrality:** the NOTE is a runtime result string, not schema text, so it cannot move the tools-array ceilings in `src/agent/factory.rs`; the two memory tools' *schema* strings are unchanged by round 2 (only a Rust doc comment and this result string changed), so the recorded 2027-01-15 measured values stand.

### No other doc/description in the change set overstates behavior

I re-read every changed description against the code: `memory_update`'s tool description and the `find`/`replace_with` property docs (:772-797), `repair_by_id`'s doc (:681-685), `replace_unique`'s doc (:467-474), `replace_in_body`'s doc, `strip_self_headings`' doc (:916-935), `MemoryAmendTool`'s struct doc (:907-914), `memory_amend`'s description + `paragraph` property (:946-968), plus PLAN.md, agent.md, docs/FEATURES.md and prompt.rs's two edited lines. Each states what the code enforces: exactly-once matching, front-matter never matched, empty replacement refused, first-line-only recognition, the tool's date authoritative. The one absolute-sounding phrase — "so exactly one heading ever lands" (amend description and struct doc) — is scoped by its own first clause to "A leading 'Amended …:' / 'Amendment (…):' heading", and the operative rule ("A heading is recognized only on the FIRST LINE") is stated in `strip_self_headings`' doc, so a paragraph carrying a mid-paragraph date-headed line is covered by the spec rather than contradicted by it. Recorded as a note, not a finding (round 1 reached the same conclusion, and nothing in round 2 changed these strings).

### Re-checks of the round-1 verified items the LOW 1 fix could have disturbed

- **Post-strip emptiness check** (:984-990) intact: `"Amended 2027-01-14:"` → `("", 1)` → error, so a header-only amendment is impossible without a header (asserted :1582), and the message names the rule ("pass the amendment text itself (the tool stamps the heading)"). The strip cannot make a non-empty paragraph empty except in the intended case: the remainder is always the text after the chosen colon/line, and the loop only ends on `None`.
- **Multi-line preservation** intact: the colon form returns `&paragraph[colon + 1..]` verbatim (assertions `"…: first\n\nsecond"` and `"…:\nSecond line."` :1618-1626) and the whole-line form consumes only line 1 plus one newline — no later line is ever dropped.
- **Prose controls** all survive verbatim (:1648-1656), and I independently traced two extra interior-colon prose shapes (`Amended the docs: 2027-01-14 (see notes): x`, `Amended the readme: 2027-01-14 was the year: really`) — neither strips (see (b)).
- **Doubled-chain collapse** intact: `Amended A: Amended B: z` → `("z", 2)`; the same loop collapses the no-colon chain (`Amended 2027-01-14\nAmended 2027-01-15\nBody` → `Body`), with termination per (d).
- **No existing knowledge record modified.** `git status`: no tracked `.coding/knowledge/**` file is modified; the untracked set is exactly this plan's HOW memory (`how/2027-01-11-large-file-edit-payloads-…md`), plan b4812291's two records (`bug/b4812291.md`, `spec/2027-01-11-sandbox-write-protection-hardlink-8-3-alias-ladd.md`), this plan's file (`plans/ecdd67ac.md`) and round 1's report. `.coding/backlog.jsonl`'s only change is this plan's own dispatch claim on item 488248ce (`pending` → `in_flight` + `plan_id`/`plan_title`); `.coding/plans/b4812291.md`'s diff is that interrupted plan's step checkboxes `[x]`→`[ ]` plus a `## Regression test` line — both pre-existing bookkeeping, as the spawn brief states.

### Notes (not findings)

1. **Time-in-head residual** (`Amended 2027-01-14 12:30: x` → `"30: x"`): pre-existing and byte-identical pre/post fix (candidate 1 is round 1's candidate and it passes), outside the acceptance list and outside the round-1 finding's named shapes; no body text is lost, and the new `find`/`replace_with` mode repairs the residue in-tool. A "prefer the LAST stamp-shaped colon" or "skip a colon followed by two digits" rule would change previously-stripping inputs and so would re-open the regression question (e) just closed — deliberately not taken here.
2. **Group not followed by a colon** (`Amendment (a) b: c`) is not stripped: the head must end at a colon that yields the shape. Pre-existing, and the doc discloses the operative rule ("The head ends at the first colon yielding such a shape"), so a reader is not misled; the round-1 finding's alternative close (terminate at the group's closing paren) was not taken, which is correct for the settled scope.
3. **A first line that is one long parenthesized group, text on the next line, now strips** (the no-colon extension). Intended per the documented stamp shape and disclosed by the tool's NOTE; the caller loses heading material only.
4. **`replace_unique`'s doc says "empty `find`"/"empty body" while the code refuses whitespace-only on both sides** (`find.trim().is_empty()`, `repaired.trim().is_empty()`). The code is the stricter side and the error messages match it — an understatement, not the overstatement class round 1 flagged.
5. **`KnowledgeStore::amend`'s own doc comment** (:946-952) still doesn't mention the paragraph normalization; the operative spec lives on `strip_self_headings`, the call site comments it (:979-982), and the tool description states it. Completeness nit only.
6. **`stamp_shaped`'s `date_char` still carries the `b':'` arm**: with the candidate chain it is no longer dead in every path (a whole-line candidate may contain a colon), but it remains harmless and permissive by one character; round 1's "invites the reader" note is moot now that interior colons are genuinely handled.

### Verification limits

The reviewer surface is read-only (no shell), so I did not re-run `cargo test`; every claim above is traced against source, and the parent's recorded evidence is consistent with it. The round-1 interim source was never committed (the entire change is uncommitted vs `HEAD`), so pre-fix behavior for the RED-ness and equivalence arguments is cited from round-1's report — which quotes the exact pre-fix lines and their per-input outcomes — and re-derived from `line.find(':')?` semantics.
