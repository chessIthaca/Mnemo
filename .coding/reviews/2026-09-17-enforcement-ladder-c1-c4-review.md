## Verdict: FINDINGS (0 high, 1 low)

Review of c3d70cb (plan e43684e7, branch wt/agenticcoder) — enforcement ladder C1–C4. All four features are correctly implemented and match the plan with one gap: the C2 literal-metachar retry hint is missing from search_read's index-engine empty branch (present in the other three of the four empty-result branches), which also makes the SPEC/README claims over-broad.

## Finding 1 (low) — C2 hint missing on search_read's index-engine zero-hit branch

- `src/tool/agent/search_read.rs:311-316` — `build_index_output`'s `ix.hits.is_empty()` branch emits `"no matches found (engine: index; …)"` without the `literal_metachar_hint`, unlike the equivalent search.rs index branch (`src/tool/agent/search.rs:979-991`) which appends it. The branch is reachable: the index path is `args.literal`-gated by construction (`search_read.rs:178-183`), and `try_index` returns `Some(FtsOutcome::Hits)` with an empty hit vec when a tokenizable literal genuinely doesn't occur (`search.rs:751-756` — an empty raw fetch is authoritative per the comment at 717-722). A `literal:true` search_read over `(a|b)gamma`-shaped metachars therefore gets the mode-mistake signature with no retry hint.
- This contradicts the SPEC record (`knowledge/spec/2026-08-29-enforcement-ladder-c1-c4-alternation-absorption.md`: "appended to EMPTY literal-mode results only, on both engines, in search AND search_read") and the README's blanket claim (`README.md:53`: "a zero-hit `literal:true` search over a metachar-bearing pattern appends a 'retry with literal:false' hint"). The plan step 2 only demanded the walk branch in search_read, so plan-vs-code is consistent — but the shipped docs over-claim.
- Secondary coverage note: both new C2 tests (`search.rs` `literal_metachar_zero_hits_get_the_retry_hint`, `search_read.rs` `literal_metachar_zero_hits_hint`) build their tool with the index-less `make_tool`, so only the walk branches are regression-tested; the index branches (the changed search.rs:983-987 and the unchanged search_read.rs:311-316) have no coverage.
- Suggested fix: pass `args.pattern` (or a precomputed hint string) into `build_index_output` and append `search::literal_metachar_hint()` when `has_regex_metachars(&pattern)` (the index path is literal-gated, so no `args.literal` check is needed there — mirroring search.rs:983). Add an indexed-fixture test (via `make_indexed_tool`) asserting the hint rides an index-mode zero hit in search_read.

## Verified clean

- **(a) C1 reduction edge cases** — `reduce_alternation_branch` (`search.rs:468-482`): empty branches can never reduce (`is_bare_identifier` requires a first char, `search.rs:402-409`, so `""`, `".*"`, `"fn .*"` all reject); the keyword false positive the docs worry about is actually prevented — for `classic` the `class`±space path requires `.*` immediately after (`475`), so it falls through to the plain bare-identifier check and reduces as `classic` itself, never `ic`; nested/unbalanced shapes (`(a|b)|c`, `a||b`, trailing `|`) all reject via the per-branch bare-identifier gate; leading/trailing `.*` combos (`.*watcher.*`, `fn.*update`) reduce correctly; the 2–3 branch cap is enforced by rejection (`494-497`), matching the doc comment. `pub fn .*update` / `fn update.*` shapes are conservatively not reduced — outside the documented accepted set, acceptable for an advisory nudge. The alternation note always carries `SEARCH_NUDGE_MARK` via each entry (`527-534`), dedups by id (`526`), and the byte-pinned bare-identifier sentence is untouched (`565-591`).
- **(b) `symbol_id_fuzzy` SQL** (`store.rs:529-545`) — tier ORDER BY correct (CASE 0/1/2 = exact / ci-equal / substring); `instr` (not LIKE) so `_` in identifiers is never a wildcard; deterministic via `length(name), id`; single `?1` parameter reused 4× (no injection); `LIMIT 1`. `lower()` is ASCII-only — acceptable for an advisory best-effort resolver.
- **(c) C3 escalation** — one-shot via `HashSet::insert` gating (`steering_stats.rs:394`); switch-cancel path increments `agent_switched[kind]` only on a target-tool switch (`433-437`), so `switched == 0` blocks forever after; fired-only kinds skip via `targets().is_empty()` (`379`); borrow scoping is sound (the `fired` block ends the mutable entry borrow before `escalated`/`pending_escalations` are touched); the note phrasing contains no marker substring from the 9-entry `marker()` set (`95-107`) and detect for SearchNudge/LiteralTip/etc. is first-line-scoped anyway.
- **(d) `attach_escalations`** (`dispatch.rs:644-665`) — called AFTER `observe_result` at both funnel arms (`415`, `441`), success-only (`if !r.success`), appends after the body; error results leave notes queued for the next success, exactly as documented. Cannot self-count: observe_result already ran on the pre-append output.
- **(e) C2 firing discipline** — no over-firing: the index branch is literal-gated by construction (`search.rs:975` guards `try_index`; the hint at 983 keys on metachars only, equivalent there), the walk branches gate on `args.literal && has_regex_metachars` (`search.rs:1090`, `search_read.rs:240`), non-empty results and regex-mode misses never carry it. `has_regex_metachars` matches the plan's 14-char conservative set and `is_index_eligible_regex` was correctly deduped onto it.
- **(C4)** — `has_blinding_redirection` covers PowerShell/POSIX/cmd forms (`>$null`/`>/dev/null`/`>nul`); prepend point stacks cleanly with GREP_NUDGE (each ends in `\n`); `ShellRedirect` is a proper 9th kind (index 8, MARKERS/`Inner::counts` len 9, fired-only `targets() == &[]`, shell + first-line detect, MATRIX 7 rows).
- **Constitution** — public fns (`Store::symbol_id_fuzzy`, `CodeGraph::symbol_id_fuzzy`, `take_escalations`) all have doc comments; no Windows-only code or `cfg(windows)` additions, all three shell families covered; README/PLAN.md/knowledge synced (except the C2 over-claim in finding 1); all six named regression tests exist (`alternation_symbol_hunts_absorb_the_graph_lookup`, `literal_metachar_zero_hits_get_the_retry_hint`, search_read `literal_metachar_zero_hits_hint`, `redirection_detector_matches_null_sinks_only`, `redirected_output_carries_the_warning`, `repeat_ignored_actionable_nudges_escalate_once`, `escalation_notes_attach_to_success_only`). The 1691-passed claim is taken as given (read-only review); test code sanity-checked and present. The plan file's unticked step 5 is correct at this point — the closing sequence (this review) is still in progress.
