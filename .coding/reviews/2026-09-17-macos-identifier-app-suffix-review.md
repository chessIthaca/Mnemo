## Verdict: FINDINGS (0 high, 1 low)

Review of the uncommitted change set on `wt/mnemo` (plan `96fe545c`, kind=implementation): rename `src-tauri/tauri.conf.json` `identifier` from `com.mnemo.app` to `com.mnemo.desktop` and sweep every live reference.

**One-line summary.** The functional change is correct, minimal and complete: the new value is a valid reverse-DNS bundle id with no `.app` suffix, the conf's JSON/LF/indentation are intact, runtime propagation is confirmed by code (`webview_udf.rs` derives the UDF base from `_app.config().identifier`, so the mutex arbitration and the per-pid folders follow with no code edit and unchanged primary/secondary semantics), the sweep leaves **zero** live references, the docs are consistent with both the new value and what the code actually computes, and nothing else in the tree consumes the old identifier. The single LOW is confined to the *evidence artifacts and the plan record*: the two build logs are not UTF-8 text, so the repo's own search tool reports **no content match** inside them — which makes the plan's recorded sweep sentence ("…plus the BEFORE evidence log") factually wrong as the tree stands.

**Scope / basis.** `git status --short` + `git diff HEAD` confirm exactly the briefed set: 7 modified (`.coding/backlog.jsonl`, `.coding/browser-debugging.md`, `PLAN.md`, `docs/FEATURES.md`, `src-tauri/src/webview_udf.rs`, `src-tauri/tauri.conf.json`, `src/webview_args.rs`) plus 4 untracked (`.coding/analysis/mac-conf-warning.txt`, `…-after.txt`, `.coding/plans/96fe545c.md`, `.coding/plans/f67072a8.md`). Read-only review: no mutation, no test suites re-run (parent's matrix accepted as evidence); every claim below comes from reading the tree/diff plus independent searches.

### 1. The functional change is correct and minimal — no finding

- `src-tauri/tauri.conf.json:5` reads `"identifier": "com.mnemo.desktop",`. The diff is exactly one changed line; 2-space indent and key order preserved; the file parses as JSON (read end-to-end, lines 1–33); a regex search for `\r` over that file returns 0 matches → LF-only working tree, consistent with `.gitattributes:12` (`* text=auto eol=lf`, "LF is the canonical style").
- `com.mnemo.desktop` is a valid reverse-DNS bundle id: lowercase alphanumeric segments joined by dots, no hyphens/underscores/spaces, and — the point of the fix — no `.app` suffix. Nothing else in the conf moved (`app.windows: []`, the CSP/devCsp, `bundle.targets`, the icon set, `bundle.macOS.minimumSystemVersion` are byte-identical in the diff).

### 2. Runtime propagation and UDF semantics — verified in code, no finding

Read `src-tauri/src/webview_udf.rs:1-90`:

- `:41-42` `base = %LOCALAPPDATA% .join(_app.config().identifier)`; `:47` `default_udf = base.join("EBWebView")`; `:49` the named mutex is keyed on that resolved path (`webview_args::mutex_name_for(&default_udf)`); `:52-54` the secondary profile is `secondary_webview_data_dir(&base, pid)`. Base, mutex and per-pid folder therefore all follow the conf with **no code change** — the plan's "KEY FACT" holds exactly as stated.
- `install_default_data_dir` (`:35-57`) and `apply` (`:63-69`) are semantically unchanged: on a fresh mutex acquire the `OnceLock` is never set, so `apply` returns the builder untouched and the primary keeps the SDK-resolved persistent default profile; on `ERROR_ALREADY_EXISTS` every webview of that instance gets the per-pid folder (one profile per instance, its own browser process). That is the invariant pair the 2027-01-13 fix established, and it is untouched by this diff. `src/webview_args.rs:242-253` still asserts the `<base>\WebView2\mnemo-<pid>` shape, and `mutex_name_for`'s path-scoping test (`:256-270`) is unaffected because it passes its own literal, not the conf.
- Transition behaviour (a positive side effect worth recording, not a defect): an instance still running the *previous* build holds the OLD mutex name (`…\com.mnemo.app\EBWebView`-derived) and the OLD UDF. A new-build primary acquires the new mutex and resolves the new, empty default UDF, so it neither falsely downgrades to "secondary" nor collides with the legacy process — the documented 2027-01-13 gotcha ("the first new instance blank-screens while a legacy host holds the default UDF") does not apply across the rename.

### 3. Sweep completeness — zero live references, no finding

- A literal whole-tree search for `com.mnemo.app` (2,877 files walked) returns 16 matches in 8 files: `.coding/knowledge/**` (2 files), `.coding/plans/**` (4 files), `.coding/reviews/**` (2 files) — precisely the three deliberate-history dirs, and nothing else. **Zero** hits in `src/`, `src-tauri/` (including `src-tauri/tests/` and `src-tauri/capabilities/`), `frontend/`, `docs/`, `README.md`, `PLAN.md`, `.github/`.
- Each of the 6 intended live sites is confirmed changed at its exact location: `src-tauri/tauri.conf.json:5`, `src-tauri/src/webview_udf.rs:9`, `src/webview_args.rs:243` + `:248`, `PLAN.md:353`, `docs/FEATURES.md:56`, `.coding/browser-debugging.md:133` (both occurrences). The new value also appears in all of them (a matching `com.mnemo.desktop` search), so no site was left half-renamed.
- No other identifier consumer exists. A search for `identifier` across `*.{rs,json,json5,ts,tsx,yml,yaml,toml}` surfaces only unrelated uses: parser/grammar identifiers (`src/codegraph/extract.rs`, `src/tool/agent/search.rs`), the capability name `"identifier": "default"` at `src-tauri/capabilities/default.json:3`, and doc prose. `.github/workflows/build.yml` (Windows + macOS jobs) references no bundle id at all — macOS signing/notarization is entirely `APPLE_*`-secret driven and the macOS job just runs `npx tauri build --target aarch64-apple-darwin`, so nothing there needs the value.
- The existing guard does not pin the identifier: `src-tauri/tests/webview_udf.rs` (134 lines) asserts helper names and builder-site call ordering (`webview_udf::apply(` before `.add_child(`) and contains no identifier or UDF-path literal — it cannot go stale from this rename, and it stays valid.
- Nothing else derives a path from the identifier. A search for `app_local_data_dir|app_data_dir|local_data_dir|LOCALAPPDATA|EBWebView` finds the WebView2 base in `webview_udf.rs` as the **only** identifier-derived path in code; user config/keys live outside it (`~/.mnemo/…`, per the backlog/config text), so the rename moves no settings on either platform — only the WebView2 profile (Windows) and the bundle identity (macOS).

### 4. Multi-platform neutrality — no finding

- The change is config-only; the diff touches no Rust logic (one module-doc line + two test string literals). The Windows gates are untouched: `src-tauri/src/webview_udf.rs:36` and `:76` remain `#[cfg(windows)]`, and the module doc's non-Windows contract (`:24-26`, "On other platforms the module is a no-op") is intact. Nothing Windows-only was added; nothing assumes the app runs on Windows.
- macOS side: the only macOS-facing value that changes is the CFBundleIdentifier itself, which is exactly the intent (Tauri derives the bundle id from `identifier`), and `bundle.macOS.minimumSystemVersion` is unchanged. Informational consequence for the parent's summary, not a defect: macOS treats a new bundle id as a different application identity for LaunchServices/TCC purposes, so any previously granted system permissions would have to be re-granted — none are required by the app's current surface, and the `browser_*`/WebView2 stack stays Windows-gated while `apply` remains a pass-through on macOS.
- The Windows consequence is the profile move (`%LOCALAPPDATA%\com.mnemo.app\… → …\com.mnemo.desktop\…`) — a one-time cookie/OS-SSO reset for the running user. It is recorded in the plan context (`96fe545c.md:14`) and belongs in the step-4 final summary, which is where I check it lands.

### 5. Docs sync — no finding

- All four live prose surfaces match the new value **and** the path the code actually computes (`%LOCALAPPDATA%\<identifier>` + `EBWebView` for the primary, `<base>\WebView2\mnemo-<pid>` for additional instances): `PLAN.md:353` (the "Multi-instance WebView2 profiles" row), `docs/FEATURES.md:56` (the Browser-tab bullet), `.coding/browser-debugging.md:133` (both occurrences), `src-tauri/src/webview_udf.rs:9` (module doc). The identifier→UDF coupling itself is documented next to the derivation (`webview_udf.rs:38-40`: "The WebView2 default UDF lives under `%LOCALAPPDATA%\<identifier>`"), which is the right place for a maintainer contemplating another identifier change.
- No other doc quotes a UDF path: `README.md` no longer does (the 2026-12 marketing rewrite moved that detail to `docs/`), and `docs/CONFIGURATION.md` never did — a search for `LOCALAPPDATA|EBWebView` hits only the four places above plus history. So no user-facing/config doc went stale.
- I also looked for a doc that catalogues the `.app` warning as an outstanding macOS-readiness gap (a `bundle identifier|CFBundleIdentifier|ends with` sweep across 78 files, display-truncated): the only such mentions are the two plan files and the evidence logs — records, not live documentation. The complete, un-truncated basis for that statement is the literal `com.mnemo.app` sweep in §3, which returns nothing outside history.
- The one-time profile reset is documented at plan level only. I am **not** filing that as a documentation gap: there is no CHANGELOG/migration surface in this repo, the app is pre-release (v0.1.0, ad-hoc signed, no shipped installers), and the parent's step-4 summary is the sanctioned home for the consequence.

### 6. Constitution compliance — no finding

- **File-tools-first:** the plan record shows both edits were made with the file tools (`96fe545c.md:25-26`), and the diff carries no shell-surgery signature — no whole-file rewrite of `tauri.conf.json`, no encoding churn in source, no reflow of `PLAN.md`/`docs/FEATURES.md` beyond the single changed row/bullet.
- **Line endings:** LF preserved (`\r` search over the conf: 0 matches; `.gitattributes:12` is the canonical-style rule).
- **Warning-free:** no Rust logic changed, and the conf is schema-validated at compile time (`src-tauri/build.rs` → `generate_context!`), so a green `cargo build` proves the identifier is accepted. Both crates' `#![deny(warnings)]` are unaffected and the diff adds no `#[allow(...)]`.
- **No stale live references:** §3.

## LOW 1 — the evidence logs are not readable text, and the plan's recorded sweep claim about them is false

- **Where:** `.coding/analysis/mac-conf-warning.txt` (BEFORE, 4,296 B) and `.coding/analysis/mac-conf-warning-after.txt` (AFTER, 3,890 B) — both untracked, i.e. intended to ride the closing commit; cited at `.coding/plans/96fe545c.md:22` and `:26` (and `f67072a8.md:20`).
- **Observed:** `read_files` refuses both files ("stream did not contain valid UTF-8"). A literal one-character search for `e` scoped to exactly those two files returns **0 content matches** (only a filename match), while the identical one-character search over a control Markdown file returns 101 lines — so the repo's own search tooling cannot see inside them. The encoding is not UTF-8; it is consistent with PowerShell's default `>` / `Out-File` UTF-16LE redirect (the parent's grep evidently ran through `Select-String`, which auto-detects UTF-16, so the acceptance check passed there but is not reproducible with this project's own tools). Concretely: the whole-tree literal search for `com.mnemo.app` returns 16 matches in 8 files and **none** of them is under `.coding/analysis/`.
- **Why it is wrong:** both plan sentences record the post-sweep state as "…history dirs **plus the BEFORE evidence log**" / "…and `.coding/analysis/mac-conf-warning.txt` (the captured evidence)". As the tree stands that is inaccurate, and the inaccuracy matters here because (a) these two logs are the *only* proof for a fix that deliberately ships without a regression test — a future audit has to be able to re-derive it, and (b) `.gitattributes:12` (`* text=auto eol=lf`) classifies a UTF-16 file as binary, so the committed blob will be un-diffable and un-greppable by every tool this project uses, including the agent's own. The artifact outlives the session that made it.
- **Concrete fix (either is honest; I recommend the first):** re-encode both logs to UTF-8/LF before committing, e.g. from the project root
  `Get-Content .coding\analysis\mac-conf-warning.txt -Raw | Set-Content -Encoding utf8 .coding\analysis\mac-conf-warning.txt`
  and the same for `mac-conf-warning-after.txt`, then re-check that `ends with` hits **once** in the BEFORE log and **zero** times in the AFTER log — that turns the parent's own acceptance check into something the repo's tooling can reproduce in-tree, and it makes the plan's sentence true. Alternative: keep the encoding and correct `.coding/plans/96fe545c.md:22` and `:26` so they no longer claim a search hit that does not occur. (Committing the logs at all is fine by precedent: `.coding/analysis/*.txt` text logs and even the binary PNGs were committed with the 2027-01-13 fix, `aaec6b6`.)

## Explicitly not findings

- **No regression test for the rename.** Per the user directive of 2027-01-16 this is not expected — and it is the stated reason the plan is `kind=implementation` rather than `bug_fixing`. Judged on its merits the change is config-only and compile-validated, the behaviour it must not break is pinned by `src-tauri/tests/webview_udf.rs` + the `src/webview_args.rs` unit tests (neither of which the rename invalidates), and I found no correctness gap that the absence of a test leaves uncovered.
- **`.coding/knowledge/**`, `.coding/reviews/**`, `.coding/plans/**` still quote the old identifier.** Correct per this repo's history convention (the `cb19afcc` precedent) — these are records of how the tree got here, and rewriting them would falsify the trail.
- **The two fixture literals** (`src/webview_args.rs:243`, `:248`) now hardcode the current live identifier as an *input* to a pure helper that takes the base as a parameter. Coherence-only; a neutral base would decouple them from future config churn, but no assertion depends on the value, so this is not a defect.
- **The `Access is denied` tail in the AFTER log** — the running instance locking `target\release\mnemo-app.exe`, unrelated to the change, as briefed.

## Verdict rationale

Every axis that touches shipped code and documentation checks out: the value is valid and free of the flagged suffix, the conf is otherwise untouched with LF/JSON intact, the runtime path derives from the conf exactly as the plan claims (primary keeps the persistent default profile, additional instances get the per-pid folder, both untouched by the rename), the sweep leaves no live reference anywhere, and both platforms' build surfaces stay coherent. The single LOW is confined to the recorded evidence and the plan text — real, verifiable and trivially fixable, but with no impact on the shipped configuration. Fail-closed rules therefore put the verdict at `FINDINGS (0 high, 1 low)` rather than PASS.

**Report path note:** `write_review_report` stamped the file with the system clock's date, so the report on disk is `.coding/reviews/2026-09-17-macos-identifier-app-suffix-review.md` (the requested `2027-01-16-…` prefix was replaced). Pass that path to `finish`.
