## Verdict: PASS

Round-5 verified the three-change delta since round 4 (one new pin test, two tightened doc sentences) plus the closure of round-4 LOW 1, by trace against the working tree at `C:/Mnemo`, branch `wt/mnemo`, HEAD `630ec31`. Round-4 LOW 1 is genuinely CLOSED: the new pin is real and non-vacuous — the containment arm is the *only* reachable `Error::PathOutsideRoot` producer for its input, so deleting that arm fails the test on both Windows and macOS. The two tightened doc sentences match the code exactly. No new finding; one *inherited* comment clause is imprecise and is recorded (with the exact correction) under "Not findings" — no behavioural effect, the pin holds either way.

**Base:** the round-4 report's closures (round-3 HIGH 1 + LOW 2/3/4) and all of its standing checks are taken as the base for everything outside this delta; this round re-derived only the delta, not the 19-file diff. The code-graph index is stale for `sandbox.rs` (it still reports the deleted `is_link_free_as_spelled` at `:235`), so every claim below comes from the file text.

## Round-4 LOW 1 — the containment arm was deletable with a green suite → CLOSED, by trace

New test: `sandbox::tests::dir_creation_gate_refuses_an_out_of_root_dir_without_any_link`, `src/tool/agent/sandbox.rs:1719-1738`, inserted directly after `dir_creation_gate_allows_a_link_above_the_root` (`:1688-1716`). Subject: `validate_dir_creation` (`:203-232`). Fixture: `dir` and `outside` are two independent `tempdir()`s, `Sandbox::new(dir.path())`, `target = outside.path().join("missing/sub/skills")`, assertion `matches!(sandbox.validate_dir_creation(&target), Err(Error::PathOutsideRoot(_)))`.

**(a) The containment arm — and only it — produces the error (pass-side trace).**

| step | result for this input |
|---|---|
| `Sandbox::new(dir.path())` (`:82-97`) | `dir` exists → `canonicalize()` succeeds → a real, canonical root (verbatim `\\?\…` on Windows; `/private/var/…` on macOS) |
| `self.validate(target)` (`:113-144`) | `candidate.exists()` false → `parent` (`<outside>/missing/sub`).`exists()` false → returns `Err(Error::InvalidInput("path's parent directory does not exist…"))` at `:141-143`. **Not** `PathOutsideRoot`, so the retained-error arm (`:218`) cannot fire for this input |
| `canonical_existing_ancestor(target)` (`:718-727`) | `…/missing/sub` missing → `…/missing` missing → `<outside>` exists → `canonicalize().ok()` → the canonical `<outside>` path |
| containment (`:211-213`) | `<outside>` is a sibling tempdir, so `!ancestor.starts_with(&self.root)` → true → `Err(Error::PathOutsideRoot(ancestor))` |

`check_inside` (`:235-241`) is the only other `PathOutsideRoot` producer in the module, and it is unreachable here: it runs only on a canonicalized path that `validate` obtained, and `validate` returned at the missing-parent branch. So the assertion can be satisfied **only** by `:212` — the pin is exact.

**(b) With the containment arm deleted the call returns `Ok(dir)` on BOTH platforms — the variant assertion fails pre-fix.** `e` is `InvalidInput`, so `:218` does not fire; `refuse_if_protected(&ancestor)` (`:224` → `:591-596` → `is_protected_write_target` `:302-386`) returns `Ok(())`, because `strip_prefix(root)` fails for an out-of-root path (`:303` `unwrap_or(path)`) and the guard then exits `false` at `:349-351` (`comps.first()` is a `RootDir`/`Prefix` component, never `.coding`; the ADS trip at `:331` cannot fire — the last component is the tempdir name, not a `:`-bearing component); the function then falls to `Ok(dir.to_path_buf())` at `:229`. So a bare `is_err()` would *also* fail pre-fix, and the variant form still fails pre-fix — the required property holds, on Windows and macOS alike, for a platform-independent reason.

**(c) It cannot pass accidentally.** `Sandbox::new` succeeds (the tempdir exists); `target` genuinely does not exist and `missing` is never created by the test; nothing in the chain is a link, and no link can be involved in the refusal since neither the spelled-walk helper (`is_link_free_as_spelled` — deleted; plain-walk search over the tree: zero hits under `src/**`, the name survives only as history in `.coding/plans/8a95a233.md` and the round-3/round-4 reports) nor `link_fixture` is used by it.

## The two tightened doc sentences — accurate against the code

Sites: `docs/FEATURES.md:20` and `PLAN.md:298-301`. Both now read: *"… a planted symlink or hardlink at the target is refused, and a link at the skills dir itself is refused when it leaves the project or resolves into a protected tree"* (PLAN.md adds the shipped-skill clause that follows it). Clause by clause:

| clause | code it maps to |
|---|---|
| "a planted **symlink** … at the target is refused" | `src/tool/workflow/skill.rs:553-558` — `symlink_metadata(dir.join("<name>.toml"))` → `file_type().is_symlink()` → error "…is a symbolic link — refusing to write through it…". Deliberately stricter than the sandbox and BEFORE the overwrite question, so a DANGLING leaf is covered (`Path::exists` would read it as free). The write then routes through `validate_for_write` (`:589-592`). The name cannot carry traversal (`validate_skill_name`: `[a-z0-9_-]`, alnum-first, ≤64 bytes) |
| "… **or hardlink** at the target is refused" | the `.coding/**` fail-closed guard in `is_protected_write_target` (`src/tool/agent/sandbox.rs:349-367`): first component `.coding` ✓ for `.coding/skills/<name>.toml`, then `link_count > 1` → protected → refused by the ladder (`:520` step 2b / `:528` step 3) and by the gate's `refuse_if_protected` |
| "a link at the skills dir itself is refused **when it leaves the project**" | containment arm, `validate_dir_creation` `:211-213` (e.g. `.coding` planted as a link to an outside dir → the canonical ancestor is not under the root) |
| "**or resolves into a protected tree**" | the `Ok` arm's `refuse_if_protected`, `:205-207` (e.g. `.coding/skills` → `.coding/knowledge`) — and, for the `Err` arm's in-root case, `:224` |

The deliberate exception (an in-root **unprotected** dir link is followed — the `Ok` arm returns the canonical path; the library reads through the same spelling, so refusing would make such a config un-authorable) is precisely what round-4's "Not findings 1" asked to scope; the tightened wording no longer claims a blanket refusal, and the code is unchanged in that respect (round-2/3 `Ok` arm identical).

**Sweep for stale/over-broad wording that names the write policy.** Literal search "sandbox-mediated": 8 hits / 6 files — `PLAN.md:298` and `docs/FEATURES.md:20` (the tightened pair), `src/tool/workflow/skill.rs:432` (the model-facing tool description: "…a planted symlink or hardlink at the target is refused, and a skill shipped with the app (e.g. merge_to_main) is never rewritten…" — a strict *subset* of the claims, no dir-gate clause, therefore not stale), the SPEC record's round-1 amendment, and the round-1/round-2 review reports (historical records, not docs). The SPEC gist (`.coding/knowledge/spec/2027-01-11-skill-reload-skill-create-over-a-reloadable-skil.md:6`) carries the pre-mkdir clause — "pre-validates its mkdir with `Sandbox::validate_dir_creation` (round-3 fix)" — and its end amendment (`:10`) describes the ancestor judgement; both are accurate, and neither asserts the linked-dir policy, so nothing there is over-broad or stale. No other prose names the policy.

## Spot checks

- **Changed-file set — clean.** `git diff HEAD --stat`: **19 files, 1492 insertions(+), 83 deletions(-)** — the identical 19-file set round 4 reviewed (round 4 counted 1469 insertions; +23 is consistent with the new test's ~21 lines plus the small net growth of the two doc sentences, ≈ +2), and **no file outside it**. The bookkeeping members (`.coding/backlog.jsonl`, `.coding/knowledge/bug/b4812291.md`, the two SPEC records, `.coding/plans/b4812291.md`) are the close-out edits for plans `ecdd67ac` / `b4812291` / `60f02c26` that round 4 already analysed, not new work.
- **Reviewer tool surface — unchanged.** The only change in `src-tauri/src/ipc/spawn.rs` is `+ "skill_create", + "skill_reload"` in the must-not-appear assertion list (`:1050-1054`); `REVIEWER_BASE_TOOLS` is untouched.
- **`validate_for_write` — untouched by this delta.** Read directly (`src/tool/agent/sandbox.rs:497-585`): the five-step ladder (link-free creation gate → canonical-ancestor protection check → protected-target refusal before mutation → parent creation → canonical re-check with the one re-checked lexical fallback for the just-created-parent case) is intact and complete, and the only round-5 addition in this file lives in the test module (`:1718-1738`).
- **Test evidence (parent-run, not re-run here).** `cargo test --lib dir_creation_gate -- --nocapture` → 3 passed with no `SKIP:` lines (so `dir_creation_gate_refuses_a_link_component`, `dir_creation_gate_allows_a_link_above_the_root` and the new pin all genuinely executed); full root `cargo test` → 2402 passed / 0 failed / 5 ignored + 16 integration; both crates are `#![deny(warnings)]`.

## Not findings — recorded for the maintainer

1. **The new test's comment mis-states the macOS pre-fix mechanism** (`src/tool/agent/sandbox.rs:1724-1725`: *"pre-fix this returned `Ok(dir)` on Windows and the spelled walk's link refusal on macOS, so only the error kind fails pre-fix on both platforms"*). No revision of this function behaves that way: `validate_dir_creation`'s `Err` path touches only `validate`, `canonical_existing_ancestor` and `refuse_if_protected` — none contains a component walk, and `is_link_free_as_spelled` is deleted (plain-walk search: zero hits under `src/**`) — so with the containment arm removed the call returns `Ok(dir)` on macOS as well, for the same platform-independent reason as Windows (`validate` reports the missing parent as `InvalidInput`, so the retained-error arm `:218` cannot fire). Not raised as a finding: the claim's *conclusion* is correct — the variant assertion does fail pre-fix on both platforms, and it is the stronger pin regardless (it rules out a pass through the retained-error or protection arm). One-line fix if exactness is wanted: replace the two comment lines with "with the containment arm deleted this returns `Ok(dir)` on both platforms — `validate` reports the missing parent as `InvalidInput`, so only `:212` can produce `PathOutsideRoot`".
2. **The code-graph index is still stale for `sandbox.rs`** (round-4 §not-finding 4, reconfirmed live): `graph_search("is_link_free_as_spelled")` answers with `sandbox.rs:235-246` as a `validate_dir_creation` callee — the pre-fix revision. Harmless (a gitignored, rebuildable cache), but no graph answer for that file should be trusted until the next rebuild.

## Limitation of this pass

Read-only reviewer: no shell, nothing modified, no test run — the suite numbers above are the parent's, and my own contribution is the traces. The full-diff body was truncated by the read tool after roughly the first eight files, so the hunk-level attributions for the later files rest on round 4's byte-level verification plus my direct reads of every region this delta touches (`sandbox.rs:203-232`, `:497-585`, `:1718-1738`, `skill.rs:528-615`, `PLAN.md:292-303`, `docs/FEATURES.md:20`, `spawn.rs` assertion list) rather than on a fresh hunk listing of `src/tool/mod.rs` / `src/tool/workflow/skill.rs`. Nothing in the stat or in the readable portion of the diff contradicts round 4's analysis.

**Bottom line:** the delta is exactly what it claims to be (one pin test, two doc sentences), the pin genuinely closes round-4 LOW 1 on both platforms, and the tightened sentences are exact. Verdict stands: PASS, no findings.
