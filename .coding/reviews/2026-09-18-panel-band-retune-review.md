## Verdict: FINDINGS (0 high, 2 low)

Review of plan 4347c8a3's uncommitted changeset on `wt/mnemo` (4 source/test files + knowledge-record amendments + plan file; the 5 superseded-marker edits and 3 unrelated -2.md successors are pre-existing memory bookkeeping, verified well-formed).

The retune is correct and complete: the band arithmetic (75% flat cap, 360px chat floor, 1440px crossover) checks out at every worked viewport, the constants remain single-source (JS clamp, inline CSS caps, and the drag path all route through `PANEL_MAX_FRAC`/`CHAT_MIN_PX` in appearance.ts), the regression tests are genuinely red against the old 0.5/480 constants, docs carry no stale band mentions, and the knowledge-record amendments are coherent with intact supersede chains. Both findings are cosmetic wording defects in the new test block's titles — no code behavior is affected; one edit fixes both.

### Verified

1. **Band arithmetic + crossover.** `clampPanelFraction` (appearance.ts:270-275) is unchanged in logic — only the constants and doc comments moved. Crossover: (w−360)/w = 0.75 ⟺ w = 360/0.25 = 1440. Worked viewports recomputed by hand: w=800 → max 0.55 (chat floor binds), w=1000 → 0.64, w=1280 → 0.71875, w=1440 → 0.75 (ceilings equal), w=1920 → 0.75 (flat cap binds; (1920−360)/1920 = 0.8125 > 0.75), w=2000 → 0.75. The chat column keeps ≥ 360px window-relative at every viewport (below 1440 via the calc cap, at/above via the 25% share). All doc comments (appearance.ts ×3, RightPanel.tsx:30-31, test header :19) state the new numbers and the crossover consistently.
2. **Single source of truth (prior round-1 L1 invariant).** grep `PANEL_MAX_FRAC`/`CHAT_MIN_PX`: source hits only in appearance.ts (definition), RightPanel.tsx (import + interpolation :46-48 — code unchanged, the emitted style follows automatically), and the two test files. The drag path routes through `clampPanelFraction` at App.tsx:861 (start width) and :868-869 (`fracFromPx`, live + commit). The `min(pct%, …%, calc(100% - …px))` inline-cap shape is unchanged. `PANEL_MAX_FRAC * 100` = 75 exactly (no float artifact).
3. **Regression tests are red against the old constants.** (a) New "lets a hand drag exceed the old 50% ceiling": old constants give clamp(0.9, 1920) = 0.5 and `expect(got).toBeGreaterThan(0.5)` fails (0.5 ≯ 0.5) — the reported symptom pinned. (b) Updated literals `toContain("75%")` / `toContain("calc(100% - 360px)")` fail against the old "50%"/"480px" style. (c) appearance.test.ts: 831/800 → 0.55 (old 0.4), 0.9@2000 → 0.75 (old 0.5), legacy seed → 0.55 (old 0.4). The crossover test pins the relationship (tautological in the constants by design — documented as such). No stale band assertions remain in appearance.test.ts (the untouched 0.4@1000 in-band and 0.05@1000 → 0.3 floor tests are still correct under the new band).
4. **Docs sync.** README.md: no panel/480/50% band mentions. PLAN.md: right-panel mentions are view-registry/phase prose, none reference the width band. docs/**: nothing stale. The only remaining 0.5/480 prose lives in .coding knowledge/plans/reviews records, where it is history (the -1 records are marked superseded with -2 successors recording the retune).
5. **Knowledge records.** Both -2 successor files carry coherent dated amendments recording the retune (constants, crossover, worked maxima, regression test names, verification results); frontmatter and supersede chains intact. The three pre-existing -2 files (shell-preview ×2, window-clamp) and the five `status = "superseded"` additions are well-formed memory-system bookkeeping — not corrupt, not this plan's scope.
6. **Multi-platform neutrality.** Pure TS constants + comments; no platform APIs, paths, or shell syntax.
7. **Bug-plan checks.** Regression test present and meaningful (item 3); root cause documented in the plan context (ROOT CAUSE + CHOSEN FIX + worked numbers) and in the BUG record chain (0fe3fcfb successor amended: symptom → root cause → fix + regression test names); the BUG digest is auto-captured at finish per the bug_fixing gate.

### Findings

**L1 (low) — the new crossover test's title states the binding order inverted.** RightPanel.width.test.tsx:119: `it("crosses over at 1440px — flat cap below, chat floor above", ...)`. Below 1440 the CHAT FLOOR governs and at/above it the FLAT CAP does — as the test's own inner comment correctly says ("Above it the flat cap binds; below it the chat floor does", :121-122) and as every doc comment and the knowledge amendment state. The assertions are correct; only the title is backwards, which misleads exactly the future reader the test exists to protect. Fix: swap to "chat floor below, flat cap above".

**L2 (low) — mismatched paren in the new describe title.** RightPanel.width.test.tsx:101: `describe("clampPanelFraction — the panel band (user report 2027-01-16)")` — opens `(`, closes `)`. Trivial; fix alongside L1.

### Notes (no action required)

- **CHAT_MIN_PX is window-relative, not column-relative** (pre-existing convention, documented in the prior plan's round-2 review): at the max drag on an 800px window the chat column's actual box is ~298px (800 − 56 sidebar − 6 handle − 440 panel), not 360px. The plan's "chat 360px" worked number follows the established window-relative convention; the tighter squeeze is the tradeoff the user explicitly chose (option D).
- **App.tsx:862 `?? 480`** — a pre-existing DOM-measurement fallback for the no-explicit-width drag start, not a band constant; whatever it produces is clamped by `fracFromPx`. Correctly out of scope.
- **"Amended 2027-01-11" heading dates** on the amendment paragraphs are a memory_amend artifact (the tool dates the heading; identical in the prior, already-merged amendment); the content dates (2027-01-16) are correct. Not corrupt.
- Tests were reported green by the parent (vitest 1221, tsc clean, cargo test 2410+16); this reviewer has no shell access and verified by static analysis — every changed assertion was recomputed by hand against both old and new constants.
