## Verdict: FINDINGS (0 high, 1 low)

Round-2 review (verifying round-1 fixes) of ALL uncommitted changes on the working branch (git diff HEAD + untracked files under .coding/) for bug_fixing plan 995436c2 ("Stop emitting the visible error event for the harness tool-call correction") plus its user-directed follow-on (backlog 3e6f7887, reviewer-spawn null-model artifact). All three round-1 findings are verifiably fixed; both code fixes remain correct with true regression tests. The fresh pass found one low bookkeeping finding: the backlog item whose fix IS this diff still sits in the queue as pending.

### Round-1 fix verification

**L1 (silent drop of a model literally named "null") — FIXED.** src/tool/agent/spawn_agent.rs:250-272: the note fires only for the trimmed literal "null". The guard arm (`!model_id.is_empty() && model_id != "null"`) takes every non-empty non-"null" id into resolution, so the fallback arm (`Some(model_id) => { null_model_note = model_id == "null"; None }`) sees only "" or "null" — empty/whitespace stays silently unset (documented at :265-266 as long-standing semantics), and JSON null deserializes to `None` in its own arm (no note). The note is appended to the success output only (:365-372) and covers BOTH paths — normal spawns and reviewer spawns via dispatch — since it lives in the tool's `execute`. Padded " null " trims to "null" and is handled identically at the gate and the tool (both trim-then-compare). `null_model_string_counts_as_unset` (:456-489) asserts the note for the string form and its absence for JSON null — exactly the "note in output" hardening round 1 suggested.

**L2 (missing BUG: memory records) — FIXED.** Replicated round-1's exact check (memory_search, semantic, record_type=bug): both records now surface — "harness tool-call correction surfaced as a visible error event — FIXED (plan 995436c2)" (4347bfa8) and "reviewer-spawn gate denied the stringified 'null' model artifact — FIXED (backlog 3e6f7887)" (c35b4cfe). Both are backed by well-formed knowledge files (.coding/knowledge/bug/2027-01-11-*.md, untracked, in the diff) carrying symptom → root cause → fix + regression-test names + plan pointer, with the correct test names (`repeat_failure_injects_schema_correction_on_second_identical_error`; `dispatch_treats_null_model_as_absent_for_reviewer_spawns` + `null_model_string_counts_as_unset`).

**L3 (incident-count discrepancy) — FIXED.** EIGHT is canonical everywhere it is asserted: dispatch.rs:1406 ("eight identical reviewer-spawn refusals"), tests.rs:5370 ("eight identical refusals"), the plan amendment (.coding/plans/995436c2.md:14, "EIGHT ... backlog 3e6f7887 is canonical"), the backlog item itself ("8 consecutive identical spawn_agent refusals"), and both BUG records. The stale "9" survives only inside the plan's immutable follow-on paragraph (:12), explicitly corrected by the amendment directly below it — the sanctioned append-only erratum form.

### Fresh pass

- **turn.rs:1644-1667** — context injection retained (`messages.push` at :1657), the `AgentEvent::Error` emission removed, rationale comment in place (:1658-1665). No dead code: `fanin_tx`/`agent_id` remain in use (:1591 ToolResult send, :1612 emit_workflow_state, :1622 synthesize_not_run_results) and `tool_name` feeds the correction builder (:1656) — no unused-variable risk under `deny(warnings)`.
- **No stranded consumer** — repo-wide "tool-call correction" search: remaining hits are knowledge/plan/review docs, the inverted test assertion, and the unchanged `HARNESS_CORRECTION_PREFIX` const (turn.rs:110, still used by the builder). No frontend special-casing, no code path reads the removed event.
- **Gate parity end-to-end** — the dispatch caller (dispatch.rs:405-406) extracts model via `as_str()` (JSON null → None); the gate trims then compares (`m.trim() != "null"`, :1409-1411), matching the tool's trim-then-compare — " null ", "null", JSON null, and "" all count as absent at both sites. graph_context confirms `reviewer_spawn_gate`'s only production caller is `execute_tool_call` (plus its 4 unit tests) — no other call site to update.
- **Schema** — `"type": ["string", "null"]` (spawn_agent.rs:177) with the description "omit or pass null" is the exact compact form the landed strict normalization produces, so it is strict-legal and idempotent if spawn_agent ever joins STRICT_TOOLS (not a member today).
- **Tests are true pins** — the inverted breaker test (tests.rs:1561-1575) fails without the turn.rs fix (the emitted event would trip the assert in the drain loop) while the message-content assertions are retained (:1520-1560); the tool-level null test fails without the spawn_agent fix (the no-resolver fixture previously errored "model selection unavailable" on "null"); the dispatch test (:5366-5425) pins both null shapes through `execute_tool_call`.
- **Constitution** — doc comments thorough at every changed site; no warning risks by inspection (consistent with the reported green 2447+16, warning-free under `deny(warnings)`); multi-platform neutral (no platform APIs, no paths); no shell-based mutation (the knowledge-file changes carry the memory_supersede shape: status line + successor file); documentation sync clean — the in-code schema description is the doc surface for the model param and is updated, and no README/PLAN.md/docs reference the removed error event or the old non-nullable param shape.
- **Spec supersede chain** — `-landed.md` marked `status = "superseded"` (:5), successor `-landed-2.md` records the e37d07a merge (branch wt/mnemo deleted, pre-merge tip 8b454b0): coherent, pointer-first, a proper supersede (the merge-status fact obsoletes the "landed on branch" one).
- **Backlog additions** — the five other new items (838b6f4e, 26cdbaf8, 3f838ea1, 79a2755d, 37f8631a) are legitimately-open future work with fix directions + acceptance criteria; well-formed JSONL.

### Findings

**L1 (low) — backlog item 3e6f7887 remains queued as pending although this diff is its fix.** The item's acceptance criteria are met by this change set (nullable model schema; null-as-unset at gate + tool; regression tests at both levels; cargo test green + warning-free). The convention for fixed items in this very file (fc812ffe, 6f25fb7e) is status "done" + plan_id + soft-delete; 3e6f7887 still reads status "pending", note null, no plan_id. Committed as-is, the Backlog tab offers already-fixed work to a future session (only the BUG memory saying "FIXED" mitigates). Close/link it (status done, plan_id 995436c2) when committing — or, if it is deliberately kept open to track the un-landed hardening from its fix direction (option 2: stop auto-filling omitted optionals with null; option 3: sharpened denial text; option 4: stuck-spawn ask_user escape), record that in the item's note so the open state is deliberate rather than stale.

### Notes (no action required)

- The dispatch-level null test asserts gate non-denial only (negative assertion) — carried from round 1: matches the sibling fixture pattern (`dispatch_denies_ad_hoc_reviewer_model_pick`), and the tool-level test pins actual spawn success. Still acceptable.
- backlog.jsonl: the 6f25fb7e item's deleted_at moved (1789732784 → 1789805369) with status "done" on both sides — harmless side-car churn, no semantic change.
- The plan file's "Detailed steps" checkboxes are unchecked while the locked skeleton is 4/4 [x] and the workflow reports 4/4 complete — the skeleton is the tracked step list for bug_fixing plans; the Detailed steps persist the create_plan-time intent. Normal shape, not staleness.
- The stale "9" in the plan's follow-on paragraph (see L3 verification) is corrected in place by the amendment directly below it; no further action is possible or needed under the append-only plan-context model.
