## Verdict: FINDINGS (0 high, 1 low)

Round-2 review of ALL uncommitted changes for plan 29baa080 (bug_fixing, backlog 41cd5ad0, branch wt/mnemo). All three round-1 findings are correctly fixed and verified in the code; the allowlist additions remain correct and safe; the regression test exercises the changed path end-to-end and provably fails pre-fix. One LOW bookkeeping finding: the plan's checked-off step 2 (manual BUG memory record) has no corresponding record in the memory store — likely lost to the aborted turn's 3 consecutive tool errors.


## Scope reviewed

- `git diff HEAD` (3 files: src/tool/agent/git.rs, .coding/backlog.jsonl item 41cd5ad0 pending→done, HOW knowledge file +2 lines) plus untracked `.coding/plans/29baa080.md` and the round-1 report `.coding/reviews/2026-09-19-git-branch-show-current-review.md`.
- Code read in context: `validate_branch_list_args` + `branch_list_argv` (git.rs:259-336), the spawn path `run_git_impl` (git.rs:395-452 — `Command::new("git").args(...)`, no shell), the schema `args` description (git.rs:507-511), the `GitArgs.args` doc (git.rs:99-104), the module doc (git.rs:21-28), the test module (git.rs:1437-1542), the full HOW diff, the plan file, the round-1 report, and the backlog item.
- Repo-wide literal searches for `--show-current` and the rejection-message text: no other surface enumerates the branch-list flags — schema, module doc, `GitArgs` doc, and README carry no enumeration (correctly untouched); the only other occurrences of the old message are historical records (plan 3f3a188c's original spec and the backlog item's problem statement, both correctly left as-is).

## Round-1 findings — all three verified fixed

1. **Message omitted `-q`/`--quiet` → FIXED.** The rejection message (git.rs:317-320) now names `-q/--quiet`. Cross-checked the full enumeration against both allowlists: every `safe_exact` family (-v/-vv/--verbose, -a/--all, -r/--remotes, --list, --show-current, -q/--quiet, --no-color, --merged/--no-merged/--contains/--no-contains, -i/--ignore-case) and every `safe_prefix` entry (--points-at=, --format=, --sort=, --color=) is named — the sync is now complete, not just patched.
2. **Message-sync assertion missing → FIXED.** `branch_list_allowlist_accepts_show_current` (git.rs:1521-1527) asserts the `--bogus` refusal message contains `--show-current`, `--points-at=`, and `-q/--quiet`, pinning the enumeration to the allowlist. Placement note: the assertions live in the NEW regression test, not in `branch_list_allowlist_rejects_positionals_and_write_flags` (which still asserts only `contains("not allowed")`, git.rs:1466) — exactly the alternative placement round-1's finding sanctioned ("in ...rejects... (or the new regression test)"). The requirement is met; the task brief's scope description named the other test, but the substance is verified present.
3. **HOW amendment dated 2027-01-11 → FIXED.** The amendment now reads "Amended 2027-01-24:" — distinguishable from the prior 2027-01-11 amendment and consistent with the backlog note ("Done via plan 29baa080 (2027-01-24)") and the plan records.

## Core checks (round-2 independent verification)

- **Allowlist safety — PASS.** `--show-current` is exact-match only (git.rs:294) and purely read-only (prints the checked-out branch's name). `--points-at=` is prefix-matched (git.rs:305) with the value riding inside the single argv element; the bare two-arg form `--points-at HEAD` is doubly refused (`--points-at` matches no exact or prefix entry — every prefix ends in `=` — and `HEAD` fails the positional check at git.rs:307). Git is spawned via `Command::new("git").args(args).current_dir(...)` (git.rs:406-407) — discrete argv, no shell — so even a hostile value like `--points-at=--delete` stays one element that git parses as the option's value (a read-only "malformed object name" at worst). Same trust model as the pre-existing `--format=`/`--sort=`/`--color=` prefixes. Neither flag mutates refs, the index, or config.
- **Regression test — PASS.** `branch_list_allowlist_accepts_show_current` (git.rs:1507-1528) exercises the changed path end-to-end: validate passes AND `branch_list_argv == ["branch", "--show-current"]` (the flag provably reaches git). Pre-fix both fail (validate returns Err → `assert!(is_ok())` fails; `branch_list_argv` returns Err → `.unwrap()` panics), matching the plan's confirmed-failing-pre-fix record. `branch_list_allowlist_accepts_read_only_flags` extended with `--show-current` in the exact loop (git.rs:1482) and `--points-at=HEAD` in the prefix asserts (git.rs:1502).
- **Mutating path still pinned — PASS.** `branch_list_allowlist_rejects_positionals_and_write_flags` (git.rs:1443-1468) unchanged: positional + `--set-upstream-to=`/`--unset-upstream`/`--edit-description`/`-d`/`-m`/`--delete`/`--move`/`--force` all refused; `args_rejected_on_write_subcommands` still pins branch create with args.
- **Surfaces in sync — PASS.** Doc comment Allowed list (git.rs:274-283) covers both new flags plus the audited exclusions (`--column`/`--no-column`/`--omit-empty` display-only; `-l` deprecated synonym of `--list`); rejection message complete (above); HOW amendment content matches the code exactly; backlog note accurate. Schema/module-doc/`GitArgs`-doc/README carry no flag enumeration — correctly untouched.
- **Constitution — PASS.** Pure Rust string matching — multi-platform neutral (the only `cfg(windows)` in the file is the pre-existing CREATE_NO_WINDOW spawn guard). No public signatures changed; the private validator's doc comment is updated in place. Code style matches the file (array literals, `\`-continued `format!` strings, test naming). No shell-based file mutation: src via file tools, knowledge file via memory tools, backlog via backlog tools. Reported 2480 passed / 0 failed / 5 ignored / warning-free is consistent with the diff (no new imports, no dead code, no `#[allow]`).

## Findings

### Finding 1 (LOW) — plan step 2's BUG memory record is absent despite the checked-off step

Plan 29baa080's step 2 is checked off: "Document root cause — memory_write a BUG: record (symptom → root cause → fix + regression test name, ≤600 chars)". No such record exists in the memory store. Three targeted searches all return nothing for this fix: (1) `record_type:"bug"` with a semantic query naming the symptom, (2) title prefix `BUG: git branch` (zero matches), and (3) a general semantic query naming the flags — the closest hits are the round-1 review's own session summary (episodic), the HOW record (fc736d96), and unrelated plans. Most likely the write was lost to the aborted turn (the backlog note's harness suffix records "3 consecutive tool errors"). The root cause itself IS documented — in the plan's Bug section, the backlog item, and the validator's doc comment — only the typed memory record is missing. Fix before finish: `memory_write` the BUG record (symptom → root cause → fix + regression test name `branch_list_allowlist_accepts_show_current`), or verify the finish gate's auto-captured BUG digest will carry that content and treat the step-2 check-off as referring to the documentation above. One memory_write closes it.

## Observations (not findings)

- The backlog item's note carries a harness-appended suffix ("| Aborting turn: 3 consecutive tool errors (the model may be stuck). — item left in flight; work kept in tree; run waits for the resumed turn") that reads as contradicting the done status. It is app-harness text from the aborted turn (the same "|" suffix pattern as other items' harness annotations), not agent-authored; the `status` field ("done") and the agent-authored note are accurate, and this review IS the resumed turn it mentions. No action required or possible via the backlog tools — noted so the parent isn't surprised by it in the committed file.

## Conclusion

The fix is correct, safe, and fully verified: both new flags are genuinely read-only, the prefix form cannot smuggle a positional or inject a flag (discrete argv, no shell), the mutating path stays pinned, and the regression test fails pre-fix / passes post-fix on the real code path. All three round-1 findings are properly landed. The single LOW finding is a bookkeeping gap (the missing BUG memory record) closable with one memory_write before finish. Fix it, then commit (including both review reports) and finish.
