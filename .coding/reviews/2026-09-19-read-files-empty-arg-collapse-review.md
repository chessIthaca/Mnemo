## Verdict: FINDINGS (0 high, 1 low)

Plan 9e0b266a (backlog 26cdbaf8) — the read_files schema collapse is coherent and correctly landed; one low-severity residual: a model-facing error in `src/agent/dispatch.rs` still teaches the unadvertised `path`-shorthand call shape.

**Scope reviewed:** `git diff HEAD` — `src/tool/agent/read_files.rs` (+112/−13: schema collapse, error-path hint, updated absorption comment, 2 new tests) and `.coding/backlog.jsonl` (item 26cdbaf8 pending→done with a comprehensive note) — plus the untracked `.coding/plans/9e0b266a.md`. Supporting surfaces read for cross-checks: `src/agent/dispatch.rs` (read_files_paths, file_edit_redirect, steering call site), `src/tool/error_message.rs` (sanitize_arguments_error), `src/provider/strict.rs` (STRICT_TOOLS), `src/agent/factory.rs` (budget ceilings), `src/agent/prompt.rs`, `README.md`, `PLAN.md`, and a repo-wide "shorthand"/"read_files" text sweep.

## Verification per the requested checks

**(a) Schema collapse coherence — CLEAN.** `required: ["files"]` with the single `files` property; items keeps `required: ["path"]`. No dangling references to the removed top-level params anywhere in the advertised surface: the description's "start_line + max_lines read only the relevant slice" refers to the per-spec fields (still advertised inside `items`), and "A directory path returns a sorted listing" refers to a spec's path being a directory — both accurate for the files form. The `files` property description dropped "Omit when using the single-file `path` shorthand." The inline example `{"files":[{"path":"a.js","start_line":10,"max_lines":40}]}` matches the schema exactly (verified against the escaped Rust literal and the test's `contains` assertion). `read_files` is NOT in `STRICT_TOOLS` (strict.rs:250-260 — read-only tools excluded by design), so the advertised schema is the wire schema on every endpoint; there is no strict-normalized second surface to update. The budget ceilings (factory.rs: Executing 32_300 / PlanFrozen 33_600) were NOT raised in this diff — consistent with the reported net ~-70-char shrink passing the budget test.

**(b) Error-path hint composition — CLEAN.** `invalid_args_error` composes `{sanitized} (received keys: {keys}). {hint}`; for the empty call the model sees: "Error: The tool 'read_files' failed because parameter 'files' is required but was not provided. Please try again with 'files' included. (received keys: none). Always pass files (an array of {path, start_line?, max_lines?} specs) — there is no zero-argument form; rewrite the full call, do not resend the empty shape." For non-empty arg errors (e.g. `files: "str"` → type error): the sanitized base names the type problem and the received-keys context names what was sent; the hint's "do not resend the empty shape" clause is not literally applicable there, but every actionable clause ("Always pass files (an array of specs)", "rewrite the full call") points at the correct retry — no wrong action is suggested, so it cannot mislead into a bad retry. Considered, acceptable.

**(c) Unadvertised absorption — INTACT.** The execute() lifting is kept verbatim (read_files.rs:212-224) with the comment updated to document the unadvertised status. `read_files_paths` (dispatch.rs:1284-1297) is unchanged and parses BOTH forms; the steering call site (dispatch.rs:1275) is unchanged. Existing tests pin the compat and are untouched by the diff: `top_level_path_is_read_as_a_single_file` (lift + slice args ride along + files-array wins), `directory_path_returns_listing`, `directory_listing_caps_entries`, `big_indexed_rust_file_whole_read_nudges_with_symbol_count` (uses `{"path": "big.rs"}`), and `read_files_paths_extracts_single_and_batch_paths` (dispatch.rs:1607). The acceptance criteria's conditional ("if the path shorthand is removed, update read_files_paths + its tests") correctly does not trigger.

**(d) Model-facing references to the shorthand — ONE LOW FINDING (below).** Otherwise clean: README.md has zero `read_files` mentions; PLAN.md's two mentions are incidental (neither documents input forms); prompt.rs:42 ("read several files at once with read_files") teaches no call shape; the module doc (read_files.rs:5-16) describes the files-array form only; a repo-wide "shorthand" sweep over *.rs finds only read_files.rs compat comments plus an unrelated console.rs model-shorthand. Frontend toolCardPaths.ts / Message.tsx argPaths/argLabel handle the shorthand for DISPLAY — display-side compat, consistent with keeping the absorption, not schema. Historical .coding/knowledge + .coding/plans records mention the shorthand — accurate historical records, not model-facing docs.

**(e) Multi-platform neutrality / file-tools-first / documentation sync — CLEAN.** Pure schema/description/test-text change; no platform-specific code, paths, or shell syntax. No shell-based file mutation in the diff. Documentation: the schema description is the model-facing doc and was rewritten; the backlog item was closed with a comprehensive note (plan id + title, decisions, test counts); the plan file matches the executed work step-for-step (all 4 steps verified against the code). No README/PLAN updates required — no read_files documentation exists there.

**(f) General correctness / bugs / security — CLEAN.** The Rust string continuations join without double spaces; the example literal is intact. The two new tests are proper regression pins; the `contains("files")` assertion in `empty_call_error_carries_the_recovery_hint` is weakly satisfiable by the hint alone, but the sanitized missing-field branch independently names parameter 'files', so the pin holds for the right reason. Sandbox validation, per-file error isolation, output caps, and the path-traversal test are all unchanged. No new attack surface. Reported suite: 2479 passed / 0 failed / 5 ignored, warning-free under `#![deny(warnings)]` — not re-run by this reviewer (read-only), but the change surface is small, self-consistent, and nothing outside read_files.rs + backlog.jsonl was touched, so no cross-module test could see a different schema.

## Findings

**LOW-1 — dispatch.rs:1323: the EDIT INTERCEPTED error still teaches the unadvertised shorthand call shape.**
`file_edit_redirect`'s interception message tells the model: `First re-read the file (read_files path="{path}"), then retry the edit with the exact current text.` — spelling out the `path`-shorthand call form the advertised schema no longer offers. Not a correctness bug: the execute() absorption keeps the habit-shaped call working, so a model following this instruction succeeds. But it is the one residual model-facing surface steering toward the collapsed-away form — working against the plan's goal of converging the model on the files-array shape (and, given the observed adjacency-steering failure mode, re-teaching the dual-form habit is exactly what this plan removes everywhere else). The sibling stale-read nudges (dispatch.rs:1559/1605/1643, steering_stats.rs:946) name the tool only ("Re-read the file (read_files, this exact path)") — fine. No test pins the `read_files path=` substring (literal search: exactly 1 match, the message itself), so rewording is low-risk: drop the call shape ("First re-read the file with read_files, then retry…") or use the files-array shape. Fix, or justify leaving it (absorption covers it).

## Considered and dismissed (no action needed)

- The empty-array error (`{"files": []}`) does not carry the recovery hint — its message ("files array is empty — provide at least one file spec") is self-contained and actionable; the observed failure mode was the empty OBJECT, which does carry the hint.
- The error-path hint omits "up to 10" (the description has it) — cosmetic.
- The description's "'files is required' error" phrasing vs the actual sanitized "parameter 'files' is required but was not provided" — close enough for the model to connect the rule to the error it sees.
