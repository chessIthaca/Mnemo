## Verdict: FINDINGS (0 high, 2 low)

Source change is correct, minimal, and consistent with the browserArgLabel precedent; regression coverage and root-cause documentation satisfy the bug-plan checks. The 2 findings are bookkeeping/hygiene in the wider uncommitted set (a stray build artifact, a silently dropped backlog line), not the fix itself.


### Reviewed
All uncommitted changes on wt/agenticcoder vs HEAD (ff7a633): `frontend/src/lib/toolCardPaths.ts` (new `webFetchLabel`), `frontend/src/components/chat/Message.tsx` (argLabel dispatch + import), `frontend/src/lib/toolCardPaths.test.ts` (new describe block), plus the `.coding` bookkeeping riding in the working tree (backlog.jsonl, decision-doc supersede, new bug/plan/decision files, untracked `frontend/tsconfig.tsbuildinfo`). Read against the browserArgLabel precedent (toolCardPaths.ts:399-432), argLabel (Message.tsx:438-616), ToolCard chip loop (Message.tsx:666-676), argPaths (toolCardPaths.ts:51-80), the bug knowledge file, plan 2c6c78e0, and git history.

### Findings

**LOW 1 — `frontend/tsconfig.tsbuildinfo` is untracked and NOT gitignored; it must not land in the commit.**
`git status --short` shows `?? frontend/tsconfig.tsbuildinfo` — a `tsc -b` incremental artifact. `.gitignore` (read in full) has no `*.tsbuildinfo` entry, so a blanket `git add` at the commit step would sweep it in (machine-specific paths, pure churn, guaranteed merge noise later). Resolution: add `*.tsbuildinfo` to .gitignore and exclude the file from the commit.

**LOW 2 — backlog.jsonl silently DROPS item 323036fd (deleted line, not a status change); verify it's intentional.**
The diff removes the entire line for 323036fd ("backlog is marked failed because the model was steered… status:failed, provider-error note") rather than requeuing/annotating it, while 69cf7e9c pending→in_flight is the expected in-flight stamp for THIS plan. 323036fd was the source request for the just-merged steer/interrupt requeue work (ff7a633), so a cleanup is plausible — but (a) the new intervention semantics (decision doc, plan 0a58db61) say requeue/annotate, never delete, and (b) backlog.jsonl merges via the git **union** driver: a deletion on this branch will NOT propagate — main still carries the line and the union merge will resurrect it, leaving the worktree silently diverged. If the deletion was deliberate (e.g. user removed it in the Backlog UI), note that it cannot stick across merges; if not, restore the line. The main agent should confirm intent and either restore it or record that the line will reappear on merge.

### Bug-plan checks
- **(a) Regression test exercises the changed path — YES.** Three tests call `webFetchLabel` directly (happy path, 60-char truncation — the fixture URL is 71 chars, so the `> 60` branch is genuinely taken — and the null family: missing url, blank url, malformed JSON, `browser_navigate`, other tool). A fourth source-contract test asserts Message.tsx contains `webFetchLabel(toolName ?? "", args)`, pinning the dispatch that actually renders the chip — the same readFileSync/source-contract pattern already used in this file (node-env vitest can't import the .tsx). Reported RED→GREEN (4 failures pre-fix) is consistent: the import alone fails without the helper.
- **(b) Root cause documented — YES.** BUG memory 3c57404d + `.coding/knowledge/bug/2026-09-01-web-fetch-card-hides-the-fetched-url-no-arglabel.md`: symptom → root cause (no path/file field → no chip; no argLabel case → no label) → fix + regression-test pointer. Complete and accurate.
- **(c) Minimal & consistent with browser_navigate — YES.** One helper + one dispatch + one import + tests; mirrors browserArgLabel's shape exactly (tool-name gate → try/JSON.parse/catch → trim → truncate → null). No unrelated refactors.

### Project checks
- **Documentation sync — none warranted.** Comparable label features (browserArgLabel/graphCallLabel/memorySearchLabel/search-pattern chips, plans 5ae26d22, 233773ab, 302153bc) shipped without README/PLAN.md bullets; a repo-wide .md search finds no README ToolCard-label section that would now be stale. The toolCardPaths.ts module doc ("Pure path/label extraction … drives the ToolCard header") remains accurate for the new helper, and the new function has a full doc comment (public-item rule satisfied).
- **Multi-platform neutrality — PASS.** Pure TypeScript string handling (JSON.parse, slice, trim); no platform APIs, paths, or shell syntax. Behaves identically on macOS and Windows.

### Correctness verification detail
- **Dispatch wiring is sound.** web_fetch is not in argPaths' label-only exclusion list, but its args `{url, max_length}` yield no `files[].path`/`path`/`file` (and salvagePathLiteral only matches `"path"`/`"file"` literals), so `argPaths` returns [] → ToolCard takes the `argLabel` branch (Message.tsx:673-675) → `webFetchLabel` supplies the chip. No double-chip risk; CallDetail (Message.tsx:862) benefits automatically. Placement after the browser dispatch and before the common path fallback is correct (no name collisions; web_fetch never carries path/file).
- **Null-safety matches the file convention.** Malformed JSON, non-string `url`, blank/whitespace url, and every other tool name return null (tested). Import placement is alphabetical (`toolErrorSummary, webFetchLabel, type ToolCardChip`); no unused imports (warning-free build claim is consistent).
- **No security surface.** The label renders as a plain-text chip (`chips.push({key, text, path: null})`), never as HTML/innerHTML; no URL munging needed for display.
- **Considered, not findings:** (1) `JSON.parse("null")` would make `parsed.url` throw — but the identical post-parse property access exists in browserArgLabel/argPaths/argLabel (argLabel's own `parsed.path` already threw on such input before this change), and real web_fetch args are always serialized JSON objects; hardening only the new helper would be inconsistent dead code. (2) A truncated/interrupted args stream (known ToolInvocation.args failure mode) still yields a bare card — same limitation as browserArgLabel, and URL-salvage is out of scope for a minimal fix. (3) The 60-char cap is web_fetch-only (browser_navigate's url is untruncated) — defensible and documented in the doc comment; a CSS cap may also exist. None block.

### Bookkeeping notes (not findings)
The superseded decision doc + new untracked `…-merged.md` follow the repo's established MERGED-supersede pattern and belong in the commit; plan file 2c6c78e0.md and the bug knowledge file are the expected bug-plan artifacts. Test claims (vitest 702/51 files, tsc clean, cargo 1718 — Rust untouched, no .rs in the diff) are consistent with the diff; re-run per the closing sequence after addressing the findings.
