## Verdict: FINDINGS (0 high, 1 low)

Round-2 verification of plan 402cc091 (backlog 77efc1b7), commit 0c4ede0 on wt/mnemo (HEAD; working tree clean — `git diff HEAD` and `git status` both empty, no uncommitted residue). Both round-1 LOWs are fixed and verified accurate against the code; the commit contents match the plan exactly; the riding files are as the round-1 scope notes described. One residual: round-1 LOW 1's blanket rationale survives in one living knowledge record (the MCP SPEC amendment) — the only spot the fix missed.

## Round-1 fix verification

**LOW 1 (Skill-arm rationale) — FIXED, and accurate against the code.** The arm comment (src/tool/mod.rs:867-875) and the test doc for `load_tools_allowed_in_every_state_and_inside_skills` (:2534-2542) now name the MCP case as the gap, in the requested form ("a skill whose allow-list names MCP tools needs the loader to materialize them — they don't exist in the registry until the group is revealed (named browser/image tools already bypass deferral via `explicitly_names`)"). Verified against the code:
- `explicitly_names` (src/tool/mod.rs:525-530) returns true only for Skill/Reviewer allow-lists naming the tool; the `schemas` bypass (:1189-1193 — `!self.loaded_groups.contains(group) && !filter.explicitly_names(name)`) then advertises a named deferred tool even when its group is unloaded, and the Skill `_` arm's `allowed.iter().any(|n| n == name)` (:876) admits it at dispatch — so "named browser/image tools already bypass deferral via `explicitly_names`" is exactly right.
- "they don't exist in the registry until the group is revealed" is right for MCP: `get`/`iter` (:1139-1156) consult the static map + the dynamic `ToolSlot`; McpTool adapters enter the slot only via the reveal path (load_tools.rs:145-171), and `hidden_groups_for`'s own comment (:1109-1112) states MCP groups have NO registered tools until the reveal — pre-reveal there is no tool object for `explicitly_names` to rescue, so the loader is the only materialization path.
- "The loaded tools still pass through this allow-list, so this widens nothing" — the addition admits only `load_tools` itself; every other tool still requires `allowed.iter().any`.
- No other CODE spot carries the old claim: repo-wide "browser/image/mcp" → zero src/ matches (one living-doc residual — see the finding; the remaining matches are historical/citational, see scope notes).

**LOW 2 (amendment date skew) — FIXED, internally consistent as documented.** The SPEC amendment line (.coding/knowledge/spec/2026-08-28-mcp-servers-v1-config-lazy-manager-deferred-grou.md:8) now reads "Amended 2027-01-11: … (user request 2027-02-05 — session-recorded; the Amended-stamp clock lags session dates, a known skew — backlog 77efc1b7, plan 402cc091)" — the "say so in the amendment line" option round 1 offered. The line is now internally consistent: the stamp is identified as the mechanical tool-clock artifact, the request date as session-recorded event truth, and the two no longer contradict. The new HOW memory (.coding/knowledge/how/2027-01-11-environment-date-surfaces-disagree-tool-stamps-l.md) records the systemic artifact — three date surfaces (tool stamps / review-filename dates / session-recorded dates), both instances (this amendment + the pre-existing 2027-01-07 chat-links one), and the treatment rule — and is live in the semantic index.

## Findings

### LOW 1 — round-1 LOW 1's blanket rationale survives in the MCP SPEC amendment

`.coding/knowledge/spec/2026-08-28-mcp-servers-v1-config-lazy-manager-deferred-grou.md:8` — "load_tools also rides the ToolFilter::Skill always-available set so a skill whose allow-list names deferred tools (browser/image/mcp) can reveal them." This is the blanket "(browser/image/mcp)" rationale round 1 corrected everywhere else: the arm comment, the test doc, and the commit message all now name MCP as the gap (named browser/image tools already bypass deferral via `explicitly_names` — verified above), but the SPEC amendment — a living current-truth knowledge record amended by this very commit — still frames the skill-membership addition with the blanket list. As a capability statement "can reveal them" is true, but as the recorded rationale it repeats the corrected imprecision and now disagrees with the authoritative code comment. Fix: memory_update find/replace on the SPEC record — "so a skill whose allow-list names deferred tools (browser/image/mcp) can reveal them" → "so a skill whose allow-list names MCP tools can materialize them (they don't exist in the registry until the group is revealed; named browser/image tools already bypass deferral via `explicitly_names`)". No code change.

## Commit contents vs the plan — verified

- **Gate deletion (step 1):** `mcp_reveal_allowed` (fn + doc comment) and its pinning test `mcp_reveal_gate_matches_the_tool_filter` are gone from src/tool/agent/load_tools.rs (the file flows LoadToolsArgs → Tool impl at :78-84; the tests jump from `schema_enum_tracks_the_runtime_table` to FakeDeferredTool); the dispatch enforcement block is gone from src/agent/dispatch.rs — the workflow-lock section (:244-263) now ends at the `filter.allows` re-check + `denial_message`, with the plan-mutations gate (:265-284) and review-exit gate (:286+) untouched; `parsed_call` is still consumed downstream (:323-330). Zero dangling references: plain-text `mcp_reveal_allowed` → only .coding/ historical records (three old review reports + the backlog problem statement, all describing the OLD state as past); "not available in the current workflow" → only the plan file's exploration notes + the round-1 quote; zero `load_tools` matches in dispatch.rs.
- **Skill-arm addition (step 2):** `|| name == "load_tools"` at :875 with the corrected comment (:867-874), plus the leading-comment extension (:843-844, "load_tools rides it too (backlog 77efc1b7) — see its comment below").
- **Matrix test (step 3):** `load_tools_allowed_in_every_state_and_inside_skills` (:2543-2570) uses the tool's real pair (Agent, AutoRun — load_tools.rs :89-91/:119-123); every expectation verified against the arms: Planning/Complete (`AutoRun && !mcp__`, :571/:794), Executing/PlanFrozen/Reviewing (`Agent => true`, :615/:697/:731), ExecutingResearch (not in the file-tools denied list, not mcp__-prefixed, :667-672), Skill(vec![])/Skill(named) via :875, Reviewer(vec![]) false (:888 — `current_plan` || named only).
- **Non-goals intact:** `hidden_groups_for` (:1103-1128, mcp probe still filter-aware) and the no-loadable-group advertisement skip (:1205-1207) unchanged; revealed McpTools stay filter-gated (mcp__ exclusion in Planning/Complete/research; the Reviewer arm never auto-grants the loader).
- **Docs (step 4):** PLAN.md:987, docs/CONFIGURATION.md:15, McpSection.tsx — no reveal-gate claims, all still true. Backlog 77efc1b7 correctly pending → in_flight with plan_id 402cc091.

## Riding files — as the round-1 scope notes described

- `.coding/knowledge/bug/ec425270.md` + `.coding/knowledge/spec/2027-01-11-self-healing-offscreen-browser-cdp-timeouts-forc.md` — the previous plan's (ec425270) knowledge records: the referenced regression test `wedge_renderer_times_out_and_self_heals` exists (src/browser/mod.rs:2471), and the referenced commits (880736c, bc3fd5e) match git log — both sit after the 3ddfb62 merge of wt/mnemo, so the records' "unmerged" claims are accurate.
- `.coding/plans/ec425270.md` — the bare-name touch (`browser::tests::wedge_renderer_times_out_and_self_heals` → bare), cosmetic, aligning with the bug/spec records' bare-name convention.
- The round-1 review report, the plan file 402cc091.md (all 5 steps [x], matching the executed work), the HOW memory, the SPEC amendment, and the backlog transition all ride in 0c4ede0 — coherent with the closing sequence.

## Scope notes

- **Historical records not flagged:** the plan file's Context (:10) and step-2 text (:14) and the backlog item's problem statement carry the plan-time blanket framing ("needs the loader … unusable mid-skill"). These are historical planning/request records — round 1 reviewed both and scoped the LOW 1 fix to the shipped code; rewriting committed history would falsify the trail, and the review trail documents the correction. The SPEC amendment is different: a living current-truth record amended by this change — hence the finding.
- **Stale graph index:** the code graph initially auto-reported `mcp_reveal_allowed` as still defined (stale codegraph.db — gitignored, rebuildable); the plain file search (after reindex) and direct reads confirm zero code matches. Not a code issue.
- **Tests:** not re-run (read-only reviewer). Reliance on the stated green runs (cargo test 2489+16; --features browser 2511+16, incl. tools_array_stays_within_context_budget) — corroborated by direct reads: no dead code, no dangling references, no schema growth for base-state arrays (load_tools already rode every base filter).
- **Constitution:** warning-free per the stated runs under deny(warnings); multi-platform neutral (pure filter/dispatch logic, no platform APIs or paths); file-tools-first (no shell mutation in the diff); no `#[allow]` added; touched public surfaces remain doc-commented.
