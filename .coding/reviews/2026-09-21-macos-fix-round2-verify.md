## Verdict: FINDINGS (0 high, 1 low)

Round-2 verification of the round-1 remediation (commit d0e4807 + bookkeeping 3f66dfc) for plan 263a9e31 on `wt/macos-fix`: **all three round-1 findings are correctly and completely remediated**, the two commits carry precisely the remediation (zero drift; working tree clean), the round-1 conclusions hold on the current tree, and landing via PR #3 is correct (verified live: open, head = 3f66dfc, mergeable, 8 commits ahead / 0 behind of main @ 841d677). One new LOW, cosmetic: `pub mod shell_path;` is misfiled one slot late in `src/lib.rs`'s otherwise strictly alphabetical mod list.

### Findings

**LOW 1 (new, cosmetic) — `pub mod shell_path;` breaks `src/lib.rs`'s alphabetical mod ordering.**
`src/lib.rs:29` places `shell_path` after `skill`, but the list is otherwise strictly alphabetical and `shell_path` < `skill` ("she" < "ski") — it belongs between `safety_rules` (:27) and `skill` (:28). One-line move; same class as round-1's LOW 3 (cosmetic, non-blocking).

### Round-1 LOW 1 (stale DECISION records) — REMEDIATED ✓
- The `-2` record (`.coding/knowledge/decision/2027-01-11-macos-ci-job-temporarily-disabled-releases-windo-2.md`, memory b787d888): `status = "superseded"` added to its frontmatter in d0e4807, body preserved as history; the memory index now shows it [superseded] — excluded from live recall. Supersede chain intact: `-1` (3016d466) → `-2` (b787d888) → restored (319f52d2, live).
- The successor record (`2027-01-11-macos-ci-job-restored-to-the-normal-path-release.md`, live): every claim verified against the tree and the live API — dual-platform release job (`needs: [windows, macos]` build.yml:228, both artifact downloads :235–242, all four dist globs :246–250, `fail_on_unmatched_files: true`) ✓; no `if:` gate, no `run_macos` input, no TEMPORARILY DISABLED note anywhere in build.yml (the retained "DIAGNOSTIC (2026-09-21)" comment :110–115 is the capture's rationale — the record itself says the capture INTENTIONALLY REMAINS, so this is consistent, not a stale disable note) ✓; "never ran on branch pushes" ✓ (`on:` is workflow_dispatch + v* tag pushes only, :24–27); run 35578123470 macos completed success (round-1 live-verified) ✓; the ruleset note is exact — verified live: id 23755694 "Protect from direct pushing.", enforcement active, target = default branch, rules include update/deletion/non-fast-forward blocks + `pull_request` with `required_approving_review_count: 1` ✓; "Plan 263a9e31, branch wt/macos-fix, PR #3" ✓ (PR #3 open, head 3f66dfc).
- The ci-owns-release record (d898bb80): RESOLVED 2026-09-21 amendment appended, accurate on every point (dual-platform restoration, the real-bug cause, run 35578123470, the ruleset). The older "TEMPORARILY Windows-only" amendment remains above it as dated history — the correct amendment pattern (newest paragraph resolves the older).
- Sweep for any other live record claiming macOS is disabled/Windows-only: **none**. The remaining live mentions are historical PLAN/REVIEW digests of the disable plan (61e643e6 "MERGED into main (53534b6)" — accurate history; 03c6eb1c — the same plan's goal digest, no current-state claim; two f4512a03 REVIEW records — point-in-time reviews). Pre-existing and out of scope, not introduced by this remediation: 03c6eb1c and 61e643e6 are two live digests of plan f4512a03 (one without the MERGED marker) — a memory-index quirk that predates this branch.

### Round-1 LOW 2 (platform-neutral extraction) — CORRECT AND COMPLETE ✓
- **Predicate semantics IDENTICAL**: `!value.trim().is_empty() && !value.chars().any(|c| c.is_control())` (src/shell_path.rs:40) — character-for-character the reviewed fix. Contract unchanged: rejects trimmed-empty + control characters, allows internal spaces.
- **All three tests moved, none lost, none duplicated**: `adoptable_path_allows_spaces`, `adoptable_path_rejects_empty_and_control_characters` (including the `"   "` regression assertion plus `\t`/`\n`/BEL/internal-tab cases), `adoptable_path_keeps_internal_spaces` — verbatim (one line re-wrapped, one doc sentence added) in src/shell_path.rs:43–82, now plain `#[cfg(test)]` so every platform runs them. The old `#[cfg(all(test, unix))] mod tests` in main.rs is fully removed; a full-repo textual sweep finds no other `adoptable_path` test references (only the delegate, its caller, doc links, and two historical review files).
- **Doctest valid** (src/shell_path.rs:33–38): `use mnemo::shell_path::is_adoptable_path` — crate name `mnemo` (root Cargo.toml `[lib] name = "mnemo"`), `pub mod shell_path` registered, `pub fn`; the three asserts match the predicate's contract (spaces allowed, whitespace-only rejected, newline rejected). The executor's green `cargo test --workspace` includes the lib doctests.
- **Registration**: `pub mod shell_path;` present in src/lib.rs:29 (modulo the ordering nit above).
- **Delegate + caller**: main.rs:119–122 is a `#[cfg(unix)]` one-liner delegating to `mnemo::shell_path::is_adoptable_path`, with a doc comment explaining why; the only caller `inherit_shell_path` (`#[cfg(unix)]`, :91–111, call at :100) is unchanged and compiles — `mnemo-app` depends on `mnemo` (src-tauri/Cargo.toml:12), so the path resolves; the `#[cfg(not(unix))]` no-op counterpart (:124–126) is untouched.
- **Warning risk under `#![deny(warnings)]`**: none found. Lib: pub items, no unused imports. App/Windows: the removed tests mod was `cfg(all(test, unix))` — never compiled on Windows, so its removal cannot affect the Windows build (the executor's green workspace run on Windows proves it). App/macOS: the only NEW unix-only code is the one-line delegate — signature-exact and used by `inherit_shell_path`. Residual (inherent to `cfg(unix)`, not a defect): the macOS leg has not compiled d0e4807 yet (no dispatch/tag since; the workflow has no per-push CI), so the delegate first compiles on the next v* tag or dispatch — nil risk by inspection, and the predicate itself is now guarded on both legs, which is the point of the remediation.
- **Module home**: `src/shell_path.rs` in the lib is the right call. The lib is the platform-neutral core; the module doc states the split (decision here, `#[cfg(unix)]` capture/spawn in the app), and lib placement is exactly what makes the Windows leg and dev box guard the predicate. It mirrors the lib's module-per-concern style (safety_rules et al.); the pub fn carries a doc comment per the public-doc rule. (Judged below the feature-scale bar that would require a landed-design amendment + SPEC memory: one pure fn, no new types/deps/concurrency, and the architecture is documented in the module doc and the new DECISION record.)

### Round-1 LOW 3 (step name/comment) — ACCURATE NOW ✓
build.yml:122–131: step renamed "Re-run the test suite with full output"; the comment now says "Re-run the WHOLE workspace … cargo has no 'only the failed tests' mode, so this re-runs everything (it only runs when the first pass already failed, and appends to the same log)" — exactly matching the command (`cargo test --workspace --no-fail-fast -- --nocapture … | tee -a macos-test-output.txt`).

### No drift ✓
- **d0e4807** carries precisely the remediation, nothing else: new src/shell_path.rs, `pub mod shell_path;` in src/lib.rs, the main.rs extraction (delegate + tests-mod removal), the build.yml reword, the three decision-record changes (new successor, `-2` supersede marker, ci-owns-release amendment), and the round-1 review report itself (the closing-sequence convention).
- **3f66dfc** (branch tip) is plan bookkeeping only: `.coding/plans/263a9e31.md` gains the "## Regression test — shell_path::tests::adoptable_path_rejects_empty_and_control_characters" section (accurate — that is the test's new path).
- **Working tree clean** (git diff HEAD empty; status empty).
- **PR #3 (live API)**: open, head = 3f66dfc (everything pushed), base main @ 841d677, ahead_by 8 / behind_by 0 — exactly round-1's six commits (6928ea4…b16b24c) plus d0e4807 and 3f66dfc; 14 files, +228/−78 (round-1's 8 files + the remediation's six file-changes); no scratch files in the net diff (the diagnostic artifact / noop-temp.toml net-zero from round-1 still holds).

### Round-1 conclusions hold on the current tree ✓
- **Fix semantics**: identical predicate, now delegated — the production path (`inherit_shell_path` pre-trims the capture) is untouched; the zero-production-regression conclusion is unchanged.
- **Workflow restoration**: `on:` = workflow_dispatch + v* tag pushes only; macos job ungated; release job dual-platform with `fail_on_unmatched_files: true`.
- **Diagnostic capture wiring intact and unchanged by the remediation** (only the step's name/comment reworded): `id: rust-tests` + `continue-on-error: true` + `set -o pipefail` + `tee` (:116–121); re-run keyed on `steps.rust-tests.outcome == 'failure'` (:126–131); `if: always()` upload with `if-no-files-found: warn` (:132–138); explicit "Fail if tests failed" → `exit 1` (:139–143).
- **Executor's post-remediation evidence** (`cargo test --workspace` green, exit=0; `cargo test --lib shell_path` → 3 passed) is consistent with the code as read — and the 3-passed-on-Windows result is the LOW 2 hardening demonstrably working.

### Landing via PR #3 — correct; no correctness problem ✓
- Ruleset verified live (detail endpoint): 23755694 "Protect from direct pushing.", active, targets the default branch; rules block update/deletion/non-fast-forward pushes and require a PR with `required_approving_review_count: 1` (allowed merge methods include "merge" — consistent with the main-only-receives-merge-commits policy). Direct pushes to main are rejected (GH013, as round-1 observed), so the PR path is the only — and the correct — way; the records' "merge_to_main's direct merge+push flow no longer works" note is accurate.
- PR #3: open, `mergeable: true`, behind_by 0, includes the full remediation (head 3f66dfc). `mergeable_state: "unstable"` is the expected pre-approval state (the required approving review is not yet granted; there is no branch-push CI that could fail).
- Process notes (not defects): (1) the merge needs 1 approving review — the PR author cannot approve their own PR, so a human approval is required; (2) round-1's caveat still stands: until PR #3 merges, main carries the intermediate gated workflow — a v* tag pushed before the merge would publish Windows installers only; (3) after merging, delete `wt/macos-fix` (the cleanup `merge_to_main` would have done); (4) optional polish: the PR body's "never compiles on Windows" line describes the pre-remediation state — historically accurate, but a one-line edit noting the platform-neutral extraction would keep it exact. Not a finding (PR bodies are transient).

### Constitution checks
- **Documentation sync** ✓ — records accurate and live; module + fn doc comments; plan regression-test note; the new_release skill note was already fixed in c29f9cc. **Multi-platform neutrality** ✓ — the extraction is platform-neutral by design; the delegate is `cfg(unix)` with the sanctioned `cfg(not(unix))` counterpart; the workflow change is YAML text only. **File-tools-first** ✓ — no shell-based mutation in the diff. **Security** ✓ — no secrets touched; the capture artifact is test logs only, produced before any Apple-secret step. **Warning-free** ✓ — argued above; proven on Windows by the green run. **Regression-test rule** ✓ — the moved test is the regression test, now guarded on both platforms.
