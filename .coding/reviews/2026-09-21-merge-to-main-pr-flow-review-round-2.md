## Verdict: PASS

Round-2 verification of plan 8ad6fa91 (backlog 006ee7d6) on `wt/macos-fix`: all seven round-1 findings (H1, L1–L6) are correctly and completely remediated; the no-drift check confirms the working tree is exactly the round-1-verified state plus those remediations (HEAD unchanged at the pre-item checkpoint `1a7aab4`; every diff hunk maps to round-1-verified content or a remediation); and the remediations introduced no new blocking issues. The change-set is safe to commit.

## Remediation verification (all seven)

**H1 — GH013 recovery (merge_to_main.toml:74, new_release.toml:48, comment block :35-38, DECISION record points 1-2). VERIFIED.** The direct path's tail now reads "Green → `git push` on the git tool, approval-gated (optional — skip on no remote); push success → `git branch -d <branch>` (never -D)" — push precedes delete, and deletion is gated on push success, so a GH013 arrives with the branch ref intact. The recovery clause resets local main to origin/main (`git reset --hard origin/main` — discarding only the unpushed local merge commit, whose changes live on the branch) before taking the PR path "with the branch intact". Traced end-to-end: false-negative ruleset check → merge → push rejected → branch still exists → reset → step 4 pushes the branch and opens the PR → the human merge re-lands the work; no lost work, no diverged local main, no impossible action. new_release's direct path is reordered identically ("verify BOTH builds … push, then `git branch -d`"). The comment block's coverage claim is corrected to "ruleset-check false-negatives with a working, authenticated gh (a ruleset the list call missed, or a race) — NOT gh-missing/unauthed, where step 4's gh pr create would fail too"; the DECISION record's point 1 carries the same corrected claim plus the reset step, point 2 the push-before-delete rationale. The reorder is also safe on the unprotected/no-remote direct path (`git branch -d` succeeds regardless — the branch is merged into local main).

**L1 — "optional" restored. VERIFIED.** merge_to_main.toml step 3: "`git push` on the git tool, approval-gated (optional — skip on no remote)" — a no-remote repo (ruleset check fails → direct path; sync step says "no remote → continue") can again skip the push.

**L2 — new_release PR-path build gate + re-run sync. VERIFIED.** The PR path now opens with "verify the BRANCH builds first (`npm run build` in frontend/ AND `cd src-tauri && cargo build`; PRs get no CI)" — matching merge_to_main's gate and the no-PR-CI reality (build.yml triggers: workflow_dispatch + v* tags only). The re-run instruction now reads "on re-run: `git checkout main`, sync via shell (`git fetch origin`, `git pull --no-rebase`) — main already carries the bump → continue to step 3", so step 3's `git tag -a v<version>` (which pins "Tag the MERGED main commit (never a branch tip)") points at synced main, not the working branch's tip. The PR body carries "+ build status".

**L3 — user-facing docs. VERIFIED.** MergeToMainDialog.tsx: the doc comment (lines 25-31) and all three body bullets (lines 76-78) now describe the two-path landing (direct merge vs. PR + human approving review; branch deletion "on the PR path, after the human merges"). StatusBar.tsx merge-button comment (lines 779-786): same two-path description. docs/FEATURES.md Working-branch topology bullet: two-path landing. PLAN.md line 357 Branch topology row: two-path landing plus the run-all app-managed landing "equally blocked by the ruleset (follow-up backlog item)". README.md remains clean (no merge-flow text, unchanged by this diff).

**L4 — dates reconciled. VERIFIED.** The new DECISION record is 2027-01-11 throughout (front matter `created`, filename, body "DECISION (2027-01-11, backlog 006ee7d6, plan 8ad6fa91)", "verified 2027-01-11"); its only 2026-09-21 references are the ruleset-discovery fact, consistent with every other record. Both decision-file amendments' bodies now say "(plan 8ad6fa91, backlog 006ee7d6, 2027-01-11)" under their tool-dated "Amended 2027-01-11:" headings. Both skill-file comments' "verified" dates read 2027-01-11. Backlog b52b041a carries the date-reconciliation note naming 2027-01-11 as the correct date and explaining the queue-vs-memory clock skew (queue created_at 2026-09-21); the note rides the item, so a future reader cannot be misled by the body's residual "2027-02-05".

**L5 — agent.md accuracy. VERIFIED.** Line 85 now reads "main is protected and only receives merges — directly via the `merge_to_main` skill on unprotected repos, or through the human's chosen PR merge method where a main-protection ruleset applies" (squash/rebase PR merges no longer contradict it), and the sync clause is scoped: "On the direct path the skill syncs `main` with `origin` first …".

**L6 — behavior pinned. VERIFIED.** `shipped_skill_files_parse_and_merge_to_main_cleans_up_memories` (src/skill/mod.rs:513-549) gained the five pins: the prompt contains "gh api repos/<owner>/<repo>/rulesets"; that check PRECEDES "git checkout main" (detection before action); contains "gh pr create --base main"; contains "NEVER bypass"; and "git push` on the git tool" PRECEDES "git branch -d <branch>" (push-before-delete). Each pin was traced against the actual prompt text, not just the green run: every asserted substring's first occurrence is where the pin claims it — ruleset check in step 2; checkout/fetch/pull/merge/push/branch-d in step 3 in that order; PR path and NEVER bypass in step 4; "MERGED into main" in step 5 (step 4's "MERGED-at-<sha>" does not contain it). The existing pins (fetch < pull < `--no-ff <branch>`, "MERGED into main") are unchanged and still hold first-in-order.

## No-drift check — clean

HEAD is unchanged at the pre-item checkpoint `1a7aab4` (the 006ee7d6 in_flight note carries its full sha `1a7aab49…`), so the uncommitted diff is the complete change-set — nothing was committed mid-remediation. Every hunk maps to round-1-verified content or one of the seven remediations: backlog.jsonl (006ee7d6 in_flight + plan linkage; b52b041a + the L4 note), the two decision-file amendments, merge_to_main.toml (comment block + two-path prompt + the H1/L1 edits), new_release.toml (comment + ruleset-aware step 2 + the H1/L2 edits), the PLAN.md row, agent.md's four edits + the L5 rewording, the FEATURES.md bullet, both TSX files, and the mod.rs test pins. The round-1-verified state is intact: the two-path prompt structure (ruleset check → direct path → PR path → memory supersede → skill_end), the ordered sync/merge substrings, new_release's ruleset-aware step 2, agent.md's four edits, the two amended DECISION records + the new DECISION record, and the worktrees.rs follow-up item (b52b041a, pending). The comment block's internal step references were correctly renumbered ("step 5 is the post-merge hygiene" matches the 6-step prompt). Untracked files are exactly the expected three: the new DECISION record, the plan file 8ad6fa91.md, and the round-1 review report.

## New-issue scan — none blocking

- Multi-platform: the remediation text is prompt/doc/comment/test-only; the new Rust test code is platform-neutral; the pre-existing `Invoke-RestMethod` in new_release step 4 is untouched.
- File-tools-first: all edits are file-tool territory; the knowledge amendments went through `memory_amend`, the backlog note through the backlog tools — no shell mutation in the diff.
- Security: no new surfaces; the `git reset --hard origin/main` instruction is scoped to the GH013 recovery and discards only the unpushed local merge commit (the branch retains the work).

Edge cases assessed — no finding: (a) the GH013 recovery's reset leaves the agent on main; step 4's "verify the BRANCH builds" implies checking the branch out (not spelled out, but inferable — the direct path's "`git checkout <branch>` if needed" is the established idiom, and the branch ref exists); (b) new_release's direct path carries the reorder but no explicit GH013 recovery clause — round 1 prescribed only the reorder for it, and with push-before-delete a false-negative ruleset check surfaces as a failed push with the branch intact (recoverable, benign); (c) "push success → branch -d" read against "optional — skip on no remote": skipping is not failing — the natural reading proceeds to deletion, and mechanically `git branch -d` succeeds regardless (the branch is merged into local main); (d) the plan file `.coding/plans/8ad6fa91.md` retains plan-time wording (branch -d before push; the old gh-missing/unauthed coverage claim in design decision (a)) — it is a point-in-time record, not project documentation (the root PLAN.md row was updated), and the DECISION record + the review trail carry the corrected design.

## Test evidence

The parent's runs are consistent with the diff: `cargo test --workspace` green (2,522 passed, 0 failed, 21 ignored, warning-free under `#![deny(warnings)]`) includes the extended skill test — which loads the working-tree `.coding/skills` via `CARGO_MANIFEST_DIR`, so the green run genuinely proves the edited TOML parses and carries the pins; the embedded copy is `include_str!` of the same file (one source of truth). Frontend npm test green (90 files, 1,246 tests) covers the TSX edits, which are comment/copy-only (no logic). No Rust source outside the test module changed.

## Summary for the parent

All seven remediations verified correct and complete; no drift beyond them; no new blocking issues. Commit the change-set as-is on the current working branch (`wt/macos-fix`) and finish.
