## Verdict: FINDINGS (0 high, 1 low)

Round-2 verification for plan e3d0758b ("multi_edit tool + compact line-op syntax for file_edit"), `wt/mnemo`, uncommitted working tree. Scope: all uncommitted changes (25 modified files + 5 untracked at verification time — incl. the plan, the round-1 report, the new engine/tool sources, and one knowledge memory file; this report becomes a 6th untracked file), verified against the working-tree sources, not the fix summary. Round 1's five LOW findings (L1–L5) are all genuinely fixed, each with a real regression pin, and the fix delta disturbed none of the round-1-verified properties (atomicity, grammar, aliases, strict schemas, gates/bookkeeping, docs, budget). One residual surfaced during fix verification, split out as finding 1r (low): the PLAN.md diff-viewer row's auto-show clause is still untrue for `multi_edit`.

### Round-1 fixes — verified closed

**L1 — closed (rendering); its doc-row residual is finding 1r.**
- `frontend/src/components/views/DiffViewer.tsx:38-45` — the new exported `isFileEditTool` includes `multi_edit`; the component is wired to it at `:328` (`const isFileEdit = isFileEditTool(toolName)`), so `pendingResolved` (`:331-334`) and `showPending` (`:346`) engage for a pending `multi_edit` approval and the `:411-420` "No file diff for this tool" branch is no longer reached for it.
- `:80-89` — exported `contentFromPreviewOrArgs` gained the `multi_diff` branch (`path: preview.paths[0] || args.path || "(unknown path)"`, `mode: "unified"`, `unifiedDiff: preview.diff`), rendered by `DiffBody` through `UnifiedDiffView` (`:146-152`). The handling is live, not inert. `previewPath` (`:28-31`) remains as the defensive fallback at `:111` — reachable only for a malformed preview (non-string `diff`, since `:80` precedes it); harmless, noted for accuracy.
- Tests: `DiffViewer.test.ts:127-139` (`isFileEditTool`, positives + negatives) and `:141-155` (`multi_diff` → unified / first path / combined body) — genuine regression pins (16 tests in the file, matching the supplied vitest evidence).
- `PLAN.md:938` — the combined-diff claim is now accurate; matches the shipped `ApprovalPreview::MultiDiff { paths, diff }` (one diff with per-file `---`/`+++` sections; `multi_edit.rs:348-354`).

**L2 — closed.** The duplicate guard moved into `prepare_files`, keyed on the sandbox-validated target: `seen: HashSet<PathBuf>` + `seen.insert(validated.clone())` (`multi_edit.rs:146-158`), emitting the same "files[{idx}] '{path}' appears twice — merge its ops into one entry" message. `validate_files` no longer checks uniqueness, with the reason documented (`:96-98`). Equivalent spellings are caught because `Sandbox::validate` canonicalizes existing paths (`sandbox.rs:126-130`) — `a.txt` and `./a.txt` yield the same `PathBuf` before any read/write. `rejects_bad_shapes_before_any_write` (`:462-498`) now covers both the exact duplicate and the `./a.txt` variant, and asserts the file is byte-identical afterwards. Ordering nuance (benign, pre-write refusal either way): a duplicated path that does not exist now reports the first entry's "does not exist — use file_write" rather than "appears twice". No other behavior changed.

**L3 — closed.** The write-phase report is `write_failure_message(prepared, failed_idx, written, err)` (`multi_edit.rs:215-237`), called with the enumerate index (`:310-318`). The failed file gets its own clause ("'{failed}' may be truncated/partial") and only `prepared[failed_idx + 1..]` is claimed byte-identical — the failed file is never listed under "not written". Checked index 0 (written `[]`, tail = all others), mid, and last (tail `[]`): honest in every case. The new unit test `write_failure_message_excludes_the_failed_file_from_not_written` (`:570-594`) pins the mid case, including the negative assertion that the failed file must not appear in the not-written list. The write loop itself is otherwise unchanged.

**L4 — closed.** `sandbox.rs`'s async-callers contract (`:45-58`) now names the hook's three tools and multi_edit's shape: "additionally validates and reads EVERY entry of a prompted multi-file call (N `validate`s + N in-memory applies, one preview read per entry) … Only those three tools override the trait default". Matches the shipped code: `MultiEditTool::approval_preview` (`multi_edit.rs:281-284`) → `prepare_for_approval` (`:348-354`) → `prepare_files` (N validate/N read/N apply/N diff, `:146-190`), invoked inline — no `spawn_blocking` — from `execute_tool_call` (`dispatch.rs:174-635`; the call is at `:536`). Override inventory re-verified by search: exactly `file_write.rs:98`, `file_edit.rs:702`, `multi_edit.rs:281` (plus the trait default `tool/mod.rs:329`); `file_append` and `convert_line_endings` have no hook — the enumeration is complete.

**L5 — closed.** The hand-written const is gone (no `MULTI_EDIT_DRIFT_ERR` matches remain). `real_multi_edit_drift_err()` (`dispatch.rs:1750-1781`) genuinely executes `MultiEditTool` (real `Sandbox`, tempdir, failing anchor on the second entry), asserts the call fails, that the output names `b.txt`, and that `steering_stats::is_edit_drift_failure("multi_edit", &output)` holds; it returns `result.output` verbatim. Both funnel tests are `#[tokio::test]` (`:1804-1844`, `:1846-1894`) and consume the derived error. The contract pinning is real: `observe_edit_freshness_tracks_multi_edit_per_path` feeds the tool-derived text through the shipped funnel and asserts the error-named path (`b.txt`) arms drift while the sibling (`a.txt`) stays clean, then the no-path fallback, then a success clearing both — exactly the extraction at `:1288-1296`. Detection backing is tool-scoped and first-line (`steering_stats.rs:267-270`: `EditStaleRead` = `file_edit | multi_edit` + "Re-read the file"). Nuance noted, not a defect: the intercept test additionally calls `note_edit_drift(1, "b.txt")` after observing the derived error, but the end-to-end per-path extraction is exercised by the second test.

### 1r (low) — PLAN.md's Diff-viewer row promises auto-show for a pending approval, but a pending `multi_edit` approval never auto-reveals the Diff tab (the one frontend family site this change did not extend)

- `PLAN.md:938` — the row updated by this change reads: "… diff for `file_edit` / `file_write` / `multi_edit` approvals (`multi_edit` shows one combined diff covering every file it changes). **Auto-shown when an approval is pending.**" (and `:929` "toggled or auto-shown when an approval diff is pending"). The row was updated by this change — it names `multi_edit`, a tool that exists only in the uncommitted tree — so the inaccuracy is introduced here, not pre-existing.
- `frontend/src/hooks/useAgentEvents.ts:599-607` — the `approval_request` auto-reveal fires only for `file_edit | file_write | file_append`; `multi_edit` is absent. `autoRevealDiff` has no other call site (the store definition `useAgentStore.ts:942` plus this hook, verified by search). The right panel is hidden by default (`PLAN.md:929`), so a pending `multi_edit` approval leaves the Diff tab closed while the row naming it says otherwise. The comment at `:600-601` ("file tool approvals (file_edit / file_write / file_append)") is likewise stale relative to the change's own family definition: every other frontend site the change touched includes `multi_edit` (`ApprovalPrompt.tsx:19-22`/`:32-45`/`:73-76`, `DiffViewer.tsx:38-45`, `Message.tsx:599`, `toolCardPaths.ts:92-98`).
- Impact is contained (the same shape as L1): the inline `ApprovalPrompt` renders the combined diff (`ApprovalPrompt.tsx:336-351`), so the approver is never blind; what is untrue is the auto-reveal claim for `multi_edit`.
- Fix (either — one line, or one clause): add `multi_edit` to the condition at `useAgentEvents.ts:605` (+ its comment), so the tab auto-reveals consistently with its own `isFileEditTool` set; or scope the auto-show sentence in `PLAN.md:938` (and `:929`) to the tools that actually auto-reveal.

### Fix-delta regression spot-checks (round-1 properties re-verified)

- **Atomicity seam intact.** `prepare_files` still prepares every entry (validate → dedupe → dir/protected → read → apply → diff) before the write loop, and any prepare error returns before a single write (`multi_edit.rs:296-320`); the only write-loop change is the L3 failure-message call on the error path. `a_failing_op_leaves_every_file_byte_identical` (`:403-434`) still pins it, and the preview-purity test (`:534-563`) still holds.
- **Engine untouched.** `edit_ops.rs` was not part of the fix delta; `compute_diff` is exported (`edit_ops.rs:45`) and shared by both tools; round 1's grammar/alias/strict coverage stands.
- **Drift funnel unchanged in behavior.** The multi_edit arm of `observe_edit_freshness` (`dispatch.rs:1277-1298`) and `multi_edit_paths` (`:1325-1336`, pinned at `:1784-1802`) are as round 1 verified; `file_edit_redirect` untouched; now additionally pinned against the real tool message (L5).
- **Frontend family coherent.** `ApprovalPrompt.isProjectScoped` multi_edit case + combined-diff render + file-count label; `types.ts:146-158` `multi_diff` variant; `Message.tsx:599` opensDiff + `:800` editDiff; `toolCardPaths.ts:92-98` chips + `:1043` displayName + `:736-739` fileEditDiff. `agentEventReducer.ts:800-807` deliberately excludes multi_edit from last-diff snapshots with a reasoned comment (one snapshot = one path; the combined diff rides the card) and guards the preview path at `:824-826` — documented and coherent, not a defect. Only the auto-reveal site (`useAgentEvents.ts:605`) sits outside the family → finding 1r.
- **macOS + Windows neutrality.** The fix delta is TypeScript + std/tempdir only; the `./a.txt`-vs-`a.txt` canonicalization case holds identically on both platforms (`Sandbox::validate` canonicalizes existing paths, `sandbox.rs:126-130`); no new `cfg(windows)`, no platform-specific path or shell syntax.
- **File-tools-first.** No shell-based file mutation introduced anywhere in the delta or the change.
- **Warnings/typecheck.** No `#[allow(...)]` added by the fixes; the green `cargo test` under `#![deny(warnings)]` proves warning-free. Test evidence for this round (reviewer is read-only; not re-run): root `cargo test` exit 0 (full suite incl. doc-tests), frontend `npx tsc --noEmit` exit 0, `npx vitest run` 90 files / 1251 tests passed (`DiffViewer.test.ts`: 16 tests). `src-tauri/mnemo-app` remains environment-blocked (memory d67ac741) and is not a gate.
- **Budget/docs.** Untouched by the fix delta; round 1's budget-accounting verification stands.

Bottom line: all five fixes are real and complete in substance, each with a regression pin, and the fix delta introduced no regressions — the change remains sound. One low residual (1r) remains on the PLAN.md Diff-viewer row's auto-show clause. Recommended next step: fix 1r (one line or one clause), re-run the frontend `tsc --noEmit` + `vitest` (a Rust re-run is not implicated by a frontend/doc-only fix, but re-run root `cargo test` if any Rust file is touched), then commit to `wt/mnemo` with both reports.
