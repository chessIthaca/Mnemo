## Verdict: PASS

Summary: all four round-1 findings (L1–L4) are fixed and verifiable in the current uncommitted tree; the fix round introduced no regression (the three signature changes are fully propagated to their single callers each, the frontend invoke shapes are untouched, lock discipline holds); and every round-1 verified invariant — the disabled gate, the failure-protected `None` contract, the pinned `/v1/systemone` wire mapping, the Rust↔TS contract, both fallback arms + the rewire path — re-confirms unchanged. Nothing new found.

### Fix verification

**L1 — save-time `classifier://status` emit (FIXED).**
- `rewire_vision_embedder_and_classifier` now takes `app: &tauri::AppHandle` (src-tauri/src/ipc/rewire.rs:27-31) and emits at the end of the fn (:111-113). The classifier rebuild block (:85-106) sits outside every `if let` guard, so **every** rewire path — disable, enable, client-build-failure fallback — reaches the emit. The only gate is `if let Ok(status)`, skipped solely on a poisoned status lock (the accessor's documented error path; degenerate panic-recovery case).
- Payload discipline: `state.classifier_status()` clones and releases the read lock inside the accessor (state.rs:344-354); the slot write lock at :91-95 is a statement-scoped temporary; the emit at :112 holds **no lock**. Payload `&status` (a `ClassifierStatus`, lowercase-string serde) is byte-identical in shape to the startup emit (main.rs:583) and matches the frontend `listen<string>` handler (tauri.ts:669).
- Both rewire callers updated: `save_settings` (:980, via its new `app: tauri::AppHandle` param at :946) and `resync_runtime_state` (:346, via its new `app: &tauri::AppHandle` param at :270; `save_endpoints` — which already held an `AppHandle` at :195 — passes `&app` at :245, so the endpoints-save path emits too). The code graph confirms these are the only callers of the rewire fn and that `save_settings` has no other Rust caller (the `save_settings_*` tests exercise the DTO, not the command fn — no AppHandle-mock hazard).
- Frontend transparency: `AppHandle` is framework-injected (the same-file `save_endpoints` pattern, :194-201), so `invoke("save_settings", { patch })` (tauri.ts:631) and the `SettingsSavePatch` type are unchanged; handler registration untouched (main.rs:829/:831).
- Doc claims now true: rewire.rs:24-26, settings.rs:35-39 (`get_classifier_status`), tauri.ts:662-665 (`onClassifierStatus`), ClassifierSection.tsx:89-91 (subscribe comment). Each describes exactly "emitted at startup and after a settings save rewires the backend" — which is now the emitted reality (startup main.rs:583 + every rewire).

**L2 — NoClassifier docs describe the shipped design (FIXED).** All four sites reworded: module doc (classifier.rs:10-15 — "`build_classifier` returns `None` and the runtime slot stays empty… `NoClassifier` remains available as an explicit no-op"), `ClassifierStatus::Disabled` (:121-123 — "no backend object exists"), `NoClassifier` (:154-158 — explicitly "NOT what the app builds while Laya is off"), test comment (:765-768). A repo-wide sweep confirms `NoClassifier` is still constructed only by the two tests and that no other doc site (client_factory.rs:335-349, state.rs:151-160, README/FEATURES/CONFIGURATION) claims the app builds it — all describe the `None`-slot design accurately.

**L3 — `[general.laya]` omitted while default (FIXED).** `#[serde(default, skip_serializing_if = "LayaConfig::is_default")]` on `GeneralSection::laya` (general.rs:117) + private `LayaConfig::is_default` (:203-211, `!enabled && endpoint.is_none()` — mirrors the `SteeringNotesCfg::is_empty` convention; the field doc :114-116 states the omission contract).
- Absent section still parses to default: `#[serde(default)]` + `laya_defaults_to_disabled` (:1106-1114).
- Meaningful state never dropped: `enabled = true` ⇒ `is_default` false ⇒ written (pinned: `laya_round_trips` :1116-1137 and the new test's second half); a leftover endpoint with `enabled = false` ⇒ `endpoint.is_none()` false ⇒ `is_default` false ⇒ written and round-trips (logic verified by inspection; see residuals).
- The new test `laya_section_is_omitted_while_default_and_written_once_touched` (:1139-1164) genuinely fails without the skip attribute (it asserts the serialized default config contains no `general.laya`).
- No other serialization/round-trip site assumes presence: the GET wire always constructs `LayaWire` explicitly (settings.rs:882-885; wire test :1437-1440; contract_fixtures.rs:242; dto-get-settings.json:15-18), and `persist_and_reload` re-parses exactly what it wrote (absent ⇒ default). docs/CONFIGURATION.md:9 documents the omission. Configs written by the pre-fix build (`enabled = false`) simply self-heal to omitted on the next save — no behavior change.

**L4 — snapshot `classifier_status` doc matches reality (FIXED).** tauri.ts:256-263 now says "Seed value for status consumers; the Settings → Classifier section reads the live status via `getClassifierStatus()` and the `classifier://status` event" — which is exactly the shipped flow (section: live read on load :77, subscribe :92-101, re-poll after save :125; nothing consumes `snap.classifier_status`). The ClassifierSection load comment (:75-77) says the same. The backend population (startup.rs:43-45, :153) is unchanged and correct.

### Re-confirmed round-1 invariants (all hold)
- **Disabled gate** (client_factory.rs:350-390): `laya.enabled == false`/absent/blank endpoint ⇒ `None` + `Disabled` with the client **never constructed**; enabled-without-endpoint warns + stays Disabled; unbuildable client ⇒ `None` + `Failed`. Unchanged from round 1.
- **All three arms** (main.rs): Ready :521-522 shares `brain.classifier_status` + wraps `brain.classifier`; NeedsProject :670-673 and Err :761-764 are `Disabled` + `None`. Startup build :1332-1335, Brain wiring :1900-1901.
- **Rewire path** (rewire.rs:85-113): rebuild through `build_classifier` on the saved config, slot swap under the (statement-scoped) write lock, log via the `classifier()` accessor.
- **Failure-protected `None`** (classifier.rs): every transport/protocol failure ⇒ `None` (classify :312-337); hostile bodies cannot panic (`parse_answer` :269-301 — missing `confidence` reads 0.0, mismatched primitive ⇒ `None`, non-numeric probabilities filtered); 10 s bound via the client builder (:186, :236) covering the whole request; no retries; `.expect` only on the status lock (documented idiom, :303-304).
- **Wire mapping**: `systemone_request` (:252-257) = `POST {endpoint}/v1/systemone` (:320), body `{"state": …, "questions": {"question": {typed question}}}`; `parse_answer` per primitive; request + parse centralized in one file; trailing-slash normalization; the stub round-trip asserts method, path, and the full body (:636-653); per-primitive parse pins :656-719.
- **Rust↔TS contract**: `LayaWire` (settings.rs:471-481, always nested, never null) ↔ `AppSettings.general.laya` (tauri.ts:447-449, with tolerant `laya?`) ↔ ClassifierSection (`laya?.enabled ?? false`) ↔ dto-get-settings.json ↔ contract_fixtures.rs:242; patch `laya_enabled`/`laya_endpoint` (settings_dto.rs:256-264; apply :554-564 — absent keeps, blank clears, trimmed) ↔ tauri.ts patch type ↔ section sends both (:119-122); lowercase status serialization pinned (classifier.rs:368-384); snapshot field (startup.rs:43-45/:153) ↔ tauri.ts:263; DTO absent-field pins (settings.rs:1303-1304) + `settings_save_dto_parses_laya` (:1318-1325); `get_classifier_status` registered (main.rs:831); frontend source-contract suite pins command/event/patch/snapshot names and nav placement (ClassifierSection.test.ts).

### Regression hunt — nothing new
- The three signature changes (`save_settings` +app, `resync_runtime_state` +app, rewire +app) propagate to every caller (graph-verified, single callers each); no dead code, no visibility leak (`is_default` is private and consumed by the serde attribute — warning-free under `deny(warnings)`).
- Multi-platform neutrality preserved: the emit uses the same cross-platform `Emitter` pattern as the embedder block; no new platform-shaped code anywhere in the fix round.
- The ClassifierSection `onClassifierStatus` subscribe/cleanup shape mirrors the established `EmbeddingSection` `onEmbedderStatus` idiom — pre-existing convention, benign here.
- Author's post-fix runs (root 2577 passed / app 304 passed + integration legs / tsc clean / FE 1268) were not repeated per instructions; nothing in the static review would break them.

### Residual non-findings (no action required)
1. `classify`-driven status flips (Ready⇄Failed during live calls) do **not** emit `classifier://status` — no doc claims they do, and there are no production call sites yet (items 2-5 are the consumers); when they land, they should either re-poll or extend the emit.
2. The save-path emit has no source-contract pin (the repo's convention for untestable Tauri wiring, e.g. startup.rs:180-196) — optional in-style hardening only; the frontend tests pin the listener + command names, and the emit is verified by inspection.
3. The disabled + leftover-endpoint serialization case (written, round-trips) is covered by `is_default`'s two-field logic but not by an explicit test case (the new test pins default-omitted and enabled-written).
4. settings.rs's module-doc overview line (:10) enumerates "runtime rewire of vision/embedder" without the classifier — an incomplete enumeration, not a false claim; rewire.rs's own module doc (:5-12) is complete.
5. Commit hygiene: the untracked `.coding/knowledge/spec/2027-01-11-multi-edit-tool-compact-line-op-grammar-for-file.md` is unrelated to the Laya change set (parallel workstream) — the author should confirm it belongs in this commit.
6. In the `save_endpoints` path the rewire + emit run while the config tokio-Mutex is held (settings.rs:344-351) — pre-existing lock scope from round 1; `app.emit` is non-blocking and non-reentrant, so the emit adds no hazard.

All four round-1 findings are fixed in code (not just prose), the fix round is regression-free, and the acceptance-critical invariants hold. Nothing outstanding remains.