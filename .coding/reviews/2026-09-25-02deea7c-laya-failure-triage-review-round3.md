## Verdict: PASS

Round-3 (final) verification of plan 02deea7c (Laya failure triage + startup fine-tune, backlog 1a4049c1) — the full uncommitted changeset on `wt/mnemo`: 29 tracked files, +1911/−44, plus the untracked `src/agent/failure_triage.rs`, `src-tauri/src/ipc/finetune.rs`, and five `.coding/` artifacts. **The round-2 LOW-1 doc fix landed verbatim as claimed, it is the ONLY change since round 2, and every round-1/round-2 verified invariant re-checked intact by direct reading. Nothing remains.**

### 1. Round-2 LOW-1 — both doc comments reworded to trigger semantics: VERIFIED

- `src-tauri/src/ipc/finetune.rs:150-152` — `write_finetune_marker`'s doc now reads: "Write the last-fine-tune marker (`ts` unix seconds — the training rows newer than it form the next fine-tune's ≥50-row trigger; never its dataset — the run retrains on the full resolved corpus)." — the claimed wording, verbatim (the old 2-line comment grew to 3 lines).
- `src-tauri/src/ipc/laya.rs:331-335` — `finetune_marker_path`'s doc now reads: "…its content is the unix-seconds ts of the last completed fine-tune — the training rows newer than it form the next fine-tune's ≥50-row trigger (never its dataset — the run retrains on the full resolved corpus)." — likewise (the old 3-line comment grew to 5 lines).

Both blocks are `///` doc comments — comment-only, zero behavior delta, exactly as claimed.

### 2. Cross-check — no surface still claims increment-only dataset semantics

- **`finetune_decision` (finetune.rs:116-140), the shipped behavior the docs must match**: the trigger counts resolved rows strictly newer than the marker (`r.ts > ts` via `is_none_or`; no marker → every resolved row counts), the run fires at ≥ `FINETUNE_MIN_NEW_ROWS` = 50, and the dataset is ALL resolved rows (:131-137 collect every `disposition.is_some()` row). Both reworded comments match this exactly; the "≥50-row" phrasing matches the `>= 50` boundary (a run at exactly 50).
- **Module doc (finetune.rs:13-17)**, unshifted from round-2's pin: "counts the labeled rows newer than the last-fine-tune marker (the TRIGGER) and — at [`FINETUNE_MIN_NEW_ROWS`] or more — runs the fine-tune through the managed venv on the FULL resolved corpus (the marker gates the trigger, never the dataset — a retrain never discards earlier labeled rows)".
- **`Run` variant doc (:90-93)**, unshifted: "`new_rows` is the accrual since the last-fine-tune marker (the trigger); `dataset` is the FULL resolved corpus (every row with a disposition)".
- `FINETUNE_MIN_NEW_ROWS` doc (:55-57), `dataset_path` doc (:65-68), and the marker-written-first rationale (:557-560) — all trigger-consistent (the rationale's "the next run would just re-train on the same rows" reads as the trigger re-firing, a harmless double-run — true under the shipped semantics, and verified by both earlier rounds).
- **docs/FEATURES.md:48**: "when ≥50 new labeled rows have accrued since the last fine-tune marker (the marker gates the trigger, never the dataset — the run retrains on the full resolved corpus)".
- **README.md:43**: "…every classified failure is logged with its true outcome so a startup fine-tune can retrain the checkpoint from real dispositions (managed mode)" — no increment claim.
- **PLAN.md consumer bullet** (the new Laya failure-triage entry): "re-trains the checkpoint from that log when ≥50 new labeled rows accrued and hot-swaps the served checkpoint" — trigger phrasing.
- **main.rs:640-646** startup-hook comment (the :644 pin, exact): "the spawned task counts the labeled rows newer than the fine-tune marker and, when ≥50 accrued, fine-tunes via the managed venv".
- **general.rs:268-276** `auto_finetune` config doc: trigger language ("compares the logged failure corpus against the last run's marker and, when enough new labeled rows accumulated, runs the Laya fine-tune").
- **Repo-wide literal sweep**: the old sentence "form the next fine-tune's dataset" survives ONLY inside the round-2 review report file itself — a historical finding record, correctly left untouched. The single "increment-only" occurrence in code is finetune.rs:656's regression-test comment, which NEGATES it ("never the increment-only slice"). No doc surface contradicts any other.
### 3. The delta since round 2 is EXACTLY those two comment blocks

Three independent proofs, all from the current tree:

1. **Diff-stat arithmetic.** Round 2 recorded 29 tracked files, +1909/−44; the current tree is 29 files, +1911/−44 — net **+2 insertions, zero deletions**. Unchanged deletions (44→44) mean no HEAD-committed line was modified or removed anywhere since round 2. The +2 is precisely laya.rs's doc comment growing 3→5 lines (finetune.rs is untracked, so its 2→3-line growth is invisible to the stat).
2. **Pin-shift analysis.** finetune.rs: every round-2 pin upstream of the reworded doc is unshifted (module doc :13-17, `Run` doc :90-93, `finetune_decision` :116-140); every pin downstream shifted by EXACTLY +1 with content identical to round-2's descriptions (`restore_status_if_ours` :243-257, `slot_still_holds` :262-274, `run_finetune_process` doc :289-296, `child.wait` :335, guard sites :391/:398/:406/:427/:444/:451, `observed_slot` :481-484, `export_dataset` :522, `spawn_blocking` :529-538, both regression tests :650-669/:672-686). laya.rs: every pin upstream unshifted (`hide_console_window` :228-239), every pin downstream shifted by EXACTLY +2 (`stop_if_generation` :380, `owns_generation` :399, `spawn_server` :426-461, the `start_server_models` region :1011-1059). Both shifts begin exactly at the reworded comments — no other line-count change exists in either file.
3. **Byte-exact pins everywhere else.** `git status` shows the identical file set as round-2's baseline (the same 29 modified files + the same untracked set: the two new code files + five `.coding/` artifacts — nothing added, nothing removed). Every round-2 pinned site re-read at the SAME line numbers, byte-identical: turn.rs (:3407/:3417/:3487 bad-JSON arm, :1442-1517 tiers, :852-857, :1781-1789), tests.rs (:12139-12199, :12202-12264), dispatch.rs (:1064-1115), runtime/agent.rs (:564-657, :713-720), error.rs (:116-118, :150-156, :226-235), failure_triage.rs (:235, :314-341, :350-364, :375-385, :925-941 — byte-exact unshifted, so the untracked file is untouched), main.rs (:620-653, :1471-1480, :1999), general.rs (:251-309, :1316-1354), settings_dto.rs (:282, :286, :601, :604, :957, :976-977), settings.rs (:496, :499, :907-908), contract_fixtures.rs (:248-249), loop_impl.rs (:789-799), rewire.rs (:85-176), src/agent/mod.rs (:31 + the cap constants), src-tauri/src/ipc/mod.rs (:29), classifier.rs (:126-170), frontend tauri.ts (:683-695) — plus the full frontend diff hunks (ClassifierSection.tsx/.test.ts, types.ts, dto-get-settings.json) matching round-2's verification. **Conclusion: no code, test, config, or frontend change since round 2.**

### 4. Round-2 "verified correct" invariants — all intact (re-read this round)

1. **Flags-off byte-identical**: all four triage surfaces still gate on `if let Some(gate)` (turn.rs:3417 bad-JSON, turn.rs:1443-1447 tool-batch, dispatch.rs:1090 inner, runtime/agent.rs:610 outer) and `FailureTriageHandle::triage` still returns `NoAnswer` on the disabled flag WITHOUT any classifier call (failure_triage.rs:329-341).
2. **429 never classified**: inner — the serialization-bug return (dispatch.rs:1075) and the `is_non_retryable() || is_rate_limited()` return (:1077-1079) still precede the triage arm (:1090-1115); outer — the 429-fallback (:564-587) and non-retryable (:588-595) arms still precede the triage arm (:596-657). The `is_rate_limited` needles (error.rs:231-234) match none of the skip-hint wording.
3. **No duplicate rows across layers**: the inner layer's marked error drives `is_classified_skip` (error.rs:150-156) → `is_non_retryable` (:116-118), so the outer layer fast-fails before re-classifying; outer rows once per attempt sequence (:609); tool-batch once per batch (`batch_triage_logged`, :1442/:1477/:1514); bad-JSON resolves immediately (:3442-3445). Dispositions intact: `retry_succeeded` on a failure-free batch (:1784-1789), `cap_reached` at the abort (:852-857), `retry_failed` on the outer final failure (:715-720).
4. **Read-only auto-retry allowlist** unchanged (failure_triage.rs:350-364 — search, search_read, read_files, file_read, graph_*, memory_search, backlog_list, current_plan, list_models, git_read); `should_auto_retry` still requires Transient + allowlist + both budgets (:375-385); per-call ≤1 via the `0` attempt argument (turn.rs:1452); the retry's stop-signal merge preserved (:1471-1475).
5. **Status + generation discipline**: `restore_status_if_ours` only-if-own with the lock dropped before the emit (finetune.rs:243-257); `spawn_server` still holds the child lock across stop→spawn→record (laya.rs:426-461); `stop_if_generation`/`owns_generation` still gate the teardown and Ready writes (laya.rs:380-405, :1021/:1042).
6. **Wiring/config surface**: handle built in build_brain_inner (main.rs:1475-1480), installed via `.with_failure_triage` (:1999), accessor `failure_triage()` (loop_impl.rs:797-799); flag writers = startup init + rewire.rs:176; `laya_flag_off` + `is_default` cover all three opt-ins (general.rs:249/:266/:277, :301-309) with the round-trip test (:1316-1354); the whole settings chain (patch fields :282/:286 → apply :601/:604 → round-trip tests :957/:976-977 → `LayaWire` :496/:499 built :907-908 → contract fixture :248-249 → frontend union/toggles/fixtures) re-read intact.
7. **The four round-1 fixes** (verified in round 2, re-read this round): HIGH-1 bad-JSON arm + both regression tests intact (turn.rs:3407-3487; tests.rs:12139-12199, :12202-12264 — call counts, wording, and row assertions all as pinned). LOW-1 slot guard (`slot_still_holds` :262-274; check→start→re-check→swap :391/:444/:451; restore-path re-check :427; snapshot :481-484). LOW-2 full corpus (decision :116-140; the export receives the full dataset :522; tests :650-669/:672-686). LOW-3 spawn_blocking (:529-538 with join-error mapping; `child.wait` :335 on the blocking thread; doc :289-296).

### 5. Test status

`cargo test -p mnemo-app` was re-run green on this exact tree AFTER the doc fix (328 lib + 13 integration / 0 failed) — the correct scope, since the only files changed this round (finetune.rs, laya.rs) live in that crate. The root `cargo test` 2633/0 (warning-free under `#![deny(warnings)]`) and frontend tsc 0 / vitest 1276/0 results from the earlier rounds carry over: this review confirmed by direct reading that NO root (src/) and NO frontend file changed since round 2 (all pins byte-exact at the same line numbers; the unchanged −44 proves no HEAD line modified anywhere). A doc-comment-only change cannot alter test outcomes, and the unchanged mnemo-app counts (328 + 13, identical to round 2's) are consistent with zero behavior delta. Not re-runnable by this read-only reviewer.

## Observations (no action required)

1. finetune.rs:557-560's marker-first rationale ("the next run would just re-train on the same rows") reads as the trigger re-firing — true under the trigger semantics, verified by both earlier rounds; no change needed.
2. Pre-existing quirk, untouched this round (proved by the unchanged −44 arithmetic): laya.rs:230's `hide_console_window` fn signature carries a stray leading indent (body/closing brace at standard depth). Cosmetic only — compiles warning-free; noted so a future reviewer doesn't misread it as new.
3. The round-2 report's own quotes of the old wording remain in `.coding/reviews/` — historical finding records, correctly not rewritten.

**Bottom line:** the round-2 LOW-1 fix is in verbatim, it is the complete and only change since round 2, both doc comments now state the trigger/dataset split that matches the shipped `finetune_decision`, and every earlier verified invariant holds. Nothing remains — the changeset is ready to land.