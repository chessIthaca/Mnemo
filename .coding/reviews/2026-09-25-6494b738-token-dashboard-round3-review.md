## Verdict: PASS

**Scope**: round-3 closing verification of all uncommitted changes on `wt/mnemo` — `git diff HEAD` (14 tracked files, stat + full diff) + git status (5 untracked: the plan file, the round-1 and round-2 reports, `DashboardView.tsx`, `DashboardView.test.tsx`, both read in full) — for plan 6494b738, against the R2-1 fix (view marker + test fixture/figure/comment) and the standing re-confirmations.

**Summary**: R2-1 is closed exactly as prescribed. The recent-events card now carries `data-testid="dashboard-recent"`, so `cardMarkup(render(), "dashboard-per-day")` is genuinely bounded — the slice runs from the per-day card's own opening `<div` to the recent card's marker, with no recent/cache content inside. Both per-day assertions are now satisfiable only from within the per-day card: the date label is the only date-formatted string in the slice (a ×1000 `fmtDay` regression fails on every machine, UTC and UTC−5 alike), and the figure discriminates — `fmtTokens(12_345)` = "12.3K" is produced nowhere else in the rendered markup (the recent row's `tokens_before` 12_000 renders "12K", not "12.3K"). The per-kind slice is unchanged in extent, no other assertion depends on marker absence or breaks, and the delta since round 2 is exactly the two described frontend edits. All re-confirmations hold. Nothing blocks landing.

### (a) The per-day slice is now genuinely bounded — R2-1's scenario closed

The rendered marker sequence is now `dashboard-view` (DashboardView.tsx:160) → `dashboard-per-kind` (:186) → `dashboard-per-day` (:208) → `dashboard-recent` (:235) → (the prompt-cache card :260 carries none, by design — nothing slices it).

Concrete walk of `cardMarkup(html, "dashboard-per-day")` (test.tsx:100-107):
- `at` = index of `data-testid="dashboard-per-day"` — on the per-day card's own tag, rendered `<div class="rounded border border-slate-700 bg-slate-800/40 p-2" data-testid="dashboard-per-day">`.
- `start = html.lastIndexOf("<div", at)` = that same opening `<div` — only the class attribute sits between it and the marker; no nested div precedes it inside the tag.
- `next = html.indexOf('data-testid="', at + marker.length)` — the per-day card's content (:208-233) contains no testid, so the first following occurrence is `data-testid="dashboard-recent"` at the recent card's opening tag. `next !== -1`: the end-of-string fallback is no longer taken.
- Slice = `[start, next)` = the per-day card's entire markup plus only the recent card's opening-tag prefix (`<div class="…p-2" `) — no recent-card CHILD content is inside: the fmtTime row, kind, before→after and saved figures all follow the recent card's completed opening tag.

The ×1000 regression now fails on every machine. The test computes its expectation with the same expression `fmtDay` uses (`new Date(19_675 * 86_400_000).toLocaleDateString()`, test.tsx:176), so locale cancels. On a UTC/en-US machine the expectation is "11/14/2023"; a regressed `fmtDay(epochDay * 1000)` renders `new Date(19_675_000).toLocaleDateString()` = "1/1/1970" (Jan 1 1970, 19,675 s past epoch) — and that regressed label is now the ONLY date-formatted string in the slice, so `toContain("11/14/2023")` fails. On UTC−5 the expectation is "11/13/2023" (the bucket 2023-11-14T00:00Z lands on Nov 13 locally) and the slice still contains only the regressed "1/1/1970" — fails there too. The escape hatch R2-1 documented (the recent row's `fmtTime(1_700_000_000)` → "11/14/2023, 10:13:20 PM", whose date prefix satisfied the assertion on UTC/CET machines) is now OUTSIDE the slice and cannot satisfy it anywhere. Dropping the per-day card remains caught by the `expect(at).toBeGreaterThanOrEqual(0)` guard.

### (b) Both per-day assertions are card-internal, and the figure genuinely discriminates

- Date label: verified in (a) — the slice's only date-formatted string is the per-day label itself.
- Figure: `fmtTokens(12_345)` = **"12.3K"** (format.ts K band at :41-52: `round1(12.345) = 12.3`, one decimal kept). Sweep of every value the fixture renders: saved_tokens_total 123_456 → "123.5K"; per-kind 111_456 → "111.5K", −4_000 → "-4,000"; per-day 12_345 → "12.3K"; recent row `tokens_before` **12_000 → "12K"** (12_000 crosses the 10_000 K threshold — the band is entered on the ROUNDED value, format.ts:41 — and `round1(12) = 12` renders an integer mantissa), `tokens_after` 900 → "900", `tokens_saved` 11_100 → "11.1K"; cache 1_000_000 → "1M", 400_000 → "400K", "40.0%", "10"/"10"; events 3 → "3"; ESTIMATED "$0.37"/"$1.08"; bar width "100%". No other rendered string contains "12.3K" as a substring ("123.5K"/"111.5K" don't: after "12"/"11" comes a digit, not a "."). The recent row still renders its own 12_000 — as "12K", distinct from "12.3K" — so the per-day figure can only come from the per-day card. The test's own comment (:177-178) claims exactly this and is accurate: even if the slice bound ever regressed again, "12.3K" appears nowhere else in the FULL markup, so the figure assertion stays discriminating regardless of slice bounds.

### (c) The per-kind slice is unchanged in extent

`cardMarkup(render(), "dashboard-per-kind")` (test.tsx:123): its `next` is still the first `data-testid="` after :186 — `dashboard-per-day` at :208, which PRECEDES the new :235 marker. The new marker sits after the per-kind slice's end and changes nothing about it; all five per-kind assertions (:124-130) evaluate over the identical extent round 2 verified.

### (d) The new marker breaks no other assertion

`cardMarkup` has exactly two call sites (:123, :175); no test slices the recent card. The recent-events test (:133-138) asserts on the FULL html ("src/agent/turn.rs", `fmtTokens(12_000)`, `fmtTokens(900)`) — the marker is an additive attribute on an existing div and removes no content, so that test passes on its own terms. No assertion depends on marker ABSENCE: the only negative assertions are `not.toContain("$123")` (:116), `not.toContain("$0.37")` (:167) and `not.toContain("never part of the metered totals")` (:191) — none involves a testid, and the marker string introduces no dollar figure or note text. A repo search for `dashboard-recent` finds exactly two occurrences: the marker itself (DashboardView.tsx:235) and round 2's fix recommendation (history). The empty-state render also carries the marker (static attribute, always rendered); no assertion touches it.

### (e) cardMarkup's doc comment is now true for every use site

"The markup of one `data-testid` card, bounded by the next card's marker" (test.tsx:95-99): the per-kind call is bounded by `dashboard-per-day`; the per-day call is bounded by `dashboard-recent`. Both use sites now have a next marker — the contract holds everywhere it is exercised. Stated plainly for the record (not a finding): the helper retains its end-of-string fallback, which would fire only for a hypothetical FUTURE call on `dashboard-recent` — now the last marked card, since the cache card deliberately carries no marker because nothing slices it. No current call hits it; a future test adding a recent-card slice should mark the cache card first.

### (f) Nothing else changed since round 2

Nothing is committed for this plan, so "diff since round 2" cannot be read off git directly; it was verified by cross-checking the current state against round 2's own citations:
- Tracked set identical: the same 14 modified files round 2 reviewed; the untracked set adds only the round-2 report itself (round 2's own output). No Rust file, no other TS/TSX.
- DashboardView.tsx: every line number round 2 cited still aligns (fmtDay :47-49; rateEstimate's `matched` :63/:67/:80; hitRate :150-153; `dashboard-view` :160; metered cards :162-173; per-kind :186; per-day :208; recent :235; cache card :260; ESTIMATED :285/:291; three-way note :296-300). The ONLY delta from round 2's description is :235 — which round 2 recorded as carrying NO marker and now carries `data-testid="dashboard-recent"` — the one described edit, applied in place on the existing div.
- DashboardView.test.tsx: `cardMarkup` :100-107 is byte-identical to round 2's citation; the first seven `it` blocks sit at the same lines (:110, :119, :133, :140, :151, :164, :170); the per-day fixture's `saved_tokens` changed 12_000 → 12_345 (:65, in place); the assertion → `fmtTokens(12_345)` (:179) plus the two-line rationale comment (:177-178) — the file grew exactly 2 lines, shifting only the final no-match test :180 → :182. Same 8 `it` blocks, same titles — no test added, deleted, or weakened; the recent fixture's `tokens_before` 12_000 (:72), `tokens_after` 900 (:73), `tokens_saved` 11_100 (:74) are untouched.
- All tracked-file content matches round 2's verbatim descriptions: the mod.rs per-day comment (epoch-DAY unit, × 86_400_000 ms not 1000), the types.rs/tauri.ts CacheEfficiency docs, the aggregate SQL, the IPC command + registration line, the registry/union edits, the vitest include line, and all four doc replacements.

### Re-confirmations (spot checks; round 2 did the deep pass)

- **Reuse constraint**: `src/memory/schema.rs` and `src/agent/turn.rs` are ABSENT from git status — the `savings_events` schema and its single writer are not in the diff; the mod.rs/types.rs hunks touch only the aggregate read path and its new types.
- **Aggregate SQL unchanged and sound** (src/memory/mod.rs:2200-2299): COALESCE totals; per-kind `GROUP BY kind ORDER BY SUM(tokens_saved) DESC, kind` (deterministic tie-break, negative kind last); per-day `created_at / 86400 … ORDER BY day`; recent `ORDER BY created_at DESC, id DESC LIMIT 50` (bounded); NULL-safe cache sums (`COUNT(cached_tokens)` counts only non-NULL rows, `SUM(COALESCE(cached_tokens, 0))`). Identical to rounds 1-2's verified statements.
- **Metered/ESTIMATED separation intact**: the two metered cards render token counts only, labeled "metered from the savings ledger" (DashboardView.tsx:162-173); the only dollar figures live in the cache section, both labeled ESTIMATED (:285, :291), never summed into metered totals; the three-way pricing note (:296-300) is intact and pinned by tests (:116, :148, :166-167, :190-191).
- **Registry ordering**: `"dashboard"` immediately before `"memory"` in the RightPanelTab union AND ALL_RIGHT_PANEL_TABS (agentState.ts) and in the registry (rightPanelViews.tsx, Gauge icon). `rightPanelViews.test.ts` is ABSENT from git status — unmodified, passes by construction (both sides grew by exactly one entry).
- **Vitest registration**: `frontend/src/components/views/DashboardView.test.tsx` is in the explicit include list (vitest.config.ts — the file's single changed line).
- **Docs**: a repo-wide "still-pending" search returns no dashboard-pending claim in any doc — every hit is unrelated code machinery, historical reviews/plans, or other backlog items' own text; README.md, docs/CONFIGURATION.md, docs/FEATURES.md and PLAN.md all describe the Dashboard as shipped, immediately before Memory, with the ESTIMATED wording intact.

### Prior non-blocking observations — still out of scope

The four carry-overs (no live refresh while the tab is open; persisted `disabledTabs` lists predating the tab; `fmtCost(0)` collapsing "no pricing" and "priced at $0"; the pre-existing `measured` doc wording from 1a37d2f) are unchanged by this fix round and still carry no concrete failure scenario beyond what rounds 1-2 already judged acceptable — none blocks landing.

### Test-evidence consistency

The claims are consistent with the code: 8 `it` blocks in DashboardView.test.tsx (the per-day test modified in place, none added or removed — matches "8 tests, count unchanged"); 92 files / 1289 tests identical to round 2's post-fix run (consistent with a modified-in-place test adding no `it`); root `cargo test` 2712/0/5 identical to round 2's — consistent with zero Rust delta since round 2; `tsc --noEmit` 0 is consistent with the change (a valid numeric-separator literal, a valid expression, two comment lines — all existing symbols).