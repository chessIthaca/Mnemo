## Verdict: PASS

Round-2 verification of plan 90fab97e (backlog 9201704f), branch wt/mnemo, reviewed as commit 06276aa (HEAD; working tree clean — no modifications, nothing untracked). Round 1's single finding LOW-1 (commit hygiene: the `.coding/` supersede set must land whole) is RESOLVED by the commit itself, and the reviewed code is unchanged since round 1 — all six verification points re-trace identically at the committed state. Nothing is outstanding.

### 1. LOW-1 RESOLVED — the commit carries the whole supersede set

`git show 06276aa --stat`: 13 files, 354 insertions, 54 deletions — exactly round 1's scope (8 modified files + 4 untracked artifacts) plus the round-1 review report itself:

- **Both halves of the supersede pair**: the modified `.coding/knowledge/decision/2027-01-05-content-index-staleness-repairs-inline-8-files-i.md` (+1 line — the front-matter `status = "superseded"`, verified on disk at line 5) AND its successor `.coding/knowledge/decision/2027-01-11-content-index-staleness-repairs-inline-under-an.md` (new, 23 lines; line 3 `supersedes = "2027-01-05-content-index-staleness-repairs-inline-8-files-i"`, verified on disk). Committed together — the pair lands whole.
- **The BUG record**: `.coding/knowledge/bug/2027-01-11-content-index-staleness-root-causes-coding-index.md` (new, 14 lines; no `supersedes` front matter — nothing to dangle).
- **Both plan files**: `.coding/plans/90fab97e.md` (the finished 7/7 plan) and `.coding/plans/e36d0a54.md` (the completed branch-prep research plan).
- **`.coding/backlog.jsonl`** (the app-managed 9201704f requeue note + the 652ae094 done-item sweep).
- **The round-1 report**: `.coding/reviews/2026-09-25-adaptive-stale-reindex-90fab97e-review.md` (61 lines, byte-identical to the copy under `.coding/reviews/`).
- The source/docs half: `src/codegraph/mod.rs`, `src/tool/agent/search.rs`, `src/tool/agent/codegraph.rs`, `src/tool/agent/search_read.rs`, `PLAN.md`, `docs/FEATURES.md`.

**No dangling supersede anywhere in the knowledge store.** Full sweep of every `supersedes = "` reference — 162 matches in 125 files across decision/ (36), spec/ (80), bug/ (37), how/ (8), plus one prose mention in the top-level `tool-call-echo-map.md` — cross-checked against complete directory listings (decision 141, spec 219, bug 239, how 58 entries): every front-matter target resolves to an existing, tracked file. This commit's own chain is fully closed: 2027-01-04 (`status = "superseded"`, tracked, untouched by this commit) ← 2027-01-05 (modified in this commit) ← 2027-01-11 (new in this commit; its body also references the 2027-01-04 file — present). The seven Laya `-2.md` superseder pairs from the branch-prep plan (e36d0a54, committed at 48fec85) are all present in both halves. The clean working tree means every store file is committed — existence in the tree equals presence in the tree the commit produces.

One corroborating observation: the per-directory `search` for `supersedes` momentarily MISSED the brand-new 2027-01-11 record's own line-3 hit (the global search had returned it minutes earlier) — the content index lagging a just-written `.coding/` file the watcher never watches, i.e. the exact open root cause the BUG record documents. The file's content was verified directly from the commit diff and on disk, so this index artifact affects nothing here; it is live evidence that the queued watcher-fix root-cause plan is warranted.
### 2. The reviewed code is unchanged since round 1

06276aa's parent is 6ad8fad — round 1's tip — and the commit contains exactly the 13 files round 1 reviewed, with the diff matching round 1's trace line-for-line (the committed line numbers match round 1's citations exactly: the budget clock/gate at codegraph/mod.rs:604-619, the reworked attempt-0 arm at search.rs:990-1003, the new tests at search.rs:3294 and mod.rs:1524). Nothing slipped in at commit time; nothing was dropped. Re-trace of the six points at the committed state:

**(a) The refreshed count can never over-report — unchanged.** `try_index` (search.rs:973-1009): `reindexed` is set only from the actual `refreshed` return (`reindex_stale_files(..., STALE_REINDEX_BUDGET).unwrap_or(0)`) when `> 0`, and surfaces only on the `Hits` arm — reachable only when the attempt-1 re-query returns `stale_paths.is_empty()` (every file on the re-served page fresh; `fts_page` recomputes mtime mismatches at :927-943). A mid-set budget overrun (e.g. 5 of 9) `continue`s to attempt 1, whose re-query still flags the un-refreshed files → `Stale { files: 4 }` — the `reindexed` value is discarded and the note is the honest walk note. Busy pass: `Ok(0)` → `unwrap_or(0)` → falls to `Stale`; the pre-existing flag is never touched.

**(b) Flag-claim + IndexFlagGuard semantics unchanged.** `reindex_stale_files` (mod.rs:587-597): the `compare_exchange(false, true)` claim is untouched — busy → `Ok(0)` returned at :595 BEFORE any flag mutation; `IndexFlagGuard` (:102-108, Drop → `store(false)`) is created at :597 after a successful claim. The budget `break` (:617-619) falls through to the `rebuild_edges()` + `Ok(refreshed)` tail (:685-688) with the guard alive until return; the `?` on `rebuild_edges` drops it on the error path; a panic unwind runs `Drop` (the additions — `Instant::now`/`elapsed` — cannot panic). Pinned by `reindex_stale_files_busy_returns_zero_and_leaves_the_flag` (mod.rs:1436) and `stale_while_an_index_pass_runs_walks` (search.rs:3392).

**(c) The out-of-root containment branch is unaffected by the budget break.** The gate (:611-619) sits at the TOP of the loop body — before the vanish-prune read (:621), before `canonicalize` (:641), and before the `canon.starts_with(&root_canon)` check (:645-648), which is unchanged. An early break leaves an out-of-root key stale exactly the way the skip branch does (caller walks). `reindex_stale_files_skips_out_of_root_keys` (mod.rs:1465) still passes a generous budget and pins the skip (planted decoy row untouched, outside content never read into the index).

**(d) Still exactly ONE re-query, max 2 fts_page calls, max 1 repair per try_index call.** `for attempt in 0..2` → at most two `fts_page` calls; the repair is gated on `attempt == 0` → at most one reindex per call; `continue` fires only inside `if refreshed > 0`; `refreshed == 0`, above-cap, and attempt 1 all fall through to the shared `return Some(FtsOutcome::Stale { … })` (:1004-1006). Attempt 1 can never re-repair; the reworked inner `if` adds no path to a second repair.

**(e) Cap 32 × the 500 ms budget bounds the worst case — unchanged.** `STALE_REINDEX_CAP = 32` (search.rs:351) + `STALE_REINDEX_BUDGET = Duration::from_millis(500)` (mod.rs:209), the gate checked BEFORE each file — overshoot bounded by one file's parse, the same single-file exposure the old 8-file pass had, now amortized under a monotonic `Instant` that starts AFTER the flag claim (:604-607). `Duration::ZERO` is a documented no-op, so the boundary is deterministic. The symbol-index sweep shares the budget (codegraph.rs:346) while its own cap stays 8 (:239, gate :340) per the separate reviewed contract (plan 55163f1e).

**(f) The new tests genuinely fail on the old code — unchanged.** `nine_stale_files_reindex_inline` (search.rs:3294-3342): 9 > the old 8 → old code never attempts the repair → `Stale` → walk; `contains("reindexed 9 stale file(s)")`, `contains("engine: index")`, and `!contains("engine: walk")` all fail on the old code, and the test also asserts the fresh-index path itself (`needle v2` served; the +5 s mtime bump rules out granularity false-negatives) so it cannot pass vacuously. `reindex_stale_files_respects_the_budget` (mod.rs:1524-1568): `Duration::ZERO` → the gate trips before file 1 → `Ok(0)` with both markers unsearchable; a generous budget → `Ok(2)` with both searchable; against the old one-arg signature it does not compile. `stale_above_the_reindex_cap_walks` (search.rs:3345-3389) re-pinned via `let cap = STALE_REINDEX_CAP` → 33 files → walk, asserting `index stale for {cap + 1} file(s)` (:3377) with the single note prefix. The remaining F10 tests (`stale_index_reindexes_and_serves_fresh_index_results`, `stale_file_beyond_the_display_cap_still_surfaces`, `stale_while_an_index_pass_runs_walks`, and search_read.rs's 1-file variant) are behaviorally unchanged.
### 3. Standing checks

- **Documentation sync — PASS.** docs/FEATURES.md (the FTS bullet: "up to 32 stale files are re-indexed within a ~500 ms budget ... beyond that ceiling, a set whose repair exhausted the budget, or a running index pass serves the authoritative walk") and PLAN.md:1369-1377 both describe the committed code accurately. README.md carries no F10 staleness wording (only the unrelated "stale context" allocation line at :28) — nothing to update there. Module docs all match: the `STALE_REINDEX_CAP` doc (search.rs:341-350), `FtsOutcome::Stale` (:845-851), `try_index` (:961-972), the search.rs call-site comment (:1379-1384), the mirrored search_read.rs comment (:268-273 — the note strings at :286/:297 are untouched, per the plan's "do not touch the note strings"), and the codegraph.rs sweep comment (:321-329). codegraph.rs's module doc "reindexes up to 8 stale files inline" (:36), its const (:239), and its `stale.len() <= STALE_REINDEX_CAP` gate (:340) remain 8 deliberately — the separate symbol-index contract (plan 55163f1e), now passing the shared budget (:346). No stale-boundary wording remnant anywhere else in the tree.
- **Multi-platform neutrality — PASS.** The commit's additions are pure std: `std::time::{Duration, Instant}` (monotonic), `Duration::from_millis`/`ZERO`, and `std::fs::File::options().set_modified` (std — SetFileTime on Windows, utimensat on Unix). No `cfg(windows)`, no platform paths, no shell syntax anywhere in the diff. Builds and behaves identically on macOS and Windows.
- **File-tools-first — PASS.** The commit is entirely source/doc/`.coding/` content edits; no shell-based file mutation anywhere in the diff.

### Test execution note

My spawn-time allow-list carries no shell tool, so I could not run `cargo test` myself. Execution evidence is the parent's green run recorded in the commit message (2714 passed / 0 failed / 5 ignored, plus 19 integration tests and 1 doc-test; both crate roots are `#![deny(warnings)]`, so green = warning-free), corroborated by the static re-trace of every new/changed test above. Live corroboration of the feature itself: my own `search` calls this round returned "note: reindexed 6 stale file(s) — serving fresh index results" and served index-engine results — the adaptive path repaired this session's own just-written `.coding/` artifacts mid-review, the exact watcher-lag scenario it exists for.

Bookkeeping carry-over from round 1 (informational, not a finding): backlog item 9201704f still reads status "pending" in the committed backlog.jsonl (its note now says "interrupted by the user, returned to queue") — the parent's finish flow owns flipping it done; not actionable from this read-only review.

Nothing is outstanding: LOW-1 resolved by commit 06276aa, the committed code is byte-identical to the round-1 reviewed content, all six verification points hold, the docs are synced, and the knowledge store is supersede-consistent.