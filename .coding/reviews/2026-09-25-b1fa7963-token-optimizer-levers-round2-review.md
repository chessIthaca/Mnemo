## Verdict: FINDINGS (0 high, 1 low)

Round-2 verification of plan b1fa7963 (token-optimizer levers, backlog e4a50d22) on `wt/mnemo` — the full uncommitted changeset (33 modified + 8 untracked files) after the five round-1 fixes. All five fixes landed correctly and completely (each verified at its cited location, below); the one LOW is a leftover instance of round-1 LOW 4(a)'s own class — a fourth code comment carrying the `[optimizer]` shorthand that the fix round's three cited sites did not cover. Nothing functionally blocks landing; fix the one comment and land.

### LOW — `frontend/src/lib/types.ts:186`: the `ContextQuality` interface doc comment still says `[optimizer] quality_score`

- **Where**: frontend/src/lib/types.ts:182-187 — the doc comment on the NEW `ContextQuality` interface (added by this changeset): "Rides `context_usage` only while the backend's `[optimizer] quality_score` flag is on." The interface-level comment is a different site from the field comment round 1 cited (types.ts:248-249, now fixed), so the fix round's three-site sweep left it behind.
- **Failure scenario**: identical to round-1 LOW 4(a) — the real TOML section is `[general.optimizer]` (OptimizerConfig is a field of the general section); a user copying the comment's form into `config.toml` gets no effect, silently. Worse than at round 1: two comments in the same file now disagree (the field comment at :247 says the correct form).
- **Suggested fix**: one word at line 186 — "`[general.optimizer] quality_score` flag is on."
- **Checked and NOT findings**: the only other literal `[optimizer]` hits in the tree are `.coding/plans/b1fa7963.md` (planning-time shorthand inside the historical plan record — not a config-copying surface, and rewriting plan history would falsify the record) and the round-1 review report quoting the then-current comments (historical). No other code comment and none of the four prose docs (README / PLAN.md / FEATURES.md / CONFIGURATION.md) carries the shorthand — all say `[general.optimizer]`.

---
### Fix 1 — LOW 1 (PLAN.md digest-on-tail claim): VERIFIED FIXED

- PLAN.md's Invariants bullet now reads: the lean-output nudge rides the *volatile tail*, popped right after the request, while the post-compaction digest is a **persistent** system note appended to the freshly-compacted conversation (the summary itself is that rewrite). docs/FEATURES.md's optimizer bullet carries the same corrected claim.
- Code matches: turn.rs:2347-2360 — `*messages = summarized;` then `messages.push(Message::system(digest + checkpoint pointer))` behind `survival`; the twin at runtime/agent.rs:1252-1268 (`self.messages = summarized; … self.messages.push(Message::system(…))`). The nudge: built per request (turn.rs:486-487), appended into the volatile tail (turn.rs:2814-2818 `append_nudge(&mut volatile_tail, nudge)`), pushed with it (:2819-2825), and popped right after the request (:514-515 — before the hard-stop early return, so always executed).
- Repo-wide "volatile tail" sweep: no other doc claims the digest rides the tail. README's feature row says only "cache-safe lean-output nudges"; CONFIGURATION.md's lever text claims the tail only for the nudge; the historical plan file b1fa7963.md (step 7) likewise describes only the nudge on the tail. No remaining doc-vs-code contradiction anywhere.

### Fix 2 — LOW 2 (undocumented `/new` quality emission): VERIFIED FIXED

- Docs: src/runtime/channels.rs:253-258 names the `/new` reset (fill 0) among the graded sites and lists the None sites (flag off, mid-stream provider-exact re-anchor, app-side spawn seed); frontend/src/lib/types.ts:245-250 mirrors it on the DTO; agentState.ts:298-302 documents the flag plus the carry semantics.
- Documented set vs code — complete match, re-enumerated from a fresh full-index sweep of every `ContextUsage {` construction site in the repo: **graded** = turn.rs:464 (top-of-loop), turn.rs:2427 (post-compaction, automatic), runtime/agent.rs:1352-1354 (post-compaction, manual `/compact`), runtime/agent.rs:1404 (`/new` — `quality_report(0, max, 0)`); **None** = turn.rs:3522 (mid-stream re-anchor), src-tauri/src/ipc/spawn.rs:389-396 (app-side seed, with its comment), src-tauri/src/ipc/events.rs:2102-2105 (direct test construction). Every other hit is a destructure or a test — no emission site exists outside the documented set.
- Regression test: src/runtime/agent.rs:1541-1586 `clear_context_grades_the_report_only_when_the_quality_flag_is_on`. **Genuine gate exercise, not a tautology**: it builds `AgentLoop::new(…).with_optimizer(Arc<RwLock<OptimizerConfig { quality_score, ..Default::default() }>>)` — the real plumbing (`with_optimizer` loop_impl.rs:782-788 stores the handle `optimizer_config()` reads, loop_impl.rs:793-798) — then asserts `quality_report(0, 128_000, 0)`: flag on → `Some`, `fill_pct == 0`, `grade == S`; flag off → `None` (exercising the actual gate at turn.rs:2953, `if !self.optimizer_config().quality_score { return None; }`). The S-at-fill-0 expectation is consistent with the published formula (fill <40% → 0 penalty; empty counters → waste 0% and stale 0% → 0 penalties → score 100 → S). Shape note, not a finding: the test pins the helper with clear_context's exact arguments (0, max, 0) rather than dispatching a full `/new` turn; the emission wiring itself is the single line at runtime/agent.rs:1404, code-visible, and this helper-level shape is what round 1's suggested fix prescribed.

### Fix 3 — LOW 3 (archive_preview served raw head/tail): VERIFIED FIXED

- src/agent/turn.rs:3105-3127: `redact_secrets(output)` (:3110 — the same `crate::tool::agent::output_compactor::redact_secrets` used inside `compact()` and by `expand_result`) runs BEFORE slicing, on **both** branches — the barely-over branch serves the whole redacted text plus pointer (:3115-3118), the head/tail branch slices the redacted text (:3111-3121).
- **No panic on char boundaries**: the function collects `served.chars()` into a `Vec<char>` and slices by CHAR index — slicing cannot land inside a UTF-8 sequence, so no input can panic it. **No off-by-one**: `chars.len() <= HEAD + TAIL` (1500 + 500) guards the whole-text branch; in the split branch `chars[..1500]` + `chars[len - 500..]` never overlap (len > 2000 ⟺ len − 500 > 1500).
- **Size/token figures still describe the archived original**: `total = output.chars().count()` and `tokens = estimate_tokens(output)` are computed from the RAW text before redaction (:3108-3109), matching what the archive row holds; `expand_result` names `fetched.char_count` "chars original" and redacts again on BOTH its serve paths (expand_result.rs:177 by-id, :220 keyword snippet) — so the preview and the retrieval never disagree about a credential, exactly as the new doc comment at turn.rs:3100-3104 states.
- **Nothing else regressed**: `archive_preview` has exactly ONE consumer — `ingest_tool_output` (turn.rs:3027), where the archive write is still awaited BEFORE the preview is built (:3025) and every error fails open to `cap_tool_result_text` (:3019-3021 no store, :3038-3043 Err). The savings row (:3028-3035) still uses before = raw-output estimate, after = preview estimate, `measured: false`.
- Docs now match code: PLAN.md's redaction invariant and FEATURES' "redacted on every model-served surface" are true of the preview too.

### Fix 4 — LOW 4 (four nits): (a) three cited sites FIXED — one uncited sibling remains (the LOW above); (b) (c) (d) FIXED

- (a) The three round-1-cited comments now say `[general.optimizer]`: channels.rs:255, types.ts:247, agentState.ts:299. The remaining instance is types.ts:186 (see the finding).
- (b) src/tool/agent/mod.rs:16-18 — the module header now carries the `expand_result` bullet ("auto-run; serves back a tool result that was archived for size — by row id, or a keyword search over the archive. Always registered, because archive rows can outlive the flag that created them").
- (c) frontend/src/components/chat/InflightBar.tsx — the dangling `/** The bar itself — receives the agent's state slice (guaranteed defined). */` is back directly above `InflightBarPanel`; `qualityColor` has its own doc block, so no dangling comment remains.
- (d) src/agent/factory.rs:1931 — the ledger entry now reads "(2027-01-25)". Chronology observation, not a finding: the ledger entries stacked above it are dated 2027-02-05 / 2026-12-08 / 2027-01-07 — those are PRE-EXISTING committed lines (git log on factory.rs shows the empty-call-sweep and git_read-status raises landed in commits a44d1c8 / 37540e0, both in HEAD), not this changeset's additions; the only date this changeset writes is the correct 2027-01-25. The uncommitted kNN spec amendment ("Amended 2027-01-11") is pre-existing bookkeeping for the separately-committed kNN work (commit 876a909, in HEAD) and is internally consistent with that item's own dated records — no stale date there either.

### Fix 5 — LOW 5 (budget-raise ledger): VERIFIED

factory.rs:1931-1934: "19_500 → 20_100 (2027-01-25): token-optimizer lever 3 (backlog e4a50d22) adds the always-advertised expand_result schema (~+418, rides every filter); measures Planning at 19_659 chars. Ceiling = measured + headroom, deliberate raise." — accurately dated, measured figure + explicit headroom policy, honest about the cause. The raise itself was judged acceptable in round 1 and is unchanged since.

---
### Hard invariants re-confirmed (regression check on the fix round)

The fix round's only behavior-affecting change is the `archive_preview` redaction; every other fix is docs/comments/tests. Spot re-verification of every invariant the round-1 report pinned:

1. **Default-off + flag-off byte-identity** — `OptimizerConfig`: all six flags default `false`; `is_default()` covers flags AND knobs so the `[general.optimizer]` section is omitted while untouched (general.rs:339-427, Default at :429-433). Gates re-read: `quality_report` (turn.rs:2953), `nudge_for_request` (both nudge flags off → `None`, :2978-2980), `ingest_tool_output` (flag off / below `archive_min_chars` / no store → `cap_tool_result_text`, turn.rs:3019-3024), read_files delta gate (read_files.rs:305-312, flag-off byte-identity pinned by its module tests, :1306/:1333/:1357), shell compression gate (shell.rs:650). No existing truncation/compaction/cap test was modified to accommodate a lever.
2. **Archive-before-substitution + fail-open** — the archive write is awaited BEFORE the preview is built and served (turn.rs:3025-3036); Err → logged + cap fallback (:3038-3043); no store → cap (:3019-3021). `checkpoint_before_compaction` archives before the summary and fails open (:3053-3094 — an error returns `None`, compaction proceeds, and the ledger row honestly records before==after / saving 0).
3. **Tools store-free; the turn loop is the single ledger writer** — tools emit `data.savings {kind, detail, tokens_before, tokens_after, measured:false}` (src/tool/agent/optimizer.rs:29-43); `expand_result` READS the store for retrieval only and reports its own savings in `data` (expand_result.rs:196-197) — it never writes ledger rows; `AgentLoop::record_savings_event` (turn.rs:2899-2939, fire-and-forget `tokio::spawn` + log-on-Err, mirroring `record_stats_row`) is the single writer.
4. **Token counts are estimates** — `estimate_tokens` is `chars().count() / 4` with the measured-vs-metered contract documented and unit-pinned (src/tool/agent/optimizer.rs:13-22, :49-54); every ledger write passes `measured: false` (turn.rs:3034, :3086; the tool helper :41).
5. **Already-sent conversation prefix never mutated** — the digest is appended to the freshly-compacted conversation (turn.rs:2347-2360, runtime/agent.rs:1252-1268), where the summary itself is the pre-existing rewrite; the nudge rides the volatile tail, popped unconditionally right after the request (turn.rs:2814-2825 → :514-515). The provider prefix cache stays valid on every path.
6. **Quality wire backward compatible** — `#[serde(default, skip_serializing_if = "Option::is_none")]` (channels.rs:585) + the bare/graded round-trip test (channels.rs:1772-1816: `None` → no `quality` key on the wire, graded → full five-field object that round-trips). The pre-existing frontend fixture `event-context-usage.json` is tracked and byte-identical (absent from the working-tree changes); the new `event-context-usage-quality.json` matches the reducer test's expected shape exactly (grade C, fill 75, waste 1200, stale 30, decisions 50 @ used 6000/max 8000); absent quality = "unchanged" in the reducer (agentEventReducer.ts carry, pinned by the new keeps-the-grade ipc-contract test); the src-tauri construction sites stay additive `quality: None` (events.rs test vector, spawn.rs seed).
7. **Multi-platform neutrality, file-tools-first, subagent isolation** — the fix round introduced no platform-specific code (`Vec<char>` slicing, pure redaction), no shell-based file mutation anywhere in the changeset, and no shared mutable lever state across agents (fresh `ReadCache` per tool build, unchanged).

### Test-evidence consistency

As the read-only reviewer I cannot execute the suites; the claims were checked against the code instead. The new `clear_context_grades…` test exists and uses real, correctly-shaped APIs (AgentLoopConfig fields, `with_optimizer`, `QualityGrade::S`); the frontend changes are comment-only plus additive tests, with the one new type import actually used (tsc-clean plausible); the ipc-contract quality tests pollute no later quality-free assertion (the tests that follow assert compacted/compact-started, not contextUsage shapes); the new fixture matches the reducer expectations field-for-field. Nothing in the code contradicts the claimed 2711 root / mnemo-app compile / tsc 0 / 1281 vitest results, and nothing in the changeset depends on a test I found missing.

### Scope confirmation

No dashboard/aggregates/IPC scope creep — backlog 652ae094's remaining scope (savings_stats rollups, IPC surface, DashboardView) is correctly untouched. The changeset is exactly what round 1 assessed: the six levers, the `savings_events` + `tool_result_archive` recording path, the always-registered `expand_result`, the quality wire + frontend ctx-popup display, and the docs.

Recommendation: apply the one-word fix at frontend/src/lib/types.ts:186 (`[optimizer]` → `[general.optimizer]`) — optionally re-run the frontend vitest/tsc for form — and land.