## Verdict: FINDINGS (0 high, 1 low)

Reviewed ALL uncommitted changes on `wt/mnemo` (git diff HEAD + untracked; 21 modified + 1 untracked plan artifact): the Settings → Pricing → Savings rename plus the token-optimizer lever surface (plan febcd6f5, backlog ebe21e3c).

Summary: the change is correct and scope-faithful end to end. All six required semantics were verified against the actual code (details below). The src-tauri half — which cannot be compiled on this machine — was reviewed at inspection level and is compile-plausible: the import path, the new pub field, the builder line, all three `GetSettingsGeneral` construction sites, the four `DEFAULT_*` consts, and the serde_json assertions all check out; no other construction site misses the new required field. The renames are complete (no stale section-id "pricing" anywhere in shipped code; all remaining `pricing` matches are the config/store field, correctly kept per the scope boundary). One stale user-facing doc reference remains: docs/CONFIGURATION.md:7 still says "Settings → Pricing model field" — the section is now Savings.

Findings: LOW-1 — stale section-name reference in the CONFIGURATION.md paragraph describing the model combobox.

---

### LOW-1 — stale "Settings → Pricing" reference in docs/CONFIGURATION.md:7

- **Anchor:** docs/CONFIGURATION.md:7 (the `endpoints.toml` paragraph).
- **What:** The paragraph still reads "`endpoints.toml` (providers + models + pricing — the **Settings → Pricing** model field is an editable combobox listing the models the configured endpoints serve …". The section this change renames is now Savings (nav label at frontend/src/components/settings/types.ts:144; component heading at SavingsSection.tsx:201), so the model combobox lives in Settings → Savings.
- **Failure scenario:** A user following CONFIGURATION.md to find the model combobox opens Settings looking for a "Pricing" section and finds none — the nav item is now "Savings".
- **Fix:** One string replace in that paragraph: "Settings → Pricing model field" → "Settings → Savings model field". (The levers paragraph in the same file, plus README.md:45/:163-167 and docs/FEATURES.md:56, were all synced correctly; this one occurrence on the same line 7 was missed. Repo-wide it is the only non-historical "Settings → Pricing" left — every other match is a historical .coding record, which is correct to leave.)
- **Why low:** doc-only staleness inside the change's own doc-sync scope; no code impact.
---

## Verified semantics (each checked against the actual code, not the description)

1. **Absent = keep — HOLDS.** All eleven `optimizer_*` fields on `SettingsSaveDto` are `Option<...>` with `#[serde(default)]` (src/config/settings_dto.rs:299-345), and the applicator only writes when `Some` (src/config/settings_dto.rs:672-708): each flag `if let Some(v) = patch.optimizer_* { ...optimizer.<field> = v; }`, each knob likewise. `Some(vec![])` on `optimizer_compress_extra_commands` clears the list (the `map/filter/collect` over an empty iter yields `[]`), and entries are trimmed with blanks dropped on apply (:702-708, `cmd.trim()` + non-empty filter). The new test `optimizer_patch_applies_knobs_and_extra_commands` pins trim/blank-drop and knob application; `optimizer_patch_applies_levers` pins flag application with absent knobs keeping current values. An untouched Savings section never saves at all — the dialog OK saves only dirty sections, and `dirty` requires a changed snapshot.

2. **Default-omitted rule survives — HOLDS.** `OptimizerConfig::is_default` (src/config/general.rs:414-426) is value-based and untouched by this diff; `#[serde(default, skip_serializing_if = "OptimizerConfig::is_default")]` on `GeneralSection.optimizer` (general.rs:128) is untouched; the `DEFAULT_*` consts (:401-408) are untouched. `git diff HEAD` touches neither src/config/general.rs nor anything the omission depends on. Critically, the Savings section always sends the loaded (possibly default) values, and explicitly-written default values still omit because the check is value-based — the settings_dto tests pin exactly this ("a config still on defaults keeps it omitted").

3. **Live, no restart — HOLDS.** The new rewire call (src-tauri/src/ipc/rewire.rs:189) sits inside the `if let Some(factory) = &state.runtime.factory` guard (:169-190), where `cfg` is `&mnemo::config::Config` (param at :30) — `cfg.general.general.optimizer` is the same double-`.general` path already used at rewire.rs:58-63/:98 and main.rs:1814-1815, and `set_optimizer_config(&self, cfg: OptimizerConfig)` (src/agent/factory.rs:592-594) writes *through* the shared `Arc<RwLock<OptimizerConfig>>` (the same Arc seeded at main.rs:1814, wired via `with_optimizer_config` at :2017, and cloned `Arc::clone(&self.optimizer)` into every lever-bearing build: AgentLoop factory.rs:864, ReadFilesTool :1036, ShellTool :1055). `rewire_vision_embedder_and_classifier` is called from `save_settings` at src-tauri/src/ipc/settings.rs:1017, AFTER `persist_and_reload` (:1004) — so the pushed value is the just-saved/reloaded config. The turn loop reads the mirror per call (`optimizer_config()` at src/agent/loop_impl.rs:793-798; call sites turn.rs:1897/:2162/:2953/:2976/:3018). Nothing else in the app caches a plain `OptimizerConfig` copy at startup — the only other `.with_optimizer(Arc::new(...))` sites are tests (src/runtime/agent.rs:1569 is a unit test with a MockProvider; read_files.rs:1305-1341 and shell.rs:922-989 are test helpers). Worktree/Run-All agents share the same factory Arc (register_agent_tools uses `self.optimizer` regardless of root spec), so they observe the rewire too.

4. **One patch, both halves — HOLDS.** SavingsSection.handleSave sends `pricing: cleaned` AND all eleven optimizer fields in one `saveSettings` call (SavingsSection.tsx:161-180), and the dirty snapshot covers both halves: load sets `setSnapshot(JSON.stringify({ pricing: p, flags: f, knobs: k, extra }))` (:108) and the dirty check compares the same shape against the live state (:125-127); the post-save snapshot (:182-185) rebuilds it identically. Key order is stable (all four objects are constructed pricing→flags→knobs→extra; the knob object literal order matches between load and save), so `JSON.stringify` comparisons don't spuriously trip. A lever-only edit therefore dirties the section and gets saved by the dialog OK.

5. **Rename completeness — HOLDS.** No section-id "pricing" remains anywhere in shipped code: the union member, `isSettingsSectionId`, nav entry, icon/ref maps, the section block in SettingsDialog.tsx, the deep-link doc comment in useAgentStore.ts, and the dirty-ids test expectation are all "savings". The only `openSettings("<id>")` callers pass "embeddings" (StatusBar.tsx:817/:839) — no stale "pricing" deep-link. Conversely, every remaining `pricing` match is the config/store field and is correctly kept: `SettingsSaveDto.pricing`, `AppSettings.pricing`/`PricingEntry`, `setPricing`, the export/import bundle's `pricing` + its "pricing must be an array." validation message, the fixture's `"pricing": []`, and row-level copy ("No pricing rows", "Add pricing row", "Remove pricing row" aria-label) — exactly the scope boundary the plan's revision note demands. The `?raw` import in the renamed test points at `./SavingsSection.tsx?raw` and no reference to PricingSection.tsx remains outside historical .coding artifacts.

6. **No behaviour change when levers are off — HOLDS.** The diff adds no code to any lever's execution path: the backend changes are DTO fields + applicator (config surface only), the factory change is the new setter (writes the existing mirror), the app change is the one rewire push, and main.rs (the mirror creation/wiring) is NOT in the changed-file set — it predates this change from the merged b1fa7963 work. `#![deny(warnings)]` green cargo test (implementer-run, unpiped, exit=0) confirms the mnemo crate compiles warning-free.

---

## src-tauri inspection assessment (cannot compile on this machine — Application Control policy)

I could not compile `mnemo-app`, so both files were verified by inspection, and I am *confident but not certain* they compile. Everything the task asked me to check verified:

- **rewire.rs:189 — `cfg` scope and path:** the function signature is `pub(super) fn rewire_vision_embedder_and_classifier(app: &AppHandle<IpcState>, state: &State<'_, IpcState>, cfg: &mnemo::config::Config)` (:28-30); `cfg.general.general.optimizer.clone()` follows the identical path already exercised at :58-63 (`cfg.general.general.bundled_embedding_model`) and :98 (`cfg.general.general.laya`), and matches main.rs:1814-1815. `Config.general` → `.general` (`GeneralSection`) → `optimizer: OptimizerConfig` (pub field, general.rs:129). `OptimizerConfig` derives Clone. The call sits inside the `if let Some(factory)` guard, after the Laya setters, before the closing brace.
- **settings.rs — import/field/builder:** `use mnemo::config::general::{OptimizerConfig, UiConfig};` (a valid module path — `UiConfig` was already imported from it; `OptimizerConfig` is a `pub struct` also re-exported at src/config/mod.rs:28). `pub optimizer: OptimizerConfig` on `GetSettingsGeneral` (:625, doc comment included) — `OptimizerConfig` derives `Serialize` (general.rs:344), so the wire struct serializes. Builder line `optimizer: config.general.general.optimizer.clone()` (:924).
- **All `GetSettingsGeneral` construction sites:** exactly three in the repo — the builder (:905-924) and the two hand-written test constructors (:1428, :1589) — all three carry `optimizer`, the tests with `OptimizerConfig::default()` (reachable via `use super::*` from the file-top import). No other site misses the required field.
- **The four consts:** `DEFAULT_ARCHIVE_MIN_CHARS`, `DEFAULT_COMPRESS_MIN_CHARS`, `DEFAULT_LEAN_OUTPUT_FILL_PCT`, `DEFAULT_NUDGE_COOLDOWN_REQUESTS` all exist as `pub const` in `impl OptimizerConfig` (general.rs:401-408) with those exact names, reachable as `OptimizerConfig::DEFAULT_*`.
- **serde_json assertions:** `assert_eq!(v["general"]["optimizer"][key], serde_json::json!(OptimizerConfig::DEFAULT_* as u64))` is Value-vs-Value; `usize`/`u8`/`u32` serialize as JSON integers and `as u64` produces the same PosInt, so equality holds; `v[..]["compress_extra_commands"] == Null` is correct because `compress_extra_commands` carries `skip_serializing_if = "Vec::is_empty"` (general.rs:394) and the default is empty — which also matches the fixture (dto-get-settings.json's optimizer block deliberately omits the key) and the frontend's optional `compress_extra_commands?: string[]` with its `?? []` read.

---

## Process notes

- **The `git mv` renames were the right call.** The file tools have no rename/delete primitive — file_edit/file_write can only edit or create — so `git mv` is the sanctioned fallback here, with the justification stated. The resulting `R` entries are complete: content follows the rename (status shows `RM` for both files), the test's `?raw` import path was updated, and repo-wide no stale reference to the old file names remains outside historical .coding records (which are correct to leave).
- **backlog.jsonl hunk** (adds `"deleted_at":1790372361` to the landed item 9201704f) is app-side bookkeeping of an already-landed, reviewed item — unrelated to this plan's code, harmless, consistent with the backlog lifecycle.
- **Observation (not a finding):** the SavingsSection.test.ts comment "a lever or knob added backend-side without this file listing it fails loudly here" overstates slightly — the test pins the six levers/four knobs the UI already knows; a *newly added* backend lever would not fail it (there is no cross-check enumerating the DTO's fields). The ipc-contract fixture partially covers the shape (4 of 10 keys type-asserted). Static-contract test limitation consistent with the project's node-env convention; no functional impact.
- **Observation (not a finding):** the optimizer knobs get no backend range validation (unlike some other patch fields). Out-of-range values (e.g. fill pct > 255, a negative) fail serde deserialization of the whole patch and surface as an error toast — fail-closed, and no weaker than hand-editing config.toml, whose schema uses the same `u8`/`usize`/`u32` types.

## Constitution / project checks

- **Doc comments:** every new pub item is documented (`set_optimizer_config`, the DTO fields, the wire field, SavingsSection and its module consts, the tests). ✔
- **Multi-platform neutrality:** nothing platform-specific in the diff — no `cfg(windows)`, no Windows paths or shell syntax in app/library code; the frontend and Rust changes are neutral. ✔
- **File-tools-first:** all content edits conform; only the two renames used shell (`git mv`), justified above. ✔
- **Docs sync:** README/FEATURES/CONFIGURATION updated accurately — except LOW-1 (one stale section-name reference in CONFIGURATION.md:7). PLAN.md has no Pricing/Savings references, so nothing needed there. ✔ (modulo LOW-1)

## Verification evidence (judged, not re-run — read-only review)

`cargo test` green (2717 + 19 integration + 1 doc, unpiped, exit=0, under `#![deny(warnings)]`) is plausible and consistent with what I read — but note it does NOT cover `mnemo-app` (src-tauri), which cannot be compiled on this machine and was assessed by inspection only. Frontend numbers are internally consistent: 92 files (PricingSection.test.ts → SavingsSection.test.ts keeps the count), 1295 = 1289 baseline + 6 new tests (SavingsSection.test.ts 4 → 10), and every static-contract string in the new/updated tests matches the actual SavingsSection.tsx source line-for-line (checked: the dirty expression, the load/save snapshot shapes, the `BOOL_FIELDS` loop, the per-field save patch lines, the extra-commands join/split/trim/filter, the null-draft render guard, and the `if (!flags || !knobs) return false;` save guard).