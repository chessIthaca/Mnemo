## Verdict: PASS

Round-3 closing verification of plan febcd6f5 (backlog ebe21e3c) on `wt/mnemo`, on top of round 1 (`.coding/reviews/2026-09-25-febcd6f5-savings-section-review.md`, FINDINGS 0 high / 1 low) and round 2 (`.coding/reviews/2026-09-25-febcd6f5-savings-section-round2-review.md`, FINDINGS 1 high / 0 low).

**Round-2's HIGH-1 is fixed exactly as prescribed, nothing else drifted since round 2, the four-site inventory is complete, the golden-fixture equality holds value-for-value, and all six required semantics still hold at their anchors. No new findings.** The only unverifiable-by-compilation residue is the standing one: `mnemo-app` (src-tauri) cannot be compiled on this machine (Application Control policy), so the fix — like the rest of src-tauri — is verified at inspection level, the same level at which round 2 found HIGH-1. Details below.

---

### 1. Round-2 HIGH-1 fix — landed exactly as described

- **contract_fixtures.rs:26** — `use mnemo::config::{LayaMode, OptimizerConfig, SafetyMode};` (verified in the file). The path resolves: `OptimizerConfig` is re-exported at `mnemo::config` (`pub use general::{ … OptimizerConfig … }`, src/config/mod.rs:26-30, verified in the file). Round 2 suggested `mnemo::config::general::OptimizerConfig`; the implemented `mnemo::config::OptimizerConfig` is equally valid and matches the file's existing brace-import style (`use mnemo::runtime::channels::{…}`, `use mnemo::backlog::{…}`).
- **contract_fixtures.rs:254** — `optimizer: OptimizerConfig::default(),` sits directly after `auto_compact_on_plan_complete: false,` (:253); the `GetSettingsGeneral` literal (:235) now closes at :255. Exactly the one-field + one-import fix, nothing else in the file touched (full-file read: `assert_fixture` :58-81 unchanged; the file is 392 lines = round-2's state + the one inserted line; the sample is otherwise identical to round 2's description).

### 2. Nothing else drifted since round 2

- **Diff base unchanged:** HEAD is still 7591538 — no commits since round 2 recorded the same base.
- **Changed-set delta is exactly the fix + bookkeeping:** 22 modified files = round 2's 21 + `contract_fixtures.rs` itself; 3 untracked = round 2's 2 (the plan artifact + round 1's report) + round 2's own report. Nothing extra.
- **Round-1 LOW-1 fix intact:** docs/CONFIGURATION.md:7 reads "the **Settings → Savings** model field is an editable combobox"; the levers paragraph (:9) names Savings and documents the live no-restart behavior.
- **Every anchor rounds 1-2 recorded re-verified exact this round** (read, not trusted): settings.rs:15 (import), :565 (response field), :592-626 (struct def, `pub optimizer: OptimizerConfig` at :625 with doc comment :620-624), :905-924 (builder, `optimizer: config.general.general.optimizer.clone()` at :924), :1004 (`persist_and_reload`) → :1017 (rewire call, after it), :1428/:1589 (both test ctors use `OptimizerConfig::default()`), :1495-1518 (flag + `DEFAULT_*` knob + Null `compress_extra_commands` assertions); rewire.rs:169-190 (guard) + :189 (push); settings_dto.rs:299-345 (eleven `Option` fields) + :672-708 (applicator); general.rs:128 (skip), :394 (skip-while-empty), :401-408 (consts), :414-426 (`is_default`), :429-444 (`Default`) — and general.rs is NOT in the changed set, correct per semantics 2; factory.rs:585-594 (doc-commented setter writing through the shared Arc); SavingsSection.tsx:122/:139-141/:152/:177-190/:194-196/:223-225; types.ts:23/:141-143/:516/:587/:611-612; SettingsDialog.tsx:72/:125/:423-429; useAgentStore.ts:414; types.test.ts:72-74; DashboardView.tsx:180-182; README.md:45/:163-169; docs/FEATURES.md:56.

### 3. Four-site inventory — complete, no fifth site

Repo-wide literal search for `GetSettingsGeneral` across all `*.rs` (whole tree, `tests/` included — round 2's process note followed): 7 matches — **exactly four construction literals**: settings.rs:905 (builder, `optimizer` :924), settings.rs:1409 (test ctor, :1428), settings.rs:1567 (test ctor, :1589), contract_fixtures.rs:235 (fixture sample, :254). The other three matches are the response field decl (settings.rs:565), the struct definition (:592), and the import (contract_fixtures.rs:35) — not constructions. `GetSettingsGeneral` derives only `Debug, Clone, Serialize` (settings.rs:591) — no `Default`, so no `..Default::default()` functional-update escape could exist anywhere. All four sites carry the field; E0063 is closed at inspection level.
### 4. Golden-fixture equality — holds value-for-value

- **Comparison mechanics** (read in the file): `assert_fixture` serializes via `serde_json::to_value` (:60), parses the fixture as a `serde_json::Value` (:70), and compares with `actual != expected` (:71) — Value-vs-Value, key-order-insensitive. A `usize`/`u8`/`u32` knob and a JSON integer compare as equal `Value`s.
- **`OptimizerConfig::default()`** (general.rs:429-444, built from the `DEFAULT_*` consts :401-408): six `false` flags, `archive_min_chars` 20 000, `compress_min_chars` 2 000, `lean_output_fill_pct` 25, `nudge_cooldown_requests` 10, `compress_extra_commands: Vec::new()`.
- **The fixture block** (frontend/src/lib/ipc-fixtures/dto-get-settings.json:25-36): exactly `delta_reads: false`, `compress_output: false`, `archive: false`, `compaction_survival: false`, `quality_score: false`, `lean_output_nudge: false`, `archive_min_chars: 20000`, `compress_min_chars: 2000`, `lean_output_fill_pct: 25`, `nudge_cooldown_requests: 10` — and **no** `compress_extra_commands` key, which matches: `#[serde(default, skip_serializing_if = "Vec::is_empty")]` (general.rs:394) omits the empty Vec on serialize. No `rename_all` on the struct (general.rs:344-346), so serialized key names equal the Rust field names, which equal the fixture keys. Ten keys on both sides, identical values — the fixture equality passes with no further fixture edit.
- The rest of the sample matches the rest of the fixture (all pre-existing and untouched by this diff; spot-checked: config_dir, context, laya block, safety, null-emitting optionals, `pricing: []` — the config field, correctly kept), so the full `GetSettingsResponse` comparison holds.

### 5. Six semantics — re-spot-checked at current anchors, all HOLD

1. **Absent = keep — HOLDS.** Eleven `optimizer_*` patch fields, all `Option<…>` + `#[serde(default)]` (settings_dto.rs:299-345); applicator writes only on `Some` (:672-709); `Some(vec![])` clears the extra-command list (the `map/filter/collect` over an empty iter yields `[]`), entries trimmed with blanks dropped on apply (:702-708). SavingsSection's null-draft guard (`if (!flags || !knobs) return false;`, SavingsSection.tsx:152) keeps a pre-load editor from saving over real config.
2. **Default-omitted rule survives — HOLDS.** `src/config/general.rs` is not in the changed set; `is_default` is value-based (:414-426), `Default` uses the consts (:429-444), the skip sits at :128 — so the always-send loaded values from the Savings section still omit `[general.optimizer]` while every value is default.
3. **Live, no restart — HOLDS.** rewire.rs:189 pushes `cfg.general.general.optimizer.clone()` inside the `if let Some(factory)` guard (:169-190), called from `save_settings` at settings.rs:1017 after `persist_and_reload` (:1004) — the pushed value is the just-saved/reloaded config; the setter (factory.rs:592-594) writes through the shared `Arc<RwLock<OptimizerConfig>>` read per tool call.
4. **One patch, both halves — HOLDS.** handleSave sends `pricing: cleaned` AND all eleven optimizer fields in ONE `saveSettings` call (SavingsSection.tsx:177-190); the load snapshot covers all four objects (:122), the dirty check compares the same shape (:139-141) — a lever-only edit dirties the section — and the post-save snapshot is rebuilt identically (:194-196).
5. **Rename completeness — HOLDS.** Union member (types.ts:23), nav id/label/keywords (:141-149), `isSettingsSectionId` (:516), icon map (SettingsDialog.tsx:72), ref map (:125), aria block (:423-424), `setSectionDirty("savings", …)` (:429), the deep-link doc (useAgentStore.ts:414), and the dirty-ids test (types.test.ts:72-74) are all "savings". Repo-wide sweeps find no `PricingSection` and no section-id/`Settings → Pricing` in shipped code — every remaining match is a historical `.coding/` record (correct to leave). Every remaining `pricing` in shipped code is the config/store field — `settings.pricing`/`setPricing` (App.tsx:306-307), the draft serializer + `"pricing must be an array."` validation (types.ts:587/:611-612), `Settings.pricing`/`SaveSettingsDto.pricing` (tauri.ts:584/:673), the Stats/Dashboard cost paths, the AdvancedSection export/import bundle, and the fixture's `"pricing": []` — exactly the plan's scope boundary. The only `openSettings("<id>")` callers pass "embeddings" (StatusBar.tsx:817/:839).
6. **No lever's execution path changed — HOLDS.** The changed src/ set is factory.rs (the setter only) + settings_dto.rs (DTO fields + applicator); no lever execution file (turn.rs, read_files, shell, loop_impl, general.rs) is in the set, and main.rs (mirror creation) is untouched — backend surface is config surface plus one rewire push, so with all levers off behavior is byte-identical.

### 6. Constitution / project checks

- **Doc comments:** the fix's only pub-visible item is none (a test fixture field), and the change's new pub items remain documented (setter factory.rs:585-591, DTO fields, wire field settings.rs:620-624, tauri.ts interfaces :480-497/:620-641, SavingsSection consts, tests). ✔
- **Multi-platform neutrality:** the two-line fix is a pure Rust import + struct field — nothing platform-specific; the whole changeset (unchanged since round 2) is neutral. ✔
- **File-tools-first:** the fix is two content edits in a file the file tools handle; no new renames since round 2; the two `git mv` renames remain the sanctioned fallback (no rename/delete primitive in the file tools), with content following (status `RM`). ✔
- **Docs sync:** README.md:45/:163-169, docs/FEATURES.md:56, docs/CONFIGURATION.md:7 + levers paragraph, and DashboardView.tsx:180-182 all name Settings → Savings consistently; ModelCombobox.tsx / modelOptions.ts / modelOptions.test.ts doc comments updated to "Savings section" while genuinely-pricing-row copy ("No pricing rows" :321, "Remove pricing row" aria :375, "Add pricing row" :395) is correctly kept; PLAN.md needs nothing; endpoints.toml and `[[pricing]]` stay untouched per the scope boundary. ✔

### 7. What was NOT verified, and test evidence

- **No compilation or test execution by this reviewer** (read-only review session), and — the standing constraint — `mnemo-app` (src-tauri) cannot be compiled on this machine at all (Application Control policy). The fixture fix, the rewire push, the wire field, and all four construction sites are **inspection-verified only**. That is the same verification level at which round 2 found HIGH-1, and this round applied exactly that level again: the specific E0063 round 2 identified is closed (all four sites carry the field; the import resolves through the verified re-export; the fixture matches `Default` value-for-value), and no fifth site exists. Remaining residual risk is the generic one for any inspection-verified code, with no concrete candidate defect found.
- **Implementer's post-fix evidence (judged, not re-run):** root `cargo test` 2717 + 19 integration + 1 doc, unpiped, exit=0 under `#![deny(warnings)]` (covers the `mnemo` crate — settings_dto applicator tests, factory setter — NOT `mnemo-app`, exactly the blind spot that hid HIGH-1); `npx tsc --noEmit` exit=0; `npx vitest run` 92 files / 1295 passed (1289 baseline + 6 new lever contracts; SavingsSection.test.ts 4 → 10 — 136 lines, 4 combobox + 6 lever contracts, `?raw` import pointing at `./SavingsSection.tsx?raw`, and every static-contract string re-checked line-for-line against the component this round). Internally consistent with the change.

### 8. Process notes

- Round 2's construction-site rule (grep the literal `StructName {` across the whole app crate, `tests/` included) was followed for this verification and is now satisfied: four sites, all carrying the field. Keep the rule for any future wire-struct field addition.
- Round 1's two non-finding observations remain accurate and still warrant no finding: the SavingsSection.test.ts "fails loudly" comment slightly overstates its coverage (a *newly added* backend lever would not trip it; the ipc-contract frontend half pins 4 of the 10 optimizer keys, :481-484, same class of limitation), and the optimizer knobs carry no backend range validation (out-of-range values fail serde deserialization of the whole patch — fail-closed, no weaker than hand-editing config.toml).
- Round 1's and round 2's reports are uncommitted bookkeeping in the set, as this one will be — expected.

**Bottom line:** the round-2 defect is fixed exactly as prescribed, the changeset is otherwise unchanged since round 2's full verification, and nothing new surfaced. Nothing remains beyond what is now resolved — the verdict is **PASS**.