## Verdict: PASS

Round-2 verification of commit 8c7200a (wt/mnemo HEAD, working tree clean) for plan 2a2ae0aa. All five round-2 questions are established: LOW-2's both halves are fixed; the protected-path banner write corrupted nothing; the committed source is the code round 1 assessed (no post-review source edit); LOW-1's deferral is coherent, documented, and sound; and the standing project checks hold. Three non-blocking observations are recorded (none is a defect of this change; none requires action before landing).

## Scope reviewed

- Commit 8c7200a via `git show` (stat + full diff): exactly 9 files — `src/tool/agent/search.rs`, `src/codegraph/watcher.rs`, `docs/FEATURES.md`, and the `.coding` side-car (backlog.jsonl, the supersede-pair BUG knowledge records, plans 2a2ae0aa.md + 7b5b5244.md, the round-1 review report).
- Working tree clean (`git status` empty, exit 0); `git log` shows 8c7200a is the ONLY commit since round 1's baseline d8152f7 — the commit IS the entire post-round-1 delta.
- Committed source read at HEAD; symbol-graph lookups confirm the committed line spans (`is_coding_data_file` search.rs:92-109, `watcher_reindexes_on_new_coding_artifact` watcher.rs:452-490).
- Memory store including superseded records; the backlog queue; the `.coding/plans` inventory (731 historical plan files — the house keeps every plan; only these two concern this work).
- `cargo test` cannot run from this session (no shell in my allow-list); the parent's twice-run evidence is accepted and found consistent by trace (see Test evidence).

## 1. LOW-2 (stale duplicate plan artifact) — RESOLVED, both halves ✓

**(a) Banner ✓** — `.coding/plans/7b5b5244.md:3-8` now opens the document with an explicit `> **ABANDONED 2027-01-25**` banner: it states the plan was the `bug_fixing` first cut abandoned at creation (re-typed `kind=implementation`), points at **2a2ae0aa** as the plan that executed the work, says **"Nothing shipped under this plan id"**, names `.coding/plans/2a2ae0aa.md` as the live record, instructs **"do not act on this file"**, and carries the provenance attribution "(Reviewer LOW-2, plan 2a2ae0aa.)". It sits before every content section, so no future session can act on the file without seeing it. The body below is unchanged in substance from what round 1 assessed (all steps still `[ ]`, same Goal/Kind/Context/Detailed-steps/Bug structure).

**(b) Memory ✓** — `memory_search` with superseded records included shows the duplicate PLAN record **702a0358** marked `[superseded]`, and its successor (2eec645a) titled "PLAN: **(abandoned)** Watcher root cause — re-filed bug_fixing -> implementation as 2a2ae0aa" — an abandonment record, not a second live plan. Exactly **ONE live PLAN record** remains for the executed work: **9dea4744**, matching `.coding/plans/2a2ae0aa.md`'s goal text.

**No contradictory pair lands in the mergeable side-car**: the two plan files no longer contradict (the banner resolves which is live), and the BUG knowledge records land as a proper supersede pair — the old record gained only `status = "superseded"` in its header (body untouched), the successor carries the `supersedes` pointer plus the FIXED state — exactly the convention round 1 invoked.

## 2. Protected-path write integrity — VERIFIED ✓ (one pre-existing cosmetic blemish documented)

The banner insertion (approval-gated shell into `.coding/plans/` — the path the file tools refuse by design) left the document intact:

- **Body unchanged apart from the banner**: the inserted text is banner-only (lines 3-8) plus the expected cosmetic double blank line (lines 9-10); every body section parallels the executed plan's carried text and matches round 1's description.
- **Line endings**: LF throughout — a CR sweep over the 731 plan files found **zero** carriage returns anywhere (the plans directory's house style); nothing was silently converted, and the banner text itself is ASCII-only (`--` in place of em-dashes — encoding-safe by construction).
- **Valid, readable markdown** — renders cleanly end to end.
- **Pre-existing blemish (NOT introduced by this write)**: line 46 renders two U+FFFD replacement characters where the executed plan's parallel text has a plain space ("…is_coding_data_file(rel)` □□ so a…"). This predates the banner write, by elimination: a whole-file re-encode (the only mechanism by which a top-of-file insert could alter line 46) would have mangled EVERY non-ASCII character identically — but all other em-dashes in the file (dozens) are intact, so the write preserved the body bytes and the blemish was already in the abandoned plan's authored text. It is cosmetic, sits in a tombstone file marked "do not act on this file", and requires no action. (The live record `.coding/plans/2a2ae0aa.md` is clean.)

## 3. Committed code == round-1 assessed content — VERIFIED ✓ (no post-review source edit)

- The commit touches exactly two source files (`search.rs`, `watcher.rs`) + `docs/FEATURES.md` + `.coding` artifacts (the stat is definitive); no other commit exists between round 1's baseline (d8152f7) and 8c7200a; the tree is clean, so disk == HEAD.
- Every round-1 anchor resolves at the SAME line numbers in the committed source — any post-review edit in these regions would have shifted or altered them:
  - `is_coding_data_file` at search.rs:92-109 — same first-component `.coding` gate, same `rel.file_name()` membership check, `DATA_FILES` byte-identical (8 names, same order: codegraph.db + -wal/-shm/-journal, memory.db + -wal/-shm/-journal).
  - `should_search` :118-146 — `strip_prefix` at :122-125 unchanged, out-of-root fail-closed at :124, store exclusion now delegated to the shared predicate at :136 (the pure refactor round 1 verified).
  - `is_indexable_path` (watcher.rs:54-91) — the `|| name == ".coding"` clause is gone; `under_base` keeps the `is_ignored_component` loop and returns `!crate::tool::agent::search::is_coding_data_file(rel)` at :75 with the confirmation comment block (:67-74); the canonicalize fallback (:87-90) untouched.
  - The re-pinned `is_indexable_path_accepts_any_file_and_rejects_ignored` (:274-330): `stack.json` INVERTED to TRUE with the churn-not-real comment (:293-303); `codegraph.db` FALSE kept (:304); `memory.db-wal` FALSE added (:305); knowledge/reviews/backlog TRUE cases added (:310-326) — all as round 1 described.
  - The new live test `watcher_reindexes_on_new_coding_artifact` at :452-490 (graph-confirmed span; round 1 cited :451-490, the `#[tokio::test]` attribute line): seed a.rs → in-memory index → spawn(100 ms) → write `.coding/knowledge/live-artifact.md` with `codingartifactmarker` → poll `search_content` 200×50 ms → assert — the exact end-to-end body round 1 verified as genuine; the `cfg(unix)` canonical-path test is pre-existing and untouched.
- The five round-1 points re-check against the committed source: (1) shared-predicate semantics identical to the assessed refactor; (2) loop-safety boundary unchanged (only the DATA_FILES family excluded; nothing else moved); (3) the regression-test body is unchanged; (4) the re-pinned assertions are unchanged; (5) docs/FEATURES.md's committed bullet is the assessed one ("trigger set matches the content index's coverage… only the app's own SQLite stores excluded"). No regression.

## 4. LOW-1 disposition — COHERENT AND SOUND ✓

- **(a) Backlog item 39b3bbfb exists, `[pending]`, fully specified**: provenance (round-1 LOW-1 + the reviewer report path), the two-part analysis (`.worktrees` not in IGNORED_DIRS → the main index always covered worktree trees; post-2a2ae0aa the main watcher fires on worktree DB writes, the `VACUUM INTO` snapshot dest living under `.worktrees/`, not the main `.coding/`), NOT-a-loop reasoning, the FIX with its file anchor (add `".worktrees"` to `IGNORED_DIRS` in src/tool/agent/search.rs — verified accurate against the committed source: IGNORED_DIRS at :56-71 indeed lacks `.worktrees`), TESTS (re-pin the ignore-dir enumerations + add a `.worktrees` case to `is_indexable_path_accepts_any_file_and_rejects_ignored`), ACCEPTANCE criteria (`.worktrees/**` neither indexed nor watched; a worktree-only string unfindable; no main-tree pass on a worktree DB write; cargo test green), and POINTERS.
- **(b) The skip is documented in BOTH required places**: the commit message (explicit "JUSTIFIED SKIP for this diff" paragraph with the coverage-invariant and review-invalidation rationale, naming backlog 39b3bbfb) and the plan context (`.coding/plans/2a2ae0aa.md:35`).
- **(c) I concur the deferral is sound, not a silently-shipped defect**: the inline fix changes INDEX COVERAGE (worktree files are currently indexed; removing them is a coverage change), which this plan's FIX DIRECTION explicitly forbids and which would have invalidated the round-1 assessment; the quirk is pre-existing on the index side — the fix merely made the trigger set equal to that coverage, as designed; the churn is bounded, debounced, run-all-only, and provably not a loop (a main-tree pass — `debounce_loop` → `graph.index(None)` over the main root — never writes under `.worktrees`). A queued, anchored, accepted follow-up is the right vehicle.

## 5. Standing project checks — HOLD ✓

- **Documentation sync**: docs/FEATURES.md's coverage bullet is the committed, accurate one. Module + fn doc comments state the new contract correctly at HEAD (watcher.rs module doc :5-25, `is_indexable_path` doc :36-53, `is_coding_data_file` doc :78-91, `should_search` doc :111-117). README.md/PLAN.md are untouched by this commit and round 1 verified they carry no stale watcher claim; the successor BUG knowledge record documents the FIXED mechanism, the regression test, and the branch status without claiming main-merge.
- **Multi-platform neutrality**: no `cfg` gates added, no Windows-only APIs/paths/shell syntax — `std::path` + `tempfile` + `notify` + `tokio` throughout; the new test keeps the 200×50 ms poll (FSEvents-latency insurance); the `cfg(unix)` symlink test is pre-existing macOS regression coverage, untouched.
- **File-tools-first**: the one shell write targeted `.coding/plans/7b5b5244.md`, a path the file tools refuse BY DESIGN — the constitution's own named standing residual (the approval-gated shell call for `.coding/plans/`), executed through the approval gate. That is the sanctioned exception; no unsanctioned shell mutation exists anywhere in the diff (the src edits are file-tool edits; the knowledge records are memory-tool writes; the backlog is backlog-tool state; the review report is reviewer-authored). Nuance, not a finding: the commit documents WHAT was written and WHY (the LOW-2 disposition, the banner's own provenance attribution) but does not carry an explicit "via shell because the file tools refuse" sentence — the mechanism-level justification rests on the constitution's standing residual and the approval record; since the file tools could not have been used on that path at all, no policy bypass occurred.

## Observations (non-findings, for the record)

1. The commit's backlog.jsonl carries one line beyond the round-2 briefing's enumeration: the ebe21e3c optimizer-levers item (with its 2027-01-25 Savings-rename revision note) is new to the committed file alongside the expected 39b3bbfb addition and the 9201704f pending→done note (the stat's 3+/1−). It is well-formed shared-queue state — the union-merge side-car carries cross-workstream items by design, and it is most plausibly pre-round-1 working-tree state that round 1's scope sketch did not enumerate — consistent with `backlog_list` today; no action needed.
2. The round-1 report file (committed here) ends without a trailing newline — cosmetic, reviewer-authored.
3. The pre-existing U+FFFD pair at 7b5b5244.md:46 (see §2) — no action; repairing it would demand another protected-path shell write for a cosmetic character in a do-not-act tombstone.

## Test evidence

`cargo test` could not be run from this reviewer session (no shell in the allow-list). Parent's evidence, run twice (pre-commit and post-marker): 2715 passed / 0 failed / 5 ignored, plus 19 integration + 1 doc-test, exit=0, warning-free under `#![deny(warnings)]` at both crate roots — accepted as stated and found consistent with the committed code by trace: the re-pinned unit cases resolve against the committed predicate exactly (stack.json / knowledge / reviews / backlog → first component `.coding`, names outside DATA_FILES → watched; codegraph.db / memory.db-wal → DATA_FILES → excluded), and the live test's marker is reachable only through the changed event → `is_indexable_path` → debounce → index → cg_content path.

## Summary

LOW-2 is fixed on both halves (the banner + the superseded duplicate record; one live PLAN record remains: 9dea4744 / `.coding/plans/2a2ae0aa.md`); the protected-path banner write left the file intact (LF endings, body unchanged, banner-only insertion — the single U+FFFD blemish provably predates the write); the committed source is the code round 1 assessed (same two files, same line-precise anchors, no post-review source edit); LOW-1's deferral is documented in the commit message and plan context and is sound (queued as backlog 39b3bbfb with anchors and acceptance criteria; the inline fix would have violated this plan's no-coverage-change invariant and invalidated the review); and the standing checks (documentation sync, multi-platform neutrality, file-tools-first) hold. Nothing is outstanding — the plan may close.