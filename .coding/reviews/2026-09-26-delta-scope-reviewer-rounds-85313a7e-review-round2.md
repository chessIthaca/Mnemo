## Verdict: FINDINGS (0 high, 2 low)

Round-2 (delta-scoped) verification of plan f4636852 / backlog 85313a7e against base 4a2bd99. Scope: the four delta files only — `src/agent/review_scope.rs`, `src/agent/factory.rs`, `src/tool/agent/write_review_report.rs`, `src/tool/agent/spawn_agent.rs` (tests); round-1-verified material was not re-reviewed. Net: **LOW 2 is fully closed** and its test is non-vacuous; **LOW 3 is substantially closed** (spawn half full-path, report half helper-level — stated plainly below, not a finding); **LOW 1 is closed in its literal form** (no unrunnable shell command survives anywhere in the rendered preamble) **but re-opens as a narrowed instance of the same class**: the replacement wording names three git tools the reviewer does not have (F1). The rewording also left four prose descriptions claiming the preamble still carries the removed `git diff <base>` instruction (F2). Both findings are wording/docs-only.

### F1 (low) — LOW-1's replacement wording names git tools the reviewer does not have: `git_log` / `git_show` / `git_diff` are not registered tools; the reviewer's surface is the single `git_read` tool (ops `diff`/`log`/`show`/`status`)

Fixed as prescribed: no bare `git diff <base>`-style instruction survives in **any** render branch — normal delta, >40-path truncation, changed-set-derivation failure, empty set, missing/blank base, and the contract's Empty-diff rule all lost the shell form. The residual `git diff` strings in the file are two source comments explaining the removal (`review_scope.rs:150-151`, `:248`) and the negative test assertions (`:254-255`, `:273`).

Not fixed: the replacement names tools that do not exist on the reviewer's surface.

- Rendered sites: `review_scope.rs:111` (contract Empty-diff rule — "walk the branch's commits with `git_log` and read each with `git_show <commit>`"), `:139` (delta intro — "enumerate the intervening commits with `git_log`, read each one's diff with `git_show <commit>`, and take the uncommitted remainder with `git_diff`"), `:154` (unavailable-set branch — "Enumerate it yourself: `git_log` for the commits since `{base}`, then `git_diff`…"), `:177` (truncation line — "(`git_log` since `{base}` lists them)"), plus the source comment at `:62`.
- Ground truth: the reviewer's only read-only git tool is **`git_read`** — `GitReadTool::name()` = `"git_read"` (`src/tool/agent/git_read_tool.rs:63-65`), schema name `"git_read"` with `op` enum `["diff","log","show","status"]` (`:71-101`); its module doc states it "Replaces `git_log`, `git_show` and `git_diff`" (`:5-10`); the three legacy structs survive only as internal delegates constructed inside `GitReadTool::new` (`:53-55`) — their only other constructions are their own unit tests (`git_read.rs:357`, `:389`; `git_diff.rs:152`). `factory.rs:1080-1086` registers only `GitReadTool`.
- Mechanical proof no standalone trio is registered: `expected_tool_names_registered_when_fully_wired` lists `"git_read"` but not `git_log`/`git_show`/`git_diff` (`factory.rs:2793-2794`) and asserts the registry's tool **count matches the expected set exactly** (`:2877-2888`) — a fourth read-only git tool cannot exist without failing that test. Direct witness from this round: the live reviewer surface in this session exposes `git_read` (ops `diff`/`log`/`show`/`status`) and no tool named `git_log`, `git_show` or `git_diff`.
- The new tests now pin the wrong names: `review_scope.rs:229`, `:250-252`, `:301`.
- Impact, and why low rather than high: this is the same defect class as round 1 (advice the reviewer cannot execute verbatim), one notch milder — the three names map 1:1 onto `git_read`'s op values, so a reviewer recovers in one step (this round's reviewer did, silently). It still spends a step and leaves LOW 1's stated acceptance test — "every instruction names a tool the reviewer actually has" — formally unmet.
- Suggested fix: name the tool and its op, e.g. "enumerate the intervening commits with `git_read` (`op="log"`), read each one's diff with `git_read` (`op="show"`, `commit="<sha>"`), and take the uncommitted remainder with `git_read` (`op="diff"`)" — and move the four test assertions (`:229`, `:250-252`, `:301`) plus the `:62` comment with it. The same naming drift predates this delta in `agent.md:157` and the spawn tool's role description (`spawn_agent.rs:249`, "git_diff/git_log/git_show") — the likely source of the wrong names; since the fix touches this vocabulary, correcting those two spots as well removes the trap.

### F2 (low) — the rewording left four prose descriptions claiming the preamble carries the removed `git diff <base>` instruction

- `src/agent/review_scope.rs:23` (module doc of the rendering module): "the `git diff <base>` instruction, and the mechanically-derived changed file set" — no longer rendered.
- `PLAN.md:1291` (technical decisions): "the preamble names that base, the `git diff <base>` instruction and the changed file set" — this sentence was **true pre-fix** and is the one round 1 verified as accurate; the rewording made it stale.
- `src/tool/agent/spawn_agent.rs:410` (comment): "the `git diff <base>` instruction, and the mechanically-derived changed file set" — same claim.
- `agent.md:142` (constitution): "the preamble names that base, the `git diff <base>` scope and the mechanically-derived changed file set" — softer (scope, not instruction), same drift.
- Explicitly **not** flagged: `README.md:60` and `docs/FEATURES.md:10` ("the app scopes it to `git diff <base>`") describe the scope concept rather than quoting the preamble, and remain defensible — the fix should not be over-applied there; `docs/FEATURES.md:71`'s `git diff` mention concerns the never-truncate-the-diff rule and is pre-existing/correct.
- Why a finding rather than noise: a future agent reading the module doc or `PLAN.md:1291` would believe the preamble instructs a shell command — precisely what round 1 removed — and could "restore" it; the fix's own counter-comment (`review_scope.rs:150-151`) shows the intent, so these four sentences simply missed the sweep. Fix: reword to "the delta since `<base>` instruction" (or drop the clause).

### LOW 2 — verified fixed, and the test genuinely pins both halves

- Empty changed set (`review_scope.rs:161-168`): "If your task names NO findings awaiting a fix, report PASS without re-reading a file. If it DOES name findings, an unchanged tree means none of them were fixed — report that as FINDINGS, not PASS." ✅ A reviewer whose task names unfixed findings is now directed to FINDINGS.
- Carry-over (`:183-189`): the exemption is scoped to hunks "that a round ending in PASS already verified", and the hole is closed explicitly — "Hunks a round ending in FINDINGS reported are NOT in that class: if your task names findings awaiting a fix, verify each fix is present in the delta before you report PASS." ✅ Both halves of the round-1 prescription are present (scope to a PASS round; verify each named fix is present in the delta).
- New test `an_unchanged_tree_never_passes_over_unresolved_findings` (`:364-382`) renders round 2 with an empty changed set — so both the empty-set branch and the carry-over block are in `text` — and asserts "report that as FINDINGS, not PASS", "a round ending in PASS", "verify each fix is present in the delta". Non-vacuity check against the whole render: each phrase is unique to its clause, and each fails against the pre-fix wording ("report PASS without re-reading a single file" / "a previous round already verified" / no verify clause). It cannot pass vacuously.
- The sibling test `an_empty_changed_set_says_the_diff_should_be_empty_too` (`:277-287`) still passes on the no-findings half ("report PASS without") — the two tests are consistent, not in tension. General substring caveat only: the assertions pin the clauses, not the sentence's conditional structure.

### LOW 3 — substantially closed; exactly where the coverage is partial

- **Spawn half: genuine full-path coverage.** The test (`factory.rs:1873-1913`) takes the tool out of the factory-built registry (`registry.get("spawn_agent")`), with the factory's own `set_spawner` mock (`:1881-1882`) and its own workflow/root, executes a reviewer spawn whose task supplies none of the contract ("review the diff"), and asserts the recorded task carries "## Reviewer contract" / "## Verdict: PASS". The contract enters a reviewer task only through `render_review_preamble`, whose **only call site** is the `(Some("reviewer"), Some(wiring))` arm of `SpawnAgentTool::execute` (`spawn_agent.rs:421-455`, render at `:448`), reachable only when `with_review_scope` was applied at registration. Drop the wiring → the `_` arm (`:459`) forwards the task verbatim → assertion fails. This is the factory's registration path, not a test-constructed builder. ✅
- **Report half: partial — plainly.** `build_write_review_report_tool(dir.path()).provenance_wired()` (`factory.rs:1919-1924`) calls the same construction point the registry uses (`:1091`), so dropping `with_review_scoper` inside the helper fails the test ✅ — the wiring-drop class round 1 named is caught. What it does **not** witness: that the registration site still *calls* the helper. If `:1091` were replaced by a directly-constructed unwired tool while the helper stayed wired, this test would still pass. That residual is narrow, and the outright-deletion case is covered separately by `expected_tool_names_registered_when_fully_wired` (write_review_report in the expected set `:2798`; registered-check `:2877-2882`; exact-count assert `:2884-2888`). The test's own comment (`:1915-1918`) and the accessor's doc (`write_review_report.rs:170-175`) acknowledge the structural approach; downcasting the registered tool out of the registry would close the last gap if ever wanted. Recorded as an observation, not a finding: the round-1 hole is closed for both wiring drops; only a present-but-unwired direct construction slips through.
- The accessor is `#[cfg(test)]`-gated (`write_review_report.rs:176`) — no production behaviour change, as the delta claimed. ✅

### Acceptance criteria (a)-(e) after the rewording — all hold

- **(a) names its base revision — yes.** "Base revision: `abc1234`" renders (`review_scope.rs:135`) and is asserted at `:244` plus in both spawn_agent degrade tests (`:713`, `:751`); the raw git command is gone, the sha is not. The delta wording "the delta since `abc1234`" is asserted at `spawn_agent.rs:714`, `:785`.
- (b)/(d): the contract still rides on every reviewer spawn without the dispatcher supplying it (`review_scope.rs:108-115`; asserted `:205-231`, and via the registry in the factory test) — untouched by the delta; non-reviewer spawns stay byte-identical (the `_` arm + its tests).
- (c): best-effort degradation intact — unavailable set (`:152-158`), blank base (`:120`; test `:323-332`), missing base → full review (`:120-128`; test `:309-321`).
- (e): budgets honest — `the_rendered_preamble_stays_within_a_prompt_budget` (`:334-362`) enforces contract < 1800 (comment: measured 1705) and delta < 3000 (comment: measured 2853 after the LOW-1/2 rewording; 2460 before). The 2853 is consistent with the rendered text (contract block ≈1705 + the delta section's header/scope/file-set/carry-over lines), and the +393 over 2460 matches the two added LOW-1/LOW-2 sentences; the green run proves the < 3000 bound holds.

### Docs sweep

Covered by F2 for the stale ones (`PLAN.md:1291`, `agent.md:142`, plus `review_scope.rs:23` and `spawn_agent.rs:410` comments). Everything else still reads accurately: `README.md:58-61`, `docs/FEATURES.md:10`, `:86`, `:87` describe the scope/workflow in terms that remain true; `docs/FEATURES.md:71`'s `git diff` mention concerns the never-truncate rule (pre-existing, correct).

### `.coding/**` bookkeeping — accuracy check, one line

Plan f4636852 carries the "Landed design —" amendment (its architecture/invariants paragraph matches what shipped) and the round-1 report sits at `.coding/reviews/2026-09-26-delta-scope-reviewer-rounds-85313a7e-review.md`; both accurate. No findings.

### Harness note (not docked)

Per round 1's Observation 1 and this round's instruction: the running binary predates the change, so the harness preamble and a round stamp could not appear in this session; the delta was verified from source plus the reviewer's own live surface.

### Test evidence

Round-2 re-run (parent's, unpiped): root `cargo test --quiet` → 2749 passed / 0 failed / 5 ignored, plus 19 passed, plus 1 passed / 1 ignored, exit 0 (2754 total; +2 over round 1). `#![deny(warnings)]` ⇒ warning-free. Frontend untouched by the delta.
