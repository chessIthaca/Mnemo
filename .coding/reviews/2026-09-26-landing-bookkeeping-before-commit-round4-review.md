## Verdict: PASS

Both round-3 findings are fixed exactly as prescribed. LOW 1: the codegraph.rs module doc now reads "reindexes up to `STALE_REINDEX_CAP` (32) stale files inline under the shared 500 ms stale-reindex budget — the budget, not the ceiling, is the latency guard" — matching the const's own doc and the shipped code, with no other stale-ceiling number left in the module doc. LOW 2: DECISION 7278fb92 carries a single dated `memory_amend` paragraph that retires the "OTHER CALLER" symbol-cap-8 claim (original retained as history, content-index core untouched), and every factual claim in it checks against the shipped code. The cap change and its two tests are unchanged since the state round 3 verified clean — every element round 3 described sits byte-equivalent at exactly +1 line, the LOW-1 fix's single net line being the only new codegraph.rs hunk. The repo-wide sweep finds no live knowledge record, doc comment, or test still asserting a symbol-index ceiling of 8 or a 9-file above-cap boundary: the above-cap test pins ceiling+1 = 33 dynamically and the 9-file case is pinned REPAIRED. The delta since base 0a32445 is exactly one commit (d2caf26 — the dispatch's 7-file changed set, every hunk accounted for) plus the uncommitted round-4 plan-frame stamp — nothing else.

Reviewed-state: d2caf26b35e3d4eab6f6c85a226aa55417fc1b13

## Scope — what was actually read

- **Delta enumeration:** `git_read` log (base `0a32445` → HEAD `d2caf26`: exactly ONE intervening commit), status (1 modified file, 0 untracked), diff HEAD (a single uncommitted line — the `## Reviews` round-4 stamp `4 d2caf26…`), `git show d2caf26` (message + stat: 7 files, +221/−22, byte-for-byte the dispatch's changed file set) plus the full diff's head/tail, `git show 0a32445` stat (base contents), and the base file's module-doc head via `git show 0a32445:src/tool/agent/codegraph.rs`. Not an empty-diff review: the scope is the base→worktree delta; the uncommitted diff is only the stamp line because the delta's substance was committed as d2caf26.
- **src/tool/agent/codegraph.rs (current tree = committed state), in full:** :1-50 (module doc), :233-250 (const + doc), :330-360 (the total-miss staleness gate), :1060-1110 (above-cap test), :1111-1175 (within-ceiling test, complete).
- **Cross-checks:** `src/codegraph/mod.rs` :203-216 (`STALE_REINDEX_BUDGET`, pub, 500 ms, at :209); the complete 16-hit `STALE_REINDEX_CAP` list across the three `.rs` files (codegraph.rs :36/:247/:333/:348/:1073/:1076/:1102; search.rs :371/:867/:982/:1010/:1401/:3260/:3409/:3414; search_read.rs :269) and the search.rs :1003-1020 budget pass-site region.
- **Bookkeeping, in full:** the DECISION record (25 lines), both SPEC files (7 lines each), the round-2 report (49 lines), the round-3 report (62 lines), the plan frame :35-52.
- **Sweeps:** `STALE_REINDEX_CAP` over `**/*.rs`, `**/*.md`, and `.coding/knowledge/**` (re-issued for the plain file list after memory auto-delegation); `STALE_REINDEX` over `PLAN.md`, `README.md`, `docs/**/*.md` (all zero hits); repo-wide literal sweeps `up to 8`, `at most 8`, `8 stale`, `8-file`, `cap of 8`, `9-file`, `9 stale`, `eight stale`, `STALE_REINDEX_CAP: usize = 8`, `STALE_REINDEX_CAP = 8`.

## LOW 1 fix — verified (codegraph.rs module doc :35-41)

The fixed paragraph reads: "…a miss first sweeps the indexed source files' mtimes (stats only), reindexes up to `STALE_REINDEX_CAP` (32) stale files inline under the shared 500 ms stale-reindex budget — the budget, not the ceiling, is the latency guard — and re-resolves once from the fresh view, disclosing the side effect…". That is round 3's prescribed wording verbatim, and it matches the const's own doc (:235-246): "an ADAPTIVE upper bound, NOT the latency guard itself. Within it the repair runs under [`crate::codegraph::STALE_REINDEX_BUDGET`], whose pass stops between files once the budget is spent…". The budget it names is real and shared: `pub const STALE_REINDEX_BUDGET: Duration = Duration::from_millis(500)` (mod.rs:209), passed at both repair sites (codegraph.rs:354, search.rs:1017). The rest of the module doc (:1-46 read in full) states no other stale-ceiling number — the only ceiling anywhere in it is :36's "(32)", the shipped value; the surrounding numbers are dates, plan/backlog ids, and the unrelated "360°" view phrase. The doc now also matches the code it describes: total miss (:344) → stats-only sweep (:346) → `stale.len() <= STALE_REINDEX_CAP` (:348) → reindex under the budget (:353-354) → single re-resolve with disclosure. The old "up to 8 stale files inline" text is gone repo-wide — `up to 8` and `8 stale` return zero `.rs` hits.

## LOW 2 fix — verified (DECISION 7278fb92, the live content-index staleness record)

The file now ends with exactly one dated amendment paragraph (:25, `Amended 2027-01-11:`) — appended, not a supersede, exactly the `memory_amend` shape round 3 prescribed — which retires the "OTHER CALLER" claim on all three false counts: "`STALE_REINDEX_CAP` is now 32, NOT 8, and that const WAS deliberately touched by the follow-on", and "No test pins a 9-file above-cap boundary any more: `stale_above_the_reindex_cap_notes_instead_of_reindexing` now builds ceiling+1 (33) stale files, and the 9-file case is pinned by the new `stale_within_the_reindex_ceiling_repairs_inline` as REPAIRED (count 1 + the 'reindexed … serving fresh graph results' note)". Every factual claim in that paragraph was re-verified against the shipped code: the above-cap test builds 2 + (`STALE_REINDEX_CAP − 1`) = 33 stale files and pins the note count via `format!(…, STALE_REINDEX_CAP + 1)` (:1073-1103); the within-ceiling test makes exactly 9 files stale (lib.rs, main.rs, extra0-6; extra0 rewritten post-index to `fresh_symbol_xyz` so only a re-read can answer), then asserts count 1, a note containing both "reindexed" and "serving fresh graph results", and `!contains("may be stale")` (:1111-1168). The original paragraph (:15) is retained byte-identical as history (it matches the round-3 report's quote), the record's content-index core is untouched and still accurate, and the amendment points to the live aligned SPEC for the joint picture — the shape the task prescribed.
## Sweep — no live 8-claim or 9-file above-cap boundary remains

- **Code (complete 16-hit list):** every `STALE_REINDEX_CAP` occurrence across codegraph.rs, search.rs, and search_read.rs is name-based (auto-accurate under any cap value) or explicitly 32; both const defs verified = 32 (codegraph.rs:247, search.rs:371). No `.rs` line asserts 8.
- **Knowledge (complete enumeration — the glob search returns 8 matches in exactly 4 files):** the amended DECISION (live; its remaining "8"s are the retired-history paragraph and accurate content-index narrative), `bug/2027-01-11-content-index-staleness-root-causes` (live; asserts the content index's 32 + 500 ms budget — accurate), the diverged-caps SPEC (`status = "superseded"` verified in its own frontmatter — history), and the superseded 2027-01-04 content-index DECISION. The live aligned SPEC carries no symbol-8 claim: both ceilings 32, budget the real guard — every pointer re-verified (search.rs:371 ✓, mod.rs:209 500 ms ✓, search.rs:1017 ✓, both test names exist ✓, both note strings byte-match production ✓, `supersedes` chain wired ✓).
- **User-facing docs:** PLAN.md, README.md, docs/**, agent.md carry zero stale-reindex mentions — no doc states a symbol-index ceiling, so nothing further needed syncing (corroborates round 3's documentation-sync check).
- **English-phrase sweeps repo-wide:** every remaining "8"/"9-file" hit is immutable review/plan history (the 90fab97e and 55163f1e rounds — the precedent round 3 explicitly endorsed leaving alone), the done backlog item 9201704f, accurate past-tense comments (search.rs:3358-3359 "one past the old cap of 8" describes the old content-index behavior), or the content index's own correct 9-file regression test (9 ≤ 32 repairs inline). `eight stale` — zero hits. Nothing live asserts a symbol-index ceiling of 8 or a 9-file above-cap boundary.
- **The required pins are in place:** above-cap = ceiling+1 = 33, built dynamically (`STALE_REINDEX_CAP - 1` extras + 2 fixture files; count asserted via `STALE_REINDEX_CAP + 1`) so it survives any future cap change — no hardcoded 9 anywhere in the file; and the 9-file case is pinned REPAIRED by `stale_within_the_reindex_ceiling_repairs_inline`.

## Delta confinement + carry-over

- **Since base `0a32445`: one commit (`d2caf26`) + one uncommitted line (the round-4 plan-frame stamp). Nothing else.** d2caf26's 7 files are exactly the dispatch's changed set — codegraph.rs, the DECISION record, both SPEC files, the plan frame, and the round-2/round-3 reports — and every hunk is accounted for: the module-doc fix, the amendment, the round stamps, the reports, and the committed carry-over below.
- **Cap change + tests unchanged since round 3:** round 3 read line-by-line const :246 / ceiling gate :347 / budget pass :352-353 / above-cap test :1062-1107 / within-ceiling test :1109-1167, with "no hunks outside :231-246 and :1054-1167". The current file carries every one of those elements byte-equivalent at a uniform **+1 line** (const :247, gate :348, budget pass :354, tests :1064-1108 / :1111-1168) — the +1 being the LOW-1 module-doc fix's single net line. The module-doc fix is therefore the ONLY new codegraph.rs hunk since round 3; the stat corroborates (111+/22− ≈ round 3's 98 changed lines + the doc fix's ~13 gross lines).
- **Process remark (one line):** d2caf26 commits previously-reviewed uncommitted material — the round-3-verified cap change and tests, both SPEC files, and the round-2/round-3 reports (round 3 saw all of these in its working tree and verified/accuracy-checked them; the round-2 report is PASS-round material) — verified present and undisturbed here, not re-line-reviewed beyond confirming the two fixes.
## Standard checks

- **Correctness / bugs:** none — the delta's only code change is comment text (the module doc), so no semantic delta exists; the test assertions byte-match the production note strings; the +1 line shift moves line numbers only.
- **Security:** nothing — no new input surface, no `unsafe`, tempdir-scoped tests, no secrets.
- **Documentation sync:** PASS — LOW 1 was the one stale doc line, now closed; module doc ↔ const doc ↔ code all agree; no user-facing doc (README/PLAN/FEATURES/agent.md) mentions the symbol-index ceiling, so nothing else needed syncing for this delta.
- **Multi-platform neutrality:** PASS — comment-only code change; the tests use `std::fs` / `SystemTime::set_modified` (the same cross-platform std pattern the sibling tests use and the 2026-09-07 round-2 review vetted); no `cfg(windows)`, no shell, no Windows paths.
- **File-tools-first:** PASS — no shell-based file mutation anywhere in the delta; the knowledge amendment is `memory_amend` (the sanctioned `.coding/knowledge/**` writer), the reports are `write_review_report` outputs, and the code hunk is an ordinary file edit now committed.
- **Warning-free build:** the delta introduces no warning source (comments + bookkeeping only); covered by the parent's unpiped green suite under `#![deny(warnings)]` — no `#[allow]` anywhere.
- **Regression tests:** present and unchanged — the within-ceiling test is the genuine red/green pin for the cap raise (red-before established statically: the old test pinned count 0 + "may be stale — 9 file(s)" + no "reindexed" on the identical 9-file setup, green at cap 8; the new assertions are its direct negation), and the above-cap test re-pins the boundary dynamically at ceiling+1. The two fixes verified here are doc/bookkeeping-only and need no new test of their own.

## `.coding/` bookkeeping — accuracy check (one line each)

- **Round-2 report:** opens `## Verdict: PASS`, reviewed-state `0a32445` — matches its memory digest and the shipped commit; now committed in d2caf26 (was untracked carry-over).
- **Round-3 report:** opens `## Verdict: FINDINGS (0 high, 2 low)` — matches the dispatch's framing; both its prescriptions are implemented verbatim; now committed.
- **Plan frame:** stamps 1-3 committed (round 3 adjudicated them correct), round-4 stamp `4 d2caf26…` uncommitted (harness bookkeeping, the expected delta line); step 5 remains unchecked — this review runs inside it.
- **Diverged-caps SPEC:** `status = "superseded"` in its frontmatter, successor pointer on the aligned file — correct hygiene; its symbol-8 content is point-in-time history, excluded from the live-record sweep by design.
- **Aligned-at-32 SPEC:** substance matches what shipped (every claim re-verified against code). Its two codegraph.rs line pointers (:239, :346) are stale — round 3 explicitly adjudicated this exact drift "harmless in a pointer-first record (path + symbol accurate); no fix required", and the LOW-1 fix moved those lines one more (:247/:354), which does not change that ruling. Not a finding; a one-line `memory_update` (:239→:247, :346→:354) would polish it if the parent wants.
- **DECISION 7278fb92:** the amendment is accurate on every count; the retired paragraph retained as history with exactly one dated `Amended` heading — the sanctioned amend shape. (The knowledge-layer/git date offset in its heading — "Amended 2027-01-11" over a "2026-09-26" landing — is this project's systemic clock quirk, stamped by the memory tool itself, pre-existing across every record in this area; not introduced or fixable by this delta.)
- **Commit message d2caf26:** matches the shipped content (cap raise, both tests with their red/green story, module-doc fix, amendment, spec supersede) �� accurate.

## Evidence note

I ran nothing (read-only). Two claims lean on runtime evidence I could not obtain: (1) the full suite green + warning-free — the parent's unpiped `cargo test`, exit 0, under `#![deny(warnings)]` (per the dispatch evidence note), which also covers the post-LOW-1 re-run round 3 asked for; (2) the within-ceiling test's red-before property — established statically, as above. One residual inherent to the process: round 3's reviewed state was never a commit, so "cap change + tests unchanged since round 3" rests on round 3's line-by-line descriptions matching the current file byte-for-byte at a uniform +1 line — which they do, for every element it named (const, gate, budget pass, both tests, and the no-other-hunks statement). Everything else was verified by direct reads of the delta's files and sweeps of the live tree.