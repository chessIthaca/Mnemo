## Verdict: FINDINGS (0 high, 7 low)

All three round-2 fixes verify correct and complete, both round-2 hardening notes are applied exactly as described, and every point of the round-1 + round-2 contract verification re-confirms against the current code — including the dispatch paths (checkpoint failure, no-main-agent) and the agent-facing `RUN_ALL_STEER` text, all now read directly by this reviewer. The final sweep, however, surfaced seven remaining stale comments/docs of the same documentation-sync class round 2 flagged: pre-existing text in changed files that this change's deletions (the automatic rollback arm, harness `CantResolve` stamps, the approval-halt `Failed` stamp, premature-delivery `Failed` stamping) have left misdescribing the code. Zero behavioral impact — the code is correct and `cargo test` is green (1920+16 passed, 0 failed, warning-free) as reported by the dispatcher.

### Round-2 fix verification — all three FIXED

**LOW-1 (stale A9 comment) — fixed.** `run_all.rs:1316-1319` now reads "…A9: log the dispatch error instead of silently swallowing it (the no-main-agent path inside dispatch_next already clears the run and returns Err; this surfaces every dispatch failure)" — the round-2 prescribed text, verbatim; the "marks the item Failed" claim is gone. The comment's factual claim now verifies directly against the code: the no-main-agent arm (`run_all.rs:1001-1004`) does `end_run` + `return Err("no main agent registered")` — no stamp — and `checkpoint_failure_leaves_the_item_pending` (:740-756) pins `!body.contains("BacklogStatus::Failed")` over the whole dispatch fn.

**LOW-2 (stale parenthetical) — fixed.** `run_all.rs:1690-1693` now reads "(No sha recovery needed here: commit_success commits the working tree like the run-all success arm, which also never uses the sha — the automatic rollback arm is gone (backlog 45dcf577); the sha stays in the note as the manual resume/rollback anchor.)" — the deleted rollback arm is no longer referenced as living code.

**LOW-3 (duplicate spec deliverable) — fixed.** Exactly one canonical spec remains: `.coding/knowledge/spec/2026-12-06-backlog-status-plan-lifecycle.md` (46 lines, comprehensive, with code anchors — `stamp_backlog_in_flight`/events.rs, `BacklogItem.plan_id`, `plan_loop_allows_done`, `is_root_plan_abandonment` + `TurnResolveLatch.plan_abandoned` + gate-before-abandonment ordering, `plan_open_note`, no-rollback, `resolved_terminally`, approval-keeps-run-state, `drain_run_all_on_main_exit`, checkpoint-failure→Pending, escape hatches, CantResolve agent-only, supersedes-note). Exactly one live SPEC memory record (45e4d970) points at it — a prefix search for "SPEC: backlog item status" returns only it. The duplicate digest file `2026-12-29-backlog-item-status-plan-lifecycle-failed-plan-a.md` is deleted from disk (read → not found) and its record 9314f6f9 is gone from the memory store. The superseded 2026-09-01 file carries `status = "superseded"` and a banner pointing at the 2026-12-06 file with the correct one-line delta — valid.

### Round-2 Notes hardening — both APPLIED

- `plan_open_note_names_what_happened` (:491-514) now asserts the "no further items dispatched" suffix on the open (Executing) variant too (:505, alongside :502-504), in addition to the never-ran (:499) and unverified (:513) variants.
- `main_agent_exit_drains_an_active_run_all` (:759-795) pins the was_main-before-mgr.remove ordering with two `find()` positions over the events.rs source and `assert!(was_main < remove)` (:781-785), plus the drain-body assertions (annotate + `end_run`). events.rs:596 captures `was_main` under the manager lock immediately before :597 `mgr.remove(agent_id)`, and :618 gates the drain on it.

### Contract re-verification — round-1 + round-2 points all hold

- **Gate → Done.** Run-all success arm `(Some(main_state), true) if plan_loop_allows_done(main_state, true)` (:1238-1248) → `commit_success` + `transition(Done)`; the single-dispatch arm mirrors it (:1330-1334).
- **Gate BEFORE abandonment.** The gate arm is the first match arm; `_ if plan_abandoned` follows (:1255) in the success path, `else if plan_abandoned` (:1280) in the failure path — an abandon followed by a successor plan that closes the loop resolves Done.
- **Abandonment → Failed, run continues.** :1255-1261 / :1280-1288 transition `Failed` "plan abandoned (abandon_plan)", bump the done counter, no `end_run`; single-dispatch :1355/:1362.
- **Everything else: no transition, no rollback, halt.** Success catch-all :1269-1278 and failure catch-all :1289-1303 annotate `plan_open_note` + `end_run` + return; the single-dispatch open-loop arm yields `(None, note)` with `resolved_terminally = false` (:1391). No `rollback(` survives (source-contract negative :728-732).
- **Approval halt keeps the run state.** `halt_run_all` sets the stop flag (:1444), then the `stamp_failed` arm annotates (once, via `already_halted`) and returns BEFORE `end_run` (:1480-1499); the steer arm annotates + latches + ends the run (:1500-1524). The source-contract test asserts the approval block annotates and contains no `BacklogStatus::Failed` (:589-597), and events.rs:643-657 routes main-agent approval requests into `halt_run_all(..., true)`.
- **Checkpoint failure → Pending (read directly this round).** `run_all.rs:960-978`: annotate "git checkpoint failed: {e}" + `end_run` + `Err` — the item is NOT stamped, it never left `Pending` — matching the table doc and `checkpoint_failure_leaves_the_item_pending`.
- **Main-exit drain.** events.rs Exited arm: `was_main` captured under the manager lock before `mgr.remove` (:595-597), `turn_resolve.on_exited`, the deferred-resolution flush, then `if was_main { drain_run_all_on_main_exit }` (:618-620); the drain annotates + ends the run and the item keeps its status (:1536-1561).
- **Auto-feed only past terminal resolution.** `resolved_terminally` starts true (:1334), is set false only in the no-transition branch (:1391), and gates the feed (:1396).
- **plan_id linkage.** `stamp_backlog_in_flight` stamps Pending→InFlight (note preserved) and calls `set_plan_id(top_plan_id)` on EVERY Executing entry (:1733-1770); events.rs passes `top_plan_id`, main-agent-gated (:438-449); `set_plan_id` (:428-444) records the root-plan id without touching status, re-linking when a fresh plan replaces an abandoned one mid-dispatch; `in_flight_stamp_only_applies_to_pending_items` (:684-702) pins both halves.
- **Intervention latch consumed before any stamping** (:1133-1195, closed-loop fallthrough → `finish_captured_item_done`); all five `on_main_turn_resolved` call sites (events.rs :528, :560, :570, :887, :908) pass the real `turn_resolve.plan_abandoned(...)`; the latch mirrors `workflow_changed` (noted on transition, cleared in `on_started`/`on_exited`).
- **Agent-facing text.** `RUN_ALL_STEER` rule 4 (:154-158): done ONLY when the plan reaches Complete; ending the turn early leaves the task in flight and halts the run; only abandoning the plan marks the task failed — matches the contract (the round-1 fix, still in place).
- **Docs.** Module doc (:1-110), transition-table doc (src/backlog.rs:340-378), README.md:76, PLAN.md:60/:892-894, and the backlog_status tool docs all state the new contract correctly.

### Findings — 7 low, all documentation-sync (stale claims in changed files)

**LOW-1 — `run_all.rs:1625-1626`: stale "an approval halt stamped it" example.** `handle_user_intervention`'s status match has a `_ => {}` arm commented "// Already terminal (e.g. an approval halt stamped it): leave it alone." Under 45dcf577 an approval halt stamps NOTHING — the item stays InFlight with the run state deliberately kept (module doc :18-24, `halt_run_all` :1480-1499, source-contract test :589-597) — so an approval halt can never leave the item terminal and can never be the reason this arm fires. The arm itself is correct (defensive: the agent's `backlog_status` tool may have marked the item, or a prior resolution already resolved it); only the example is wrong, and it sits ~40 lines below the rewritten doc stating "neither a steer nor an approval halt may stamp a terminal status". **Fix:** reword, e.g. "// Already terminal (e.g. the agent's backlog_status tool marked it, or a prior resolution resolved it): leave it alone."

**LOW-2 — `src/backlog.rs:102-104`: stale `BacklogStatus` variant docs.** The enum docs still describe the deleted semantics: `Failed` — "The turn errored." (:102) and `CantResolve` — "Rolled back to its git checkpoint; needs manual review." (:104). Under 45dcf577 a turn error does NOT produce Failed (only root-plan abandonment does — a plain error annotates + halts), and nothing rolls back (CantResolve is the agent's explicit dead end; the work stays in the tree). The transition-table doc in the same file (:342-355) states the new contract correctly — the variant docs above it were missed. **Fix:** e.g. `Failed` — "The root plan was abandoned (backlog 45dcf577); the work stays in the tree." / `CantResolve` — "An explicit dead end named by the agent (backlog_status); the work stays in the tree."

**LOW-3 — `src/backlog.rs:1135`: stale "checkpoint-failure" justification.** The `transition_legal` test comments the (Pending, Failed) row "// Pending → Failed (checkpoint-failure / pre-dispatch resolution)." A checkpoint failure no longer stamps anything — the item stays Pending (`run_all.rs:960-978`, verified above), and the updated table doc in this same file (:347-349) says "agent marking; the harness no longer stamps here — a checkpoint failure leaves the item Pending". Both parenthetical justifications describe the pre-45dcf577 harness; the live justification is the agent's explicit marking. **Fix:** "// Pending → Failed (the agent's explicit backlog_status marking)."

**LOW-4 — `src/backlog.rs:1147`: stale "plan-gate rollback" justification.** Same test, the (InFlight, CantResolve) row: "// InFlight → CantResolve (plan-gate rollback)." The plan-gate rollback arm was deleted by this change (round-2 LOW-2's own fix text: "the automatic rollback arm is gone"), and the harness never stamps CantResolve (module doc :99-100; table doc :350-352 "CantResolve only as the agent's explicit dead end"; source-contract negatives :722-727). **Fix:** "// InFlight → CantResolve (the agent's explicit dead end)."

**LOW-5 — `events.rs:867-870`: stale "would mark the item Failed" consequence.** `try_flush_deferred_main_resolution`'s doc justifies the Complete-only success gate with "Delivering it then would mark the item Failed ('plan loop did not close (workflow: Reviewing)') even though the main agent hasn't finished its work yet." Under 45dcf577 a premature delivery does NOT mark Failed — it hits the catch-all: annotate `plan_open_note(Reviewing)` + `end_run`, item left in_flight, auto-feed suppressed. The gate is still right (premature delivery would halt the run while the agent is mid-review); the stated consequence is the old contract. **Fix:** e.g. "Delivering it then would close the turn as a non-closure — the run halts with the item left in_flight ('plan loop did not close (workflow: Reviewing)') even though the main agent hasn't finished its work yet."

**LOW-6 — `events.rs:25-26` + `src/runtime/turn_resolve.rs:9-10` (+ `run_all.rs:934-935` tail): stale "would roll back" counterfactuals.** Both latch module docs carry the identical sentence "without a latch the forwarder would roll back on Error and then commit-success on Finished", and the busy-guard comment's tail (:934-935) says interleaving "rolls back the wrong item's work". The rollback-on-Error arm no longer exists (this change deleted it) and commit-success only fires through the plan-loop gate — so the stated manifestation of a latch-less forwarder is the pre-45dcf577 mechanics. The latch is still needed (a double delivery would still corrupt statuses — the busy-guard's primary claim at :929, "its resolution would stamp the wrong item's status", is current and correct); only the rollback elaboration is stale. **Fix:** reword to the current manifestation, e.g. "without a latch both resolutions would run — historically rollback-on-Error then commit-success-on-Finished; today the failure resolution would halt the run and the success resolution would then act on stale state."

**LOW-7 — `run_all.rs:841-845` + `src/project/git_ops.rs:166`: rollback-arm helper leftovers claim dead callers.** `extract_checkpoint_sha`'s doc says the sha-prefix recovery exists "so rollback/commit logic still works", and `git_ops::rollback`'s doc says it is "used when an item fails". The automatic rollback arm that consumed the sha was deleted by this change and `commit_success` never used it (per the LOW-2 fix comment itself); a full-tree search shows both functions now have zero production callers (extract_checkpoint_sha: def :846, module-doc mention :110, test import/calls :861-896, a doc mention in src/backlog.rs:397 — nothing else; rollback: only its own impl/tests and run_all.rs:729's negative test assertion). Both docs misdescribe the code this change leaves behind. **Fix:** reword to the surviving purpose — extract_checkpoint_sha: "so the checkpoint sha stays recoverable from an annotated note (the manual resume/rollback anchor)"; rollback: "the manual rollback primitive (git reset --hard to the checkpoint sha; no automatic caller since backlog 45dcf577)" — or remove the orphaned helpers with their tests if they are no longer intended as contract surface. (Related pre-existing nit, no action required: src/backlog.rs:397's doc link `[`extract_checkpoint_sha`](crate::agent::loop_impl)` targets the wrong path.)

### Sweep confirmation — otherwise clean

Every `Failed` / `CantResolve` / `rollback` / `roll` occurrence in the changed files was audited (regex tree-walks over the full files plus direct reads of every hit's region; note the content index is stale for the uncommitted files, so index-engine "no matches" results were disregarded and re-run as tree walks — two initially "missing" tests were located this way):
- `run_all.rs`: the remaining occurrences are correct contract statements (:16, :21, :72, :104, :235, :1266, :1292, :1344, :1418, :1453, :1575, :1692-1693), accurate historical regression notes explicitly framed as past defects (:142, :174, :308, :541, :572-574, :624), or test assertions (:709, :722-732, :751-755). :951 "record HEAD for rollback" and :963 "can't safely roll back" are acceptable — the sha is the manual rollback anchor, and without a checkpoint there is no anchor to roll back to. The agent-facing `RUN_ALL_STEER` (:144-163) and `run_all_prompt` docs are correct. Every production line of the file was read directly this round.
- `events.rs`: :425 correct; :750 is the unrelated failed-reviewer protocol; :867-870 is LOW-5; :25-26 is LOW-6. The approval-request path (:643-657) and the Exited arm (:588-620) are correct.
- `backlog_cmds.rs`: :35 ("no rollback; see run_all's plan-tied status contract") and :186 ("resume/rollback anchor") correct.
- `src/backlog.rs`: table + docs (:78, :342-355, :428-444) correct; :102-104/:1135/:1147 are LOW-2/3/4; the rest is table/test code (the :1077 "rolled back" string is test data for a set_status call, not a claim).
- `src/runtime/turn_resolve.rs`: :61 (historical bug rationale), :68, :151, :316 correct; :9-10 is LOW-6.
- `src/tool/workflow/backlog.rs`: only :503 (unrelated backlog_add validation events). Clean.
- `README.md` (:76) and `PLAN.md` (:60, :892-894) state the new contract correctly; the other "failed" hits are unrelated (provider retries, failed-reviewer protocol).
- `src/project/git_ops.rs` / `contract_fixtures.rs`: fixture updates only, plus the LOW-7 doc on `rollback`.

### Notes

- **Tests:** reported green by the dispatcher (1920+16 passed, 0 failed, warning-free under `#![deny(warnings)]`); not re-run by this reviewer (read-only). The source-contract tests inspected are consistent with the current source.
- **`.coding/backlog.jsonl`** (2-line diff) is the running binary's own state update for item 45dcf577 — expected, as round 2 already noted (the item predates the plan_id linkage).
- **Multi-platform neutrality:** clean — no platform-specific code in the change (pure Tauri state management + docs).
- **Documentation sync:** README/PLAN/spec/module docs/tool docs updated; the seven LOWs above are the residual drift, all comment/doc-level with zero behavioral impact. After they are reworded, the 45dcf577 contract documentation is fully consistent end to end.
