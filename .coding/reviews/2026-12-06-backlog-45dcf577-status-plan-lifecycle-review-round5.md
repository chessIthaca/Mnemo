## Verdict: FINDINGS (0 high, 1 low)

Both round-4 fixes verify exactly as prescribed, and the re-run exhaustive sweep confirms every prescribed surface — src-tauri/src/ipc/run_all.rs, events.rs, backlog_cmds.rs, src/backlog.rs, src/runtime/turn_resolve.rs, src/tool/workflow/backlog.rs, src/project/git_ops.rs, README.md, PLAN.md, and the .coding/knowledge/ corpus — is free of stale 45dcf577 claims: round-4's conclusion (the contract documentation is fully consistent end to end) holds for the prescribed scope. The sweep surfaced one remaining stale-claim cluster on a surface no prior round covered: docs/why-mnemo-deck.md slide 14 (the user-facing explainer deck, untracked in the working tree) still teaches the pre-45dcf577 Run-All failure semantics this change removed — "rollback on failure" (twice) and "a turn that ends mid-loop is rolled back and marked `CantResolve` for review". Documentation-sync class, zero behavioral impact, four line edits to fix. The behavioral contract is unchanged since round 4; cargo test is green (1920+16 passed, 0 failed, warning-free under `#![deny(warnings)]`) per the dispatch. Detail below.

## 1. R4-LOW-1 (interrupt in leaves-untouched lists) — VERIFIED FIXED

Both spots re-read fresh in the current tree:

- **src-tauri/src/ipc/run_all.rs:101-107** — "A turn that ends with the plan still active (session end, crash, the agent stopping early) changes NOTHING: the item stays `InFlight` (the plan persists; a resumed session continues it), the work is NOT rolled back, and the run halts — the next item may only start once this item's loop verifiably closed. The deliberate `InFlight` → `Pending` deferral (intervention requeue) and the terminal → `Pending` requeue remain the escape hatches." — "interrupt" is gone; the list names exactly the no-transition turn-ends, and the escape hatches follow in the same breath.
- **PLAN.md:891-898** — "Item status is tied to the plan lifecycle (backlog 45dcf577): `Done` only via the plan-loop gate (the plan finished), `Failed` only on root-plan abandonment, and every other turn end (loop open, crash, halt-for-approval) leaves the item untouched — no rollback, the run halts, a resumed session continues the plan. A user steer or interrupt requeues the in-flight item non-terminally; items are stamped `InFlight` (and linked to the root plan id) only when execution begins (workflow enters `Executing`)." — "interrupt" is gone from the leaves-untouched list, and the very next sentence states the requeue.

The intervention requeue remains correctly stated as the escape hatch everywhere else, including immediately around both edits: run_all.rs:28-39 ("**User intervention = requeue, never a failure.** A steer or interrupt on the main agent mid-item records an intervention latch … returns the item to `Pending` (checkpoint sha preserved in the note) and stops the run — no terminal stamp, no next dispatch, no auto-feed") and :43-44 ("A pre-planning steer/interrupt therefore leaves the item queued, untouched: it is 'not failed yet'"); PLAN.md:71-73 ("A user steer or interrupt on the main agent is an **intervention, not a failure**: the affected item returns to `pending` … the run stops without dispatching the next item"). The round-4 self-contradiction inside the same PLAN.md bullet is resolved.

Re-examined and passed (consistent with round 4's judgment; the edits introduced no new inconsistency):

- README.md:76 — "a session ending, crash, interrupt, or halt-for-approval never **fails** an item" is a never-FAILS claim (correct: the requeue is non-terminal), not a leaves-untouched claim, and the same sentence names both escape hatches ("the in_flight→pending deferral and terminal requeue remain the escape hatches").
- The "stays `in_flight`/`InFlight` through interruptions, steering pauses, and sub-plan pushes" phrasing (run_all.rs:87-89, PLAN.md:55, README.md:76, canonical spec :14) — the plan-active derivation claim (the root plan stays active through those events, so nothing is stamped at event time); the escape hatches are carved out within a few lines in every one of those documents.

## 2. R4-LOW-2 (two live knowledge records) — VERIFIED FIXED

- **.coding/knowledge/spec/2026-08-31-steering-the-main-agent-during-run-all-halts-the.md** — `status = "superseded"` frontmatter (line 4) plus a SUPERSEDED banner (line 7) pointing at 2026-12-06-backlog-status-plan-lifecycle.md, noting BOTH disposition changes (the steer path requeues via the intervention latch per 0a58db61: InFlight→Pending, sha preserved; the approval halt stamps NOTHING — annotate + keep the run state per 45dcf577) while stating the HALT mechanic itself stands (send_suggestion → halt_run_all BEFORE sending; never interleaves); the original "in-flight item is marked Failed" claims are explicitly marked historical (pre-0a58db61/45dcf577). Exactly the prescribed fix.
- **.coding/knowledge/decision/2026-09-01-backlog-steer-interrupt-requeues-the-item-merged.md** — points (1)-(3) unchanged and accurate (latch + non-terminal requeue; items leave Pending only at Executing; a steer absorbed into a closed loop still commits + marks Done); point (4) amended exactly as prescribed: "AMENDED 2026-12-06 (backlog 45dcf577): approval halts stamp NOTHING — annotate + keep the run state so the post-approval turn resolution resolves the item under the plan-tied rules (the original 'keep stamping Failed (terminal, load-bearing)' claim was reversed; halt_run_all still takes stamp_failed, now selecting the annotate+keep arm vs the steer arm's latch+end_run)." Points at the canonical spec as the current contract. The amendment matches the code: halt_run_all's doc (run_all.rs:1421-1441) states "Approval halts (`stamp_failed = true`) stamp NOTHING (backlog 45dcf577: a halt is not a plan abandonment — the one true `Failed`). The run state is deliberately KEPT … The steer arm (`stamp_failed = false`) annotates, hands the item id to the intervention latch, and ends the run."
- **Memory records** — dfaf47d2 (the steering spec) now shows [superseded] carrying the corrected banner content: the derived index re-derived from the amended file. a48759b7 (the decision) remains live with its digest matching the corrected file's opening (points 1-3); derived records regenerate from the files on content-hash drift (dfaf47d2's re-derivation proves the corpus pass ran), and two independent stale-claim probes ("the in-flight item is marked Failed with its checkpoint sha preserved — approval halts keep stamping Failed terminal load-bearing"; "approval halts stamp NOTHING annotate keep the run state") surface NO live record carrying the stale claims. (Search digests are display-truncated before point 4; the file — which stays the truth — was read in full.)
- **No duplicate successor files** — corpus-wide sweeps across every family (interrupt/intervention, rollback/roll, stamp, CantResolve, approval, failed, marked/marking/marks, plus a run-all topic enumeration) surfaced exactly the known backlog-related corpus files; no new spec/decision duplicating the amended ones exists, and the round-3 duplicate (2026-12-29-…-failed-plan-a.md) remains deleted. The inflig decision twin keeps its historical point (4) inside its `status = "superseded"` frontmatter (verified, line 4) — by-design history, identical treatment to the superseded specs (round 4 examined and accepted this); the resolution-paths spec likewise carries `status = "superseded"` plus a banner stating the new contract ("halt stamping is NO LONGER terminal for approvals — the plan-tied contract removed every harness `Failed`/`CantResolve` stamp except root-plan abandonment. The trace below records the PRE-45dcf577 behavior (historical).").

## 3. The exhaustive re-sweep — all ten prescribed surfaces clean

Method: per-file term-hit audits (both casings; PascalCase `Failed`/`CantResolve` plus case-insensitive families so snake_case tool-schema text was covered) over the seven source files, README.md, and PLAN.md; full reads of the run_all.rs module doc, the halt_run_all/handle_user_intervention docs, src/runtime/turn_resolve.rs, and the five backlog-related knowledge files; corpus-wide family sweeps via all-repo-md tree walks. (The .coding/knowledge-glob family searches auto-delegate to memory by the search tool's memory-hunt heuristic, so the corpus sweep ran on the root-style `*.md` glob, which walks every repo .md file with the knowledge corpus sorting first in the results — coverage is complete. The content index is stale for uncommitted files, so all results came from tree walks — the round-3 lesson, applied.)

- **run_all.rs** — module doc: "**Approval = halt.** … The item is stamped NOTHING at halt time (backlog 45dcf577): the run state is deliberately kept" (:18-24); `Failed` ⇔ root-plan abandonment (:64, :93); "`CantResolve` ⇔ an explicit dead end (the agent's `backlog_status` tool) — never set by the harness resolution paths" (:99-100); the fixed no-transition list (:101-107). Code/docs: halt_run_all's two arms (:1421-1441, quoted in §2); handle_user_intervention requeue-without-terminal-stamp; drain_run_all_on_main_exit; auto-feed only past a terminally resolved item. The test module pins the contract — no-transition turns "never `CantResolve`, never a rollback" (:709, :722), and the halt-disposition selector test asserts the approval arm annotates without stamping `Failed`. The dated defect-history notes are explicitly historical framings. Clean.
- **events.rs** — the root-plan abandonment latch ("failure means exactly 'the plan was abandoned'", :425-431); stamp_backlog_in_flight on Executing entry, main-agent only, never at dispatch (:432-449); the main-exit drain comment (:611-619, "an approval halt, whose kept run state expects a later turn resolution"); the one old-contract mention (:26) is explicitly historical. Clean.
- **backlog_cmds.rs** — pre-planning stays Pending, stamped `InFlight` only when the workflow enters `Executing` (:323-326); terminal-status handling (done/failed/cant-resolve) for drop/reset/requeue (:168-183); the halt-for-approval resume anchor (:424). Clean.
- **src/backlog.rs** — the enum docs (`Failed` = "The root plan was abandoned (backlog 45dcf577); the work stays in the tree."; `CantResolve` = "An explicit dead end named by the agent (`backlog_status`); the work stays in the tree.", :102-107); the plan_id linkage doc (:72-78); the guarded transition table and its doc (:334-360 — Pending→InFlight on execution entry, pre-dispatch terminal, turn-resolution terminal, InFlight→Pending requeue/deferral, terminal→Pending requeue, "Anything else … is refused"); requeue refuses non-terminal; :1079 "rolled back" is test data. Clean.
- **src/runtime/turn_resolve.rs** (read in full) — the abandonment-driven `Failed` latch (:70-71, :153, :318); :63 is the historical bug description; :9-11 frames the old rollback contract as historical ("the old contract rolled back on Error and then commit-successed on Finished; today …"). Clean.
- **src/tool/workflow/backlog.rs** — module doc (:22-25), tool doc (:212-217: "Unfinished work is NOT failure — the item stays `in_flight` … `cant_resolve` is the explicit dead end"), and schema (:247-256: "NEVER mark an item failed merely because work is unfinished — unfinished work stays in_flight … use cant_resolve for an explicit dead end") all state the plan-tied contract; wire strings and tests match. Clean.
- **src/project/git_ops.rs** — every "failed" hit is an error-path string; the rollback primitive is documented as manual-only (:166-168: "the manual rollback primitive (git reset --hard to the checkpoint sha; no automatic caller since backlog 45dcf577 removed the rollback arm)"). Clean.
- **README.md** — the backlog bullet (:76) states the full contract correctly (in_flight while the dispatched plan is active, done via the turn-resolution plan-loop gate, failed only when the plan is abandoned, the never-fails list with both escape hatches named, pre-planning stays pending). All other hits are unrelated (persistence, approval gating, provider retries, embeddings status). Clean.
- **PLAN.md** — the contract section (:50-80) and the Backlog subsystem bullet (:887-898) are correct; all other approval/failed/interrupt hits are approval-flow/UI/IPC mechanics or the failed-reviewer protocol. Clean.
- **.coding/knowledge/ corpus** — the only pre-45dcf577 halt-disposition claims live inside the superseded records (the 2026-08-31 steering spec and the 2026-09-01 resolution-paths spec — both `status = "superseded"` with banners stating the new contract — and the superseded inflig twin); both live records are corrected; bug/plan records carrying old-behavior text (deferred-success, run-all-dispatches, the 0a58db61 plan) are dated histories. Clean.

The behavioral contract itself is unchanged since round 4 — only the two doc words and the two knowledge files changed this round, confirmed by comparing the current tree against round-4's recorded baseline at every fix location — and cargo test is green (1920+16 passed, 0 failed, warning-free under `#![deny(warnings)]`) per the dispatch, with rounds 1-4 having verified the behavior end to end.

## 4. R5-LOW-1 — docs/why-mnemo-deck.md slide 14: pre-45dcf577 Run-All semantics in the user-facing deck

The deck's Run-All slide still states the exact behavior backlog 45dcf577 removed:

- :805 (on-slide bullet) — "Git checkpoint per item, **rollback on failure**"
- :814-815 (speaker notes) — "Per-item git checkpoint, commit on success, rollback on failure, and it **halts** when an approval is required rather than auto-approving."
- :819-820 — "A turn that ends mid-loop is rolled back and marked `CantResolve` for review."
- :822 — "*Exec:* throughput without a night shift, with per-item rollback as the safety net."

Under the shipped contract, a turn that ends mid-loop changes NOTHING — no status transition, no rollback (the work stays in the tree; the checkpoint sha in the note is the manual resume/rollback anchor), the run halts; `CantResolve` is only the agent's explicit dead end; failure means root-plan abandonment → `Failed` with the work kept. These are the same "rollback-on-failure / CantResolve-on-non-closure" claims rounds 1-4 eliminated from the module doc, README, PLAN, and the knowledge corpus. The slide's neighboring claims remain true (commit on success; the Done gate via `finish` + an observed state transition; halts for approval, never auto-approves) — so the fix is surgical.

Why rounds 1-4 passed it: their sweep surfaces were the changed files plus the knowledge corpus; the deck is neither. It is untracked (`git log -- docs/why-mnemo-deck.md` is empty — it is part of the uncommitted working-tree state) and predates 45dcf577 (it describes the 4bc070c9-era gate). This round's sweep ran broader — every term family across every repo .md file — and surfaced it. Same documentation-sync class as R4-LOW-2 (a live record carrying pre-45dcf577 claims), here a live user-facing document; under the project's documentation-sync review expectation ("a feature that ships with its docs not updated is an incomplete change"), the deck's Run-All slide is user-facing documentation of exactly the behavior this change rewrote.

**Fix (four line edits in slide 14, or equivalent wording matching the module doc's contract):**

- :805 → "Git checkpoint per item; on failure the work **stays in the tree**"
- :814-815 → "Per-item git checkpoint, commit on success, and on failure the work stays in the tree for a resumed session (the checkpoint sha is the manual rollback anchor); it **halts** when an approval is required rather than auto-approving."
- :819-820 → "A turn that ends mid-loop changes nothing — the item stays in flight, the work stays in the tree, and the run halts for the user to resume."
- :822 → "with per-item checkpoints as the safety net"

Since the deck is untracked, fixing it before the commit keeps the working tree self-consistent — landing 45dcf577 while the tree carries a doc contradicting it would ship the contradiction whenever the deck is committed.

## 5. Conclusion

Round-4's conclusion holds for everything rounds 1-5 prescribed: with the two round-4 fixes, the 45dcf577 contract documentation is fully consistent end to end across the changed files, README.md, PLAN.md, and the .coding/knowledge/ corpus — no stale claim remains on any prescribed surface, and neither fix introduced a new inconsistency. The one remaining stale claim in the repository lives in the user-facing explainer deck, a surface no prior round swept. Fix R5-LOW-1 (four line edits in docs/why-mnemo-deck.md slide 14), re-run cargo test (docs-only change), and the 45dcf577 documentation is consistent everywhere the contract is stated.
