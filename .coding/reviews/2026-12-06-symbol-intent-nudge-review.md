## Verdict: FINDINGS (0 high, 3 low)

Review of ALL uncommitted changes on wt/agenticcoder for plan 08eb13d8 (backlog 8b8f40d2, symbol-intent nudge). Core implementation is correct: the candidates chain preserves precedence, the wording contract holds, the C5 gate inherits the intent shapes automatically, and the tests discriminate. Three low findings: one stale doc (PLAN.md), one promised regression assertion never landed, one commit-completeness risk on sibling-plan artifacts. Detail below.

## Scope reviewed

`git diff HEAD` (uncommitted, wt/agenticcoder): src/tool/agent/search.rs (strip_symbol_intent + symbol_nudge candidates chain + module doc/DESCRIPTION + 2 new tests), src/tool/agent/search_read.rs (description clause), README.md:53, .coding/plans/08eb13d8.md, .coding/backlog.jsonl — PLUS two sibling-plan artifacts the task brief did not list: .coding/plans/c8b3228d.md (modified) and .coding/knowledge/bug/c8b3228d.md (untracked). All of it reviewed; tests reported green (root 1753+16, src-tauri 178+4, under `#![deny(warnings)]`).

## Findings (all LOW — fix or justify skip)

- **F1 (LOW) — PLAN.md stale: intent shapes missing from the Agent-tools nudge enumeration.** PLAN.md:102-106 still reads "when the pattern is a bare identifier OR a definition-prefixed name (`fn X`, `pub struct X` — `strip_definition_prefix`) that exactly names an indexed symbol" — no mention of the new shapes (`X(`, `a::b`, `who calls X`). README.md:53 and the module doc/DESCRIPTIONs were updated; PLAN.md's search bullet is the established sync surface for nudge widenings (precedent: plan 812e1f0a step 6, 2026-09-15 review check (h)), and the project constitution names PLAN.md in the reviewer's docs-sync duty. Fix: extend that clause with the intent shapes (one sentence, mirror the README phrasing).
- **F2 (LOW) — promised regression assertion never landed.** Plan step 3 item (7): "regression: bare 'foo' still emits the byte-pinned sentence exactly (assert equality with the current pinned string)" — step is checked [x] but no such equality assertion exists. Every pin is a `contains` of a substring (marker "is an indexed symbol", the variant prefix, the embedded id); the sentence TAIL ("gives its definition + callers in one call; prefer the graph tools for symbol lookups (search is for text)") is not asserted against real output anywhere — the full-sentence literals in dispatch.rs:801 (`HELLO_NUDGE`) and steering_stats.rs tests are input fixtures, not output assertions. A future rewording of the tail would pass every test while violating the 2026-09-15 byte-pin spec. Fix: in `bare_identifier_symbol_searches_earn_the_graph_nudge` (search.rs:1759), add one assertion that the output contains the full pinned sentence: `"'hello' is an indexed symbol — graph_context(id=\"a.rs::hello::1\") gives its definition + callers in one call; prefer the graph tools for symbol lookups (search is for text)"` (or equality on the extracted note line).
- **F3 (LOW) — sibling-plan artifacts at commit-orphan risk.** The uncommitted set includes .coding/plans/c8b3228d.md (regression-test reference de-qualified to the bare name — verified correct: the test is src/memory/knowledge.rs:1044, module path `memory::knowledge::tests`, so the old `knowledge::tests::` prefix was wrong; bare name is unique, one hit) and the untracked .coding/knowledge/bug/c8b3228d.md (content matches the already-committed fix c9d4b40 "knowledge exports can no longer be back-dated by a stale clock" — coherent). Neither is in the task brief's CHANGED FILES list; if the closing commit stages only the listed files, the bug export stays untracked and the plan edit dangles. Fix: include both in the closing commit (they are legitimate bookkeeping for the merged d7b2e722 fix).

## Verification of the requested checks

1. **Candidates-chain correctness — PASS.** Order in symbol_nudge is definition-prefix → bare → strip_symbol_intent → alternation fallback (else arm). Note the plan's step-2 parenthetical said "prefix/bare/alternation first, then the new intent shapes" while the implementation puts intent BEFORE alternation — this is provably equivalent: alternation patterns require a top-level `|`, which the path guard rejects, the call-site branch rejects (`a|b(` → base `a|b` fails `is_bare_identifier`), and the phrasing targets reject (rest must be a bare identifier), so the two matchers are disjoint — no pattern can reach both, order is behaviorally irrelevant. Prefix/bare precedence over intent is preserved; previously-silent intent patterns (old code: alternation_nudge on a `|`-free pattern → None) now nudge; alternation behavior unchanged.
2. **Extraction precision — PASS.** `::`-path guard requires every char in alnum/_/:/.// — any prose (spaces) fails; the tail additionally passes `is_bare_identifier` and EVERY candidate resolves through the exact case-sensitive `symbol_id` (verified against src/codegraph/mod.rs:209 — `symbol_id_fuzzy` is a separate method used only by the alternation path). Phrasing heads are case-sensitive, space-terminated; targets must reduce to bare identifiers after one optional qualifier; "who calls the shots" → "the shots" → None, "callers of hello world" → None, unindexed targets → lookup None (all covered by `non_symbol_intent_patterns_stay_silent`). Minor nit (not a finding): the path guard uses Unicode `is_alphanumeric` while `is_bare_identifier` is ASCII-only — harmless, the exact lookup gates everything and the nudge is advisory.
3. **Wording contract — PASS with F2.** Both note variants carry the marker substring (search.rs:594 bare, :599 variant); the byte-pinned bare sentence is byte-identical (the diff only rewires candidate resolution, not the format! strings); existing pinning tests are unmodified (the test-region hunks are pure additions after old line 1764); the new shapes ride the variant sentence as the contract requires.
4. **C5 gate — PASS, no sync needed.** dispatch.rs:717 `symbol_search_redirect` calls `crate::tool::agent::search::symbol_nudge` directly (single shared shape authority — intent shapes inherit the intercept automatically); no second shape enumeration exists anywhere. Steering detection stays intact for the merged-note cases: `merged_note` "; "-joins fallback/nudge/tip onto ONE first line and `with_note` prepends it, while `MarkerKind::detect` (steering_stats.rs:193-196) is first-line `contains` — so even the broken-regex `hello(` case (literal-fallback note + nudge on one line) still counts toward C3/C5 escalation.
5. **`hello\(` negative — correct as designed, no flag.** The backslash fails `is_bare_identifier`, so the escaped form stays silent. In regex mode `hello\(` IS a call-site hunt (a recall miss), but in literal mode it hunts the verbatim text `hello\(` (e.g. regex definitions in source), where a nudge is noise; `symbol_nudge` is mode-blind (no literal flag), so silence is the conservative precision-first choice, consistent with the 2026-08-29 review's precision stance. Keeping the pin is right.
6. **False positives / tests / security / platform — PASS.** FP surface is exactly "pattern names an indexed symbol" (exact lookup) — the only residual is a symbol literally named `it` making "where is it defined" nudge, which is accurate behavior. Both new tests discriminate: pre-change all 6 positive patterns returned None (assert on the variant sentence + embedded id fails), and the 4 negative cases pin the precision guards. Security: no new input surface — pattern text feeds string ops + one indexed SELECT; no path/fs handling, no injection path. Multi-platform: pure std string operations, no OS-specific behavior, no cfg(windows); zero-warning build reported under deny(warnings).
7. **Docs sync — F1 (PLAN.md).** Module doc (search.rs), search + search_read DESCRIPTIONs, and README.md:53 are accurate and mutually consistent ("graph-first for symbol questions… this tool is for text occurrences"); dispatch.rs's C5 doc comment stays generically worded ("a pattern that names an indexed symbol") — still true.
8. **Bookkeeping coherence — PASS with F3.** backlog.jsonl: 8b8f40d2 pending→in_flight correct; removals of d7b2e722 (fix committed in c9d4b40) and 21e23788 (reviewer git access — exercised by this very review via git_read) match the established removal-on-done convention (past done items b5503915/a634835c are absent from HEAD's file). The new HOW export and the c8b3228d bug export both carry date 2026-09-01 — verified NOT the back-dating bug: the monotonic artifact floor (max(wall clock, newest on-disk artifact)) yields 2026-09-01 since every on-disk knowledge artifact is ≤ 2026-09-01; the fix is behaving as designed.

## Bottom line

0 high / 3 low. The mechanism is sound and demonstrably live (this session's own searches were intercepted by it). Fix F1 (PLAN.md sentence) + F2 (one equality assertion) + F3 (commit the sibling artifacts), re-run `cargo test` unpiped, then commit everything on wt/agenticcoder including this report.
