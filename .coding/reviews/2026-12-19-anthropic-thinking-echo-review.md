## Verdict: PASS

Bug fix is correct, minimal, and complete: the fabricated unsigned `thinking` echo is removed from the only Anthropic assistant-serialization site, the receiving/SSE side is untouched, the DeepSeek/OpenAI echo is untouched, and the flipped regression test demonstrably fails without the fix and passes with it. One informational note (no action required).

**Scope reviewed** (git diff HEAD, branch wt/agenticcoder, plan 8f7c2095): `src/provider/anthropic.rs` (+29/−15); `.coding/knowledge/decision/2026-12-19-file-allowed-in-debug-browsers-for-local-html-de.md` (pointer metadata only, see below); untracked `.coding/plans/8f7c2095.md` (the plan file itself — expected).

### 1. Correctness — PASS
- **Echo gone from the Anthropic request path.** `"role": "assistant"` wire construction exists in exactly one place in src/ (anthropic.rs:420, inside `message_to_json`); the echo block is deleted and replaced by the NOTE comment (:374-383). `build_request_json` (:255-276) only delegates to `message_to_json` (plus tool-result coalescing) — no alternate path. Exhaustive literal search for `"type": "thinking"` across all of src/: **zero matches** — no remaining fabricated thinking-block serializer anywhere.
- **Receiving side untouched.** `thinking_delta` → `LlmEvent::ReasoningDelta` at :563-569 intact, so reasoning still streams, displays, and is stored on `Message.reasoning_content`; its SSE test `thinking_delta_emits_reasoning_delta` (:1875) is intact.
- **openai.rs DeepSeek echo untouched.** openai.rs:1323-1332 still echoes `reasoning_content` on assistant messages (required by DeepSeek — missing field is a 400 there), guarded by its own test `build_request_json_echoes_assistant_reasoning_content` (:3071). openai.rs is not in the diff.
- **Regression test is a true red/green pair.** Old test asserted the buggy echo (content[0] = thinking block); new test `assistant_reasoning_is_not_echoed_as_an_unsigned_thinking_block` (:1714-1738) seeds `reasoning_content = Some("thinking step")` (non-empty, so the old builder definitely fired) and asserts NO block of type "thinking" in the outgoing content array plus content[0] = text "answer". Pre-fix: `any(...)` is true → assert fails (matches the reported red run at :1729/'no fabricated thinking block may be sent'). Post-fix: passes, and it additionally pins ordering/content (stronger than the old test). No dangling references to the old test name remain. Tests were reported green via unpiped `cargo test` and `cargo test --features browser` (all 30 provider::anthropic tests); as a read-only reviewer I could not re-run cargo, but structurally the change (code removal + comments + string literal + test-body rewrite, no new symbols/imports) carries zero warning risk under `#![deny(warnings)]`.

### 2. Root-cause match — PASS
The observed 400 `messages.N.content.0.thinking.signature: Field required` is precisely what the old builder produced: it fabricated `{"type":"thinking","thinking":r}` with no `signature` field as content[0] of every prior assistant message. Anthropic requires any thinking block sent in history to carry the original per-block signature, delivered in SSE `content_block_start` — and the parser's `content_block_start` arm (:530-550) handles only `tool_use` and `text`, so the signature is never captured (verified: `signature` appears nowhere in src/provider outside the new comments). With the echo removed, no unsigned thinking block can enter an Anthropic request, so the 400 cannot recur. No other Anthropic-path serializer exists (check 1).

### 3. Project rules — PASS
- **Doc comments accurate.** The NOTE (:374-383) is factually correct on all three claims (signature required in history; parser never captures it; only DeepSeek/OpenAI-compatible endpoints require the echo — confirmed at openai.rs:1323). Error text at :417 correctly synced to "no text or tool calls" (thinking is never serialized on this path now). `Message.reasoning_content` doc (mod.rs:160-164) scopes the echo requirement to DeepSeek thinking mode and remains accurate. anthropic.rs module doc (:11) lists content blocks as text/image/tool_use/tool_result — thinking was never among them; still accurate. README/PLAN surface: user-facing docs don't document request-history echo behavior; no doc update owed.
- **Multi-platform neutrality.** No platform-specific code, paths, or shell syntax added — pure request-builder + test changes.
- **Knowledge-file edit (unrelated but verified benign).** The decision record pointer was corrected from "commit 7280bd9 on wt/agenticcoder, 2026-12-08" to "merged into main at 297ce1c3 on 2026-12-19"; verified `297ce1c3` exists and is exactly the merge commit that landed the file:// decision (plan 541b6381). Accurate knowledge-store metadata sync, no code impact.

### Informational note (low, no action required)
With extended thinking + tool use, Anthropic *recommends* passing back the prior assistant turn's genuine thinking blocks (with their real signatures) for best continuation quality — but doing so faithfully would require capturing `signature` in `content_block_start` (a feature-sized change, out of scope for this bug fix). Dropping the blocks is the only safe option today and is API-valid; the NOTE comment already documents the signature-capture limitation. Flagging for a possible future enhancement, not a defect.
