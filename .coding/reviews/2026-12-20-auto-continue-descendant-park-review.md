## Verdict: FINDINGS (0 high, 4 low)

**Scope reviewed:** full `git diff HEAD` (`.coding/plans/9d232076.md`, `src/agent/loop_impl.rs` +19, `src/runtime/agent.rs` +182) plus untracked `.coding/plans/bbd712c9.md`, `.coding/knowledge/bug/9d232076.md`, `.coding/knowledge/bug/2026-12-20-auto-continue-spams-a-parent-waiting-on-spawned.md`, on branch `wt/agenticcoder`, bug plan bbd712c9.

**Summary:** The fix is correct, minimal, and consistent with the dispatch gate it mirrors. The park gate sits only in the None arm; no lock is held across the new await; None-tracker loops keep pre-change behavior; the two regression tests genuinely exercise the changed path and are red-pre-fix/green-post. Only minor doc-sync nits below.

### What I verified (PASS items)

- **Gate placement (agent.rs:230-233).** The `&& !self.agent_loop.has_running_descendants().await` condition is only in the `None` arm. Steer/InterruptWithSteers (:181-205), Cancel (:206), Compact (:207/211-213), Clear (:214), Interrupt (:216) arms untouched — Suggestion/Steer/Cancel/Compact behavior is byte-identical to before.
- **No lock across await (loop_impl.rs:1555-1562).** `self.descendant_tracker` is a direct field read (no lock); `agent_id()` (:1206-1208) acquires the std Mutex, copies the `Option`, and drops the guard within the expression before `.await`; `is_workflow_executing()` (:1540-1543) acquires/releases the workflow tokio Mutex inside its own body. The prod tracker (IpcSpawner) walks AgentManager under its own lock. No deadlock, matching the dispatch gate's own comment (dispatch.rs:168-172).
- **Dispatch-gate consistency.** Identical None-tolerant pattern and identical field/semantics as dispatch.rs:184-196 ("tracker + agent_id, None → skip gate"). The parallel-work trade-off (parking pauses a parent's own non-step work while children run) is documented in the new method's doc comment and is defensible: `complete_step` is itself gated during a child wait, so no plan progress was possible anyway.
- **None-tracker loops unchanged.** `None → false` means every test loop built via bare `AgentLoop::new` keeps pre-change behavior; sibling test `prompt_resets_auto_continue_budget_after_exhaustion` (:4765, no tracker) passes in the reported clean run.
- **Regression tests.** `FlagDescendantTracker` (agent.rs:4857+, `#[async_trait]`, matches the `DescendantTracker` trait signature; import already present via `CountingMockProvider`). `auto_resume_parks_while_descendants_running`: the 400ms absence assertion is deterministic post-fix (flag=true ⇒ gate fails at end of turn 1, zero further calls possible), and pre-fix it produced 13 calls — genuinely RED/GREEN. `auto_resume_resumes_after_descendants_finish` proves a parked task still processes a fresh Prompt (liveness) within a 10s deadline. Both drain fan-in and join the task handle at teardown (no channel-full wedge). Workflow-Executing setup via `create_plan` before loop build matches the sibling harness (:4767-4775). No `#[allow]`, no dead code, no new imports; new pub method is doc-commented per project rule.
- **Multi-platform neutrality.** Pure Rust core (`std::sync::atomic`, tokio); no platform-specific API, path, or shell syntax. Frontend untouched.
- **.coding/ sidecar.** Plan file bbd712c9.md matches the shipped fix; the new BUG knowledge file is accurate (one pointer nit → L4); the `9d232076.md` edit normalizes the regression-test line to the concrete test path and now agrees with the `9d232076.md` knowledge file. In-scope bookkeeping, not code.
- **Flaky-approval-test note.** `cancel_during_approval_exits_agent` / `interrupt_during_approval_stops_turn_and_keeps_agent_alive` exercise the Cancel (:206) and Interrupt (:216) arms, which `return` unconditionally before the None arm — consistent with the assessment that their one-off parallel-load failure is unrelated to this change; both pass in isolation and in the final clean full run. I verified by inspection (reviewer is read-only, no shell) and rely on the reported unpiped `cargo test` numbers (1758 passed / 0 failed / 4 ignored; src-tauri 178+4) with `#![deny(warnings)]` at both roots proving warning-free builds.

### Findings

**L1 (low, doc sync) — `descendant_tracker` field + builder docs name only one of two consumers.** `src/agent/loop_impl.rs:257-262` ("lets the dispatch layer check…") and `with_descendant_tracker` doc (:652-656) ("so the dispatch layer can gate workflow-state transitions") predate this change, which adds the auto-continue park as a second consumer of the same field. Fix: add one clause each, e.g. "…and by the auto-continue logic (runtime/agent.rs) to park a parent while descendants run" — so the next reader doesn't have to rediscover the second use.

**L2 (low, doc sync) — `DescendantTracker` trait doc stale.** `src/runtime/mod.rs:265-269` says "Used by the dispatch layer to enforce the refined subagent permission model…" — now also consumed by the auto-continue park. Fix: extend to "Used by the dispatch layer (workflow-state transition gate) and the runtime auto-continue logic (park-while-descendants-run)…".

**L3 (low, doc sync) — `auto_continue_streak` field doc doesn't mention the new park reason.** `src/runtime/agent.rs:42-47` describes parking only via MAX_AUTO_CONTINUE exhaustion ("prevents infinite loops on genuine deadlocks"). The plan's own FIX DIRECTION called for updating this comment (:42-47); it wasn't. Fix: add one line, e.g. "A turn also parks (without consuming the streak) while spawned descendants are running — see the None arm in `run_turn_with_retry`."

**L4 (low, stale pointer in new knowledge file).** `.coding/knowledge/bug/2026-12-20-auto-continue-spams-a-parent-waiting-on-spawned.md` line 6 cites "descendant_tracker field, loop_impl.rs:618" — the field is at `loop_impl.rs:262`; the new method is `:1555` (`with_descendant_tracker` is `:657`; nothing descendant-related lives at `:618`). Fix the pointer before commit so the file's own grep-ability claim holds.

**Non-blocking observation (test strength, no action required):** in `auto_resume_resumes_after_descendants_finish`, the post-flip assertion exits at `calls >= 2`, which the fresh-Prompt turn itself satisfies (a Prompt also resets the streak at agent.rs:533), so the test pins park-liveness + the descendant gate but does not strictly distinguish "auto-continue resumed after descendants finished" (that would be `calls >= 3`). Test 1 is the RED/GREEN regression carrier, so this doesn't weaken the defect coverage; noted for future tightening only.

**Verdict rationale:** zero high findings; the four low findings are documentation-sync items squarely inside the project's review expectations (module doc comments must match shipped behavior). Fix all four (trivial edits, no logic changes), then commit — no test re-run strictly required for comment-only edits, though the standing rule (re-run tests after fixing) applies.
