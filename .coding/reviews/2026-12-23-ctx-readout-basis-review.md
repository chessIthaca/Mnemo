## Verdict: PASS

Re-review of plan addf3727 ("Fix ctx readout basis: provider-anchored, tools+reasoning-inclusive token accounting", bug da4fc87d) on wt/agenticcoder, after the round-1 finding (1 high) was fixed. Every fix verifies against the working tree; no new findings.

## Re-review of the round-1 finding

**Original finding (HIGH):** the tools-schema overhead (`TokenAccounting.tools_tokens`) was recorded *after* the iteration's only accounting reads — `update()` at the old turn.rs:362 fed the top-of-loop ContextUsage emission, the summarize trigger, and the preflight hard-ceiling check — and each loop-back `reset()` zeroed it again, so the value never reached a live read (dead store).

**Fix verification (file/line evidence from the current working tree):**

1. **Hoist to iteration top (turn.rs).** The order is now: `tool_filter` read at 364-367 (own workflow lock, `wf.allowed_tokens()`) → `tool_schemas` built at 369 (`provider.capabilities()` — `provider` is resolved for the iteration at 316-330) → plan-mutation pruning at 370-379 (`plan_mutations_allowed()`, a construction-time per-agent flag per loop_impl.rs:1212-1218, not mutable mid-turn) → `set_tools_tokens(estimate_tools_tokens(&tool_schemas))` at 391-392 → `update(messages)` at 399. The comment block at 380-390 documents the ordering rationale and the 2026-12-23 review finding. The old late block is gone (no `set_tools_tokens` remains anywhere except 392 — graph confirms one call site in `run_turn`), and the head/tail builder no longer returns `tool_filter` (comment at 704-706; it returns only `stable_head`/`volatile_tail`).
2. **Every live read now follows the set.** Summarize trigger 409, preflight hard-ceiling 447-448, no-summarize ContextUsage emission 601-612, and the summarize-path emission 530-544 (`reset()` → `update()` → `used`) all consume a count that includes the block — `update()` returns `total()` (context.rs:715), which adds `tools_tokens` (777-783). Stale-value leak is impossible: the loop-back `reset()` at 1986 preserves the field, and the next iteration top re-sets it from the fresh schemas *before* that iteration's first read. The estimated vector is exactly the vector sent: `&tool_schemas` at 917 is the same variable estimated at 392 (only the retain at 373 mutates it, before both uses).
3. **reset() preserves tools_tokens (context.rs:682-695).** It clears `counted_len`/`head_content`/`head_count`/`breakdown` but keeps `tools_tokens`, with the documented rationale (684-689): the schema array is independent of the conversation, so the post-summarization recount emission must still include it; the turn loop re-sets it each iteration anyway. Safe at both call sites: the summarize path (530) recounts rewritten messages while the schemas actually sent this iteration are unchanged; the loop-back (1986) is followed by an iteration-top re-set before any read. `reset()` has exactly one production caller (`run_turn`), so no other contract violation exists; the doc states the contract for future callers. `set_tools_tokens` doc (766-770) states the top-of-iteration, before-first-update contract.
4. **Hoist is behavior-preserving.** Everything the filter/schemas depend on is fixed at iteration top: workflow state is read under short-lived read-only locks (364-367, 318-330); provider capabilities come from the per-iteration `provider` snapshot; `plan_mutations_allowed()` is immutable per agent. Between iteration top and the old late site, only summarization (operates on cloned context manager + provider stream; no workflow mutation), auto-recall (memory store only), and the read-only head/tail build run. The tool registry can change mid-turn (load_tools), but schemas are rebuilt at the next iteration top and the same vector is used for estimate and send — accounting always matches the wire.
5. **Consistency (item f).** The bar re-anchors to provider-reported `usage.prompt_tokens` after each response (turn.rs:1194-1217, regression test `context_usage_reanchors_to_provider_prompt_tokens` at tests.rs:668); the local estimate is message buckets + the schema block via `total()`; `estimate_tools_tokens` (provider/mod.rs:712-727) uses the documented chars/4 fallback basis consistent with `estimate_prompt_tokens` — an estimate corrected by the provider anchor, not a text-length-as-tokens count.
6. **Constitution.** No `#[allow(]` anywhere in `src/**/*.rs`; suites green under `#![deny(warnings)]` (Rust 1868 + 16 passed, frontend vitest 709/709 — frontend TS untouched). Public fns documented (`reset`, `set_tools_tokens`, `estimate_tools_tokens`). Multi-platform neutral: pure Rust logic + one TSX tooltip string, no Windows-only APIs/paths. Doc-sync: the InflightBar tooltip (InflightBar.tsx:350-353) documents the provider-anchored basis; README.md/PLAN.md make no claims that contradict the new accounting.

## Regression-test check (bug-plan requirement)

- **New test `context_usage_estimate_includes_tools_schema_overhead` (tests.rs:588)** asserts the first ContextUsage `used ==` per-role buckets `+ estimate_tools_tokens(`the exact schemas the loop sends`)`, with `expected_tools > 0`. Under the old ordering `tools_tokens` was 0 at the first emission and zeroed at every loop-back, so `used` would equal buckets only — the test fails without the fix and exercises the exact changed path (iteration-top set → update → emission).
- **`context_usage_event_carries_cached_token_count` (tests.rs:509)** updated to the same provider-basis expectation (messages + schema block, `expected_tools > 0`) — fails without the fix; its old message-only premise is documented as superseded in the test comment.
- **`run_turn_completes_deferred_swap_after_summarization` (tests.rs:5443)** re-sized to a tool-free registry, with a comment (5558-5562) explaining the mechanism: the 8-tool schema block is now correctly part of the summarize trigger, which would re-fire after model-switch summarization against the deliberately tiny 2_000-token sizing and consume the new provider's queued response — a sizing artifact, not a semantic change.
- **Root cause documented:** BUG memory record 21c583e0 ("ctx readout undercount fixed — provider-anchored bar + reasoning/tools-inclusive accounting") plus inline root-cause doc in the regression tests (tests.rs:589-595, 668-674; context.rs:1124-1130, with unit test `token_accounting_includes_tools_overhead` at 1131).

No new findings.
