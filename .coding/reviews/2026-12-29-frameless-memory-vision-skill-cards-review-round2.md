## Verdict: FINDINGS (0 high, 1 low)

Round-2 re-review of backlog 900fe8d8 / plan 10650092 (frameless + normal-size activity cards: memory/vision/skill cards frameless, card body text at chat content size). All four round-1 fixes are correctly applied and complete, introduce no regressions, and the strengthened source-contract block passes for the right reasons. One low finding remains — a round-1 miss of the same class as F1: the shared `UnifiedDiffView`'s default `max-h-64` scroll cap (plus rem-based line padding) renders inside the expanded file_edit card, so the DECISION/SPEC records' blanket "all card utilities are em-based" claim still has one literal exception.

Method: full `git diff HEAD` + `git status` (5 modified, 3 untracked); complete read of Message.tsx (all five card scopes, 1005 lines), Conversation.tsx, the strengthened test block (toolCardPaths.test.ts :735-866) and its imports, the DECISION/SPEC records, the plan file, the backlog diff, and the round-1 report; regex sweeps for rem-based utilities over Message.tsx, Conversation.tsx, and the whole chat/ directory; `max-h-64` and `text-sm` sweeps over frontend/src; globals.css anchors re-verified (:8, :234). Test evidence (vitest 768/768, 53 files, 93 toolCardPaths tests; tsc+vite green; cargo 1922+16/0, no Rust changes) accepted as reported — the reviewer is read-only.

## Round-1 fixes — all four verified

**F1 — CallDetail scroll caps → `max-h-[24em]` (VERIFIED).** Exactly three sites (:983 stdout, :990 stderr, :997 generic output), all in CallDetail; zero `max-h-64` left in Message.tsx (the only remaining frontend hits are DiffView.tsx ×3 + MemoryDebugView.tsx ×1 — neither is Message.tsx; see Finding L1 — plus the denylist entry itself). `"max-h-64"` pinned in REM_UTILITIES (:789). The 24em value is correct and the deviation from round 1's 16–18em suggestion is sound: em in max-height resolves against the pre's OWN `text-[0.75em]` font, and the chain from Conversation's `fontSize: var(--app-font-size)` (:94) through the ToolCard outer (:754), expanded section (:845), and CallDetail container (:910) sets no intermediate font-size — so the pre font is 0.75 × 14px = 10.5px and 24em = 252px ≈ the old 256px (16rem): parity at the default, scaling thereafter. Round 1's 16–18em would have resolved against the same 10.5px font (168–189px, a ~30% shrink). The DECISION records the reasoning accurately ("resolving against the pre's own 0.75em font ≈ the old 16rem at the default size").

**F2 — `text-sm` in REM_UTILITIES (VERIFIED, no false positives).** Entry at :775. `text-sm` occurs in Message.tsx only at :313/:337/:375/:386/:417/:418 — the user bubble, streaming assistant text, error entry, steer bubble, and qa record, all MessageImpl non-card cases outside every audited scope; `text-slate-*`/`text-violet-*`/`text-amber-*` substrings don't collide (re-verified as round 1 predicted).

**F3 — ToolCard scope + vision affordance pins (VERIFIED, passing for the right reasons).** `["ToolCard", fnSrc(src, "ToolCard")]` at :798. fnSrc resolves to the unique `function ToolCard(` (:664) and scopes :664–872 (to `function CallDetail(`) — the genuine component, not an empty or wrong slice. Direct read confirms it is clean: no `rounded-lg`, none of the five frame strings, `py-[0.125em]` outer (:754), and zero REM_UTILITIES substrings (independent regex sweep found no rem-based utility anywhere in :664–871; the images row's gap-2 is now `gap-[0.5em]` :863, chevrons `h-[1em] w-[1em]` :769). Vision affordance pins (:861-864: `role="button"`, ChevronRight, ChevronDown) all genuinely present in VisionEntryCard (:240/:252/:254). The frame-class loop now covers all four card families.

**F4 — transcript stack gap (VERIFIED).** Conversation.tsx:97 `space-y-[0.5em]`, resolving against the container's `var(--app-font-size)` (7px vs the old 8px at the default 14px — imperceptibly tighter, scales with the setting). The only rem-based utilities left in Conversation.tsx are pre-existing, unchanged, out-of-scope layout values (:91 `px-4 py-4` scroll padding, :97 `max-w-4xl` column width — not card spacing). The SPEC 4c2f16ba "chat transcript" tension is resolved.

## No regressions from the fixes

- All five audited scopes are genuinely clean (sweep + direct read): MemoryEntryCard :120-215, VisionEntryCard :230-306, skill case :392-410 (caseSrc's `lastIndexOf` picks MessageImpl's — the only other `case "skill":` is arePropsEqual's :67; the `\n    case ` terminator lands on `case "qa":` :411), ToolCard :664-872, CallDetail :873-1005 (slice-to-EOF; last fn in the file). Zero rem-based utilities in any of them; the only regex hits inside those ranges are the doc-comment words "em-based" (:104/:228), which contain no pinned string.
- fnSrc's include-next-fn's-doc-comment behavior is harmless as documented: VisionEntryCard's doc comment (:217-229) falls inside MemoryEntryCard's scope and matches nothing pinned.
- `readFileSync` imported (:16, pre-existing); the new describe sits cleanly between the memory_search block (ends :733) and fileEditDiff (:868). The fixes strengthened existing `it` blocks without adding new ones, so 768/768 (round 1: 768/768 incl. 4 new) is consistent; 93 toolCardPaths tests = 89 + 4.
- Records: the DECISION's test-pin claim now matches the code exactly (ToolCard in scopes, text-sm/max-h-64 in the denylist, py-[0.125em] outer, affordances for memory + vision); the max-h-[24em] and space-y-[0.5em] lines are accurate; the SPEC (d69d168f) citations verified (Conversation.tsx:94, globals.css:234 `.prose { font-size: inherit; }`, globals.css:8 `--app-font-size: 14px`). Backlog bookkeeping correct: 900fe8d8 pending → in_flight (plan active, in review — flips to done at finish) with the plan-id note per the 45dcf577 linkage model. Doc sync unchanged from round 1 (README/PLAN.md carry no styling claims; the fixes touch none). Multi-platform neutral (className/TSX + the established node:fs test pattern). Diff seams tight — no line-ending or splice damage from the fix edits.

## Finding

### L1 (low) — `UnifiedDiffView`'s default `max-h-64` (and rem line padding) renders inside the expanded file_edit card; the records' "all card utilities are em-based" claim still has one literal exception

CallDetail renders `<UnifiedDiffView diffText={editDiff} />` (Message.tsx:954) with no `maxHeightClass` prop, so the shared renderer's default applies — `maxHeightClass = "max-h-64"` (DiffView.tsx:225, applied by renderDiffLines :160-163) — a fixed 256px rem-based scroll cap inside the expanded card of exactly the tool family whose diff IS the card body (file_edit). The diff lines' padding is rem-based too (`px-2 py-0.5`, DiffView.tsx:173/:188/:249). The diff TEXT scales (the container carries `text-[0.75em]`), so the two-font-size guarantee for text holds — only the cap and line padding stay fixed: at large `--app-font-size` every other expanded output caps at 24em while the file_edit diff stays at 256px. This is the same class as round-1 F1 (a rem-based scroll cap inside the expanded card falsifying the records' claim), one level deeper — a shared child component round 1 recorded as "UnifiedDiffView intact" but did not audit. The card fns in Message.tsx are clean; the blanket sentences are what remain overstated:

- DECISION 344bc076: "All card utilities are em-based per SPEC 4c2f16ba — including the CallDetail pre-block scroll caps…"
- SPEC d69d168f: "All card spacing/font utilities em-based per SPEC 4c2f16ba"

**Fix (either resolves; both is best):**

1. Records: narrow the claim to the card components ("all utilities in the card components (Message.tsx) are em-based") and add UnifiedDiffView to the existing shared-child carve-out alongside CodeBlock/InlineMarkdown (shared code-viewer treatment, also used by the approval prompt and the Diff tab).
2. Code (recommended, one line): pass `maxHeightClass="max-h-[24em]"` at the CallDetail call site (:954) — card path only; the shared default stays for the other consumers. The class lands on the container that carries `text-[0.75em]`, so it resolves exactly like the pre-block caps (24 × 10.5px = 252px ≈ the old 256px). Optionally pin it in the CallDetail test: `expect(detail).toContain('maxHeightClass="max-h-[24em]"')`.

## Notes (no action required)

- "fn-scoped over ToolCard, MemoryEntryCard, VisionEntryCard, and the skill case" (DECISION) — the skill case is caseSrc-scoped, not fn-scoped; trivial wording, the coverage claim itself is accurate.
- REM_UTILITIES remains a curated denylist of the old code's values (p-2, gap-4, text-base, h-4 etc. would slip through) — settled as acceptable in round 1 (F2); `text-[0.875em]` sitting in the list is em-based but intentional (it pins the removed header downscale).
- Pre-existing out-of-scope rem usage, unchanged and documented: streaming assistant `text-sm` (:337), user/error/steer/qa message frames, Conversation's `px-4 py-4`/`max-w-4xl`, ToolImage dialog/copy chrome, CodeBlock/InlineMarkdown (assistant prose), MemoryDebugView (debug panel, not a card).
- Tier-badge padding `px-[0.375em] py-[0.125em]` resolves against the badge's own 0.65em font (~3.4px/1.1px at default vs the old 6px/2px) — a tighter, proportionally scaled chip; consistent with the em-based convention and round 1's acceptance.
- The round-1 report (untracked, to be committed with the fix commit) remains an accurate snapshot of round 1; the 24em deviation from its 16–18em suggestion is justified and documented in the DECISION.
