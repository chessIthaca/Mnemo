## Verdict: PASS

Round-3 (final) review of backlog 900fe8d8 / plan 10650092 — frameless + normal-size activity cards (memory/vision/skill cards frameless, card body text at chat content size). The round-2 L1 fix is correctly applied on both prongs (the card-path diff cap and the narrowed records), the test pin is present and passing for the right reasons, and a full independent sweep of the entire change set found nothing further. No findings; the change is ready to commit.

## Method

Full `git diff HEAD` + `git status` (5 modified, 4 untracked); complete read of Message.tsx (all 1005 lines — every card scope line by line: MemoryEntryCard :120-215, VisionEntryCard :230-306, skill case :392-410, ToolCard :664-871, CallDetail :873-1005 incl. the file tail), DiffView.tsx (all 257 lines), DiffViewer.tsx call sites, Conversation.tsx, the full new test block, both knowledge records, the plan file, the backlog diff, and both prior review reports; a project-wide `UnifiedDiffView` symbol lookup (all call sites); CR sweeps over `frontend/src/**` (187 files, zero hits) and `.coding/**/*.md` (only two pre-existing CRs in `.coding/analysis/cache-hit-4-report.md`, untouched by this change set); globals.css anchors re-verified (:8, :234). Test evidence (vitest 768/768, 53 files, 93 toolCardPaths tests; tsc+vite green; cargo 1922+16/0, no Rust changes) accepted as reported — the reviewer is read-only.

## Round-2 L1 fix — verified, both prongs

**Code prong (VERIFIED).** Message.tsx:954 — `<UnifiedDiffView diffText={editDiff} maxHeightClass="max-h-[24em]" />` — and it is the *single* UnifiedDiffView call site in Message.tsx (symbol lookup: import :10 + usage :954 only). The prop is the existing optional `maxHeightClass?: string` (DiffView.tsx:219) with default `"max-h-64"` (:225) — DiffView.tsx is untouched (not in the modified set), so the shared default stands. Non-card consumers are unaffected exactly as claimed: ApprovalPrompt.tsx:287 renders with the default (unchanged behavior), and all three DiffViewer.tsx sites (:85, :113-116, :449) pass their own `maxHeightClass="max-h-full"`.

**Resolution math (VERIFIED).** `renderDiffLines` (DiffView.tsx:160-163) puts `text-[0.75em]` and `${maxHeightClass}` on the *same* container div, so `max-h-[24em]` resolves against that div's own 0.75em font. The chain from Conversation's `fontSize: var(--app-font-size)` (:94) through the ToolCard outer (:754), expanded section (:845), CallDetail container (:910), and result div (:943) sets no intermediate font-size — so the diff font is 0.75 × 14px = 10.5px and 24em = 252px ≈ the old 256px (`max-h-64` = 16rem) at the default, scaling with the setting. Identical resolution to the pre-block caps at :983/:990/:997 (same element carries `text-[0.75em]` + `max-h-[24em]`), so the expanded file_edit card now caps exactly like every other expanded output.

**Test pin (VERIFIED, passing for the right reasons).** The "expanded CallDetail body stays em-based" test now asserts `expect(detail).toContain('maxHeightClass="max-h-[24em]"')`. `fnSrc(src, "CallDetail")` resolves to the unique `function CallDetail(` (:873 — the :847 occurrence is JSX usage `<CallDetail`, not matched) and slices to EOF; the file tail (:1000-1005) confirms CallDetail is the last function, so the scope is exactly :873-1005 and :954 is inside it. The assertion cannot pass vacuously (`toContain` on a real scope; `fnSrc` throws loud if the fn disappears). The REM_UTILITIES loop over the same scope includes `"max-h-64"` and passes genuinely — zero `max-h-64` occurrences remain anywhere in Message.tsx.

**No new rem utility (VERIFIED).** The only code change since round 2 is the one-line prop addition (em-based). Full re-read of all five scopes confirms zero rem-based spacing/font utilities; every value is em-based, a color, or a layout keyword.

## Records — now accurate, no remaining overstatements

- **DECISION 344bc076**: the narrowed claim "All utilities in the card components (Message.tsx) are em-based per SPEC: tool-card spacing must be em-based (4c2f16ba)" is true for the spacing/font-size utilities across all five scopes (direct read). Every specific sentence checks out against code: the pre-block caps (`max-h-[24em]`, resolving against the pre's own 0.75em font ≈ the old 16rem — round-1 F1), the card-path diff cap (`CallDetail passes maxHeightClass="max-h-[24em]" to UnifiedDiffView; the shared max-h-64 default stays for non-card consumers` — round-2 L1), the inter-card gap (`space-y-[0.5em]`, Conversation.tsx, converted from space-y-2 — finding F4), and the shared-child carve-out (CodeBlock/InlineMarkdown; UnifiedDiffView's rem line padding — DiffView.tsx:173/:188 `px-2 py-0.5`, unchanged) alongside the tier-badge chip. The reworded pin description ("scoped via fnSrc over ToolCard/MemoryEntryCard/VisionEntryCard and caseSrc over the skill case: no frame classes, no rem utilities incl. text-sm/max-h-64, py-[0.125em] outer, affordances intact for memory + vision, the card-path diff cap pinned") matches the test source exactly — the round-2 "fn-scoped" wording note is fixed.
- **SPEC d69d168f**: same narrowed claim + carve-out, and every citation is exact — Conversation.tsx:94 (`fontSize: var(--app-font-size)`), globals.css:8 (`--app-font-size: 14px`), globals.css:234 (`.prose { font-size: inherit; }`), label treatments `text-[0.75em]` (status/running/section labels/code pre blocks) and `text-[0.65em]` (tier badges), and the card-path diff cap pin in the CallDetail test.

## Final sweep — nothing further found

1. **All five card scopes clean (direct read).** No `text-xs`/`text-sm`/`text-[0.875em]`/`text-[0.65rem]`, no `gap-2`/`px-3`/`py-1.5`/`h-3`/`w-3`/`mt-1.5`/`pt-1.5`/`px-1.5`/`py-0.5`/`space-y-0.5`/`space-y-1.5`/`max-h-64`, no `rounded-lg` or frame strings; `py-[0.125em]` outer on all four card families; body text inherits 1em.
2. **Affordances intact everywhere.** Memory: `role="button"` + tabIndex + Enter/Space with preventDefault, chevrons `h-[1em] w-[1em]`, 🧠 + name + query chip + tier badge + title + snippet + matched count + ✓/✗, expanded hit rows (tier/title/score). Vision: same expand interaction, query/response sections with `text-[0.75em]` labels and 1em content. Skill: `▶ skill "name" start` + prompt subtitle. ToolCard: header button semantics, chevrons, chips with `stopPropagation` + deep links + titles, running dots, ✓/✗ ×N, searchNotes, error summary, CallDetail list, images row `gap-[0.5em]`. CallDetail: labels, clickable readSections rows, diff, shell stdout/stderr sections.
3. **Conversation.tsx:97** `space-y-[0.5em]` — the F4 conversion, resolving against the container's `var(--app-font-size)`.
4. **Bookkeeping.** backlog.jsonl: 900fe8d8 pending → in_flight with the plan-id note (the 45dcf577 linkage model; flips to done at finish) — same state round 2 verified. The four untracked files (SPEC, plan 10650092, round-1 + round-2 reports) are present and consistent with being committed alongside this report in the fix commit.
5. **Splice/line-ending integrity.** Zero CR in all 187 frontend/src files (covers all three modified code files) and in every changed/untracked `.coding` markdown; git diff seams clean; both code files read as well-formed TS/TSX end to end.
6. **Multi-platform neutrality.** Pure className/TSX changes + the established node:fs source-contract test pattern; no platform assumptions anywhere in the diff.
7. **Doc sync.** README/PLAN.md carry no card-styling claims (settled in round 1, untouched since); the DECISION/SPEC records are the contract docs for this change and are now accurate. Nothing further to sync.
8. **Test evidence consistency.** 768/768 vitest with 93 toolCardPaths tests matches round 2's counts — the pin strengthened an existing `it` block, so no count change; tsc+vite green confirms the prop type-checks against the existing optional `maxHeightClass?: string`; cargo 1922+16/0 with no Rust changes is consistent with the diff (zero Rust files touched).

## Notes (no action required)

- **Fixed-px cosmetics inside the card components** — `rounded` (4px border-radius) on CallDetail's pre blocks and the tier-badge chips, and `underline-offset-2` (px-based) at ToolCard :794 / CallDetail :968. Round 1 explicitly settled both as label/code/cosmetic treatments ("no action required"), round 2 prescribed the current narrowed wording with the same code in view, and neither interacts with the two-font-size guarantee (corner radius and underline offset don't need to scale with font size; the pre blocks' rounded+bg chrome is the in-file mirror of the carved-out CodeBlock treatment). The records' "all utilities … are em-based" sentence, read in its SPEC-4c2f16ba (spacing) context with the carve-out and the round-1 note, is not misleading. Recorded here so the trail stays complete.
- REM_UTILITIES remains a curated denylist of the old code's values (`p-2`, `gap-4`, `text-base`, `h-4` etc. would slip through) — settled as acceptable in round 1 (F2) and re-affirmed in round 2; `text-[0.875em]` sitting in the list is em-based but intentionally pins the removed header downscale.
- Pre-existing out-of-scope rem usage, unchanged and documented since round 1: streaming assistant `text-sm` (:337), user/error/steer/qa message frames, Conversation's `px-4 py-4`/`max-w-4xl`, ToolImage dialog chrome, CodeBlock/InlineMarkdown (assistant prose), MemoryDebugView (debug panel).
- The auto-derived memory index still shows backlog 900fe8d8's digest as "status pending" — derived state that converges at finish; the backlog.jsonl source of truth is correctly in_flight. Not a defect in this change set.

**Verdict: PASS — all three rounds' findings are resolved, the records match the code, and the full change set is ready to commit.**
