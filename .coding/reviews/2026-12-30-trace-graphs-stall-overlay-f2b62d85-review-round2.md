## Verdict: FINDINGS (0 high, 1 low)

Round-2 verification for plan f2b62d85 "Trace graphs: stall counts as generate with a dithered stall overlay" (branch `wt/agenticcoding`, fix commit 672fa27 = HEAD, clean tree — `git diff HEAD` and `git status` both empty). **All three round-1 low findings are resolved and verified correct by direct read of the shipped code.** The sanity sweep of the rest of 672fa27 surfaced one new LOW — pre-existing stale phase lists in source doc comments (same class as round-1 LOW 2, in a doc block this commit extended) — and nothing else. No behavioral issues.


## Round-1 fix verification

### LOW 1 (PhaseChart `allHidden`) — RESOLVED; correct and a true mirror of the TokenChart precedent

- `TraceStats.tsx:281` hoists `const stacked = PHASE_SEGMENTS.filter((s) => s.key !== "stall")` above `:285` `const allHidden = stacked.every((s) => hidden.has(s.key))`, with a comment stating the mirror; `hasData`/`maxTotal` (`:287-288`) are unchanged, still computed over `phaseHeightVisibility`.
- TokenChart (`:390`, `:396-399`) has the identical shape: `stacked = TOKEN_SEGMENTS.filter((s) => s.key !== "cached")` → `heightVisibility` → `allHidden = stacked.every(...)` → `hasData`/`maxStack` over `tokenHeightVisibility`. The only difference is declaration order (`allHidden` before `heightVisibility` in PhaseChart, after it in TokenChart) — semantically identical.
- A full side-by-side of both chart bodies (`:277-364` vs `:387-470`) finds **no other allHidden/hasData divergence**: both legends list the full palette including the overlay key (`:296`/`:407`); both hints render `!hasData ? (allHidden ? "All series hidden — click a legend chip to show one." : chart-specific empty text)` (`:301-306`/`:412-417`); both columns render `stackSegments(stacked, …, visibility)` (`:312`/`:432`); both sliver guards use `Math.max(2, columnHeightPct(total, max, mode))` over the heightVisibility total (`:315-316`/`:421-422`); both overlays gate on `!hidden.has(key) && value > 0` with min(100, …) coverage (`:338-348`/`:444-454`).
- The behavioral edge is fixed: hiding all 8 stacked phases while leaving the stall chip on now shows "All series hidden — click a legend chip to show one." instead of the wrong "No phase timings recorded yet."

### LOW 2 (README) — RESOLVED; accurate against the shipped rendering

- `README.md:62` now reads "stacked phase times (prep/compact/backoff/connect/wait/reason/generate/tools, with a dithered red stall overlay inside the generate bar showing the stalled share)" — exactly the shipped 8 stacked keys (PHASE_SEGMENTS minus stall) plus the overlay. The chip sentence extends to "…the `cached` chip toggles only the dithered overlay inside the prompt bar, and the `stall` chip only the overlay inside the generate bar", matching the `!hidden.has("stall")` gating at `TraceStats.tsx:338`.
- `README.md:61`'s field gloss (stall = byte-silence gaps inside the generation window) is intentionally unchanged and still accurate.
- No other stale trace-graph prose: `README.md:60` describes the live inflight-bar indicator — a different surface this plan intentionally left unchanged (per the DECISION record); its waiting list not naming mid-stream stalls is pre-existing incompleteness from plan 6e734ccb, not a contradiction introduced here. The rest of the README trace prose (`:61-62`) is verified current. PLAN.md's only "stall" matches are unrelated (async runtime, line 93; hang watchdog, line 230) — it contains no trace-phase prose at all.

### LOW 3 (SPEC record) — RESOLVED; no contradiction with the code, no other live record stale

- The SPEC file on disk (clean tree → matches 672fa27) now opens the frontend-chart sentence with "AMENDED 2026-12-30 by plan f2b62d85 (see .coding/knowledge/decision/2026-12-30-trace-graphs-stall-counts-as-generate-with-a-dit.md)" and describes: 8 stacked segments prep/compact/backoff/connect/wait/reason/generate/tools + a NON-ADDITIVE stall overlay; generate = gen − reason (stall INCLUDED); dithered red hatch inside the generate segment; coverage = stallSharePct = min(100, stall/(gen−reason)×100) with the clamp rationale; height-neutral chip via phaseHeightVisibility (like cached/tokenHeightVisibility); totalMs = prep+compact+backoff+connect+wait+gen+tools (gen inclusive of stall); PhaseTimes chips p c b n w r g (s) t. Every clause cross-checks against the shipped code: `phaseMsByKey` (traceStats.ts:319-320), `stallSharePct` (:334-338), `phaseHeightVisibility` (:299-301), `totalMs` (:151), the overlay (TraceStats.tsx:338-348), the merged text treatment (LlmTraceView.tsx:95, :103-108, :113). The record title notes the amendment; the backend-attribution parts are untouched and still accurate.
- The memory record (14aa3665) was re-derived: the index title now reads "…(frontend chart amended by f2b62d85)", matching the amended file's frontmatter, so the derived digest no longer serves the old chart description.
- No OTHER live record describes the old semantics. Repo-wide sweeps for "9 disjoint segments", "gen − reason − stall", and "p c b n w r g s t" hit only historical artifacts: the 6e734ccb plan file (its step list), the f2b62d85 plan file's pre-change context section, and review reports (including round-1's own quote of the old text). In memory, the PLAN/REVIEW records are historical digests of past work; the live current-state records (the amended SPEC and DECISION c033ad5d) describe the new semantics; no HOW record covers the trace charts.


## Sanity check of the rest of 672fa27

- **Commit contents** (11 files, +291/−51): the four source files plus tests, the README fix, the SPEC amendment, the new DECISION file, the plan file, the round-1 review report, the finished round-3 report of plan edfff8d9 (expected rider, per the round-1 note), and backlog.jsonl (edfff8d9 item marked done — expected bookkeeping). Nothing unexpected rides along.
- **Core math re-verified by direct read** (traceStats.ts): `phaseSeries` totalMs sums prep+compact+backoff+connect+wait+gen+tools — gen inclusive of stall, no double count (`:151`); `phaseMsByKey` keeps stall for the overlay/tooltip (`:311-323`); `stallSharePct` returns 0 for no-stall/no-measurable-generate and clamps to 100 (`:334-338`); `phaseTipRows` keeps the explicit stall row with the exact ms (`:387-397`); `stackTotal`'s doc now covers non-additive keys generally (`:362-366`).
- **Regression tests confirmed on disk** (traceStats.test.ts): "stall counts as generate" (`:136`; fixture generate 6400 / stall 400 at `:346-347`), "the stall chip is height-neutral (phaseHeightVisibility excludes stall)" (`:146-156`), the clamp case for a reasoning-window stall (`:162-163`) plus no-stall → no hatch (`:169`), and the PHASE_SEGMENTS key list still asserting all 9 keys including stall (`:440`). (Search-tool caveat for follow-ups: an index-engine query for `stallSharePct` in the test file returns a false "no matches" — the content index is stale for recently changed files; tree-walk results are the truth.)
- **LlmTraceView**: `genShown = gen − reason` (`:95`); both PhaseTimes (`:103-108`) and UsageCard (`:304-305`) render the merged `g X (s Y)`; the title reads "generate (s = stalled share)" (`:113`); no disjoint `s` part remains anywhere in the file. LlmTraceView.test.ts correctly needed no changes — its only stall fixture is `stall_ms: null` (`:38`), which renders no parenthetical under both the old and new code, so the commit not touching it is consistent with the passing suite. (Observation, not a finding: the stall > 0 parenthetical is untested at the component level; the underlying math is fully covered in traceStats.test.ts — a one-line fixture would lock the format if desired.)
- **Σ stall summary line** (TraceStats.tsx:551-552): a labeled session aggregate with an accurate title ("total mid-stream stall time (byte-silence gaps inside the generation window)").
- **Multi-platform neutrality**: pure CSS (flex, rgba, repeating-linear-gradient) — no platform APIs anywhere in the change. **Security**: presentational only; nothing executed.
- **Tests/tsc**: not re-run by this reviewer (read-only, no shell). Per the task: `npx tsc --noEmit` clean; vitest 789 passed / 0 failed (55 files). Round-1 additionally verified the new tests fail without the fix, and the test bodies on disk match that claim.

## Findings

### LOW 1 (new; root cause pre-existing): stale phase lists in source doc comments — "send" (pre-rename name) and missing "backoff"

- `TraceStats.tsx:38` (module doc): "per-request phase time (prep/compact/send/wait/reason/generate/tools stacked columns)" — `send` was renamed to `connect` in plan 6e734ccb, and `backoff` is missing from the list.
- `TraceStats.tsx:255-256` (PhaseChart doc — the very block this commit extended with the stall-exception sentences at `:258-262`): "Stacked per-request phase-time columns (prep / compact / send / wait / reason / generate / tools)." — same two problems.
- `traceStats.ts:119` (phaseSeries doc): "Per-request phase timings (prep / compact / connect / wait / generate / tools)" — current names, but `backoff` is missing.
- Root cause: the 6e734ccb send→connect rename (and the backoff addition) did not update these doc lists; f2b62d85 carried them over unchanged. Doc-only, zero behavioral impact — but it is the same class as round-1 LOW 2 (a phase list not matching the shipped segments), it sits in a doc block this commit edited, and the project's review expectations treat module doc comments as a documentation-sync surface.
- **Fix**: update all three lists to the shipped stacked set — prep/compact/backoff/connect/wait/reason/generate/tools, with stall as the non-additive overlay — exactly as README.md:62 now correctly says. Optional polish while there: the module-doc chip sentence at `TraceStats.tsx:49-50` mentions only the `cached` chip and could gain the `stall` counterpart.