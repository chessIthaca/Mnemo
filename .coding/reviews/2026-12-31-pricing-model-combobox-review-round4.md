## Verdict: PASS

Round-4 verification of plan ec52b07f ("Pricing model field: editable combobox of configured models", backlog 82dd66fc) at commit 9b89d71 — HEAD of `wt/agenticcoding`, parent a96ab1c (both confirmed via git log), tree clean (`git diff HEAD` and `git status --short` both empty), round-3 report included in the commit as a new file. The round-3 F3 fix is in place exactly as prescribed: the toggle's onClick pulls focus into the field with the activeElement-guarded refocus, so the "open ⇒ focus inside the root" invariant now holds on both platforms and the blur-close is always armed. The full interaction matrix traces clean (including all four requested scenarios), the skipOpenRef flag has no stale path at either of its now-two write sites, and every round-1/2/3 contract string still matches the source at 9b89d71 — the onMouseDown count stays exactly 2, and the new round-3 pin's comment-tolerant bridge cannot jump to the entry button's guard. No new findings.

### F3 resolution (verified against the diff vs parent a96ab1c and the full source at 9b89d71)

**RESOLVED, exactly the round-3 prescribed shape.** The toggle's onClick (ModelCombobox.tsx:114-127) now reads `setOpen((o) => !o);` (:115) followed by the guarded refocus `if (document.activeElement !== inputRef.current) { skipOpenRef.current = true; inputRef.current?.focus(); }` (:123-126), with an accurate comment block (:116-122) explaining the mechanism. The doc comment's a11y/focus paragraph (:17-31) now covers rounds 1-3, including the toggle refocus and why it exists (:29-31). The diff touches exactly three files — ModelCombobox.tsx, ModelCombobox.test.tsx (the new pin), and the round-3 report (new file) — nothing else.

### Interaction matrix (traced on Windows/WebView2 and macOS/WKWebView; the fix is platform-uniform by construction — it no longer relies on engine mousedown-focus behavior at all)

1. **Chevron-open with focus outside the root (the F3 scenario)** — e.g. focus on the same row's cost input. Mousedown on the chevron is prevented (:113) → focus stays outside; no focusout originates inside this root → no blur-close. Click (:114-127): `setOpen(o => !o)` → true; the guard (:123) is true (activeElement = the cost input ≠ inputRef.current) → flag set (:124), `focus()` (:125). The focus event fires synchronously → onFocus (:99-105) consumes the flag (:100-102) and returns before `setOpen(true)` (:104) → the toggle's open stands. Focus is now on the field, inside the root → the blur-close is armed. ✓ / ✓
   - **Cross-row variant (the exact F3 symptom):** focus on row B's field with B's dropdown open → click row A's chevron → A opens; A's `focus()` moves focus from B's field to A's field, so B's root onBlur fires with an uncontained relatedTarget → B closes. Exactly one dropdown open. ✓ / ✓
2. **Chevron-close with focus outside the root:** click → `setOpen(o => !o)` → false; guard true → flag set, `focus()` → onFocus consumes the flag and returns before `setOpen(true)` → the toggle's close stands, no reopen; focus lands on the field. ✓ / ✓ (Post-fix this state is only transiently reachable — any focus departure from an open root fires the blur-close first — but the path is correct if reached, e.g. via a programmatic focus move that fires no focusout.)
3. **Chevron toggle with focus on the field (the common case):** mousedown prevented (no focus change anyway); click toggles open; the guard (:123) is false (activeElement === inputRef.current) → no flag, no `focus()` call → behavior identical to rounds 2-3. ✓ / ✓
4. **skipOpenRef has no stale path — at either of its now-two write sites.** The flag's write sites are :124 (toggle) and :165 (entry), each inside its own `document.activeElement !== inputRef.current` guard, each immediately before `inputRef.current?.focus()`, each consumed synchronously in onFocus (:100-102). Staleness at either site would require `focus()` to fire no focus event while the guard held — excluded: (a) the guard's operand cannot change between check and call (synchronous code, nothing intervenes); (b) when the guard is true, focus genuinely moves to the input, and a genuine focus move fires the focus event synchronously; (c) `inputRef.current` is non-null whenever either onClick can run (the input is unconditionally rendered, :88-108; both buttons live in the same always-mounted subtree). The mouse-select path never sets the flag (focus never left the field); the keyboard path and both toggle paths set and consume it within the same tick. No stale path. ✓
5. **Round-3 primary flows untouched:** the fix adds code only to the toggle's onClick. Mouse entry select (entry guard :164 false — focus never moved), keyboard Enter/Space select (guard true, flag consumed, entry unmounts with focus safe), typing/filter, Tab out, and overlay click all trace exactly as verified in round 3. Rapid chevron double-click from outside: first click opens + pulls focus in (flag consumed); second click hits a false guard and closes — no stuck state. ✓ / ✓

**Structural note (stronger than the round-3 minimum):** post-fix, the state "dropdown open with focus outside the root" is unreachable through any traced flow — both open paths (field onFocus, toggle onClick) land focus inside the root, and any subsequent departure fires the blur-close. The invariant F3 violated now holds structurally on both platforms.

### Contract strings (all pins checked line-by-line against ModelCombobox.tsx at 9b89d71)

- **Round 1:** `role="listbox"` ↔ :142; `role="option"` ↔ :147; `aria-autocomplete="list"` ↔ :94; `skipOpenRef.current = true;` ↔ :124/:165; `inputRef.current?.focus();` ↔ :125/:166; the consume regex (`if (skipOpenRef.current) {` / `skipOpenRef.current = false;`) ↔ :100-101; `rootRef.current?.contains(to)` ↔ :73; `onBlur={onBlur}` ↔ :79. All match.
- **Round 2:** the onMouseDown count-2 regex ↔ exactly two occurrences (:113 toggle, :149 entry) — the F3 fix adds no onMouseDown, and neither the doc comment nor the new onClick comment block contains the literal; `document.activeElement !== inputRef.current` ↔ :123/:164; `visible.length > 0 ? listId : undefined` ↔ :93. All match.
- **Round 3 (new pin, test :113-120):** the regex matches the toggle's onClick at :114-126. Bridge semantics verified char-by-char: `(?:\s|\/\/[^\n]*)*` crosses the comment block (:116-122) — leading indentation matches `\s`, each `// …` line matches the comment alternative, newlines match `\s` — but cannot cross `}}` or any code token. Since `setOpen((o) => !o);` occurs exactly once in the file (the entry uses `setOpen(false)`, as do the overlay and Escape), a removed toggle guard leaves the regex no start position that can bridge to the entry's guard at :164 — the pin genuinely fails on regression. Sound.
- **Interactive/markup pins:** `onChange={(e) => onChange(e.target.value)}` ↔ :98; the entry-select regex (`onClick={() => {` / `onChange(o.model);` / `setOpen(false);`) ↔ :150-152; `filterModelOptions(options, value)` ↔ :56; `e.key === "Escape"` ↔ :59; `onClick={() => setOpen(false)}` ↔ :83 (overlay); markup pins (role="combobox" :91, value passthrough, aria-expanded :92, "Toggle model list" :111, no entries while closed, aria-autocomplete :94) all hold — the closed-state static markup is unchanged by the fix (the toggle's onClick body does not affect renderToStaticMarkup output).

### Test evidence (stated by the implementer, statically verified, not re-run — read-only reviewer, no shell)

- **ModelCombobox.test.tsx: 14 tests counted** — 3 markup (:26/:35/:46) + 4 interactive (:55/:59/:64/:68) + 3 round-1 (:75/:81/:89) + 4 in the round-2 describe (:96/:103/:107/:113), the last being the new round-3 F3 pin. Matches the claim.
- **modelOptions.test.ts (8) and PricingSection.test.ts (4)** are untouched by 9b89d71 (the diff spans exactly three files), so their counts carry over from round 3's verification at a96ab1c.
- **Count math:** 789 baseline + 26 (8 + 14 + 4) = 815 ✓; round 3 verified 814 with 13 ModelCombobox tests → +1 = 815 ✓; no new test files → the 58-file count carries over ✓. `npx tsc --noEmit` exit 0 is consistent by inspection — the diff adds no imports or types (standard DOM properties and the existing refs only).

### Commit contents & tree

9b89d71 is HEAD of `wt/agenticcoding` with parent a96ab1c (git log confirms both, matching the task's claim). The tree is clean (`git diff HEAD` and `git status --short` both empty). The round-3 report is included in the commit verbatim (68 lines, new file). The commit message accurately describes the F3 mechanism, the guarded refocus, the pin test, and the test results. **Documentation sync:** the component doc comment (:17-31) is updated to rounds 1-3 and accurately covers the toggle refocus; no README/PLAN changes are needed (a focus-management fix inside an existing component — no feature or config surface changed). **Multi-platform neutrality:** the fix uses only platform-neutral web APIs (`document.activeElement`, `.focus()`) and removes the last engine-dependent focus assumption rather than adding one — behavior is now identical on WebView2 and WKWebView by construction.

### Notes (non-findings, no action required)

- The round-3 pin test lives inside the describe titled "review round 2 — cross-platform + listbox structure" rather than a new round-3 describe. Cosmetic: the test title carries "(round 3 F3)" and the describe's topic (cross-platform focus behavior) fits the test.
- The round-3 pre-existing notes are unchanged by 9b89d71 and remain out of scope: Escape on a keyboard-focused entry button bubbles to the dialog (the handler lives only on the input, :58-64); mousedown on the dropdown panel's own scrollbar may fire the blur-close mid-scroll on long lists; `aria-expanded={open}` is true in the empty-filter state while `aria-controls` is undefined (permitted — aria-controls is optional in the ARIA 1.2 combobox pattern).
- Backlog 82dd66fc remains in_flight with plan_id ec52b07f — the done-flip belongs to the post-PASS closing sequence, consistent with the repo pattern.
