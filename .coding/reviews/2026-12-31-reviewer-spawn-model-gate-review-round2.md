## Verdict: FINDINGS (0 high, 1 low)

Round-2 verification of commit a632de6 (wt/agenticcoding, HEAD, clean tree — confirmed via empty `git diff HEAD` / `git status`) against the six round-1 findings. **All six are resolved in the committed code**: H1's three doc sites are reworded and accurate, L1's two comment clusters are rewritten, L2's abandon_plan close is in and correctly placed, L3's one-shot-at-dispatch semantic is documented as a conscious decision, L4's new end-to-end test exercises the real dispatch wiring (JSON arg extraction + sanction store-back) in three phases, and L5's role trim + padded-role assertion are in. The fixes introduced **no new functional issues** — interception ordering, store-back sequencing, the trimmed-role comparison, and doc accuracy all check out; the change stays multi-platform neutral; the reported test counts are consistent with the diff. One **low doc-consistency residual**: the `reviewer_retry_sanctioned` field doc in loop_impl.rs still carries the exact "can never authorize a later ad-hoc model pick" absolute that round-1 L2 established as not the true invariant, contradicting the gate doc's corrected "at most ONE" phrasing in the same commit.

### Finding

**LOW (L2 residual) — loop_impl.rs:172-174: the field doc retains the round-1 "never" invariant the fix corrected everywhere else.**
- The field doc for `reviewer_retry_sanctioned` (loop_impl.rs:168-174) ends: *"Consumed by ANY reviewer spawn (the retry window closes with it), so a lingering sanction (e.g. the user chose "abandon") can never authorize a later ad-hoc model pick."* — the same sentence round-1 L2 flagged as false, surviving in a third site the fix didn't touch (the fix updated the gate doc and the test comment, both in dispatch.rs).
- The gate doc (`reviewer_spawn_gate`, dispatch.rs) now states the true invariant: *"A lingering window therefore authorizes at most ONE model-carrying reviewer spawn — the first after the consultation — and never a second (the agent ignoring the user's answer and later ad-hoc picking is the narrow residual: one-shot, requiring deliberate misbehavior)."*
- With the new abandon_plan close, the field doc's parenthetical example (user chose "abandon") is now structurally true — but the general claim still overstates: a sanction can linger with no reviewer spawn, no abandon_plan, and no unrelated ask_user (e.g. the plan finishes via an already-running reviewer's report, and the next plan's first reviewer spawn carries an ad-hoc model) and authorizes exactly one later model-carrying pick — the documented one-shot residual. Two docs in one commit now state the same lifecycle at different strictness.
- Fix (one line, doc-only): align the field doc with the gate doc — e.g. *"…Consumed by ANY reviewer spawn, so a lingering window authorizes at most ONE model-carrying reviewer spawn (the first after the consultation); closed by abandon_plan or an unrelated ask_user — full protocol in `reviewer_spawn_gate`'s doc (dispatch.rs)."*

### Round-1 findings — verification detail

**H1 (doc sync) — RESOLVED.**
- README.md:56 (committed): now reads "…a spawned reviewer runs on the `[models.reviewing]` model — falling back `[models.executing]` → `[models.subagent]` → default; an explicit `model` on a reviewer spawn is dispatch-denied unless it is the user-sanctioned failed-reviewer retry (for non-reviewer spawns the arg still wins)…" — the stale "unless `spawn_agent` passes an explicit `model`" clause is gone and the replacement matches the implemented semantics.
- PLAN.md:470-473: "an explicit `spawn_agent` `model` arg on a reviewer spawn is dispatch-denied (`reviewer_spawn_gate`) unless it is the user-sanctioned failed-reviewer retry — for non-reviewer spawns the arg still wins" — accurate ("still wins" ↔ `forced_model.or(reviewer_pin)`, spawn_agent.rs:333).
- PLAN.md:893-903 (Failed-reviewer protocol bullet): extended with the gate + full sanction lifecycle — ask_user opens a one-spawn `reviewer_retry_sanctioned` when a failure was pending; consumed by any reviewer spawn; closed by `abandon_plan` or an unrelated `ask_user`; the configured reviewing model is authoritative. Matches the code exactly.
- Repo-wide sweep: "always wins" and "unless `model`" now hit only historical artifacts (`.coding/knowledge`, `.coding/plans`, `.coding/reviews` — records quoting the pre-fix text, correctly left as history) and unrelated code (frontend store / config / memory comments on other subjects). No living doc or source re-teaches the bypass.

**L1 (stale inline comments) — RESOLVED.**
- spawn_agent.rs:258-271 (committed state): "the reviewer's own loop is stamped `WorkflowState::Subagent` and never transitions… would silently run on the default model. Skipped when an explicit `model` arg is set (for reviewer spawns that path is dispatch-denied unless it is the user-sanctioned failed-reviewer retry — see the struct doc)…" — "born in Planning", "PLANNING model", and "already won" are all gone.
- spawn_agent.rs:326-332: "An explicit `model` arg takes precedence (for reviewer spawns it is dispatch-denied unless it is the user-sanctioned failed-reviewer retry); otherwise a spawned reviewer gets the reviewing-model pin…" — the "wins" phrasing is eliminated and the dispatch-denial is noted.
- The struct doc comment is rewritten to match (stamped-Subagent + default-model claims, plus a dedicated OMIT/gate paragraph describing the sanctioned-retry exception).

**L2 (invariant gap) — RESOLVED structurally; one doc residual (finding above).**
- Structural fix: dispatch.rs:248-254 — `if tc.name == "abandon_plan" { self.set_reviewer_retry_sanctioned(false); }` — non-returning (abandon_plan still executes), placed after the ask_user early-return and before the spawn gate. The review-exit transition gate above (dispatch.rs:180-221) matches only `finish` / final `complete_step` (abandon_plan was deliberately un-gated by backlog 569b5922), so every abandon_plan call that reaches dispatch hits the close; a ToolFilter-denied abandon_plan abandons nothing, so leaving the sanction untouched there is correct.
- The gate doc and the `reviewer_spawn_gate_no_model_spawn_consumes_sanction_and_passes` test comment now state the TRUE invariant (closes on any reviewer spawn / abandon_plan / unrelated ask_user; a lingering window authorizes at most ONE model-carrying spawn — the ignore-the-answer residual documented as one-shot). The loop_impl.rs field doc is the one site not aligned (finding above).

**L3 (one-shot-at-dispatch semantic) — RESOLVED.**
- The gate doc now carries it explicitly: "the sanction burns BEFORE the approval gate and before the spawn executes — a denied approval or a failed spawn (e.g. unknown model id) closes the window, and with the failure latch clear a fresh ask_user cannot re-open it. Recovery is always clean: a no-model reviewer spawn always passes (the denial text says exactly that)." — exactly the "note it in the gate's doc" option round-1 offered, taken as a conscious decision.

**L4 (missing end-to-end test) — RESOLVED.**
- New test `dispatch_denies_ad_hoc_reviewer_model_pick` (src/agent/tests.rs:3693-3780) drives `execute_tool_call` with real JSON args through the shared `make_dispatch_fixture` harness (tests.rs:3452-3473), in three phases: (1) ad-hoc reviewer+model denied with "must OMIT the model parameter" — exercises the role/model JSON-key extraction and the denial return; (2) with the sanction set, the same call passes the gate and `reviewer_retry_sanctioned()` reads false afterwards — exercises the store-back through dispatch; (3) the same model-carrying call is denied again — proves the store-back persisted. Red-without-fix: without the gate, phase 1 proceeds with no denial → the assertion fails.
- `dispatch_clears_failure_pending_on_ask_user` extended (tests.rs:3684-3690): asserts the sanction OPENS on ask_user-while-pending — red without the interception's `set_reviewer_retry_sanctioned(self.reviewer_failure_pending())`.
- Phase 2's design is robust: it asserts only the absence of the gate denial ("whatever the tool itself does next in this fixture"), so the test pins the gate wiring, not the fixture's spawner behavior.

**L5 (untrimmed role) — RESOLVED.**
- The gate now compares `role.map(str::trim) != Some("reviewer")` with a comment citing the tool's trim parity — matching spawn_agent's own `args.role.as_deref().map(str::trim)` semantics; the pin gate at spawn_agent.rs:272 compares the already-trimmed value, so a padded `" reviewer "` is a reviewer at both layers now.
- Padded-role assertion added to `reviewer_spawn_gate_denies_ad_hoc_model_picks`: `gate(false, false, Some(" reviewer "), Some("deepseek-v4-flash"))` → denial present.

### New-issue checks on the fixes

- **Interception ordering (dispatch.rs:240-273)**: the ask_user interception reads `reviewer_failure_pending()` BEFORE clearing it and sets the sanction from that read — opens only on a pending failure, closes on an unrelated ask_user. The abandon_plan close and the spawn gate are disjoint by tool name; the store-back (line 269) runs before the denial return (270-272), so every gate outcome persists. Gate-1 denial preserves the sanction; gate-2 denial writes false (a no-op when it was already false); the sanctioned pass-through consumes. All combinations are unit-tested in the four gate tests.
- **abandon_plan close is unconditional** (fires even if the abandon call later fails, e.g. no active plan) — the same one-shot-at-dispatch shape as the accepted L3 semantic, with the same clean recovery via a no-model spawn; the gate doc's "closed by `abandon_plan`" accurately describes the call-level close. Not a defect.
- **Trimmed-role comparison** handles non-string role JSON safely (`as_str()` → None → non-reviewer pass-through; the tool's own deserialize rejects it before any spawn — round-1 verified, unchanged).
- **Doc accuracy**: all three H1 sites match the implemented semantics, including the non-reviewer "arg still wins" claim. The struct doc's unqualified "a spawned reviewer is pinned at spawn time" is made accurate by the OMIT paragraph immediately following, which documents the sanctioned-retry exception.
- **Multi-platform neutrality**: PASS — the new code is pure Rust string/Option/mutex ops; no paths, no shell, no `cfg(windows)`.
- **Bug-plan artifacts**: intact — the BUG knowledge file and the HOW record are updated and accurate (the HOW record now says "STRUCTURALLY ENFORCED" with the gate + lifecycle and the four regression-test names); the plan file records all steps complete + the regression test name; the commit message documents the round-1 findings and fixes.
- **Tooling note**: one sweep search ("never authorize") hit the known stale-content-index false-negative (backlog 92a41825 — the re-issue escape did not bypass it, twice). Compensated by direct reads of every file in the change set: the only living-code site of the round-1 phrasing is the loop_impl.rs field doc (the finding above).

### Tests

Attested by the main agent: root `cargo test` 1960 passed / 0 failed; src-tauri 186+4 passed / 0 failed; dispatch-filtered 35/35; gate tests 4/4. As the read-only reviewer I cannot execute cargo test; the counts are consistent with the diff (round-1 reported 1959 root tests; the round-2 fixes add exactly one new test, `dispatch_denies_ad_hoc_reviewer_model_pick`, and extend an existing one → 1960; the diff touches no src-tauri sources → 186+4 unchanged), and the code-level verification above is consistent with a green run — under `#![deny(warnings)]` a green `cargo test` also proves zero warnings.
