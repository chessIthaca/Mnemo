## Verdict: FINDINGS (0 high, 1 low)

Review of all uncommitted changes on wt/agenticcoder (plan 73ddb869, tool-call discipline: system-prompt block + runtime enforcement). The change is sound: the prompt const renders correctly, the runtime guards are correctly placed and genuinely regression-tested, and no existing behavior or test is contradicted. One low finding on intra-head duplication + decision-record accuracy.

### Scope reviewed
PLAN.md, src/agent/prompt.rs (TOOL_CALL_DISCIPLINE const + build_stable_head + marker test), src/agent/turn.rs (malformed-JSON teach-back), src/agent/tests.rs (extended assertions), src/tool/workflow/plan.rs (blank guards + schema descriptions + 4 tests), src/workflow/mod.rs (no-op message), plus untracked .coding/knowledge/decision/2026-12-20-tool-call-discipline-*.md and .coding/plans/73ddb869.md.
### Finding L1 (low) — CHUNK LARGE PAYLOADS duplicates the preamble chunking sentence; decision record's "not duplicated" claim is inaccurate for chunking

- `TOOL_CALL_DISCIPLINE` bullet (prompt.rs:167-168): "CHUNK LARGE PAYLOADS — for a big file_write, write the first section, then append the rest (mode:\"append\") — never one giant body."
- Preamble CORE PRINCIPLES (prompt.rs:42-44, same cached stable head): "…for large files, write the first section, then file_write mode:\"append\" the rest."

The operative instruction is restated verbatim-equivalently in two blocks of the same byte-stable head. The new const's own doc comment says "Kept lean: the head is cached but still costs tokens once per session", and the project has an enforced de-dup precedent (`STATE_REVIEWING` is deliberately a pointer to APP_RULES, guarded by `reviewing_state_block_points_at_app_rules_without_duplicating_them`). Compounding it, the untracked decision record (`.coding/knowledge/decision/2026-12-20-…md`) asserts the existing chunking/never-resend rules were "not duplicated" — accurate for never-resend (the TWO-FAILURES bullet only cross-references APP_RULES, which is fine) but inaccurate for chunking, which IS restated. Side evidence the bullet adds little: the plan step 1(4) also specified the "update_plan append=true" half of the chunking rule, which the shipped bullet dropped.

Remedy (either is sufficient): (a) trim the CHUNK bullet to a short pointer ("chunking: see CORE PRINCIPLES" or delete the bullet) and re-run `cargo test` (no marker asserts on the chunk bullet, so test fallout is nil), or (b) keep the bullet but amend the decision record to say chunking is deliberately restated in the discipline block. Prefer (a) for the token economy + record accuracy together.

### Verified clean (per focus checks)

1. **Prompt const** — escape rendering verified by source read: `\"` → quotes (`kind="bug_fixing"`, `mode:"append"`, the quoted error string), `(\\x, \\d)` → literal `(\x, \d)` illegal-escape examples, backslash-newline continuations join with exactly the intended single spaces (next lines start at col 0), `→`/`≥`/`—` unicode intact, `C:/repo/path` renders as guidance text. `build_stable_head` pushes exactly six compiled blocks (PREAMBLE, WORKFLOW_LIFECYCLE, APP_RULES, TOOL_CALL_DISCIPLINE, TOOL_STRATEGY, MEMORY_RECORDS) before the constitutions — the "six compiled prompt blocks" doc comment is accurate and the APP_RULES → TOOL_CALL_DISCIPLINE → TOOL_STRATEGY order matches the plan. All 10 marker assertions in `stable_head_carries_tool_call_discipline` match the rendered string, including the continuation-spanning `"create_plan → title, goal, steps"`.
2. **No contradiction** — the TWO-FAILURES bullet explicitly subordinates to the APP_RULES never-resend rule (cross-reference, not restatement); CONTENT-FIRST ("write the content in your reply first") does not conflict with APP_RULES "re-issue corrected immediately, without pausing to narrate" (drafting ≠ narrating). MEMORY_RECORDS untouched by the new block. Only the duplication above.
3. **create_plan guard** — placed after serde parse, before the bug_fixing dispatch; the bug_fixing branch auto-fills only *steps* (skeleton) while title/goal pass straight to `create_plan_with_kind`, so no auto-fill path is contradicted and a bug_fixing call with blank title/goal is still rejected. Trim semantics correct (`trim().is_empty()`, original untrimmed values persisted — covered by the padded-success test). Schema `required` unchanged; only descriptions extended; no test pins the old description text. Guard is tool-level only, so non-tool `create_plan_with_kind` callers are unaffected — right blast radius. No other caller passes blank titles (repo-wide search: only the new regression test).
4. **update_plan** — the "changed nothing" substring survives (workflow/mod.rs:634) and the new "non-blank" wording now matches the *pre-existing* trim semantics (lines 544-578: every optional field is applied only when `!t.trim().is_empty()`), so blank-title-only is a genuine no-op and `update_plan_blank_title_errors_as_noop` passes against real behavior. Empty-steps ("steps must be non-empty", lines 581-585) and regression_test paths untouched.
5. **turn.rs** — single hunk; only the Tool-role retry-path message changed. UI-facing arms ("arguments malformed or truncated — not run" at :1356 and "…— not run; retrying" at :1453) untouched. The kept substrings survive: the "malformed or truncated" opening, "please retry with valid, complete JSON.", and the split-chunks closing sentence; the teach-back is inserted between, model-facing only.
6. **Tests** — the two create_plan regressions are genuine fail-without-fix cases (serde accepts whitespace-only strings, so without the guard `execute` succeeds and `!result.success` fails); `update_plan_blank_title_errors_as_noop` pins real pre-existing behavior. The extended tests.rs assertions match the exact new message text ("rewrite the COMPLETE call", "never resend the broken call unchanged"); no test asserts removed text (old message content fully retained; `"provide at least one of"` appears only in the error itself).
7. **Platform/docs/warnings** — no Windows-only API added ("C:/repo/path" is prompt guidance, sanctioned); PLAN.md's compiled-constants list updated; no `#[allow(...)]` in the diff; new const/tests are all referenced, doc comments present — no known warning source. I cannot execute `cargo test` from this read-only review; the closing sequence must run it unpiped and read `test result:` lines (deny(warnings) makes green == zero warnings).

### Non-blocking observations (no action required)
- The turn.rs teach-back names only "current_plan/backlog_list" as no-arg examples while the const lists three (memory_search browse omitted); "like" makes it non-exhaustive and the message directs the model to the schema, so not misleading — optional alignment.
- Pre-existing mojibake in tests.rs:903 comment ("Î“Ã‡Ã¶") is untouched context, not introduced here — housekeeping candidate for another pass.
- Plan step 3's literal "mirror create_plan rejection for blank update_plan fields" was implemented via the pre-existing trim/no-op semantics instead — resolved sensibly and test-pinned.
