## Verdict: FINDINGS (0 high, 1 low)

Round-2 verification of commit 60f508d (HEAD of `wt/agenticcoding`, working tree clean) against round 1 (`.coding/reviews/2027-01-04-content-index-inline-reindex-review.md`, FINDINGS 0 high / 2 low). **Both round-1 findings are substantively resolved and verified; nothing regressed.** The single low below is a doc-comment precision issue ON the LOW-1 fix itself: the guarded-against harm (out-of-root content entering the FTS index) is genuinely closed and test-pinned — only the doc's description of the guard's placement overstates it by one operation.

### Round-1 LOW 1 (containment check) — RESOLVED ✓ (with one doc-precision low, below)

Verified in `src/codegraph/mod.rs:395-485`:
- Root canonicalized once (`root_canon`, :408-411; the `unwrap_or_else` fallback to the un-canonicalized root fails in the SAFE direction — over-skip, never under-skip).
- Every successfully-read path is canonicalized (:437) and checked with component-wise `starts_with(&root_canon)` (:441) before any content is hashed or upserted; out-of-root → `eprintln` + skip (:442-443), the file stays stale → on the retry `fts_page` still sees it stale → `FtsOutcome::Stale` → walk (the authoritative engine). Unreadable (canonicalize fails) → skip (:438).
- The vanished branch is documented as safe and is safe: `remove_file` mutates DB rows only (:418-421).
- The method doc comment states the containment check (:390-393).
- Component-wise `starts_with` avoids the string-prefix trap (`root` vs `root-evil`); canonicalize on BOTH sides keeps Windows `\\?\` prefixes and macOS `/var`→`/private/var` resolutions consistent, so normal in-root files pass the guard (confirmed by the unchanged refresh/prune tests). Canonicalize-based checking also catches in-root symlinks pointing outside root — strictly stronger than the round-1-suggested lexical `relpath` re-derivation.

**Regression test `reindex_stale_files_skips_out_of_root_keys`** (mod.rs:828-881) — verified it genuinely reproduces the defect: plants `"../cg-outside-secret/secret.txt"` directly in the store with DECOY content `"planted_decoy"`, creates a real file OUTSIDE the project root containing `"outside_secret_marker"`, and asserts n == 0, `search_content("outside_secret_marker")` empty, `search_content("planted_decoy")` non-empty. **Fails without the guard** (statically traced): `.txt` is non-source (`Lang::from_extension` covers only rs/ts/tsx — walk.rs:40-47), so the round-1 code would take the content-only upsert arm and index `"outside_secret_marker\n"` → n = 1 (≠ 0), the outside content searchable (`is_empty` fails), and the decoy row replaced (non-empty fails) — all three assertions trip. With the guard: read succeeds → canonicalize resolves outside root → skip → all three pass. The decoy design is exactly right: only an actual out-of-root READ could surface the real content, so the empty-`outside_secret_marker` assertion isolates the guard.

### LOW (new, round 2) — the guard's doc comment overstates its placement: the check runs AFTER the read

The doc comment says "Every path is containment-checked against the project root (canonicalized) **before it is read**" (mod.rs:390-391), but the guard runs after `std::fs::read(&abs)` (:417 read, :437-444 check). An out-of-root key therefore still causes the outside file's bytes to be transiently read into process memory — then discarded, never indexed, never surfaced in any output or log (only the attacker-supplied `rel` is printed). The round-1 LOW-1 harm (information disclosure into search results) IS closed and test-pinned; the doc's ordering claim is not. Note the check CANNOT simply move before the read: `canonicalize()` fails for non-existent paths, and the read-failure branch IS the vanished-file signal the prune path depends on (moving it would break `reindex_stale_files_prunes_vanished_files`). Related imprecision: a NON-existent out-of-root key takes the vanished branch and is PRUNED (DB rows only — safe) rather than skipped, so "out-of-root paths are skipped" holds only for existing ones. **Fix (one line):** reword the doc to what actually happens — "before its content is indexed" / "before anything is upserted" — or, if "never read" is the desired contract, add a lexical `..`-component pre-check on `rel` ahead of the read (which would also make non-existent out-of-root keys skip instead of prune). Non-blocking.

### Round-1 LOW 2 (DECISION record) — RESOLVED ✓

- Semantic memory exists: `DECISION: content-index staleness repairs inline (≤ 8 files) instead of warn-only` (id 234a47f9-ca66-5dcd-850e-070182766fe4, semantic tier — confirmed via memory_search).
- Knowledge file `.coding/knowledge/decision/2027-01-04-content-index-staleness-repairs-inline-8-files-i.md` is committed in 60f508d (new file in the commit; working tree clean, so disk == commit).
- Accuracy against the shipped code — every claim checked: ≤ STALE_REINDEX_CAP=8 + no running pass gates the inline reindex (search.rs `try_index`: `stale_paths.len() <= STALE_REINDEX_CAP`, busy → `Ok(0)` → `is_ok_and(|n| n > 0)` false → Stale) ✓; vanished→prune / source→`reindex_one` / text→content-only upsert / became-binary→prune (mod.rs:415-474) ✓; containment guard ✓; atomic flag claim via `compare_exchange` + `IndexFlagGuard` (mod.rs:396-405) ✓; `try_index` re-queries ONCE (2-attempt loop) ✓; "reindexed N stale file(s) — serving fresh index results" disclosure (both tools' Hits arms) ✓; Stale on above-cap / pass running / reindex failure / re-edited-during-retry ✓; single "note: " prefix via prefix-less components + `with_note`/`push_note` ✓; supersedes the warn-only F10 SPEC (historical file left unedited — not in the commit) ✓; all 9 named regression tests exist ✓. The knowledge file's containment wording ("a crafted out-of-root DB key is skipped") is accurate — it does not repeat the doc comment's "before it is read" overstatement.

### Spot-checks (no regressions found)

- **Normal paths intact** — `reindex_stale_files_refreshes_the_given_files` (:736-775: stored mtime catches up, FTS serves new content, untouched file unchanged), `reindex_stale_files_prunes_vanished_files` (:778-796: rows gone, "boot" no longer served), `reindex_stale_files_busy_returns_zero_and_leaves_the_flag` (:799-825: Ok(0), flag untouched, then success clears it and serves) — all byte-identical to what round 1 reviewed, still asserting the original behavior; the guard passes for in-root files (both sides canonicalized consistently).
- **Rest of the commit unchanged vs round 1** — the `try_index` 2-attempt loop, `fts_page`, `push_note`, the single-prefix strings, both call sites, and the round-1 test set match round-1's reviewed state; 60f508d = round-1 changes + the guard + doc updates + the new test + bookkeeping (knowledge file, plan file, round-1 review report, backlog flip). No sneaky edits.
- **Warning-free build (static)** — no new imports needed (`FileData` already imported mod.rs:48; `DefaultHasher`/`Hasher` :38-39); every new binding is used (`root_canon`, `canon`, `bytes`, `data`); no `#[allow]`; nothing dead. I am read-only (no shell) and could not execute `cargo test` myself — same limitation as round 1 — so the reported green runs (root 1981 passed; src-tauri 186 + 4 passed) rest on the main agent's execution; I found no static warning source, and no src-tauri file is touched by this commit, so the src-tauri suite is unaffected by construction.

## Recommendation

Apply the one-line doc reword (LOW — "before its content is indexed" instead of "before it is read", or the lexical `..` pre-check if "never read" is the contract), run `cargo test` (root + src-tauri) per the project rule, commit, and finish. Both round-1 findings are verified resolved; the change is otherwise ready.
