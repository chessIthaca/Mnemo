## Verdict: PASS

Verification review (round 2) of the LOW 1 fix from `.coding/reviews/2027-01-06-checkpoint-sha-backlogitemview-review.md`: `startup_snapshot`'s backlog read path now routes through `resolve_item_view` and delivers the same `BacklogItemView` wire shape as `backlog_list`. The fix is correct and complete, the new source-contract test is sound and not self-referential, no consumer of `StartupSnapshot.backlog` breaks on either side of the IPC, and the `#[allow]`-free invariant holds across src/, src-tauri/, and frontend/. No findings.

**Scope & method.** Full `git diff HEAD` re-read (8 files: startup.rs +47, backlog_cmds.rs, run_all.rs, contract_fixtures.rs, types.ts, ipc-contract.test.ts, dto-backlog-changed-payload.json, backlog.jsonl bookkeeping; plus untracked decision/plan/review files). The fix region verified against the surrounding production code: the store-lock pattern shared with `emit_backlog_changed`/`backlog_list`, the TS `StartupSnapshot` interface and its single consumer (App.tsx), and the `setBacklog` store signature. Literal sweeps: `#[allow(` across src/, src-tauri/src/, frontend/; `StartupSnapshot` across Rust + TS; `extract_checkpoint_sha` (pub → pub(crate) safety); `setBacklog` (TS consumer chain). Tests NOT re-run (read-only reviewer) — evidence is the main agent's, with every new/changed test body inspected against the changed code.

## Fix verification (LOW 1)

### 1. The fix is correct and complete

- **Field retype + doc.** `StartupSnapshot.backlog` is now `Vec<BacklogItemView>` (startup.rs:39); the field doc reads "same as `backlog_list` — the frontend-facing views, with image paths resolved to data URLs and the run-all checkpoint sha parsed from each note" — both previously-inaccurate claims (the field doc and types.ts's "always present from the backend") are now true for all four read paths.
- **Routing.** The command maps `store.items().iter().map(|i| resolve_item_view(&store, i))` inside the store-lock block (startup.rs:112-119) — byte-for-byte the pattern of `emit_backlog_changed` (backlog_cmds.rs:105-112) and `backlog_list` (:161-168). The lock is held only for the map and dropped at the block end, before `embedder_status()`; `agent_loops` was already dropped at :108, so there is no lock nesting and no new deadlock surface. This closes both gaps named in the finding: `checkpoint_sha` AND the pre-existing image-resolution gap (stored relative paths → data URLs) in the startup payload.
- **Visibility.** `resolve_item_view` is now `pub(crate)` (backlog_cmds.rs:97) — required and sufficient for the cross-module call; `BacklogItemView` stays `pub` (imported by contract_fixtures.rs). The stale `mnemo::backlog::BacklogItem` import was replaced by the view import — no unused import (green `cargo test` under `deny(warnings)` proves it).
- **No consumer breaks (Rust).** `StartupSnapshot` is referenced only inside startup.rs (struct :28, command :52, construction :124) — no other Rust file constructs or reads it; command registration is by name and unaffected by the field type.
- **No consumer breaks (TS).** `StartupSnapshot.backlog: BacklogItem[]` (tauri.ts:240) is unchanged and correct: the view's wire shape is flat (`#[serde(flatten)]`) plus always-serialized `checkpoint_sha`, and the TS `BacklogItem` interface now carries `checkpoint_sha?: string | null`. The single consumer, App.tsx:247 `setBacklog(snap.backlog)`, matches `setBacklog: (items: BacklogItem[]) => void` (useAgentStore.ts:566/:863); the runtime shape is a structural superset, so seeding typechecks and behaves. The sibling seeding path (useAgentEvents.ts:525, `backlog://changed` → `setBacklog`) already receives views — the two paths now agree.

### 2. The new source-contract test is sound

`startup_snapshot_backlog_routes_through_resolve_item_view` (startup.rs:141-157): `src.find("async fn startup_snapshot(")` hits the real definition at :52 first — the test module's own literal sits at :145 (later in the file) and no doc comment contains the literal, so the slice starts at the genuine body. The end marker `"\n}\n"` first matches the function's own column-0 closing brace at :131 — every inner block (`let handles = { … };`, the `for` loop, `let backlog = { … };`) closes with an indented `};`/`}` that cannot match. The body slice (:52-131) contains the real `resolve_item_view(` call at :117; the body's comments do not contain that literal, so the assertion can only pass via the actual call — not self-referential. It follows the established `backlog_retry_routes_through_guarded_requeue` / `backlog_read_paths_surface_checkpoint_sha` pattern, and the Tauri-`State` constraint on the command justifies source-contract over invocation. (The three backlog_cmds.rs slices were re-verified the same way — first `find` hit is each real definition, test literals come later — unchanged from round 1.)

### 3. No regressions elsewhere; `#[allow]` sweep clean

- The remainder of the diff is unchanged since round 1 and re-confirmed: the fixture chain still locks the wire shape end-to-end (Rust `BacklogItemView::new` over the sha-headed note → exact `serde_json::Value` equality against dto-backlog-changed-payload.json → TS assertions on the null case, the string case, and `note.startsWith(checkpoint_sha)`), and `extract_checkpoint_sha`'s `pub` → `pub(crate)` is safe — its only consumers are `BacklogItemView::new` (backlog_cmds.rs) and run_all.rs's own tests, both inside the src-tauri crate.
- `#[allow(` sweep: **zero attribute matches** in src/, src-tauri/src/, and frontend/ (210 files searched). The only non-vendor hits are two doc comments mentioning the term in prose (backlog_cmds.rs:524, main.rs:894 — explicitly fine) and vendored `vendor/tao/` (out of scope by policy). The attribute, its callerless comment block, and the old `pub` visibility are gone from run_all.rs; `extract_checkpoint_sha` has a real production caller chain (`BacklogItemView::new` ← `resolve_item_view` ← four read paths).

### 4. Test evidence

Main agent's runs (read-only reviewer did not re-run): root `cargo test` 2003 passed; src-tauri `cargo test` 191+4 passed (proves zero warnings under `deny(warnings)` at both crate roots); `npm test --workspace frontend` exit 0; `npx tsc --noEmit` exit 0. All new/changed test bodies inspected against the changed code and found to exercise the changed paths: the unit test hits `BacklogItemView::new`'s three note arms; the two source-contract tests pin the routing of all four read paths (`backlog_list`, `backlog_add`, `emit_backlog_changed`, `startup_snapshot`); the fixture test serializes the exact payload shape the commands emit; the TS assertions read the fixture the Rust test is compared against.

## Notes (non-findings)

- The code graph's caller list for `resolve_item_view` omits `startup_snapshot` — a stale index edge in the rebuildable, gitignored `codegraph.db` (backlog_cmds.rs was re-indexed; the startup.rs edge lags). The file content is authoritative and the source-contract test pins the wiring; not a code issue.
- Round-1 non-findings all still present and still harmless: the dead `?? ""` fallback in the TS test's `startsWith`, the unreachable `.unwrap_or(note)` in `extract_checkpoint_sha`, and the source-contract tests not pinning `BacklogItemView::new` inside `resolve_item_view` (its return type forces construction; the fixture pins the output).
- Bookkeeping is consistent with the session and should ride along in the commit: `.coding/backlog.jsonl` 212c14af in_flight with the checkpoint-sha note, the decision file `.coding/knowledge/decision/2027-01-06-checkpoint-sha-exposed-via-backlogitemview-ipc-l.md`, and the plan file — plus both review reports (round 1 and this verification).
