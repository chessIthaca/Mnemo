## Verdict: FINDINGS (0 high, 2 low)

The fix is correct and minimal: backlog_add now rides the Skill allow-list branch's always-available set, the flipped Skill assertion is a genuine fails-pre-fix regression test, and the new run_all_dispatch_next source-contract test is sound (the fn_body needle provably cannot match the test's own text). Both findings are documentation-sync LOWs: one stale doc comment enumerating the widened set (workflow/mod.rs:252-255), and pre-fix line coordinates in the BUG knowledge record (self-shifted by the fix's own test insertion).

## Scope reviewed

`git diff HEAD` on wt/agenticcoding (3 modified + 2 untracked files): src/tool/mod.rs (Skill branch + two test doc comments + flipped assertion), src-tauri/src/ipc/run_all.rs (new source-contract test), .coding/backlog.jsonl (item 66c65db9 in_flight stamp), .coding/knowledge/bug/2027-01-07-run-all-surface-lacked-backlog-add-the-skill-all.md, .coding/plans/274af962.md. Read-only review: every claim below was verified by reading the current tree and the diff; I could not run cargo test (reviewer is read-only), so the parent's recorded runs are cross-checked by construction instead.

## 1. Correctness — verified

**Skill branch edit (tool/mod.rs:505-526).** `|| name == "backlog_add"` added to the always-available set next to backlog_status/backlog_list, with the branch comment updated to carry the 66c65db9 rationale (the 2026-12-30 reversal's principle extends mid-skill). backlog_add is `ToolCategory::Workflow` (backlog.rs:99-101), so under `ToolFilter::Skill` it flows through the `_ =>` name-check arm — the edit is in the right place, minimal, and stylistically identical to its neighbors. All five base-state arms already name backlog_add (mod.rs:328, 367, 399, 455, 499) and are untouched by the diff — the interactive base surface is unchanged, as claimed.

**Flipped assertion = genuine regression test.** `backlog_add_visible_in_all_states` now asserts `visible(ToolFilter::Skill(vec![]))`. Pre-fix (per the diff's removed lines), the Skill always-available set was ask_user/current_plan/backlog_status/backlog_list; with an empty allowed list, backlog_add was hidden — the removed `!visible(...)` assertion codified exactly the reported symptom (status+list present, add absent). The flip therefore fails pre-fix and passes post-fix; the parent's recorded pre-fix failure (exit=101) is what the code requires by construction. The test exercises the changed path end-to-end (r.schemas → allows → the Skill branch).

**fn_body soundness (the tests-precede-code hazard).** `fn_body` (run_all.rs:526-536) composes the needle `fn {name}(` and extracts through the first column-0 `\n}\n`. I enumerated all 14 occurrences of "run_all_dispatch_next" in run_all.rs: only line 1289 (`pub(crate) async fn run_all_dispatch_next(`) contains the contiguous needle. The new test's own comment (line 772) and its `fn_body(...)` call (line 774) contain the bare name but never `fn run_all_dispatch_next(` — the composition defense documented on fn_body itself (lines 519-525) holds, and the same pattern is already proven by the halt_run_all / checkpoint tests. The extraction (1289 → the column-0 `}` at 1482; every inner closing brace is indented — the function was read in full) covers the whole body including the `AgentCommand::Prompt` send at 1468.

**The pinned fact is true.** run_all_dispatch_next (1289-1482) sends a plain `manager.send(main_id, AgentCommand::Prompt { text: run_all_prompt(&item.text), images })` — no ToolFilter, no tool_allowlist, no set_reviewer_allowlist, no `.schemas(` anywhere in the function. The banned-strings anchors are structural code identifiers, not prose — consistent with the bfbab877 source-contract re-anchoring guidance. The test's comment ("needs a Tauri AppHandle, so the wiring is pinned on source") is accurate. The test passes by design because it pins a verified design fact, exactly as the item's Tests field requested ("source-contract or unit test on the surface builder").

**Doc-comment updates.** Both updated test doc comments (backlog_add_visible_in_all_states, backlog_status_visible_in_all_states_including_skills) and the branch comment accurately describe the new behavior and cite the incident/backlog correctly.

## 2. Bugs / security — no issues found

- **Loop prevention / skill isolation:** the widening applies to every Skill-filtered surface — real skills AND non-reviewer subagent allow-lists (set_tool_allowlist also resolves to ToolFilter::Skill, workflow/mod.rs:378-382). The risk class (an agent queueing work for itself) is already accepted for every base state — the run-all agent has backlog_add in Planning/Executing/Reviewing/Complete today — and the tool's own description ("Only add items the user actually asked to queue — pending items are auto-dispatched", backlog.rs:106-111) is the standing guard. No new risk class; the uniform widening (option 1, no run-all special-casing) matches the state-derived filter design.
- **Reviewer ROLE filter genuinely unaffected:** the `ToolFilter::Reviewer` arm (mod.rs:527-537) is unchanged (`current_plan` || listed only); REVIEWER_BASE_TOOLS (spawn.rs:263+) is read-only queries and does not name backlog_add; `reviewer_filter_is_a_strict_allow_list` still asserts `!backlog_add` (mod.rs:1979) and `reviewer_allowlist_is_a_strict_filter` (workflow/mod.rs:1768-1825) pins nothing-auto-granted. Reviewers cannot mutate the backlog.
- **No other codification of the old divergence missed:** all 47 `"backlog_add"` occurrences (8 files) and all 25 `ToolFilter::Skill` constructions (3 files) checked. The only test asserting backlog_add's Skill visibility was the one flipped; the other Skill-surface tests (skill_filter_is_an_allow_list, schemas_reviewer_excludes_memory_mutations, the workflow allow-list tests at workflow/mod.rs:1706-1751) assert unrelated tools and remain green. The Skill-state prompt fragment (prompt.rs:698-720) does not enumerate the always-available set — the "no prompt change needed" claim holds. merge_to_main.toml's tool list doesn't name backlog_add (no redundancy introduced) and its prompt doesn't claim unavailability.
- **write_review_report isolation unaffected:** still hidden under Skill even when a skill lists it (mod.rs:1888-1908) — backlog_add's admission doesn't touch that rule.
- **backlog.jsonl stamp:** pending → in_flight with plan_id/plan_title and the checkpoint sha note — the standard Executing-entry stamp; normal bookkeeping, valid JSONL, field order matches the existing format.

## 3. Findings

**LOW-1 — workflow/mod.rs:252-255: `set_tool_allowlist`'s doc comment still enumerates the Skill auto-grant set without backlog_add.** The doc says the permissive allow-list "still auto-grants memory tools + `ask_user` + `backlog_status`". This change widened exactly that set (the branch comment in tool/mod.rs was updated accordingly) but missed this doc site — a reader of workflow/mod.rs now gets an enumeration that omits the newly admitted backlog_add. (It was already missing current_plan/backlog_list pre-change; the NEW omission is backlog_add.) Fix: one line — e.g. "…auto-grants memory tools + `ask_user` + `current_plan` + the backlog tools (add/status/list)".

**LOW-2 — BUG knowledge record cites pre-fix line coordinates.** .coding/knowledge/bug/2027-01-07-run-all-surface-lacked-backlog-add-the-skill-all.md says "run_all_dispatch_next (src-tauri/src/ipc/run_all.rs:1257-1450)". Those were the pre-fix coordinates; the fix's own 32-line test insertion shifted the function to 1289-1482, so a reader following the committed pointer lands 32 lines off (inside compact_then_dispatch_next's tail). Same class, smaller: "the Skill allow-list branch (src/tool/mod.rs:505-521)" — the branch is now 505-526. Fix: update the two ranges (or keep only the start anchors, which are exact).

## 4. Non-finding observations (pre-existing drift, NOT introduced by this change — no action required for this plan)

- tool/mod.rs:1857-1858 (`skill_filter_is_an_allow_list` doc: "only the named tools (+ all memory tools) are visible") and :1994-2002 ("the Skill auto-grant trio") enumerate the always-available set incompletely — they already omitted ask_user/current_plan/backlog_status/backlog_list before this change; the change makes them one tool more incomplete. If LOW-1's sweep happens, these could be aligned in the same pass.
- prompt.rs STATE_SUBAGENT ("your tools are exactly your spawn-time allow-list") was already not literally true pre-change (memory/ask/backlog tools ride along); unchanged by this diff.

## 5. Constitution checks

- **Documentation sync:** branch comment + both test doc comments + plan file accurate; BUG memory (id 65f8d22b) written and consistent with the knowledge record; two LOW findings above are the gaps.
- **Multi-platform neutrality:** pure Rust logic + comments + an include_str!-based test; no paths, no platform APIs, no shell syntax. PASS.
- **Code style:** the new test follows run_all.rs's convention (internal `//` comment, like should_stamp_in_flight / halt_run_all tests); the filter edit matches the name-chain style; no new public functions (no doc-comment obligation triggered, and the test carries a rationale comment anyway). PASS.
- **Bug-plan checklist:** the recorded regression test (backlog_add_visible_in_all_states, flipped Skill assertion) exists and exercises the changed path; the root cause is documented (knowledge record + BUG memory); the plan records the regression test name. PASS.
- **Tests:** not runnable by this read-only reviewer; the parent's recorded runs (root 2031 passed / 0 failed / 4 ignored; src-tauri 233 passed / 0 failed; both exit=0, warning-free under deny(warnings)) are consistent with everything verified by reading — the only behavior change (backlog_add visible under Skill filters) has no contradicting assertion anywhere in the tree, and the flipped assertion is the fails-pre-fix regression the bug plan requires.
