## Verdict: FINDINGS (0 high, 2 low)

Review of plan 8b5f37fb ("Swallowed errors on durable writes — log + collect", quality review LOW 6, bug_fixing) — ALL uncommitted changes on wt/agenticcoding: `src/agent/turn.rs`, `src/config/mod.rs`, `.coding/backlog.jsonl` (item 5fd31477 pending → in_flight, normal dispatch bookkeeping), untracked `.coding/plans/8b5f37fb.md`.

Both named defects are correctly fixed. The turn.rs fix preserves fire-and-forget semantics exactly and follows the "mnemo:" log prefix pattern; the rollback extraction is behavior-identical to the old inline block (same restore order, same cleanup, byte-identical error when no restore fails) plus the requested logging + collection; the four regression tests genuinely pin the collection; the DI seam is the right cross-platform call. Two low findings below — one scope-adjacent residual of the same defect class, one bookkeeping.

### Findings

**LOW 1 — Residual same-class swallowed durable-write errors at the two scoped-out sites (`src/agent/turn.rs:1182`, `src/agent/turn.rs:2163`).**
The plan's scope decision (5) keeps `let _ = store.record_tool_event(...)` (turn.rs:1182 — working-memory capture for recall, inline awaited) and `let _ = store.record_request_stats(&stats)` (turn.rs:2163 — usage telemetry, inside `tokio::spawn`, identical shape to the fixed site) as silent fire-and-forget, on the rationale that "the finding explicitly blesses fire-and-forget for non-named sites." That mischaracterizes the finding: LOW 6 blesses fire-and-forget for **UI events** ("the fanin_tx pattern"), not for durable writes — and both of these are durable writes whose failed Results vanish with no log, the exact defect class this plan's own root-cause statement names ("`let _ =` discards Results on durable writes"). The stakes are lower than the two fixed sites (telemetry / recall-capture vs. episodic memory / config-set integrity), so this does not block the change — but the fix shape is the identical one-liner already applied at turn.rs:957-961. Recommend: apply the same `if let Err(e) = ... { eprintln!("mnemo: ...") }` pattern to both sites, or queue a follow-up backlog item so the class is closed rather than left half-eradicated.

**LOW 2 — BUG: memory record not findable despite plan step 2 marked complete.**
The bug-plan closing check requires a BUG: record (symptom → root cause → fix + regression test name). Five memory searches — including `record_type:"bug"` (returned only 3 unrelated BUG records) and a `BUG:` prefix search — surface no BUG record for this defect, and `git status` shows no new `.coding/knowledge/bug/` file; yet plan step 2 ("memory_write a BUG: record") is checked off. The finish-time auto-capture may still write it, but per the plan it should already exist. Action: write (or confirm) the BUG record before `finish`.

### Verification detail (all eight requested checks)

**1. turn.rs fix (a) — VERIFIED CLEAN.** The handler (turn.rs:952-962) is still `tokio::spawn(async move { ... })` — fire-and-forget semantics unchanged: no await propagation change, the spawn's Result is still dropped by the caller, and the error path cannot panic (`eprintln!` is infallible; `if let Err(e)` only binds). The comment documents the rationale ("memory capture must never block or break the turn") and the diff shows no other change to the handler. The message `"mnemo: failed to persist plan-completion memory: {e}"` follows the codebase's established prefix pattern — confirmed at `src/thread_util.rs:83` ("mnemo: latest-wins worker sink panicked — continuing") and `src-tauri/src/watchdog.rs:271` ("mnemo: hang report write failed ({}): {e}"). Logging-only inside spawn with no unit-test seam is acceptable here: the finding's ask for (a) is exactly "log on Err", and the change is two lines verified by inspection.

**2. Rollback behavior-identity — VERIFIED.** Old inline block (diff deletion): (i) `for done in &committed { let _ = restore_from_backup(done); }`, (ii) `let _ = restore_from_backup(path);`, (iii) `for p in &paths { let _ = fs::remove_file(staging_path(p)); }`, (iv) `return Err(e);`. New `rollback_with_restore` (config/mod.rs:331-347): (i)+(ii) fused as `committed.iter().copied().chain(std::iter::once(failed))` — **same order** (committed paths first, then the failed one), each now logged + collected on Err; (iii) cleanup loop is **byte-identical** (still best-effort/silent, per documented decision 4 — a leftover `.tmp` is inert clutter the next save overwrites, and the finding only asked for restore errors); (iv) `save_all` returns `Err(describe_rollback_failure(e, &restore_failures))`, and with an empty failure list `describe_rollback_failure` returns `commit_err` **unchanged** (the same `Error` value moved through, not re-wrapped) — byte-identical to the old `return Err(e)`, pinned by `describe_rollback_failure_without_failures_returns_original` (expected string "config error: commit failed" matches the Display at `src/error.rs:15-16`). One accepted nuance: in the non-empty case the error is re-wrapped as `Error::Config` regardless of the original variant — fine, since save_all's callers format the error for the UI rather than matching variants.

**3. Regression tests — VERIFIED, they genuinely pin the collection.** `rollback_collects_restore_failures`: the injected closure fails for exactly `config` (the committed path) and succeeds for `endpoints` (the failed path); asserts exactly one failure, `failures[0].0.ends_with("config.toml")`, error contains "disk full", and both staging temps cleaned — meaningful assertions on path, error text, and cleanup. `rollback_clean_when_restores_succeed` exercises the **production wrapper** `rollback_partial_commit` with the real `restore_from_backup` (config.toml + .bak restored to "old", bak consumed, endpoints tmp cleaned) — so the DI seam does not leave the real path untested. The two `describe_rollback_failure` tests pin both branches (original unchanged / enriched message). Written-first: the tests reference helpers absent from the old code, so they fail to compile against it — the appropriate reproduce step for an extraction+collection change. The only un-pinned surface is save_all's two-line wiring (delegating directly to the helpers), verified by inspection; an end-to-end save_all rollback test would need a second DI seam on the commit side, which the plan reasonably declined.

**4. Message format — VERIFIED user-actionable.** `"{commit_err}; additionally, rollback failed — the config set may be half-committed: {path: err; ...}"` names every failed path with its error, preserves the original commit error, and explicitly warns about the half-committed state. The `\` line-continuation in the format string correctly strips the newline + indentation ("may be half-committed"), consistent with the test's `contains("half-committed")`.

**5. Multi-platform neutrality — VERIFIED, and I agree with the DI reasoning.** The diff contains no platform-specific code: pure `std::{fs, path}`, `eprintln!`, no `cfg(windows)`, no Windows paths or shell syntax. The DI seam (`restore: &dyn Fn(&Path) -> Result<()>`) exists precisely because real rename-failure injection is not portable — the plan documents the failed Windows attempt (bak-as-directory + bak.restore-as-directory → `restore_from_backup` returned Ok on Windows, whose rename semantics rescue or allow cases Unix rejects). Filesystem-trick failure injection would be flaky per-platform; the DI closure is deterministic everywhere, and the production wrapper still gets real-filesystem coverage via `rollback_clean_when_restores_succeed`. This is the right call.

**6. Dead code / unused imports — VERIFIED none.** All three new helpers are used in production and/or tests: `rollback_partial_commit` (save_all:217 + test), `rollback_with_restore` (wrapper:361 + test), `describe_rollback_failure` (save_all:218 + tests). No new imports were added to either file — the tests reuse imports already present for the existing config tests (`tempdir`, `fs`, `backup_path`, `staging_path`). Both suites stated green (root 2008+16, src-tauri 216+4) under `#![deny(warnings)]`, which makes green ≡ zero warnings; code inspection found nothing the compiler would flag.

**7. Documentation sync — VERIFIED, no updates required.** All three helpers carry doc comments (they are private, so the constitution's pub-doc rule doesn't even apply — documented anyway). The module doc (config/mod.rs:5-15) describes the four-file loader, not rollback mechanics — still accurate. The atomic-write strategy docs live on `write_atomic_staged`/`commit_atomic_rename`/`restore_from_backup` and remain accurate; the new helpers' docs complement them. Neither README.md nor PLAN.md describes save_all's rollback specifics (searched "rollback"/"atomic" across *.md — no hits in either); `.coding/grok.md:723`'s "Atomic temp+rename + `.bak` rollback on mid-rename failure" one-liner remains true (the rollback still happens; failures are now logged/surfaced). Nothing to sync.

**8. Bug-plan checks — root cause addressed at both named sites; tests exercise the changed path; BUG record gap flagged as LOW 2.** (a) turn.rs:957 logs on Err; (b) config/mod.rs:217-218 collects and surfaces. The regression tests exercise the changed path (see check 3). The BUG: memory record is not findable — see LOW 2.

### Scope note on the backlog item flip

`.coding/backlog.jsonl` changes only item 5fd31477's status (pending → in_flight) and note (checkpoint sha) — standard dispatch bookkeeping, no content loss (the union-merge driver + full-text preservation verified in the diff).
