## Verdict: FINDINGS (0 high, 1 low)

The extraction is a faithful, behavior-preserving code motion with genuinely compensating behavior tests. One low finding: the move left four dead imports in Message.tsx (basename, browserArgLabel, graphCallLabel, webFetchLabel) — their only call sites were inside the relocated argLabel. Fix before commit; everything else verified clean.


### Scope reviewed

All uncommitted changes on `wt/agenticcoding` vs HEAD (git diff + untracked): Message.tsx (−311 lines), messageArgLabel.test.ts, EndpointCard.tsx, types.ts/.test.ts, attachImages.test.ts, toolCardPaths.ts/.test.ts, vitest.config.ts, backlog.jsonl bookkeeping, plus the untracked `frontend/src/lib/messageEquality.ts`, `frontend/src/lib/messageEquality.test.ts`, and `.coding/plans/74e8d388.md`. Read against plan 74e8d388 and quality review LOW 2. As a read-only reviewer I could not re-run vitest/tsc; claims are assessed for consistency (they are — see checks below), and the main agent re-runs the suite per the closing sequence.

### Verification against the task's VERIFY list

1. **Pure code motion — CONFIRMED.** `arePropsEqual` + `MessageProps` in messageEquality.ts:12-98 are byte-identical to the diff-deleted Message.tsx bodies (doc comments included; only `export` was added, required for the import). `argLabel`/`BACKLOG_TITLE_MAX`/`displayName` appended to toolCardPaths.ts:765-983 are verbatim; all of argLabel's dependencies (basename, graphCallLabel, browserArgLabel, webFetchLabel) were already in-file there, so no new imports were needed and no circular import exists. The memo wiring is intact (`export const Message = memo(MessageImpl, arePropsEqual)`), and the rendering/store reads are untouched — ToolCard still calls `argLabel` (Message.tsx:442, 658), `displayName` (:512), `buildPathChips` (:434), `argPaths` (:440), etc.
2. **Four number-input sites deduped and behavior-identical — CONFIRMED.** All four onChange handlers (endpoint-level max_context/max_output_tokens + ModelRowConfig's two) now use `parsePositiveIntInput(e.target.value.trim())` with the `if (v !== undefined)` guard: `""` → null persists the clear, finite > 0 → floor, invalid → no onChange call — matching the old inline logic exactly (including the trim-before-parse). No `Number(`/`Math.floor` parse remains anywhere in EndpointCard.tsx. `modelConfigFor` is still used (EndpointCard.tsx:783), so its import stays live. The efforts input's `parseEffortsList` is the same split/trim/filter-empty.
3. **Removed source-contract tests compensated — CONFIRMED.** The attachImages imagesEvicted check is replaced by a real behavior test (messageEquality.test.ts:57-79 — images → imagesEvicted flip, count change, and equal-count cases, i.e. the mem-perf LOW 4 regression); the toolCardPaths webFetchLabel source-contract is replaced by the `web_fetch delegates to webFetchLabel` behavior test. The remaining `?raw`/readFileSync source-contracts all still target code that stayed in Message.tsx (attachImages.test.ts:130-133 placeholder chip; messageArgLabel.test.ts:279-289 InlineMarkdown wiring; toolCardPaths.test.ts:692-728 + 740-764 memory/frameless card wiring — none reference the moved functions).
4. **effectiveCaps `?? null` normalization — behavior-equivalent at every site — CONFIRMED.** All usages are loose comparisons or guarded by them: EndpointCard.tsx:543 (`effCapsCtx != null &&`), :544 (`!==` only reached when non-null), :547 (`fmtTokens` only under the != null guard), :564-569 (same for out), :585-586 (`== null` — `undefined == null` and `null == null` are both true). The capsRefModel-null guard is preserved as the ternary's else branch ({ ctx: null, out: null }).
5. **vitest registration — CONFIRMED.** `src/lib/messageEquality.test.ts` added at vitest.config.ts:57 (correct alphabetical slot). All other touched test files were already registered: settings glob (:17 covers types.test.ts), messageArgLabel.test.ts (:23), attachImages.test.ts (:50), toolCardPaths.test.ts (:69). No other registration needed.
6. **Import hygiene — ONE FINDING** (below). Everything else verified used: TranscriptEntry (Message.tsx:42), ToolInvocation (:407, :621), memorySearchLabel (:58), argPaths/buildPathChips/searchResultInfo/browserResultInfo/toolErrorSummary/shellCallFromArgs/parseShellOutput/fileEditDiff/parseReadFilesSections/ToolCardChip (:434-652); readFileSync in toolCardPaths.test.ts (:694, :742); `source` in messageArgLabel.test.ts (:279-289); `messageSource` in attachImages.test.ts (:130-133).
7. **Multi-platform neutrality — CONFIRMED.** Pure TS string/number logic; no platform-specific APIs, paths, or shell syntax anywhere in the moved code or tests.
8. **Documentation sync — CONFIRMED.** No README.md/PLAN.md references to argLabel/arePropsEqual/displayName/messageEquality/toolCardPaths exist (searched — zero matches), so nothing is stale. Module doc comments moved with the code; toolCardPaths.ts's module doc ("pure path/label extraction … drives the ToolCard header") remains accurate for argLabel/displayName.

### Finding 1 (LOW) — four dead imports left in Message.tsx by the extraction

**Where:** `frontend/src/components/chat/Message.tsx:12`

`basename`, `browserArgLabel`, `graphCallLabel`, and `webFetchLabel` are imported from toolCardPaths but no longer used in Message.tsx — their only call sites were inside `argLabel`, which moved to toolCardPaths.ts. A whole-file search confirms the only remaining occurrences are the import line itself and a comment at :427 ("share a basename get parent-dir-qualified…"). `npx tsc --noEmit` passing is consistent with this: frontend/tsconfig.json:15 sets `noUnusedLocals: false`, so nothing flags them. No runtime or test impact, but this is exactly the extraction residue a hygiene change should not ship (and the task's own VERIFY item 6 asserted "no unused imports anywhere").

**Fix:** drop the four names from the Message.tsx:12 import, keeping `argLabel, argPaths, browserResultInfo, buildPathChips, displayName, fileEditDiff, memorySearchLabel, parseReadFilesSections, parseShellOutput, searchResultInfo, shellCallFromArgs, toolErrorSummary, type ToolCardChip` (all verified used). One-line edit; no other file is affected.

### Consistency checks (not findings)

- **Test-count math:** 923 pre-item − 2 removed source-contracts + 26 new (11 messageEquality + 9 "argLabel — remaining branches" + 2 displayName + 4 "endpoint caps + input parsing") = 947 — matches the claimed green run.
- **New tests are correct against the moved code:** messageEquality.test.ts's 11 cases map 1:1 to arePropsEqual's branches (streaming, kind mismatch, text kinds incl. steer, user images ref, imagesEvicted, tool calls ref, qa, skill, memory all-fields + hits ref, vision all-fields, unknown-kind default). The argLabel-remaining-branches expectations match the moved bodies (quoted search pattern + glob, graph delegation, basename fallback, malformed JSON → null). types.test.ts's effectiveCaps case correctly exercises override-wins and endpoint-level fallback via modelConfigFor's default config.
- **toolCardPaths.test.ts's `caseSrc` helper** (:756-763) uses `lastIndexOf('case "KIND":')` with a comment noting the equality switch "carries an earlier one" — with arePropsEqual's switch now gone from Message.tsx, the last occurrence is still MessageImpl's render switch, so the helper keeps working (the comment is now slightly stale but harmless).
- **VisionModelInfo** (lib/tauri.ts:332-339) has optional `context_length`/`max_output_tokens` — `discoveredCapsById`'s `?? null` matches the old inline loop exactly.
- **No other importer of the moved symbols:** messageArgLabel.test.ts was rewired to toolCardPaths; no other file imports argLabel/displayName/arePropsEqual from Message.tsx (repo-wide search; the `?raw`/readSrc hits are source reads, not imports).
- **.coding/backlog.jsonl** delta is the expected pending → in_flight bookkeeping for plan 74e8d388; the untracked plan doc rides along in the closing-sequence commit.
