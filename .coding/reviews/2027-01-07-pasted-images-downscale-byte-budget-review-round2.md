## Verdict: PASS

Round-2 verification review of ALL uncommitted changes on `wt/agenticcoding` for plan ab0ef952 — "Pasted images: downscale on paste + transcript image byte budget (mem-perf review LOW 4)". All three round-1 fixes (LOW 1, LOW 2, minor test gap) are correctly applied and accurate; the fixes are comment/doc/test-only and introduce no code-path changes; and the round-1 verification detail (eviction pass, downscale helper, Message memoization, type safety, platform neutrality, security) re-checked against the current code — all still holds. No findings.

### Fix verification

**Fix 1 (LOW 1 — `/save` exports the post-eviction transcript): correctly applied, accept-and-document.**
- `frontend/src/hooks/agentState.ts:432-437` — the `capTranscriptImages` doc comment now reads "Eviction is display- AND export-side: the model's context echo is built server-side and is unaffected, but `/save` serializes the capped transcript — budget-evicted images are permanently absent from saved files (they restore as the placeholder chip). That is the intended trade-off: exporting pre-eviction payloads would reintroduce the unbounded multi-MB save files this budget exists to bound." Accurate on every claim: the model's context is the server-side message history (unaffected by display-transcript eviction); `/save` serializes `agent.transcript`, which is post-`capTranscript` (all append sites funnel through it — verified in round 1, unchanged); evicted entries serialize as `{ kind: "user", text, imagesEvicted: n }` with no payload, and `/load` restores them as the placeholder chip. The rationale matches round-1's recommended option (a).
- `frontend/src/components/layout/InputBar.tsx:480-484` — the `/save` case gained the comment "Serializes the CAPPED transcript: entries beyond the 1000-entry count cap are already gone, and images evicted by the transcript image byte budget (capTranscriptImages) serialize as `imagesEvicted` placeholder counts — no payload. Intended: saves stay bounded instead of growing unbounded with pasted images." Accurate: `MAX_TRANSCRIPT_ENTRIES = 1000`, and the eviction rebuild destructures `images` out entirely.

**Fix 2 (LOW 2 — README/PLAN doc sync): correctly applied, wording matches shipped behavior.**
- `README.md:69` — new bullet placed exactly after the image-parsing bullet (line 68) and before "React + Tailwind UI" (line 70): "Pasted/dropped image attachments are memory-bounded: each is downscaled on attach to a 1568 px long edge (the common vision-API input limit) and re-encoded as JPEG before it reaches the transcript and the model, and the transcript keeps a rolling byte budget for retained image payloads — once exceeded, the oldest attachments are unloaded and render as an 'N images unloaded to save memory' placeholder chip (the model already saw them; `/save` writes the capped transcript)." Every element checks out against the code: `MAX_IMAGE_LONG_EDGE = 1568` (attachImages.ts:22), `toDataURL("image/jpeg", 0.85)`, newest-first retention = oldest unloaded, chip text "unloaded to save memory" (Message.tsx), and the parenthetical matches the Fix-1 documentation.
- `PLAN.md:886-888` — the vision-fallback bullet now ends "Attachments are capped at a 1568 px long edge (JPEG) on paste before they are sent — bounding both the model payload and the transcript's retained image bytes." Accurate and correctly scoped to that bullet's topic.

**Fix 3 (minor gap — multi-image `imagesEvicted` pin): correctly applied and semantically sound.**
- `frontend/src/hooks/agentState.images.test.ts:117-130` — "counts every image of a multi-image entry in the placeholder" builds one entry with `[imageChars(MAX_TRANSCRIPT_IMAGE_CHARS), imageChars(10)]` and asserts the evicted result is `{ kind: "user", text: "multi", imagesEvicted: 2 }`. Traced against the implementation: total = MAX+10 > budget → over-budget walk; the single entry's chars (MAX+10) overflow at kept=0 → `evictFrom = 0` → rebuild evicts it with `imagesEvicted: images.length` = 2. The test pins the whole-entry count (not 1, not the overflowing image only) and would fail on any wrong count. File now has 14 tests (12 `capTranscriptImages` + 2 composition), matching the reported suite growth 917 → 918.

### Regression check — none

The only deltas vs the round-1-reviewed state are: the two doc comments (agentState.ts, InputBar.tsx), the README/PLAN.md bullets, and the new test. Zero runtime code changed: the `capTranscriptImages` body (agentState.ts:439-483), `capTranscript`, the attachImages.ts pipeline, Message.tsx chip + `arePropsEqual`, types.ts, BacklogView/InputBar wiring, and vitest.config.ts entries are byte-for-byte what round 1 verified. The `.coding/backlog.jsonl` change (item 0be09983 pending → in_flight, plan ab0ef952) is expected bookkeeping. Untracked files are the expected set: `attachImages.ts`, both test files, the plan file, and the round-1 report.

### Round-1 spot-check — still holds

- **Eviction pass** (agentState.ts:439-483): fast-path total → same-array return under budget; newest-first walk with `kept + chars <= MAX` and `evictFrom` break; rebuild evicts the overflow entry itself (`i > evictFrom`) and every older image-bearing entry; `images: []` never rewritten; `const { images, ...rest }` keeps `ts`/`entryId` and makes `images`/`imagesEvicted` genuinely mutually exclusive (idempotent); untouched entries keep object references (memoization-safe). All re-verified in the current source.
- **Downscale helper** (attachImages.ts, read in full): non-`data:image/` and ≤512 KiB-char fast paths before any decode; white fill before `drawImage`; keep-smaller-of original vs re-encoded; zero-natural-dimension guard; whole decode/canvas path in try/catch returning the original; `fileToAttachedDataUrl` as the single entry point with both local `fileToDataUrl` copies deleted from InputBar/BacklogView.
- **Message.tsx**: chip renders only for `imagesEvicted != null && > 0` with a static `title` (no injection); `arePropsEqual` compares both `images` refs and `imagesEvicted` values, so the eviction flip re-renders exactly the affected row.
- **Type safety**: `imagesEvicted?: number` on the user variant only, with an accurate doc comment (types.ts:320-333); accessed inside the narrowed `case "user"`.
- **Platform neutrality**: only standard DOM APIs (`Image`, `FileReader`, `canvas.getContext("2d")`, `toDataURL`, `imageSmoothingQuality`) — available in both WebView2 and macOS WebKit; no platform-specific code.
- **Security**: no new IPC surface, no path handling, no untrusted-input parsing beyond the browser's data-URL decode; data URLs render via `<img src>`.

### Notes (non-findings)

- The README's "each is downscaled … and re-encoded as JPEG" is a user-level simplification: the ≤512 KiB fast path skips the re-encode (preserving PNG alpha for small images). The bullet's actual claim — attachments are memory-bounded — remains true (small images are already small), and the wording matches what round 1's fix suggestion proposed. No action needed.
- Test status as reported by the parent: `npm test` 918 tests / 66 files passing (consistent with round 1's 917 + the one new test); `cargo test` passing (no Rust changes). Both new test files are wired into `frontend/vitest.config.ts`.
