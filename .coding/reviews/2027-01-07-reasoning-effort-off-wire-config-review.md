## Verdict: FINDINGS (0 high, 1 low)

Review of plan f2b14799 (backlog 1e40407c) — `reasoning_effort_off_wire` config-override for the effort-"off" wire encoding — over all uncommitted changes on `wt/agenticcoding` (11 files, +359/−24). The change is correctly implemented end-to-end and purely additive; all nine verification points pass. One low-severity stale doc comment to fix (L1).

### Verification results (task points 1–9)

**1. Three seams agree — PASS.** `effective_reasoning_effort_for` (src/config/endpoints.rs:408-417), `resolve_reasoning_effort` (src-tauri/src/ipc/config_io.rs:97-102), and the builder guard (src/provider/openai/request.rs:338-356) all fold identically: config (`reasoning_effort_off_wire_for`, model-level → endpoint-level) first, then the kind+name policy as zero-config fallback. The code graph confirms `reasoning_effort_off_wire_value` has exactly these three callers, and `body["reasoning_effort"]` is written at exactly one site (request.rs:351/354) — no fourth seam exists (the Responses-API path shares `build_request_json`; the self-healing 400 retry at openai.rs:631-637 only *removes* the field). The allow-list clamp paths in both resolvers use the same folded `off_wire`, so a value clamped to `"off"` is encoded consistently too. A literal `"off"` cannot reach the wire on any default path (the policy never returns `"off"`); it is emitted only when a user explicitly configures `reasoning_effort_off_wire = "off"` — the documented semantics of the override (the field IS "the wire value off encodes as"), same trust model as `extra_body` pass-through. By design, not a defect.

**2. Zero behavior change with no config — PASS.** With the field unset everywhere, `reasoning_effort_off_wire_for` returns `None` and `.or_else(policy)` reproduces the previous behavior exactly at all three seams — including the Anthropic early-return (yields `None`, same as the policy's own Anthropic-kind gate) and the `model_id.unwrap_or_default()` edge (empty id → no model spec → endpoint level, matching the old `unwrap_or_default` policy call). Serde `default` + `skip_serializing_if = "Option::is_none"` on both structs → absent = `None`, no serialization churn. Parent reports cargo test green (2015+16) and vitest green (971); the diff touches no frontend file (the field is deliberately not in the settings DTO — same pattern as `stop_boundary_strings`).

**3. Round-trip preservation — PASS.** `apply_endpoints` carries the field over at both levels (src/config/patch.rs:242-244 endpoint, 265-267 model), matching the established hand-edited-TOML pattern; `apply_endpoints_preserves_reasoning_effort_off_wire` covers both levels. The load path (`load_or_default`, src/config/endpoints.rs:636-658) deserializes directly into `EndpointsFile` (`Vec<Endpoint>`) and only normalizes `base_url` — no field stripping on load. The UI save path round-trips correctly: `ModelSpecDto` → `into_endpoint` (uses `..Default::default()`, so the new ModelSpec field compiles clean and arrives `None`) → `validate_endpoint` (sets `None`) → carry-over restores from `current`. (Pre-existing caveat, unchanged by this diff: carry-over is keyed by endpoint name + model id, so renaming an endpoint/model in the UI drops the field — identical to temperature/stop/extra_body.)

**4. Anthropic gate — PASS.** `reasoning_effort_off_wire_for` early-returns `None` for Anthropic kind (endpoints.rs:531-533); the policy fallback also returns `None` for Anthropic kind, so config on an anthropic endpoint stays inert at every seam. Test `reasoning_effort_off_wire_for_ignores_anthropic_kind` covers both the method and `effective_reasoning_effort_for`.

**5. serde/TOML shape — PASS.** Exact field name `reasoning_effort_off_wire`, `#[serde(default, skip_serializing_if = "Option::is_none")]` on both `Endpoint` and `ModelSpec`; `parses_and_resolves_reasoning_effort_off_wire` covers TOML parse at both levels, model-wins, and unknown-model-inherits-endpoint-level.

**6. Documentation sync — PASS (one stale bullet → L1).** README config paragraph documents the field (name, both levels, model-wins, default behavior, escape-hatch rationale — mirrors the `stop_boundary_strings` doc style); PLAN.md reasoning-state item 7 notes the override; doc comments added on every new public item (both struct fields, the resolver method, `OpenAiClientConfig` field, `client_factory` doc, policy.rs module + fn docs). No shipped endpoints.toml example exists in-repo (only `debug.config.toml`, a config.toml example that merely references endpoints.toml) — README is the established config-docs surface for hand-edited TOML fields, so the backlog's "update endpoints.toml examples" is adequately covered by the README paragraph.

**7. Multi-platform neutrality — PASS.** Pure Rust config logic; no paths, no platform APIs, no shell syntax.

**8. Security — PASS.** The configured value is emitted via `serde_json::json!` (proper JSON escaping) and sent verbatim, exactly like `stop`/`extra_body` — the user's own config file to their own provider; no new trust boundary, no injection surface.

**9. Code quality — PASS.** Doc comments on all new public items; no dead code (`reasoning_effort_off_wire_for` used at 3 sites; the field read at all seams + carry-over); the only production `OpenAiClientConfig` constructor is `openai_client_config` (client_factory.rs:90), which threads the field (vision.rs:319 is test-only); warning-free per the parent's green `cargo test` under `#![deny(warnings)]`. Non-blocking observation (not a finding): `reasoning_effort_off_wire_for` returns an owned `Option<String>` (clones), and the two resolver seams then `as_deref()` + re-`to_string()` — two small allocations where an `Option<&str>` return would need one. Cold path (provider build / model swap / settings save, never per-request) and consistent with sibling resolvers (`stop_for`, `extra_body_for` return owned) — noted for the record only.

**Regression-test shapes** (all four required by backlog 1e40407c, present): config encodes off regardless of name (endpoints.rs, config_io.rs, openai/tests.rs); absent config + non-DeepSeek name omits off (pre-existing tests unchanged); absent config + deepseek-v4-flash → none (pre-existing tests unchanged); explicit config beats policy when both apply (endpoints.rs + openai/tests.rs). Plus the anthropic-gate, TOML-parse, and patch round-trip tests — 8 new tests total.

### Findings

**L1 (low, docs) — stale `None`-return bullet in `effective_reasoning_effort_for`'s doc comment.** src/config/endpoints.rs:368-370 still reads: returns `None` (omit the request field) when "the configured value is `"off"` and the model is not DeepSeek-family" — but with `reasoning_effort_off_wire` configured, `"off"` on a non-DeepSeek name now resolves to `Some(config value)`. The paragraph immediately below (lines 372-375) describes the new resolution correctly, so the doc contradicts itself. Fix (one line): reword the bullet to e.g. "the configured value is `"off"` and no off-encoding applies (no `reasoning_effort_off_wire` config and not a DeepSeek-family model)". Optional related polish in the same spirit: openai.rs:81-83 ("it encodes per provider (`"none"` for DeepSeek-family models, omitted otherwise)") could name the config override — though the adjacent `reasoning_effort_off_wire` field doc (openai.rs:85-91) already covers it, so that part is optional.

### Summary

The config-override-with-default design (option b) is faithfully realized: config wins at every seam, the policy survives as the zero-config fallback, the Anthropic gate holds, hand-edited TOML survives both load and Settings saves, and the no-config path is byte-identical to before. Fix the one-line stale doc bullet (L1), re-run `cargo test`, and this is ready to commit.
