## Verdict: FINDINGS (0 high, 2 low)

Review of all uncommitted changes on `wt/agenticcoding` for plan 531fc221 (quality review LOW 4 — extract main.rs startup decision logic into startup.rs + tests). The delta: new `src-tauri/src/startup.rs` (364 lines, five `pub(crate)` pure helpers + 20 cross-platform unit tests), six targeted edits in `src-tauri/src/main.rs`, plus app-managed bookkeeping (`.coding/backlog.jsonl` status flip — note hash matches HEAD `98b2f92`, consistent; untracked plan file).

**Bottom line:** the extraction is a faithful, behavior-preserving code motion; the call-site rewiring is correct; the tests genuinely pin the boundary cases against the real library semantics (all verified against `src/config/mod.rs`, `src/config/endpoints.rs`, `src/config/general.rs`, `src/memory/embedder.rs`). The deliberate lock-read restructure at the re-embed gate is behavior-identical in all reachable states (rationale below). Two low-severity findings, neither a defect in the shipped behavior.

### Findings

**LOW 1 — re-embed status test misses two `EmbedderStatus` variants.** `src-tauri/src/startup.rs:317-333` (`reembed_skips_when_status_not_ready`) loops over `Checking`, `Failed`, `Fallback` but not `Pulling` or `Downloading { .. }` (`src/memory/embedder.rs:101-112` — six variants total, five non-`Ready`). The helper's `*status == Ready` equality handles them identically, so this is coverage strength, not correctness. Fix: add `EmbedderStatus::Pulling` and `EmbedderStatus::Downloading { model: "m".into(), progress: 0.5 }` to the loop's variant list (one line).

**LOW 2 — the endpoint/model fallback chain now exists in three places.** The new tested helper (`src-tauri/src/startup.rs:48-62`) duplicates `initial_selection` (`src-tauri/src/console.rs:527-535`, self-documented as "an exact mirror of `build_brain`'s startup provider construction (main.rs)") and the provider block in `src-tauri/src/ipc/memory_maintenance.rs:252-268` (self-documented as "same resolution as the startup build in main.rs"). This duplication is **pre-existing and outside this plan's scope** (the finding named main.rs only) — the change neither introduced nor worsened it. Recommended resolution: file a backlog follow-up to consolidate the two mirror sites onto `resolve_startup_provider` (both have their own test suites: console.rs:2151-2222 etc.), rather than expanding this delta. Deferring to the backlog is a reasonable resolution; silently leaving the mirrors undocumented is not.

### Verification detail (all eight requested checks)

**1. Pure code motion — VERIFIED.** Each helper was compared against the removed inline code in the diff:
- `console_mode_requested` (startup.rs:21-23): predicate `a == "-console" || a == "--console"` identical to the removed `std::env::args().any(...)` closure; `std::env::args()` satisfies the `IntoIterator<Item = String>` bound.
- `project_flag_from_args` (startup.rs:28-37): the `collect::<Vec<_>>().windows(2).find(|w| w[0] == "--project").and_then(|w| w.get(1)).map(PathBuf::from)` chain is byte-identical.
- `resolve_startup_provider` (startup.rs:48-62): the fallback ordering is identical — `default_provider`-named endpoint → `config.default_endpoint()`; model = `default_model` → endpoint's first `model_ids()` → `"gpt-4o"`. Verified against the real semantics: `Config::endpoint` (src/config/mod.rs:87-89, find-by-name), `default_endpoint` (src/config/mod.rs:120-127, named → **first** endpoint), `model_ids` (src/config/endpoints.rs:303).
- `should_reembed_at_startup` (startup.rs:78-91): `configured.is_some() && !configured.is_some_and(|v| v.eq_ignore_ascii_case(EMBEDDING_MODEL_SENTINEL_HASH))` is logically identical to the original `configured_bundled.is_some() && !is_hash_opt_out` (short-circuit preserved); sentinel is `"hash"` (src/config/general.rs:234, re-exported at src/config/mod.rs:29); the call site passes `pending_download.is_some()` / `pending_load.is_some()` and the helper negates — matching the original `.is_none()` gates.
- `window_title` (startup.rs:98-107): `NeedsProject` → `"Mnemo"`, else `"Mnemo — {folder}"` with `file_name() → to_str() → unwrap_or(".")` — identical, em dash (U+2014) preserved.

**2. Call-site rewiring — VERIFIED.** All five sites confirmed in the current main.rs: console check (main.rs:140, still the first statement in `main()`), window title (main.rs:293-297 — `matches!(&brain_result, Ok(BrainOutcome::NeedsProject(_)))` is equivalent to the removed match arm, still inside `if let Some(window)`), `--project` parse (main.rs:993, same else-if position in the project-resolution chain), provider resolution (main.rs:1037 — `Option<&Endpoint>` is `Copy`, so the subsequent `if let Some(ep) = endpoint` consumption is unchanged; the dummy-provider else-branch still uses `model.clone()`), re-embed gate (main.rs:1339-1355). A crate-wide search confirms the moved logic now exists **only** in startup.rs (helper + tests) — no stale duplicates left in main.rs.

**3. Lock-read restructure — AGREED behavior-identical.** The `embedder_status` read moved before the cheap gates. Verified the lock's write history up to that point: created at main.rs:1081 (initial `Checking`), written synchronously on the main thread at main.rs:1108-1111 and 1127-1130, and by `build_embedder` (src/provider/client_factory.rs:283-300) which also writes `Ready`/`Failed` synchronously before returning. The background download/load tasks are spawned later (setup hook, after `build_brain_inner` returns), so the unconditional read at main.rs:1340 is uncontended in every reachable startup state. The only theoretical deltas — panicking on a poisoned lock when the cheap gates fail, or blocking on a held write lock — are unreachable here, and the original code performed the identical read whenever the gates passed. No deadlock risk (single lock, no nesting).

**4. Tests genuinely pin the boundaries — VERIFIED.** Every assertion was checked against real library behavior, not just internal consistency: `Config::default()` has an empty `endpoints` vec (derive `Default`, src/config/mod.rs:41-46) and `GeneralConfig::default()` has `default_provider`/`default_model` = `None` (src/config/general.rs:217-218), so `provider_no_endpoints_is_none_with_gpt4o_terminal` is a valid pin; the dangling-default test matches `default_endpoint()`'s named→first semantics; first-occurrence-wins matches `windows(2).find()` order; the equals-form (`--project=x`) correctly yields `None` (never matches `w[0] == "--project"`). No tautological or wrong-thing assertions found — each test fails if its helper regresses.

**5. Cross-platform tests + fixtures — VERIFIED.** Plain `#[cfg(test)] mod tests` (startup.rs:109) — not the unix-only cfg of the path-adoption tests. Forward-slash paths are valid `Path` input on both platforms; `Path::new("/").file_name()` is `None` on both, so the `"Mnemo — ."` assertion holds on Windows and macOS. `Endpoint::test_default()` is `#[cfg(any(test, feature = "test-support"))]` (src/config/endpoints.rs:259) and src-tauri's dev-dependencies enable mnemo's `test-support` feature (src-tauri/Cargo.toml:40) — the struct-update fixture usage is the sanctioned pattern; `ModelSpec` implements `Default` (src/config/endpoints.rs:225-235).

**6. mod registration + imports — VERIFIED.** `mod startup;` at main.rs:34 (alphabetical in the mod block). The crate is `#![deny(warnings)]`, so the parent's recorded green suites (src-tauri 216+4, root 2004+16) prove no unused imports survived (e.g., `BrainOutcome` is still live at the `matches!` site). Note: as a read-only reviewer I could not re-run `cargo test` myself; this is static verification plus the parent's plan step-2 record.

**7. Multi-platform neutrality — VERIFIED.** startup.rs contains zero `cfg` attributes, no platform APIs, paths, or shell syntax; the helpers are pure functions over argv/config/state values. The doc comment's Windows context (WebView2, GUI-subsystem, NULL std handles) is inherited explanatory prose mirroring the existing main.rs comment — documentation, not code, and not a constitution violation.

**8. Documentation sync — no action required.** The new module carries its own doc comment; main.rs's module doc (lines 5-8, "wires the brain … to the Tauri IPC bridge") still accurately describes its wiring role. README.md has no main.rs-internals content. PLAN.md:711's src-tauri tree is a historical phase-A scaffold diagram that already omits `console.rs`, `watchdog.rs`, and the current `ipc/` file set — this change does not make it materially staler, and updating a historical diagram is out of scope. No `endpoints.toml`/config examples are affected (no behavior change).

**Security:** no new I/O, no `unsafe`, no secrets, no new attack surface — the helpers are pure decisions over already-validated inputs, moved verbatim.
